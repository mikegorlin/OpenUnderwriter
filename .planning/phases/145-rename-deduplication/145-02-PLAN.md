---
phase: 145-rename-deduplication
plan: 02
type: execute
wave: 2
depends_on: [145-01]
files_modified:
  - src/do_uw/templates/html/sections/key_stats.html.j2
  - src/do_uw/templates/html/sections/executive/company_profile.html.j2
  - src/do_uw/templates/html/sections/identity.html.j2
  - src/do_uw/templates/html/sections/company.html.j2
  - src/do_uw/templates/html/sections/report/page0_dashboard.html.j2
  - src/do_uw/templates/html/sections/executive/risk_classification.html.j2
  - tests/stages/render/test_dedup_metrics.py
autonomous: true
requirements: [DEDUP-01, DEDUP-02, DEDUP-03, DEDUP-04]

must_haves:
  truths:
    - "Revenue with full provenance appears only in the Financial section and the header bar"
    - "Market cap appears only in the Decision Dashboard (Page-0) and the header bar"
    - "Stock price appears only in Stock & Market section and the header bar"
    - "Board size appears only in the Governance section"
    - "Header bar shows MCap, Revenue, Price, Employees as compact reference"
  artifacts:
    - path: "tests/stages/render/test_dedup_metrics.py"
      provides: "Automated dedup verification test"
      min_lines: 30
  key_links:
    - from: "src/do_uw/templates/html/sections/uw_analysis.html.j2"
      to: "header bar"
      via: "MCap/Revenue/Price/Employees loop at line 40"
      pattern: "MCap.*Revenue.*Price.*Employees"
---

<objective>
Remove redundant headline metric displays from non-home sections per D-05/D-06/D-07. Each metric has one "home section" with full provenance — the header bar is the only allowed cross-section duplicate. Analytical uses of metrics (revenue growth %, revenue model, segments) are NOT touched.

Purpose: Each fact appears once with full context. Underwriters see the metric where it matters most, not scattered across every section.
Output: Cleaner templates with dedup enforcement test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-rename-deduplication/145-CONTEXT.md
@.planning/phases/145-rename-deduplication/145-RESEARCH.md
@.planning/phases/145-rename-deduplication/145-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove redundant headline metrics from non-home templates</name>
  <files>
    src/do_uw/templates/html/sections/key_stats.html.j2,
    src/do_uw/templates/html/sections/executive/company_profile.html.j2,
    src/do_uw/templates/html/sections/identity.html.j2,
    src/do_uw/templates/html/sections/company.html.j2,
    src/do_uw/templates/html/sections/report/page0_dashboard.html.j2,
    src/do_uw/templates/html/sections/executive/risk_classification.html.j2
  </files>
  <read_first>
    src/do_uw/templates/html/sections/uw_analysis.html.j2,
    src/do_uw/templates/html/sections/key_stats.html.j2,
    src/do_uw/templates/html/sections/executive/company_profile.html.j2,
    src/do_uw/templates/html/sections/identity.html.j2,
    src/do_uw/templates/html/sections/company.html.j2,
    src/do_uw/templates/html/sections/report/page0_dashboard.html.j2,
    src/do_uw/templates/html/sections/executive/risk_classification.html.j2,
    src/do_uw/templates/html/sections/report/stock_market.html.j2,
    src/do_uw/templates/html/sections/report/page0_dashboard.html.j2
  </read_first>
  <action>
    Apply the home section assignments from D-05 and D-06. The dedup targets are HEADLINE METRIC VALUES only (the dollar amount / number itself). Analytical uses of metrics (revenue growth %, revenue model, revenue segments, EV/Revenue ratios, revenue concentration) are NOT dedup targets and MUST stay.

    **Home section assignments (per D-05, DEDUP-02):**
    - Revenue ($X.XB headline value): HOME = Financial section. ALSO allowed in header bar (line 40 of uw_analysis.html.j2).
    - Market Cap: HOME = Decision Dashboard (page0_dashboard.html.j2). ALSO allowed in header bar.
    - Stock Price ($X.XX headline value): HOME = Stock & Market (stock_market.html.j2). ALSO allowed in header bar.
    - Board Size: HOME = Governance section. NOT in header bar (header has MCap/Revenue/Price/Employees per D-06).
    - Employees: HOME = header bar (persistent reference per D-06). If it appears in identity.html.j2 or company_profile.html.j2 as a standalone value, keep it — employees is a basic identity metric and these sections serve different purposes than the Financial/Market sections.

    **Specific removals to make:**

    1. **key_stats.html.j2** — Lines ~143-170: Remove the stock price panel (stock price, 52-week range visualization). Stock price home = Stock & Market. Leave all other key_stats panels (they show section-specific content like revenue model, scoring).

    2. **company_profile.html.j2** — Find and remove any standalone market_cap display (the "$X.XB" number as a featured metric). Keep any analytical context that uses market cap (e.g., "large-cap company" classification text).

    3. **identity.html.j2** — Find and remove any standalone market_cap value display. Keep company identity fields (name, ticker, SIC, etc.).

    4. **company.html.j2** — Find and remove any standalone market_cap value display.

    5. **risk_classification.html.j2** — Find and remove any standalone stock_price or market_cap headline value. Keep the risk classification categories and tier labels (these use market cap as a classification input, which is analytical, not a headline display).

    6. **page0_dashboard.html.j2** — This IS the Decision Dashboard (home for Market Cap per D-05). Keep market cap here. Revenue card: The research notes this card shows Revenue & Growth with sparkline and EV/Revenue. This is analytical content (sparkline, EV/Revenue ratio) beyond the headline number. KEEP the revenue card on Page-0 — it provides growth context that the Financial section headline does not duplicate. Only remove standalone stock_price headline values if present as a featured metric (not as part of stock combo chart context).

    **CRITICAL DISTINCTION — what to remove vs keep:**
    - REMOVE: `<span class="metric-value">${{ "%.2f" | format(stock_price) }}</span>` (headline display)
    - KEEP: `Revenue grew {{ growth_pct }}% YoY` (analytical use)
    - KEEP: `EV/Revenue: {{ ev_revenue }}x` (ratio using revenue as input)
    - KEEP: Revenue sparkline/trend chart (analytical visualization)
    - KEEP: `{{ market_cap_tier }}` classification label (uses market cap as input, doesn't display the number)

    **When removing a panel/block:** Remove the entire HTML block cleanly. Do not leave empty `<div>` containers or orphaned CSS classes. If a grid layout had N items and you remove one, check if the remaining items still lay out correctly.

    Per D-07: No "See Financial section" cross-references. Just remove.
  </action>
  <verify>
    <automated>uv run pytest tests/stages/render/ -x -q --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - `key_stats.html.j2` does NOT contain `stock_price` or `ks.stock_price` or `ks_stock_current` as a headline metric panel (grep returns 0 for stock price panel block)
    - `company_profile.html.j2` does NOT contain a standalone `market_cap` value display (analytical classifications OK)
    - `identity.html.j2` does NOT contain a standalone `market_cap` value display
    - `uw_analysis.html.j2` header bar STILL contains `("MCap", b.market_cap), ("Revenue", b.revenue), ("Price", b.stock_price), ("Employees", b.employees)`
    - `page0_dashboard.html.j2` STILL contains market_cap display (it's the home section)
    - `report/stock_market.html.j2` is UNCHANGED (it's the stock price home section)
    - No empty `<div>` containers left where removed panels were
    - `uv run pytest tests/stages/render/ -x -q --timeout=30` exits 0
  </acceptance_criteria>
  <done>
    Headline metric values appear only in their home section + header bar. Analytical uses of all metrics are preserved. No orphaned HTML. Render tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create dedup verification test</name>
  <files>tests/stages/render/test_dedup_metrics.py</files>
  <read_first>
    src/do_uw/templates/html/sections/uw_analysis.html.j2,
    tests/stages/render/test_reading_paths.py
  </read_first>
  <action>
    Create `tests/stages/render/test_dedup_metrics.py` that verifies the dedup home section rules are enforced. The test should:

    1. **Test: header bar contains exactly MCap, Revenue, Price, Employees** (DEDUP-03).
       Read `src/do_uw/templates/html/sections/uw_analysis.html.j2`, find the header bar stats block (the `{% for label, val in [("MCap"...` loop), verify it contains exactly these 4 metrics in this order: MCap, Revenue, Price, Employees.

    2. **Test: key_stats.html.j2 has no stock_price headline panel** (DEDUP-04).
       Read `src/do_uw/templates/html/sections/key_stats.html.j2`, assert `ks.stock_price` or `stock_price` does NOT appear as a featured panel metric value (grep for stock price display patterns like `ks.stock_price`, `ks_stock_current`, `format(ks.stock_price)`).

    3. **Test: company_profile.html.j2 has no standalone market_cap value** (DEDUP-04).
       Read the template file, assert no `market_cap` formatted as a standalone headline value.

    4. **Test: stock_market.html.j2 IS the stock price home** (DEDUP-02).
       Read the template, verify `stock_price` appears (confirming the home section has it).

    5. **Test: page0_dashboard.html.j2 IS the market cap home** (DEDUP-02).
       Read the template, verify `market_cap` appears.

    Use simple file reads and string assertions — no need to render templates. Pattern:
    ```python
    from pathlib import Path

    TEMPLATES = Path("src/do_uw/templates/html/sections")

    def test_header_bar_has_four_metrics():
        content = (TEMPLATES / "uw_analysis.html.j2").read_text()
        # Find the header stats for-loop
        assert '("MCap"' in content
        assert '("Revenue"' in content
        assert '("Price"' in content
        assert '("Employees"' in content

    def test_key_stats_no_stock_price_panel():
        content = (TEMPLATES / "key_stats.html.j2").read_text()
        assert "ks.stock_price" not in content
        assert "ks_stock_current" not in content

    def test_stock_market_is_price_home():
        content = (TEMPLATES / "report" / "stock_market.html.j2").read_text()
        assert "stock_price" in content

    def test_page0_dashboard_is_mcap_home():
        content = (TEMPLATES / "report" / "page0_dashboard.html.j2").read_text()
        assert "market_cap" in content
    ```
  </action>
  <verify>
    <automated>uv run pytest tests/stages/render/test_dedup_metrics.py -x -q --timeout=10</automated>
  </verify>
  <acceptance_criteria>
    - `tests/stages/render/test_dedup_metrics.py` exists with at least 4 test functions
    - Test file contains `def test_header_bar_has_four_metrics`
    - Test file contains `def test_key_stats_no_stock_price_panel`
    - Test file contains assertions for `uw_analysis.html.j2` (not `beta_report.html.j2`)
    - `uv run pytest tests/stages/render/test_dedup_metrics.py -x -q --timeout=10` exits 0
  </acceptance_criteria>
  <done>
    Automated dedup test enforces home section rules. Test passes against the deduped templates. Future changes that re-introduce redundant metrics will be caught by CI.
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/stages/render/test_dedup_metrics.py -v` — all dedup rules verified
2. `uv run pytest tests/stages/render/ -x -q --timeout=60` — full render test suite passes
3. Manual spot check: key_stats section no longer shows stock price panel
4. Header bar still shows MCap/Revenue/Price/Employees (verified by test)
</verification>

<success_criteria>
- DEDUP-01: Home sections defined and enforced by test (Revenue=Financial, MCap=Dashboard, Price=Stock&Market, Board=Governance)
- DEDUP-02: Each metric's home section verified to contain the metric by automated test
- DEDUP-03: Header bar contains exactly MCap/Revenue/Price/Employees (verified by test)
- DEDUP-04: Non-home sections no longer display headline metric values (verified by test + grep)
</success_criteria>

<output>
After completion, create `.planning/phases/145-rename-deduplication/145-02-SUMMARY.md`
</output>
