---
phase: 116-d-o-commentary-layer
plan: 02
type: execute
wave: 2
depends_on: [116-01]
files_modified:
  - src/do_uw/stages/render/sections/sect3_audit.py
  - src/do_uw/stages/render/sections/sect4_market_events.py
  - src/do_uw/stages/render/sections/sect5_governance.py
  - src/do_uw/stages/render/sections/sect6_litigation.py
  - src/do_uw/stages/render/sections/sect7_scoring_detail.py
  - src/do_uw/templates/html/sections/financial/distress_indicators.html.j2
  - tests/render/test_do_context_migration.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "D&O commentary for all 5 migrated sections is present and traceable to signal do_context -- zero Python functions generate commentary"
    - "The Jinja2 inline D&O conditionals in distress_indicators.html.j2 are replaced by {{ do_context }} pass-through variables"
    - "Rendered output for migrated sections is identical or equivalent to pre-migration output"
  artifacts:
    - path: "src/do_uw/stages/render/sections/sect3_audit.py"
      provides: "Audit section consuming signal do_context instead of _add_audit_do_context()"
      contains: "safe_get_result"
    - path: "tests/render/test_do_context_migration.py"
      provides: "Golden parity tests for migration"
      min_lines: 80
  key_links:
    - from: "src/do_uw/stages/render/sections/sect3_audit.py"
      to: "src/do_uw/stages/render/context_builders/_signal_fallback.py"
      via: "safe_get_result(signal_results, SIGNAL_ID).do_context"
      pattern: "safe_get_result.*do_context"
    - from: "src/do_uw/templates/html/sections/financial/distress_indicators.html.j2"
      to: "context dict"
      via: "{{ z_do_context }} instead of {% if zone == 'Distress' %}"
      pattern: "do_context"
---

<objective>
Migrate all 5 hardcoded Python D&O commentary functions and 1 Jinja2 template to consume signal do_context from brain YAML. Delete the Python functions after migration. Establish golden parity tests to prove output equivalence.

Purpose: Eliminate all hardcoded D&O commentary from Python/Jinja2, completing the move to brain-YAML-driven commentary. This is a prerequisite for the CI gate promotion in Plan 05.
Output: 5 deleted Python functions, 1 cleaned Jinja2 template, golden parity tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-d-o-commentary-layer/116-CONTEXT.md
@.planning/phases/116-d-o-commentary-layer/116-RESEARCH.md

<interfaces>
From src/do_uw/stages/render/context_builders/_signal_fallback.py:
```python
def safe_get_result(signal_results: dict[str, Any] | None, signal_id: str) -> SignalResultView | SignalUnavailable:
    """Get typed view or SignalUnavailable sentinel. Never raises."""

# Consumer pattern:
sig = safe_get_result(signal_results, "FIN.ACCT.material_weakness")
do_ctx = sig.do_context if sig and sig.do_context else ""
```

From src/do_uw/stages/render/context_builders/_signal_consumer.py:
```python
@dataclass(frozen=True)
class SignalResultView:
    signal_id: str
    status: str
    value: str | float | None
    threshold_level: str
    evidence: str
    do_context: str  # Pre-rendered D&O commentary from brain YAML
    # ... other fields
```

Migration targets (from RESEARCH.md):
| File | Function | Signal Replacement |
|------|----------|-------------------|
| sect3_audit.py | _add_audit_do_context() | FIN.ACCT.* audit signals |
| sect4_market_events.py | _departure_do_context() | EXEC.PROFILE.departure / GOV.* departure signals |
| sect5_governance.py | _add_leadership_do_context() | EXEC.PROFILE.prior_litigation signals |
| sect6_litigation.py | _add_sca_do_context() | LIT.SCA.* signals |
| sect7_scoring_detail.py | _add_pattern_do_context() | Pattern signal do_context |
| distress_indicators.html.j2 | {% if zone == ... %} | {{ z_do_context }}, {{ beneish_do_context }}, etc. |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create golden parity snapshots and migrate sect3-5 Python functions</name>
  <files>
    tests/render/test_do_context_migration.py,
    src/do_uw/stages/render/sections/sect3_audit.py,
    src/do_uw/stages/render/sections/sect4_market_events.py,
    src/do_uw/stages/render/sections/sect5_governance.py
  </files>
  <read_first>
    - src/do_uw/stages/render/sections/sect3_audit.py (find _add_audit_do_context function, understand all conditions it handles: material weaknesses, restatements, going concern, CAMs, tenure)
    - src/do_uw/stages/render/sections/sect4_market_events.py (find _departure_do_context function, understand departure type to D&O string mapping)
    - src/do_uw/stages/render/sections/sect5_governance.py (find _add_leadership_do_context function, understand prior litigation D&O context per executive)
    - src/do_uw/stages/render/context_builders/_signal_fallback.py (safe_get_result pattern)
    - src/do_uw/stages/render/context_builders/financials_evaluative.py (reference consumer pattern from Phase 115)
    - tests/test_do_context_golden.py (Phase 115 golden test pattern)
  </read_first>
  <action>
**Step 1: Create golden parity test file** `tests/render/test_do_context_migration.py`:
- Import the 5 target functions BEFORE they're deleted (capture their output for comparison)
- For each function, create a test that:
  1. Builds mock input data (audit findings, departure records, leadership profiles, SCA cases, patterns)
  2. Calls the OLD function to get baseline output strings
  3. Calls the NEW signal-do_context-based approach to get replacement output
  4. Asserts the replacement output conveys equivalent D&O intelligence (not necessarily identical strings, but same risk assessment intent)
- Tests: `test_audit_do_context_parity`, `test_departure_do_context_parity`, `test_governance_do_context_parity`, `test_sca_do_context_parity`, `test_pattern_do_context_parity`

**Step 2: Migrate sect3_audit.py**:
- Delete `_add_audit_do_context()` function (lines ~189-253)
- Replace callers with signal do_context consumption:
  - Material weakness: `safe_get_result(signal_results, "FIN.ACCT.material_weakness").do_context`
  - Restatement: `safe_get_result(signal_results, "FIN.ACCT.restatement_risk").do_context`
  - Going concern: `safe_get_result(signal_results, "FIN.ACCT.going_concern").do_context`
  - CAMs: `safe_get_result(signal_results, "FIN.ACCT.audit_cam_risk").do_context` (or closest matching signal)
  - Auditor tenure: `safe_get_result(signal_results, "FIN.ACCT.auditor_tenure").do_context`
- Add `from do_uw.stages.render.context_builders._signal_fallback import safe_get_result` import
- The function that calls _add_audit_do_context must accept `signal_results` dict parameter

**Step 3: Migrate sect4_market_events.py**:
- Delete `_departure_do_context()` function (lines ~409-415)
- This is a simple 3-line mapping of departure type to D&O string
- Replace with: departure-related signal do_context (likely GOV.EVENT.executive_departure or similar)
- If no exact signal match exists, the do_context from the nearest matching signal should cover the D&O interpretation

**Step 4: Migrate sect5_governance.py**:
- Delete `_add_leadership_do_context()` function (lines ~419-436)
- Replace with signal do_context from EXEC.PROFILE.prior_litigation or GOV.* governance signals
- Each executive's D&O context should come from signal evaluation, not hardcoded text

Ensure all 3 files still pass existing tests: `uv run pytest tests/render/ -k "sect3 or sect4 or sect5" -x`
  </action>
  <verify>
    <automated>uv run pytest tests/render/test_do_context_migration.py -x -q && uv run pytest tests/render/ -k "sect3 or sect4 or sect5 or audit or market_event or governance" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - sect3_audit.py does NOT contain `def _add_audit_do_context`
    - sect3_audit.py contains `safe_get_result` import and usage
    - sect4_market_events.py does NOT contain `def _departure_do_context`
    - sect5_governance.py does NOT contain `def _add_leadership_do_context`
    - tests/render/test_do_context_migration.py contains `def test_audit_do_context_parity`
    - tests/render/test_do_context_migration.py contains `def test_departure_do_context_parity`
    - tests/render/test_do_context_migration.py contains `def test_governance_do_context_parity`
    - uv run pytest tests/render/ -k "sect3 or sect4 or sect5" exits 0
  </acceptance_criteria>
  <done>3 Python D&O commentary functions deleted and replaced with signal do_context consumption; golden parity tests confirm equivalent output</done>
</task>

<task type="auto">
  <name>Task 2: Migrate sect6-7 Python functions and Jinja2 template</name>
  <files>
    src/do_uw/stages/render/sections/sect6_litigation.py,
    src/do_uw/stages/render/sections/sect7_scoring_detail.py,
    src/do_uw/templates/html/sections/financial/distress_indicators.html.j2,
    tests/render/test_do_context_migration.py
  </files>
  <read_first>
    - src/do_uw/stages/render/sections/sect6_litigation.py (find _add_sca_do_context function, lines ~259-323, understand: active SCA count, settlement history, lead counsel tier, coverage implications)
    - src/do_uw/stages/render/sections/sect7_scoring_detail.py (find _add_pattern_do_context function, lines ~182-197, understand: HIGH/SEVERE pattern D&O text, allegation mapping)
    - src/do_uw/templates/html/sections/financial/distress_indicators.html.j2 (find {% if zone == ... %} D&O conditionals)
    - src/do_uw/stages/render/context_builders/financials_evaluative.py (see how z_do_context, beneish_do_context are already passed)
    - tests/render/test_do_context_migration.py (extend with remaining parity tests from Task 1)
  </read_first>
  <action>
**Step 1: Migrate sect6_litigation.py**:
- Delete `_add_sca_do_context()` function (lines ~259-323)
- This function handles: active SCA count + D&O implications, settlement history analysis, lead counsel tier assessment, coverage trigger conditions
- Replace with LIT.SCA.* signal do_context consumption:
  - Active SCA: `safe_get_result(signal_results, "LIT.SCA.active_count").do_context` or `LIT.SCA.sca_exposure`
  - Settlement: `safe_get_result(signal_results, "LIT.SCA.settlement_history").do_context`
  - Lead counsel: `safe_get_result(signal_results, "LIT.SCA.lead_counsel_tier").do_context`
- Find the exact signal IDs by reading the brain YAML files in `src/do_uw/brain/signals/lit/`
- Add `from do_uw.stages.render.context_builders._signal_fallback import safe_get_result` import

**Step 2: Migrate sect7_scoring_detail.py**:
- Delete `_add_pattern_do_context()` function (lines ~182-197)
- This function handles HIGH/SEVERE pattern severity text and allegation mapping D&O text
- Replace with pattern signal do_context from the relevant composite pattern signals
- The `signal_contributions` field on FactorScore already links to signal IDs -- use those to retrieve do_context

**Step 3: Migrate distress_indicators.html.j2**:
- Find all `{% if zone == 'Distress' %}` or `{% if zone == ... %}` conditional blocks that contain D&O evaluative text
- Replace with `{{ z_do_context }}`, `{{ beneish_do_context }}`, `{{ piotroski_do_context }}`, `{{ ohlson_do_context }}` variables
- These variables are ALREADY passed by `financials_evaluative.py` (Phase 115 established this)
- Delete all inline D&O evaluative text strings from the template
- Keep structural HTML (containers, headers, formatting) -- only remove the evaluative content

**Step 4: Add remaining parity tests** to `tests/render/test_do_context_migration.py`:
- `test_sca_do_context_parity`: verify LIT.SCA signal do_context covers the same D&O analysis as _add_sca_do_context()
- `test_pattern_do_context_parity`: verify pattern signal do_context covers HIGH/SEVERE pattern D&O text
- `test_distress_template_no_inline_do_context`: grep distress_indicators.html.j2 for D&O patterns, assert zero matches

Run full test suite: `uv run pytest tests/render/ -k "sect6 or sect7 or litigation or scoring" -x`
  </action>
  <verify>
    <automated>uv run pytest tests/render/test_do_context_migration.py -x -q && uv run pytest tests/render/ -k "sect6 or sect7 or litigation or scoring_detail" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - sect6_litigation.py does NOT contain `def _add_sca_do_context`
    - sect6_litigation.py contains `safe_get_result` import and usage
    - sect7_scoring_detail.py does NOT contain `def _add_pattern_do_context`
    - distress_indicators.html.j2 does NOT contain `{% if zone ==` with D&O evaluative text after it
    - distress_indicators.html.j2 contains `{{ z_do_context }}` or `{{ beneish_do_context }}`
    - tests/render/test_do_context_migration.py contains `def test_sca_do_context_parity`
    - tests/render/test_do_context_migration.py contains `def test_distress_template_no_inline_do_context`
    - uv run pytest tests/render/ -k "sect6 or sect7" exits 0
  </acceptance_criteria>
  <done>All 5 Python D&O commentary functions deleted, Jinja2 template cleaned, all rendered output uses signal do_context exclusively</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/render/test_do_context_migration.py -x -v` -- all 7 parity tests pass
- `grep -rn "def _add_audit_do_context\|def _departure_do_context\|def _add_leadership_do_context\|def _add_sca_do_context\|def _add_pattern_do_context" src/do_uw/stages/render/sections/` -- zero matches (all functions deleted)
- `uv run pytest tests/brain/test_do_context_ci_gate.py -x -v` -- CI gate still passes (WARN files still expected at this point)
- `uv run pytest tests/render/ -x -q` -- full render test suite passes
</verification>

<success_criteria>
All 5 hardcoded Python D&O commentary functions are deleted. Jinja2 template uses do_context pass-through variables. Golden parity tests confirm equivalent D&O intelligence in rendered output. Full render test suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/116-d-o-commentary-layer/116-02-SUMMARY.md`
</output>
