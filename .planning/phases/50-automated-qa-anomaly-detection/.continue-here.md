---
phase: 50-automated-qa-anomaly-detection
task: wave-2
total_tasks: 4 plans (3 waves)
status: in_progress
last_updated: 2026-02-27T03:15:53.111Z
---

<current_state>
Executing Phase 50: Automated QA & Anomaly Detection.
Wave 1 complete (Plans 50-01 and 50-02). Wave 2 (Plan 50-04) and Wave 3 (Plan 50-03) not started.

Post-Wave-1 fix applied: brain_health.py and brain_audit.py were reading signal definitions from DuckDB (a copy). Fixed to read from YAML (source of truth). DuckDB only used for historical run data.
</current_state>

<completed_work>

- Plan 50-01: Signal result enrichment + post-analyze health summary - DONE
  - Added `details: dict[str, Any]` field to SignalResult
  - Created `signal_details.py` for post-evaluation enrichment (stock drops, insider, litigation, financial)
  - Created `health_summary.py` with anomaly detection heuristics
  - Wired into cli.py post-pipeline hook
  - Commits: be76044, 4583e2e, 52aad4f

- Plan 50-02: Brain health + brain audit CLI commands - DONE
  - Created `brain_health.py` with BrainHealthReport (coverage, fire rates, freshness, feedback queue)
  - Created `brain_audit.py` with BrainAuditReport (staleness, peril coverage, threshold conflicts, orphans)
  - Created `cli_brain_health.py` with Rich CLI for `do-uw brain health` and `do-uw brain audit`
  - Commits: 1809c3a, 96bec5c, 33df1e0

- Post-execution fix: YAML-first architecture correction - DONE
  - brain_health.py: signal counts from YAML, DuckDB only for run history/effectiveness/feedback
  - brain_audit.py: staleness/thresholds/orphans from YAML, DuckDB only for peril coverage matrix
  - Commit: d56fcfa
</completed_work>

<remaining_work>

- Plan 50-04 (Wave 2, depends on 50-01): Signal Composites foundation
  - Create brain_composite_schema.py (CompositeDefinition, CompositeResult models)
  - Create brain_composite_engine.py (evaluate_composites() engine)
  - Create 3 composite YAMLs: stock_drop_analysis, stock_short_analysis, stock_insider_analysis
  - Evolve FacetSpec to reference composites via `content` list
  - Wire evaluate_composites() into stages/analyze/__init__.py
  - Create tests/brain/test_brain_composites.py

- Plan 50-03 (Wave 3, depends on 50-01 AND 50-02): Brain delta command
  - Create brain_delta.py with compute_delta() -> DeltaReport
  - Add `do-uw brain delta <TICKER>` CLI command to cli_brain_health.py
  - Create tests/brain/test_brain_delta.py

- After all plans: Phase verification (gsd-verifier), roadmap update
</remaining_work>

<decisions_made>

- THREE-LAYER ARCHITECTURE (critical): Signals (atomic eval) -> Composites (brain analysis) -> Facets (display). These must NOT be conflated. Composites are brain-level analytical grouping. Facets are purely display/presentation.
- YAML IS SOURCE OF TRUTH for signal definitions. DuckDB only for historical runtime data (run results, effectiveness, feedback). Fixed brain_health.py and brain_audit.py accordingly.
- Signal Composites: new concept. Declared groups of related signals that evaluate together after individual signals fire. Produce CompositeResult with structured conclusions. Live in brain/composites/*.yaml.
- Facets reference composites via `content` list (ref: COMP.xxx, render_as: narrative_with_table). Existing `signals` list preserved for backward compat.
- QA health summary is CLI output only (not in HTML worksheet).
- All sections get same standard of rendering quality (not just stock).
- Stock drops: full narrative per event + grouped by pattern type. Both per-event detail AND grouped-by-pattern view.
- brain.duckdb removed from git history (370MB, exceeded GitHub limit). Added to .gitignore.
</decisions_made>

<blockers>
None. Ready to continue with Wave 2.
</blockers>

<context>
The user has a strong vision for systematic signal hydration and rendering. The key insight from discussion: grouping signals together is a BRAIN function first (analysis), then a display function. Facets were being conflated with analytical grouping. Signal Composites were introduced as the missing middle layer.

The execute-phase workflow should continue: spawn Plan 50-04 (Wave 2), then Plan 50-03 (Wave 3), then verify phase goal, then update roadmap.

IMPORTANT: When spawning Plan 50-04 executor, make sure it understands the YAML-first principle. The composite engine should load composite definitions from YAML, not DuckDB.
</context>

<next_action>
Resume with `/gsd:resume-work`. It will:
1. Continue execute-phase 50 from Wave 2
2. Spawn Plan 50-04 executor (Signal Composites foundation)
3. After Wave 2, spawn Plan 50-03 executor (brain delta)
4. Run phase verification
5. Update roadmap
</next_action>
