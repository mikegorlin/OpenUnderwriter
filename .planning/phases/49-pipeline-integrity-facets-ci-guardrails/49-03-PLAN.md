---
phase: 49-pipeline-integrity-facets-ci-guardrails
plan: 03
type: execute
wave: 2
depends_on: [49-01]
files_modified:
  - src/do_uw/stages/analyze/signal_mappers_sections.py
  - src/do_uw/stages/analyze/signal_field_routing.py
  - src/do_uw/stages/extract/def14a.py
  - src/do_uw/brain/signals/gov/*.yaml
  - src/do_uw/brain/brain_build_signals.py
autonomous: true
requirements: [INT-01]

must_haves:
  truths:
    - "SKIPPED count for AAPL drops from ~68 toward ~34 (at least 20 fewer SKIPPEDs)"
    - "Population B DEF14A signals that have viable extraction paths now EVALUATE instead of SKIP"
    - "Signals with no reliable extraction path are marked INACTIVE in YAML (not counted as SKIPPED)"
    - "No regression in existing TRIGGERED signals (AAPL triggered count stays >= 24)"
    - "brain build succeeds with updated YAML"
  artifacts:
    - path: "src/do_uw/stages/analyze/signal_mappers_sections.py"
      provides: "map_governance_fields() with expanded field mapping for DEF14A signals"
    - path: "src/do_uw/stages/analyze/signal_field_routing.py"
      provides: "FIELD_FOR_SIGNAL expanded with new DEF14A signal field_key mappings"
    - path: "src/do_uw/brain/signals/gov/*.yaml"
      provides: "Updated GOV signals -- INACTIVE lifecycle_state for no-path signals"
  key_links:
    - from: "src/do_uw/stages/analyze/signal_mappers_sections.py"
      to: "src/do_uw/stages/analyze/signal_field_routing.py"
      via: "map_governance_fields populates fields consumed by FIELD_FOR_SIGNAL routing"
      pattern: "board_attendance|board_diversity|clawback_policy"
    - from: "src/do_uw/stages/extract/def14a.py"
      to: "src/do_uw/stages/analyze/signal_mappers_sections.py"
      via: "DEF14AExtraction fields mapped through GovernanceData to evaluator"
      pattern: "DEF14AExtraction"
---

<objective>
Fix Population B DEF14A signals so they evaluate instead of SKIP, and mark no-path signals as INACTIVE.

Purpose: Reduce SKIPPED count from ~68 toward ~34 by wiring existing DEF14AExtraction fields through the governance mapper to GOV signals that currently have no data route. Signals that genuinely cannot be extracted get marked INACTIVE so they do not inflate the SKIPPED count.

Output: ~34 fewer SKIPPED signals, no regression in TRIGGERED count.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-pipeline-integrity-facets-ci-guardrails/49-01-SUMMARY.md
@.planning/phases/49-pipeline-integrity-facets-ci-guardrails/49-RESEARCH.md

DEF14A signals with viable extraction paths (from research, ~34 signals):
- GOV.BOARD.* (9): board_size, independence, ceo_chair, departures, overboarding, tenure, attendance, diversity, classified_board
- GOV.COMP.* (5): clawback_policy, insider_ownership, dilution_rate, pay_ratio, golden_parachute
- GOV.PAY.* (8): ceo_total, peer_comparison, say_on_pay, structure, equity_grants, option_repricing, perquisites, severance
- GOV.RIGHTS.* (7): dual_class, voting_rights, bylaws, poison_pill, supermajority, blank_check, forum_selection
- GOV.EFFECT.* (5): say_on_pay_approval, proxy_advisory, iss_score, shareholder_proposals, proxy_contest

Some of these are ALREADY mapped (e.g., board_size, independence, ceo_chair, dual_class).
Focus on the UNMAPPED ones that currently SKIP.

Signals to mark INACTIVE (no viable extraction path):
- Signals requiring ISS scores, Glass Lewis data, or other external proprietary sources
- Signals whose DEF14A data is too inconsistently disclosed

Key files (post-rename from Plan 01):
- signal_mappers_sections.py: map_governance_fields() -- add new field mappings
- signal_field_routing.py: FIELD_FOR_SIGNAL dict -- add new signal->field mappings
- def14a.py: DEF14AExtraction schema -- verify fields exist for all signals
- brain/signals/gov/*.yaml: lifecycle_state field for INACTIVE signals
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage SKIPPED DEF14A signals and mark INACTIVE where no path exists</name>
  <files>
    src/do_uw/brain/signals/gov/*.yaml
  </files>
  <action>
**Step 1 - Identify current SKIPPED GOV signals:**
Run a quick analysis to identify which GOV.* signals currently SKIP. This requires either:
- Reading the most recent AAPL state output (if available in output/AAPL/), OR
- Running the signal engine in dry-run mode and checking which signals have no data route

For each SKIPPED GOV signal, classify it:
- **FIXABLE**: DEF14AExtraction has a matching field, and the LLM extraction prompt is likely to populate it
- **INACTIVE**: Requires external API (ISS, Glass Lewis), proprietary data, or field is too inconsistently disclosed

**Step 2 - Mark INACTIVE signals:**
For each signal classified as INACTIVE, update its YAML entry to add:
```yaml
lifecycle_state: INACTIVE
```

Signals that are likely INACTIVE candidates:
- `GOV.EFFECT.iss_score` -- requires ISS proprietary data
- `GOV.EFFECT.proxy_advisory` -- requires Glass Lewis/ISS data
- Any signal whose required data source is an external paid API

The `lifecycle_state: INACTIVE` field already exists in the schema (used in Phase 46). These signals will be skipped by the signal engine but NOT counted as SKIPPED in CI metrics -- they are explicitly opted out.

**Step 3 - Verify INACTIVE count:**
Count INACTIVE signals. The goal is that SKIPPED + INACTIVE together account for the same signals that previously were all SKIPPED, but now INACTIVE ones are segregated.

Run `uv run do-uw brain build` to verify build succeeds.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run python -c "
import yaml; from pathlib import Path
gov_dir = Path('src/do_uw/brain/signals/gov')
inactive = []
for f in sorted(gov_dir.rglob('*.yaml')):
    data = yaml.safe_load(f.read_text())
    if isinstance(data, list):
        for s in data:
            if s.get('lifecycle_state') == 'INACTIVE':
                inactive.append(s['id'])
print(f'INACTIVE GOV signals: {len(inactive)}')
for s in inactive: print(f'  {s}')
" && uv run do-uw brain build 2>&1 | tail -3</automated>
  </verify>
  <done>GOV signals triaged: FIXABLE signals identified, INACTIVE signals marked in YAML. brain build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DEF14A extraction fields through governance mapper for FIXABLE signals</name>
  <files>
    src/do_uw/stages/analyze/signal_mappers_sections.py
    src/do_uw/stages/analyze/signal_field_routing.py
    src/do_uw/stages/extract/def14a.py (read-only verification)
  </files>
  <action>
For each FIXABLE DEF14A signal that currently SKIPs, wire the data route:

**Step 1 - Verify DEF14AExtraction has the field:**
For each fixable signal, confirm the corresponding field exists in `DEF14AExtraction` (in def14a.py). The extraction schema already has 30+ fields from Phase 19/20.

**Step 2 - Add mapper lines in signal_mappers_sections.py:**
In `map_governance_fields()`, add mappings for each fixable signal. The pattern follows existing mappings:

```python
# Existing pattern:
if governance_data.board_profile:
    bp = governance_data.board_profile
    if bp.board_size and bp.board_size.value is not None:
        mapped["board_size"] = bp.board_size.value

# New mappings follow same pattern:
if governance_data.board_profile:
    bp = governance_data.board_profile
    if bp.meeting_attendance and bp.meeting_attendance.value is not None:
        mapped["board_attendance"] = bp.meeting_attendance.value
    if bp.board_diversity and bp.board_diversity.value is not None:
        mapped["board_diversity"] = bp.board_diversity.value
```

For each unmapped signal, determine:
1. Which GovernanceData sub-object has the field (board_profile, compensation, executive_profiles, etc.)
2. What the field name is on that sub-object
3. What field_key the signal's data_strategy expects
4. Add the mapping line connecting extraction field -> data_strategy.field_key

**Step 3 - Add signal_field_routing entries:**
In signal_field_routing.py, add entries to `FIELD_FOR_SIGNAL` (or equivalent dict) for any new signal->field mappings that don't already exist.

**Step 4 - Verify signal evaluation:**
After wiring, verify signals now evaluate by running a subset check:
```python
# Quick check: load signal engine, attempt to evaluate a previously-SKIPPED signal
# with mock data to confirm it no longer SKIPs
```

The goal is NOT to run a full AAPL pipeline (that takes too long). Instead, verify that the code paths now connect by:
1. Importing the mapper function and calling it with mock GovernanceData
2. Confirming the expected field_keys appear in the output dict
3. Confirming FIELD_FOR_SIGNAL has entries for the fixed signals

**IMPORTANT:** Per user decision, new evaluations go live immediately -- no staging gate. Once wired, these signals will evaluate on the next pipeline run.

Run `uv run pytest tests/ -k "signal_mapper or governance" -x --timeout=60` to verify no test regressions.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run pytest tests/ -k "signal_mapper or governance or field_routing" -x --timeout=60 -q 2>&1 | tail -10</automated>
  </verify>
  <done>DEF14A signals wired through governance mapper. Previously-SKIPPED signals now have complete data routes (YAML -> extraction -> mapper -> field_routing -> evaluator). SKIPPED count reduced by at least 20. No regression in TRIGGERED count.</done>
</task>

</tasks>

<verification>
1. `lifecycle_state: INACTIVE` set on signals with no viable extraction path
2. `map_governance_fields()` has new mapping lines for all FIXABLE signals
3. `FIELD_FOR_SIGNAL` has entries for all newly-mapped signals
4. `uv run do-uw brain build` succeeds
5. Tests pass: `uv run pytest tests/ -k "signal_mapper or governance" -x`
6. Expected SKIPPED reduction: from ~68 to ~34 (verified when full pipeline runs in Plan 04/05)
</verification>

<success_criteria>
- INT-01 satisfied: Population B DEF14A signals evaluate instead of SKIP, SKIPPED count moves toward ~34
- No regression in existing TRIGGERED signals
- INACTIVE signals explicitly marked (not counted as SKIPPED)
- Ready for full pipeline validation in Plan 05
</success_criteria>

<output>
After completion, create `.planning/phases/49-pipeline-integrity-facets-ci-guardrails/49-03-SUMMARY.md`
</output>
