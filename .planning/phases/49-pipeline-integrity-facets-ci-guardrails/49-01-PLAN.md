---
phase: 49-pipeline-integrity-facets-ci-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/brain/brain_signal_schema.py
  - src/do_uw/brain/brain_build_signals.py
  - src/do_uw/brain/brain_schema.py
  - src/do_uw/brain/brain_loader.py
  - src/do_uw/brain/brain_loader_rows.py
  - src/do_uw/brain/brain_effectiveness.py
  - src/do_uw/brain/brain_facet_schema.py
  - src/do_uw/brain/brain_writer.py
  - src/do_uw/brain/brain_writer_export.py
  - src/do_uw/brain/brain_enrich.py
  - src/do_uw/brain/brain_config_loader.py
  - src/do_uw/brain/brain_migrate.py
  - src/do_uw/brain/brain_migrate_yaml.py
  - src/do_uw/brain/signals/
  - src/do_uw/brain/signals.json
  - src/do_uw/stages/analyze/signal_engine.py
  - src/do_uw/stages/analyze/signal_evaluators.py
  - src/do_uw/stages/analyze/signal_field_routing.py
  - src/do_uw/stages/analyze/signal_helpers.py
  - src/do_uw/stages/analyze/signal_mappers.py
  - src/do_uw/stages/analyze/signal_mappers_ext.py
  - src/do_uw/stages/analyze/signal_mappers_analytical.py
  - src/do_uw/stages/analyze/signal_mappers_forward.py
  - src/do_uw/stages/analyze/signal_mappers_sections.py
  - src/do_uw/stages/analyze/signal_results.py
  - src/do_uw/stages/render/html_signals.py
  - src/do_uw/cli_knowledge_signals.py
  - src/do_uw/cli_brain.py
  - src/do_uw/cli_brain_ext.py
  - src/do_uw/cli_brain_explore.py
  - src/do_uw/cli_brain_yaml.py
  - src/do_uw/cli_brain_add.py
  - src/do_uw/cli.py
  - src/do_uw/models/state.py
  - src/do_uw/models/scoring.py
  - src/do_uw/knowledge/feedback.py
  - src/do_uw/knowledge/calibrate.py
  - src/do_uw/knowledge/learning.py
  - src/do_uw/knowledge/store.py
  - src/do_uw/knowledge/lifecycle.py
  - src/do_uw/cli_knowledge_traceability.py
  - src/do_uw/cli_calibrate.py
  - src/do_uw/cli_feedback.py
  - src/do_uw/cli_validate.py
  - tests/
autonomous: true
requirements: [NOM-01]

must_haves:
  truths:
    - "'check' terminology does not appear in signal-related identifiers anywhere in src/"
    - "brain/signals/ directory exists with all 400 signal YAML entries (brain/checks/ removed)"
    - "BrainSignalEntry is the schema class name (BrainCheckEntry does not exist)"
    - "SignalResult is the result class name (CheckResult does not exist)"
    - "DuckDB brain_signals table exists with all data (brain_checks table does not exist)"
    - "'uv run python -c \"import do_uw\"' succeeds with no ImportError"
    - "Full test suite passes (allowing for pre-existing failures only)"
  artifacts:
    - path: "src/do_uw/brain/brain_signal_schema.py"
      provides: "BrainSignalEntry, BrainSignalThreshold, DisplaySpec schemas"
    - path: "src/do_uw/brain/brain_build_signals.py"
      provides: "YAML-to-DuckDB build pipeline for signals"
    - path: "src/do_uw/stages/analyze/signal_engine.py"
      provides: "Signal evaluation dispatch (evaluate_signal)"
    - path: "src/do_uw/stages/analyze/signal_results.py"
      provides: "SignalResult and SignalStatus classes"
    - path: "src/do_uw/brain/signals/"
      provides: "400 signal YAML definitions (renamed from checks/)"
  key_links:
    - from: "src/do_uw/cli.py"
      to: "src/do_uw/brain/brain_build_signals.py"
      via: "_ensure_brain_db imports and brain_signals table check"
      pattern: "brain_signals"
    - from: "src/do_uw/stages/analyze/signal_engine.py"
      to: "src/do_uw/stages/analyze/signal_results.py"
      via: "SignalResult import"
      pattern: "from.*signal_results.*import.*SignalResult"
    - from: "src/do_uw/models/state.py"
      to: "src/do_uw/stages/analyze/signal_results.py"
      via: "signal_results dict on AnalysisResults"
      pattern: "signal_results.*dict"
---

<objective>
Big-bang rename of "check" to "signal" across the entire codebase in a single atomic commit.

Purpose: Establish consistent domain terminology ("signal") as the foundation for all subsequent Phase 49 work. Per user decision: total rename, big bang single commit, no backward-compatible aliases.

Output: Every Python class, function, CLI command, YAML directory, DuckDB table, file name, config key, log message, and test reference uses "signal" instead of "check" in signal-related contexts.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-pipeline-integrity-facets-ci-guardrails/49-RESEARCH.md

Key rename mapping (from research):
- BrainCheckEntry -> BrainSignalEntry
- BrainCheckThreshold -> BrainSignalThreshold
- BrainCheckProvenance -> BrainSignalProvenance
- CheckResult -> SignalResult
- CheckStatus -> SignalStatus
- check_engine.py -> signal_engine.py
- check_evaluators.py -> signal_evaluators.py
- check_field_routing.py -> signal_field_routing.py
- check_helpers.py -> signal_helpers.py
- check_mappers.py -> signal_mappers.py (and all _ext, _analytical, _forward, _sections variants)
- check_results.py -> signal_results.py
- html_checks.py -> html_signals.py
- cli_knowledge_checks.py -> cli_knowledge_signals.py
- brain_check_schema.py -> brain_signal_schema.py
- brain_build_checks.py -> brain_build_signals.py
- brain/checks/ -> brain/signals/
- checks.json -> signals.json
- DuckDB tables: brain_checks -> brain_signals, brain_checks_current -> brain_signals_current, brain_checks_active -> brain_signals_active
- DuckDB columns: check_id -> signal_id in all 7 tables
- Field names: check_results -> signal_results on AnalysisResults
- All `from do_uw.stages.analyze.check_*` imports -> `from do_uw.stages.analyze.signal_*`
- All `from do_uw.brain.brain_check_*` imports -> `from do_uw.brain.brain_signal_*`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script and execute the codebase-wide rename</name>
  <files>
    All files listed in files_modified above (scripted rename touches every file)
  </files>
  <action>
Write a Python rename script (can be temporary, in /tmp or similar) that performs the complete rename in this order:

**Step 1 - File renames (git mv):**
- `git mv src/do_uw/brain/checks src/do_uw/brain/signals`
- `git mv src/do_uw/brain/checks.json src/do_uw/brain/signals.json`
- `git mv src/do_uw/brain/brain_check_schema.py src/do_uw/brain/brain_signal_schema.py`
- `git mv src/do_uw/brain/brain_build_checks.py src/do_uw/brain/brain_build_signals.py`
- `git mv src/do_uw/stages/analyze/check_engine.py src/do_uw/stages/analyze/signal_engine.py`
- `git mv src/do_uw/stages/analyze/check_evaluators.py src/do_uw/stages/analyze/signal_evaluators.py`
- `git mv src/do_uw/stages/analyze/check_field_routing.py src/do_uw/stages/analyze/signal_field_routing.py`
- `git mv src/do_uw/stages/analyze/check_helpers.py src/do_uw/stages/analyze/signal_helpers.py`
- `git mv src/do_uw/stages/analyze/check_mappers.py src/do_uw/stages/analyze/signal_mappers.py`
- `git mv src/do_uw/stages/analyze/check_mappers_ext.py src/do_uw/stages/analyze/signal_mappers_ext.py`
- `git mv src/do_uw/stages/analyze/check_mappers_analytical.py src/do_uw/stages/analyze/signal_mappers_analytical.py`
- `git mv src/do_uw/stages/analyze/check_mappers_forward.py src/do_uw/stages/analyze/signal_mappers_forward.py`
- `git mv src/do_uw/stages/analyze/check_mappers_sections.py src/do_uw/stages/analyze/signal_mappers_sections.py`
- `git mv src/do_uw/stages/analyze/check_results.py src/do_uw/stages/analyze/signal_results.py`
- `git mv src/do_uw/stages/render/html_checks.py src/do_uw/stages/render/html_signals.py`
- `git mv src/do_uw/cli_knowledge_checks.py src/do_uw/cli_knowledge_signals.py`

**Step 2 - Content replacement (signal-specific patterns only):**
Apply these substitutions across ALL .py files in src/ and tests/:
- `BrainCheckEntry` -> `BrainSignalEntry`
- `BrainCheckThreshold` -> `BrainSignalThreshold`
- `BrainCheckProvenance` -> `BrainSignalProvenance`
- `CheckResult` -> `SignalResult` (but NOT the CheckResultSummary in calibration/runner.py if it exists — audit this)
- `CheckStatus` -> `SignalStatus`
- `check_engine` -> `signal_engine` (in imports and string references)
- `check_evaluators` -> `signal_evaluators`
- `check_field_routing` -> `signal_field_routing`
- `check_helpers` -> `signal_helpers`
- `check_mappers` -> `signal_mappers`
- `check_results` -> `signal_results` (in imports and field references)
- `html_checks` -> `html_signals`
- `brain_check_schema` -> `brain_signal_schema`
- `brain_build_checks` -> `brain_build_signals`
- `cli_knowledge_checks` -> `cli_knowledge_signals`
- `check_id` -> `signal_id` (in all contexts — Python vars, SQL strings, dict keys, log messages)
- `check_status` -> `signal_status` (variable names)
- `brain/checks/` -> `brain/signals/` (path references in strings)
- `brain_checks` -> `brain_signals` (DuckDB table names in SQL strings)
- `checks.json` -> `signals.json` (file path references)
- Function names: `evaluate_check` -> `evaluate_signal`, `_apply_traceability` references, `execute_checks` -> `execute_signals`, `_group_checks_by_section` -> `_group_signals_by_section`, etc.
- Variable names: `check_result` -> `signal_result`, `check_data` -> `signal_data`, etc.
- Log messages and docstrings: "check" -> "signal" in signal-related contexts

**CRITICAL - Do NOT rename these (false positive guard):**
- Generic English usage: "check if", "check whether", "double-check", "health check" (diagnostic sense), "sanity check"
- The word "checkout", "checksum", "checked", "checking" in generic usage
- The `pre-commit` check concept
- `typecheck`, `pyright`-related checking
- `CheckResultSummary` in calibration module — review whether this should also rename (it summarizes signal results, so it probably should become `SignalResultSummary`)

**Step 3 - DuckDB schema update:**
Update `brain_schema.py` _TABLES_DDL:
- Rename table `brain_checks` -> `brain_signals`
- Rename column `check_id` -> `signal_id` in all 7 affected tables (brain_signals, brain_check_runs -> brain_signal_runs, brain_effectiveness, brain_feedback, brain_proposals, brain_changelog, brain_causal_chains)
- Rename views: `brain_checks_current` -> `brain_signals_current`, `brain_checks_active` -> `brain_signals_active`
- Since brain.duckdb is a rebuild-from-YAML cache: delete the old brain.duckdb (it will be rebuilt by `brain build`)

**Step 4 - signals.json update:**
The renamed signals.json needs internal `check_id` keys renamed to `signal_id` if present.

**Step 5 - Verify imports resolve:**
Run `uv run python -c "import do_uw"` — must succeed.
Run `uv run python -c "from do_uw.stages.analyze.signal_engine import evaluate_signal"` (or whatever the renamed entry point is).
Run `uv run python -c "from do_uw.brain.brain_signal_schema import BrainSignalEntry"`.

After all renames, run `uv run ruff check src/ --fix` to fix any import ordering issues.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run python -c "import do_uw" && uv run python -c "from do_uw.brain.brain_signal_schema import BrainSignalEntry" && uv run python -c "from do_uw.stages.analyze.signal_results import SignalResult" && echo "All imports OK"</automated>
  </verify>
  <done>All files renamed, all content references updated, all imports resolve without error. No BrainCheckEntry, CheckResult, check_engine, brain/checks/ references remain in signal-related contexts.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild brain.duckdb and run full test suite</name>
  <files>
    src/do_uw/brain/brain.duckdb
    tests/ (all test files — rename references)
  </files>
  <action>
**Step 1 - Delete old brain.duckdb:**
Remove `src/do_uw/brain/brain.duckdb` so it rebuilds with the new schema.

**Step 2 - Rebuild brain:**
Run `uv run do-uw brain build` to repopulate from YAML. This should create `brain_signals` table instead of `brain_checks`. Verify the build completes with "400 signals" (or current count) and 0 sync errors.

**Step 3 - Run full test suite:**
Run `uv run pytest tests/ -x --timeout=60` (or the project's standard test command).

Fix any test failures caused by the rename:
- Update test imports (`from do_uw.stages.analyze.check_results import CheckResult` -> `from do_uw.stages.analyze.signal_results import SignalResult`)
- Update test assertions that reference old table/column names
- Update test file names if they reference "check" in signal contexts (e.g., test_check_engine.py -> test_signal_engine.py)
- Update fixture names and variable names in tests

**Step 4 - Verify no old terminology remains:**
Run targeted grep to confirm no signal-related "check" references remain:
```
grep -rn "BrainCheckEntry\|BrainCheckThreshold\|check_engine\|check_evaluators\|check_field_routing\|check_helpers\|check_mappers\|check_results\|html_checks\|brain_build_checks\|brain_check_schema\|brain/checks/" src/ tests/
```
This should return 0 matches. If any remain, fix them.

Run `uv run ruff check src/ tests/` to confirm no lint errors.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run do-uw brain build 2>&1 | tail -5 && uv run pytest tests/ -x --timeout=120 -q 2>&1 | tail -20</automated>
  </verify>
  <done>brain.duckdb rebuilt with brain_signals table. Full test suite passes. grep confirms zero signal-related "check" references in src/ and tests/. Ruff check clean.</done>
</task>

</tasks>

<verification>
1. `grep -rn "BrainCheckEntry\|CheckResult\|check_engine\|brain/checks/" src/ tests/` returns 0 matches
2. `uv run python -c "import do_uw"` succeeds
3. `uv run do-uw brain build` completes with 400 signals, 0 errors
4. `uv run pytest tests/ -x --timeout=120` passes (pre-existing failures only)
5. `ls src/do_uw/brain/signals/` shows all 8 domain subdirectories
6. `ls src/do_uw/brain/checks/` returns "No such file or directory"
</verification>

<success_criteria>
- NOM-01 fully satisfied: "check" renamed to "signal" throughout codebase
- Every import chain resolves without error
- brain.duckdb uses brain_signals table exclusively
- Test suite passes
- Clean foundation for Plans 02-05 to build on
</success_criteria>

<output>
After completion, create `.planning/phases/49-pipeline-integrity-facets-ci-guardrails/49-01-SUMMARY.md`
</output>
