---
phase: 124-css-density-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["124-01"]
files_modified:
  - src/do_uw/stages/render/formatters.py
  - src/do_uw/stages/benchmark/narrative_generator.py
  - src/do_uw/stages/extract/dossier_extraction.py
  - src/do_uw/stages/extract/llm/extractor.py
  - src/do_uw/stages/render/sections/sect1_findings_neg.py
  - src/do_uw/stages/render/sections/sect1_findings_pos.py
  - src/do_uw/stages/render/sections/meeting_questions.py
  - src/do_uw/stages/extract/earnings_guidance.py
  - tests/stages/render/test_boilerplate_elimination.py
autonomous: true
requirements: [FIX-03]

must_haves:
  truths:
    - "Grepping the rendered HTML for known boilerplate narrative phrases returns 0 matches"
    - "All narrative content is company-specific with actual data points"
  artifacts:
    - path: "tests/stages/render/test_boilerplate_elimination.py"
      provides: "Test that greps for known boilerplate patterns in rendered output"
      contains: "BOILERPLATE_PATTERNS"
  key_links:
    - from: "tests/stages/render/test_boilerplate_elimination.py"
      to: "src/do_uw/stages/render/formatters.py"
      via: "Test validates formatters don't emit boilerplate"
      pattern: "boilerplate"
---

<objective>
Eliminate all remaining boilerplate narrative phrases from the worksheet output. Every sentence must contain company-specific data per CLAUDE.md rules.

Purpose: Underwriters should never read a sentence that could apply to any company by swapping the name. Generic narrative destroys credibility.
Output: Updated Python files with boilerplate filters, plus a test that catches regressions.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-css-density-overhaul/124-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Catalog boilerplate patterns and create regression test</name>
  <read_first>
    - src/do_uw/stages/render/formatters.py
    - src/do_uw/stages/benchmark/narrative_generator.py
    - src/do_uw/stages/render/sections/sect1_findings_neg.py
    - src/do_uw/stages/render/sections/sect1_findings_pos.py
    - src/do_uw/stages/render/sections/meeting_questions.py
    - src/do_uw/stages/extract/dossier_extraction.py
    - src/do_uw/stages/extract/llm/extractor.py
    - src/do_uw/stages/extract/earnings_guidance.py
  </read_first>
  <files>
    tests/stages/render/test_boilerplate_elimination.py
  </files>
  <behavior>
    - Test 1: A list of 20+ known boilerplate patterns (gathered by grepping the Python source files that reference "boilerplate") should be defined as BOILERPLATE_PATTERNS
    - Test 2: Each pattern should be a regex that matches generic narrative phrases like "has experienced a notable", "the company has shown", "demonstrates a commitment to", "warrants further investigation", "may impact future", "is positioned to", "faces potential", "has maintained a", "continues to demonstrate", "reflects the company's", "underscores the importance", "it is worth noting", "it should be noted", "given the current landscape", "in the current environment", "going forward", "moving forward", "remains to be seen", "time will tell"
    - Test 3: A function `check_for_boilerplate(html_content: str) -> list[str]` returns all matched boilerplate phrases found
    - Test 4: An empty/clean narrative returns empty list
    - Test 5: A narrative with "has experienced a notable decline" returns that match
  </behavior>
  <action>
    Create `tests/stages/render/test_boilerplate_elimination.py`:

    1. Define `BOILERPLATE_PATTERNS`: a list of regex patterns matching known generic narrative phrases. Gather these by:
       - Reading the source files listed in read_first
       - Searching for hardcoded fallback strings, default narrative text, and template-language phrases
       - Adding the standard boilerplate phrases: "has experienced a notable", "warrants further investigation", "demonstrates a commitment", "is positioned to", "may impact future", "faces potential challenges", "has maintained a strong", "continues to demonstrate", "reflects the company's", "underscores the importance", "it is worth noting", "it should be noted", "given the current landscape", "in the current environment", "going forward", "moving forward", "remains to be seen", "time will tell", "has shown a trend", "the company has exhibited"

    2. Create utility function `find_boilerplate_matches(text: str, patterns: list[str]) -> list[str]` that returns all matching phrases

    3. Write tests:
       - `test_boilerplate_patterns_defined()` -- at least 20 patterns
       - `test_clean_text_passes()` -- specific company text returns no matches
       - `test_generic_text_caught()` -- text with known boilerplate returns matches
       - `test_all_patterns_are_valid_regex()` -- each pattern compiles without error

    Run tests (RED phase -- these define the contract).
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/render/test_boilerplate_elimination.py -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep for `BOILERPLATE_PATTERNS` in test file returns 1+ match
    - grep for `find_boilerplate_matches` in test file returns 1+ match
    - At least 20 patterns defined
    - All tests pass
  </acceptance_criteria>
  <done>Boilerplate pattern catalog exists as test fixture with 20+ patterns. Utility function validates text against patterns. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminate boilerplate from narrative generators and formatters</name>
  <read_first>
    - tests/stages/render/test_boilerplate_elimination.py
    - src/do_uw/stages/render/formatters.py
    - src/do_uw/stages/benchmark/narrative_generator.py
    - src/do_uw/stages/render/sections/sect1_findings_neg.py
    - src/do_uw/stages/render/sections/sect1_findings_pos.py
    - src/do_uw/stages/render/sections/meeting_questions.py
    - src/do_uw/stages/extract/dossier_extraction.py
    - src/do_uw/stages/extract/llm/extractor.py
    - src/do_uw/stages/extract/earnings_guidance.py
  </read_first>
  <files>
    src/do_uw/stages/render/formatters.py,
    src/do_uw/stages/benchmark/narrative_generator.py,
    src/do_uw/stages/extract/dossier_extraction.py,
    src/do_uw/stages/extract/llm/extractor.py,
    src/do_uw/stages/render/sections/sect1_findings_neg.py,
    src/do_uw/stages/render/sections/sect1_findings_pos.py,
    src/do_uw/stages/render/sections/meeting_questions.py,
    src/do_uw/stages/extract/earnings_guidance.py
  </files>
  <action>
    For each file that references "boilerplate":

    1. **formatters.py**: Search for any fallback strings that produce generic narrative. Replace with either:
       - Empty string (omit the field)
       - "Not Available" (explicit data gap)
       - A template that REQUIRES a data value: e.g., instead of "The company has shown improvement" use "{company} {metric} changed from {old} to {new} ({pct_change}%)"

    2. **narrative_generator.py**: Audit LLM prompts for instructions that could produce generic output. Add to every narrative prompt:
       - "NEVER use generic phrases like 'has experienced', 'warrants further investigation', 'demonstrates a commitment'. Every sentence MUST contain a specific dollar amount, percentage, date, or named entity."
       - "If you lack specific data for a claim, omit the sentence entirely rather than using generic language."

    3. **sect1_findings_neg.py / sect1_findings_pos.py**: Find any hardcoded fallback narratives. Replace with data-driven templates or omit.

    4. **meeting_questions.py**: Find any template questions that use generic phrasing. Make them data-specific or remove.

    5. **dossier_extraction.py / extractor.py / earnings_guidance.py**: Check LLM prompts for boilerplate-inducing instructions. Add anti-boilerplate directives.

    6. Add a `strip_boilerplate(text: str) -> str` utility to formatters.py that uses the BOILERPLATE_PATTERNS to detect and flag (log warning) any remaining boilerplate that slips through. This is a safety net, not a replacement for fixing the source.

    IMPORTANT: Do NOT remove analytical content or shorten findings. Only remove GENERIC phrases that add no information. If a phrase contains a specific number, name, or date, it is NOT boilerplate.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/render/test_boilerplate_elimination.py tests/stages/render/ -x -q --tb=short 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep for `strip_boilerplate` in formatters.py returns 1+ match
    - grep for `NEVER use generic phrases` in narrative_generator.py returns 1+ match
    - All render tests pass
    - All boilerplate elimination tests pass
  </acceptance_criteria>
  <done>All identified boilerplate sources eliminated or guarded. LLM prompts include anti-boilerplate directives. Safety-net strip_boilerplate() utility exists in formatters.py. All tests pass.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/stages/render/test_boilerplate_elimination.py -v` -- all boilerplate tests pass
2. `uv run pytest tests/stages/render/ -x -q` -- all render tests pass
3. `grep -c "strip_boilerplate" src/do_uw/stages/render/formatters.py` returns 1+
4. `grep -c "BOILERPLATE_PATTERNS" tests/stages/render/test_boilerplate_elimination.py` returns 1+
</verification>

<success_criteria>
- 20+ known boilerplate patterns cataloged in test fixture
- Utility function to detect boilerplate exists and is tested
- LLM prompts include anti-boilerplate directives
- Hardcoded fallback narratives replaced with data-specific templates or omitted
- Safety-net strip_boilerplate() function added to formatters.py
- All existing render tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/124-css-density-overhaul/124-02-SUMMARY.md`
</output>
