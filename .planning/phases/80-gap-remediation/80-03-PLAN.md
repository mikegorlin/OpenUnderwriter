---
phase: 80-gap-remediation
plan: 03
type: execute
wave: 3
depends_on: ["80-01", "80-02"]
files_modified:
  - src/do_uw/brain/output_manifest.yaml
autonomous: false
requirements: [GAP-03]

must_haves:
  truths:
    - "A full pipeline run produces a valid HTML worksheet with no template errors"
    - "Newly-wired facets render content (not empty sections)"
    - "brain audit reports zero orphaned signals and zero unwired facets"
  artifacts:
    - path: "output/"
      provides: "Pipeline output directory with rendered worksheet"
  key_links:
    - from: "src/do_uw/brain/output_manifest.yaml"
      to: "src/do_uw/templates/html/sections/**/*.html.j2"
      via: "manifest-driven rendering"
      pattern: "template:"
---

<objective>
Run brain audit to confirm zero orphans/unwired, then run the full pipeline on a test ticker to verify all newly-wired facets render correctly in the HTML output.

Purpose: Plans 01-02 wired signals and marked INACTIVE in YAML. This plan validates the wiring is correct end-to-end by running the actual pipeline and verifying the HTML output contains no broken templates, no empty sections from wiring mistakes, and that the brain audit reports clean.

Output: Verified pipeline output with all facets rendering. Confirmed zero-orphan, zero-unwired state.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-gap-remediation/80-01-SUMMARY.md
@.planning/phases/80-gap-remediation/80-02-SUMMARY.md
@src/do_uw/brain/output_manifest.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run brain audit, contract tests, and full pipeline verification</name>
  <files></files>
  <action>
Step 1 — Run brain audit to verify signal-facet wiring is complete:

```bash
cd /Users/gorlin/projects/UW/do-uw && uv run python -m do_uw.cli brain audit
```

Expected output should show:
- 0 orphaned signals (all active signals are in a facet)
- 0 unwired facets (all non-display-only facets have signals)
- Total signal count (~476)
- INACTIVE count (however many were marked in Plan 02)

If brain audit reports any remaining orphans or unwired facets, fix them:
- Orphaned signal: Either wire to an appropriate facet in output_manifest.yaml OR mark INACTIVE in the signal's YAML file
- Unwired facet: Either wire appropriate signals OR document as data-display-only

Step 2 — Run contract enforcement tests:

```bash
uv run pytest tests/brain/test_contract_enforcement.py -v
```

Step 3 — Run the actual pipeline on a test ticker (AAPL) with --fresh to exercise all stages including rendering:

```bash
uv run python -m do_uw.cli run AAPL --fresh
```

Step 4 — Verify the HTML output exists and contains no template errors:
- Check that `output/AAPL-*/AAPL_worksheet.html` was created
- Open and review the HTML output section by section: verify real data is populated (not N/A everywhere), financials render, board/governance names present, score/tier reasonable, no raw data leaks or Jinja template errors
- Check that newly-wired facets from Plans 01-02 render content (not empty sections)

If the pipeline fails or the HTML has template errors, diagnose and fix the wiring in output_manifest.yaml before proceeding.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/brain/test_contract_enforcement.py -v --tb=short 2>&1 | tail -20 && ls -la output/AAPL-*/AAPL_worksheet.html 2>&1 | tail -5</automated>
  </verify>
  <done>Brain audit reports zero orphaned signals and zero unwired facets. All contract enforcement tests pass. Pipeline produces HTML output with no template errors and newly-wired facets rendering content.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verification of pipeline output and signal-facet wiring</name>
  <files></files>
  <action>
Present the gap remediation results to the user for verification.

What was built: Complete gap remediation — brain audit clean (zero orphans, zero unwired), contract tests passing, and full pipeline run on AAPL producing HTML worksheet with all facets rendering.

How to verify:
1. Review brain audit output — confirm zero orphaned signals, zero unwired facets
2. Review the INACTIVE signals list — confirm markings are sensible
3. Open the AAPL HTML worksheet in browser and spot-check:
   - Sections render with real data (not empty or N/A everywhere)
   - No Jinja template errors or raw variable names visible
   - Score/tier displays correctly
   - Newly-wired facets from Plans 01-02 show content
4. Confirm contract enforcement tests all pass (baseline 0)

Resume signal: Type "approved" or describe issues.
  </action>
  <verify>User confirms the wiring is correct, INACTIVE markings are sensible, and HTML output quality is acceptable</verify>
  <done>User approves the gap remediation. Phase 80 complete.</done>
</task>

</tasks>

<verification>
- `brain audit` reports zero orphans, zero unwired facets
- Contract enforcement tests all pass
- Pipeline run completes without errors on AAPL
- HTML output renders without template errors and newly-wired facets show content
</verification>

<success_criteria>
- brain audit: 0 orphaned, 0 unwired
- All contract enforcement tests pass (baseline 0)
- Pipeline output renders correctly for at least one ticker (AAPL)
- HTML reviewed section-by-section with no template errors
- User verifies the wiring is sensible
</success_criteria>

<output>
After completion, create `.planning/phases/80-gap-remediation/80-03-SUMMARY.md`
</output>
