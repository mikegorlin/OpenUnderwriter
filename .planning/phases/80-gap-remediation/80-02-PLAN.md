---
phase: 80-gap-remediation
plan: 02
type: execute
wave: 2
depends_on: ["80-01"]
files_modified:
  - src/do_uw/brain/signals/base/filings.yaml
  - src/do_uw/brain/signals/base/forensics.yaml
  - src/do_uw/brain/signals/base/litigation.yaml
  - src/do_uw/brain/signals/base/market.yaml
  - src/do_uw/brain/signals/base/news.yaml
  - src/do_uw/brain/signals/base/peer.yaml
  - src/do_uw/brain/signals/base/xbrl.yaml
  - src/do_uw/brain/signals/fin/peer_xbrl.yaml
  - tests/brain/test_contract_enforcement.py
autonomous: true
requirements: [GAP-01, TRACE-03]

must_haves:
  truths:
    - "Every signal in brain YAML is either assigned to a manifest facet or explicitly marked INACTIVE"
    - "INACTIVE signals have a documented reason in their YAML entry"
    - "The contract enforcement regression test baseline is updated to 0"
    - "FIN.PEER.* signals pass schema validation or are marked INACTIVE"
  artifacts:
    - path: "tests/brain/test_contract_enforcement.py"
      provides: "Updated regression baseline (0 broken refs)"
      contains: "_KNOWN_BROKEN_SIGNAL_BASELINE = 0"
    - path: "src/do_uw/brain/signals/fin/peer_xbrl.yaml"
      provides: "Fixed or INACTIVE FIN.PEER.* signals"
      contains: "FIN.PEER"
  key_links:
    - from: "tests/brain/test_contract_enforcement.py"
      to: "src/do_uw/brain/output_manifest.yaml"
      via: "validate_signal_references"
      pattern: "validate_signal_references"
---

<objective>
Mark remaining orphaned signals as INACTIVE with documented reasons, fix the 6 invalid FIN.PEER.* signals, and update the contract enforcement test baseline to 0.

Purpose: After Plan 01 wires signals to facets, some signals may legitimately not belong to any facet (e.g., BASE.* foundational signals that are acquisition-only, not display). These need explicit INACTIVE marking per TRACE-03. The regression test baseline must also drop from 200 to 0.

Output: Zero orphaned signals, zero broken signal refs, all tests pass.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-gap-remediation/80-01-SUMMARY.md
@src/do_uw/brain/output_manifest.yaml
@src/do_uw/brain/contract_validator.py
@tests/brain/test_contract_enforcement.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark orphaned signals INACTIVE and fix FIN.PEER.* validation</name>
  <files>src/do_uw/brain/signals/base/filings.yaml, src/do_uw/brain/signals/base/forensics.yaml, src/do_uw/brain/signals/base/litigation.yaml, src/do_uw/brain/signals/base/market.yaml, src/do_uw/brain/signals/base/news.yaml, src/do_uw/brain/signals/base/peer.yaml, src/do_uw/brain/signals/base/xbrl.yaml, src/do_uw/brain/signals/fin/peer_xbrl.yaml</files>
  <action>
1. Run a script to identify which signals are STILL orphaned (in YAML but not in manifest) after Plan 01:
```python
import yaml, os
from do_uw.brain.manifest_schema import load_manifest

m = load_manifest()
manifest_sigs = set()
for s in m.sections:
    for f in s.facets:
        manifest_sigs.update(f.signals)

actual = set()
for root, dirs, files in os.walk('src/do_uw/brain/signals'):
    for f in files:
        if f.endswith('.yaml'):
            with open(os.path.join(root, f)) as fh:
                data = yaml.safe_load(fh)
            entries = data if isinstance(data, list) else (data.get('signals', []) if isinstance(data, dict) else [])
            for e in entries:
                if isinstance(e, dict) and 'id' in e:
                    actual.add(e['id'])

orphaned = actual - manifest_sigs
print(f'Still orphaned: {len(orphaned)}')
for o in sorted(orphaned):
    print(f'  {o}')
```

2. For each remaining orphaned signal, add `lifecycle_state: INACTIVE` and `inactive_reason` to its YAML entry. Use these reason categories:
   - `acquisition_only` -- BASE.* signals that drive data acquisition but are not evaluated or displayed (they are infrastructure, not analytical signals)
   - `subsumed` -- Signal's concern is covered by another active signal in the same domain
   - `no_facet_match` -- Signal has no natural display location in current worksheet layout
   - `schema_invalid` -- FIN.PEER.* signals that fail schema validation

3. For the 6 FIN.PEER.* signals in `fin/peer_xbrl.yaml`: Check if they fail schema validation. If so, either fix the schema issue OR mark them INACTIVE with reason `schema_invalid`. The FIN.PEER.* signals are: `FIN.PEER.cash_flow_weak`, `FIN.PEER.leverage_top_decile`, `FIN.PEER.margin_compression`, `FIN.PEER.profitability_laggard`, `FIN.PEER.revenue_bottom_quartile`, `FIN.PEER.size_outlier_small`. NOTE: If Plan 01 already wired some of these to facets (e.g., peer_matrix), they should NOT be marked INACTIVE.

IMPORTANT per CLAUDE.md: Brain changes require documented reasons. Each INACTIVE marking needs WHY (no facet matches its purpose) and WHAT evidence (the gap analysis showing it has no display target).
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && python3 -c "
import yaml, os
from do_uw.brain.manifest_schema import load_manifest

m = load_manifest()
manifest_sigs = set()
for s in m.sections:
    for f in s.facets:
        manifest_sigs.update(f.signals)

actual = set()
inactive = set()
for root, dirs, files in os.walk('src/do_uw/brain/signals'):
    for f in files:
        if f.endswith('.yaml'):
            with open(os.path.join(root, f)) as fh:
                data = yaml.safe_load(fh)
            entries = data if isinstance(data, list) else (data.get('signals', []) if isinstance(data, dict) else [])
            for e in entries:
                if isinstance(e, dict) and 'id' in e:
                    actual.add(e['id'])
                    if e.get('lifecycle_state') == 'INACTIVE':
                        inactive.add(e['id'])

orphaned = actual - manifest_sigs - inactive
print(f'Active orphaned: {len(orphaned)}')
for o in sorted(orphaned)[:20]:
    print(f'  {o}')
assert len(orphaned) == 0, f'Expected 0 active orphaned signals, got {len(orphaned)}'
print(f'Total signals: {len(actual)}, In manifest: {len(manifest_sigs)}, INACTIVE: {len(inactive)}')
print('PASS')
"</automated>
  </verify>
  <done>Zero active orphaned signals. Every signal is either wired to a facet or explicitly INACTIVE with a documented reason. FIN.PEER.* signals either pass validation or are INACTIVE.</done>
</task>

<task type="auto">
  <name>Task 2: Update regression test baseline and verify all contract tests pass</name>
  <files>tests/brain/test_contract_enforcement.py</files>
  <action>
1. Update the regression baseline in `test_real_signal_references`:
   - Change `_KNOWN_BROKEN_SIGNAL_BASELINE = 200` to `_KNOWN_BROKEN_SIGNAL_BASELINE = 0`
   - Update the test to FAIL if ANY broken references exist (not just regression above baseline)
   - The test should now assert `len(violations) == 0` with a clear message listing any broken refs

2. Add a new test `test_zero_orphaned_signals` that verifies no ACTIVE signal is orphaned:
   - Load all signal IDs from YAML, excluding INACTIVE ones
   - Load all signal refs from manifest
   - Assert every active signal appears in at least one facet
   - This is the automated equivalent of `brain audit` zero-orphans check

3. Run the full contract enforcement test suite: `uv run pytest tests/brain/test_contract_enforcement.py -v`
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/brain/test_contract_enforcement.py -v --tb=short 2>&1 | tail -40</automated>
  </verify>
  <done>All contract enforcement tests pass. Regression baseline is 0. New test confirms zero active orphaned signals.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/brain/test_contract_enforcement.py -v` all pass
- Zero active orphaned signals
- Zero broken signal references
- Every INACTIVE signal has a documented reason
</verification>

<success_criteria>
- Zero active orphaned signals (was 467+)
- Regression baseline updated from 200 to 0
- All 28+ contract enforcement tests pass
- FIN.PEER.* signals resolved (fixed or INACTIVE)
- INACTIVE signals have documented reasons per CLAUDE.md brain provenance rules
</success_criteria>

<output>
After completion, create `.planning/phases/80-gap-remediation/80-02-SUMMARY.md`
</output>
