---
phase: 76-output-manifest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/brain/output_manifest.yaml
  - src/do_uw/brain/manifest_schema.py
  - tests/brain/test_manifest_schema.py
autonomous: true
requirements: [MAN-01]

must_haves:
  truths:
    - "A single manifest YAML exists declaring every section and facet the worksheet must contain"
    - "Manifest includes explicit section ordering, facet ordering within sections, and data_type tags"
    - "Manifest is versioned with manifest_version field"
    - "Loading the manifest produces validated Pydantic models with deterministic iteration order"
  artifacts:
    - path: "src/do_uw/brain/output_manifest.yaml"
      provides: "Single source of truth for worksheet structure"
      contains: "manifest_version"
    - path: "src/do_uw/brain/manifest_schema.py"
      provides: "Pydantic schema for manifest loading and validation"
      exports: ["OutputManifest", "ManifestSection", "ManifestFacet", "load_manifest"]
    - path: "tests/brain/test_manifest_schema.py"
      provides: "Validation tests for manifest schema and loading"
      min_lines: 50
  key_links:
    - from: "src/do_uw/brain/manifest_schema.py"
      to: "src/do_uw/brain/output_manifest.yaml"
      via: "load_manifest() reads and validates YAML"
      pattern: "yaml\\.safe_load"
    - from: "src/do_uw/brain/manifest_schema.py"
      to: "src/do_uw/brain/brain_section_schema.py"
      via: "ManifestFacet mirrors FacetSpec fields"
      pattern: "class ManifestFacet"
---

<objective>
Create the output manifest YAML and its Pydantic schema/loader.

Purpose: Establish a single declared contract specifying every section, facet, and data_type the worksheet must contain, with explicit ordering. This manifest becomes the authority for output structure.
Output: output_manifest.yaml, manifest_schema.py, tests
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-output-manifest/76-CONTEXT.md

@src/do_uw/brain/brain_section_schema.py
@src/do_uw/brain/sections/executive_summary.yaml
@src/do_uw/brain/sections/business_profile.yaml

<interfaces>
<!-- Existing section schema that manifest builds upon -->

From src/do_uw/brain/brain_section_schema.py:
```python
class FacetSpec(BaseModel):
    id: str
    name: str
    render_as: str
    signals: list[str] = []
    columns: list[str] = []
    template: str = ""

class SectionSpec(BaseModel):
    id: str
    name: str
    display_type: str
    signals: list[str] = []
    content: list[SectionContentRef] = []
    facets: list[FacetSpec] = []
    display_config: dict[str, Any] = {}

def load_all_sections(sections_dir: Path) -> dict[str, SectionSpec]:
    ...
```

<!-- Current section IDs (12 total, in brain/sections/*.yaml):
executive_summary, business_profile, executive_risk, financial_health,
filing_analysis, forward_looking, governance, litigation, market_activity,
red_flags, scoring, ai_risk -->

<!-- Current HTML template includes (worksheet.html.j2):
identity, executive, red_flags, financial, market, governance,
litigation, ai_risk, scoring, meeting_prep, sources, qa_audit, coverage -->

<!-- Current Word renderer sections (word_renderer.py _get_section_renderers):
Executive Summary, Company Profile, Financial Health, Market & Trading,
Governance & Leadership, Litigation & Regulatory, Scoring & Risk Synthesis,
AI Transformation Risk, Calibration Notes, Meeting Preparation -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create manifest schema and loader</name>
  <files>src/do_uw/brain/manifest_schema.py, tests/brain/test_manifest_schema.py</files>
  <behavior>
    - load_manifest() returns OutputManifest with ordered sections
    - ManifestSection has id, name, template, render_mode (required/optional), facets list
    - ManifestFacet has id, name, template, data_type (extract_display/extract_compute/extract_infer/hunt_analyze), render_as, signals
    - Manifest has manifest_version field
    - Sections maintain declaration order (list, not dict)
    - Facets within sections maintain declaration order
    - Invalid data_type raises ValidationError
    - Missing manifest file raises FileNotFoundError with clear message
    - Duplicate section IDs raise ValidationError
    - Duplicate facet IDs within a section raise ValidationError
  </behavior>
  <action>
    Create `src/do_uw/brain/manifest_schema.py` with Pydantic v2 models:

    - `ManifestFacet(BaseModel)`: id, name, template (str), data_type (Literal["extract_display", "extract_compute", "extract_infer", "hunt_analyze"]), render_as (str), signals (list[str], default [])
    - `ManifestSection(BaseModel)`: id, name, template (str for parent wrapper template), render_mode (Literal["required", "optional"], default "required"), facets (list[ManifestFacet])
    - `OutputManifest(BaseModel)`: manifest_version (str), sections (list[ManifestSection])
    - Add model_validator to OutputManifest that checks for duplicate section IDs and duplicate facet IDs within each section
    - `load_manifest(path: Path | None = None) -> OutputManifest`: loads YAML from default path (src/do_uw/brain/output_manifest.yaml) or override, validates via Pydantic
    - `get_section_order(manifest: OutputManifest) -> list[str]`: returns ordered list of section IDs
    - `get_facet_order(manifest: OutputManifest, section_id: str) -> list[str]`: returns ordered list of facet IDs for a section

    Use `model_config = ConfigDict(extra="forbid")` on all models for strict validation.
    Follow the pattern from brain_section_schema.py for YAML loading.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/brain/test_manifest_schema.py -x -v</automated>
  </verify>
  <done>ManifestFacet, ManifestSection, OutputManifest models validated; load_manifest loads and validates; duplicate detection works; order preserved</done>
</task>

<task type="auto">
  <name>Task 2: Create the output manifest YAML</name>
  <files>src/do_uw/brain/output_manifest.yaml</files>
  <action>
    Create `src/do_uw/brain/output_manifest.yaml` composing all current worksheet content into a single ordered manifest. The manifest must declare EVERY section and facet currently rendered by the HTML and Word renderers.

    Structure — compose from existing brain/sections/*.yaml files plus the non-section items (identity, appendices):

    ```yaml
    manifest_version: "1.0"
    sections:
      - id: identity
        name: "Cover & Identity"
        template: "sections/identity.html.j2"
        render_mode: required
        facets: []  # No sub-facets — single template
      - id: executive_summary
        name: "Executive Summary"
        template: "sections/executive.html.j2"
        render_mode: required
        facets:
          - id: data_quality_notice
            name: "Data Quality Notice"
            template: "sections/executive/data_quality_notice.html.j2"
            data_type: extract_display
            render_as: conditional_alert
            signals: []
          # ... all 7 facets from executive_summary.yaml
      - id: red_flags
        ...
      # etc for all 12 brain sections
      # Then appendices:
      - id: meeting_prep
        name: "Meeting Preparation"
        template: "appendices/meeting_prep.html.j2"
        render_mode: required
        facets: []
      - id: sources
        name: "Sources"
        template: "appendices/sources.html.j2"
        render_mode: required
        facets: []
      - id: qa_audit
        name: "QA Audit"
        template: "appendices/qa_audit.html.j2"
        render_mode: required
        facets: []
      - id: coverage
        name: "Coverage Analysis"
        template: "appendices/coverage.html.j2"
        render_mode: required
        facets: []
    ```

    Section ordering must match current worksheet.html.j2: identity, executive_summary, red_flags, financial_health (maps to "financial" template), market_activity (maps to "market" template), governance, litigation, ai_risk, scoring, then appendices.

    For each of the 12 brain sections, pull ALL facets from the corresponding brain/sections/*.yaml file. Assign data_type to each facet based on what it displays:
    - `extract_display`: Static data display (company profile, board lists, stock tables)
    - `extract_compute`: Computed metrics (distress indicators, financial ratios, Beneish)
    - `extract_infer`: Pattern recognition (risk factors, D&O exposure, red flags)
    - `hunt_analyze`: Broad search + analysis (litigation cases, regulatory actions, SCA)

    Map brain section IDs to HTML template paths:
    - executive_summary -> sections/executive.html.j2
    - business_profile -> sections/company.html.j2
    - red_flags -> sections/red_flags.html.j2
    - financial_health -> sections/financial.html.j2
    - market_activity -> sections/market.html.j2
    - governance -> sections/governance.html.j2
    - litigation -> sections/litigation.html.j2
    - ai_risk -> sections/ai_risk.html.j2
    - scoring -> sections/scoring.html.j2
    - executive_risk, filing_analysis, forward_looking: these sections exist in brain but are rendered WITHIN other parent sections (executive_risk within executive, filing_analysis within financial, forward_looking within scoring). Include them in the manifest with a note, or fold their facets into the parent section. Use Claude's discretion — the key is that every facet appears somewhere.

    After creating the YAML, validate it loads correctly:
    ```bash
    cd /Users/gorlin/projects/UW/do-uw && uv run python -c "from do_uw.brain.manifest_schema import load_manifest; m = load_manifest(); print(f'Loaded {len(m.sections)} sections, {sum(len(s.facets) for s in m.sections)} facets')"
    ```
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run python -c "
from do_uw.brain.manifest_schema import load_manifest, get_section_order
m = load_manifest()
assert m.manifest_version == '1.0', f'Expected version 1.0, got {m.manifest_version}'
order = get_section_order(m)
assert 'executive_summary' in order, 'Missing executive_summary'
assert 'scoring' in order, 'Missing scoring'
total_facets = sum(len(s.facets) for s in m.sections)
print(f'OK: {len(m.sections)} sections, {total_facets} facets, version {m.manifest_version}')
assert total_facets >= 90, f'Expected 90+ facets, got {total_facets}'
print('All manifest validation checks passed')
"</automated>
  </verify>
  <done>output_manifest.yaml exists with all sections in correct order, all ~100 facets declared with data_type tags, loads and validates via manifest_schema.py</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/brain/test_manifest_schema.py -x -v` -- schema tests pass
2. `load_manifest()` returns manifest with all sections and facets
3. Section order matches current worksheet.html.j2 includes
4. Every facet from brain/sections/*.yaml appears in the manifest
5. `manifest_version: "1.0"` present
</verification>

<success_criteria>
- Single manifest YAML declares all sections and facets with explicit ordering
- Manifest has data_type tags on all facets
- Manifest is versioned
- Pydantic schema validates manifest with duplicate detection
- All existing content is represented -- nothing lost in the manifest
</success_criteria>

<output>
After completion, create `.planning/phases/76-output-manifest/76-01-SUMMARY.md`
</output>
