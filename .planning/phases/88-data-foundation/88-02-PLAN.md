---
phase: 88-data-foundation
plan: 02
type: execute
wave: 2
depends_on: [88-01]
files_modified:
  - src/do_uw/stages/render/charts/stock_chart_data.py
  - src/do_uw/stages/render/charts/drawdown_chart.py
  - src/do_uw/stages/render/html_renderer.py
  - src/do_uw/templates/html/sections/market/stock_analysis.html
autonomous: false
requirements: [STOCK-07, STOCK-01, STOCK-03]

must_haves:
  truths:
    - "Stock analysis HTML section shows 1Y and 5Y return decomposition with three labeled components (market, sector, company-specific)"
    - "Three decomposition components visibly sum to the total return in the display"
    - "MDD ratio displayed for both 1Y and 5Y periods in the drawdown section"
    - "Underwriter can see whether a drawdown is normal or outsized relative to sector"
  artifacts:
    - path: "src/do_uw/stages/render/charts/stock_chart_data.py"
      provides: "Return decomposition and MDD ratio in chart stats"
      contains: "market_contribution"
    - path: "src/do_uw/stages/render/charts/drawdown_chart.py"
      provides: "MDD ratio display in drawdown chart stats header"
      contains: "mdd_ratio"
  key_links:
    - from: "src/do_uw/stages/render/charts/stock_chart_data.py"
      to: "src/do_uw/models/market.py"
      via: "Reads StockPerformance decomposition fields from state"
      pattern: "returns_1y_market|mdd_ratio_1y"
    - from: "src/do_uw/templates/html/sections/market/stock_analysis.html"
      to: "src/do_uw/stages/render/html_renderer.py"
      via: "Jinja2 template context receives decomposition and MDD ratio data"
      pattern: "return_decomposition|mdd_ratio"
---

<objective>
Display return decomposition and MDD ratio in the stock analysis HTML output, and update chart data extraction to surface 2Y data.

Purpose: The underwriter needs to see the three-component return breakdown and peer-relative drawdown ratio directly in the worksheet output. Without rendering, the computed data sits unused in state.

Output: Updated chart stats showing decomposition, MDD ratio in drawdown chart, and a return decomposition table in the stock analysis HTML section.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-data-foundation/88-RESEARCH.md
@.planning/phases/88-data-foundation/88-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (new fields on StockPerformance): -->
```python
# Return decomposition fields (SourcedValue[float] | None):
returns_1y_market, returns_1y_sector, returns_1y_company
returns_5y_market, returns_5y_sector, returns_5y_company

# MDD fields (SourcedValue[float] | None):
mdd_ratio_1y, mdd_ratio_5y
max_drawdown_5y
sector_mdd_1y, sector_mdd_5y
```

<!-- From chart_computations.py (new functions): -->
```python
def compute_return_decomposition(
    company_prices: list[float], spy_prices: list[float], sector_prices: list[float],
) -> dict[str, float] | None
# Returns: {"total_return", "market_contribution", "sector_contribution", "company_residual"}

def compute_mdd_ratio(
    company_prices: list[float], sector_prices: list[float],
) -> float | None
```

<!-- From existing stock_chart_data.py: -->
```python
def compute_chart_stats(data: ChartData) -> dict[str, str | float | None]
# Returns: current_price, high_52w, low_52w, total_return_pct, sector_return_pct, alpha_pct

def extract_chart_data(state: AnalysisState, period: str) -> ChartData | None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add decomposition + MDD ratio to chart stats and template</name>
  <files>
    src/do_uw/stages/render/charts/stock_chart_data.py,
    src/do_uw/stages/render/charts/drawdown_chart.py,
    src/do_uw/stages/render/html_renderer.py,
    src/do_uw/templates/html/sections/market/stock_analysis.html
  </files>
  <action>
    1. **stock_chart_data.py** — Extend `compute_chart_stats()`:
       - Add parameter `state: AnalysisState | None = None` (optional, for backward compat)
       - When state is provided, read decomposition from state.extracted.market.stock:
         - For "1Y" period: add stats keys "market_contribution", "sector_contribution", "company_residual" from returns_1y_market.value, returns_1y_sector.value, returns_1y_company.value (check for None)
         - For "5Y" period: same from returns_5y_* fields
       - Add "mdd_ratio" key: read from state.extracted.market.stock.mdd_ratio_1y.value (1Y) or mdd_ratio_5y.value (5Y)
       - Add "sector_mdd" key from sector_mdd_1y or sector_mdd_5y
       - Add "max_drawdown" key from max_drawdown_1y or max_drawdown_5y
       - Update all callers of compute_chart_stats to pass state when available

    2. **stock_chart_data.py** — Extend `extract_chart_data()`:
       - When extracting 1Y chart data, also check for history_2y in market_data. If present and the period is "1Y", consider using history_2y for the chart to show a longer date range. However, per anti-pattern in research: "The 1Y charts must continue to work. The 2Y data is an extension, not a replacement." So keep the 1Y chart using history_1y. The 2Y data already feeds metric computation via Plan 01.
       - No change needed here for chart rendering — charts stay 1Y and 5Y.

    3. **drawdown_chart.py** — Add MDD ratio to stats header:
       - Find where the drawdown chart renders its stats/header text
       - Add MDD ratio display: "MDD Ratio: {ratio}x" (e.g., "MDD Ratio: 1.8x" meaning company's MDD is 1.8x the sector's)
       - Show sector MDD alongside company MDD for context: "Company MDD: -18.5% | Sector MDD: -10.3% | Ratio: 1.8x"

    4. **html_renderer.py** — Add return decomposition data to template context:
       - In the function that builds the stock analysis section context, add:
         - `return_decomposition_1y`: dict with market, sector, company, total (or None if not computed)
         - `return_decomposition_5y`: same for 5Y
         - `mdd_ratio_1y`, `mdd_ratio_5y`: float or None
         - `sector_mdd_1y`, `sector_mdd_5y`: float or None
       - Read these from state.extracted.market.stock fields (using .value if SourcedValue, else None)

    5. **stock_analysis.html template** — Add return decomposition display:
       - After the stock charts section, add a "Return Attribution" table:
         ```
         | Component       |    1Y    |    5Y    |
         |-----------------|----------|----------|
         | Total Return    |  +12.3%  |  +45.6%  |
         | Market (SPY)    |   +8.1%  |  +38.2%  |
         | Sector Excess   |   +2.5%  |   +3.1%  |
         | Company-Specific|   +1.7%  |   +4.3%  |
         ```
       - Style as a compact table matching the existing CIQ-style layout patterns
       - Add MDD comparison row or section near the drawdown discussion:
         ```
         MDD Ratio (1Y): 1.8x sector | MDD Ratio (5Y): 1.2x sector
         ```
       - Use `| format_na` filter for None values. Use conditional rendering: only show decomposition table if at least 1Y decomposition data exists.

    IMPORTANT: Find the correct template file by checking what template currently renders the stock analysis section. It may be in a different path than listed — search for "stock" in the templates directory. The html_renderer context builder function may be in html_context_builder.py or similar — follow the import chain from html_renderer.py.
  </action>
  <verify>
    <automated>uv run pytest tests/stages/render/charts/ tests/stages/render/ -x -q --timeout=60</automated>
  </verify>
  <done>
    - compute_chart_stats returns decomposition and MDD ratio keys when state is provided
    - Drawdown chart header shows MDD ratio alongside company/sector MDD
    - HTML template renders return attribution table with 1Y and 5Y columns
    - HTML template shows MDD ratio near drawdown section
    - All render tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify data foundation output quality</name>
  <action>
    Human visual verification of the complete data foundation: 2Y acquisition, return decomposition (market + sector + company-specific), and peer-relative MDD ratio displayed in HTML output.

    Steps:
    1. Run: `underwrite AAPL --fresh`
    2. Open the HTML output file in browser
    3. Verify stock charts section:
       - Charts still render correctly (1Y and 5Y)
       - Return attribution table appears with 1Y and 5Y columns
       - Three components (Market, Sector Excess, Company-Specific) are shown
       - Components sum to Total Return (check the math)
    4. Verify drawdown section:
       - MDD ratio displayed for 1Y and 5Y
       - Company MDD and Sector MDD shown for context
       - Ratio makes intuitive sense (e.g., AAPL should not have wildly different MDD than XLK)
    5. Verify data completeness:
       - No "N/A" where real data should exist
       - No raw dict/object strings leaking into display
  </action>
  <verify>Visual inspection of HTML output</verify>
  <done>User confirms return decomposition and MDD ratio display correctly in HTML output</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/ -x -q --timeout=60` — full suite green
2. `uv run python -c "from do_uw.stages.render.charts.stock_chart_data import compute_chart_stats; print('Import OK')"` — no import errors
3. Visual: HTML output shows return decomposition and MDD ratio
</verification>

<success_criteria>
- Return decomposition table visible in HTML with 1Y + 5Y columns, three components summing to total
- MDD ratio displayed for 1Y and 5Y, showing company vs sector drawdown context
- All stock charts continue rendering correctly
- Full test suite passes
- Underwriter can see at a glance whether returns and drawdowns are market-driven or company-specific
</success_criteria>

<output>
After completion, create `.planning/phases/88-data-foundation/88-02-SUMMARY.md`
</output>
