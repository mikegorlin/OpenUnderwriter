---
phase: 123-market-condensation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/stages/render/context_builders/_market_display.py
  - src/do_uw/stages/render/context_builders/market.py
  - src/do_uw/stages/render/context_builders/governance.py
  - tests/stages/render/test_market_condensation.py
autonomous: true
requirements: [MARKET-01, MARKET-02, MARKET-03, MARKET-04, MARKET-05]

must_haves:
  truths:
    - "Insider data context contains at most 10 sale transactions and 5 other transactions"
    - "Stock drop events are filtered to 1 year lookback and limited to top 5 by severity"
    - "Institutional holders limited to top 10 in main context"
    - "Full overflow data preserved in separate context keys for audit appendix"
    - "Stock charts context distinguishes main-body charts (1Y only) from audit charts (5Y)"
  artifacts:
    - path: "src/do_uw/stages/render/context_builders/_market_display.py"
      provides: "Condensed build_insider_data, build_drop_events with overflow output"
      contains: "transactions_overflow"
    - path: "src/do_uw/stages/render/context_builders/market.py"
      provides: "extract_market with condensed + overflow context keys"
      contains: "drop_events_overflow"
    - path: "src/do_uw/stages/render/context_builders/governance.py"
      provides: "Top 10 holders in main, overflow for audit"
      contains: "top_holders_overflow"
    - path: "tests/stages/render/test_market_condensation.py"
      provides: "Tests for all condensation limits and overflow preservation"
      min_lines: 80
  key_links:
    - from: "src/do_uw/stages/render/context_builders/_market_display.py"
      to: "src/do_uw/stages/render/context_builders/market.py"
      via: "build_insider_data and build_drop_events return condensed + overflow"
      pattern: "transactions_overflow|drop_events_overflow"
    - from: "src/do_uw/stages/render/context_builders/market.py"
      to: "templates (Plan 02)"
      via: "context dict keys consumed by Jinja2 templates"
      pattern: "result\\[.overflow"
---

<objective>
Condense all market context builders to produce top-N limited data for the main worksheet body, while preserving full overflow data in separate context keys for the audit layer appendix.

Purpose: The Market section renders 52K+ lines because context builders pass unlimited transaction lists, drop histories, holder tables, and 12 chart embeds to templates. This plan caps the data at the source (context builders) so templates in Plan 02 can render condensed main-body and full audit-appendix views from distinct context keys.

Output: Modified context builders producing `{key}` (condensed, top-N) and `{key}_overflow` (remaining items) for insider transactions, stock drops, institutional holders, and chart references. Test suite proving limits hold.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-manifest-reorder-audit-layer/122-02-SUMMARY.md

<interfaces>
<!-- Key functions and signatures the executor needs -->

From src/do_uw/stages/render/context_builders/_market_display.py:
```python
def build_insider_data(mkt: Any) -> dict[str, Any]:
    # Current: sale_rows[:20], other_rows[:15]
    # Keys: transactions, other_transactions, sale_count, other_count, ownership_alerts, cluster_details

def build_drop_events(drops: Any) -> list[dict[str, str]]:
    # Current: sorted_drops[:12], no date filter
    # Returns list of dicts with date, drop_pct, trigger, recovery, etc.

def _build_transaction_rows(transactions: list[Any]) -> tuple[list, list]:
    # Current: transactions[:40]
```

From src/do_uw/stages/render/context_builders/market.py:
```python
def extract_market(state: AnalysisState, *, signal_results: dict[str, Any] | None = None) -> dict[str, Any]:
    # Calls build_insider_data, build_drop_events, build_earnings_guidance
    # Returns result dict consumed by market templates
```

From src/do_uw/stages/render/context_builders/governance.py (line ~96-110):
```python
# Top holders -- currently sliced to [:5]
holders: list[dict[str, str]] = []
if ownership.top_holders:
    for holder in ownership.top_holders[:5]:
        # ...builds holder dicts
result["top_holders"] = holders
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Condensation limits in context builders with overflow preservation</name>
  <files>
    src/do_uw/stages/render/context_builders/_market_display.py
    src/do_uw/stages/render/context_builders/market.py
    src/do_uw/stages/render/context_builders/governance.py
    tests/stages/render/test_market_condensation.py
  </files>
  <read_first>
    src/do_uw/stages/render/context_builders/_market_display.py
    src/do_uw/stages/render/context_builders/market.py
    src/do_uw/stages/render/context_builders/governance.py
    tests/stages/render/test_market_context_drops.py
  </read_first>
  <behavior>
    - Test: build_insider_data returns at most 10 items in "transactions" key
    - Test: build_insider_data returns at most 5 items in "other_transactions" key
    - Test: build_insider_data["transactions_overflow"] contains remaining sales beyond 10
    - Test: build_insider_data["other_transactions_overflow"] contains remaining beyond 5
    - Test: build_insider_data["sale_count"] still reflects TOTAL sale count (not truncated count)
    - Test: build_drop_events returns at most 5 items when lookback_days=365
    - Test: build_drop_events filters out events older than lookback_days from start of list
    - Test: build_drop_events second return value (overflow) contains ALL events including old ones
    - Test: governance extract_governance result["top_holders"] has at most 10 entries
    - Test: governance extract_governance result["top_holders_overflow"] contains entries 11+
    - Test: extract_market result contains "main_charts" list with only 1Y chart keys
    - Test: extract_market result contains "audit_charts" list with 5Y + drop analysis chart keys
  </behavior>
  <action>
**_market_display.py changes:**

1. In `build_insider_data()`:
   - Change `sale_rows[:20]` to `sale_rows[:10]` for `insider_data["transactions"]`
   - Add `insider_data["transactions_overflow"] = sale_rows[10:]` (remaining sales)
   - Change `other_rows[:15]` to `other_rows[:5]` for `insider_data["other_transactions"]`
   - Add `insider_data["other_transactions_overflow"] = other_rows[5:]`
   - Keep `sale_count` and `other_count` as total counts (already correct -- they use len())

2. In `_build_transaction_rows()`:
   - Change `transactions[:40]` to process ALL transactions (remove the [:40] slice) -- the truncation now happens in build_insider_data, and we need full data for overflow

3. In `build_drop_events()`:
   - Change signature to `build_drop_events(drops: Any, *, lookback_days: int = 365, limit: int = 5) -> tuple[list[dict[str, str]], list[dict[str, str]]]`
   - Return a TUPLE: (condensed_events, all_events)
   - First filter `all_drops` by date: keep only events where `evt.date.value` is within `lookback_days` of today for the condensed list
   - Sort by decay_weighted_severity descending (existing logic)
   - `condensed_events` = sorted[:limit] (top 5 within 1Y)
   - `all_events` = full sorted list (no date filter, no limit) for audit appendix
   - Use `from datetime import date, timedelta` for date comparison; parse evt.date.value with `date.fromisoformat()` if it's a string

**market.py changes:**

4. In `extract_market()`:
   - Update call to `build_drop_events(drops)` to unpack tuple: `condensed_drops, all_drops = build_drop_events(drops)`
   - Set `result["drop_events"] = condensed_drops` (main body)
   - Set `result["drop_events_overflow"] = all_drops` (audit appendix -- full list)
   - Add chart classification:
     ```python
     result["main_charts"] = ["stock_1y"]
     result["audit_charts"] = ["stock_5y", "drop_analysis_1y", "drop_analysis_5y",
                                "drawdown_1y", "drawdown_5y", "volatility_1y", "volatility_5y",
                                "relative_1y", "relative_5y", "drop_scatter_1y", "drop_scatter_5y"]
     ```

**governance.py changes:**

5. In `extract_governance()`:
   - Change `ownership.top_holders[:5]` to `ownership.top_holders[:10]`
   - After building the holders list from [:10], build overflow from [10:]
   - Set `result["top_holders_overflow"] = overflow_holders`

**Test file:**

6. Create `tests/stages/render/test_market_condensation.py` with tests for all the behaviors listed above. Use mock state objects following patterns from existing `test_market_context_drops.py`. Test:
   - Transaction limits (10 sales, 5 other, overflow contains remainder)
   - Drop event date filtering (events older than 365 days excluded from condensed, present in overflow)
   - Drop event limit (top 5 by severity)
   - Holder limits (10 main, overflow for rest)
   - Chart classification (main_charts vs audit_charts)
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/render/test_market_condensation.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "transactions_overflow" src/do_uw/stages/render/context_builders/_market_display.py returns >= 1
    - grep -c "drop_events_overflow" src/do_uw/stages/render/context_builders/market.py returns >= 1
    - grep -c "top_holders_overflow" src/do_uw/stages/render/context_builders/governance.py returns >= 1
    - grep -c "main_charts" src/do_uw/stages/render/context_builders/market.py returns >= 1
    - grep -c "def test_" tests/stages/render/test_market_condensation.py returns >= 8
  </acceptance_criteria>
  <done>
    Context builders produce condensed main-body data (10 insider transactions, 5 drop events within 1Y, 10 holders, 1 main chart ref) and full overflow data in separate keys. All condensation tests pass. Existing test suite unbroken.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify existing tests still pass with condensation changes</name>
  <files>
    tests/stages/render/test_market_context_drops.py
  </files>
  <read_first>
    tests/stages/render/test_market_context_drops.py
  </read_first>
  <action>
Run the existing market context test suite. The signature change to `build_drop_events` (now returns tuple instead of list) will likely break existing callers in tests.

1. Run `uv run pytest tests/stages/render/test_market_context_drops.py -x -v` to find failures
2. Update any test that calls `build_drop_events()` directly to unpack the tuple return: `condensed, full = build_drop_events(...)`
3. Update any test that checks `result["drop_events"]` from `extract_market()` -- the condensed list is now limited to 5 events within 1Y, so assertions about count or content may need adjustment
4. Run the full render test suite: `uv run pytest tests/stages/render/ -x --timeout=60`
5. Fix any other breakage from the governance.py holder limit change (was [:5], now [:10])

Do NOT weaken test assertions -- if a test previously checked for 12 drop events, update it to verify the condensed list has <= 5 AND the overflow has the rest. Strengthen tests where possible.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/render/ -x --timeout=60</automated>
  </verify>
  <acceptance_criteria>
    - uv run pytest tests/stages/render/ -x --timeout=60 exits with code 0
    - grep -c "build_drop_events" tests/stages/render/test_market_context_drops.py shows updated tuple handling
  </acceptance_criteria>
  <done>
    All existing render tests pass with the new condensation limits. No test weakened -- overflow assertions added where appropriate.
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/stages/render/test_market_condensation.py -x -v` -- all new condensation tests pass
2. `uv run pytest tests/stages/render/ -x --timeout=60` -- all existing render tests pass
3. `grep -r "transactions_overflow\|drop_events_overflow\|top_holders_overflow" src/do_uw/stages/render/context_builders/` confirms overflow keys exist in all three builders
</verification>

<success_criteria>
- Context builders produce condensed data: 10 insider sales, 5 stock drops (1Y), 10 holders, 1Y-only main charts
- Overflow data preserved in `*_overflow` keys for audit appendix consumption
- Total counts (sale_count, other_count) still reflect full dataset size
- All render tests pass (new + existing)
</success_criteria>

<output>
After completion, create `.planning/phases/123-market-condensation/123-01-SUMMARY.md`
</output>
