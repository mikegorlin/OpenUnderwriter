---
phase: 123-market-condensation
plan: 02
type: execute
wave: 2
depends_on: ["123-01"]
files_modified:
  - src/do_uw/templates/html/sections/market/stock_charts.html.j2
  - src/do_uw/templates/html/sections/market/insider_trading.html.j2
  - src/do_uw/templates/html/sections/market/stock_drops.html.j2
  - src/do_uw/templates/html/sections/market/stock_performance.html.j2
  - src/do_uw/templates/html/appendices/market_overflow.html.j2
  - src/do_uw/brain/output_manifest.yaml
  - src/do_uw/stages/extract/dossier_extraction.py
  - tests/stages/render/test_market_templates.py
autonomous: false
requirements: [MARKET-01, MARKET-02, MARKET-03, MARKET-04, MARKET-05, MARKET-06, FIX-01]

must_haves:
  truths:
    - "Market section in main body renders under 5K lines for a typical ticker"
    - "Insider trading table shows exactly top 10 transactions with overflow count and audit link"
    - "Stock drop table shows top 5 events from past 1 year only"
    - "Only the 1Y stock chart appears in the main body"
    - "Full insider list, all holders, all drops, and all charts accessible in audit appendix"
    - "Revenue segments populated for AAPL (FIX-01)"
  artifacts:
    - path: "src/do_uw/templates/html/sections/market/stock_charts.html.j2"
      provides: "Condensed chart section -- 1Y price chart only in main body"
      contains: "main_charts"
    - path: "src/do_uw/templates/html/sections/market/insider_trading.html.j2"
      provides: "Top 10 insider sales with overflow summary"
      contains: "transactions_overflow"
    - path: "src/do_uw/templates/html/sections/market/stock_drops.html.j2"
      provides: "Top 5 drops (1Y) with overflow link"
      contains: "drop_events_overflow"
    - path: "src/do_uw/templates/html/appendices/market_overflow.html.j2"
      provides: "Audit appendix with full market data"
      contains: "audit-market-overflow"
    - path: "src/do_uw/brain/output_manifest.yaml"
      provides: "market_overflow section in audit layer"
      contains: "market_overflow"
    - path: "tests/stages/render/test_market_templates.py"
      provides: "Template rendering tests for condensed output"
      min_lines: 60
  key_links:
    - from: "src/do_uw/templates/html/appendices/market_overflow.html.j2"
      to: "src/do_uw/brain/output_manifest.yaml"
      via: "manifest audit layer entry pointing to overflow template"
      pattern: "market_overflow"
    - from: "src/do_uw/templates/html/sections/market/insider_trading.html.j2"
      to: "src/do_uw/templates/html/appendices/market_overflow.html.j2"
      via: "anchor link from condensed view to audit appendix"
      pattern: "href.*audit-market"
---

<objective>
Rewrite market templates to render condensed main-body content (top 10 insiders, top 5 drops, 1Y chart) and create an audit appendix template that renders full overflow data. Fix AAPL revenue segment extraction (FIX-01).

Purpose: Plan 01 produced condensed context data with overflow keys. This plan rewires the Jinja2 templates to consume those keys, producing a <5K line main-body Market section while preserving all data in the audit appendix. Also fixes the AAPL revenue segment extraction gap.

Output: Condensed market templates, audit overflow appendix template registered in manifest, revenue segment fix, visual verification against a real ticker.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-market-condensation/123-01-SUMMARY.md

<interfaces>
<!-- Context keys available from Plan 01 context builders -->

From extract_market() result dict:
```python
# Condensed (main body)
result["drop_events"]          # list[dict] -- top 5 within 1Y by severity
result["insider_data"]["transactions"]  # list[dict] -- top 10 sales
result["insider_data"]["other_transactions"]  # list[dict] -- top 5 other
result["insider_data"]["sale_count"]    # int -- TOTAL sale count
result["insider_data"]["other_count"]   # int -- TOTAL other count
result["main_charts"]          # ["stock_1y"]
result["audit_charts"]         # ["stock_5y", "drop_analysis_1y", ...]

# Overflow (audit appendix)
result["drop_events_overflow"]  # list[dict] -- ALL drops, no limit
result["insider_data"]["transactions_overflow"]  # list[dict] -- sales 11+
result["insider_data"]["other_transactions_overflow"]  # list[dict] -- other 6+
```

From extract_governance() result dict:
```python
result["top_holders"]          # list[dict] -- top 10
result["top_holders_overflow"] # list[dict] -- holders 11+
```

From output_manifest.yaml audit layer pattern:
```yaml
- id: market_overflow
  name: Market Data -- Full Detail
  template: appendices/market_overflow.html.j2
  render_as: data_table
  layer: audit
```

From worksheet.html.j2 Zone 4:
```html
<details class="audit-layer">
  {% for section in manifest_sections %}
    {% if section.layer == 'audit' %}
    {% include section.template %}
    {% endif %}
  {% endfor %}
</details>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Condense market templates and create audit overflow appendix</name>
  <files>
    src/do_uw/templates/html/sections/market/stock_charts.html.j2
    src/do_uw/templates/html/sections/market/insider_trading.html.j2
    src/do_uw/templates/html/sections/market/stock_drops.html.j2
    src/do_uw/templates/html/appendices/market_overflow.html.j2
    src/do_uw/brain/output_manifest.yaml
    tests/stages/render/test_market_templates.py
  </files>
  <read_first>
    src/do_uw/templates/html/sections/market/stock_charts.html.j2
    src/do_uw/templates/html/sections/market/insider_trading.html.j2
    src/do_uw/templates/html/sections/market/stock_drops.html.j2
    src/do_uw/templates/html/sections/market/stock_drop_catalyst.html.j2
    src/do_uw/templates/html/sections/market/stock_performance_summary.html.j2
    src/do_uw/brain/output_manifest.yaml
    src/do_uw/templates/html/worksheet.html.j2
  </read_first>
  <action>
**stock_charts.html.j2 -- show only 1Y chart in main body:**

1. Replace the current template content with a condensed version:
   - Keep ONLY the 1Y stock price chart: `embed_chart("stock_1y", ...)` with figure_num=1
   - Keep the Return Attribution table (it's compact KV data, not chart bloat)
   - REMOVE all 5Y charts, drop analysis charts, drawdown charts, volatility charts, relative performance charts, drop scatter charts from the main body
   - Keep the D&O Risk Flags and Positive Market Indicators sections (they're compact text)
   - Keep the MDD Ratio comparison boxes (compact)
   - Add a note after the 1Y chart: `<p class="text-xs text-gray-400 mt-1">Full chart analysis (5Y, drawdown, volatility, relative performance) available in <a href="#audit-market-overflow">Audit Trail</a>.</p>`

**insider_trading.html.j2 -- top 10 sales:**

2. The template already iterates `{% for t in sale_txns %}` where `sale_txns = ins.get('transactions', [])`. Since Plan 01 limits transactions to 10, the template renders correctly WITHOUT changes to the for loop.
   - After the sales table closing `</table>`, add an overflow notice if more exist:
     ```html
     {% set overflow_sales = ins.get('transactions_overflow', []) %}
     {% if overflow_sales %}
     <p class="text-xs text-gray-400 mt-1">Showing top 10 of {{ ins.get('sale_count', 0) }} sales. <a href="#audit-market-overflow" class="text-blue-600 hover:underline">See all {{ ins.get('sale_count', 0) }} in Audit Trail</a>.</p>
     {% endif %}
     ```
   - Do the same for other_transactions overflow after the `</details>` for grants/exercises

**stock_drops.html.j2 -- top 5 events (1Y):**

3. The template iterates `{% for evt in drop_events %}` which Plan 01 already limits to 5. Add overflow notice:
   - After the closing `</table>` (line ~115), before the footnote `<p>`:
     ```html
     {% set overflow_drops = mkt.get('drop_events_overflow', []) %}
     {% if overflow_drops|length > drop_events|length %}
     <p class="text-xs text-gray-400 mt-1">Showing top {{ drop_events|length }} drops from past 12 months. <a href="#audit-market-overflow" class="text-blue-600 hover:underline">See all {{ overflow_drops|length }} historical drops in Audit Trail</a>.</p>
     {% endif %}
     ```

**Create src/do_uw/templates/html/appendices/market_overflow.html.j2:**

4. New template for the audit appendix. This renders inside Zone 4 (`<details class="audit-layer">`). Structure:
   - `<div id="audit-market-overflow" class="audit-section">` wrapper with anchor target
   - `<h3>Market Data -- Full Detail</h3>` heading
   - Full Stock Charts section: render ALL 12 charts in 2-column grid (1Y+5Y for price, drop analysis, drawdown, volatility, relative, drop scatter) using `embed_chart` macro from `components/charts.html.j2`
   - Full Insider Transactions table: combine `ins.get('transactions', []) + ins.get('transactions_overflow', [])` into one table with columns: Insider, Date, Type, Shares, Value
   - Full Stock Drops table: render `mkt.get('drop_events_overflow', [])` with columns: Date, Drop, Trigger, Recovery, Company-Specific
   - Use compact text-xs styling throughout (this is reference data, not primary analysis)

**output_manifest.yaml -- add audit entry:**

5. Add a `market_overflow` entry to the manifest. Insert it in the audit layer section (after the existing audit entries around line 715-727):
   ```yaml
   - id: market_overflow
     name: Market Data Full Detail
     template: appendices/market_overflow.html.j2
     render_as: data_table
     layer: audit
   ```

**Test file:**

6. Create `tests/stages/render/test_market_templates.py` to verify:
   - The `market_overflow` section exists in manifest with `layer: audit`
   - The market_overflow template file exists
   - Insider overflow link contains `href="#audit-market-overflow"`
   - Drop overflow link contains `href="#audit-market-overflow"`
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/render/test_market_templates.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "audit-market-overflow" src/do_uw/templates/html/appendices/market_overflow.html.j2 returns >= 1
    - grep -c "transactions_overflow" src/do_uw/templates/html/sections/market/insider_trading.html.j2 returns >= 1
    - grep -c "drop_events_overflow" src/do_uw/templates/html/sections/market/stock_drops.html.j2 returns >= 1
    - grep -c "market_overflow" src/do_uw/brain/output_manifest.yaml returns >= 1
    - grep -c "layer: audit" src/do_uw/brain/output_manifest.yaml | result includes market_overflow entry
  </acceptance_criteria>
  <done>
    Market templates render condensed main-body content with overflow links to audit appendix. Audit appendix template renders full data. Manifest includes market_overflow in audit layer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix AAPL revenue segment extraction (FIX-01)</name>
  <files>
    src/do_uw/stages/extract/dossier_extraction.py
  </files>
  <read_first>
    src/do_uw/stages/extract/dossier_extraction.py
    src/do_uw/stages/extract/llm/schemas/dossier.py
    src/do_uw/stages/extract/profile_item1_helpers.py
    src/do_uw/templates/html/sections/company/revenue_segments.html.j2
  </read_first>
  <action>
Investigate why AAPL shows 0 revenue segments and fix the extraction.

1. Read `dossier_extraction.py` to understand how segment_dossiers are populated. The extraction likely parses 10-K Item 1 text using LLM extraction with the dossier schema.

2. The issue is likely one of:
   a. AAPL's 10-K segments are in a non-standard format (geographic vs product segments)
   b. The LLM prompt doesn't explicitly ask for revenue segments
   c. The extraction doesn't handle XBRL segment data

3. Investigate AAPL's actual segment reporting:
   - AAPL reports by product line (iPhone, Mac, iPad, Wearables, Services) AND geography (Americas, Europe, Greater China, Japan, Rest of Asia)
   - These appear in the 10-K and in XBRL as operating segments

4. Fix the extraction:
   - If the dossier extraction LLM prompt doesn't explicitly request "revenue by segment/product line", add it
   - If there's a fallback that reads XBRL segment data, ensure it's activated
   - If the issue is that `segment_dossiers` is populated but `revenue_segments` in the company profile is empty, trace the data flow and fix the mapping

5. The fix should be targeted -- modify only the extraction logic that's failing for AAPL. Do not restructure the entire dossier extraction.

6. Verify by checking that the extraction schema (`llm/schemas/dossier.py`) has fields for segment data and that the prompt instructs extraction of product/service revenue breakdowns.

NOTE: This is a data acquisition/extraction fix. Since v9.0 scope says "no new data acquisition sources", this fix must work with existing acquired data (10-K text already in state). The LLM extraction prompt may need enhancement to better identify segment disclosures in AAPL's 10-K format.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/extract/ -k "segment or dossier" -x -v --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "segment" src/do_uw/stages/extract/dossier_extraction.py shows segment extraction logic present
    - The LLM prompt or extraction logic explicitly handles product/service revenue breakdowns
  </acceptance_criteria>
  <done>
    Revenue segment extraction enhanced to capture AAPL's product-line segments (iPhone, Mac, iPad, Wearables, Services). Extraction tests pass. Full verification requires pipeline run in checkpoint task.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of condensed Market section</name>
  <files>output/AAPL_worksheet.html</files>
  <action>
Run `underwrite AAPL --fresh` to produce a full pipeline output. Then open the generated HTML and visually inspect the Market Activity section for condensation, audit overflow, and revenue segments.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/render/ -x --timeout=60</automated>
  </verify>
  <done>
    Human confirmed: Market section condensed (under 5K lines), audit overflow accessible with full data, revenue segments populated for AAPL.
  </done>
  <what-built>
    Condensed Market section: top 10 insider sales, top 5 stock drops (1Y), single 1Y chart in main body. Full data in audit appendix. Revenue segments fix for AAPL.
  </what-built>
  <how-to-verify>
    1. Run `underwrite AAPL --fresh` (full pipeline with fresh data)
    2. Open the generated HTML worksheet
    3. Scroll to Market Activity section and verify:
       - Only 1 stock chart visible (1Y price chart) -- NOT 12 charts
       - Insider trading shows at most 10 sale rows with "See all N in Audit Trail" link
       - Stock drops shows at most 5 events with "See all N in Audit Trail" link
       - No raw data leaks or broken formatting
    4. Click "Audit Trail" at bottom, expand it, scroll to "Market Data -- Full Detail":
       - All 12 charts present (1Y+5Y for price, drawdown, volatility, relative, drop analysis, drop scatter)
       - Full insider transaction list present
       - Full stock drop history present
    5. Check Revenue Segments in Company and Operations section:
       - AAPL should show product segments (iPhone, Mac, iPad, Wearables/Home/Acc, Services)
       - NOT "0 segments"
    6. Estimate total line count of Market section (view source, search for market section boundaries)
       - Target: under 5,000 lines
  </how-to-verify>
  <resume-signal>Type "approved" if Market section is condensed, audit overflow works, and revenue segments populated. Or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. `uv run pytest tests/stages/render/test_market_condensation.py tests/stages/render/test_market_templates.py -x -v` -- all condensation and template tests pass
2. `uv run pytest tests/stages/render/ -x --timeout=60` -- full render test suite passes
3. Pipeline run on AAPL produces Market section under 5K lines with overflow in audit appendix
4. Revenue segments populated for AAPL
</verification>

<success_criteria>
- Market and Trading section HTML under 5,000 lines (MARKET-01)
- Insider trading shows top 10 transactions (MARKET-02)
- Institutional holders show top 10 (MARKET-03)
- Stock drops show 1Y history, top 5 events (MARKET-04)
- Single 1Y chart in main body (MARKET-05)
- Full data in audit appendix (MARKET-06)
- Revenue segments populated for AAPL (FIX-01)
- Human visually verified the condensed output
</success_criteria>

<output>
After completion, create `.planning/phases/123-market-condensation/123-02-SUMMARY.md`
</output>
