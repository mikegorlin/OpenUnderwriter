# Deferred Items - Phase 123

## Pre-existing Test Failures (not caused by Phase 123)

1. **financials_evaluative.py line limit** - `test_builder_under_line_limit[financials_evaluative.py]` - file is 347 lines (limit 300)
2. **html_signals grouped entry keys** - `TestGroupSignalsBySection::test_grouped_entry_has_required_keys` - `do_context` key not in expected set
