---
phase: 89-statistical-analysis
plan: 02
type: execute
wave: 2
depends_on: [89-01]
files_modified:
  - src/do_uw/stages/extract/stock_performance.py
  - src/do_uw/stages/render/charts/volatility_chart.py
  - src/do_uw/stages/render/sect2_company_hazard.py
  - tests/stages/extract/test_stock_performance.py
autonomous: true
requirements: [STOCK-02, STOCK-04, STOCK-05]

must_haves:
  truths:
    - "After extraction, StockPerformance.ewma_vol_current and vol_regime are populated from 2Y daily price data"
    - "After extraction, each StockDropEvent has abnormal_return_pct and t-stat computed via market model"
    - "After extraction, StockDropAnalysis.ddl_exposure shows market_cap x worst_drop with 1.8% settlement estimate"
    - "Volatility chart shows both rolling vol and EWMA vol lines with regime background shading"
    - "DDL exposure dollar amount and settlement estimate appear in the company hazard / pricing section HTML"
    - "Statistically significant drop days (|t| >= 1.96) are visually flagged in drop detail display"
  artifacts:
    - path: "src/do_uw/stages/extract/stock_performance.py"
      provides: "Wiring of EWMA, AR, DDL computations into extraction pipeline"
    - path: "src/do_uw/stages/render/charts/volatility_chart.py"
      provides: "EWMA line + regime shading on volatility chart"
    - path: "src/do_uw/stages/render/sect2_company_hazard.py"
      provides: "DDL exposure and settlement estimate display"
  key_links:
    - from: "src/do_uw/stages/extract/stock_performance.py"
      to: "chart_computations.py"
      via: "import compute_ewma_volatility, compute_abnormal_return, compute_ddl_exposure, classify_vol_regime"
      pattern: "from do_uw.stages.render.charts.chart_computations import"
    - from: "src/do_uw/stages/render/charts/volatility_chart.py"
      to: "StockPerformance model"
      via: "reads ewma_vol series and vol_regime for chart rendering"
      pattern: "ewma_vol|vol_regime"
    - from: "src/do_uw/stages/render/sect2_company_hazard.py"
      to: "StockDropAnalysis model"
      via: "reads ddl_exposure and ddl_settlement_estimate for pricing display"
      pattern: "ddl_exposure|ddl_settlement"
---

<objective>
Wire the statistical computations from Plan 01 into the extraction pipeline (stock_performance.py) so model fields are populated during a pipeline run, then display EWMA volatility + regimes on the volatility chart, DDL exposure in the pricing section, and abnormal return significance flags on drop events.

Purpose: Make the new statistical analysis visible to underwriters in the HTML output.
Output: Fully integrated extraction-to-rendering pipeline for DDL, AR, EWMA, and regime features.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/89-statistical-analysis/89-RESEARCH.md
@.planning/phases/89-statistical-analysis/89-01-SUMMARY.md

@src/do_uw/stages/extract/stock_performance.py
@src/do_uw/stages/render/charts/volatility_chart.py
@src/do_uw/stages/render/charts/chart_computations.py
@src/do_uw/stages/render/sect2_company_hazard.py
@src/do_uw/models/market.py
@src/do_uw/models/market_events.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire computations into extraction pipeline and add integration tests</name>
  <files>src/do_uw/stages/extract/stock_performance.py, tests/stages/extract/test_stock_performance.py</files>
  <action>
In stock_performance.py, after existing stock metrics are computed (look for where StockPerformance and StockDropAnalysis fields are populated), add calls to the new computation functions:

1. **EWMA + Regime**: After stock prices are available:
   - Call `compute_ewma_volatility(close_prices)` to get EWMA series
   - Call `classify_vol_regime(ewma_series)` to get regime + duration
   - Store `ewma_vol_current` (last value as SourcedValue), `vol_regime` (as SourcedValue), `vol_regime_duration_days` on StockPerformance
   - Source: "yfinance/computed", confidence: MEDIUM

2. **Abnormal Returns per drop**: After drop events are identified:
   - For each StockDropEvent, find its index in the daily returns series
   - Call `compute_abnormal_return(company_returns, spy_returns, event_idx)`
   - Store `abnormal_return_pct`, `abnormal_return_t_stat`, `is_statistically_significant`, `market_model_alpha`, `market_model_beta` on each StockDropEvent
   - Skip (leave None) if compute_abnormal_return returns None (insufficient data)

3. **DDL Exposure**: After worst_single_day is identified on StockDropAnalysis:
   - Get market_cap from StockPerformance.market_cap_yf
   - Get worst_drop_pct from worst_single_day.drop_pct.value
   - Get max_drawdown from StockPerformance.max_drawdown_1y.value (for MDL)
   - Call `compute_ddl_exposure(market_cap, worst_drop_pct, max_drawdown_pct)`
   - Store `ddl_exposure`, `mdl_exposure`, `ddl_settlement_estimate` as SourcedValues on StockDropAnalysis
   - Source: "yfinance/computed", confidence: MEDIUM

Add integration tests in tests/stages/extract/test_stock_performance.py:
- test_extraction_populates_ewma_and_regime: mock price data, verify ewma_vol_current and vol_regime populated
- test_extraction_populates_abnormal_returns: mock drops + price series, verify AR fields on drop events
- test_extraction_populates_ddl_exposure: mock market_cap + worst drop, verify DDL and settlement fields

Import from chart_computations: `compute_ewma_volatility`, `compute_abnormal_return`, `compute_ddl_exposure`, `classify_vol_regime`.
  </action>
  <verify>
    <automated>uv run pytest tests/stages/extract/test_stock_performance.py -x -q -k "ewma or abnormal or ddl"</automated>
  </verify>
  <done>Extraction pipeline populates all new model fields. EWMA vol + regime on StockPerformance, AR + t-stat on each StockDropEvent, DDL + settlement on StockDropAnalysis.</done>
</task>

<task type="auto">
  <name>Task 2: Add EWMA line and regime shading to volatility chart, DDL display to pricing section, AR flags to drops</name>
  <files>src/do_uw/stages/render/charts/volatility_chart.py, src/do_uw/stages/render/sect2_company_hazard.py</files>
  <action>
1. **Volatility chart** (volatility_chart.py): In create_volatility_chart():
   - Accept EWMA vol series and regime data (from chart_stats or state)
   - Add a second line to the top volatility subplot: EWMA vol in a distinct color (e.g., orange dashed) alongside the existing rolling vol line (blue solid)
   - Add legend distinguishing "Rolling 30d Vol" vs "EWMA Vol (lambda=0.94)"
   - Add regime background shading using axvspan: green tint for LOW, no tint for NORMAL, amber tint for ELEVATED, red tint for CRISIS (use very low alpha ~0.1 so it's subtle)
   - Show current regime label + duration in the chart header stats area (e.g., "Regime: ELEVATED (47 days)")

2. **DDL exposure display** (sect2_company_hazard.py): In the classification/pricing table area:
   - Add a row or card showing "Observed DDL Exposure" = formatted dollar amount (e.g., "$1.5B")
   - Add "Est. Settlement (1.8%)" = formatted dollar amount (e.g., "$27.0M")
   - Add "Observed MDL Exposure" if available
   - Label clearly as "Based on actual worst observed drop" to distinguish from the existing prospective DDL
   - Use the existing format_currency or similar helper for dollar formatting

3. **AR significance flags**: In whichever template/renderer shows drop event details:
   - For drops with is_statistically_significant=True, add a visual indicator (e.g., a badge or icon)
   - Show the t-stat value next to the drop percentage
   - Only show AR data for drops where abnormal_return_pct is not None

Run existing render tests to confirm no regressions.
  </action>
  <verify>
    <automated>uv run pytest tests/stages/render/ -x -q --timeout=60</automated>
  </verify>
  <done>Volatility chart shows dual vol lines (rolling + EWMA) with regime background shading. DDL exposure and settlement estimate displayed in pricing section. Drop events show t-stat and significance flag.</done>
</task>

</tasks>

<verification>
uv run pytest tests/ -x -q --timeout=120
</verification>

<success_criteria>
- Pipeline run populates ewma_vol_current, vol_regime, vol_regime_duration_days on StockPerformance
- Pipeline run populates abnormal_return_pct and t-stat on each StockDropEvent
- Pipeline run populates ddl_exposure and ddl_settlement_estimate on StockDropAnalysis
- Volatility chart renders EWMA line alongside rolling vol with regime shading
- Pricing section shows DDL exposure dollar amount and 1.8% settlement estimate
- Drop events show significance flags for |t| >= 1.96
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/89-statistical-analysis/89-02-SUMMARY.md`
</output>
