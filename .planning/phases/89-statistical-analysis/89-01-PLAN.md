---
phase: 89-statistical-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/stages/render/charts/chart_computations.py
  - src/do_uw/models/market.py
  - src/do_uw/models/market_events.py
  - tests/stages/render/charts/test_chart_computations.py
autonomous: true
requirements: [STOCK-02, STOCK-04, STOCK-05]

must_haves:
  truths:
    - "compute_ddl_exposure returns DDL = market_cap x |worst_drop_pct|/100 and settlement = DDL x 0.018"
    - "compute_abnormal_return returns AR, t-stat, and significance flag using 120-day estimation window with 5-day gap"
    - "compute_ewma_volatility returns annualized EWMA vol series with lambda=0.94"
    - "classify_vol_regime returns regime label (LOW/NORMAL/ELEVATED/CRISIS) and duration based on percentile thresholds"
    - "Model fields exist on StockPerformance, StockDropEvent, and StockDropAnalysis for all new computed values"
  artifacts:
    - path: "src/do_uw/stages/render/charts/chart_computations.py"
      provides: "compute_ddl_exposure, compute_ewma_volatility, compute_abnormal_return, classify_vol_regime"
    - path: "src/do_uw/models/market.py"
      provides: "ewma_vol_current, vol_regime, vol_regime_duration_days on StockPerformance"
    - path: "src/do_uw/models/market_events.py"
      provides: "abnormal_return fields on StockDropEvent, DDL fields on StockDropAnalysis"
    - path: "tests/stages/render/charts/test_chart_computations.py"
      provides: "Unit tests for all four new computation functions"
  key_links:
    - from: "tests/stages/render/charts/test_chart_computations.py"
      to: "chart_computations.py"
      via: "import and call compute functions"
      pattern: "from do_uw.stages.render.charts.chart_computations import"
---

<objective>
Add DDL exposure computation, abnormal return event study, EWMA volatility, and regime classification as pure computation functions in chart_computations.py, with corresponding model fields on StockPerformance/StockDropEvent/StockDropAnalysis, and comprehensive unit tests.

Purpose: Establish all statistical computation logic and data model contracts before wiring them into extraction and rendering (Plan 02).
Output: Four tested computation functions + model fields ready for integration.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-statistical-analysis/89-RESEARCH.md

@src/do_uw/stages/render/charts/chart_computations.py
@src/do_uw/models/market.py
@src/do_uw/models/market_events.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add model fields for DDL exposure, abnormal returns, and EWMA volatility</name>
  <files>src/do_uw/models/market.py, src/do_uw/models/market_events.py</files>
  <behavior>
    - StockPerformance has ewma_vol_current (SourcedValue[float] | None), vol_regime (SourcedValue[str] | None), vol_regime_duration_days (int | None) fields
    - StockDropEvent has abnormal_return_pct (float | None), abnormal_return_t_stat (float | None), is_statistically_significant (bool = False), market_model_alpha (float | None), market_model_beta (float | None) fields
    - StockDropAnalysis has ddl_exposure (SourcedValue[float] | None), mdl_exposure (SourcedValue[float] | None), ddl_settlement_estimate (SourcedValue[float] | None) fields
    - All new fields default to None/False so existing serialized state.json files remain compatible
  </behavior>
  <action>
Add fields to StockPerformance in market.py:
- `ewma_vol_current: SourcedValue[float] | None = Field(default=None, description="Current EWMA volatility (annualized %, lambda=0.94)")`
- `vol_regime: SourcedValue[str] | None = Field(default=None, description="Volatility regime: LOW / NORMAL / ELEVATED / CRISIS")`
- `vol_regime_duration_days: int | None = Field(default=None, description="Trading days in current volatility regime")`

Add fields to StockDropEvent in market_events.py:
- `abnormal_return_pct: float | None = Field(default=None, description="Abnormal return on drop day via market model (%)")`
- `abnormal_return_t_stat: float | None = Field(default=None, description="T-statistic for abnormal return")`
- `is_statistically_significant: bool = Field(default=False, description="True if |t-stat| >= 1.96")`
- `market_model_alpha: float | None = Field(default=None, description="Market model alpha from estimation window")`
- `market_model_beta: float | None = Field(default=None, description="Market model beta from estimation window")`

Add fields to StockDropAnalysis in market_events.py:
- `ddl_exposure: SourcedValue[float] | None = Field(default=None, description="DDL exposure: market_cap x worst_drop magnitude (USD)")`
- `mdl_exposure: SourcedValue[float] | None = Field(default=None, description="MDL exposure: market_cap x max_drawdown magnitude (USD)")`
- `ddl_settlement_estimate: SourcedValue[float] | None = Field(default=None, description="Estimated settlement: DDL x 1.8% (USD)")`
  </action>
  <verify>
    <automated>uv run pytest tests/models/ -x -q --timeout=30</automated>
  </verify>
  <done>All three model classes have new fields, existing tests pass, new fields default to None/False</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement DDL, abnormal return, EWMA, and regime computation functions with tests</name>
  <files>src/do_uw/stages/render/charts/chart_computations.py, tests/stages/render/charts/test_chart_computations.py</files>
  <behavior>
    - compute_ddl_exposure(market_cap=10e9, worst_drop_pct=-15.0) returns ddl_amount=1.5e9, settlement_estimate=27e6
    - compute_ddl_exposure with worst_drop_pct=0.0 returns both values as 0.0
    - compute_abnormal_return with synthetic data returns (ar_pct, t_stat, is_significant) tuple
    - compute_abnormal_return returns None when estimation window has fewer than 60 observations
    - compute_abnormal_return returns None when event_idx is out of bounds or est_start < 0
    - compute_ewma_volatility with constant prices returns near-zero vol series
    - compute_ewma_volatility with a single price spike shows vol spike that decays
    - compute_ewma_volatility with fewer than 2 prices returns list of 0.0s
    - classify_vol_regime with uniform series returns NORMAL for median value
    - classify_vol_regime returns CRISIS for value above 90th percentile
    - classify_vol_regime returns duration count of consecutive same-regime days
  </behavior>
  <action>
Add four functions to chart_computations.py following the existing pure-Python pattern (no numpy/scipy):

1. `compute_ddl_exposure(market_cap: float, worst_drop_pct: float, max_drawdown_pct: float | None = None, settlement_ratio: float = 0.018) -> dict[str, float]`
   - ddl_amount = market_cap * abs(worst_drop_pct) / 100.0
   - mdl_amount = market_cap * abs(max_drawdown_pct) / 100.0 if max_drawdown_pct provided
   - settlement_estimate = ddl_amount * settlement_ratio
   - Return dict with ddl_amount, mdl_amount (or None), settlement_estimate

2. `compute_abnormal_return(company_returns: list[float], market_returns: list[float], event_idx: int, estimation_window: int = 120, gap: int = 5) -> tuple[float, float, bool] | None`
   - Use code from 89-RESEARCH.md verbatim (OLS alpha+beta from estimation window, AR on event day, t-stat)
   - Return None if insufficient data (est_start < 0, len(est) < 60, var_m near zero, res_std near zero)

3. `compute_ewma_volatility(prices: list[float], lam: float = 0.94) -> list[float]`
   - Use code from 89-RESEARCH.md verbatim (log returns, recursive EWMA variance, annualized)

4. `classify_vol_regime(ewma_series: list[float], current_idx: int = -1) -> tuple[str, int]`
   - Use code from 89-RESEARCH.md verbatim (percentile-based thresholds: <25th=LOW, 25-75=NORMAL, 75-90=ELEVATED, >90=CRISIS)
   - Count consecutive days in same regime for duration

Add tests to tests/stages/render/charts/test_chart_computations.py (file exists, append new test classes):
- TestComputeDDLExposure: basic calc, zero drop, negative pct handling
- TestComputeAbnormalReturn: synthetic data with known AR, insufficient data returns None, boundary conditions
- TestComputeEWMAVolatility: constant prices, spike detection, short series
- TestClassifyVolRegime: all four regimes, duration counting, empty series
  </action>
  <verify>
    <automated>uv run pytest tests/stages/render/charts/test_chart_computations.py -x -q -k "ddl or abnormal or ewma or regime"</automated>
  </verify>
  <done>All four computation functions implemented and tested. DDL returns correct dollar amounts, AR returns t-stats with significance flags, EWMA produces annualized vol series, regime classifier uses percentile thresholds with duration counting.</done>
</task>

</tasks>

<verification>
uv run pytest tests/stages/render/charts/test_chart_computations.py tests/models/ -x -q
</verification>

<success_criteria>
- Four new pure computation functions in chart_computations.py
- Model fields on StockPerformance (3), StockDropEvent (5), StockDropAnalysis (3)
- All new computation functions have unit tests covering happy path, edge cases, and boundary conditions
- Existing tests unbroken
</success_criteria>

<output>
After completion, create `.planning/phases/89-statistical-analysis/89-01-SUMMARY.md`
</output>
