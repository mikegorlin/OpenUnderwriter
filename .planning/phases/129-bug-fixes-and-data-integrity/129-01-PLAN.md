---
phase: 129-bug-fixes-and-data-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/stages/render/sca_counter.py
  - src/do_uw/stages/render/context_builders/_key_stats_helpers.py
  - src/do_uw/stages/render/context_builders/_narrative_generators.py
  - src/do_uw/stages/render/context_builders/_litigation_helpers.py
  - src/do_uw/stages/render/context_builders/monitoring_context.py
  - src/do_uw/stages/render/context_builders/key_stats_context.py
  - src/do_uw/stages/render/context_builders/ddl_context.py
  - src/do_uw/stages/render/sections/sect1_findings_data.py
  - src/do_uw/stages/render/sections/sect6_litigation.py
  - src/do_uw/stages/render/md_narrative_sections.py
  - src/do_uw/stages/score/factor_data.py
  - src/do_uw/stages/score/pattern_fields.py
  - src/do_uw/stages/analyze/section_assessments.py
  - src/do_uw/stages/score/red_flag_gates.py
  - src/do_uw/stages/render/context_builders/crf_bar_context.py
  - src/do_uw/stages/render/context_builders/assembly_registry.py
  - src/do_uw/stages/render/context_builders/scoring.py
  - src/do_uw/stages/render/context_builders/assembly_html_extras.py
  - src/do_uw/stages/render/sections/sect1_executive_tables.py
  - tests/render/test_sca_count_consistency.py
  - tests/render/test_crf_insolvency_suppression.py
  - tests/render/test_crf_ceiling_display.py
autonomous: true
requirements: [FIX-04, FIX-05]

must_haves:
  truths:
    - "SCA count is identical in executive brief, key stats, monitoring triggers, litigation section, meeting prep, and narrative sections for any given ticker"
    - "CRF insolvency trigger does not fire for companies with healthy balance sheets (Altman Z > 3.0)"
    - "CRF ceiling values displayed in HTML match the size-resolved values from apply_crf_ceilings"
  artifacts:
    - path: "src/do_uw/stages/render/sca_counter.py"
      provides: "Canonical get_active_genuine_scas() and count_active_genuine_scas() functions"
      exports: ["get_active_genuine_scas", "count_active_genuine_scas"]
    - path: "src/do_uw/stages/score/red_flag_gates.py"
      provides: "Centralized should_suppress_insolvency() function"
      contains: "def should_suppress_insolvency"
    - path: "tests/render/test_sca_count_consistency.py"
      provides: "Tests proving all SCA count paths produce identical results"
      min_lines: 40
    - path: "tests/render/test_crf_insolvency_suppression.py"
      provides: "Tests proving insolvency suppression consistency across all 3 sites"
      min_lines: 30
    - path: "tests/render/test_crf_ceiling_display.py"
      provides: "Tests proving ceiling display values match size-resolved ceilings from apply_crf_ceilings"
      min_lines: 20
  key_links:
    - from: "src/do_uw/stages/render/sections/sect1_findings_data.py"
      to: "src/do_uw/stages/render/sca_counter.py"
      via: "import get_active_genuine_scas"
      pattern: "from do_uw\\.stages\\.render\\.sca_counter import"
    - from: "src/do_uw/stages/render/context_builders/monitoring_context.py"
      to: "src/do_uw/stages/render/sca_counter.py"
      via: "import get_active_genuine_scas"
      pattern: "from do_uw\\.stages\\.render\\.sca_counter import"
    - from: "src/do_uw/stages/render/context_builders/crf_bar_context.py"
      to: "src/do_uw/stages/score/red_flag_gates.py"
      via: "import should_suppress_insolvency"
      pattern: "from do_uw\\.stages\\.score\\.red_flag_gates import.*should_suppress_insolvency"
---

<objective>
Centralize SCA counting and CRF insolvency suppression into single canonical functions, then rewire all 13+ SCA call sites and 3+ CRF suppression sites to use them.

Purpose: SCA counts currently diverge across sections because each of 13+ locations implements its own filter logic with different status criteria. CRF insolvency suppression is duplicated in 3 places with subtle differences. Both cause user-visible inconsistencies.

Output: Two new shared functions (sca_counter.py + should_suppress_insolvency in red_flag_gates.py), all call sites rewired, comprehensive tests proving consistency.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-bug-fixes-and-data-integrity/129-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create canonical SCA counter + rewire all 13 call sites</name>
  <files>
    src/do_uw/stages/render/sca_counter.py,
    tests/render/test_sca_count_consistency.py,
    src/do_uw/stages/render/sections/sect1_findings_data.py,
    src/do_uw/stages/render/context_builders/_key_stats_helpers.py,
    src/do_uw/stages/render/context_builders/_narrative_generators.py,
    src/do_uw/stages/render/context_builders/_litigation_helpers.py,
    src/do_uw/stages/render/context_builders/monitoring_context.py,
    src/do_uw/stages/render/context_builders/key_stats_context.py,
    src/do_uw/stages/render/context_builders/ddl_context.py,
    src/do_uw/stages/render/sections/sect6_litigation.py,
    src/do_uw/stages/render/md_narrative_sections.py,
    src/do_uw/stages/score/factor_data.py,
    src/do_uw/stages/score/pattern_fields.py,
    src/do_uw/stages/analyze/section_assessments.py
  </files>
  <read_first>
    src/do_uw/stages/score/red_flag_gates.py,
    src/do_uw/stages/render/sections/sect1_findings_data.py,
    src/do_uw/stages/render/context_builders/_narrative_generators.py,
    src/do_uw/stages/render/context_builders/monitoring_context.py,
    src/do_uw/stages/render/context_builders/key_stats_context.py,
    src/do_uw/stages/render/context_builders/_key_stats_helpers.py,
    src/do_uw/stages/render/context_builders/_litigation_helpers.py,
    src/do_uw/stages/render/context_builders/ddl_context.py,
    src/do_uw/stages/render/sections/sect6_litigation.py,
    src/do_uw/stages/render/md_narrative_sections.py,
    src/do_uw/stages/score/factor_data.py,
    src/do_uw/stages/score/pattern_fields.py,
    src/do_uw/stages/analyze/section_assessments.py
  </read_first>
  <behavior>
    - Test: get_active_genuine_scas returns only non-regulatory SCAs with status ACTIVE/PENDING/None/N/A
    - Test: get_active_genuine_scas excludes SCAs with status SETTLED/DISMISSED/CLOSED
    - Test: get_active_genuine_scas excludes SCAs where _is_regulatory_not_sca returns True (FCPA, SEC enforcement, etc.)
    - Test: get_active_genuine_scas includes SCAs with status=None (unknown = assume active per conservative underwriting principle)
    - Test: count_active_genuine_scas returns len(get_active_genuine_scas)
    - Test: All 13 call sites produce identical count when given the same AnalysisState (mock state with mix of ACTIVE, PENDING, None, SETTLED, regulatory SCAs)
  </behavior>
  <action>
    **RED phase:** Create `tests/render/test_sca_count_consistency.py` with tests for the canonical SCA counter behavior listed above. Build a mock AnalysisState with 6 SCAs: 1 ACTIVE genuine, 1 PENDING genuine, 1 status=None genuine, 1 SETTLED genuine, 1 ACTIVE regulatory (FCPA), 1 status="N/A" genuine. Expected count = 4 (ACTIVE + PENDING + None + N/A, excluding SETTLED and regulatory). Run tests -- they must fail (module doesn't exist yet).

    **GREEN phase:** Create `src/do_uw/stages/render/sca_counter.py` with two functions:

    ```python
    def get_active_genuine_scas(state: AnalysisState) -> list:
        """Canonical active genuine SCA list -- single source of truth.
        Active = status in (ACTIVE, PENDING, N/A, None) -- conservative: unknown = active
        Genuine = NOT filtered by _is_regulatory_not_sca
        """
    ```

    and `count_active_genuine_scas(state) -> int` returning `len(get_active_genuine_scas(state))`.

    Canonical filter criteria:
    - Import `_is_regulatory_not_sca` from `do_uw.stages.score.red_flag_gates`
    - Get SCAs from `state.extracted.litigation.securities_class_actions` (guard for None at every level)
    - Exclude regulatory: `if _is_regulatory_not_sca(sca): continue`
    - Include if status is None (unknown = assume active)
    - Include if status string (after `.value` if enum) uppercased is in `{"ACTIVE", "PENDING", "N/A", ""}`
    - Exclude SETTLED, DISMISSED, CLOSED, etc.

    Then rewire ALL 13 call sites to use the canonical functions. For each file:
    1. Add `from do_uw.stages.render.sca_counter import get_active_genuine_scas, count_active_genuine_scas`
    2. Replace the inline SCA filtering logic with a call to the canonical function
    3. Remove the now-unused inline filter variables

    The 13 call sites (search for `_is_regulatory_not_sca` imports across src/):
    - `sect1_findings_data.py` -- replace `counts["sca"]` computation
    - `_narrative_generators.py` -- replace `active_scas` list comprehension
    - `monitoring_context.py` -- replace `active_genuine` list comprehension
    - `key_stats_context.py` -- replace `r["sca_count"]` computation
    - `_key_stats_helpers.py` -- replace both SCA count call sites
    - `_litigation_helpers.py` -- replace per-case filter
    - `ddl_context.py` -- replace SCA exclusion filter
    - `sect6_litigation.py` -- replace active+pending filter
    - `md_narrative_sections.py` -- replace "ACTIVE" only filter
    - `factor_data.py` -- replace scoring-stage count
    - `pattern_fields.py` -- replace pattern detection count
    - `section_assessments.py` -- replace analysis-stage count

    Note: `red_flag_gates.py` itself defines `_is_regulatory_not_sca` and uses it internally for CRF evaluation -- do NOT change that usage (it's the canonical definition, not a consumer).

    Run tests. All must pass.
  </action>
  <verify>
    <automated>uv run pytest tests/render/test_sca_count_consistency.py -x -v && uv run pytest tests/ -x -q --timeout=60 -k "sca"</automated>
  </verify>
  <acceptance_criteria>
    - `src/do_uw/stages/render/sca_counter.py` exists and exports `get_active_genuine_scas` and `count_active_genuine_scas`
    - `grep -r "_is_regulatory_not_sca" src/do_uw/stages/render/ src/do_uw/stages/score/factor_data.py src/do_uw/stages/score/pattern_fields.py src/do_uw/stages/analyze/section_assessments.py` shows imports only in `sca_counter.py` and `red_flag_gates.py` (consumers use `sca_counter` instead)
    - `grep -rn "from do_uw.stages.render.sca_counter import" src/` shows 12+ import sites
    - All SCA-related tests pass
  </acceptance_criteria>
  <done>Single canonical SCA counter exists; all 13 call sites rewired; inline filter logic removed; tests prove count consistency</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Centralize CRF insolvency suppression + ceiling display consistency</name>
  <files>
    src/do_uw/stages/score/red_flag_gates.py,
    tests/render/test_crf_insolvency_suppression.py,
    tests/render/test_crf_ceiling_display.py,
    src/do_uw/stages/render/context_builders/crf_bar_context.py,
    src/do_uw/stages/render/context_builders/assembly_registry.py,
    src/do_uw/stages/render/context_builders/scoring.py,
    src/do_uw/stages/render/context_builders/assembly_html_extras.py,
    src/do_uw/stages/render/sections/sect1_executive_tables.py
  </files>
  <read_first>
    src/do_uw/stages/score/red_flag_gates.py,
    src/do_uw/stages/render/context_builders/crf_bar_context.py,
    src/do_uw/stages/render/context_builders/assembly_registry.py,
    src/do_uw/stages/render/context_builders/scoring.py,
    src/do_uw/stages/render/context_builders/assembly_html_extras.py,
    src/do_uw/stages/render/sections/sect1_executive_tables.py,
    src/do_uw/stages/score/__init__.py
  </read_first>
  <behavior>
    - Test: should_suppress_insolvency returns True when Altman Z-Score > 3.0 and current ratio > 1.5
    - Test: should_suppress_insolvency returns False when Altman Z-Score < 1.8
    - Test: should_suppress_insolvency returns True when no distress signals and company has positive cash flow + healthy ratios
    - Test: CRF ceiling displayed in context equals the size-resolved ceiling from apply_crf_ceilings (not the flat config value)
    - Test: All 3 suppression sites (crf_bar_context, assembly_registry, sect1_executive_tables) produce identical suppression decision
  </behavior>
  <action>
    **RED phase:** Create `tests/render/test_crf_insolvency_suppression.py` with tests for centralized insolvency suppression. Mock AnalysisState with (a) healthy company (Altman Z=4.5, current ratio=2.0, positive cash flow) -- expect suppressed=True, and (b) distressed company (Altman Z=1.2, current ratio=0.5) -- expect suppressed=False. Also test ceiling display consistency: mock a triggered CRF with size-resolved ceiling, verify the display context uses the resolved value. Run tests -- must fail.

    Also create `tests/render/test_crf_ceiling_display.py` with dedicated tests for ceiling display consistency: mock a state with apply_crf_ceilings output containing size-resolved ceiling values, then verify the HTML context builder reads the resolved ceiling (not the flat config value). Test at least 2 company sizes (e.g., mid-cap with $25M ceiling, large-cap with $50M ceiling).

    **GREEN phase:**

    1. In `src/do_uw/stages/score/red_flag_gates.py`, add a public function:
    ```python
    def should_suppress_insolvency(state: AnalysisState) -> bool:
        """Single source of truth for CRF insolvency suppression.
        Suppresses insolvency CRF when company shows no distress signals:
        - Altman Z-Score > 3.0 (safe zone)
        - Current ratio > 1.5
        - No negative working capital or cash flow crisis
        Consolidates logic from crf_bar_context.py, assembly_registry.py,
        and sect1_executive_tables.py.
        """
    ```
    Read the existing implementations from the 3 locations to capture ALL distress metric checks (Altman Z, current ratio, interest coverage, cash flow). Merge into the single function. The function should extract financial metrics from `state.extracted.financials` and `state.scoring` using `safe_float()` for all value access.

    2. Rewire the 3 suppression sites:
    - `crf_bar_context.py`: Replace inline `_get_distress_metrics` + suppression logic with `from do_uw.stages.score.red_flag_gates import should_suppress_insolvency`
    - `assembly_registry.py`: Replace `_should_suppress_insolvency_crf` function with import of centralized function
    - `sect1_executive_tables.py`: Replace `_should_suppress_insolvency_crf_flag` with import of centralized function
    - `scoring.py` and `assembly_html_extras.py`: Check if they reference suppression -- rewire if so

    3. For ceiling display consistency: In `crf_bar_context.py`, verify that the ceiling value shown in the CRF bar visualization reads from the same `ceiling_details` dict that Step 6.5 in `score/__init__.py` populates. If it reads a different path (e.g., flat `ceiling_applied` from `RedFlagResult`), fix it to use `state.scoring.crf_ceiling_details` (or wherever the resolved value lives after Step 6.5). Add a test in `test_crf_ceiling_display.py` confirming this.

    Run tests. All must pass.
  </action>
  <verify>
    <automated>uv run pytest tests/render/test_crf_insolvency_suppression.py tests/render/test_crf_ceiling_display.py -x -v && uv run pytest tests/ -x -q --timeout=60 -k "crf or insolvency or ceiling"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def should_suppress_insolvency" src/do_uw/stages/score/red_flag_gates.py` returns a match
    - `grep -rn "_should_suppress_insolvency_crf" src/do_uw/stages/render/` returns zero matches (old functions removed)
    - `grep -rn "from do_uw.stages.score.red_flag_gates import.*should_suppress_insolvency" src/do_uw/stages/render/` shows 3+ import sites
    - `tests/render/test_crf_ceiling_display.py` exists with 2+ test functions
    - All CRF/insolvency/ceiling tests pass
  </acceptance_criteria>
  <done>Single should_suppress_insolvency function in red_flag_gates.py; all 3 suppression sites rewired; ceiling display uses size-resolved value; dedicated ceiling display tests prove consistency</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/render/test_sca_count_consistency.py tests/render/test_crf_insolvency_suppression.py tests/render/test_crf_ceiling_display.py -v` -- all new tests pass
- `uv run pytest tests/ -x -q --timeout=120` -- full suite passes, no regressions
- `grep -r "_is_regulatory_not_sca" src/ | grep -v "sca_counter.py" | grep -v "red_flag_gates.py" | grep -v "__pycache__"` -- returns zero lines (no inline SCA filtering remains outside canonical locations)
- `grep -rn "_should_suppress_insolvency_crf" src/` -- returns zero lines (old duplicated functions removed)
</verification>

<success_criteria>
- SCA count is computed by exactly ONE function across the entire codebase
- CRF insolvency suppression is computed by exactly ONE function across the entire codebase
- All existing tests pass without regression
- New consistency tests prove identical results across all render paths
- CRF ceiling display tests prove size-resolved values are used
</success_criteria>

<output>
After completion, create `.planning/phases/129-bug-fixes-and-data-integrity/129-01-SUMMARY.md`
</output>
