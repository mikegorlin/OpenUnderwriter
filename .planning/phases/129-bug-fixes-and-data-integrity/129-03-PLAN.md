---
phase: 129-bug-fixes-and-data-integrity
plan: 03
type: execute
wave: 2
depends_on: [129-01]
files_modified:
  - src/do_uw/stages/render/sections/meeting_questions.py
  - src/do_uw/stages/render/sections/meeting_questions_analysis.py
  - src/do_uw/stages/render/sections/meeting_questions_gap.py
  - src/do_uw/stages/render/sections/meeting_prep.py
  - src/do_uw/stages/benchmark/narrative_generator.py
  - tests/render/test_meeting_prep_specificity.py
autonomous: true
requirements: [FIX-03]

must_haves:
  truths:
    - "Meeting prep questions reference company-specific findings (dollar amounts, names, dates, case references) not generic SCA templates"
    - "Every meeting prep question contains at least one company-specific data point from AnalysisState"
    - "Meeting prep SCA count matches the canonical count from sca_counter.py"
  artifacts:
    - path: "src/do_uw/stages/render/sections/meeting_questions.py"
      provides: "Company-specific question generators using state data"
      contains: "state.company"
    - path: "tests/render/test_meeting_prep_specificity.py"
      provides: "Tests proving meeting prep questions are company-specific"
      min_lines: 40
  key_links:
    - from: "src/do_uw/stages/render/sections/meeting_questions.py"
      to: "state.extracted"
      via: "Reads specific risk findings from state"
      pattern: "state\\.extracted|state\\.scoring|state\\.company"
    - from: "src/do_uw/stages/render/sections/meeting_prep.py"
      to: "src/do_uw/stages/render/sca_counter.py"
      via: "Uses canonical SCA count for meeting prep"
      pattern: "from do_uw\\.stages\\.render\\.sca_counter import"
---

<objective>
Make meeting prep questions company-specific by tying them to actual risk findings, signal results, and financial data from AnalysisState.

Purpose: Meeting prep currently produces generic SCA templates applicable to any company. An underwriter preparing for a meeting needs questions about THIS company's specific risk profile -- its actual litigation, its governance gaps, its financial anomalies.

Output: Meeting prep question generators produce questions with embedded company-specific data; any LLM-generated meeting prep includes the full analytical context; tests verify specificity.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-bug-fixes-and-data-integrity/129-RESEARCH.md
@.planning/phases/129-bug-fixes-and-data-integrity/129-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit meeting prep paths and fix generic question generation</name>
  <files>
    src/do_uw/stages/render/sections/meeting_questions.py,
    src/do_uw/stages/render/sections/meeting_questions_analysis.py,
    src/do_uw/stages/render/sections/meeting_questions_gap.py,
    src/do_uw/stages/render/sections/meeting_prep.py,
    tests/render/test_meeting_prep_specificity.py
  </files>
  <read_first>
    src/do_uw/stages/render/sections/meeting_questions.py,
    src/do_uw/stages/render/sections/meeting_questions_analysis.py,
    src/do_uw/stages/render/sections/meeting_questions_gap.py,
    src/do_uw/stages/render/sections/meeting_prep.py,
    src/do_uw/stages/benchmark/narrative_generator.py
  </read_first>
  <behavior>
    - Test: meeting_questions.py generators produce questions containing company ticker or company name
    - Test: meeting_questions.py generators produce questions containing dollar amounts, dates, or specific names from state
    - Test: a question like "What is the status of pending SCAs?" is rejected in favor of "What is the status of the [CaseName] SCA filed on [Date] seeking [$Amount]?"
    - Test: meeting prep SCA count uses canonical sca_counter (not inline filter)
    - Test: meeting_questions_analysis.py bear case questions reference actual scoring factors (e.g., "F.7 = 5/8") not generic risk language
  </behavior>
  <action>
    **RED phase:** Create `tests/render/test_meeting_prep_specificity.py`:
    1. Build a mock AnalysisState with: company name "Acme Corp", ticker "ACME", one active SCA with case_name="Smith v. Acme", filing_date="2025-06-15", damages="$50M", revenue=$1B, score=72.5, tier="WRITE", top scoring factor F.3=7/8
    2. Test that `generate_clarification_questions(state)` (or equivalent function name -- read the actual file) produces questions mentioning "Acme", "$50M", "Smith v. Acme", or "2025-06-15" -- at least 2 company-specific data points per question
    3. Test that no question is a generic template (regex check: should not match "What is the company's exposure to SCA risk?" without specific case details)
    4. Test that SCA-related questions use `count_active_genuine_scas` from `sca_counter.py`
    5. Test that bear case / peril questions reference scoring factor IDs (e.g., "F.3", "F.7")

    **GREEN phase:**

    **Step 1 -- Identify the generic output path:**
    Read all 4 meeting question files. Determine:
    - Which functions produce the user-reported generic output?
    - Is it the rule-based generators (meeting_questions*.py) or the LLM path (narrative_generator.py)?
    - Check the HTML template for meeting prep section to see which data source it uses.

    **Step 2 -- Fix rule-based generators:**
    For each generator function in `meeting_questions.py`, `meeting_questions_analysis.py`, and `meeting_questions_gap.py`:
    - Where questions reference SCAs: inject actual case names, filing dates, damages from `get_active_genuine_scas(state)` (import from `sca_counter.py`)
    - Where questions reference financial risk: inject actual revenue, net income, Altman Z from `state.extracted.financials`
    - Where questions reference scoring: inject actual factor scores and tier from `state.scoring`
    - Where questions reference governance: inject actual board size, independence ratio, GC name from `state.extracted.governance`
    - Replace ALL template strings like "the company" with `state.company.name` or `state.company.ticker`
    - Replace generic phrases like "pending litigation" with actual case names and amounts

    **Step 3 -- Fix LLM-generated meeting prep (if applicable):**
    If `narrative_generator.py` also generates meeting prep content:
    - Ensure the prompt includes: company name, ticker, all scoring factor results, active SCA list with details, top risk findings, financial highlights
    - Add to the prompt: "Every question MUST reference a specific finding, dollar amount, or date from the provided data. Do NOT generate generic questions applicable to any company."
    - Use the same canonical SCA data from `get_active_genuine_scas`

    **Step 4 -- Wire SCA count consistency:**
    In `meeting_prep.py` (the Word renderer) and any HTML template path, ensure SCA counts come from `count_active_genuine_scas(state)` -- not inline counting.

    Run all tests.
  </action>
  <verify>
    <automated>uv run pytest tests/render/test_meeting_prep_specificity.py -x -v && uv run pytest tests/render/ -x -q --timeout=60 -k "meeting"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/render/test_meeting_prep_specificity.py` exists with 5+ test functions
    - `grep -rn "sca_counter" src/do_uw/stages/render/sections/meeting_questions.py src/do_uw/stages/render/sections/meeting_prep.py` shows canonical import
    - `grep -rn "state.company.name\|state.company.ticker\|company_name\|ticker" src/do_uw/stages/render/sections/meeting_questions.py` shows company-specific data injection
    - `grep -c "the company" src/do_uw/stages/render/sections/meeting_questions.py src/do_uw/stages/render/sections/meeting_questions_analysis.py` -- zero generic "the company" references in question templates (replaced with actual company name)
    - No test regressions
  </acceptance_criteria>
  <done>Meeting prep questions contain company-specific data points (names, amounts, dates); generic templates eliminated; SCA counts use canonical counter; tests verify specificity</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/render/test_meeting_prep_specificity.py -v` -- all specificity tests pass
- `uv run pytest tests/ -x -q --timeout=120` -- full suite passes
- Manual spot-check: `grep -n "the company" src/do_uw/stages/render/sections/meeting_questions*.py` returns zero generic references
</verification>

<success_criteria>
- Every meeting prep question function injects at least one company-specific data point
- No generic "the company" or "pending SCAs" language remains in question templates
- SCA count in meeting prep matches executive brief and monitoring triggers (via canonical counter)
- Tests verify specificity with mock AnalysisState
</success_criteria>

<output>
After completion, create `.planning/phases/129-bug-fixes-and-data-integrity/129-03-SUMMARY.md`
</output>
