---
phase: 129-bug-fixes-and-data-integrity
plan: 02
type: execute
wave: 2
depends_on: [129-01]
files_modified:
  - src/do_uw/stages/benchmark/narrative_data_sections.py
  - src/do_uw/stages/benchmark/narrative_generator.py
  - src/do_uw/stages/benchmark/narrative_prompts.py
  - src/do_uw/stages/extract/llm_governance.py
  - src/do_uw/stages/extract/board_parsing.py
  - src/do_uw/stages/extract/leadership_profiles.py
  - src/do_uw/stages/extract/llm/schemas/def14a.py
  - src/do_uw/stages/extract/sec_enforcement.py
  - tests/benchmark/test_narrative_validation.py
  - tests/extract/test_governance_extraction.py
autonomous: true
requirements: [FIX-01, FIX-02]

must_haves:
  truths:
    - "AAPL worksheet shows no $383B services revenue figure anywhere in the output"
    - "No DOJ_FCPA case appears in the securities class actions section of any worksheet"
    - "Board composition includes gender diversity percentage derived from DEF 14A"
    - "Current GC shows Jennifer Newstead (not Kate Adams) for AAPL"
  artifacts:
    - path: "src/do_uw/stages/benchmark/narrative_generator.py"
      provides: "Post-generation cross-validation of dollar amounts against state data"
      contains: "cross_validate"
    - path: "tests/benchmark/test_narrative_validation.py"
      provides: "Tests for narrative dollar amount cross-validation"
      min_lines: 30
    - path: "tests/extract/test_governance_extraction.py"
      provides: "Tests for gender diversity and GC extraction"
      min_lines: 30
  key_links:
    - from: "src/do_uw/stages/benchmark/narrative_generator.py"
      to: "src/do_uw/stages/extract/xbrl_llm_reconciler.py"
      via: "Uses reconciled XBRL values for cross-validation"
      pattern: "reconcil|xbrl|cross.?valid"
    - from: "src/do_uw/stages/benchmark/narrative_data_sections.py"
      to: "state.extracted.financials"
      via: "Passes XBRL-reconciled revenue to LLM prompt"
      pattern: "xbrl|reconcil"
---

<objective>
Eliminate LLM hallucination ($383B revenue) and DOJ_FCPA misclassification in narratives, and fix governance extraction gaps (gender diversity, GC succession, board profiles).

Purpose: The $383B hallucination and DOJ_FCPA misclassification produce incorrect data in the worksheet. Governance extraction gaps mean board composition data is incomplete. Both undermine the "single source of truth" promise.

Output: Narrative generator cross-validates dollar amounts against XBRL/state data, DOJ_FCPA cases are filtered from SCA paths, governance extraction produces gender diversity and current GC.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-bug-fixes-and-data-integrity/129-RESEARCH.md
@.planning/phases/128-infrastructure-foundation/128-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add narrative cross-validation gate + fix DOJ_FCPA data path</name>
  <files>
    src/do_uw/stages/benchmark/narrative_data_sections.py,
    src/do_uw/stages/benchmark/narrative_generator.py,
    src/do_uw/stages/benchmark/narrative_prompts.py,
    src/do_uw/stages/extract/sec_enforcement.py,
    tests/benchmark/test_narrative_validation.py
  </files>
  <read_first>
    src/do_uw/stages/benchmark/narrative_data_sections.py,
    src/do_uw/stages/benchmark/narrative_generator.py,
    src/do_uw/stages/benchmark/narrative_prompts.py,
    src/do_uw/stages/benchmark/narrative_data.py,
    src/do_uw/stages/extract/xbrl_llm_reconciler.py,
    src/do_uw/stages/extract/sec_enforcement.py,
    src/do_uw/stages/score/red_flag_gates.py
  </read_first>
  <behavior>
    - Test: narrative_data_sections passes XBRL-reconciled revenue values (not raw LLM-extracted) to prompts
    - Test: post-generation validation flags dollar amounts that differ >2x from state.extracted.financials values
    - Test: a narrative containing "$383B" when state revenue is $394B (actual AAPL) passes; "$383B" when state revenue is $94B fails validation
    - Test: DOJ_FCPA entries in securities_class_actions are filtered before any narrative generation uses SCA data
  </behavior>
  <action>
    **RED phase:** Create `tests/benchmark/test_narrative_validation.py`:
    1. Test that `narrative_data_sections` extracts revenue from XBRL-reconciled path (state.extracted.financials.reconciled or XBRL-preferred values), not raw LLM extraction
    2. Test that `generate_section_narrative` or a new `validate_narrative_amounts` function detects when a generated narrative mentions a dollar amount that differs >2x from the corresponding state value
    3. Test that SCA data passed to narrative prompts uses canonical `get_active_genuine_scas` from Plan 01 (which already excludes regulatory cases)

    **GREEN phase:**

    **Mandatory pre-condition:** Before writing any fix code, inspect an AAPL state.json (or load the AAPL AnalysisState) and search for the value "383" to confirm the exact data path where the hallucinated figure originates. Is it in `state.extracted.financials.statements`? In `state.extracted.financials.segments`? In an LLM extraction cache? Document the finding as a code comment in narrative_data_sections.py (e.g., `# FIX-01: $383B originated from [exact path] -- now using XBRL-reconciled value`). This step is mandatory -- do not skip it even if the fix seems obvious.

    **Fix 1 -- Narrative data uses XBRL-reconciled values:**
    In `narrative_data_sections.py`, find where revenue/financial figures are extracted for LLM prompts. Ensure they pull from the XBRL-reconciled values. Check if `state.extracted.financials.statements` has XBRL-reconciled data from Phase 128's `xbrl_llm_reconciler.py`. If the data sections pass raw `llm_extracted_revenue` instead of `xbrl_revenue`, fix to prefer XBRL. Trace the exact origin of the $383B figure -- it may be a segment revenue being passed as total revenue, or an LLM hallucination in extraction.

    **Fix 2 -- Post-generation cross-validation:**
    In `narrative_generator.py`, after `generate_section_narrative` produces text, add a validation step:
    - Extract dollar amounts from the generated narrative text using regex (pattern: `\$[\d,.]+[BMK]?`)
    - Compare each against known state values (revenue, net income, total assets, market cap)
    - If any amount differs >2x from the closest matching state value, either (a) append a `[UNVERIFIED]` tag, or (b) replace with the verified state value
    - Log the discrepancy for audit trail (use existing reconciliation_warnings pattern from Phase 128)

    **Fix 3 -- DOJ_FCPA data path:**
    Search state data for where DOJ_FCPA entries live. Two scenarios:
    (a) If they enter via `securities_class_actions` list: Plan 01's canonical `get_active_genuine_scas` already filters via `_is_regulatory_not_sca`. Verify narrative_data_sections uses the canonical function (import from `sca_counter.py`) instead of raw SCA list.
    (b) If they enter via a different path (e.g., `sec_enforcement` or `regulatory_actions`): trace in `sec_enforcement.py` and ensure narrative_data_sections doesn't conflate regulatory actions with SCAs.

    Ensure `narrative_data_sections.py` and `narrative_prompts.py` use `get_active_genuine_scas(state)` for any SCA-related data passed to LLM prompts.

    Run all tests.
  </action>
  <verify>
    <automated>uv run pytest tests/benchmark/test_narrative_validation.py -x -v && uv run pytest tests/benchmark/ -x -q --timeout=60</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "cross_validate\|validate_narrative\|XBRL\|reconcil" src/do_uw/stages/benchmark/narrative_generator.py` shows validation logic
    - `grep -n "get_active_genuine_scas\|sca_counter" src/do_uw/stages/benchmark/narrative_data_sections.py` shows canonical SCA import
    - `tests/benchmark/test_narrative_validation.py` exists with 4+ test functions
    - No test regressions
  </acceptance_criteria>
  <done>Narrative generator cross-validates dollar amounts against state/XBRL data; DOJ_FCPA cases excluded from narrative SCA data via canonical counter; $383B hallucination prevented by XBRL-preferred data path</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix governance extraction gaps (gender diversity, GC succession, board profiles)</name>
  <files>
    src/do_uw/stages/extract/llm_governance.py,
    src/do_uw/stages/extract/board_parsing.py,
    src/do_uw/stages/extract/leadership_profiles.py,
    src/do_uw/stages/extract/llm/schemas/def14a.py,
    tests/extract/test_governance_extraction.py
  </files>
  <read_first>
    src/do_uw/stages/extract/llm_governance.py,
    src/do_uw/stages/extract/board_parsing.py,
    src/do_uw/stages/extract/leadership_profiles.py,
    src/do_uw/stages/extract/llm/schemas/def14a.py,
    src/do_uw/models/governance.py
  </read_first>
  <behavior>
    - Test: DEF 14A extraction schema includes board_gender_diversity_pct field
    - Test: LLM governance extraction prompt explicitly asks for gender breakdown (X of Y directors are female/male)
    - Test: Leadership extraction prompt asks for current General Counsel by name and appointment date
    - Test: Board parsing handles the case where a director (e.g., Wanda Austin) is in the DEF 14A but missing from extracted board list
  </behavior>
  <action>
    **RED phase:** Create `tests/extract/test_governance_extraction.py`:
    1. Test that DEF 14A schema has `board_gender_diversity_pct` field (or equivalent)
    2. Test that governance LLM prompt includes language requesting gender diversity data
    3. Test that leadership LLM prompt requests current GC name + appointment date
    4. Test that board extraction does not silently drop directors

    **GREEN phase:**

    **Fix 1 -- Gender diversity extraction:**
    In `llm/schemas/def14a.py`, verify `board_gender_diversity_pct` exists in the schema. If missing, add it as an Optional[float] field with description "Percentage of board that is female."
    In `llm_governance.py`, find the LLM prompt for DEF 14A extraction. Enhance it to explicitly request:
    - Total number of directors
    - Number of female directors
    - Board gender diversity percentage
    - Names of female directors

    **Fix 2 -- GC succession (Kate Adams -> Jennifer Newstead):**
    In `leadership_profiles.py`, find where General Counsel is extracted. The issue is likely stale LLM cache or the prompt not specifically asking "Who is the CURRENT General Counsel?" Add explicit prompt language: "Identify the current General Counsel or Chief Legal Officer. Note: Apple's GC changed from Katherine Adams to Jennifer Newstead -- extract the CURRENT officer from the most recent filing."

    IMPORTANT: The Apple-specific hint is for the prompt only. The general fix is to ensure the prompt asks for "current" officers from "the most recent proxy filing date" -- not historical ones. The LLM cache must be invalidated when the prompt changes. Check if `stages/extract/llm/cache.py` includes prompt hash in the cache key.

    **Fix 3 -- Board member completeness (Wanda Austin pattern):**
    In `board_parsing.py`, examine how directors are extracted from DEF 14A text. If the parser uses a limited pattern (e.g., only extracting from a "Board of Directors" table), it may miss directors mentioned in other sections. Enhance to:
    - Parse the full nominee list from proxy statement proposals
    - Cross-reference against committee membership lists
    - Include a completeness check: if DEF 14A mentions N directors but only N-1 are extracted, log a warning

    Run all tests.
  </action>
  <verify>
    <automated>uv run pytest tests/extract/test_governance_extraction.py -x -v && uv run pytest tests/extract/ -x -q --timeout=60 -k "governance or board or gender"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "gender_diversity\|gender" src/do_uw/stages/extract/llm/schemas/def14a.py` shows gender diversity field
    - `grep -n "General Counsel\|Chief Legal\|current" src/do_uw/stages/extract/leadership_profiles.py` shows explicit current-officer prompt language
    - `tests/extract/test_governance_extraction.py` exists with 4+ test functions
    - No test regressions
  </acceptance_criteria>
  <done>DEF 14A extraction captures gender diversity; leadership extraction asks for current GC; board parsing includes completeness check; all extraction tests pass</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/benchmark/test_narrative_validation.py tests/extract/test_governance_extraction.py -v` -- all new tests pass
- `uv run pytest tests/ -x -q --timeout=120` -- full suite passes
- `grep -rn "383" src/do_uw/stages/benchmark/` -- no hardcoded $383B figure
- `grep -rn "DOJ_FCPA\|_FCPA" src/do_uw/stages/benchmark/` -- no FCPA misclassification in benchmark stage
</verification>

<success_criteria>
- Narrative generator validates dollar amounts against XBRL/state data before storing
- DOJ_FCPA entries cannot appear in SCA-related narrative content
- Gender diversity is part of the DEF 14A extraction schema and LLM prompt
- GC extraction explicitly requests current officer, not historical
- Board parsing includes completeness cross-check
</success_criteria>

<output>
After completion, create `.planning/phases/129-bug-fixes-and-data-integrity/129-02-SUMMARY.md`
</output>
