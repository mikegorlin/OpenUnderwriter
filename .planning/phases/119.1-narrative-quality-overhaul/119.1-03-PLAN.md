---
phase: 119.1-narrative-quality-overhaul
plan: 03
type: execute
wave: 2
depends_on:
  - 119.1-01
  - 119.1-02
files_modified:
  - src/do_uw/brain/signals/fin/forensic_opportunities.yaml
  - src/do_uw/brain/signals/fin/balance.yaml
  - src/do_uw/brain/signals/fin/peer_xbrl.yaml
  - src/do_uw/brain/signals/fin/income.yaml
  - src/do_uw/brain/signals/fin/distress.yaml
  - scripts/fix_yaml_do_context.py
  - tests/stages/render/test_narrative_quality.py
autonomous: true
requirements:
  - NQ-04

must_haves:
  truths:
    - "Zero brain YAML signals contain the phrase 'historically correlated with increased D&O claim frequency and severity'"
    - "Each formerly-boilerplate TRIGGERED_RED template now explains the signal-specific D&O risk mechanism (e.g., goodwill -> ASC 350 impairment -> Section 10(b) claims)"
    - "Template variable syntax ({company}, {value}, etc.) is preserved in all regenerated templates"
  artifacts:
    - path: "scripts/fix_yaml_do_context.py"
      provides: "Batch YAML do_context regeneration script"
      min_lines: 50
  key_links:
    - from: "src/do_uw/brain/signals/**/*.yaml"
      to: "do_context_engine.py"
      via: "TRIGGERED_RED templates consumed by SafeFormatDict"
      pattern: "TRIGGERED_RED"
---

<objective>
Batch-replace the 42 YAML signals containing "historically correlated with increased D&O claim frequency and severity" boilerplate in TRIGGERED_RED templates with signal-specific D&O risk explanations.

Purpose: These 42 signals produce identical D&O Risk column text in every data table. When an underwriter reads "historically correlated with increased D&O claim frequency and severity" in 15 different table rows, it means nothing. Each signal's D&O risk mechanism is different -- goodwill impairment triggers Section 10(b) claims, revenue recognition flags trigger restatement-driven SCAs, leverage signals trigger creditor derivative suits. The template must explain the SPECIFIC mechanism.

Output: 42 regenerated YAML do_context templates + batch script for reproducibility + verification test.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119.1-narrative-quality-overhaul/119.1-RESEARCH.md

<interfaces>
<!-- YAML do_context template structure -->

From brain YAML signals:
```yaml
presentation:
  do_context:
    TRIGGERED_RED: '{company} [Signal Name] at {value} (threshold: [thresh])
      signals elevated [Factor Category] risk ([Factor ID]).
      This agent risk (executive/director actions and decisions that create
      D&O exposure) has historically correlated with increased D&O claim
      frequency and severity.'
    TRIGGERED_YELLOW: '{company} [Signal Name] at {value} ...'
    CLEAR: '{company} [Signal Name] at {value} ...'
```

Template variables available (from SafeFormatDict in do_context_engine.py):
- {company} -- company name
- {value} -- signal evaluation value
- {zone} -- RED/YELLOW/CLEAR
- {threshold} -- signal threshold
- {evidence} -- signal evidence text
- {source} -- data source
- {confidence} -- confidence level
- {ticker} -- company ticker
- {details_*} -- any details_ prefixed fields from signal result
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create batch script and regenerate 42 YAML do_context templates</name>
  <files>
    scripts/fix_yaml_do_context.py,
    src/do_uw/brain/signals/fin/forensic_opportunities.yaml,
    src/do_uw/brain/signals/fin/balance.yaml,
    src/do_uw/brain/signals/fin/peer_xbrl.yaml,
    src/do_uw/brain/signals/fin/income.yaml,
    src/do_uw/brain/signals/fin/distress.yaml
  </files>
  <read_first>
    src/do_uw/brain/signals/fin/forensic_opportunities.yaml,
    src/do_uw/brain/signals/fin/balance.yaml,
    src/do_uw/brain/signals/fin/peer_xbrl.yaml
  </read_first>
  <action>
    **Step 1: Create the batch fix script `scripts/fix_yaml_do_context.py`.**

    The script should:
    1. Find all YAML files in `src/do_uw/brain/signals/` containing "historically correlated with increased D&O claim frequency and severity"
    2. For each signal, parse the YAML to extract: signal_id, signal_name, factor_id, threshold, signal category
    3. Look up the signal-specific D&O risk explanation from a mapping dict (created below)
    4. Replace ONLY the boilerplate sentence with the signal-specific explanation
    5. Preserve all template variables ({company}, {value}, etc.)
    6. Write back the modified YAML, preserving formatting
    7. Report: how many signals fixed, any that couldn't be auto-fixed

    **Step 2: Create the signal-category-to-D&O-mechanism mapping.**

    The 42 signals fall into these categories based on their signal names and factor mappings. For each category, write a SPECIFIC D&O mechanism explanation:

    **Balance Sheet / Leverage signals** (balance.yaml -- ~10 signals):
    - Liquidity signals (current ratio, cash burn): "Liquidity deterioration at this level triggers covenant monitoring and increases probability of going-concern qualification -- the single strongest D&O claim catalyst, present in 78% of distressed-company SCAs."
    - Debt structure signals (D/E, debt maturity): "Elevated leverage creates covenant breach risk; covenant violations force disclosure events that plaintiffs cite as corrective disclosures in Section 10(b) complaints."
    - Credit/covenant signals: "Credit deterioration forces accelerated debt disclosure that creates the 'material omission' element plaintiffs need for securities fraud claims."

    **Income Statement signals** (income.yaml):
    - Revenue/margin signals: "Revenue deterioration signals potential earnings management -- when management faces declining fundamentals, the temptation to manage disclosures creates the scienter element for 10b-5 claims."
    - Profitability signals: "Profitability decline below peer benchmarks creates analyst downgrade risk, which frequently triggers the stock decline that initiates SCA filing decisions."

    **Forensic signals** (forensic_opportunities.yaml -- ~8 signals):
    - Revenue recognition: "Revenue recognition anomalies are the #1 restatement trigger. Restatements generate automatic SCA filings with near-certain class certification."
    - Fair value/Level 3: "Level 3 asset concentration requires management judgment in valuation -- the discretion itself creates scienter exposure when valuations are subsequently written down."
    - Goodwill: "Annual ASC 350 impairment testing creates a recurring disclosure risk event. When goodwill is subsequently written down, plaintiffs allege management knew acquisitions were overvalued, triggering Section 10(b) claims."
    - Related party: "Related party transactions create duty-of-loyalty exposure for directors -- these transactions are presumptively suspect under Entire Fairness review, shifting the burden of proof to defendants."
    - Stock compensation: "Excessive stock compensation dilution signals misaligned incentives between management and shareholders -- a governance failure theory in derivative claims."
    - Pension: "Underfunded pension obligations create contingent liabilities that must be disclosed. Failure to adequately reserve generates restatement risk."

    **Peer comparison signals** (peer_xbrl.yaml -- ~6 signals):
    - Revenue/margin peer outlier: "Peer-relative underperformance at this level makes the company a statistical outlier -- outlier financial metrics are disproportionately represented in SCA filing populations."
    - Size outlier: "Relative size creates asymmetric litigation risk: smaller companies within a peer group face higher per-market-cap legal costs and are more vulnerable to event-driven claims."
    - Leverage peer outlier: "Peer-relative leverage outlier status increases the probability that a sector-wide downturn hits this company disproportionately -- concentrated exposure amplifies D&O claim risk during industry stress events."

    **Distress signals** (distress.yaml):
    - Z-Score/O-Score: "Distress scores at this level indicate >80% probability of financial difficulty within 2 years. D&O exposure shifts from standard securities fraud theories to creditor-oriented claims including Zone of Insolvency fiduciary expansion."
    - Piotroski: "Low Piotroski F-Score confirms fundamental weakness across profitability, leverage, and efficiency dimensions -- comprehensive deterioration patterns are strong predictors of future disclosure events."

    **Step 3: Run the script and verify.**

    ```bash
    uv run python scripts/fix_yaml_do_context.py
    ```

    After running, verify:
    - `grep -rl "historically correlated with increased D&O claim frequency and severity" src/do_uw/brain/signals/ | wc -l` returns 0
    - All YAML files still parse correctly: `uv run python -c "from do_uw.brain.brain_schema import load_all_signals; sigs = load_all_signals(); print(f'{len(sigs)} signals loaded')"` outputs 562+
    - Template variables preserved: `grep -c "{company}" src/do_uw/brain/signals/fin/*.yaml` shows same count before and after

    **Step 4: Add a regression test.**

    Add to `tests/stages/render/test_narrative_quality.py`:
    ```python
    def test_no_boilerplate_in_yaml():
        """No brain YAML signals contain the boilerplate phrase."""
        import glob
        boilerplate = "historically correlated with increased D&O claim frequency and severity"
        yaml_files = glob.glob("src/do_uw/brain/signals/**/*.yaml", recursive=True)
        violations = []
        for f in yaml_files:
            with open(f) as fh:
                if boilerplate in fh.read():
                    violations.append(f)
        assert violations == [], f"Boilerplate found in: {violations}"
    ```

    **Justification for YAML changes (per NON-NEGOTIABLE brain evidence rule):**
    These are NOT new brain knowledge -- they are corrections to batch-generated presentation text from Phase 116-01. The signal definitions, thresholds, evaluation logic, and factor mappings are unchanged. Only the `presentation.do_context` TRIGGERED_RED text is being corrected from a single boilerplate template to signal-specific D&O mechanism descriptions. Evidence: Phase 116-01 used a single LLM prompt to generate 562 do_context templates, producing identical closing sentences for 299 of them. This violates QUAL-01 (company-specific narrative quality).
  </action>
  <verify>
    <automated>grep -rl "historically correlated with increased D&O claim frequency and severity" src/do_uw/brain/signals/ | wc -l && uv run pytest tests/stages/render/test_narrative_quality.py::test_no_boilerplate_in_yaml -x -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "historically correlated with increased D&O claim frequency and severity" src/do_uw/brain/signals/ | wc -l` returns 0
    - `uv run python -c "from do_uw.brain.brain_schema import load_all_signals; print(len(load_all_signals()))"` returns 562 or more (all signals still valid)
    - `grep "{company}" src/do_uw/brain/signals/fin/balance.yaml | wc -l` is unchanged from before (template variables preserved)
    - `test -f scripts/fix_yaml_do_context.py` exists
    - `uv run pytest tests/stages/render/test_narrative_quality.py -x -q` passes including the new YAML boilerplate test
    - `uv run pytest tests/ -x -q --timeout=120` passes (no regressions from YAML changes)
  </acceptance_criteria>
  <done>42 YAML signals have signal-specific D&O mechanism text replacing the boilerplate; all signals still parse; template variables intact; regression test prevents future boilerplate; batch script archived for reproducibility</done>
</task>

</tasks>

<verification>
1. `grep -rl "historically correlated with increased D&O claim frequency" src/do_uw/brain/signals/ | wc -l` returns 0
2. `uv run python -c "from do_uw.brain.brain_schema import load_all_signals; print(len(load_all_signals()))"` returns 562+
3. `uv run pytest tests/stages/render/test_narrative_quality.py -v` -- all tests pass including YAML boilerplate check
4. Spot-check 3 YAML files manually: each TRIGGERED_RED template now explains the specific D&O risk mechanism
</verification>

<success_criteria>
- Zero YAML signals contain "historically correlated with increased D&O claim frequency and severity"
- Each signal's TRIGGERED_RED template explains the specific D&O risk mechanism for that signal category
- All template variables ({company}, {value}, {threshold}) preserved
- All 562+ signals still parse correctly
- Batch script exists for audit trail
- Regression test prevents reintroduction of boilerplate
</success_criteria>

<output>
After completion, create `.planning/phases/119.1-narrative-quality-overhaul/119.1-03-SUMMARY.md`
</output>
