---
phase: 119.1-narrative-quality-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/stages/render/test_narrative_quality.py
  - src/do_uw/stages/render/sections/sect1_findings_neg.py
  - src/do_uw/stages/render/sections/sect1_findings_pos.py
  - src/do_uw/stages/render/sections/sect1_findings_data.py
  - src/do_uw/stages/render/sections/sect1_findings.py
autonomous: true
requirements:
  - NQ-01
  - NQ-02
  - NQ-03
  - NQ-06
  - NQ-07

must_haves:
  truths:
    - "neg_generic() is deleted -- no catch-all narrative function exists"
    - "ANGI (distressed, 73.7% decline) and AAPL (healthy, $3T) produce visibly different narrative text for the same finding types"
    - "Stock decline language says 'catastrophic 73.7% decline' for ANGI, not 'notable decline'"
    - "Every neg_* function uses 3+ distinct data points from AnalysisState, not just market_cap and name"
    - "pos_stable_leadership differentiates a 15yr CEO at a healthy company from a 2yr CEO at a distressed company"
  artifacts:
    - path: "tests/stages/render/test_narrative_quality.py"
      provides: "Quality gate tests for all NQ requirements"
      min_lines: 150
    - path: "src/do_uw/stages/render/sections/sect1_findings_neg.py"
      provides: "Company-specific negative finding narratives"
      contains: "def neg_from_finding"
    - path: "src/do_uw/stages/render/sections/sect1_findings_pos.py"
      provides: "Company-specific positive finding narratives"
      contains: "ceo_tenure"
  key_links:
    - from: "src/do_uw/stages/render/sections/sect1_findings.py"
      to: "sect1_findings_neg.py"
      via: "dispatcher calls neg_from_finding instead of neg_generic"
      pattern: "neg_from_finding"
    - from: "src/do_uw/stages/render/sections/sect1_findings_neg.py"
      to: "sect1_findings_data.py"
      via: "data extraction helpers for stock decline, CEO tenure, scoring tier"
      pattern: "stock_decline_pct|ceo_tenure_years|scoring_tier"
---

<objective>
Eliminate all boilerplate from Executive Brief key findings by replacing neg_generic() with finding-aware generation and enriching every neg_*/pos_* function with deep state data extraction.

Purpose: The Executive Brief is the FIRST thing an underwriter reads. Every sentence must contain company-specific data -- dollar amounts, percentages, dates, names. "Notable decline" for a 73.7% crash is unacceptable. Identical text for opposite company profiles is unacceptable.

Output: Test scaffold + rewritten narrative builders that produce differentiated, data-rich text for every finding type.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119.1-narrative-quality-overhaul/119.1-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/do_uw/stages/render/sections/sect1_findings.py:
```python
def build_negative_narrative(finding: KeyFinding, idx: int, context: dict[str, Any]) -> tuple[str, str]:
    # Dispatches based on keyword matching in finding.evidence_narrative
    # Falls through to neg_generic() for unrecognized patterns -- THIS IS THE BUG
    # context["_state"] provides the full AnalysisState

def build_positive_narrative(finding: KeyFinding, idx: int, context: dict[str, Any]) -> tuple[str, str]:
    # Same pattern for positives
```

From src/do_uw/models/executive_summary.py:
```python
class KeyFinding(BaseModel):
    evidence_narrative: str       # Raw text from scoring engine
    scoring_impact: str | None    # e.g. "Critical Red Flag: Ceiling 65"
    section_origin: str | None    # e.g. "SECT3" or "Signal-driven scoring: ..."
```

From src/do_uw/stages/render/sections/sect1_findings_data.py:
```python
# Existing helpers (keep using these):
def market_cap_billions(state) -> float | None
def employee_count(state) -> int | None
def subsidiary_count(state) -> int | None
def jurisdiction_count(state) -> int | None
def goodwill_ratio(state) -> float | None
def litigation_counts(state) -> dict[str, int]
def factor_score(state, factor_id) -> dict | None
def triggered_signal_ids(factor) -> list[str]
def ceo_name(state) -> str | None
def bs_line_item(state, concept) -> float | None
def is_line_item(state, concept) -> float | None
def going_concern(state) -> bool | None
```

From src/do_uw/stages/render/sections/sect1_helpers.py:
```python
def safe_distress(state, key) -> float | None
def safe_leverage_ratio(state, key) -> float | None
def safe_short_interest(state) -> float | None
def safe_governance_field(state, field) -> float | None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffold + new data extraction helpers</name>
  <files>
    tests/stages/render/test_narrative_quality.py,
    src/do_uw/stages/render/sections/sect1_findings_data.py
  </files>
  <read_first>
    src/do_uw/stages/render/sections/sect1_findings_neg.py,
    src/do_uw/stages/render/sections/sect1_findings_pos.py,
    src/do_uw/stages/render/sections/sect1_findings_data.py,
    src/do_uw/stages/render/sections/sect1_helpers.py,
    src/do_uw/stages/render/sections/sect1_findings.py
  </read_first>
  <behavior>
    - test_no_neg_generic: importing neg_generic from sect1_findings_neg raises ImportError (function deleted)
    - test_no_neg_generic_in_dispatcher: sect1_findings.py source code does not contain "neg_generic"
    - test_neg_stock_risk_severity_calibration: given 73.7% decline state, output contains "catastrophic" and "73.7%"; given 12% decline, output contains "moderate pullback" and "12"
    - test_neg_data_richness_stock: neg_stock_risk output for ANGI-like state contains decline_pct, dollar amounts (from/to), trading days, DDL estimate -- at least 4 distinct data points
    - test_neg_data_richness_governance: neg_governance output contains at least 3 of: independence%, board size, CEO tenure, departure count, compensation references
    - test_neg_data_richness_distress: neg_distress output contains at least 3 of: Altman Z value, D/E ratio, Piotroski score, current ratio, interest coverage
    - test_neg_data_richness_audit: neg_audit_issues output contains Beneish M-Score value and specific component flags, not generic "strongest predictors" text
    - test_neg_data_richness_guidance: neg_guidance output contains actual miss count or credibility score, not just "concerning patterns"
    - test_pos_stable_leadership_differentiates: with CEO tenure 15yr + healthy financials, output differs from CEO tenure 2yr + distressed financials; specifically, long-tenure healthy mentions "continuity" or similar positive framing, short-tenure distressed mentions the risk
    - test_differentiated_output: full build_negative_narrative for stock risk finding produces different text for AAPL-like state vs ANGI-like state (not just swapped names/numbers -- different descriptors and interpretations)
    - test_no_boilerplate_phrases: none of the neg_* or pos_* functions produce text containing "even moderate risk factors can generate material D&O exposure" or "historically correlated with increased D&O claim frequency"
  </behavior>
  <action>
    1. Create `tests/stages/render/test_narrative_quality.py` with two mock AnalysisState fixtures:
       - `aapl_like_state`: market_cap=3.4T, employees=164000, Altman Z=9.93, D/E=1.87, CEO "Tim Cook" tenure 13yr, independence_ratio=0.88, board_size=8, stock decline 5%, short_interest=0.7%, no litigation, clean audit (Deloitte, 7yr), going_concern=False, Piotroski=7, current_ratio=1.07, interest_coverage=30.0, Beneish M-Score=-2.8 (no flags). Scoring tier=COMPETE.
       - `angi_like_state`: market_cap=0.6B, employees=4200, Altman Z=0.98, D/E=3.2, CEO "Joey Levin" tenure 2yr, independence_ratio=0.57, board_size=5, stock decline 73.7% from $15.30 to $4.02 over 326 days, short_interest=17.95%, 2 SCAs + 1 derivative, going_concern=True, Piotroski=2, current_ratio=0.6, interest_coverage=0.8, Beneish M-Score=-2.1 (3 flags: DSRI 1.42, SGAI 1.31, TATA 0.09). Scoring tier=WALK.

    Write all 11 tests from the behavior list above. Tests should fail initially (RED).

    2. Add new data extraction helpers to `sect1_findings_data.py`:
       - `stock_decline_pct(state) -> float | None` -- extracts max decline percentage from `state.extracted.market.stock_analysis.max_decline_pct` or computes from price data
       - `stock_decline_prices(state) -> tuple[float, float, int] | None` -- returns (high_price, low_price, trading_days)
       - `ceo_tenure_years(state) -> float | None` -- extracts from governance leadership executives, compute from appointment date if available
       - `executive_departure_count(state) -> int | None` -- from `state.extracted.governance.leadership.recent_departures`
       - `scoring_tier(state) -> str | None` -- from `state.scoring.tier.tier.value`
       - `beneish_m_score(state) -> float | None` -- from signal results for FIN.FORENSIC.beneish_m_score
       - `piotroski_score(state) -> int | None` -- from signal results or extracted distress data
       - `credibility_score(state) -> float | None` -- from `state.forward_looking.credibility.track_record_pct`
       - `guidance_miss_count(state) -> int | None` -- from forward_looking credibility data
       - `ddl_estimate(state, decline_pct: float | None = None) -> float | None` -- DDL = market_cap * decline_pct; if decline_pct not given, use stock_decline_pct
       - `revenue_trend(state) -> str | None` -- "growing", "declining", "flat" from multi-period revenue comparison
       - `short_interest_pct(state) -> float | None` -- re-export of safe_short_interest for consistency

    Each helper follows the existing safe_sv pattern: returns None on missing data, never raises.
  </action>
  <verify>
    <automated>uv run pytest tests/stages/render/test_narrative_quality.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/stages/render/test_narrative_quality.py` returns 11 or more
    - `grep -c "def stock_decline_pct\|def ceo_tenure_years\|def ddl_estimate\|def beneish_m_score\|def scoring_tier\|def revenue_trend" src/do_uw/stages/render/sections/sect1_findings_data.py` returns 6
    - `grep "aapl_like_state\|angi_like_state" tests/stages/render/test_narrative_quality.py | wc -l` returns 5 or more (fixtures used across tests)
  </acceptance_criteria>
  <done>Test scaffold with 11+ tests exists; 12 new data helpers exist in sect1_findings_data.py; tests initially fail (RED phase of TDD)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite all neg_* and pos_* functions + delete neg_generic</name>
  <files>
    src/do_uw/stages/render/sections/sect1_findings_neg.py,
    src/do_uw/stages/render/sections/sect1_findings_pos.py,
    src/do_uw/stages/render/sections/sect1_findings.py
  </files>
  <read_first>
    src/do_uw/stages/render/sections/sect1_findings_neg.py,
    src/do_uw/stages/render/sections/sect1_findings_pos.py,
    src/do_uw/stages/render/sections/sect1_findings.py,
    src/do_uw/stages/render/sections/sect1_findings_data.py,
    tests/stages/render/test_narrative_quality.py
  </read_first>
  <behavior>
    - All 11 tests from Task 1 pass (GREEN)
    - neg_generic function and its import are completely removed
    - neg_stock_risk uses stock_decline_pct, stock_decline_prices, ddl_estimate, short_interest_pct -- severity-calibrated language
    - neg_governance uses independence_ratio, board_size, ceo_tenure_years, executive_departure_count, scoring tier
    - neg_distress uses altman_z, D/E, piotroski, current_ratio, interest_coverage, going_concern, revenue_trend
    - neg_audit_issues uses beneish_m_score value and component flags, not generic "strongest predictors" text
    - neg_guidance uses guidance_miss_count, credibility_score, not just factor score
    - pos_stable_leadership uses ceo_tenure_years and financial health context to differentiate healthy/distressed companies
    - Dispatcher in sect1_findings.py replaces neg_generic fallthrough with neg_from_finding(state, name, finding)
  </behavior>
  <action>
    **DELETE neg_generic entirely** from sect1_findings_neg.py. Remove its import from sect1_findings.py.

    **Replace the else clause** in build_negative_narrative dispatcher (line 84-85) with:
    ```python
    else:
        sentences = neg_from_finding(state, name, finding)
    ```

    **Create neg_from_finding(state, name, finding)** as the new fallback that extracts signal data from the finding's origin/narrative to build a company-specific paragraph. It should:
    - Parse the finding's evidence_narrative for signal IDs or factor references
    - Look up the signal result in state.analysis.signal_results if available
    - Pull the signal's value, evidence, threshold_context, and do_context
    - Build sentences from actual data, never from generic templates
    - If no signal data available, use the raw evidence_narrative directly (still better than neg_generic)

    **Rewrite neg_stock_risk:**
    - Add severity calibration function `_stock_decline_descriptor(pct)`:
      - >= 70%: "catastrophic collapse"
      - >= 50%: "severe decline"
      - >= 30%: "substantial decline"
      - >= 15%: "significant decline"
      - < 15%: "moderate pullback"
    - First sentence: "{name}'s stock has undergone a {descriptor} of {pct:.1f}% from ${high:.2f} to ${low:.2f} over {days} trading days."
    - DDL sentence: "This creates an estimated Disclosure Dollar Loss (DDL) of ${ddl:.1f}B, {ddl_context}." where ddl_context varies by DDL size (>$10B "among the largest potential DDL exposures", $1-10B "well above institutional lead plaintiff thresholds", <$1B "sufficient to attract plaintiff attention")
    - Short interest sentence if elevated (>5%): "Short interest at {si:.1f}% of float indicates institutional bearish positioning that often precedes or amplifies SCA filings."
    - If no decline data available, fall back to volatility framing with actual volatility numbers

    **Rewrite neg_governance:**
    - Use independence_ratio, board_size, ceo_tenure_years, executive_departure_count
    - CEO tenure context: <3yr = "recent CEO transition (tenure {n} years) creates leadership uncertainty", 3-10yr = "CEO tenure of {n} years is within normal range", >10yr with poor governance = "despite {n}-year CEO tenure, governance structure shows weaknesses"
    - Departure context if departures > 0: "Recent C-suite departures ({n} in past 12 months) strengthen the 'rats leaving a sinking ship' inference"
    - Drop the generic Caremark closing sentence; replace with specific governance risk tied to the company's actual governance metrics

    **Rewrite neg_distress:**
    - Use altman_z, D/E, piotroski, current_ratio, interest_coverage, going_concern, revenue_trend
    - Altman Z zones: <1.1 "distress zone (bankruptcy probability >80%)", 1.1-2.6 "gray zone", >2.6 "safe zone but other indicators suggest stress"
    - Add Piotroski context: score <= 3 = "Piotroski F-Score of {n}/9 confirms fundamental weakness across profitability, leverage, and operating efficiency dimensions"
    - Revenue trend context: "declining" = "Revenue trend is declining, compounding the distress profile"
    - Replace generic "financial distress elevates" closing with specific claim theory tied to the actual indicators

    **Rewrite neg_audit_issues:**
    - Remove the final generic sentence "Audit-related issues are among the strongest predictors..."
    - Add Beneish M-Score value: "Beneish M-Score of {m:.2f} with {n}/8 component flags triggered ({flag_list})"
    - Goodwill remains (already specific)
    - Close with factor-specific D&O theory tied to the actual triggered signals, not a generic statement

    **Rewrite neg_guidance:**
    - Replace generic "concerning patterns" opener with: "{name} has missed earnings guidance {n} times, with management credibility score at {pct:.0f}%"
    - If credibility_score < 50: "This sub-50% credibility score means management's forward statements carry diminished investor trust, making future misses more likely to trigger SCA filings"
    - Keep DDL context (already somewhat specific)

    **Rewrite pos_stable_leadership:**
    - Add ceo_tenure_years: "{ceo} has led {name} for {tenure:.0f} years"
    - If tenure >= 10 AND no distress indicators: "This {tenure}-year leadership continuity during sustained financial health demonstrates management competence that undermines scienter allegations"
    - If tenure < 5 OR distress present: "While {ceo} leads the organization, {context}" where context reflects the actual situation (short tenure = "the relatively brief {n}-year tenure means limited track record for evaluating management's response to adversity"; distress = "leadership stability must be weighed against current financial stress indicators")
    - Drop the generic "rats leaving a sinking ship" sentence; it's a D&O primer, not company analysis

    **Existing neg_prior_litigation, neg_enforcement, neg_doj, neg_ipo_ma, neg_short_interest:**
    Review and tighten -- remove any generic closing sentences that could apply to any company. Specifically:
    - neg_prior_litigation line 73-84: replace the generic goodwill OR "Historical litigation patterns" closing with factor-specific text using the actual litigation count and scoring impact
    - neg_enforcement lines 95-98: "SEC enforcement actions historically lead to formal charges in approximately 80% of cases" -- this is a generic primer. Add the company's actual enforcement details if available
    - neg_short_interest: "often preceding negative disclosures" -- replace with actual short interest trajectory if available, or at minimum tie to the company's specific risk profile

    **Existing pos_clean_audit, pos_strong_governance, pos_no_distress, pos_low_short, pos_no_enforcement:**
    Review and remove generic D&O primer closing sentences:
    - pos_clean_audit lines 46-49: "This eliminates several high-frequency D&O claim catalysts" is generic. Replace with specific reference to the company's clean audit track record
    - pos_strong_governance lines 89-94: "Strong governance provides a robust defense against Caremark derivative claims" is generic. Tie to actual board metrics
    - pos_no_distress lines 134-138: "Strong financial position eliminates going-concern qualifications" is generic. Tie to actual Z-score / D/E interpretation
    - pos_low_short lines 170-172: "Absence of short seller campaigns eliminates a common catalyst" is generic. Replace with actual short interest positioning context
  </action>
  <verify>
    <automated>uv run pytest tests/stages/render/test_narrative_quality.py -x -q && uv run pytest tests/stages/render/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "neg_generic" src/do_uw/stages/render/sections/sect1_findings_neg.py` returns 0
    - `grep -c "neg_generic" src/do_uw/stages/render/sections/sect1_findings.py` returns 0
    - `grep "catastrophic\|severe decline\|substantial decline\|significant decline\|moderate pullback" src/do_uw/stages/render/sections/sect1_findings_neg.py | wc -l` returns 5 (all severity levels present)
    - `grep "even moderate risk factors can generate material D&O exposure" src/do_uw/stages/render/sections/sect1_findings_neg.py` returns empty (boilerplate deleted)
    - `grep "Audit-related issues are among the strongest predictors" src/do_uw/stages/render/sections/sect1_findings_neg.py` returns empty (boilerplate deleted)
    - `grep "rats leaving a sinking ship" src/do_uw/stages/render/sections/sect1_findings_pos.py` returns empty (boilerplate deleted)
    - `grep "ceo_tenure_years\|executive_departure_count\|stock_decline_pct\|ddl_estimate\|beneish_m_score\|credibility_score" src/do_uw/stages/render/sections/sect1_findings_neg.py | wc -l` returns 3 or more (new helpers being used)
    - `uv run pytest tests/stages/render/test_narrative_quality.py -x -q` passes all 11+ tests
    - `uv run pytest tests/stages/render/ -x -q` passes (no regression)
  </acceptance_criteria>
  <done>neg_generic deleted; all neg_*/pos_* functions produce company-specific narratives using 3+ data points each; ANGI and AAPL produce visibly different text for same finding types; all quality tests pass; no render test regressions</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/stages/render/test_narrative_quality.py -v` -- all 11+ NQ tests pass
2. `uv run pytest tests/stages/render/ -x -q` -- no regressions in existing render tests
3. `grep -r "neg_generic" src/do_uw/` returns zero matches
4. `grep -r "even moderate risk factors" src/do_uw/` returns zero matches
5. `grep -r "historically correlated" src/do_uw/stages/render/` returns zero matches (in Python code)
</verification>

<success_criteria>
- neg_generic() is deleted, all references removed
- Every neg_*/pos_* function uses 3+ company-specific data points
- ANGI 73.7% decline produces "catastrophic collapse" not "notable decline"
- pos_stable_leadership produces different text for 15yr healthy CEO vs 2yr distressed CEO
- 11+ quality gate tests pass
- Zero render test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/119.1-narrative-quality-overhaul/119.1-01-SUMMARY.md`
</output>
