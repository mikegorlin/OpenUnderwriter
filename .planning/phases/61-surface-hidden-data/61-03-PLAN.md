---
phase: 61-surface-hidden-data
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/do_uw/stages/render/context_builders/governance.py
  - src/do_uw/templates/html/sections/governance/people_risk.html.j2
  - src/do_uw/templates/html/sections/governance/board_forensics.html.j2
  - src/do_uw/stages/render/context_builders/scoring.py
  - src/do_uw/templates/html/sections/scoring/allegation_mapping.html.j2
  - src/do_uw/templates/html/sections/scoring/hazard_profile.html.j2
  - src/do_uw/brain/sections/scoring.yaml
  - src/do_uw/templates/html/components.css
autonomous: true
requirements: [SURF-04, SURF-06, SURF-07, SURF-08]

must_haves:
  truths:
    - "Executive profiles in people_risk facet show shade_factors (credential issues, legal issues, pledged shares) with individual detail cards"
    - "Board forensics show shade-factor-equivalent flags (relationship_flags, true_independence_concerns, interlocks) per director"
    - "Allegation mapping template shows full theory detail: specific claim types, factor sources, D&O coverage mapping, not just exposure level"
    - "Hazard interaction effects (dynamic_interactions) show amplification multipliers and triggered dimension detail"
    - "All new displays include source attribution and confidence indicators per SURF-08"
    - "Existing templates render correctly with enhanced data"
    - "All tests pass after changes"
  artifacts:
    - path: "src/do_uw/templates/html/sections/governance/people_risk.html.j2"
      provides: "Enhanced executive profile cards with shade_factors, departure context, and forensic detail"
    - path: "src/do_uw/templates/html/sections/governance/board_forensics.html.j2"
      provides: "Enhanced director profiles with independence concerns, relationship flags, and interlocks"
    - path: "src/do_uw/templates/html/sections/scoring/allegation_mapping.html.j2"
      provides: "Enhanced allegation mapping with full theory detail, claim types, factor sources, and D&O coverage"
    - path: "src/do_uw/templates/html/sections/scoring/hazard_profile.html.j2"
      provides: "Enhanced interaction effects display with amplification multipliers and triggered dimensions"
  key_links:
    - from: "src/do_uw/stages/render/context_builders/governance.py"
      to: "src/do_uw/models/governance_forensics.py"
      via: "LeadershipForensicProfile.shade_factors, departure_context consumed"
      pattern: "shade_factors"
    - from: "src/do_uw/stages/render/context_builders/scoring.py"
      to: "src/do_uw/models/scoring_output.py"
      via: "TheoryExposure.findings, factor_sources consumed in full"
      pattern: "factor_sources"
---

<objective>
Surface hidden executive shade factors, allegation mapping detail, and hazard interaction multipliers in the HTML output.

Purpose: Executive profiles have shade_factors (credential issues, legal history, pledged shares) and departure_context (8-K analysis) fields that exist in LeadershipForensicProfile but are only partially rendered (shade_factors shown as comma-joined flags in the People Risk table). Board members have relationship_flags, true_independence_concerns, and interlocks that are extracted but minimally rendered. Allegation mapping shows theory + exposure level + 2-finding evidence snippet but the full TheoryExposure model has specific claim types, factor sources, and detailed findings that would help underwriters understand D&O coverage implications. Hazard interaction effects are shown with name/description/multiplier but the triggered_dimensions detail and the distinction between named vs dynamic interactions is lost. All new displays need source attribution per SURF-08.

Output: Enhanced executive/director profile cards with individual shade_factor detail, departure context, and forensic flags. Rich allegation mapping with claim type specifics, factor source traceability, and D&O coverage implications. Hazard interaction display with amplification detail and triggered dimension cross-references.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-surface-hidden-data/61-01-PLAN.md (dependency: compensation and source attribution patterns established)
@.planning/phases/61-surface-hidden-data/61-02-PLAN.md (dependency: hazard profile and confidence dot patterns established)

@src/do_uw/models/governance_forensics.py (LeadershipForensicProfile lines 29-83, BoardForensicProfile lines 133-193)
@src/do_uw/models/hazard_profile.py (InteractionEffect lines 143-173)
@src/do_uw/models/scoring_output.py (TheoryExposure lines 65-82, AllegationMapping lines 84-103)
@src/do_uw/stages/render/context_builders/governance.py (executives extraction lines 130-163, board_members lines 260-310)
@src/do_uw/stages/render/context_builders/scoring.py (allegation mapping lines 226-241)
@src/do_uw/stages/render/context_builders/analysis.py (interaction effects extraction lines 156-163)
@src/do_uw/templates/html/sections/governance/people_risk.html.j2 (existing exec display)
@src/do_uw/templates/html/sections/governance/board_forensics.html.j2 (existing board display)
@src/do_uw/templates/html/sections/scoring/allegation_mapping.html.j2 (existing allegations)
@src/do_uw/templates/html/sections/scoring/hazard_profile.html.j2 (existing interaction display)

<interfaces>
LeadershipForensicProfile shade/departure fields:
```python
shade_factors: list[SourcedValue[str]]      # Credential issues, legal issues, pledged shares
departure_type: str                          # PLANNED, UNPLANNED, ACTIVE
departure_date: str | None                   # ISO format
departure_context: SourcedValue[str] | None  # 8-K language and timing analysis
prior_litigation: list[SourcedValue[str]]    # Prior company litigation
prior_enforcement: list[SourcedValue[str]]   # SEC enforcement at prior companies
prior_restatements: list[SourcedValue[str]]  # Restatements at prior companies
```

BoardForensicProfile independence/interlock fields:
```python
interlocks: list[SourcedValue[str]]                    # Interlocking directorates
relationship_flags: list[SourcedValue[str]]            # Family/financial/professional ties
true_independence_concerns: list[SourcedValue[str]]    # Factors undermining independence
qualification_tags: list[str]                          # Structured qualification categories
age: SourcedValue[int] | None                          # Director age
```

TheoryExposure detailed fields:
```python
theory: AllegationTheory          # A_DISCLOSURE through E_MA
exposure_level: str               # HIGH, MODERATE, LOW
findings: list[str]               # Full evidence findings (currently truncated to 2)
factor_sources: list[str]         # Factor IDs contributing (F1, F3, etc.)
```

AllegationMapping:
```python
theories: list[TheoryExposure]
primary_exposure: AllegationTheory
concentration_analysis: str
```

InteractionEffect:
```python
interaction_id: str               # e.g. 'ROOKIE_ROCKET'
name: str                         # e.g. 'Rookie Rocket'
description: str
triggered_dimensions: list[str]   # Dimension IDs (e.g. ['H1-03', 'H5-01'])
multiplier: float                 # e.g. 1.3
is_named: bool                    # True=branded, False=dynamic
```

Allegation theory -> D&O claim type mapping:
- A_DISCLOSURE -> "10b-5 Securities Fraud, Section 11 Registration"
- B_GUIDANCE -> "10b-5 Forward Statement, Guidance Failure"
- C_PRODUCT_OPS -> "Breach of Fiduciary Duty (Caremark), Product Liability Director Exposure"
- D_GOVERNANCE -> "Breach of Fiduciary Duty (Entire Fairness), Waste Claims, Proxy Violation"
- E_MA -> "Revlon Duties, Appraisal Rights, Deal Protection"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance governance context with full shade factor and departure detail</name>
  <files>src/do_uw/stages/render/context_builders/governance.py</files>
  <action>
Enhance the executive and board member extraction in `extract_governance()`:

1. **Executive profiles** -- expand the executive dict with:
   - `shade_factors_detail`: list of dicts, each with `{"factor": str, "source": str, "confidence": str}` extracted from SourcedValue
   - `departure_context`: string from `exec_prof.departure_context` SourcedValue
   - `departure_date`: string from `exec_prof.departure_date`
   - `prior_enforcement`: list of strings from `exec_prof.prior_enforcement`
   - `prior_restatements`: list of strings from `exec_prof.prior_restatements`
   - `has_forensic_flags`: boolean -- True if any shade_factors, prior_litigation, prior_enforcement, or prior_restatements exist
   - `forensic_flag_count`: int -- total count of all forensic flags for this exec

   Currently the code already extracts shade_factors as `[_sv_str(s) for s in exec_prof.shade_factors]`. Enhance to preserve source:
   ```python
   shade_detail = []
   for s in exec_prof.shade_factors:
       shade_detail.append({
           "factor": _sv_str(s),
           "source": str(s.source) if hasattr(s, "source") and s.source else "",
           "confidence": str(s.confidence) if hasattr(s, "confidence") and s.confidence else "LOW",
       })
   ```

2. **Board members** -- expand the board member dict with:
   - `interlocks_detail`: list of dicts with source attribution
   - `relationship_flags_detail`: list of dicts with source attribution
   - `independence_concerns_detail`: list of dicts with source attribution
   - `qualification_tags`: list from `bf.qualification_tags`
   - `age`: string from `bf.age` SourcedValue
   - `has_forensic_flags`: boolean
   - `forensic_flag_count`: int

3. **Leaders dict** (for people_risk template) -- also add the enhanced detail:
   - `shade_factors_detail`: same as executives
   - `departure_context`: departure context string
   - `has_forensic_flags`: boolean
   - Keep existing `flags` and `shade_factors` for backward compat

4. Keep the file under 500 lines. If needed, extract a `_build_executive_detail()` and `_build_board_member_detail()` helper.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run python -c "from do_uw.stages.render.context_builders.governance import extract_governance; print('import OK')" && wc -l src/do_uw/stages/render/context_builders/governance.py</automated>
  </verify>
  <done>extract_governance() returns enhanced executive and board member profiles with full shade_factors_detail, departure_context, prior_enforcement, prior_restatements, and forensic flag counts. Source attribution preserved. File under 500 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance people_risk and board_forensics templates with shade factor cards</name>
  <files>
    src/do_uw/templates/html/sections/governance/people_risk.html.j2
    src/do_uw/templates/html/sections/governance/board_forensics.html.j2
  </files>
  <action>
Enhance the existing templates to display full forensic detail:

1. **people_risk.html.j2** -- enhance each executive row/card:

   After the existing table row (name, title, tenure, flags), add an expandable detail section per executive:
   ```html
   {% for leader in gov.leaders %}
   <div class="exec-card page-break-inside-avoid my-2 border rounded">
     <div class="exec-card-header px-3 py-2 bg-bg-alt flex items-center gap-2">
       <strong>{{ leader.name }}</strong>
       <span class="text-gray-500 text-sm">{{ leader.title }}</span>
       <span class="text-xs">{{ leader.tenure }}</span>
       {% if leader.has_forensic_flags %}
       <span class="badge-pill badge-tier-warning">{{ leader.forensic_flag_count }} flag{{ 's' if leader.forensic_flag_count != 1 }}</span>
       {% endif %}
     </div>
     {% if leader.shade_factors_detail or leader.departure_context or leader.prior_litigation_details %}
     <details class="collapsible">
       <summary class="px-3 py-1 text-xs text-gray-500">Forensic Detail</summary>
       <div class="px-3 py-2 space-y-2">
         {% if leader.shade_factors_detail %}
         <div>
           <span class="text-xs font-semibold text-amber-600">Shade Factors:</span>
           <ul class="text-xs list-disc pl-4 mt-1">
             {% for sf in leader.shade_factors_detail %}
             <li title="Source: {{ sf.source }}">
               {{ sf.factor }}
               <span class="confidence-dot confidence-{{ sf.confidence|lower }}"></span>
             </li>
             {% endfor %}
           </ul>
         </div>
         {% endif %}
         {% if leader.departure_context %}
         <div>
           <span class="text-xs font-semibold">Departure Context:</span>
           <p class="text-xs text-gray-600 mt-1">{{ leader.departure_context }}</p>
         </div>
         {% endif %}
         {% if leader.prior_litigation_details %}
         <div>
           <span class="text-xs font-semibold text-red-600">Prior Litigation:</span>
           <ul class="text-xs list-disc pl-4 mt-1">
             {% for lit in leader.prior_litigation_details %}
             <li>{{ lit }}</li>
             {% endfor %}
           </ul>
         </div>
         {% endif %}
       </div>
     </details>
     {% endif %}
   </div>
   {% endfor %}
   ```

   Keep the existing summary table above the detail cards so the quick view is preserved.

2. **board_forensics.html.j2** -- enhance each board member entry:

   Add expandable forensic detail per director:
   ```html
   {% if member.independence_concerns_detail or member.relationship_flags_detail or member.interlocks_detail %}
   <details class="collapsible">
     <summary class="text-xs text-gray-500">Independence & Forensic Detail</summary>
     <div class="px-2 py-1 space-y-1">
       {% if member.independence_concerns_detail %}
       <div class="text-xs">
         <span class="font-semibold text-amber-600">Independence Concerns:</span>
         <ul class="list-disc pl-4">
           {% for c in member.independence_concerns_detail %}
           <li title="Source: {{ c.source }}">{{ c.factor }} <span class="confidence-dot confidence-{{ c.confidence|lower }}"></span></li>
           {% endfor %}
         </ul>
       </div>
       {% endif %}
       {% if member.relationship_flags_detail %}
       <div class="text-xs">
         <span class="font-semibold text-amber-600">Relationship Flags:</span>
         <ul class="list-disc pl-4">
           {% for rf in member.relationship_flags_detail %}
           <li title="Source: {{ rf.source }}">{{ rf.factor }} <span class="confidence-dot confidence-{{ rf.confidence|lower }}"></span></li>
           {% endfor %}
         </ul>
       </div>
       {% endif %}
       {% if member.interlocks_detail %}
       <div class="text-xs">
         <span class="font-semibold">Interlocking Directorates:</span>
         <ul class="list-disc pl-4">
           {% for il in member.interlocks_detail %}
           <li title="Source: {{ il.source }}">{{ il.factor }}</li>
           {% endfor %}
         </ul>
       </div>
       {% endif %}
       {% if member.qualification_tags %}
       <div class="text-xs mt-1">
         <span class="font-semibold">Qualifications:</span>
         {% for tag in member.qualification_tags %}
         <span class="inline-block px-1.5 py-0.5 text-xs rounded bg-gray-100 border border-gray-200 mr-1">{{ tag|replace('_', ' ')|title }}</span>
         {% endfor %}
       </div>
       {% endif %}
     </div>
   </details>
   {% endif %}
   ```

   Include `age` display if available. Add source attribution via title attributes per SURF-08.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run python -c "
from jinja2 import Environment, FileSystemLoader
import os
tmpl_dir = os.path.join('src', 'do_uw', 'templates', 'html')
env = Environment(loader=FileSystemLoader(tmpl_dir))
t1 = env.get_template('sections/governance/people_risk.html.j2')
t2 = env.get_template('sections/governance/board_forensics.html.j2')
print('Templates load OK')
"</automated>
  </verify>
  <done>people_risk.html.j2 shows exec cards with shade factor detail, departure context, prior litigation. board_forensics.html.j2 shows independence concerns, relationship flags, interlocks, qualification tags per director. Source attribution via confidence dots and title tooltips.</done>
</task>

<task type="auto">
  <name>Task 3: Enhance allegation mapping with full theory detail and D&O coverage</name>
  <files>
    src/do_uw/stages/render/context_builders/scoring.py
    src/do_uw/templates/html/sections/scoring/allegation_mapping.html.j2
  </files>
  <action>
Enhance allegation mapping to show full theory detail:

1. In `extract_scoring()` in scoring.py, expand the allegation mapping extraction:

   Change from:
   ```python
   allegations.append({
       "theory": te.theory.value,
       "exposure_level": te.exposure_level,
       "evidence": "; ".join(te.findings[:2]) if te.findings else "",
   })
   ```

   To:
   ```python
   # Full allegation theory detail
   theory_name = _THEORY_NAMES.get(te.theory.value, te.theory.value)
   claim_types = _THEORY_CLAIM_TYPES.get(te.theory.value, [])
   allegations.append({
       "theory": te.theory.value,
       "theory_name": theory_name,
       "exposure_level": te.exposure_level,
       "evidence": "; ".join(te.findings[:2]) if te.findings else "",
       "all_findings": te.findings,  # Full findings list (SURF-07)
       "factor_sources": te.factor_sources,  # Contributing factors (SURF-07)
       "claim_types": claim_types,  # D&O coverage mapping (SURF-07)
   })
   ```

   Add mapping dicts at module level:
   ```python
   _THEORY_NAMES: dict[str, str] = {
       "A_DISCLOSURE": "Disclosure & Reporting",
       "B_GUIDANCE": "Guidance & Forward Statements",
       "C_PRODUCT_OPS": "Product & Operations",
       "D_GOVERNANCE": "Governance & Fiduciary",
       "E_MA": "M&A & Corporate Transactions",
   }

   _THEORY_CLAIM_TYPES: dict[str, list[str]] = {
       "A_DISCLOSURE": ["10b-5 Securities Fraud", "Section 11 Registration", "SOX Violations"],
       "B_GUIDANCE": ["10b-5 Forward Statement", "Guidance Failure", "Selective Disclosure"],
       "C_PRODUCT_OPS": ["Caremark (Oversight Failure)", "Product Liability Director Exposure", "Environmental/Safety"],
       "D_GOVERNANCE": ["Entire Fairness", "Waste Claims", "Proxy Violations", "Say-on-Pay"],
       "E_MA": ["Revlon Duties", "Appraisal Rights", "Deal Protection Scrutiny"],
   }
   ```

2. Enhance `allegation_mapping.html.j2`:

   Replace the existing simple table with a richer display:

   A. **Exposure Summary Bar** (visual overview):
   ```html
   <div class="allegation-bar my-3">
     {% for a in allegations %}
     <div class="allegation-segment
       {% if a.exposure_level == 'HIGH' %}bg-red-500
       {% elif a.exposure_level == 'MODERATE' %}bg-amber-500
       {% else %}bg-green-500{% endif %}"
       title="{{ a.theory_name }}: {{ a.exposure_level }}"
       style="flex: 1">
       <span>{{ a.theory|truncate(3, True, '') }}</span>
     </div>
     {% endfor %}
   </div>
   ```

   B. **Detail Cards** per theory (expandable):
   ```html
   {% for a in allegations %}
   <details class="collapsible allegation-card my-2"
     {% if a.exposure_level == 'HIGH' %}open{% endif %}>
     <summary>
       {{ traffic_light(a.exposure_level, a.theory_name) }}
       <span class="text-xs text-gray-500 ml-2">{{ a.factor_sources|join(', ') }}</span>
     </summary>
     <div class="px-3 py-2">
       {% if a.claim_types %}
       <div class="mb-2">
         <span class="text-xs font-semibold">D&O Claim Types:</span>
         <div class="flex flex-wrap gap-1 mt-1">
           {% for ct in a.claim_types %}
           <span class="inline-block px-2 py-0.5 text-xs rounded border border-gray-200 bg-gray-50">{{ ct }}</span>
           {% endfor %}
         </div>
       </div>
       {% endif %}
       {% if a.all_findings %}
       <div>
         <span class="text-xs font-semibold">Evidence:</span>
         <ul class="text-xs list-disc pl-4 mt-1 text-gray-600">
           {% for f in a.all_findings %}
           <li>{{ f }}</li>
           {% endfor %}
         </ul>
       </div>
       {% endif %}
       {% if a.factor_sources %}
       <div class="mt-1">
         <span class="text-xs text-gray-400">Contributing Factors: {{ a.factor_sources|join(', ') }}</span>
       </div>
       {% endif %}
     </div>
   </details>
   {% endfor %}
   ```

   C. Keep existing concentration analysis and primary exposure display.

   D. Add D&O coverage mapping summary after the cards:
   ```html
   <div class="bg-bg-alt border border-gray-200 rounded p-3 my-2 text-xs">
     <strong>D&O Coverage Implications:</strong>
     <ul class="list-disc pl-4 mt-1">
       <li>Maps risk indicators to specific claim types: 10b-5, Section 11, breach of fiduciary duty</li>
       <li>High-exposure theories indicate areas most likely to generate claims against the D&O policy</li>
       <li>Factor sources trace each theory back to the 10-factor scoring evidence</li>
     </ul>
   </div>
   ```
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run python -c "from do_uw.stages.render.context_builders.scoring import extract_scoring; print('import OK')" && wc -l src/do_uw/stages/render/context_builders/scoring.py</automated>
  </verify>
  <done>extract_scoring() returns full theory detail with claim types and factor sources. allegation_mapping.html.j2 shows exposure bar, expandable theory cards with D&O claim types, full evidence, and factor traceability. scoring.py under 500 lines.</done>
</task>

<task type="auto">
  <name>Task 4: Enhance hazard interaction display with amplification detail</name>
  <files>
    src/do_uw/stages/render/context_builders/analysis.py
    src/do_uw/templates/html/sections/scoring/hazard_profile.html.j2
  </files>
  <action>
Enhance the interaction effects display in the hazard profile:

1. In `extract_hazard_profile()` in analysis.py, enhance interaction extraction:

   Change from:
   ```python
   interactions.append({
       "name": ie.name,
       "description": ie.description,
       "multiplier": f"{ie.multiplier:.2f}x",
       "triggered_dims": ", ".join(ie.triggered_dimensions[:3]),
   })
   ```

   To:
   ```python
   interactions.append({
       "name": ie.name,
       "interaction_id": ie.interaction_id,
       "description": ie.description,
       "multiplier": f"{ie.multiplier:.2f}x",
       "multiplier_raw": ie.multiplier,
       "triggered_dims": ie.triggered_dimensions,  # Full list, not truncated
       "triggered_dims_display": ", ".join(ie.triggered_dimensions[:5]),
       "is_named": ie.is_named,
       "type_label": "Named Pattern" if ie.is_named else "Dynamic Detection",
   })
   ```

   Also separate named and dynamic interactions in the returned dict:
   ```python
   result["named_interactions"] = [i for i in interactions if i["is_named"]]
   result["dynamic_interactions"] = [i for i in interactions if not i["is_named"]]
   ```

   Add overall `interaction_multiplier` to the result:
   ```python
   result["interaction_multiplier"] = f"{hp.interaction_multiplier:.2f}x"
   ```

2. In `hazard_profile.html.j2`, enhance the "Interaction Effects" section:

   Replace the existing simple display with:

   ```html
   {% if hazard_profile.interactions %}
   <h4>Interaction Effects
     <span class="text-sm font-normal text-gray-500">
       (Combined: {{ hazard_profile.interaction_multiplier }})
     </span>
   </h4>

   {% if hazard_profile.named_interactions %}
   <div class="space-y-2 my-2">
     {% for ie in hazard_profile.named_interactions %}
     <div class="interaction-card border-l-3 border-amber-500 pl-3 py-2 bg-amber-50 rounded-r">
       <div class="flex items-center gap-2">
         <span class="font-semibold text-sm">{{ ie.name }}</span>
         <span class="badge-pill" style="background: #d97706; color: white;">{{ ie.multiplier }}</span>
         <span class="text-xs text-gray-400">Named Pattern</span>
       </div>
       <p class="text-xs text-gray-600 m-0 mt-1">{{ ie.description }}</p>
       {% if ie.triggered_dims %}
       <div class="mt-1">
         <span class="text-xs text-gray-400">Triggered Dimensions:</span>
         {% for dim_id in ie.triggered_dims %}
         <span class="inline-block px-1 py-0.5 text-xs rounded bg-white border border-gray-200 mr-1"
               title="Click to find in category cards above">{{ dim_id }}</span>
         {% endfor %}
       </div>
       {% endif %}
     </div>
     {% endfor %}
   </div>
   {% endif %}

   {% if hazard_profile.dynamic_interactions %}
   <details class="collapsible my-2">
     <summary class="text-sm">Dynamic Interactions ({{ hazard_profile.dynamic_interactions|length }})</summary>
     <div class="space-y-2 mt-2">
       {% for ie in hazard_profile.dynamic_interactions %}
       <div class="border-l-3 border-gray-300 pl-3 py-1">
         <div class="flex items-center gap-2">
           <span class="font-semibold text-sm">{{ ie.name }}</span>
           <span class="text-xs tabular-nums font-mono">{{ ie.multiplier }}</span>
         </div>
         <p class="text-xs text-gray-500 m-0">{{ ie.description }}</p>
         <span class="text-xs text-gray-400">Dims: {{ ie.triggered_dims_display }}</span>
       </div>
       {% endfor %}
     </div>
   </details>
   {% endif %}
   {% endif %}
   ```
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run python -c "from do_uw.stages.render.context_builders.analysis import extract_hazard_profile; print('import OK')" && wc -l src/do_uw/stages/render/context_builders/analysis.py</automated>
  </verify>
  <done>Hazard interactions display shows named patterns prominently with multiplier badges and triggered dimension cross-references. Dynamic interactions in collapsible section. Combined multiplier shown in header. Source attribution via dimension IDs.</done>
</task>

<task type="auto">
  <name>Task 5: Add CSS for exec cards, allegation cards, and interaction cards</name>
  <files>src/do_uw/templates/html/components.css</files>
  <action>
Add CSS to components.css for the new UI components:

1. **Executive profile cards**:
   ```css
   .exec-card {
     border: 1px solid #e2e8f0;
     border-radius: 6px;
     overflow: hidden;
   }
   .exec-card-header {
     display: flex;
     align-items: center;
     gap: 0.5rem;
     flex-wrap: wrap;
   }
   ```

2. **Allegation bar** (visual theory exposure overview):
   ```css
   .allegation-bar {
     display: flex;
     height: 24px;
     border-radius: 4px;
     overflow: hidden;
     gap: 2px;
   }
   .allegation-segment {
     display: flex;
     align-items: center;
     justify-content: center;
     color: white;
     font-size: 0.65rem;
     font-weight: 600;
   }
   ```

3. **Allegation detail cards**:
   ```css
   .allegation-card > summary {
     display: flex;
     align-items: center;
     gap: 0.5rem;
     cursor: pointer;
   }
   ```

4. **Interaction cards** (named vs dynamic):
   ```css
   .interaction-card {
     transition: background-color 0.2s;
   }
   ```

Verify components.css stays under 500 lines.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && wc -l src/do_uw/templates/html/components.css</automated>
  </verify>
  <done>CSS added for exec-card, allegation-bar, allegation-card, and interaction-card components. components.css under 500 lines.</done>
</task>

<task type="auto">
  <name>Task 6: Run full test suite and verify all SURF requirements</name>
  <files></files>
  <action>
1. Run full test suite:
   ```bash
   uv run pytest tests/ -x -q --timeout=120
   ```

2. Fix any failures.

3. Verify all SURF requirements are met by checking template coverage:
   - SURF-01: `grep -l "compensation_analysis" src/do_uw/templates/html/sections/governance/compensation_analysis.html.j2` exists
   - SURF-02: `grep -l "peer_matrix" src/do_uw/templates/html/sections/financial/peer_matrix.html.j2` exists
   - SURF-03: `grep -l "nlp_dashboard\|lm_trends\|management_tone" src/do_uw/templates/html/sections/scoring/nlp_dashboard.html.j2` exists
   - SURF-04: `grep -l "interaction_multiplier\|dynamic_interactions" src/do_uw/templates/html/sections/scoring/hazard_profile.html.j2` matches
   - SURF-05: `grep -l "categories_with_dimensions\|hazard-card" src/do_uw/templates/html/sections/scoring/hazard_profile.html.j2` matches
   - SURF-06: `grep -l "shade_factors_detail\|departure_context" src/do_uw/templates/html/sections/governance/people_risk.html.j2` matches
   - SURF-07: `grep -l "claim_types\|factor_sources\|all_findings" src/do_uw/templates/html/sections/scoring/allegation_mapping.html.j2` matches
   - SURF-08: `grep -rl "confidence-dot\|data-source\|_sources\|_confidence" src/do_uw/templates/html/` shows multiple hits across new templates

4. Verify all context builders under 500 lines:
   ```bash
   wc -l src/do_uw/stages/render/context_builders/*.py
   ```

5. Run a quick import check for all context builder modules:
   ```bash
   uv run python -c "from do_uw.stages.render.context_builders import *; print('All imports OK')"
   ```
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run pytest tests/ -x -q --timeout=120 2>&1 | tail -10</automated>
  </verify>
  <done>All tests pass. All 8 SURF requirements verified: compensation analysis visible, peer matrix visible, NLP dashboard with trends, hazard interactions with multipliers, all 55 hazard dimensions expandable, exec shade factors shown, allegation detail with D&O coverage, source attribution throughout. All files under 500 lines.</done>
</task>

</tasks>

<verification>
After all 6 tasks:
1. `uv run pytest tests/ -x -q` passes (zero regressions)
2. Executive profiles show shade_factors detail cards with source attribution (SURF-06)
3. Board members show independence concerns, relationship flags, interlocks (SURF-06)
4. Allegation mapping shows full theory detail with D&O claim types and factor sources (SURF-07)
5. Hazard interactions show named/dynamic distinction with multiplier badges and triggered dimensions (SURF-04)
6. All new displays have source attribution and confidence indicators (SURF-08)
7. All context builder files under 500 lines
8. All brain YAML files correctly define new facets
</verification>

<success_criteria>
- Executive shade factors visible with individual detail (SURF-06)
- Allegation mapping shows specific claim types and factor sources (SURF-07)
- Hazard interactions display amplification multipliers and triggered dimensions (SURF-04)
- Source attribution and confidence on all new displays (SURF-08)
- Zero test regressions
- All files under 500-line limit
</success_criteria>

<output>
After completion, create `.planning/phases/61-surface-hidden-data/61-03-SUMMARY.md`
</output>
