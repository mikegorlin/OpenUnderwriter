---
phase: 58-shared-context-layer
plan: 02
type: execute
wave: 2
depends_on:
  - "58-01"
files_modified:
  - src/do_uw/stages/render/context_builders/governance.py
  - src/do_uw/stages/render/context_builders/litigation.py
  - src/do_uw/stages/render/context_builders/scoring.py
  - src/do_uw/stages/render/context_builders/analysis.py
  - src/do_uw/stages/render/context_builders/calibration.py
  - src/do_uw/stages/render/context_builders/__init__.py
autonomous: true
requirements:
  - CTX-01
  - CTX-02
  - CTX-06

must_haves:
  truths:
    - "Governance, litigation, scoring, analysis, and calibration extractors live in context_builders/"
    - "Each extract_* function is an exact copy from the original — zero logic changes"
    - "Every context_builders module is under 500 lines"
    - "The _ext.py re-export of extract_governance is preserved: litigation.py does NOT re-export governance"
    - "__init__.py re-exports all public functions from all 9 context_builders modules (extends Plan 01's __init__.py)"
  artifacts:
    - path: "src/do_uw/stages/render/context_builders/governance.py"
      provides: "extract_governance from md_renderer_helpers_governance.py"
      min_lines: 200
    - path: "src/do_uw/stages/render/context_builders/litigation.py"
      provides: "extract_litigation from md_renderer_helpers_ext.py"
      min_lines: 300
    - path: "src/do_uw/stages/render/context_builders/scoring.py"
      provides: "extract_scoring, extract_ai_risk, extract_meeting_questions from md_renderer_helpers_scoring.py"
      min_lines: 250
    - path: "src/do_uw/stages/render/context_builders/analysis.py"
      provides: "8 extract_* functions from md_renderer_helpers_analysis.py"
      min_lines: 300
    - path: "src/do_uw/stages/render/context_builders/calibration.py"
      provides: "render_calibration_notes from md_renderer_helpers_calibration.py"
      min_lines: 100
  key_links:
    - from: "src/do_uw/stages/render/context_builders/__init__.py"
      to: "all 9 context_builders domain modules"
      via: "re-export of every public function"
      pattern: "from .governance import|from .litigation import|from .scoring import|from .analysis import|from .calibration import"
    - from: "src/do_uw/stages/render/context_builders/litigation.py"
      to: "src/do_uw/stages/render/context_builders/governance.py"
      via: "NO import — litigation is independent. The old _ext.py imported governance for re-export only."
      pattern: "extract_litigation"
---

<objective>
Move the remaining 5 extractor files into context_builders/: governance, litigation, scoring, analysis, and calibration.

Purpose: Complete the context_builders/ package with all 9 domain modules (Plan 01 created 4: company, financials, financials_balance, market). After this plan, every extract_* function exists in context_builders/ and the old helpers files can become shims (Plan 03).

Output: 5 new modules in context_builders/, __init__.py extended with all re-exports (builds on Plan 01's __init__.py).
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-shared-context-layer/58-01-PLAN.md

<interfaces>
<!-- Source files being moved. Executor needs exact function signatures. -->

From src/do_uw/stages/render/md_renderer_helpers_governance.py (374 lines):
```python
def _sv_str(sv: Any, fallback: str = "N/A") -> str: ...
def _sv_bool(sv: Any) -> str: ...
def _safe_ceo_comp(comp: Any) -> str: ...
def extract_governance(state: AnalysisState) -> dict[str, Any]: ...
```

From src/do_uw/stages/render/md_renderer_helpers_ext.py (495 lines):
```python
# NOTE: This file re-exports extract_governance from _governance.py:
from do_uw.stages.render.md_renderer_helpers_governance import extract_governance as extract_governance
# It also has a CLAIM_TYPE_NAMES dict and duplicated _sv_str helper

def _sv_str(sv: Any, fallback: str = "N/A") -> str: ...  # DUPLICATE of governance's version
def _extract_sca_cases(lit: Any) -> list[dict[str, str]]: ...
def _extract_sol_windows(lit: Any) -> list[dict[str, str]]: ...
def _extract_derivative_suits(lit: Any) -> list[dict[str, str]]: ...
def _extract_contingent_liabilities(lit: Any) -> list[dict[str, str]]: ...
def _extract_workforce_product_env(lit: Any) -> dict[str, list[str]]: ...
def _extract_whistleblower_indicators(lit: Any) -> list[dict[str, str]]: ...
def extract_litigation(state: AnalysisState) -> dict[str, Any]: ...
```

From src/do_uw/stages/render/md_renderer_helpers_scoring.py (411 lines):
```python
def _load_crf_conditions() -> dict[str, str]: ...
def _format_pattern_description(p: Any) -> str: ...
def extract_scoring(state: AnalysisState) -> dict[str, Any]: ...
def extract_ai_risk(state: AnalysisState) -> dict[str, Any] | None: ...
def _ai_threat_level(score: float | None) -> str: ...
def extract_meeting_questions(state: AnalysisState, ...) -> list[dict[str, str]]: ...
```

From src/do_uw/stages/render/md_renderer_helpers_analysis.py (477 lines):
```python
def _score_to_exposure(score: float) -> str: ...
def extract_classification(state: AnalysisState) -> dict[str, Any] | None: ...
def extract_hazard_profile(state: AnalysisState) -> dict[str, Any] | None: ...
def extract_risk_factors(state: AnalysisState) -> list[dict[str, Any]] | None: ...
def extract_forensic_composites(state: AnalysisState) -> dict[str, Any] | None: ...
def extract_executive_risk(state: AnalysisState) -> dict[str, Any] | None: ...
def extract_temporal_signals(state: AnalysisState) -> list[dict[str, str]] | None: ...
def extract_nlp_signals(state: AnalysisState) -> dict[str, Any] | None: ...
def extract_peril_map(state: AnalysisState) -> dict[str, Any] | None: ...
```

From src/do_uw/stages/render/md_renderer_helpers_calibration.py (218 lines):
```python
def render_calibration_notes(state: AnalysisState) -> str: ...
def _query_signal_counts(conn: Any) -> tuple[int, int]: ...
def _query_pending_feedback_count(conn: Any) -> int: ...
def _query_recent_changes(conn: Any) -> list[dict[str, str]]: ...
def _query_ticker_feedback(conn: Any, ticker: str) -> list[dict[str, str]]: ...
```

Key consumers of these functions:
- md_renderer.py: imports extract_governance (via _ext), extract_litigation, extract_scoring, extract_ai_risk, extract_meeting_questions, all 8 analysis extractors, render_calibration_notes (lazy)
- html_renderer.py: calls build_html_context which calls build_template_context
- Word sect2_company_hazard.py: imports extract_classification, extract_hazard_profile from _analysis
- Word sect_calibration.py: lazy imports render_calibration_notes from _calibration
- Tests: import extract_scoring, _load_crf_conditions from _scoring; render_calibration_notes from _calibration
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create governance and litigation modules</name>
  <files>
    src/do_uw/stages/render/context_builders/governance.py,
    src/do_uw/stages/render/context_builders/litigation.py
  </files>
  <action>
1. Create `context_builders/governance.py` by copying the ENTIRE contents of `md_renderer_helpers_governance.py` (374 lines):
   - Copy all imports
   - Copy `_sv_str()`, `_sv_bool()`, `_safe_ceo_comp()` private helpers
   - Copy `extract_governance()` — EXACT same code, zero logic changes
   - Update module docstring only

2. Create `context_builders/litigation.py` by copying the ENTIRE contents of `md_renderer_helpers_ext.py` (495 lines), EXCLUDING the governance re-export:
   - Copy all imports EXCEPT: `from do_uw.stages.render.md_renderer_helpers_governance import extract_governance`
   - Copy the `CLAIM_TYPE_NAMES` dict
   - Copy `_sv_str()` — NOTE: this is a DUPLICATE of the one in governance.py. Keep it as-is (the old _ext.py had the same duplication with comment "Duplicated from md_renderer_helpers to avoid circular import"). Do NOT remove the duplication — it avoids a cross-module import.
   - Copy ALL private helpers: `_extract_sca_cases()`, `_extract_sol_windows()`, `_extract_derivative_suits()`, `_extract_contingent_liabilities()`, `_extract_workforce_product_env()`, `_extract_whistleblower_indicators()`
   - Copy `extract_litigation()` — EXACT same code
   - Update module docstring only
   - This file will be ~480 lines (under 500 limit per CTX-06)

IMPORTANT: The old `md_renderer_helpers_ext.py` re-exported `extract_governance` so that `md_renderer.py` could import both from the same module. In the new structure, `md_renderer.py` will import governance and litigation from SEPARATE context_builders modules. Do NOT add a governance re-export to litigation.py.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run python -c "from do_uw.stages.render.context_builders.governance import extract_governance; from do_uw.stages.render.context_builders.litigation import extract_litigation; print('OK')"</automated>
  </verify>
  <done>governance.py has extract_governance (~374 lines), litigation.py has extract_litigation (~480 lines, no governance re-export). Both import cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create scoring, analysis, and calibration modules; finalize __init__.py</name>
  <files>
    src/do_uw/stages/render/context_builders/scoring.py,
    src/do_uw/stages/render/context_builders/analysis.py,
    src/do_uw/stages/render/context_builders/calibration.py,
    src/do_uw/stages/render/context_builders/__init__.py
  </files>
  <action>
1. Create `context_builders/scoring.py` by copying the ENTIRE contents of `md_renderer_helpers_scoring.py` (411 lines):
   - Copy all imports
   - Copy `_load_crf_conditions()`, `_format_pattern_description()`, `_ai_threat_level()` private helpers
   - Copy `extract_scoring()`, `extract_ai_risk()`, `extract_meeting_questions()` — EXACT same code
   - Update module docstring only

2. Create `context_builders/analysis.py` by copying the ENTIRE contents of `md_renderer_helpers_analysis.py` (477 lines):
   - Copy all imports
   - Copy `_score_to_exposure()` private helper
   - Copy all 8 extract_* functions — EXACT same code:
     extract_classification, extract_hazard_profile, extract_risk_factors,
     extract_forensic_composites, extract_executive_risk, extract_temporal_signals,
     extract_nlp_signals, extract_peril_map
   - Update module docstring only

3. Create `context_builders/calibration.py` by copying the ENTIRE contents of `md_renderer_helpers_calibration.py` (218 lines):
   - Copy all imports
   - Copy `render_calibration_notes()` and all `_query_*` private helpers
   - Update module docstring only

4. Update `context_builders/__init__.py` to include ALL re-exports from ALL 9 domain modules (the 4 from Plan 01 + 5 from this plan). Note: financials_balance only has private helpers — it is NOT re-exported from __init__.py (financials.py imports it directly). The final __init__.py should be:

```python
"""Format-agnostic context builders for D&O worksheet rendering.

Each module extracts structured data from AnalysisState into template-ready
dicts. Both HTML and Word renderers consume these same context builders.

Modules:
  company           - Company profile + executive summary (from _narrative.py)
  financials        - Income statement extraction (from _financial_income.py)
  financials_balance - Balance sheet helpers (from _financial_balance.py, private)
  market            - Market data + insider activity (from _tables.py)
  governance        - Board + leadership extraction (from _governance.py)
  litigation        - Litigation landscape (from _ext.py)
  scoring           - Risk scoring + AI risk + meeting questions (from _scoring.py)
  analysis          - Classification, hazard, composites, NLP, peril map (from _analysis.py)
  calibration       - Brain calibration notes (from _calibration.py)
"""

from do_uw.stages.render.context_builders.analysis import (
    extract_classification,
    extract_executive_risk,
    extract_forensic_composites,
    extract_hazard_profile,
    extract_nlp_signals,
    extract_peril_map,
    extract_risk_factors,
    extract_temporal_signals,
)
from do_uw.stages.render.context_builders.calibration import (
    render_calibration_notes,
)
from do_uw.stages.render.context_builders.company import (
    extract_company,
    extract_exec_summary,
)
from do_uw.stages.render.context_builders.financials import (
    extract_financials,
    find_line_item_value,
)
from do_uw.stages.render.context_builders.governance import (
    extract_governance,
)
from do_uw.stages.render.context_builders.litigation import (
    extract_litigation,
)
from do_uw.stages.render.context_builders.market import (
    dim_display_name,
    extract_market,
)
from do_uw.stages.render.context_builders.scoring import (
    extract_ai_risk,
    extract_meeting_questions,
    extract_scoring,
)

__all__ = [
    "dim_display_name",
    "extract_ai_risk",
    "extract_classification",
    "extract_company",
    "extract_exec_summary",
    "extract_executive_risk",
    "extract_financials",
    "extract_forensic_composites",
    "extract_governance",
    "extract_hazard_profile",
    "extract_litigation",
    "extract_market",
    "extract_meeting_questions",
    "extract_nlp_signals",
    "extract_peril_map",
    "extract_risk_factors",
    "extract_scoring",
    "extract_temporal_signals",
    "find_line_item_value",
    "render_calibration_notes",
]
```

5. Verify all imports work:
```bash
cd /Users/gorlin/projects/UW/do-uw && uv run python -c "
from do_uw.stages.render.context_builders import (
    extract_company, extract_exec_summary,
    extract_financials, find_line_item_value,
    extract_market, dim_display_name,
    extract_governance, extract_litigation,
    extract_scoring, extract_ai_risk, extract_meeting_questions,
    extract_classification, extract_hazard_profile, extract_risk_factors,
    extract_forensic_composites, extract_executive_risk,
    extract_temporal_signals, extract_nlp_signals, extract_peril_map,
    render_calibration_notes,
)
print('All 20 functions imported OK')
"
```

CRITICAL: Zero logic changes in any function. Pure file reorganization.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run python -c "from do_uw.stages.render.context_builders import extract_governance, extract_litigation, extract_scoring, extract_ai_risk, extract_classification, render_calibration_notes; print('OK')"</automated>
  </verify>
  <done>All 9 context_builders modules exist. __init__.py re-exports all 20 public functions. Every module under 500 lines. Zero logic changes.</done>
</task>

</tasks>

<verification>
```bash
# All 10 files exist (9 domain modules + __init__)
ls -la src/do_uw/stages/render/context_builders/*.py | wc -l  # should be 10 (9 modules + __init__)

# Line counts — every module under 500
wc -l src/do_uw/stages/render/context_builders/*.py

# All 20 public functions importable from package
cd /Users/gorlin/projects/UW/do-uw && uv run python -c "
from do_uw.stages.render.context_builders import (
    extract_company, extract_exec_summary,
    extract_financials, find_line_item_value,
    extract_market, dim_display_name,
    extract_governance, extract_litigation,
    extract_scoring, extract_ai_risk, extract_meeting_questions,
    extract_classification, extract_hazard_profile, extract_risk_factors,
    extract_forensic_composites, extract_executive_risk,
    extract_temporal_signals, extract_nlp_signals, extract_peril_map,
    render_calibration_notes,
)
print(f'All {len(dir())} names imported OK')
"

# Existing tests still pass (old helpers untouched, new modules are additive)
cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/render/ -x -q --timeout=60 2>&1 | tail -5
```
</verification>

<success_criteria>
1. 5 new modules in context_builders/: governance, litigation, scoring, analysis, calibration
2. Combined with Plan 01's 5 modules (company, financials, financials_balance, market) = 9 domain modules + __init__.py = 10 files in package
3. Every module under 500 lines (CTX-06)
4. 20 public functions re-exported from __init__.py
5. Zero logic changes in any function (CTX-02)
6. Existing render tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/58-shared-context-layer/58-02-SUMMARY.md`
</output>
