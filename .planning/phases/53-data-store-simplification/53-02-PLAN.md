---
phase: 53-data-store-simplification
plan: 02
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - src/do_uw/stages/acquire/brain_requirements.py
  - src/do_uw/stages/extract/extraction_manifest.py
  - src/do_uw/stages/extract/board_governance.py
  - src/do_uw/stages/extract/sca_extractor.py
  - src/do_uw/stages/extract/tax_indicators.py
  - src/do_uw/stages/extract/ownership_structure.py
  - src/do_uw/stages/extract/xbrl_mapping.py
  - src/do_uw/stages/extract/profile_helpers.py
  - src/do_uw/stages/extract/sol_mapper.py
  - src/do_uw/stages/analyze/__init__.py
  - src/do_uw/stages/analyze/temporal_engine.py
  - src/do_uw/stages/analyze/executive_forensics.py
  - src/do_uw/stages/analyze/executive_data.py
  - src/do_uw/stages/analyze/adverse_events.py
  - src/do_uw/stages/analyze/pipeline_audit.py
  - src/do_uw/stages/analyze/industry_claims.py
  - src/do_uw/stages/analyze/forensic_composites.py
  - src/do_uw/stages/analyze/layers/hazard/hazard_engine.py
  - src/do_uw/stages/analyze/layers/classify/classification_engine.py
  - src/do_uw/stages/score/__init__.py
  - src/do_uw/stages/score/case_characteristics.py
  - src/do_uw/stages/score/settlement_prediction.py
  - src/do_uw/stages/score/peril_mapping.py
  - src/do_uw/stages/score/ai_risk_scoring.py
  - src/do_uw/stages/benchmark/__init__.py
  - src/do_uw/stages/benchmark/benchmark_enrichments.py
  - src/do_uw/stages/render/scoring_peril_data.py
  - src/do_uw/stages/render/sections/sect7_coverage_gaps.py
  - src/do_uw/knowledge/compat_loader.py
  - src/do_uw/knowledge/calibrate_impact.py
  - src/do_uw/knowledge/backtest.py
  - src/do_uw/knowledge/gap_detector.py
  - src/do_uw/brain/brain_loader.py
  - src/do_uw/brain/brain_config_loader.py
  - src/do_uw/brain/brain_loader_rows.py
  - src/do_uw/config/loader.py
  - src/do_uw/config/__init__.py
  - tests/brain/test_brain_loader.py
  - tests/config/test_loader.py
  - tests/knowledge/test_compat_loader.py
  - tests/knowledge/test_persistent_store.py
  - tests/knowledge/test_integration.py
  - tests/knowledge/test_playbooks.py
  - tests/knowledge/test_enriched_roundtrip.py
  - tests/test_pattern_detection.py
autonomous: true
requirements:
  - STORE-03
  - STORE-04

must_haves:
  truths:
    - "Zero imports of BrainDBLoader remain in src/ (grep confirms)"
    - "Zero imports of BrainKnowledgeLoader remain in src/ (grep confirms)"
    - "Zero imports of load_brain_config from brain_config_loader remain in src/ (grep confirms)"
    - "Zero imports of ConfigLoader remain in src/ (grep confirms)"
    - "config/ directory is deleted (only brain/config/ exists)"
    - "Pipeline stages read signals and configs via BrainLoader without touching DuckDB definition tables"
    - "All 3,967+ tests pass after migration"
  artifacts:
    - path: "src/do_uw/brain/brain_unified_loader.py"
      provides: "Unified loader (from Plan 01, unchanged)"
    - path: "src/do_uw/brain/brain_loader.py"
      provides: "DELETED or gutted — old BrainDBLoader no longer importable for pipeline use"
    - path: "src/do_uw/knowledge/compat_loader.py"
      provides: "DELETED — BrainKnowledgeLoader/BackwardCompatLoader removed"
  key_links:
    - from: "src/do_uw/stages/analyze/__init__.py"
      to: "src/do_uw/brain/brain_unified_loader.py"
      via: "from do_uw.brain.brain_unified_loader import BrainLoader"
      pattern: "brain_unified_loader"
    - from: "src/do_uw/stages/score/__init__.py"
      to: "src/do_uw/brain/brain_unified_loader.py"
      via: "from do_uw.brain.brain_unified_loader import BrainLoader"
      pattern: "brain_unified_loader"
    - from: "src/do_uw/stages/extract/board_governance.py"
      to: "src/do_uw/brain/brain_unified_loader.py"
      via: "from do_uw.brain.brain_unified_loader import load_config"
      pattern: "load_config"
---

<objective>
Migrate all import sites from old loaders (BrainDBLoader, BrainKnowledgeLoader, BackwardCompatLoader, ConfigLoader, load_brain_config) to the new BrainLoader. Delete old loader modules and the config/ directory.

Purpose: Complete STORE-03 (single loader replaces 4) and STORE-04 (DuckDB scoped to history only) by cutting over every pipeline stage and knowledge module to read from YAML/JSON via BrainLoader instead of DuckDB definition tables.

Output: Zero remaining imports of old loaders in src/. config/ directory deleted. Old loader files deleted or gutted. All tests pass.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-data-store-simplification/53-CONTEXT.md
@.planning/phases/53-data-store-simplification/53-RESEARCH.md
@.planning/phases/53-data-store-simplification/53-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: BrainLoader API to migrate to -->

From src/do_uw/brain/brain_unified_loader.py (created in Plan 01):
```python
# Module-level functions (preferred for simple config reads)
def load_signals() -> dict[str, Any]:     # {"signals": [...], "total_signals": N}
def load_config(key: str) -> dict[str, Any]:  # reads brain/config/{key}.json
def load_scoring() -> dict[str, Any]:
def load_patterns() -> dict[str, Any]:
def load_red_flags() -> dict[str, Any]:
def load_sectors() -> dict[str, Any]:
def load_all() -> BrainConfig:
def load_perils() -> list[dict[str, Any]]:
def load_causal_chains() -> list[dict[str, Any]]:
def load_taxonomy() -> dict[str, list[dict[str, Any]]]:
def load_backlog() -> list[dict[str, Any]]:

# Backward-compat function (same signature as old brain_config_loader.load_brain_config)
def load_brain_config(key: str, config_dir: Path | None = None) -> dict[str, Any]:

# Class wrapper (for callers expecting an instance)
class BrainLoader:
    def load_signals(self) -> dict[str, Any]: ...
    def load_all(self) -> BrainConfig: ...
    # ... all same methods

# BrainConfig model (relocated from config/loader.py)
class BrainConfig(BaseModel):
    checks: dict[str, Any]
    scoring: dict[str, Any]
    patterns: dict[str, Any]
    sectors: dict[str, Any]
    red_flags: dict[str, Any]
```

Migration patterns:
```python
# OLD: from do_uw.brain.brain_config_loader import load_brain_config
# NEW: from do_uw.brain.brain_unified_loader import load_config
# (or load_brain_config for minimal diff)

# OLD: from do_uw.brain.brain_loader import BrainDBLoader
#      loader = BrainDBLoader()
#      brain = loader.load_all()
# NEW: from do_uw.brain.brain_unified_loader import BrainLoader
#      loader = BrainLoader()
#      brain = loader.load_all()

# OLD: from do_uw.knowledge.compat_loader import BrainKnowledgeLoader
#      loader = BrainKnowledgeLoader()
#      brain = loader.load_all()
# NEW: from do_uw.brain.brain_unified_loader import BrainLoader
#      loader = BrainLoader()
#      brain = loader.load_all()

# OLD: from do_uw.config.loader import BrainConfig
# NEW: from do_uw.brain.brain_unified_loader import BrainConfig

# OLD: from do_uw.config import ConfigLoader
# NEW: (deleted — use BrainLoader or load_config directly)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all pipeline stage and knowledge module imports to BrainLoader</name>
  <files>src/do_uw/stages/acquire/brain_requirements.py, src/do_uw/stages/extract/extraction_manifest.py, src/do_uw/stages/extract/board_governance.py, src/do_uw/stages/extract/sca_extractor.py, src/do_uw/stages/extract/tax_indicators.py, src/do_uw/stages/extract/ownership_structure.py, src/do_uw/stages/extract/xbrl_mapping.py, src/do_uw/stages/extract/profile_helpers.py, src/do_uw/stages/extract/sol_mapper.py, src/do_uw/stages/analyze/__init__.py, src/do_uw/stages/analyze/temporal_engine.py, src/do_uw/stages/analyze/executive_forensics.py, src/do_uw/stages/analyze/executive_data.py, src/do_uw/stages/analyze/adverse_events.py, src/do_uw/stages/analyze/pipeline_audit.py, src/do_uw/stages/analyze/industry_claims.py, src/do_uw/stages/analyze/forensic_composites.py, src/do_uw/stages/analyze/layers/hazard/hazard_engine.py, src/do_uw/stages/analyze/layers/classify/classification_engine.py, src/do_uw/stages/score/__init__.py, src/do_uw/stages/score/case_characteristics.py, src/do_uw/stages/score/settlement_prediction.py, src/do_uw/stages/score/peril_mapping.py, src/do_uw/stages/score/ai_risk_scoring.py, src/do_uw/stages/benchmark/__init__.py, src/do_uw/stages/benchmark/benchmark_enrichments.py, src/do_uw/stages/render/scoring_peril_data.py, src/do_uw/stages/render/sections/sect7_coverage_gaps.py, src/do_uw/knowledge/calibrate_impact.py, src/do_uw/knowledge/backtest.py, src/do_uw/knowledge/gap_detector.py</files>
  <action>
    Migrate all import sites from old loaders to the new BrainLoader. Per CONTEXT.md: "no shims, no re-exports, clean break."

    **Group A: load_brain_config() call sites (24 sites)**

    These are the simplest — change the import source. The function signature is compatible:

    ```python
    # OLD (in each file):
    from do_uw.brain.brain_config_loader import load_brain_config

    # NEW (minimal change — use the backward-compat function):
    from do_uw.brain.brain_unified_loader import load_brain_config
    ```

    Alternatively, for a cleaner API, use `load_config` directly:
    ```python
    from do_uw.brain.brain_unified_loader import load_config
    # Then replace load_brain_config("key") with load_config("key")
    ```

    Use the `load_config` approach (cleaner) — it has the same return type. The only difference is `load_config` does NOT accept `config_dir` parameter, but none of the 24 call sites pass `config_dir` (verified in research).

    Files with load_brain_config calls (from research audit):
    - stages/extract/: board_governance, sca_extractor, tax_indicators, ownership_structure, xbrl_mapping, profile_helpers, sol_mapper
    - stages/analyze/: temporal_engine, executive_forensics, executive_data, adverse_events, pipeline_audit, industry_claims, forensic_composites, hazard_engine, classification_engine
    - stages/score/: case_characteristics, settlement_prediction, peril_mapping, ai_risk_scoring, __init__
    - stages/benchmark/: benchmark_enrichments

    **Group B: BrainDBLoader import sites (10 sites)**

    ```python
    # OLD:
    from do_uw.brain.brain_loader import BrainDBLoader
    loader = BrainDBLoader()

    # NEW:
    from do_uw.brain.brain_unified_loader import BrainLoader
    loader = BrainLoader()
    ```

    Files: stages/acquire/brain_requirements.py, stages/extract/extraction_manifest.py, stages/render/scoring_peril_data.py, knowledge/calibrate_impact.py, knowledge/backtest.py
    (CLI files handled in Plan 03)

    For `scoring_peril_data.py`, which calls `load_perils()` and `load_causal_chains()`, update:
    ```python
    # OLD: loader = BrainDBLoader(); perils = loader.load_perils()
    # NEW: from do_uw.brain.brain_unified_loader import load_perils, load_causal_chains
    ```

    **Group C: BrainKnowledgeLoader import sites (5 sites)**

    ```python
    # OLD:
    from do_uw.knowledge.compat_loader import BrainKnowledgeLoader
    loader = BrainKnowledgeLoader()

    # NEW:
    from do_uw.brain.brain_unified_loader import BrainLoader
    loader = BrainLoader()
    ```

    Files: stages/analyze/__init__.py, stages/score/__init__.py, stages/benchmark/__init__.py, stages/render/sections/sect7_coverage_gaps.py
    (cli_knowledge_traceability.py handled in Plan 03)

    **Group D: BrainConfig import sites**

    Any file importing BrainConfig from config/loader.py:
    ```python
    # OLD: from do_uw.config.loader import BrainConfig
    # NEW: from do_uw.brain.brain_unified_loader import BrainConfig
    ```

    **Group E: Knowledge module cleanup**

    - `knowledge/compat_loader.py` — used by calibrate_impact, backtest, gap_detector. Those callers switch to BrainLoader directly.
    - `knowledge/gap_detector.py` — if it imports old loaders, migrate.

    After all imports migrated, verify with grep:
    ```bash
    grep -r "from do_uw.brain.brain_loader import" src/do_uw/ --include="*.py"
    grep -r "from do_uw.brain.brain_config_loader import" src/do_uw/ --include="*.py"
    grep -r "from do_uw.knowledge.compat_loader import" src/do_uw/ --include="*.py"
    grep -r "from do_uw.config" src/do_uw/ --include="*.py"
    ```

    All should return empty (zero hits in src/do_uw/).
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && echo "=== Old loader imports ===" && (grep -r "from do_uw.brain.brain_loader import" src/do_uw/ --include="*.py" || echo "NONE") && (grep -r "from do_uw.brain.brain_config_loader import" src/do_uw/ --include="*.py" || echo "NONE") && (grep -r "from do_uw.knowledge.compat_loader import" src/do_uw/ --include="*.py" || echo "NONE") && (grep -r "from do_uw.config import\|from do_uw.config.loader import" src/do_uw/ --include="*.py" || echo "NONE") && echo "=== All clean ==="</automated>
  </verify>
  <done>Zero imports of old loaders (BrainDBLoader, BrainKnowledgeLoader, load_brain_config from brain_config_loader, ConfigLoader) remain in src/do_uw/. All pipeline stages and knowledge modules use BrainLoader.</done>
</task>

<task type="auto">
  <name>Task 2: Delete old loaders, config/ directory, and update tests</name>
  <files>src/do_uw/brain/brain_loader.py, src/do_uw/brain/brain_config_loader.py, src/do_uw/brain/brain_loader_rows.py, src/do_uw/knowledge/compat_loader.py, src/do_uw/config/loader.py, src/do_uw/config/__init__.py, tests/brain/test_brain_loader.py, tests/config/test_loader.py, tests/knowledge/test_compat_loader.py, tests/knowledge/test_persistent_store.py, tests/knowledge/test_integration.py, tests/knowledge/test_playbooks.py, tests/knowledge/test_enriched_roundtrip.py, tests/test_pattern_detection.py</files>
  <action>
    **Step 1: Delete old loader modules**

    Delete (git rm) the following files that are no longer imported by any src/ code:
    - `src/do_uw/brain/brain_loader.py` (BrainDBLoader — replaced by brain_unified_loader.py)
    - `src/do_uw/brain/brain_config_loader.py` (load_brain_config — replaced)
    - `src/do_uw/brain/brain_loader_rows.py` (row_to_signal_dict + SECTION_MAP — both already copied into brain_unified_loader.py in Plan 01, safe to delete)
    - `src/do_uw/knowledge/compat_loader.py` (BrainKnowledgeLoader/BackwardCompatLoader — replaced)

    **Step 2: Delete config/ directory**

    Per CONTEXT.md: "config/ directory gets deleted entirely."
    - `git rm src/do_uw/config/loader.py`
    - `git rm src/do_uw/config/__init__.py`
    - `git rm src/do_uw/config/*.json` (all JSON files — already exist in brain/config/)
    - `rmdir src/do_uw/config/` (should be empty)

    IMPORTANT: Verify brain_unified_loader.py does NOT import from config/loader.py before deleting. The BrainConfig class must already be defined in brain_unified_loader.py (done in Plan 01).

    **Step 3: Update test files**

    For each test file that imports old loaders:

    a) `tests/brain/test_brain_loader.py` — DELETE the file. Its behaviors are now covered by `tests/brain/test_brain_unified_loader.py` from Plan 01. If there are tests for DuckDB history reads, keep those and point them at the history-only methods.

    b) `tests/config/test_loader.py` — DELETE. BrainConfig tests are in test_brain_unified_loader.py.

    c) `tests/knowledge/test_compat_loader.py` — DELETE. BrainKnowledgeLoader is gone.

    d) `tests/knowledge/test_persistent_store.py`, `test_integration.py`, `test_playbooks.py`, `test_enriched_roundtrip.py` — Update imports:
       - `from do_uw.knowledge.compat_loader import BrainKnowledgeLoader` → `from do_uw.brain.brain_unified_loader import BrainLoader`
       - `from do_uw.config.loader import BrainConfig` → `from do_uw.brain.brain_unified_loader import BrainConfig`
       - Adjust any test that instantiated BrainKnowledgeLoader to use BrainLoader instead

    e) `tests/test_pattern_detection.py` — Update ConfigLoader import to BrainLoader.

    f) Delete `tests/config/` directory if empty after test_loader.py removal.

    **Step 4: Final grep verification**

    Run comprehensive grep to ensure zero references to deleted modules remain anywhere in the project (src/ and tests/):
    ```bash
    grep -r "brain_loader\b" src/ tests/ --include="*.py" | grep -v "brain_unified_loader" | grep -v "__pycache__"
    grep -r "brain_config_loader" src/ tests/ --include="*.py" | grep -v "__pycache__"
    grep -r "compat_loader" src/ tests/ --include="*.py" | grep -v "__pycache__"
    grep -r "from do_uw.config" src/ tests/ --include="*.py" | grep -v "__pycache__"
    ```

    **Step 5: Run full test suite**

    ```bash
    uv run pytest tests/ -x --timeout=120 -q
    ```

    Ensure test count is close to 3,967+ (some tests deleted, some added in Plan 01, net should be stable). If test count drops significantly, investigate.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/ -x --timeout=120 -q 2>&1 | tail -10</automated>
  </verify>
  <done>Old loader files deleted. config/ directory deleted. All test imports updated. Full test suite passes with test count within reasonable range of baseline (3,967+). Zero references to deleted modules in codebase.</done>
</task>

</tasks>

<verification>
1. `grep -r "BrainDBLoader" src/do_uw/ tests/ --include="*.py"` — zero hits
2. `grep -r "BrainKnowledgeLoader" src/do_uw/ tests/ --include="*.py"` — zero hits
3. `grep -r "brain_config_loader" src/do_uw/ tests/ --include="*.py"` — zero hits
4. `grep -r "from do_uw.config" src/do_uw/ tests/ --include="*.py"` — zero hits
5. `ls src/do_uw/config/` — directory does not exist
6. `ls src/do_uw/brain/brain_loader.py src/do_uw/brain/brain_config_loader.py src/do_uw/knowledge/compat_loader.py 2>&1` — files do not exist
7. `uv run pytest tests/ -x --timeout=120 -q` — all tests pass
</verification>

<success_criteria>
- All ~45 import sites in pipeline stages + knowledge modules migrated to BrainLoader
- Zero imports of old loaders remain in src/ or tests/
- Old loader files deleted (brain_loader.py, brain_config_loader.py, brain_loader_rows.py, compat_loader.py)
- config/ directory deleted entirely
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/53-data-store-simplification/53-02-SUMMARY.md`
</output>
