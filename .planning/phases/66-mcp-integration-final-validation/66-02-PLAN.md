---
phase: 66-mcp-integration-final-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/stages/acquire/clients/exa_client.py
  - src/do_uw/stages/acquire/gap_searcher.py
  - tests/stages/acquire/test_exa_client.py
  - tests/test_cross_ticker_validation.py
autonomous: false
requirements: [MCP-03, QA-01]

must_haves:
  truths:
    - "Exa semantic search runs as second-pass after Brave/Serper in blind spot discovery"
    - "Exa gracefully degrades when API key missing (pipeline completes with existing search)"
    - "4 tickers (AAPL, SNA, RPM, WWD) produce complete HTML+PDF output with zero Python exceptions"
    - "All 12 sections render with real data for each ticker"
  artifacts:
    - path: "src/do_uw/stages/acquire/clients/exa_client.py"
      provides: "Semantic search via Exa API for blind spot discovery"
      min_lines: 60
    - path: "tests/stages/acquire/test_exa_client.py"
      provides: "Tests for Exa client"
      min_lines: 30
    - path: "tests/test_cross_ticker_validation.py"
      provides: "Cross-ticker rendering validation test"
      min_lines: 40
  key_links:
    - from: "src/do_uw/stages/acquire/gap_searcher.py"
      to: "exa_client.py"
      via: "import + call as second-pass search"
      pattern: "exa"
---

<objective>
Add Exa semantic search as a second-pass blind spot discovery tool and validate the entire v3.0 pipeline across 4 tickers.

Purpose: Exa's semantic search is materially better than keyword search for hidden risks (e.g., "accounting irregularities at {company}"). Cross-ticker validation ensures zero regressions across diverse company types before v3.0 ships.

Output: Exa client module, gap_searcher augmentation, cross-ticker validation test, and human-verified output quality across 4 tickers.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-mcp-integration-final-validation/66-CONTEXT.md

@src/do_uw/stages/acquire/gap_searcher.py (integration point for Exa)
@src/do_uw/stages/acquire/clients/web_search.py (existing search pattern)
@src/do_uw/stages/acquire/clients/serper_client.py (existing search client pattern)

<interfaces>
<!-- Key types and contracts from gap_searcher -->

From src/do_uw/stages/acquire/gap_searcher.py:
```python
GAP_SEARCH_MAX = 15
def _load_gap_eligible_checks() -> list[dict[str, Any]]: ...
# Gap searcher runs after structured acquisition
# Uses WebSearchClient for keyword searches
# Results stored in acquired.brain_targeted_search
```

From src/do_uw/stages/acquire/clients/web_search.py:
```python
class WebSearchClient:
    def __init__(self, search_fn, search_budget): ...
    # Pluggable search_fn pattern
    # Budget tracking built in
```

From src/do_uw/stages/acquire/clients/serper_client.py:
```python
def create_serper_search_fn() -> tuple[Callable | None, str]:
    # Returns (search_fn, status_message)
    # Reads API key from environment
    # Returns (None, error_message) if key missing
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Exa semantic search client + augment gap_searcher</name>
  <files>
    src/do_uw/stages/acquire/clients/exa_client.py,
    src/do_uw/stages/acquire/gap_searcher.py,
    tests/stages/acquire/test_exa_client.py
  </files>
  <behavior>
    - Exa client:
      - search_semantic(query, num_results=5) returns list of dicts with title, url, snippet, score, source="Exa", confidence=LOW
      - Uses httpx to call Exa API (https://api.exa.ai/search)
      - Reads EXA_API_KEY from environment; returns empty list if missing (graceful degradation, logged as warning)
      - D&O-relevant query templates for semantic search:
        - "{company} accounting irregularities fraud investigation"
        - "{company} executive misconduct insider trading"
        - "{company} regulatory enforcement action penalty"
        - "{company} shareholder derivative lawsuit settlement"
      - Free tier usage
    - Gap searcher augmentation:
      - After existing keyword-based gap search completes, run Exa semantic search as second pass
      - Only runs if EXA_API_KEY is set
      - Results merged into brain_targeted_search with source="Exa"
      - Capped at 5 Exa queries max (does not consume web search budget)
    - Tests:
      - Mock httpx response for Exa API, verify structured output
      - Verify graceful degradation when API key missing
      - Verify source and confidence fields
  </behavior>
  <action>
    Create `exa_client.py` following the serper_client pattern:
    - Class `ExaClient` with method `search_semantic(query: str, num_results: int = 5) -> list[dict[str, Any]]`
    - Read EXA_API_KEY from os.environ
    - If key missing: log warning, return empty list
    - POST to https://api.exa.ai/search with JSON body: {"query": query, "numResults": num_results, "type": "neural"}
    - Parse response: extract title, url, published_date, snippet from each result
    - Add source="Exa", confidence=LOW to each result
    - Wrap in try/except, return empty list on failure

    Also create a module-level function `create_exa_search_fn()` that returns `tuple[Callable | None, str]` matching the serper_client pattern. This allows the orchestrator to check availability.

    Augment `gap_searcher.py`:
    - Add function `run_exa_semantic_search(company_name: str, ticker: str) -> dict[str, list[dict]]`
    - Uses ExaClient with 4 D&O-relevant query templates
    - Returns results keyed by query category
    - Maximum 5 queries (hard cap, independent of web search budget)
    - Call this at the end of the existing gap search flow
    - Store results in a new sub-key: acquired.brain_targeted_search["exa_semantic"]

    Create test file with mocked httpx responses.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run python -m pytest tests/stages/acquire/test_exa_client.py -x -v</automated>
  </verify>
  <done>Exa client exists, gap_searcher augmented with semantic search pass, tests pass, graceful degradation verified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Cross-ticker validation — run 4 tickers and verify output quality</name>
  <files>tests/test_cross_ticker_validation.py</files>
  <action>
    Before the checkpoint, Claude will:
    1. Create `tests/test_cross_ticker_validation.py` that validates:
       - Each ticker's HTML output file exists and is non-empty
       - Each ticker's PDF output file exists and is non-empty
       - No Python tracebacks in pipeline logs
       - All 12 sections present in HTML output (grep for section headings)
    2. Run AAPL analysis (the primary test ticker) to generate fresh output
    3. Open the HTML output for user review
  </action>
  <what-built>
    Full v3.0 pipeline validation across 4 diverse tickers (AAPL, SNA, RPM, WWD). Automated test catches Python exceptions; human review confirms institutional-quality output.
  </what-built>
  <how-to-verify>
    1. Review the AAPL HTML output opened in browser:
       - All 12 sections have data (not "Not Available" everywhere)
       - Verdict badges, narratives, bull/bear framing present
       - Charts render (stock price, risk radar, sparklines)
       - Collapsible sections work
       - No visual regressions from v2.0
    2. Run additional tickers as desired (SNA, RPM, WWD) to spot-check
    3. Confirm outputs meet institutional-quality bar
  </how-to-verify>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run python -m pytest tests/test_cross_ticker_validation.py -x -v</automated>
  </verify>
  <done>AAPL output reviewed and approved as institutional-quality; cross-ticker validation test exists and catches rendering errors</done>
  <resume-signal>Type "approved" after reviewing output quality, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Exa client module exists: `src/do_uw/stages/acquire/clients/exa_client.py`
2. Exa tests pass: `uv run python -m pytest tests/stages/acquire/test_exa_client.py -x -v`
3. Gap searcher has Exa integration: grep "exa" in gap_searcher.py
4. Cross-ticker test exists: `tests/test_cross_ticker_validation.py`
5. At least AAPL output validated by human review
</verification>

<success_criteria>
- Exa semantic search client created with graceful degradation
- Gap searcher augmented with Exa as second-pass semantic search
- Cross-ticker validation test exists and catches rendering errors
- At least AAPL output reviewed and confirmed institutional-quality
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/66-mcp-integration-final-validation/66-02-SUMMARY.md`
</output>
