---
phase: 66-mcp-integration-final-validation
plan: 03
type: execute
wave: 2
depends_on: [66-01, 66-02]
files_modified:
  - tests/test_visual_regression.py
  - tests/golden/.gitkeep
  - tests/test_performance_budget.py
  - tests/brain/test_brain_framework.py
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - CLAUDE.md
  - README.md
autonomous: false
requirements: [QA-02, QA-03, QA-04, QA-05]

must_haves:
  truths:
    - "Visual regression framework captures per-section screenshots and compares against golden baselines"
    - "Performance budget enforced via pytest assertions (pipeline <25min, HTML <10s, PDF <30s)"
    - "test_brain_framework.py collection error is fixed"
    - "All tests pass cleanly (4,200+ threshold exceeded)"
    - "CLAUDE.md, PROJECT.md, ROADMAP.md, README.md updated for v3.0 completion"
  artifacts:
    - path: "tests/test_visual_regression.py"
      provides: "Per-section screenshot comparison framework"
      min_lines: 60
    - path: "tests/test_performance_budget.py"
      provides: "Performance budget pytest assertions"
      min_lines: 30
    - path: "tests/golden/.gitkeep"
      provides: "Directory for golden baseline screenshots"
    - path: "README.md"
      provides: "Updated README with setup, usage, output screenshots"
      min_lines: 50
  key_links:
    - from: "tests/test_visual_regression.py"
      to: "tests/golden/"
      via: "golden screenshot storage and comparison"
      pattern: "golden"
    - from: "tests/test_performance_budget.py"
      to: "src/do_uw/cli.py"
      via: "timing assertions against pipeline stage durations"
      pattern: "performance|budget|timing"
---

<objective>
Build visual regression framework, enforce performance budgets, fix test health, and update all documentation for v3.0 completion.

Purpose: Ship v3.0 with automated quality gates (visual regression catches layout regressions, performance budget catches slowdowns) and complete documentation. Fix the test_brain_framework.py collection error to ensure clean test runs.

Output: Visual regression test framework with golden screenshots, performance budget pytest assertions, fixed test collection, and updated docs marking v3.0 as shipped.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-mcp-integration-final-validation/66-CONTEXT.md
@.planning/phases/66-mcp-integration-final-validation/66-01-SUMMARY.md
@.planning/phases/66-mcp-integration-final-validation/66-02-SUMMARY.md

@src/do_uw/cli.py (timing display, pipeline callbacks)
@src/do_uw/stages/render/pdf_renderer.py (Playwright PDF rendering)
@tests/brain/test_brain_framework.py (collection error to fix)
@CLAUDE.md (documentation to update)
@README.md (documentation to update/create)

<interfaces>
<!-- Key types from existing code -->

From src/do_uw/cli.py:
```python
class RichCallbacks:
    _durations: dict[str, float | None]
    # Tracks per-stage timing via on_stage_complete(stage_name, index, total, duration)
```

From src/do_uw/stages/render/pdf_renderer.py:
```python
def render_pdf(state: AnalysisState, output_dir: Path, ...) -> Path:
    # Uses Playwright for PDF rendering
    # Already a project dependency
```

From tests/brain/test_brain_framework.py:
```python
# Collection error — likely an import issue with:
from do_uw.brain.legacy.brain_migrate_framework import (
    build_framework, migrate_causal_chains, migrate_perils,
    migrate_risk_framework, tag_checks_with_perils_and_chains,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Visual regression framework + performance budget tests + test health fix</name>
  <files>
    tests/test_visual_regression.py,
    tests/golden/.gitkeep,
    tests/test_performance_budget.py,
    tests/brain/test_brain_framework.py
  </files>
  <behavior>
    - Visual regression:
      - Test module that uses Playwright to capture per-section screenshots of HTML output
      - Golden screenshots stored in tests/golden/ directory (committed to git)
      - Comparison uses PIL/Pillow for structural diff (5-10% threshold for data changes)
      - pytest markers: @pytest.mark.visual_regression (skip by default, run with --visual-regression flag)
      - 12 sections per ticker = 12 screenshots per baseline
      - First run with --update-golden creates baseline; subsequent runs compare
    - Performance budget:
      - pytest assertions that check render timing
      - HTML render < 10s assertion
      - PDF render < 30s assertion
      - Pipeline total < 25min assertion (when cached data available)
      - pytest markers: @pytest.mark.performance (skip by default, run with --performance flag)
    - Test health:
      - Fix test_brain_framework.py collection error (likely stale import from do_uw.brain.legacy.brain_migrate_framework)
      - Verify all 4,921+ tests collect without errors
    - Tests verify:
      - Visual regression framework initializes without error
      - Performance budget test structure validates
      - test_brain_framework.py collects successfully
  </behavior>
  <action>
    **Fix test_brain_framework.py first:**
    - Read the full file and identify the collection error
    - Likely a stale import from `do_uw.brain.legacy.brain_migrate_framework` — module may have been moved/renamed
    - Fix the import or skip the test with a clear reason if the underlying module was intentionally removed
    - Run `uv run python -m pytest --collect-only tests/brain/test_brain_framework.py` to verify fix

    **Create tests/golden/.gitkeep:**
    - Empty file to ensure the golden screenshots directory is tracked in git
    - Actual golden screenshots will be generated on first run

    **Create tests/test_visual_regression.py:**
    - Use pytest parametrize with section IDs (12 sections)
    - Use Playwright (already a dependency) to:
      1. Open HTML output file
      2. Scroll to each section (by CSS selector or ID)
      3. Capture section screenshot
    - Compare against golden baseline using Pillow:
      1. Load golden screenshot from tests/golden/{ticker}_{section}.png
      2. If golden doesn't exist: save current as golden, skip comparison
      3. If golden exists: pixel diff, compute structural similarity percentage
      4. Fail if diff > 10% (tunable threshold)
    - Add pytest markers:
      ```python
      @pytest.mark.visual_regression
      @pytest.mark.skipif(not os.environ.get("VISUAL_REGRESSION"), reason="Set VISUAL_REGRESSION=1 to run")
      ```
    - Add `--update-golden` fixture that saves new screenshots as golden baselines
    - Use conftest.py or module-level fixtures for Playwright browser lifecycle

    **Create tests/test_performance_budget.py:**
    - Performance budget assertions using pytest:
      ```python
      @pytest.mark.performance
      @pytest.mark.skipif(not os.environ.get("PERFORMANCE_TESTS"), reason="Set PERFORMANCE_TESTS=1 to run")
      def test_html_render_under_10s():
          # Time the render_html call on cached AAPL state
          # Assert duration < 10.0

      def test_pdf_render_under_30s():
          # Time the render_pdf call on cached AAPL state
          # Assert duration < 30.0
      ```
    - These use real cached state data (from output/AAPL/) to measure render performance
    - Also add CLI timing output: update cli.py to print total pipeline duration after completion

    **Verify test count:**
    - Run `uv run python -m pytest --collect-only -q` and confirm 4,200+ tests collect cleanly (already at 4,921)
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run python -m pytest --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>test_brain_framework.py collects without error, visual regression + performance budget test frameworks exist, all 4,921+ tests collect cleanly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Update documentation + mark v3.0 shipped</name>
  <files>CLAUDE.md, README.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    Update all documentation to reflect v3.0 completion:
    1. CLAUDE.md: Update Data Source Fallback Chains to include CourtListener + FMP + Exa. Add visual regression and performance testing notes.
    2. PROJECT.md: Mark v3.0 as shipped. Update feature summary.
    3. ROADMAP.md: Mark Phase 66 complete. Mark v3.0 milestone as shipped with completion date. Update progress table.
    4. STATE.md: Mark v3.0 as complete. Update progress metrics.
    5. README.md: Add/update setup instructions (uv, env vars), usage examples, output formats, key features, MCP integrations.
  </action>
  <what-built>
    Documentation updates marking v3.0 as complete across all project files.
  </what-built>
  <how-to-verify>
    1. Review the updated README.md — does it accurately describe the system?
    2. Review ROADMAP.md — is v3.0 correctly marked as shipped?
    3. Review CLAUDE.md — are the new data sources documented?
    4. Confirm all docs are internally consistent
  </how-to-verify>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && grep -c "shipped\|complete\|v3.0" .planning/ROADMAP.md</automated>
  </verify>
  <done>All documentation updated, v3.0 marked as shipped, README has setup/usage/feature docs</done>
  <resume-signal>Type "approved" after reviewing documentation updates, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. test_brain_framework.py collects: `uv run python -m pytest --collect-only tests/brain/test_brain_framework.py`
2. All tests collect cleanly: `uv run python -m pytest --collect-only -q 2>&1 | tail -5` shows 4,200+ with 0 errors
3. Visual regression framework exists: `tests/test_visual_regression.py`
4. Performance budget tests exist: `tests/test_performance_budget.py`
5. Golden screenshot dir exists: `tests/golden/.gitkeep`
6. ROADMAP.md shows v3.0 as shipped
7. README.md has setup + usage + output docs
</verification>

<success_criteria>
- Visual regression framework captures per-section screenshots with golden baseline comparison
- Performance budget enforced via pytest (HTML <10s, PDF <30s, pipeline <25min)
- test_brain_framework.py collection error fixed
- 4,921+ tests collect cleanly with 0 errors
- CLAUDE.md, PROJECT.md, ROADMAP.md, STATE.md updated for v3.0 completion
- README.md has setup instructions, usage examples, and feature documentation
- v3.0 milestone marked as shipped
</success_criteria>

<output>
After completion, create `.planning/phases/66-mcp-integration-final-validation/66-03-SUMMARY.md`
</output>
