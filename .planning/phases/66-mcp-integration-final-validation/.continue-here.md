---
phase: 66-mcp-integration-final-validation
task: 2 (checkpoint)
total_tasks: 2
status: in_progress
last_updated: 2026-03-06T02:01:00.452Z
---

<current_state>
Phase 66 execution nearly complete. Plans 66-01 and 66-02 are DONE and committed. Plan 66-03 has both tasks coded and committed (af35e2c, d7f1253) but the human-verify checkpoint for docs was interrupted by THREE BUGS found during QA review that MUST be fixed before v3.0 can ship.

The 66-03 executor agent (ID: a2e88dd3af28cfeed) is waiting for checkpoint approval but should NOT be resumed — the bugs below need fixing first.
</current_state>

<completed_work>

- Plan 66-01: CourtListener + FMP acquire clients - DONE (prior session)
- Plan 66-02: Exa semantic search + cross-ticker validation - DONE
  - Exa client: src/do_uw/stages/acquire/clients/exa_client.py (155 lines)
  - Gap searcher augmented with Exa second-pass
  - Cross-ticker validation: 69 tests pass
  - scripts/qa_compare.py created (cross-ticker feature parity comparison)
  - Re-rendered SNA, RPM, WWD with v3.0 template (visual parity restored)
- Plan 66-03 Task 1: Visual regression + performance budget + test health - DONE (af35e2c)
  - tests/test_visual_regression.py (160 lines, Playwright screenshots)
  - tests/test_performance_budget.py (110 lines)
  - tests/golden/.gitkeep
  - 5,073 tests collect cleanly
- Plan 66-03 Task 2: Documentation updates - DONE (d7f1253) BUT needs revision after bugs fixed
  - README.md, CLAUDE.md, PROJECT.md, ROADMAP.md all updated for v3.0
</completed_work>

<remaining_work>

## BUG 1: PDF Header Overlap (VISUAL)
The PDF has overlapping text in the header area. The company title "SHERWIN WILLIAMS CO (SHW)" overlaps with a running header "CONFIDENTIAL - SHERWIN WILLIAMS CO D&O Worksheet" and the date. The CSS running header and the page content title are both rendering in the same space.

**Where to look:**
- src/do_uw/stages/render/html_renderer.py lines 480-501 (Playwright PDF margins/headers)
- Find the HTML template (not in templates/ dir — likely inline in html_renderer.py or a .html file)
- Search for `pdf_mode` conditional CSS that adds running headers
- The `margin.top: "1in"` should clear the running header but clearly doesn't

**Fix:** Either increase top margin, or hide the CSS running header content and let Playwright's headerTemplate handle it, or add `display:none` to the main page title when pdf_mode=True.

## BUG 2: False SCA Classification for SHW (DATA INTEGRITY)
"State of Texas v. The Sherwin-Williams Manufacturing Company (Garland facility)" is an ENVIRONMENTAL/REGULATORY case being misclassified as an active Securities Class Action. This triggers:
- Prior Litigation: 20/20 points (should be much lower)
- Critical Red Flag: Active SCA (FALSE — this is not an SCA)
- Score ceiling of 30 (completely wrong for an environmental case)

This is the SAME pattern as the Visa litigation bug fixed in commit c68af44. The `_is_regulatory_coverage` / `_infer_coverage_type` logic is still not catching state environmental enforcement as non-SCA.

**Where to look:**
- src/do_uw/stages/analyze/check_mappers.py (SCA classification)
- src/do_uw/stages/extract/ — wherever litigation cases get coverage_type assigned
- The fix in c68af44 added heuristics for "named parties, class periods, v., Class Action" etc. but state environmental suits slip through
- Need to also check for environmental/regulatory keywords: "facility", "environmental", "EPA", "state of", "pollution", "contamination"

**This is a NON-NEGOTIABLE data integrity fix per CLAUDE.md rules.**

## BUG 3: No Company Logo (FEATURE)
User wants company logo embedded (small) in both HTML and PDF output. Currently no logo anywhere.

**Approach:**
- Use a logo API like `logo.clearbit.com/sherwin-williams.com` or `img.logo.dev/{domain}` to fetch logos
- Fetch during ACQUIRE stage, cache in state
- Display small (32-48px) next to company name in header
- Needs to work in both HTML (inline) and PDF (embedded)
- Graceful degradation if logo unavailable

## After bugs fixed:
- Resume 66-03 checkpoint (approve docs after updating for bug fixes)
- Run phase verification step
- Update ROADMAP with phase completion
</remaining_work>

<decisions_made>

- Re-rendered stale tickers (SNA, RPM, WWD) from existing state.json — visual parity restored but 8Q trends need full pipeline re-runs (data gap, not render bug)
- Created scripts/qa_compare.py as permanent QA tool — catches feature parity issues the test suite missed
- SHW pipeline ran successfully with full v3.0 features (score 68.8, WATCH tier — but score is WRONG due to false SCA bug)
- Sparkline count difference (16 vs 3) is entirely driven by 8Q quarterly data presence — not a render issue
</decisions_made>

<blockers>
- BUG 2 (false SCA) is a scoring integrity blocker — SHW score of 68.8 is artificially depressed by a false 20-point deduction and score ceiling of 30
- PDF header overlap makes PDF output unpresentable
- Cannot approve 66-03 docs until bugs are fixed
</blockers>

<context>
User is frustrated. They ran SHW, saw the PDF looks terrible (overlapping headers), noticed a false "Active SCA" claim that's actually an environmental case, and wants company logos. These are real quality issues that matter for an institutional-quality product.

The false SCA bug is the highest priority — it's a data integrity violation. The PDF header is visual but embarrassing. The logo is a feature request but reasonable.

The execute-phase workflow is paused at the 66-03 checkpoint. After fixing these bugs, resume the checkpoint, finish phase 66, then run verification.
</context>

<next_action>
1. Fix BUG 2 first (false SCA classification) — look at how "State of Texas v. Sherwin-Williams" gets classified, add environmental/regulatory keyword filters
2. Fix BUG 1 (PDF header overlap) — find the CSS running header, fix spacing/overlap
3. Add BUG 3 (company logo) — fetch via logo API in acquire, embed in HTML/PDF header
4. Re-run SHW to verify fixes
5. Resume 66-03 checkpoint approval
6. Complete phase 66 verification
</next_action>
