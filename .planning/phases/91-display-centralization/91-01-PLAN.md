---
phase: 91-display-centralization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/brain/signals/stock/price.yaml
  - src/do_uw/brain/signals/stock/short.yaml
  - src/do_uw/brain/signals/stock/pattern.yaml
  - src/do_uw/stages/render/context_builders/chart_thresholds.py
  - src/do_uw/stages/render/html_renderer.py
  - src/do_uw/templates/html/sections/market/stock_charts.html.j2
  - src/do_uw/templates/html/sections/market/stock_performance.html.j2
  - src/do_uw/stages/render/charts/stock_chart_overlays.py
  - tests/test_chart_thresholds.py
autonomous: true
requirements:
  - DISP-01
  - DISP-02

must_haves:
  truths:
    - "Changing a chart evaluation threshold (e.g., beta elevated from 1.5 to 1.8) requires editing only the signal YAML file — no Jinja2 template changes needed"
    - "Templates reference threshold variables (e.g., thresholds.beta_ratio.red) instead of numeric literals in conditional expressions"
    - "Context builder reads signal YAML threshold blocks and injects resolved threshold values into template context"
  artifacts:
    - path: "src/do_uw/stages/render/context_builders/chart_thresholds.py"
      provides: "Signal-to-threshold extraction and injection into template context"
      exports: ["extract_chart_thresholds"]
    - path: "tests/test_chart_thresholds.py"
      provides: "Unit tests for threshold extraction from signal YAML"
      min_lines: 40
  key_links:
    - from: "src/do_uw/stages/render/context_builders/chart_thresholds.py"
      to: "brain/signals/stock/*.yaml"
      via: "BrainLoader signal data with threshold blocks"
      pattern: "threshold.*red|threshold.*yellow"
    - from: "src/do_uw/stages/render/html_renderer.py"
      to: "src/do_uw/stages/render/context_builders/chart_thresholds.py"
      via: "extract_chart_thresholds called in build_html_context"
      pattern: "extract_chart_thresholds"
    - from: "src/do_uw/templates/html/sections/market/stock_charts.html.j2"
      to: "context['thresholds']"
      via: "Jinja2 variable references"
      pattern: "thresholds\\."
---

<objective>
Move all chart evaluation thresholds from hardcoded template/overlay literals into signal YAML threshold blocks, create a context builder that reads signal thresholds and injects them into the template context, and update templates to use threshold variables instead of numeric literals.

Purpose: DISP-01 and DISP-02 — threshold ownership moves to signal YAML so changing a threshold requires only a YAML edit, not template surgery. The context builder becomes the translation layer between signal data and templates.

Output: chart_thresholds.py context builder, updated signal YAMLs with numeric evaluation thresholds, templates referencing threshold variables, overlay code reading thresholds from state.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-display-centralization/91-CONTEXT.md

@src/do_uw/brain/signals/stock/price.yaml
@src/do_uw/brain/signals/stock/short.yaml
@src/do_uw/brain/signals/stock/pattern.yaml
@src/do_uw/stages/render/context_builders/market.py
@src/do_uw/stages/render/html_renderer.py
@src/do_uw/templates/html/sections/market/stock_charts.html.j2
@src/do_uw/templates/html/sections/market/stock_performance.html.j2
@src/do_uw/stages/render/charts/stock_chart_overlays.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From brain/signals/stock/price.yaml (existing threshold schema):
```yaml
threshold:
  type: tiered
  red: Beta ratio >1.5x sector
  yellow: Beta ratio >1.2x sector
  clear: Beta ratio <=1.2x sector
```

From evaluation block (existing numeric thresholds on some signals):
```yaml
evaluation:
  thresholds:
  - op: ">"
    value: 10.0
    label: RED
  - op: ">"
    value: 5.0
    label: YELLOW
```

From html_renderer.py:
```python
def build_html_context(state: AnalysisState, chart_dir: Path | None = None) -> dict[str, Any]:
    # ... builds context dict consumed by templates
    context["chart_metrics"] = _extract_chart_metrics(state)
```

From stock_chart_overlays.py:
```python
_DIVERGENCE_THRESHOLD = 10.0
def compute_beta_ratio(data: ChartData) -> tuple[float | None, str]:
    if ratio > 1.5: return round(ratio, 2), "price_down"
    if ratio > 1.2: return round(ratio, 2), "drop_yellow"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add numeric evaluation thresholds to signal YAMLs + create chart_thresholds.py context builder</name>
  <files>
    src/do_uw/brain/signals/stock/price.yaml
    src/do_uw/brain/signals/stock/short.yaml
    src/do_uw/brain/signals/stock/pattern.yaml
    src/do_uw/stages/render/context_builders/chart_thresholds.py
    tests/test_chart_thresholds.py
  </files>
  <behavior>
    - Test 1: extract_chart_thresholds returns dict with keys for every chart-evaluated signal (beta_ratio, volatility_90d, max_drawdown_1y, decline_from_high_pct, mdd_ratio, idiosyncratic_vol, alpha_1y, company_specific_drops, vol_ratio)
    - Test 2: Each threshold entry has numeric .red and .yellow values (floats, not strings)
    - Test 3: Thresholds match the values currently hardcoded in templates (beta_ratio.red=1.3, decline_from_high.red=30, decline_from_high.yellow=15, etc.)
    - Test 4: If a signal's evaluation block has numeric thresholds, those are used; falls back to parsing threshold.red/yellow strings
    - Test 5: extract_chart_thresholds returns stable defaults even when BrainLoader is unavailable (fallback dict)
  </behavior>
  <action>
    **Step 1: Audit and update signal YAML evaluation blocks.**

    Add or update `evaluation.thresholds` on these signals with NUMERIC values (the existing `threshold:` block has human-readable strings — we need machine-parseable numbers in `evaluation.thresholds`):

    Signals to update (all in stock/ directory):
    - `STOCK.PRICE.beta_ratio_elevated` in price.yaml — already has evaluation block, verify thresholds: red >1.5, yellow >1.2 (used for beta ratio in overlays). ADD a NEW threshold set for chart callout: red >1.3 (matching current template `beta_ratio_val > 1.3`)
    - `STOCK.PRICE.technical` in price.yaml — add evaluation.thresholds: red vol >40, yellow vol >35 (matching volatility chart zones)
    - `STOCK.PRICE.idiosyncratic_vol` in price.yaml — add evaluation.thresholds: red >30, yellow >20
    - `STOCK.PRICE.drawdown_duration` in price.yaml — add evaluation.thresholds: red < -20, yellow < -15
    - `STOCK.PRICE.recent_drop_alert` in price.yaml — already has evaluation thresholds for decline_from_high
    - `STOCK.PRICE.position` in price.yaml — add evaluation.thresholds: red >=30 (off-high%), yellow >=15
    - `STOCK.PRICE.attribution` in price.yaml — add evaluation.thresholds: red <-15 (company-specific underperf), yellow >10 (positive alpha)
    - `STOCK.PRICE.chart_comparison` in price.yaml — add evaluation.thresholds for divergence: red >20, yellow >10
    - `STOCK.SHORT.short_squeeze_risk` or relevant short signal in short.yaml — if volume spike threshold (2.0x) lives here, add it

    For MDD ratio, add a `display.chart_thresholds` block on `STOCK.PRICE.drawdown_duration` (or create a dedicated mdd_ratio signal reference):
    ```yaml
    display:
      chart_thresholds:
        mdd_ratio:
          red: 1.5
          yellow: 1.0
    ```

    Also add chart_thresholds for:
    - vol_ratio: red: 1.5, yellow: 1.3 (from template `vol_90d > sector_vol * 1.3`)
    - drop_severity: red: -10.0, yellow: -5.0 (from overlay `pct <= -10.0`)
    - divergence: red: 20.0, yellow: 10.0 (from `_DIVERGENCE_THRESHOLD = 10.0`)
    - volume_spike: red: 2.0 (from overlay `v > 2.0 * ma[i]`)
    - decline_from_high: red: 30, yellow: 15 (from stock_performance template)
    - beta_performance: red: 1.5 (from stock_performance template)

    **Step 2: Create `chart_thresholds.py` context builder.**

    Create `src/do_uw/stages/render/context_builders/chart_thresholds.py` with:
    - `extract_chart_thresholds(state: AnalysisState) -> dict[str, dict[str, float]]`
    - Loads stock signals via BrainLoader (import from `do_uw.brain.brain_unified_loader`)
    - Iterates signals with `evaluation.thresholds` and `display.chart_thresholds`
    - Builds a flat dict keyed by metric name (e.g., `beta_ratio`, `mdd_ratio`, `decline_from_high_pct`)
    - Each value is `{"red": float, "yellow": float, "clear": float_or_none}`
    - Include a `_FALLBACK_THRESHOLDS` dict with the current hardcoded values as defaults, so rendering never breaks even if YAML loading fails
    - Return type: `dict[str, ThresholdSpec]` where `ThresholdSpec = TypedDict("ThresholdSpec", {"red": float, "yellow": float})`

    Map signal IDs to template metric names:
    - `STOCK.PRICE.beta_ratio_elevated` -> `beta_ratio` (red=1.3 for callout, 1.5 for overlay)
    - `STOCK.PRICE.technical` -> `volatility` (red=40, yellow=35 as percentage)
    - `STOCK.PRICE.idiosyncratic_vol` -> `idiosyncratic_vol` (red=30, yellow=20)
    - `STOCK.PRICE.drawdown_duration` -> `max_drawdown` (red=-20, yellow=-15)
    - `STOCK.PRICE.position` -> `decline_from_high` (red=30, yellow=15)
    - `STOCK.PRICE.attribution` -> `alpha` (positive threshold: 10)
    - `STOCK.PRICE.chart_comparison` -> `divergence` (red=20, yellow=10)
    - chart_thresholds on signals -> `mdd_ratio`, `vol_ratio`, `drop_severity`, `volume_spike`

    **Step 3: Write tests.**

    In `tests/test_chart_thresholds.py`:
    - Test that extract_chart_thresholds returns the expected keys
    - Test numeric values match signal YAML values
    - Test fallback works when brain loader is unavailable (mock it)
    - Test that each threshold has both .red and .yellow keys

    Export `extract_chart_thresholds` from `context_builders/__init__.py`.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run python -m pytest tests/test_chart_thresholds.py -x -v</automated>
  </verify>
  <done>Signal YAMLs have numeric evaluation thresholds for all chart-relevant metrics. chart_thresholds.py extracts them into a flat dict. Tests prove threshold values match expected numbers. Fallback ensures rendering never breaks.</done>
</task>

<task type="auto">
  <name>Task 2: Wire threshold injection into html_renderer and update templates to use threshold variables</name>
  <files>
    src/do_uw/stages/render/html_renderer.py
    src/do_uw/templates/html/sections/market/stock_charts.html.j2
    src/do_uw/templates/html/sections/market/stock_performance.html.j2
    src/do_uw/stages/render/charts/stock_chart_overlays.py
  </files>
  <action>
    **Step 1: Wire into build_html_context.**

    In `html_renderer.py`, in `build_html_context()`, after `context["chart_metrics"] = _extract_chart_metrics(state)`:
    ```python
    from do_uw.stages.render.context_builders.chart_thresholds import extract_chart_thresholds
    context["thresholds"] = extract_chart_thresholds(state)
    ```

    **Step 2: Replace hardcoded thresholds in stock_charts.html.j2.**

    Current hardcoded values to replace (use `thresholds` dict injected in context):

    MDD ratio cards (lines 128-145):
    - `mr1 > 1.5` -> `mr1 > thresholds.mdd_ratio.red`
    - `mr1 > 1.0` -> `mr1 > thresholds.mdd_ratio.yellow`
    - Same for mr5

    Risk flag evaluation (lines 193-225):
    - `beta_ratio_val > 1.3` -> `beta_ratio_val > thresholds.beta_ratio.yellow`
    - `vol_90d > sector_vol * 1.3` -> `vol_90d > sector_vol * thresholds.vol_ratio.yellow`
    - `max_dd < -20` -> `max_dd < thresholds.max_drawdown.red`
    - `decline < -25` -> `decline < (thresholds.decline_from_high.red * -1 + 5)` OR add a separate `decline_current` threshold with red=-25. Actually, better: add a `decline_current_red: 25` threshold. Use: `decline < -thresholds.decline_current.red`
    - `company_specific_drops >= 2` -> `company_specific_drops >= thresholds.company_specific_drops.yellow` (add this threshold, yellow=2)
    - `idio_vol > 30` -> `idio_vol > thresholds.idiosyncratic_vol.red`

    Positive signal evaluation:
    - `alpha_1y > 10` -> `alpha_1y > thresholds.alpha.yellow` (positive threshold)
    - `decline > -10` -> `decline > -thresholds.decline_near_high.yellow` (add threshold, yellow=10)
    - `vol_90d <= sector_vol` -> keep as-is (comparison, not threshold)
    - `total_drops == 0` -> keep as-is (zero check, not threshold)

    **Step 3: Replace hardcoded thresholds in stock_performance.html.j2.**

    - `off_high_str|float(default=0) >= 30` -> `off_high_str|float(default=0) >= thresholds.decline_from_high.red`
    - `off_high_str|float(default=0) >= 15` -> `off_high_str|float(default=0) >= thresholds.decline_from_high.yellow`
    - `mkt.get('beta')|float(default=1) > 1.5` -> `mkt.get('beta')|float(default=1) > thresholds.beta_performance.red`

    **Step 4: Update overlays to read thresholds from module-level or parameterized values.**

    In `stock_chart_overlays.py`:
    - Change `_DIVERGENCE_THRESHOLD = 10.0` to read from a parameter or keep as module constant BUT sourced from a threshold config. For simplicity, keep the module constant but document it maps to `STOCK.PRICE.chart_comparison` threshold. The chart overlays operate on ChartData (not template context), so they cannot directly read template thresholds. Leave a comment: `# Threshold sourced from STOCK.PRICE.chart_comparison evaluation.thresholds yellow=10.0`
    - Same approach for `compute_beta_ratio` thresholds (1.5, 1.2): document they map to `STOCK.PRICE.beta_ratio_elevated`
    - Same for volume spike `2.0` and drop severity `-10.0`
    - These overlay thresholds are chart-rendering constants that match signal YAML — a future refactor could parameterize them, but for now keeping them in sync via documentation + the CI lint (Plan 91-03) is sufficient

    **Step 5: Verify no numeric literals remain in conditional expressions in the two templates.**

    After replacements, manually verify that `stock_charts.html.j2` and `stock_performance.html.j2` have NO patterns like `> 1.3`, `< -20`, `>= 30` in `{% if %}` blocks. The only remaining numbers should be in format strings (e.g., `'{:.1f}'`) and non-threshold comparisons (e.g., `> 0`, `== 0`, `> 1`).
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run python -c "
from do_uw.stages.render.html_renderer import build_html_context
# Verify thresholds key exists in context (will fail fast if wiring broken)
import json
# Quick smoke: just verify the import and function signature work
from do_uw.stages.render.context_builders.chart_thresholds import extract_chart_thresholds
t = extract_chart_thresholds(None)
assert 'beta_ratio' in t, f'Missing beta_ratio in thresholds: {list(t.keys())}'
assert 'mdd_ratio' in t, f'Missing mdd_ratio in thresholds: {list(t.keys())}'
print('Threshold injection smoke test passed')
print(f'Available thresholds: {list(t.keys())}')
" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Templates reference `thresholds.X.red` / `thresholds.X.yellow` instead of hardcoded numbers. Changing a threshold in signal YAML propagates to templates without template edits. Overlay thresholds documented as signal-sourced.</done>
</task>

</tasks>

<verification>
1. Run full test suite: `uv run python -m pytest tests/ -x --timeout=60`
2. Grep templates for remaining hardcoded thresholds in conditionals: `grep -nE '(> [0-9]+\.[0-9]|< -?[0-9]+\.[0-9]|>= [0-9]+|< -[0-9]+)' src/do_uw/templates/html/sections/market/stock_charts.html.j2 src/do_uw/templates/html/sections/market/stock_performance.html.j2` — should return only format strings and zero-comparisons, not threshold values
3. Verify threshold injection: `uv run python -c "from do_uw.stages.render.context_builders.chart_thresholds import extract_chart_thresholds; t = extract_chart_thresholds(None); print(t)"`
</verification>

<success_criteria>
- All chart evaluation thresholds in stock_charts.html.j2 and stock_performance.html.j2 reference `thresholds.X.Y` variables, not numeric literals
- Signal YAMLs carry machine-parseable numeric evaluation thresholds
- chart_thresholds.py extracts thresholds and provides fallback defaults
- Unit tests pass for threshold extraction
- No rendering regression (templates produce identical output with same data)
</success_criteria>

<output>
After completion, create `.planning/phases/91-display-centralization/91-01-SUMMARY.md`
</output>
