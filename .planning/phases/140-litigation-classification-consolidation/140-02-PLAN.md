---
phase: 140-litigation-classification-consolidation
plan: 02
type: execute
wave: 2
depends_on: ["140-01"]
files_modified:
  - src/do_uw/stages/render/context_builders/litigation.py
  - src/do_uw/stages/render/context_builders/_litigation_helpers.py
  - src/do_uw/stages/render/sections/sect6_litigation.py
  - tests/stages/extract/test_litigation_classifier.py
autonomous: false
requirements: [LIT-01, LIT-02, LIT-03, LIT-04, LIT-05]

must_haves:
  truths:
    - "The litigation section in the rendered worksheet shows case type labels derived from legal theories, not data source names"
    - "Consolidated cases display the primary case name with all source references listed below"
    - "Every case name in the worksheet includes a year suffix"
    - "Each case shows its D&O coverage side classification"
    - "Cases with missing critical fields show a data quality warning in the output"
    - "Unclassified reserves appear in a separate subsection, not mixed with classified cases"
  artifacts:
    - path: "src/do_uw/stages/render/context_builders/litigation.py"
      provides: "Updated context builder that surfaces classifier output to templates"
    - path: "src/do_uw/stages/render/context_builders/_litigation_helpers.py"
      provides: "Display helpers for source references and data quality flags"
  key_links:
    - from: "src/do_uw/stages/render/context_builders/litigation.py"
      to: "src/do_uw/models/litigation.py"
      via: "reads landscape.unclassified_reserves for separate display"
      pattern: "unclassified_reserves"
    - from: "src/do_uw/stages/render/context_builders/_litigation_helpers.py"
      to: "src/do_uw/stages/render/context_builders/litigation.py"
      via: "helper functions imported by context builder"
      pattern: "from.*_litigation_helpers import"
---

<objective>
Update the litigation context builder and rendering helpers to display the classifier's output: legal-theory-based case types, consolidated source references, year-suffixed names, coverage side labels, data quality warnings, and separated unclassified reserves.

Purpose: The classifier (Plan 01) enriches the data model, but the worksheet won't show any of it unless the context builder passes it through to templates. This plan closes the loop from classification to display.

Output: Updated context builder and helpers that surface all classifier output in the rendered worksheet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-litigation-classification-consolidation/140-CONTEXT.md
@.planning/phases/140-litigation-classification-consolidation/140-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->

From src/do_uw/stages/extract/litigation_classifier.py:
```python
def classify_all_cases(landscape: LitigationLandscape) -> None
def deduplicate_all_cases(landscape: LitigationLandscape) -> None
def disambiguate_by_year(landscape: LitigationLandscape) -> None
def flag_missing_fields(landscape: LitigationLandscape) -> None
```

From src/do_uw/models/litigation.py (after Plan 01):
```python
class LitigationLandscape(BaseModel):
    securities_class_actions: list[CaseDetail] = []
    derivative_suits: list[CaseDetail] = []
    unclassified_reserves: list[CaseDetail] = []  # NEW from Plan 01
    # ... other fields
```

From src/do_uw/stages/render/context_builders/_litigation_helpers.py:
```python
COVERAGE_DISPLAY: dict[str, str] = {
    "SCA_SIDE_C": "Side C (Entity)",
    "SCA_SIDE_A": "Side A (D&O)",
    "SCA_SIDE_B": "Side B (Reimb.)",
    "DERIVATIVE_SIDE_A": "Derivative",
    "DERIVATIVE_SIDE_B": "Derivative (B)",
    "SEC_ENFORCEMENT_A": "SEC Enforcement (A)",
    "SEC_ENFORCEMENT_B": "SEC Enforcement (B)",
    "REGULATORY_ENTITY": "Regulatory",
    "EMPLOYMENT_ENTITY": "Employment",
    "PRODUCT_ENTITY": "Product Liability",
    # ...
}
def _sv_str(sv, fallback="N/A") -> str
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update litigation context builder and helpers to surface classifier output</name>
  <files>src/do_uw/stages/render/context_builders/litigation.py, src/do_uw/stages/render/context_builders/_litigation_helpers.py</files>
  <read_first>
    - src/do_uw/stages/render/context_builders/litigation.py (full file -- understand current context building flow)
    - src/do_uw/stages/render/context_builders/_litigation_helpers.py (full file -- understand existing display helpers)
    - src/do_uw/stages/render/sections/sect6_litigation.py (understand how template consumes context)
    - src/do_uw/models/litigation.py (CaseDetail fields, LitigationLandscape.unclassified_reserves)
    - src/do_uw/stages/extract/litigation_classifier.py (understand what data_quality_flags look like in allegations)
  </read_first>
  <action>
    In `_litigation_helpers.py`:

    1. Add a helper `extract_source_references(case: CaseDetail) -> str` that reads `case.case_name.source` (which Plan 01's dedup may have enriched with merged sources) and formats it as "Sources: EFTS/SCAC, 10-K Item 3" etc. If only one source, return just that source name.

    2. Add a helper `format_recovery_status(case: CaseDetail, cases_needing_recovery: list[dict]) -> str | None` that checks if this case appears in the recovery queue and returns a human-readable warning like "Missing: court, case_number" or None if no gaps.

    3. Add a helper `format_legal_theories(case: CaseDetail) -> str` that joins the legal_theories values into a human-readable string using the existing `CLAIM_TYPE_NAMES` mapping. E.g., "Rule 10b-5, Section 11".

    In `litigation.py` context builder:

    4. In the SCA case display dict construction (wherever case dicts are built for template consumption), add these keys:
       - `"legal_theories_display"`: result of `format_legal_theories(case)`
       - `"coverage_display"`: result of `COVERAGE_DISPLAY.get(case.coverage_type.value, "General")` if coverage_type exists
       - `"source_references"`: result of `extract_source_references(case)`
       - `"data_quality_flags"`: result of `extract_data_quality_flags(case)`

    5. Add an `"unclassified_reserves"` key to the litigation context dict. Build it from `landscape.unclassified_reserves` with basic display fields (case_name, filing_date, status). This surfaces D-07's separate bucket.

    6. Same treatment for derivative_suits display dicts (legal_theories_display, coverage_display, source_references, data_quality_flags).

    Note: Year suffixes are already in case_name from Plan 01's disambiguate_by_year -- no additional rendering work needed for LIT-03.

    In `sect6_litigation.py` (minimal template updates for user-visible fields):

    7. Audit where case dicts are rendered in the template. For SCA and derivative case tables/cards, add:
       - `legal_theories_display` — show as a label or column next to case name (e.g., "Rule 10b-5 | Section 11")
       - `coverage_display` — show as a badge/tag (e.g., "Side A (D&O)")
       - `source_references` — show below case name in smaller text (e.g., "Sources: EFTS/SCAC, 10-K Item 3")
    8. Add an `unclassified_reserves` subsection at the bottom of the litigation section if the list is non-empty. Simple table: case name, filing date, status.
    9. Year suffixes are already in case_name from Plan 01 — no template work needed for LIT-03.
    10. Data quality flags: if any case has data_quality_flags, show a small warning icon or note below the case entry.
  </action>
  <verify>
    <automated>uv run pytest tests/stages/extract/ -x --timeout=60 -q && uv run pytest tests/ -x --timeout=120 -k "litigation or render" -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "def extract_source_references" in _litigation_helpers.py confirms helper exists
    - grep "def extract_data_quality_flags" in _litigation_helpers.py confirms helper exists
    - grep "def format_legal_theories" in _litigation_helpers.py confirms helper exists
    - grep "legal_theories_display" in litigation.py context builder confirms key added to case dicts
    - grep "coverage_display" in litigation.py context builder confirms key added
    - grep "source_references" in litigation.py context builder confirms key added
    - grep "unclassified_reserves" in litigation.py context builder confirms bucket surfaced
    - All render and litigation tests pass
  </acceptance_criteria>
  <done>
    Context builder surfaces all classifier output: legal theory labels, coverage side labels, source references, data quality flags, and unclassified reserves bucket. No template changes required.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify litigation classification in rendered worksheet</name>
  <files>output from underwrite CLI</files>
  <action>
    Run `underwrite AAPL --fresh` to generate a full worksheet, then visually inspect the Litigation section for all 5 LIT requirements. Run a second ticker with known litigation (e.g., META) for cross-validation.
  </action>
  <verify>Human visual inspection of rendered HTML output</verify>
  <done>User confirms litigation section shows legal-theory-based types, no duplicates, year suffixes, coverage sides, data quality flags, and separated reserves.</done>
  <what-built>
    Full litigation classification and consolidation pipeline:
    - Unified post-extraction classifier (legal theory + coverage side)
    - Universal deduplication across SCA and derivative case lists
    - Year disambiguation on all case names
    - Missing field flagging with data quality annotations
    - Boilerplate reserves separated into unclassified bucket
    - Context builder updated to surface all classifier output
  </what-built>
  <how-to-verify>
    1. Run `underwrite AAPL --fresh` (or a ticker with known litigation)
    2. Open the HTML output and navigate to the Litigation section
    3. Verify:
       a. Each case shows its legal theory type (Rule 10b-5, Section 11, etc.) -- not just "SCA" or the data source name
       b. No duplicate cases appear (same case from different sources should be consolidated)
       c. Every case name includes a year suffix like "(2020)"
       d. Coverage side labels appear (Side A, Side B, Side C, etc.)
       e. If any cases have missing fields, check for data quality warnings
       f. If boilerplate reserves exist, they appear in a separate area from classified cases
    4. Try a second ticker with known litigation history (e.g., `underwrite META --fresh`) to verify consistency
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with the litigation display</resume-signal>
</task>

</tasks>

<verification>
1. `uv run pytest tests/stages/extract/test_litigation_classifier.py -x -v` -- classifier tests still green
2. `uv run pytest tests/ -x --timeout=120 -k "litigation or sca or render"` -- broad litigation + render suite green
3. `uv run ruff check src/do_uw/stages/render/context_builders/litigation.py src/do_uw/stages/render/context_builders/_litigation_helpers.py` -- no lint errors
4. `uv run pyright src/do_uw/stages/render/context_builders/litigation.py src/do_uw/stages/render/context_builders/_litigation_helpers.py` -- no type errors
5. Visual inspection of rendered worksheet confirms all 5 LIT requirements visible
</verification>

<success_criteria>
- Rendered worksheet shows legal-theory-based case types (not source-derived labels)
- Consolidated cases show merged source references
- All case names have year suffixes
- Coverage side is displayed per case
- Missing field warnings appear where applicable
- Unclassified reserves are separated from classified cases
- Minimal template updates in sect6_litigation.py to surface legal_theories_display, coverage_display, source_references, and unclassified_reserves
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/140-litigation-classification-consolidation/140-02-SUMMARY.md`
</output>
