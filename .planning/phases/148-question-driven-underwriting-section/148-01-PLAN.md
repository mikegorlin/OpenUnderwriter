---
phase: 148-question-driven-underwriting-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/stages/render/context_builders/answerers/__init__.py
  - src/do_uw/stages/render/context_builders/answerers/_helpers.py
  - src/do_uw/stages/render/context_builders/answerers/company.py
  - src/do_uw/stages/render/context_builders/answerers/financial.py
  - src/do_uw/stages/render/context_builders/answerers/governance.py
  - src/do_uw/stages/render/context_builders/answerers/market.py
  - src/do_uw/stages/render/context_builders/answerers/litigation.py
  - src/do_uw/stages/render/context_builders/answerers/operational.py
  - src/do_uw/stages/render/context_builders/answerers/program.py
  - src/do_uw/stages/render/context_builders/answerers/decision.py
  - tests/render/test_uw_questions.py
autonomous: true
requirements: [QFW-01, QFW-02, QFW-03, QFW-06]

must_haves:
  truths:
    - "Every one of the 55 brain questions has a dedicated answerer function registered in ANSWERER_REGISTRY"
    - "Answerers produce answers with specific numbers (dollar amounts, percentages, scores) not vague assessments"
    - "Questions with partial data return partial answers with inline Needs Review flags, not NO_DATA (per D-01)"
    - "No answerer file exceeds 500 lines"
    - "All 55 YAML questions have required schema fields: question_id, text, weight, domain, data_sources (validates QFW-01/QFW-02)"
  artifacts:
    - path: "src/do_uw/stages/render/context_builders/answerers/__init__.py"
      provides: "ANSWERER_REGISTRY dict and @register decorator"
      exports: ["ANSWERER_REGISTRY", "register"]
    - path: "src/do_uw/stages/render/context_builders/answerers/_helpers.py"
      provides: "Shared utilities: safe_float_extract, fmt_currency, fmt_pct, sv, suggest_filing_reference"
    - path: "src/do_uw/stages/render/context_builders/answerers/company.py"
      provides: "BIZ-01 through BIZ-06 answerers"
    - path: "src/do_uw/stages/render/context_builders/answerers/financial.py"
      provides: "FIN-01 through FIN-08 answerers"
    - path: "src/do_uw/stages/render/context_builders/answerers/governance.py"
      provides: "GOV-01 through GOV-08 answerers"
    - path: "src/do_uw/stages/render/context_builders/answerers/market.py"
      provides: "MKT-01 through MKT-07 answerers"
    - path: "src/do_uw/stages/render/context_builders/answerers/litigation.py"
      provides: "LIT-01 through LIT-07 answerers"
    - path: "src/do_uw/stages/render/context_builders/answerers/operational.py"
      provides: "OPS-01 through OPS-07 answerers"
    - path: "src/do_uw/stages/render/context_builders/answerers/program.py"
      provides: "PRG-01 through PRG-05 answerers"
    - path: "src/do_uw/stages/render/context_builders/answerers/decision.py"
      provides: "UW-01 through UW-07 answerers"
    - path: "tests/render/test_uw_questions.py"
      provides: "Tests for answerer registry completeness, answer quality, and YAML schema validation"
  key_links:
    - from: "src/do_uw/stages/render/context_builders/answerers/__init__.py"
      to: "src/do_uw/stages/render/context_builders/uw_questions.py"
      via: "ANSWERER_REGISTRY import"
      pattern: "from.*answerers.*import.*ANSWERER_REGISTRY"
    - from: "src/do_uw/stages/render/context_builders/answerers/*.py"
      to: "src/do_uw/stages/render/context_builders/answerers/__init__.py"
      via: "@register decorator auto-registration"
      pattern: "@register"
---

<objective>
Create the answerers subpackage with dedicated answerer functions for all 55 underwriting questions across 8 domains, plus a central registry and shared helpers.

Purpose: Per D-07, every question gets its own answerer function (no generic category fallbacks). Per D-01, partial data produces partial answers with inline Needs Review flags. Per D-02, BIZ-05, BIZ-06, OPS-01, OPS-04 use LLM extraction from filing text for data gaps.

Output: 10 new Python files in answerers/ subpackage, 1 test file. The existing 8 answerers in uw_questions.py are migrated into the subpackage and expanded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-question-driven-underwriting-section/148-CONTEXT.md
@.planning/phases/148-question-driven-underwriting-section/148-RESEARCH.md

@src/do_uw/stages/render/context_builders/uw_questions.py
@src/do_uw/stages/render/context_builders/screening_answers.py
@src/do_uw/brain/questions/01_company_identity.yaml
@src/do_uw/brain/questions/02_financial_health.yaml
@src/do_uw/brain/questions/03_governance_people.yaml
@src/do_uw/brain/questions/04_stock_market.yaml
@src/do_uw/brain/questions/05_litigation_claims.yaml
@src/do_uw/brain/questions/06_operational_emerging.yaml
@src/do_uw/brain/questions/07_program_pricing.yaml
@src/do_uw/brain/questions/08_underwriting_decision.yaml
@src/do_uw/models/state.py

<interfaces>
<!-- Answerer function signature (established by pre-work): -->
```python
def _answer_xxx_nn(
    q: dict[str, Any],        # Question dict from YAML
    state: AnalysisState,      # Full pipeline state
    ctx: dict[str, Any],       # Render context (has executive_summary, financials, governance, litigation, scoring, market, etc.)
) -> dict[str, Any]:
    # Returns: answer, evidence, verdict (UPGRADE|DOWNGRADE|NEUTRAL|NO_DATA), confidence, data_found
```

<!-- Answer dict structure: -->
```python
{
    "answer": str,             # Human-readable answer with specific numbers
    "evidence": list[str],     # Supporting data points
    "verdict": str,            # UPGRADE | DOWNGRADE | NEUTRAL | NO_DATA
    "confidence": str,         # HIGH | MEDIUM | LOW
    "data_found": bool,        # True if any data was found
}
```

<!-- Context keys available in ctx dict (from uw_analysis.py build): -->
# ctx["executive_summary"], ctx["financials"], ctx["governance"],
# ctx["litigation"], ctx["scoring"], ctx["market"],
# ctx["triggered_checks"], ctx["enhanced_drop_events"],
# ctx["forensic_composites"], ctx["nlp_dashboard"],
# ctx["settlement"], ctx["peril"], ctx["exec_risk"],
# ctx["temporal"], ctx["investigative"]
# ctx["_state"] = state (raw AnalysisState access)

<!-- From screening_answers.py - reusable helpers to extract into _helpers.py: -->
# _sv(val) -> unwraps SourcedValue or returns raw
# _fmt_currency(val) -> "$X.XB" / "$X.XM"
# _fmt_pct(val) -> "X.X%"
# safe_float from stages/render/formatters.py
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create answerers subpackage with registry, helpers, and all 55 answerers</name>
  <files>
    src/do_uw/stages/render/context_builders/answerers/__init__.py
    src/do_uw/stages/render/context_builders/answerers/_helpers.py
    src/do_uw/stages/render/context_builders/answerers/company.py
    src/do_uw/stages/render/context_builders/answerers/financial.py
    src/do_uw/stages/render/context_builders/answerers/governance.py
    src/do_uw/stages/render/context_builders/answerers/market.py
    src/do_uw/stages/render/context_builders/answerers/litigation.py
    src/do_uw/stages/render/context_builders/answerers/operational.py
    src/do_uw/stages/render/context_builders/answerers/program.py
    src/do_uw/stages/render/context_builders/answerers/decision.py
  </files>
  <read_first>
    src/do_uw/stages/render/context_builders/uw_questions.py
    src/do_uw/stages/render/context_builders/screening_answers.py
    src/do_uw/brain/questions/01_company_identity.yaml
    src/do_uw/brain/questions/02_financial_health.yaml
    src/do_uw/brain/questions/03_governance_people.yaml
    src/do_uw/brain/questions/04_stock_market.yaml
    src/do_uw/brain/questions/05_litigation_claims.yaml
    src/do_uw/brain/questions/06_operational_emerging.yaml
    src/do_uw/brain/questions/07_program_pricing.yaml
    src/do_uw/brain/questions/08_underwriting_decision.yaml
    src/do_uw/models/state.py
    src/do_uw/stages/render/formatters.py
  </read_first>
  <action>
    Create the `answerers/` subpackage under `src/do_uw/stages/render/context_builders/`.

    **Step 1: `answerers/__init__.py`** — Central registry with decorator pattern:
    ```python
    from typing import Any, Callable
    from do_uw.models.state import AnalysisState

    AnswererFunc = Callable[[dict[str, Any], AnalysisState, dict[str, Any]], dict[str, Any]]
    ANSWERER_REGISTRY: dict[str, AnswererFunc] = {}

    def register(*question_ids: str):
        def wrapper(fn: AnswererFunc) -> AnswererFunc:
            for qid in question_ids:
                ANSWERER_REGISTRY[qid] = fn
            return fn
        return wrapper
    ```
    At the bottom, import all domain modules to trigger registration:
    ```python
    from do_uw.stages.render.context_builders.answerers import (
        company, financial, governance, market, litigation,
        operational, program, decision,
    )
    ```

    **Step 2: `answerers/_helpers.py`** — Shared utilities extracted from screening_answers.py:
    - `sv(val)` — unwrap SourcedValue or return raw value (copy from screening_answers `_sv`)
    - `fmt_currency(val)` — format as "$X.XB" / "$X.XM" (copy from screening_answers `_fmt_currency`)
    - `fmt_pct(val)` — format as "X.X%" (copy from screening_answers `_fmt_pct`)
    - `safe_float_extract(val, default=None)` — wraps `safe_float` from formatters.py
    - `get_signal_results(state) -> dict` — extract signal_results from `state.analysis.disposition_summary` or `state.scoring`
    - `suggest_filing_reference(data_sources: list[str]) -> str` — copy from uw_questions.py `_suggest_filing_reference`
    - `partial_answer(answer: str, missing: str, filing_ref: str) -> dict` — returns answer dict with inline "Needs Review" flag per D-01

    **Step 3: Domain files** — Each file contains dedicated answerer functions per D-07. Migrate the 8 existing answerers from `uw_questions.py` into their respective domain files and add the 47 new ones. Every function uses `@register("XXX-NN")`.

    Question-to-data mapping (from RESEARCH.md, section "Question-to-Data Mapping"):

    **company.py** (BIZ-01 through BIZ-06):
    - BIZ-01: MIGRATE from uw_questions.py `_answer_biz_01`. Uses `ctx.executive_summary`, `yfinance.info`, `state.company.identity.sic_code`.
    - BIZ-02: NEW. Revenue from `ctx.financials.revenue` or `ctx.executive_summary.revenue`. Segment data from `state.extracted.financials.statements` if available. If no segments, answer with total revenue + "Needs Review: check 10-K Item 1/7 for segment breakdown".
    - BIZ-03: MIGRATE from uw_questions.py `_answer_biz_03`. Uses `yfinance.info.marketCap`.
    - BIZ-04: NEW. `yfinance.info.fullTimeEmployees`, `state.company.identity.headquarters`. Format: "{N} employees. HQ: {city, state}."
    - BIZ-05: NEW. Check `ctx` for ma_profile, check signal_results for `MA.*` prefixed signals. If no structured data, check `state.extracted` for 8-K filings mentioning M&A terms. LLM extraction candidate per D-02 — if filing text available in state, extract M&A from 8-K Item 1.01. Partial answer if only signal data available.
    - BIZ-06: NEW. Check `state.extracted.risk_factors` if populated. LLM extraction candidate per D-02 — extract top 5 risk factors from 10-K Item 1A text. Partial answer with signal-triggered risk factors if LLM extraction unavailable.

    **financial.py** (FIN-01 through FIN-08):
    - FIN-01: MIGRATE from uw_questions.py `_answer_fin_01`.
    - FIN-02: MIGRATE from uw_questions.py `_answer_fin_02`.
    - FIN-03: NEW. Beneish M-Score from `ctx.financials.beneish_score` + `beneish_level`. Altman Z-Score from `ctx.financials.altman_z_score` + `altman_zone`. Forensic composites from `ctx.forensic_composites`. MUST include actual score numbers (e.g., "Beneish M-Score: -2.31 (safe zone, threshold: -1.78)") NOT "inconclusive".
    - FIN-04: NEW. Check signal_results for `DISC.CTRL.*` signals. Check `state.extracted.financials.audit` for restatement flags. DOWNGRADE if restatement found.
    - FIN-05: NEW. `ctx.financials.auditor_name`, `ctx.financials.audit_alerts`. Check for material weakness signals. Reuse logic concepts from screening_answers ACCT-01/02 but as dedicated functions. Include auditor name + opinion type.
    - FIN-06: NEW. `state.extracted.financials.statements` for OCF/FCF. `yfinance.info.operatingCashflow` + `freeCashflow` as fallback. DOWNGRADE if negative OCF.
    - FIN-07: NEW. Check industry via `yfinance.info.industry`. Check signal_results for `rev_rec_*`. Industries like software/SaaS/construction flag elevated risk. Similar concept to screening ACCT-05 but dedicated.
    - FIN-08: NEW. Signal_results for `earnings_quality_*`. Beneish sub-scores. GAAP vs non-GAAP divergence.

    **governance.py** (GOV-01 through GOV-08):
    - GOV-01: MIGRATE from uw_questions.py `_answer_gov_01`.
    - GOV-02: NEW. Signal_results `exec_litigation_*`. `ctx.exec_risk` if populated. `ctx.litigation.cases` filtered by defendant names. DOWNGRADE if directors/officers named in lawsuits.
    - GOV-03: NEW. `state.extracted.governance.compensation` for CEO pay. `ctx.governance.say_on_pay` for vote results. DOWNGRADE if say-on-pay < 70%.
    - GOV-04: NEW. Signal_results `related_party_*`. `state.extracted.governance` for RPT disclosures. DOWNGRADE if significant RPTs found.
    - GOV-05: NEW. `state.extracted.governance.board.directors` — check tenure field if available. Calculate avg tenure. DOWNGRADE if avg tenure < 2 years or > 20 years (entrenchment).
    - GOV-06: NEW. `state.extracted.governance.board.directors` — check qualifications/background fields. Report count of directors with financial/legal/industry expertise.
    - GOV-07: NEW. Signal_results `exec_risk_*`. `ctx.exec_risk`. Character/integrity issues. DOWNGRADE if background flags found.
    - GOV-08: NEW. Signal_results for `controlled_company`. `state.extracted.governance.ownership` for dual-class. DOWNGRADE if controlled company or dual-class.

    **market.py** (MKT-01 through MKT-07):
    - MKT-01: MIGRATE from uw_questions.py `_answer_mkt_01`.
    - MKT-02: NEW. `yfinance.info.shortPercentOfFloat` + `shortRatio`. DOWNGRADE if short > 10%. Include actual numbers.
    - MKT-03: NEW. Market cap from yfinance. Settlement benchmarks from `ctx.settlement`. Calculate DDL exposure = market_cap * avg_stock_drop_pct. Include dollar amounts.
    - MKT-04: NEW. `state.acquired_data.market_data.insider_transactions` if available. Signal_results `insider_*`. Report total insider sells/buys in last 12 months. DOWNGRADE if net selling > $1M.
    - MKT-05: NEW. `state.acquired_data.market_data.institutional_holders`. Top holders, total institutional %. NEUTRAL typically.
    - MKT-06: NEW. `yfinance.info.fiftyTwoWeekHigh/Low/currentPrice`. Calculate % from high and % from low. DOWNGRADE if within 10% of 52-week low. UPGRADE if within 10% of 52-week high.
    - MKT-07: NEW. Signal_results `offering_*`. `state.extracted.sec_filings` for S-3/424B filings. DOWNGRADE if recent equity/debt issuance (dilution risk).

    **litigation.py** (LIT-01 through LIT-07):
    - LIT-01: MIGRATE from uw_questions.py `_answer_lit_01`.
    - LIT-02: NEW. `ctx.litigation.cases` filtered by status (active/pending). Count and summarize active cases. DOWNGRADE if active cases exist.
    - LIT-03: NEW. `ctx.litigation.risk_card_filing_history` for settlement data. `ctx.litigation.risk_card_repeat_filer` for total exposure. Include dollar amounts.
    - LIT-04: NEW. Signal_results `sec_enforcement_*`. `state.acquired_data.regulatory_data`. DOWNGRADE if SEC enforcement actions found.
    - LIT-05: NEW. `ctx.peril` for allegation theories. `state.scoring.allegation_mapping` if available. List most likely theories (10b-5, Section 11, etc.) with probability.
    - LIT-06: NEW. Signal_results `regulatory_*`. Web search results for non-SCA lawsuits (state AG, class actions, derivative suits). Partial answer from signals if no web data.
    - LIT-07: NEW. `ctx.temporal` for statute of limitations analysis. `ctx.enhanced_drop_events` for key dates. Report SOL windows for each trigger event.

    **operational.py** (OPS-01 through OPS-07):
    - OPS-01: NEW. Signal_results `cyber_*`. LLM extraction candidate per D-02 — extract from 10-K Item 1/1A. Partial answer from signals if no LLM data.
    - OPS-02: NEW. Signal_results `regulatory_*`. LLM extraction candidate per D-02 — extract regulatory risks from 10-K Item 1/1A. Partial answer from signals.
    - OPS-03: NEW. Signal_results `esg_*`. `state.alt_data` for ESG context. NEUTRAL unless specific ESG flags.
    - OPS-04: NEW. `state.extracted.financials.statements` for segment data. LLM extraction candidate per D-02 — extract customer concentration from 10-K Item 1/7. Partial answer.
    - OPS-05: NEW. Signal_results `tariff_*`. `state.alt_data` for tariff/geopolitical. NEUTRAL unless specific flags.
    - OPS-06: NEW. `state.company.identity.sic_code` mapped to SCA frequency. Risk card `scenario_benchmarks`. Report sector filing rate.
    - OPS-07: NEW. `state.forward_looking` if populated. `state.analysis.temporal_signals`. Report upcoming events (earnings, FDA, contract expiry).

    **program.py** (PRG-01 through PRG-05):
    - PRG-01: NEW. `state.scoring.tower_recommendation` if exists. Market cap for tower sizing. Answer with recommended tower size and reasoning.
    - PRG-02: NEW. `ctx.litigation.risk_card_filing_history`. `state.scoring.claim_probability` if exists. Report claim frequency and probability.
    - PRG-03: NEW. `state.benchmark` for peer comparison. `state.scoring.actuarial_pricing` if exists. Report peer rate comparison.
    - PRG-04: NEW. Market cap based retention sizing. Standard D&O retention guidelines by market cap tier.
    - PRG-05: NEW. `state.scoring.allegation_mapping` for theories needing specific exclusions. Report coverage gaps.

    **decision.py** (UW-01 through UW-07):
    - UW-01: MIGRATE from uw_questions.py `_answer_uw_01`.
    - UW-02: NEW. `state.scoring.factor_scores` — find lowest-deduction factors (strengths). Signal_results with CLEAN status. Top 3 reasons to write the risk.
    - UW-03: NEW. `state.scoring.red_flags`. `state.scoring.factor_scores` — highest deductions. Top 3 reasons NOT to write.
    - UW-04: NEW. From red flags and allegation mapping, derive required conditions (subjectivities, exclusions, retentions).
    - UW-05: NEW. `state.scoring.actuarial_pricing` + `state.scoring.severity_scenarios`. Report suggested pricing range.
    - UW-06: NEW. `state.scoring.red_flags` + `state.scoring.ceiling_details`. List conditions that would trigger a decline.
    - UW-07: NEW. `state.analysis.disposition_summary` — count SKIPPED signals needing human review. List low-confidence data needing verification.

    **CRITICAL RULES for all answerers:**
    - Use `safe_float` from formatters.py for ALL numeric conversions (CLAUDE.md non-negotiable)
    - Use `sv()` helper to unwrap SourcedValue objects
    - Every answer must include actual numbers: "$52.9B", "27.9%", "-2.31", not "significant" or "elevated"
    - Per D-01: if ANY relevant data exists, return a partial answer. Only return NO_DATA when genuinely no data path has data.
    - Per D-06: LLM extraction is OPTIONAL — always have a non-LLM fallback path. If LLM extraction not available, use signal_results + structured data and flag gaps as "Needs Review".
    - `_suggest_filing_reference` (migrated to helpers) must be used for all Needs Review flags per QFW-06.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && python -c "from do_uw.stages.render.context_builders.answerers import ANSWERER_REGISTRY; print(f'Registered: {len(ANSWERER_REGISTRY)} answerers'); assert len(ANSWERER_REGISTRY) >= 55, f'Only {len(ANSWERER_REGISTRY)} answerers registered, need 55'" && grep -rn "float(" src/do_uw/stages/render/context_builders/answerers/*.py | grep -v safe_float | grep -v "# safe" | grep -v "__pycache__" && echo "BARE_FLOAT_CHECK_DONE (any lines above are violations)"</automated>
  </verify>
  <acceptance_criteria>
    - `answerers/__init__.py` contains `ANSWERER_REGISTRY` dict and `register` decorator function
    - `answerers/_helpers.py` contains `sv`, `fmt_currency`, `fmt_pct`, `safe_float_extract`, `suggest_filing_reference`, `partial_answer`
    - `answerers/company.py` contains `@register("BIZ-01")` through `@register("BIZ-06")` — 6 functions
    - `answerers/financial.py` contains `@register("FIN-01")` through `@register("FIN-08")` — 8 functions
    - `answerers/governance.py` contains `@register("GOV-01")` through `@register("GOV-08")` — 8 functions
    - `answerers/market.py` contains `@register("MKT-01")` through `@register("MKT-07")` — 7 functions
    - `answerers/litigation.py` contains `@register("LIT-01")` through `@register("LIT-07")` — 7 functions
    - `answerers/operational.py` contains `@register("OPS-01")` through `@register("OPS-07")` — 7 functions
    - `answerers/program.py` contains `@register("PRG-01")` through `@register("PRG-05")` — 5 functions
    - `answerers/decision.py` contains `@register("UW-01")` through `@register("UW-07")` — 7 functions
    - `python -c "from do_uw.stages.render.context_builders.answerers import ANSWERER_REGISTRY; assert len(ANSWERER_REGISTRY) >= 55"` exits 0
    - No file in answerers/ exceeds 500 lines (verified by `wc -l`)
    - No bare `float()` calls in answerers/ (verified by grep — warning 4 fix)
    - BIZ-05, BIZ-06, OPS-01, OPS-04 answerers contain an LLM extraction code path with non-LLM fallback (per D-02)
  </acceptance_criteria>
  <done>All 55 underwriting questions have dedicated answerer functions registered in ANSWERER_REGISTRY. Each answerer accesses specific state/ctx data paths and returns answers with actual numbers. Partial data produces partial answers per D-01. LLM extraction is attempted for BIZ-05, BIZ-06, OPS-01, OPS-04 with non-LLM fallbacks per D-02.</done>
</task>

<task type="auto">
  <name>Task 2: Wire answerers into uw_questions.py and add tests (including YAML schema validation)</name>
  <files>
    src/do_uw/stages/render/context_builders/uw_questions.py
    tests/render/test_uw_questions.py
  </files>
  <read_first>
    src/do_uw/stages/render/context_builders/uw_questions.py
    src/do_uw/stages/render/context_builders/answerers/__init__.py
    src/do_uw/brain/questions/__init__.py
    src/do_uw/models/state.py
  </read_first>
  <action>
    **Step 1: Modify `uw_questions.py`** to use `ANSWERER_REGISTRY` instead of inline `_domain_answerers`:

    - Replace `from do_uw.stages.render.context_builders.screening_answers import answer_screening_questions` with `from do_uw.stages.render.context_builders.answerers import ANSWERER_REGISTRY`
    - Remove the 8 inline answerer functions (`_answer_biz_01` through `_answer_uw_01`) — they are now in the answerers subpackage
    - Remove the `_domain_answerers` dict at the bottom
    - Remove `_suggest_filing_reference` — now in `answerers/_helpers.py`
    - Update `build_uw_questions_context` to use `ANSWERER_REGISTRY` directly instead of the two-pass approach (screening_answers first, then domain fallback):
      ```python
      for i, q in enumerate(raw_questions):
          qid = q.get("question_id", "")
          answerer = ANSWERER_REGISTRY.get(qid)
          if answerer:
              result = answerer(q, state, ctx_with_state)
              answered.append({**q, **result})
          else:
              # No answerer registered — mark as needs review
              answered.append({
                  **q,
                  "verdict": "NO_DATA",
                  "data_found": False,
                  "answer": "",
                  "evidence": [],
                  "confidence": "LOW",
              })
      ```
    - Import `suggest_filing_reference` from `answerers._helpers` for the filing ref logic (still used in the formatting loop)
    - Keep the domain group formatting, summary stats, and net_assessment logic intact
    - The file should shrink from ~541 lines to ~150 lines

    **Step 2: Create `tests/render/test_uw_questions.py`**:

    - `test_question_schema_completeness` (QFW-01/QFW-02 validation): Load all questions via `load_all_questions()`. For EACH of the 55 questions, assert the YAML dict has required fields: `question_id`, `text`, `weight`, `domain`, `data_sources`. Assert `weight` is int 1-10. Assert `question_id` matches pattern `^[A-Z]{2,4}-\d{2}$`. Assert total count >= 55.
    - `test_all_questions_have_answerers`: Load all questions via `load_all_questions()`, check each `question_id` exists in `ANSWERER_REGISTRY`. Assert count >= 55.
    - `test_answerer_returns_required_fields`: For each registered answerer, call with a minimal mock state + ctx. Verify return dict has keys: `answer`, `evidence`, `verdict`, `confidence`, `data_found`. Verify `verdict` is one of UPGRADE/DOWNGRADE/NEUTRAL/NO_DATA.
    - `test_no_vague_assessments`: For answerers that return data_found=True, verify `answer` field is not empty and contains at least one digit or "$" or "%" character (ensures actual numbers, not vague text).
    - `test_filing_references_for_no_data`: For answerers that return NO_DATA, verify the question's `data_sources` would produce a non-empty filing reference from `suggest_filing_reference`.
    - `test_registry_no_duplicates`: Verify no question_id is registered twice (would silently overwrite).
    - `test_section_position` (QFW-07 validation): Read the worksheet template file (`templates/html/worksheet.html.j2` or the section ordering source), verify that the `uw_questions` section/include appears AFTER the scoring section and BEFORE the meeting prep section in the template include order.

    Use a minimal mock AnalysisState. Create a fixture that builds a realistic `ctx` dict with at least `executive_summary`, `financials`, `governance`, `litigation`, `scoring` populated with sample data so answerers can produce real answers.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/render/test_uw_questions.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uw_questions.py` imports `ANSWERER_REGISTRY` from `answerers` (grep confirms: `from do_uw.stages.render.context_builders.answerers import ANSWERER_REGISTRY`)
    - `uw_questions.py` does NOT contain `_answer_biz_01` or any other inline answerer function
    - `uw_questions.py` does NOT contain `_domain_answerers` dict
    - `uw_questions.py` is under 200 lines (`wc -l` confirms)
    - `tests/render/test_uw_questions.py` contains `test_question_schema_completeness`, `test_all_questions_have_answerers`, `test_answerer_returns_required_fields`, `test_no_vague_assessments`, `test_section_position`
    - `test_question_schema_completeness` validates all 55 questions have required YAML fields (QFW-01/QFW-02)
    - `test_section_position` validates uw_questions section ordering in template (QFW-07)
    - `uv run pytest tests/render/test_uw_questions.py -x -q` exits 0
  </acceptance_criteria>
  <done>uw_questions.py uses ANSWERER_REGISTRY from the answerers subpackage. All 8 inline answerers removed. Tests verify 55+ answerers registered, return valid dicts, produce answers with actual numbers, validate YAML schema (QFW-01/02), and verify section positioning (QFW-07).</done>
</task>

</tasks>

<verification>
1. `python -c "from do_uw.stages.render.context_builders.answerers import ANSWERER_REGISTRY; print(len(ANSWERER_REGISTRY))"` prints >= 55
2. `grep -rn "@register" src/do_uw/stages/render/context_builders/answerers/*.py | wc -l` shows >= 55
3. `wc -l src/do_uw/stages/render/context_builders/answerers/*.py` — no file exceeds 500 lines
4. `uv run pytest tests/render/test_uw_questions.py -x -q` passes
5. `grep -rn "_domain_answerers" src/do_uw/stages/render/context_builders/uw_questions.py` returns nothing
6. `grep -rn "float(" src/do_uw/stages/render/context_builders/answerers/*.py | grep -v safe_float` returns nothing
</verification>

<success_criteria>
- 55 answerer functions registered in ANSWERER_REGISTRY (one per question)
- Each answerer produces answers with specific numbers, not vague assessments
- uw_questions.py refactored to use registry (under 200 lines)
- All tests pass including YAML schema validation (QFW-01/02) and section position (QFW-07)
- No file exceeds 500 lines
- No bare float() calls in answerers/
</success_criteria>

<output>
After completion, create `.planning/phases/148-question-driven-underwriting-section/148-01-SUMMARY.md`

IMPORTANT: The SUMMARY.md file is a mandatory output artifact. Plan 03 depends on understanding what was built here. Ensure the SUMMARY.md documents: files created, exports available, any deviations from plan.
</output>
