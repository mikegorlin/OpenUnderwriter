---
phase: 148-question-driven-underwriting-section
plan: 03
type: execute
wave: 2
depends_on: ["148-01", "148-02"]
files_modified:
  - src/do_uw/stages/render/context_builders/uw_questions.py
  - src/do_uw/stages/render/context_builders/uw_analysis.py
  - src/do_uw/templates/html/sections/report/uw_questions.html.j2
autonomous: false
requirements: [QFW-05, QFW-07]

must_haves:
  truths:
    - "SCA questions appear inline within their matching domain groups (Litigation, Market, Operational) not as a separate section"
    - "Each domain header shows a verdict badge (FAVORABLE/UNFAVORABLE/MIXED) based on upgrade vs downgrade counts"
    - "Section header shows overall assessment badge"
    - "Answerers for peril-dependent questions (LIT-05, LIT-07) and forensic-dependent questions (FIN-03) return populated values, not NO_DATA, when peril/forensic/temporal ctx keys are available"
    - "Printing the section to PDF renders verdict dots, completeness bars, and domain badges with correct colors"
  artifacts:
    - path: "src/do_uw/stages/render/context_builders/uw_questions.py"
      provides: "SCA question integration into domain groups + domain verdict badges"
    - path: "src/do_uw/stages/render/context_builders/uw_analysis.py"
      provides: "Fixed context ordering -- uw_questions built after all analysis contexts"
    - path: "src/do_uw/templates/html/sections/report/uw_questions.html.j2"
      provides: "Domain verdict badges + SCA source badge + print CSS"
  key_links:
    - from: "src/do_uw/stages/render/context_builders/uw_questions.py"
      to: "src/do_uw/stages/render/context_builders/answerers/sca_questions.py"
      via: "generate_sca_questions import"
      pattern: "from.*sca_questions.*import.*generate_sca_questions"
    - from: "src/do_uw/stages/render/context_builders/uw_analysis.py"
      to: "src/do_uw/stages/render/context_builders/uw_questions.py"
      via: "build_uw_questions_context call moved after line 228"
      pattern: "ctx.*uw_questions.*=.*build_uw_questions_context"
---

<objective>
Wire SCA questions into domain groups, add domain/section verdict badges (D-09), fix context ordering, and add print CSS (D-10). This is the integration plan that connects Plans 01 and 02 into a working section.

Purpose: Per D-09, each domain header shows net assessment. Per D-04, SCA questions slot inline by domain. Per D-10, the section prints cleanly. The context ordering fix (Research open question #1) ensures answerers can access all ctx keys including peril, settlement, exec_risk, temporal.

Output: Modified uw_questions.py (SCA integration + verdict), modified uw_analysis.py (ordering fix), updated template (badges + print CSS).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-question-driven-underwriting-section/148-CONTEXT.md
@.planning/phases/148-question-driven-underwriting-section/148-RESEARCH.md

@src/do_uw/stages/render/context_builders/uw_questions.py
@src/do_uw/stages/render/context_builders/uw_analysis.py
@src/do_uw/templates/html/sections/report/uw_questions.html.j2
@src/do_uw/stages/render/context_builders/answerers/sca_questions.py
@src/do_uw/stages/render/context_builders/answerers/_helpers.py

<interfaces>
<!-- From Plan 02 - SCA question generator: -->
```python
from do_uw.stages.render.context_builders.answerers.sca_questions import generate_sca_questions

# Returns list of question dicts with:
# - question_id: "SCA-LIT-01", "SCA-MKT-01", etc.
# - source: "SCA Data"
# - domain: "litigation_claims" | "stock_market" | "operational_emerging"
# - answer, evidence, verdict, confidence, data_found (pre-answered)
```

<!-- From Plan 01 - answerers registry: -->
```python
from do_uw.stages.render.context_builders.answerers import ANSWERER_REGISTRY
```

<!-- From Plan 01 - helpers: -->
```python
from do_uw.stages.render.context_builders.answerers._helpers import suggest_filing_reference
```

<!-- Context ordering in uw_analysis.py (lines 208-228): -->
# Line 213: ctx["uw_questions"] = build_uw_questions_context(state, ctx)  <-- TOO EARLY
# Lines 216-222: forensic_composites, xbrl_forensics, nlp_dashboard, settlement, peril, exec_risk, temporal
# Lines 224-228: investigative, valuation_multiples, short_interest
# FIX: Move uw_questions call AFTER line 228

<!-- Domain verdict badge values (from RESEARCH.md): -->
# "FAVORABLE" if upgrades > downgrades
# "UNFAVORABLE" if downgrades > upgrades
# "MIXED" if equal
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate SCA questions into domain groups + add verdict badges + fix context ordering</name>
  <files>
    src/do_uw/stages/render/context_builders/uw_questions.py
    src/do_uw/stages/render/context_builders/uw_analysis.py
    src/do_uw/templates/html/sections/report/uw_questions.html.j2
  </files>
  <read_first>
    src/do_uw/stages/render/context_builders/uw_questions.py
    src/do_uw/stages/render/context_builders/uw_analysis.py
    src/do_uw/templates/html/sections/report/uw_questions.html.j2
    src/do_uw/stages/render/context_builders/answerers/sca_questions.py
    src/do_uw/stages/render/context_builders/answerers/_helpers.py
  </read_first>
  <action>
    **Step 1: Fix context ordering in `uw_analysis.py`**

    Move the `ctx["uw_questions"] = build_uw_questions_context(state, ctx)` call from line 213 to AFTER line 228 (after `valuation_multiples` and `short_interest_pct/ratio` are set). This ensures answerers can access:
    - `ctx["forensic_composites"]` (built at line 216)
    - `ctx["settlement"]` (built at line 219)
    - `ctx["peril"]` (built at line 220)
    - `ctx["exec_risk"]` (built at line 221)
    - `ctx["temporal"]` (built at line 222)
    - `ctx["investigative"]` (built at line 225)

    Insert at approximately line 235 (after the short_interest assignments):
    ```python
    # Underwriting Decision Framework -- must be LAST so answerers access all ctx keys
    ctx["uw_questions"] = build_uw_questions_context(state, ctx)
    ```

    Remove the old call at line 213.

    **Step 2: Integrate SCA questions into `uw_questions.py`**

    Import `generate_sca_questions` from `answerers.sca_questions`.

    In `build_uw_questions_context`, after loading brain domains and before iterating:
    ```python
    # Generate SCA-derived questions and slot into matching domains
    sca_questions = generate_sca_questions(state, ctx_with_state)
    sca_by_domain: dict[str, list[dict]] = {}
    for sq in sca_questions:
        target = sq.get("domain", "")
        sca_by_domain.setdefault(target, []).append(sq)
    ```

    Then in the domain loop, after processing brain questions, append SCA questions for that domain. SCA questions come pre-answered, so just format and count them. Include `"source": sq.get("source", "")` in the formatted dict for the template badge.

    **Step 3: Add domain verdict badge to domain_groups dict**

    After counting upgrades/downgrades per domain:
    ```python
    net = domain_upgrades - domain_downgrades
    domain_verdict = "FAVORABLE" if net > 0 else ("UNFAVORABLE" if net < 0 else "MIXED")
    ```
    Add `"verdict": domain_verdict` to domain_groups.append dict.
    Add `"section_verdict"` to the return dict using same logic on totals.

    **Step 4: Update template for verdict badges + SCA source badge + print CSS**

    In `uw_questions.html.j2`:
    - Domain verdict badge: `<span>` with FAVORABLE:#065F46, UNFAVORABLE:#991B1B, MIXED:#374151 background
    - SCA source badge: `{% if q.source %}<span style="...background:#6366F1;color:white...">{{ q.source }}</span>{% endif %}`
    - Print CSS: `@media print` block with `print-color-adjust: exact` for all colored elements
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/render/test_uw_questions.py tests/render/test_sca_questions.py -x -q 2>/dev/null; python -c "
src = open('src/do_uw/stages/render/context_builders/uw_analysis.py').read()
lines = src.split('\n')
uw_q_line = forensic_line = None
for i, line in enumerate(lines):
    if 'uw_questions' in line and 'build_uw_questions_context' in line: uw_q_line = i
    if 'forensic_composites' in line and 'build_forensic_composites_context' in line: forensic_line = i
assert uw_q_line and forensic_line and uw_q_line > forensic_line, f'ordering wrong: uw={uw_q_line} forensic={forensic_line}'
print(f'Context ordering OK: forensic at {forensic_line}, uw_questions at {uw_q_line}')
"</automated>
  </verify>
  <acceptance_criteria>
    - `uw_analysis.py`: `build_uw_questions_context` call appears AFTER forensic/settlement/peril/exec_risk/temporal calls
    - `uw_questions.py`: imports `generate_sca_questions` from `answerers.sca_questions`
    - `uw_questions.py`: domain_groups dicts contain `"verdict"` key
    - `uw_questions.py`: return dict contains `"section_verdict"` key
    - `uw_questions.html.j2`: contains `domain.verdict` for domain badge
    - `uw_questions.html.j2`: contains `q.source` for SCA badge
    - `uw_questions.html.j2`: contains `@media print` with `print-color-adjust: exact`
  </acceptance_criteria>
  <done>SCA questions integrate inline by domain. Domain verdict badges render. Context ordering fixed. Print CSS added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Underwriting Decision Framework</name>
  <files>output/ORCL/ORCL_worksheet.html</files>
  <action>
    Run ORCL worksheet generation (or re-render from existing state) and open the HTML output for visual inspection of the Underwriting Decision Framework section.
  </action>
  <verify>Human visual inspection of rendered HTML and print output.</verify>
  <done>User confirms: 8 domain groups with verdict badges, SCA questions inline with purple badges, completeness bars, verdict dots, specific numbers in answers, filing references for Needs Review items, and clean PDF print output.</done>
  <what-built>
    Complete Underwriting Decision Framework section with:
    - 55+ questions across 8 domains, each with dedicated answerer
    - SCA-derived questions slotted inline by domain with "SCA Data" badges
    - Domain-level verdict badges (FAVORABLE/UNFAVORABLE/MIXED)
    - Section-level overall assessment
    - Print-ready CSS
  </what-built>
  <how-to-verify>
    1. Run: `cd /Users/gorlin/projects/UW/do-uw && underwrite ORCL --fresh` (or re-render if state exists)
    2. Open ORCL worksheet HTML in browser
    3. Scroll to "Underwriting Decision Framework" section (between Scoring and Meeting Prep)
    4. Verify:
       a. 8 domain groups visible with domain labels
       b. Each domain header has verdict badge (FAVORABLE/UNFAVORABLE/MIXED)
       c. Section header shows overall assessment badge
       d. Completeness bars show answer percentage per domain
       e. Verdict dots: green +, red -, gray =, amber ?
       f. Answers contain specific numbers (not vague assessments)
       g. "Needs Review" items show filing references
       h. SCA questions in Litigation domain with purple "SCA Data" badge
       i. ORCL shows SCA history (REPEAT filer) with settlement data
    5. Print to PDF (Cmd+P) -- verify colors render
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Context ordering: uw_questions call line > forensic/peril/settlement call lines in uw_analysis.py
2. SCA integration: grep "generate_sca_questions" uw_questions.py shows import
3. Verdict badges: grep "domain.verdict" uw_questions.html.j2 shows template reference
4. Print CSS: grep "print-color-adjust" uw_questions.html.j2 shows print rules
5. All tests pass: `uv run pytest tests/render/test_uw_questions.py tests/render/test_sca_questions.py -x -q`
</verification>

<success_criteria>
- SCA questions appear inline within domain groups (not separate section)
- Domain verdict badges visible in browser
- Section verdict badge visible in header
- Print to PDF preserves colors
- ORCL worksheet shows SCA data with repeat filer analysis
- At least 80% of brain questions answered (per QFW-05 success criteria #2)
</success_criteria>

<output>
After completion, create `.planning/phases/148-question-driven-underwriting-section/148-03-SUMMARY.md`
</output>
