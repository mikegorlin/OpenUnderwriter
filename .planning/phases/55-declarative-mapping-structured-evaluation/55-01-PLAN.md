---
phase: 55-declarative-mapping-structured-evaluation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/stages/analyze/declarative_mapper.py
  - src/do_uw/stages/analyze/structured_evaluator.py
  - src/do_uw/brain/field_registry.py
  - src/do_uw/brain/field_registry_functions.py
  - src/do_uw/brain/field_registry.yaml
  - tests/stages/analyze/test_declarative_mapper_v2.py
  - tests/stages/analyze/test_structured_evaluator.py
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - EVAL-01
  - EVAL-02
  - EVAL-03

must_haves:
  truths:
    - "declarative_mapper.resolve_field() resolves DIRECT_LOOKUP fields via dotted-path traversal with SourcedValue auto-unwrap"
    - "declarative_mapper.resolve_field() resolves COMPUTED fields by dispatching to named Python functions with pre-resolved args"
    - "structured_evaluator.evaluate_v2() applies operator-based {op, value} thresholds in order and returns correct SignalResult"
    - "Edge cases handled: None returns SKIPPED, empty list returns 0 for count ops, N/A treated as missing"
  artifacts:
    - path: "src/do_uw/stages/analyze/declarative_mapper.py"
      provides: "Declarative field resolution for V2 signals"
      exports: ["resolve_field", "map_v2_signal"]
    - path: "src/do_uw/stages/analyze/structured_evaluator.py"
      provides: "Operator-based threshold evaluation for V2 signals"
      exports: ["evaluate_v2"]
    - path: "src/do_uw/brain/field_registry_functions.py"
      provides: "COMPUTED function implementations"
      exports: ["COMPUTED_FUNCTIONS"]
    - path: "src/do_uw/brain/field_registry.yaml"
      provides: "Field registry with cash_ratio + cash_burn_months entries for FIN.LIQ"
      contains: "cash_ratio"
  key_links:
    - from: "src/do_uw/stages/analyze/declarative_mapper.py"
      to: "src/do_uw/brain/field_registry.py"
      via: "get_field_entry() lookup"
      pattern: "get_field_entry"
    - from: "src/do_uw/stages/analyze/declarative_mapper.py"
      to: "src/do_uw/brain/field_registry_functions.py"
      via: "COMPUTED_FUNCTIONS dict dispatch"
      pattern: "COMPUTED_FUNCTIONS"
    - from: "src/do_uw/stages/analyze/structured_evaluator.py"
      to: "src/do_uw/stages/analyze/signal_helpers.py"
      via: "Reuses coerce_value, extract_factors, make_skipped"
      pattern: "from do_uw.stages.analyze.signal_helpers import"
---

<objective>
Build the two core V2 modules: `declarative_mapper.py` (resolves data from field registry paths with SourcedValue-aware traversal) and `structured_evaluator.py` (evaluates using `{op, value}` operators). Also expand the field registry with entries needed for FIN.LIQ migration and implement COMPUTED functions.

Purpose: These modules replace the legacy pattern of building 30-field dicts then narrowing + parsing English threshold text with regex. V2 resolves exactly the field needed and compares structured operators directly.

Output: Two new production modules, one COMPUTED functions module, expanded field registry, comprehensive unit tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/55-declarative-mapping-structured-evaluation/55-RESEARCH.md
@.planning/phases/55-declarative-mapping-structured-evaluation/55-CONTEXT.md

@src/do_uw/brain/field_registry.py
@src/do_uw/brain/field_registry.yaml
@src/do_uw/brain/brain_signal_schema.py
@src/do_uw/stages/analyze/signal_helpers.py
@src/do_uw/stages/analyze/signal_results.py
@src/do_uw/stages/analyze/signal_mappers.py

<interfaces>
From src/do_uw/brain/field_registry.py:
```python
class FieldRegistryEntry(BaseModel):
    type: Literal["DIRECT_LOOKUP", "COMPUTED"]
    path: str | None = None
    key: str | None = None       # Sub-key for dict-valued SourcedValue fields
    function: str | None = None
    args: list[str] = Field(default_factory=list)
    description: str = ""

def get_field_entry(field_key: str) -> FieldRegistryEntry | None: ...
def load_field_registry(*, path: Path | None = None) -> FieldRegistry: ...
```

From src/do_uw/stages/analyze/signal_helpers.py:
```python
def coerce_value(data_value: Any) -> str | float | None: ...
def extract_factors(check: dict[str, Any]) -> list[str]: ...
def make_skipped(check: dict[str, Any], data: dict[str, Any], needs_calibration: bool = False) -> SignalResult: ...
def first_data_value(data: dict[str, Any]) -> tuple[Any, str]: ...
```

From src/do_uw/stages/analyze/signal_results.py:
```python
class SignalStatus(str, Enum):
    TRIGGERED = "TRIGGERED"
    CLEAR = "CLEAR"
    SKIPPED = "SKIPPED"
    INFO = "INFO"

class SignalResult(BaseModel):
    signal_id: str
    signal_name: str
    status: SignalStatus
    value: str | float | None = None
    threshold_level: str = ""
    evidence: str = ""
    source: str = ""
    confidence: str = ""
    factors: list[str] = []
    section: int = 0
    ...
```

From src/do_uw/brain/brain_signal_schema.py:
```python
class EvaluationThreshold(BaseModel):
    op: Literal["<", ">", "<=", ">=", "==", "!=", "between", "contains"]
    value: float | str | list[float]
    label: Literal["RED", "YELLOW", "CLEAR"]

class EvaluationSpec(BaseModel):
    formula: str | None = None
    thresholds: list[EvaluationThreshold] = []
    window_years: int | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build declarative_mapper.py with SourcedValue-aware path resolution and COMPUTED dispatch</name>
  <files>
    src/do_uw/stages/analyze/declarative_mapper.py
    src/do_uw/brain/field_registry_functions.py
    src/do_uw/brain/field_registry.py
    src/do_uw/brain/field_registry.yaml
    tests/stages/analyze/test_declarative_mapper_v2.py
    tests/brain/test_field_registry.py
  </files>
  <behavior>
    - resolve_field("current_ratio", extracted, company) returns (1.48, "SEC-10Q-...", "HIGH") for DIRECT_LOOKUP with SourcedValue[dict] unwrap + key extraction
    - resolve_field("decline_from_high", extracted, company) returns (15.3, source, confidence) for DIRECT_LOOKUP with SourcedValue[float] unwrap (no key)
    - resolve_field("pe_ratio", extracted, company) returns (25.0, "", "") for DIRECT_LOOKUP with plain float (no SourcedValue wrapper)
    - resolve_field("market_cap", extracted, company) returns value from company.* root (not extracted.*)
    - resolve_field("board_independence", extracted, company) returns computed percentage via COMPUTED_FUNCTIONS dispatch
    - resolve_field("count_items_field", extracted, company) dispatches to count_items lambda, returns len(list)
    - resolve_field("nonexistent_key", ...) returns (None, "", "")
    - resolve_path("extracted.financials.liquidity", ...) with None at intermediate step returns (None, "", "")
    - map_v2_signal(sig, extracted, company) returns dict {field_key: resolved_value} for V2 signal
    - Edge: empty list arg to count_items returns 0
    - Edge: None arg to count_items returns 0
  </behavior>
  <action>
    1. Create `src/do_uw/brain/field_registry_functions.py` (~100 lines):
       - `COMPUTED_FUNCTIONS: dict[str, Callable[..., Any]]` dict mapping function names to callables
       - Include: `count_items` (lambda: len(items) if items else 0), `count_restatements` (same pattern), `count_active_scas` (filter active+non-boilerplate SCAs, return count), `sum_contingent_liabilities` (sum accrued_amount from list), `compute_board_independence_pct` (ratio * 100 if <= 1.0), `resolve_say_on_pay_pct` (comp_analysis preferred, compensation fallback), `compute_customer_concentration` (passthrough or extract top pct), `compute_cash_burn_months` (check OCF sign, return months or "Profitable (OCF positive)" string)
       - Functions receive pre-resolved argument values (not paths). They are pure functions.

    2. Update `src/do_uw/brain/field_registry.py`:
       - Add import of `COMPUTED_FUNCTIONS` from `field_registry_functions`
       - Add `get_computed_function(name: str) -> Callable | None` helper that looks up in COMPUTED_FUNCTIONS
       - Keep file under 160 lines

    3. Add 2 entries to `src/do_uw/brain/field_registry.yaml`:
       - `cash_ratio`: type DIRECT_LOOKUP, path extracted.financials.liquidity, key cash_ratio
       - `cash_burn_months`: type COMPUTED, function compute_cash_burn_months, args [extracted.financials.earnings_quality]

    4. Create `src/do_uw/stages/analyze/declarative_mapper.py` (~180 lines):
       - `resolve_path(dotted_path, extracted, company)` -> tuple[Any, str, str]:
         Traverse dotted path from root object. At each step, check for SourcedValue duck-typing (`hasattr(obj, "value") and hasattr(obj, "source") and hasattr(obj, "confidence")`) — unwrap and capture source/confidence. Do NOT use isinstance(obj, SourcedValue) — it fails at runtime with Pydantic generics.
       - `resolve_field(field_key, extracted, company)` -> tuple[Any, str, str]:
         Look up field_key via `get_field_entry()`. For DIRECT_LOOKUP: call resolve_path, then apply entry.key extraction if dict. For COMPUTED: resolve each arg path via resolve_path, call function from COMPUTED_FUNCTIONS with resolved args.
       - `map_v2_signal(sig, extracted, company)` -> dict[str, Any]:
         Given a signal dict with evaluation.formula, resolve the formula field_key via resolve_field. Return {formula_field_key: resolved_value}. This is the V2 replacement for the legacy map_signal_data() path.
       - Path root dispatch: "extracted" -> ExtractedData, "company" -> CompanyProfile

    5. Create `tests/stages/analyze/test_declarative_mapper_v2.py` with tests for all behaviors listed above. Use mock Pydantic objects (not real state) for fast isolated tests. Test SourcedValue unwrap with a minimal mock that has value/source/confidence attrs.

    6. Expand `tests/brain/test_field_registry.py` to test the 2 new entries load correctly and the COMPUTED_FUNCTIONS dict contains all expected functions.
  </action>
  <verify>
    <automated>uv run pytest tests/stages/analyze/test_declarative_mapper_v2.py tests/brain/test_field_registry.py -x -v</automated>
  </verify>
  <done>
    - declarative_mapper.py resolves DIRECT_LOOKUP (with SourcedValue unwrap + key extraction) and COMPUTED fields
    - field_registry_functions.py has COMPUTED_FUNCTIONS dict with all needed functions
    - field_registry.yaml has cash_ratio and cash_burn_months entries (17 total fields)
    - All tests pass including SourcedValue traversal edge cases
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build structured_evaluator.py with operator-based threshold evaluation and edge case handling</name>
  <files>
    src/do_uw/stages/analyze/structured_evaluator.py
    tests/stages/analyze/test_structured_evaluator.py
  </files>
  <behavior>
    - evaluate_v2(1.48, thresholds=[{op:"<",value:1.0,label:"RED"},{op:"<",value:1.5,label:"YELLOW"}], sig) -> TRIGGERED/yellow
    - evaluate_v2(0.5, same_thresholds, sig) -> TRIGGERED/red (first match wins, RED checked first)
    - evaluate_v2(2.0, same_thresholds, sig) -> CLEAR/clear (no threshold matches)
    - evaluate_v2(None, any_thresholds, sig) -> SKIPPED (None -> SKIPPED per EVAL-03)
    - evaluate_v2("N/A", any_thresholds, sig) -> SKIPPED (N/A treated as missing per EVAL-03)
    - evaluate_v2("", any_thresholds, sig) -> SKIPPED (empty string -> missing)
    - evaluate_v2(0, thresholds=[{op:"==",value:0}], sig) -> TRIGGERED (zero is valid numeric)
    - evaluate_v2(5.0, thresholds=[{op:"between",value:[2.0,8.0]}], sig) -> TRIGGERED (between inclusive)
    - evaluate_v2("Profitable (OCF positive)", thresholds, sig) with clear_conditions -> CLEAR (qualitative clear)
    - evaluate_v2 with empty thresholds list returns CLEAR (no triggers to match)
    - Threshold labels normalized to lowercase: "RED" -> "red", "YELLOW" -> "yellow", "CLEAR" -> "clear"
    - Result includes evidence string describing the comparison
    - _compare handles all 8 operators: <, >, <=, >=, ==, !=, between, contains
  </behavior>
  <action>
    1. Create `src/do_uw/stages/analyze/structured_evaluator.py` (~200 lines):
       - `evaluate_v2(value, evaluation_spec, signal_id, signal_name, sig)` -> SignalResult:
         - If value is None or "N/A" or "" -> return make_skipped(sig, {})
         - Check for `clear_conditions` in evaluation_spec (optional list of dicts with `type`, `pattern`, `result`). If value is a non-numeric string and matches a clear_condition pattern (substring match), return CLEAR with evidence. This handles the cash_burn "Profitable" case per Pitfall 3 in research.
         - Try float(value). If fails and not handled by clear_conditions -> return make_skipped (non-numeric without clear_condition = can't evaluate)
         - Iterate thresholds in order (RED first per convention). For each, call `_compare(numeric_val, op, threshold_value)`. First match returns TRIGGERED with that threshold's label (lowercased).
         - No match -> CLEAR with evidence "Value {val} within thresholds"
       - `_compare(value, op, threshold)` -> bool: Support all 8 operators. `between` checks threshold[0] <= value <= threshold[1]. `contains` checks str(threshold) in str(value).
       - Result construction: Use SignalResult with signal_id, signal_name from params. Use `coerce_value()` for value. Use `extract_factors()` for factors. Set section from sig.

    2. Create `tests/stages/analyze/test_structured_evaluator.py` with:
       - TestOperatorComparison: all 8 operators with edge values
       - TestThresholdEvaluation: RED-first ordering, CLEAR when no match, empty thresholds
       - TestEdgeCases: None, N/A, empty string, empty list (0 for counts), boolean coercion
       - TestQualitativeClear: cash_burn "Profitable" pattern match
       - TestLabelNormalization: uppercase to lowercase conversion
       - TestEvidenceStrings: verify evidence includes the comparison details
  </action>
  <verify>
    <automated>uv run pytest tests/stages/analyze/test_structured_evaluator.py -x -v</automated>
  </verify>
  <done>
    - structured_evaluator.py evaluates V2 signals with structured {op, value} operators
    - All 8 comparison operators work correctly
    - Edge cases: None->SKIPPED, N/A->SKIPPED, empty->SKIPPED, qualitative clear handled
    - Threshold labels normalized to lowercase
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/stages/analyze/test_declarative_mapper_v2.py tests/stages/analyze/test_structured_evaluator.py tests/brain/test_field_registry.py -x -v` -- all pass
- `uv run pytest tests/ -x --timeout=60` -- full suite green (no regression)
- `uv run python -c "from do_uw.stages.analyze.declarative_mapper import resolve_field, map_v2_signal; print('OK')"` -- imports work
- `uv run python -c "from do_uw.stages.analyze.structured_evaluator import evaluate_v2; print('OK')"` -- imports work
- `uv run python -c "from do_uw.brain.field_registry import load_field_registry; r = load_field_registry(); assert len(r.fields) >= 17; print(f'{len(r.fields)} fields')"` -- registry has 17+ entries
</verification>

<success_criteria>
1. declarative_mapper.py resolves DIRECT_LOOKUP fields via dotted-path traversal with SourcedValue auto-unwrap and key extraction
2. declarative_mapper.py resolves COMPUTED fields via COMPUTED_FUNCTIONS dispatch with pre-resolved args
3. structured_evaluator.py evaluates using {op, value} thresholds with correct operator dispatch
4. Edge cases handled: None->SKIPPED, empty list->0, N/A->missing, qualitative clear conditions
5. field_registry.yaml has 17+ entries including cash_ratio and cash_burn_months
6. Full test suite green (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/55-declarative-mapping-structured-evaluation/55-01-SUMMARY.md`
</output>
