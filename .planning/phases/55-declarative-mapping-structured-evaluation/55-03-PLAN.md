---
phase: 55-declarative-mapping-structured-evaluation
plan: 03
type: execute
wave: 3
depends_on: ["55-02"]
files_modified:
  - src/do_uw/brain/signals/fin/balance.yaml
  - src/do_uw/stages/analyze/signal_field_routing.py
  - src/do_uw/cli_brain.py
  - tests/brain/test_v2_migration.py
autonomous: true
requirements:
  - MAP-04

must_haves:
  truths:
    - "All 5 FIN.LIQ signals (position, working_capital, efficiency, trend, cash_burn) have V2 YAML with schema_version: 2, acquisition, evaluation, and presentation sections"
    - "FIN.LIQ signals evaluate via V2 declarative mapper + structured evaluator with shadow comparison against legacy"
    - "Shadow evaluation shows zero discrepancies for FIN.LIQ signals across a test run (3 tickers if possible, at minimum confirmed via unit test fixtures)"
    - "FIELD_FOR_CHECK entries for FIN.LIQ.* (5 entries) removed after shadow evaluation confirms parity"
    - "brain status command shows shadow evaluation summary (match count, discrepancy count)"
    - "Rollback mechanism verified: setting schema_version back to 1 causes signal to use legacy path"
  artifacts:
    - path: "src/do_uw/brain/signals/fin/balance.yaml"
      provides: "V2 YAML for all 5 FIN.LIQ signals"
      contains: "schema_version: 2"
    - path: "src/do_uw/stages/analyze/signal_field_routing.py"
      provides: "FIELD_FOR_CHECK with FIN.LIQ entries removed"
    - path: "src/do_uw/cli_brain.py"
      provides: "brain status with V2 migration stats and shadow eval summary"
      contains: "shadow"
    - path: "tests/brain/test_v2_migration.py"
      provides: "FIN.LIQ migration regression tests"
  key_links:
    - from: "src/do_uw/brain/signals/fin/balance.yaml"
      to: "src/do_uw/stages/analyze/declarative_mapper.py"
      via: "V2 evaluation.formula field references field_registry entries"
      pattern: "formula: current_ratio"
    - from: "src/do_uw/stages/analyze/signal_field_routing.py"
      to: "src/do_uw/brain/signals/fin/balance.yaml"
      via: "FIELD_FOR_CHECK entries removed because V2 handles FIN.LIQ mapping"
      pattern: "FIN.LIQ"
---

<objective>
Migrate the FIN.LIQ prefix (5 signals) end-to-end: add V2 YAML to all 5 signals, verify shadow evaluation parity, remove FIELD_FOR_CHECK entries, and add migration stats to brain status command. This is the proof that the declarative mapper + structured evaluator work correctly on real signals.

Purpose: MAP-04 requires at least one full prefix migrated with declarative mapping exclusively, FIELD_FOR_CHECK entries removed, and regression tests confirming identical results. This plan delivers that milestone.

Output: 5 V2-migrated signals, cleaned FIELD_FOR_CHECK, brain status with shadow stats, regression tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/55-declarative-mapping-structured-evaluation/55-RESEARCH.md
@.planning/phases/55-declarative-mapping-structured-evaluation/55-CONTEXT.md
@.planning/phases/55-declarative-mapping-structured-evaluation/55-01-SUMMARY.md
@.planning/phases/55-declarative-mapping-structured-evaluation/55-02-SUMMARY.md

@src/do_uw/brain/signals/fin/balance.yaml
@src/do_uw/stages/analyze/signal_field_routing.py
@src/do_uw/cli_brain.py
@tests/brain/test_v2_migration.py

<interfaces>
FIN.LIQ signals to migrate (from balance.yaml):
```
FIN.LIQ.position   -> formula: current_ratio    (already V2, needs no YAML change)
FIN.LIQ.working_capital -> formula: current_ratio    (same field, different thresholds)
FIN.LIQ.efficiency  -> formula: cash_ratio       (new field registry entry from Plan 01)
FIN.LIQ.trend       -> formula: current_ratio    (qualitative thresholds — trend detection)
FIN.LIQ.cash_burn   -> formula: cash_burn_months (COMPUTED, qualitative clear for profitable)
```

FIELD_FOR_CHECK entries to remove (line 161-166 of signal_field_routing.py):
```python
"FIN.LIQ.position": "current_ratio",
"FIN.LIQ.working_capital": "current_ratio",
"FIN.LIQ.efficiency": "cash_ratio",
"FIN.LIQ.trend": "current_ratio",
"FIN.LIQ.cash_burn": "cash_burn_months",
```

V2 YAML structure (from FIN.LIQ.position as reference):
```yaml
schema_version: 2
acquisition:
  sources:
    - type: SEC_10Q
      fields: [extracted.financials.liquidity]
      fallback_to: SEC_10K
    - type: SEC_10K
      fields: [extracted.financials.liquidity]
evaluation:
  formula: current_ratio
  thresholds:
    - op: "<"
      value: 1.0
      label: RED
    - op: "<"
      value: 1.5
      label: YELLOW
  window_years: 1
presentation:
  detail_levels:
    - level: glance
      template: "Current ratio: {value}"
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add V2 YAML sections to remaining 4 FIN.LIQ signals in balance.yaml</name>
  <files>
    src/do_uw/brain/signals/fin/balance.yaml
  </files>
  <action>
    Add V2 sections (schema_version, acquisition, evaluation, presentation) to the 4 remaining FIN.LIQ signals. FIN.LIQ.position already has V2 from Phase 54. Use the Edit tool for targeted additions at the end of each signal's existing YAML block.

    **FIN.LIQ.working_capital** (uses same field as position — current_ratio):
    ```yaml
    schema_version: 2
    acquisition:
      sources:
        - type: SEC_10Q
          fields:
            - extracted.financials.liquidity
          fallback_to: SEC_10K
        - type: SEC_10K
          fields:
            - extracted.financials.liquidity
    evaluation:
      formula: current_ratio
      thresholds:
        - op: "<"
          value: 1.0
          label: RED
        - op: "<"
          value: 1.5
          label: YELLOW
      window_years: 1
    presentation:
      detail_levels:
        - level: glance
          template: "Working capital: {value}"
        - level: standard
          template: "Working capital adequacy ({current_ratio}x) — {status_label}"
      context_templates:
        TRIGGERED: "Working capital concern: current ratio {value} below adequate levels"
        CLEAR: "Working capital adequate with current ratio of {value}"
    ```

    **FIN.LIQ.efficiency** (uses cash_ratio — new field registry entry):
    ```yaml
    schema_version: 2
    acquisition:
      sources:
        - type: SEC_10Q
          fields:
            - extracted.financials.liquidity
          fallback_to: SEC_10K
        - type: SEC_10K
          fields:
            - extracted.financials.liquidity
    evaluation:
      formula: cash_ratio
      thresholds:
        - op: "<"
          value: 0.2
          label: RED
        - op: "<"
          value: 0.5
          label: YELLOW
      window_years: 1
    presentation:
      detail_levels:
        - level: glance
          template: "Cash ratio: {value}"
        - level: standard
          template: "Liquidity efficiency ({cash_ratio}x) — {status_label}"
      context_templates:
        TRIGGERED: "Liquidity efficiency concern: cash ratio {value}"
        CLEAR: "Adequate liquidity efficiency with cash ratio of {value}"
    ```

    **FIN.LIQ.trend** (uses current_ratio — qualitative thresholds):
    NOTE: The legacy thresholds for trend are qualitative ("DETERIORATING: 3+ quarters..."). The V2 structured evaluator handles this via numeric comparison on current_ratio. Since the legacy evaluator uses the same current_ratio value and applies regex-based comparison (`<1.0` RED, `<1.5` YELLOW from the legacy tiered path), the V2 structured thresholds should mirror the effective behavior:
    ```yaml
    schema_version: 2
    acquisition:
      sources:
        - type: SEC_10Q
          fields:
            - extracted.financials.liquidity
          fallback_to: SEC_10K
        - type: SEC_10K
          fields:
            - extracted.financials.liquidity
    evaluation:
      formula: current_ratio
      thresholds:
        - op: "<"
          value: 1.0
          label: RED
        - op: "<"
          value: 1.5
          label: YELLOW
      window_years: 1
    presentation:
      detail_levels:
        - level: glance
          template: "Liquidity trend: {value}"
        - level: standard
          template: "Liquidity trend ({current_ratio}x) — {status_label}"
      context_templates:
        TRIGGERED: "Liquidity trend concern: current ratio {value}"
        CLEAR: "Stable liquidity trend with current ratio {value}"
    ```
    NOTE on FIN.LIQ.trend: The legacy English thresholds say "DETERIORATING: 3+ quarters declining" but the actual legacy evaluation uses try_numeric_compare on the current_ratio value, NOT pattern detection. So the V2 thresholds matching current_ratio numeric comparison IS parity. If shadow evaluation reveals a discrepancy, the thresholds can be adjusted.

    **FIN.LIQ.cash_burn** (COMPUTED field — qualitative clear for profitable):
    ```yaml
    schema_version: 2
    acquisition:
      sources:
        - type: SEC_10Q
          fields:
            - extracted.financials.earnings_quality
          fallback_to: SEC_10K
        - type: SEC_10K
          fields:
            - extracted.financials.earnings_quality
    evaluation:
      formula: cash_burn_months
      thresholds:
        - op: "<"
          value: 12
          label: RED
        - op: "<"
          value: 18
          label: YELLOW
      clear_conditions:
        - type: qualitative_value
          pattern: "Profitable"
          result: CLEAR
      window_years: 1
    presentation:
      detail_levels:
        - level: glance
          template: "Cash runway: {value}"
        - level: standard
          template: "Cash burn ({cash_burn_months} months) — {status_label}"
      context_templates:
        TRIGGERED: "Cash runway concern: {value} months remaining"
        CLEAR: "Company is cash flow positive"
    ```

    IMPORTANT: Use the Edit tool for each signal. Add V2 sections AFTER the existing `display:` block (before the next signal's `- id:` line). Match the existing indentation (2-space indent, 4-space for nested under signal).
  </action>
  <verify>
    <automated>uv run python -c "
from do_uw.brain.brain_unified_loader import load_signals
signals = load_signals()['signals']
liq = [s for s in signals if s['id'].startswith('FIN.LIQ.')]
v2 = [s for s in liq if s.get('schema_version', 1) >= 2]
print(f'FIN.LIQ signals: {len(liq)}, V2: {len(v2)}')
assert len(v2) == 5, f'Expected 5 V2 FIN.LIQ signals, got {len(v2)}'
for s in v2:
    assert 'evaluation' in s, f'{s[\"id\"]} missing evaluation'
    assert 'thresholds' in s['evaluation'], f'{s[\"id\"]} missing thresholds'
print('All 5 FIN.LIQ signals have V2 sections')
"</automated>
  </verify>
  <done>
    - All 5 FIN.LIQ signals have schema_version: 2 with acquisition, evaluation, and presentation sections
    - YAML loads and validates via Pydantic BrainSignalEntry
    - cash_burn has clear_conditions for qualitative "Profitable" case
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove FIN.LIQ FIELD_FOR_CHECK entries, add brain status shadow stats, and create migration regression tests</name>
  <files>
    src/do_uw/stages/analyze/signal_field_routing.py
    src/do_uw/cli_brain.py
    tests/brain/test_v2_migration.py
  </files>
  <behavior>
    - FIELD_FOR_CHECK no longer contains any "FIN.LIQ.*" entries (5 entries removed)
    - brain status shows: V2 signal count, shadow evaluation match count/total from DuckDB, top discrepant signals (if any)
    - Regression test: FIN.LIQ.position with current_ratio=0.8 evaluates to TRIGGERED/red via V2 path
    - Regression test: FIN.LIQ.position with current_ratio=1.2 evaluates to TRIGGERED/yellow via V2 path
    - Regression test: FIN.LIQ.position with current_ratio=2.0 evaluates to CLEAR via V2 path
    - Regression test: FIN.LIQ.efficiency with cash_ratio=0.1 evaluates to TRIGGERED/red via V2 path
    - Regression test: FIN.LIQ.cash_burn with "Profitable (OCF positive)" evaluates to CLEAR via V2 path (qualitative clear)
    - Regression test: FIN.LIQ.cash_burn with cash_burn_months=6 evaluates to TRIGGERED/red via V2 path
    - Rollback test: setting schema_version=1 on a FIN.LIQ signal causes _evaluate_v2_signal to return None (falls through to legacy)
  </behavior>
  <action>
    1. Remove FIN.LIQ FIELD_FOR_CHECK entries from `src/do_uw/stages/analyze/signal_field_routing.py`:
       - Delete lines containing "FIN.LIQ.position", "FIN.LIQ.working_capital", "FIN.LIQ.efficiency", "FIN.LIQ.trend", "FIN.LIQ.cash_burn"
       - Leave the "# FIN.LIQ" comment header as a tombstone: `# FIN.LIQ — migrated to V2 declarative mapping (Phase 55)`
       - This is safe because shadow evaluation (Plan 02) runs both paths — any regression is caught immediately

    2. Add shadow evaluation summary to `src/do_uw/cli_brain.py` `status()` command:
       - After the existing V2 signal count display (line ~89-91), add a shadow evaluation summary section:
       - Query `brain_shadow_evaluations` table for: total records, match count (is_match=TRUE), mismatch count (is_match=FALSE)
       - Display: "Shadow evaluations: {total} (match: {match_count}, mismatch: {mismatch_count})"
       - If any mismatches: show top 5 most discrepant signal_ids with their discrepancy counts
       - Wrap in try/except (table may not exist on first run)

    3. Expand `tests/brain/test_v2_migration.py` with FIN.LIQ regression tests:
       - Create test class `TestFINLIQMigration` with tests using mock ExtractedData objects and the real V2 signal definitions from balance.yaml
       - Use `load_signals()` to get real signal defs, filter to FIN.LIQ
       - For each test, construct mock extracted data with the relevant field values, call `map_v2_signal()` to get data, then `evaluate_v2()` to get result
       - Verify status and threshold_level match expected outcomes
       - Include rollback test: clone a V2 signal dict, set schema_version=1, verify _evaluate_v2_signal returns None

    4. Verify FIELD_FOR_CHECK removal:
       - `grep "FIN.LIQ" src/do_uw/stages/analyze/signal_field_routing.py` should show only the tombstone comment
  </action>
  <verify>
    <automated>uv run pytest tests/brain/test_v2_migration.py -x -v && uv run python -c "
from do_uw.stages.analyze.signal_field_routing import FIELD_FOR_CHECK
liq_entries = [k for k in FIELD_FOR_CHECK if k.startswith('FIN.LIQ')]
assert len(liq_entries) == 0, f'FIN.LIQ entries still in FIELD_FOR_CHECK: {liq_entries}'
print('FIELD_FOR_CHECK: 0 FIN.LIQ entries (migrated to V2)')
"</automated>
  </verify>
  <done>
    - FIN.LIQ.* entries removed from FIELD_FOR_CHECK (5 entries)
    - brain status shows shadow evaluation summary (match/mismatch counts)
    - Regression tests confirm FIN.LIQ signals evaluate correctly via V2 path
    - Rollback mechanism verified: schema_version=1 falls through to legacy
    - Full test suite green
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/brain/test_v2_migration.py tests/stages/analyze/test_shadow_evaluation.py tests/stages/analyze/test_structured_evaluator.py tests/stages/analyze/test_declarative_mapper_v2.py -x -v` -- all Phase 55 tests pass
- `uv run pytest tests/ -x --timeout=60` -- full suite green
- `uv run python -c "from do_uw.stages.analyze.signal_field_routing import FIELD_FOR_CHECK; assert not any(k.startswith('FIN.LIQ') for k in FIELD_FOR_CHECK); print('OK')"` -- FIN.LIQ entries removed
- `uv run python -c "
from do_uw.brain.brain_unified_loader import load_signals
signals = load_signals()['signals']
v2 = [s for s in signals if s.get('schema_version', 1) >= 2]
print(f'V2 signals: {len(v2)} of {len(signals)}')
liq_v2 = [s for s in v2 if s['id'].startswith('FIN.LIQ.')]
print(f'FIN.LIQ V2: {len(liq_v2)}/5')
assert len(liq_v2) == 5
"` -- all 5 FIN.LIQ signals are V2
- `uv run python -m do_uw.cli brain status` -- shows V2 count and shadow eval summary
</verification>

<success_criteria>
1. All 5 FIN.LIQ signals have schema_version: 2 with acquisition, evaluation, and presentation sections
2. FIN.LIQ signals evaluate via V2 declarative mapper + structured evaluator
3. Shadow evaluation confirms parity (verified by regression tests with fixture data)
4. FIELD_FOR_CHECK entries for FIN.LIQ.* removed (5 entries)
5. brain status shows V2 migration progress and shadow evaluation summary
6. Rollback mechanism works: schema_version=1 falls through to legacy
7. Full test suite green (zero regression)
</success_criteria>

<output>
After completion, create `.planning/phases/55-declarative-mapping-structured-evaluation/55-03-SUMMARY.md`
</output>
