---
phase: 113-context-builder-rewrites
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/stages/render/context_builders/scoring_evaluative.py
  - tests/stages/render/test_signal_consumption.py
autonomous: true
gap_closure: true
requirements: [BUILD-06, BUILD-07]

must_haves:
  truths:
    - "scoring_evaluative.py has no dead signal imports -- unused imports removed, rationale documented in module docstring"
    - "test_signal_consumption.py validates that delegation via *_evaluative.py satisfies signal consumption for primary builders"
  artifacts:
    - path: "src/do_uw/stages/render/context_builders/scoring_evaluative.py"
      provides: "Clean evaluative builder with no dead imports and documented rationale for state-driven approach"
      contains: "scoring results are post-signal computed artifacts"
    - path: "tests/stages/render/test_signal_consumption.py"
      provides: "Updated test covering delegation pattern validation"
      contains: "DELEGATION_BUILDERS"
  key_links:
    - from: "test_signal_consumption.py"
      to: "scoring_evaluative.py"
      via: "SIGNAL_CONSUMING_BUILDERS list excludes scoring_evaluative; DELEGATION_BUILDERS validates import chain"
      pattern: "DELEGATION_BUILDERS"
---

<objective>
Close 2 verification gaps from Phase 113 VERIFICATION.md:

1. **scoring_evaluative.py dead imports** -- Remove unused safe_get_result/safe_get_signals_by_prefix imports and document WHY scoring evaluative content (AI risk, meeting questions, allegation mapping, tower recommendations, severity scenarios) is correctly state-driven: these are post-signal computed artifacts from the scoring pipeline, not raw evaluative judgments. No brain signals exist for AI risk scoring or meeting question generation.

2. **Primary builders missing signal imports** -- The delegation pattern (financials.py delegates to financials_evaluative.py) is architecturally correct. Update test_signal_consumption.py to validate this delegation chain: primary builders that import from *_evaluative.py companions satisfy BUILD-07 via delegation.

Purpose: Achieve clean verification on BUILD-06 and BUILD-07 requirements.
Output: Updated scoring_evaluative.py (clean imports) + updated test_signal_consumption.py (delegation validation)
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-context-builder-rewrites/113-VERIFICATION.md
@src/do_uw/stages/render/context_builders/scoring_evaluative.py
@tests/stages/render/test_signal_consumption.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean scoring_evaluative.py dead imports and document rationale</name>
  <files>src/do_uw/stages/render/context_builders/scoring_evaluative.py</files>
  <action>
1. Remove the unused imports on lines 14-17:
   ```python
   from do_uw.stages.render.context_builders._signal_fallback import (
       safe_get_result,
       safe_get_signals_by_prefix,
   )
   ```

2. Update the module docstring (line 1-4) to document WHY scoring evaluative content does NOT consume brain signals:
   ```
   """Evaluative scoring context builders -- AI risk, meeting questions, and
   scoring helper functions for allegation mapping and tower recommendations.

   These functions extract from post-signal computed artifacts (ScoringResult,
   AllegationMapping, TowerRecommendation, SeverityScenarios, AIRiskAssessment)
   rather than from brain signal results directly. This is correct because:
   - Allegation mapping, tower recommendations, and severity scenarios are
     outputs of the scoring pipeline that already consumed signal results
   - AI risk assessment is computed from 10-K disclosure analysis, not from
     individual brain signals (no AI-risk signal IDs exist in brain/signals/)
   - Meeting questions are generated from analysis findings, not signal evaluations

   The signal_results parameter on extract_ai_risk() and extract_meeting_questions()
   is retained for interface consistency but these functions correctly read from
   state.extracted and state.analysis (post-signal artifacts).

   Extracted from scoring.py (Phase 113-04) to meet the <300 line BUILD-07 limit.
   """
   ```

3. Remove `scoring_evaluative.py` from the SIGNAL_CONSUMING_BUILDERS list in test_signal_consumption.py (handled in Task 2).

4. Verify: `uv run pytest tests/stages/render/ -x -q -k "scoring"` passes.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/render/ -x -q -k "scoring" && uv run ruff check src/do_uw/stages/render/context_builders/scoring_evaluative.py</automated>
  </verify>
  <done>scoring_evaluative.py has no dead imports, module docstring documents rationale for state-driven approach, ruff lint passes</done>
</task>

<task type="auto">
  <name>Task 2: Update test_signal_consumption.py with delegation pattern validation</name>
  <files>tests/stages/render/test_signal_consumption.py</files>
  <action>
1. Remove `scoring_evaluative.py` from SIGNAL_CONSUMING_BUILDERS (it no longer imports signal functions after Task 1).

2. Add a new DELEGATION_BUILDERS dict mapping primary builders to the companion *_evaluative.py modules they delegate to:
   ```python
   DELEGATION_BUILDERS: dict[str, list[str]] = {
       "financials.py": ["financials_evaluative.py"],
       "market.py": ["market_evaluative.py"],
       "analysis.py": ["analysis_evaluative.py"],
       "scoring.py": ["scoring_evaluative.py"],
       "narrative.py": ["narrative_evaluative.py"],
   }
   ```

3. Add a parametrized test `test_delegation_builder_imports_evaluative` that verifies each primary builder imports from its companion *_evaluative.py module:
   - Read the primary builder source
   - Assert it contains an import from the companion module name (e.g., `from do_uw.stages.render.context_builders.financials_evaluative import` or `from do_uw.stages.render.context_builders.analysis_evaluative import`)
   - This validates the delegation chain: primary builder -> evaluative module -> signal functions

4. Add a parametrized test `test_delegation_companion_consumes_signals` that verifies each companion *_evaluative.py module (listed in DELEGATION_BUILDERS values) imports signal functions:
   - Read the companion source
   - Assert it contains at least one function from _SIGNAL_FUNCTIONS
   - Exception: scoring_evaluative.py is allowed to NOT import signal functions (documented rationale)

5. Verify: `uv run pytest tests/stages/render/test_signal_consumption.py -v` passes all tests.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/render/test_signal_consumption.py -v</automated>
  </verify>
  <done>test_signal_consumption.py validates both direct signal consumption (SIGNAL_CONSUMING_BUILDERS) and delegation chain (DELEGATION_BUILDERS -> companion modules), all tests pass</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/stages/render/ -x -q` -- all render tests pass (no regressions)
2. `grep -c "safe_get_result\|safe_get_signals_by_prefix" src/do_uw/stages/render/context_builders/scoring_evaluative.py` returns 0 (dead imports removed)
3. `uv run pytest tests/stages/render/test_signal_consumption.py -v` -- both direct and delegation tests pass
4. `uv run ruff check src/do_uw/stages/render/context_builders/scoring_evaluative.py` -- clean
</verification>

<success_criteria>
- scoring_evaluative.py has zero unused signal imports and documents why state-driven is correct
- test_signal_consumption.py validates delegation pattern for 5 primary builders
- All existing render tests pass (no regressions)
- Both verification gaps from 113-VERIFICATION.md are closed
</success_criteria>

<output>
After completion, create `.planning/phases/113-context-builder-rewrites/113-05-SUMMARY.md`
</output>
