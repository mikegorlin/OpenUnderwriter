---
phase: 113-context-builder-rewrites
plan: 03
type: execute
wave: 2
depends_on: [113-01]
files_modified:
  - src/do_uw/stages/render/context_builders/governance.py
  - src/do_uw/stages/render/context_builders/governance_evaluative.py
  - src/do_uw/stages/render/context_builders/litigation.py
  - src/do_uw/stages/render/context_builders/litigation_evaluative.py
autonomous: true
requirements: [BUILD-04, BUILD-05]

must_haves:
  truths:
    - "Governance evaluative content (board quality, compensation flags, structural governance, narrative coherence) sourced from GOV.* signal results"
    - "Litigation evaluative content (defense strength, SEC enforcement, SoL urgency, reserve adequacy) sourced from LIT.* signal results"
    - "Display data (director lists, holder tables, case lists, settlement amounts, transaction details) remains as direct state reads"
    - "All existing render tests pass (no template key changes)"
    - "Each module under 300 lines"
    - "Both primary builder files (governance.py, litigation.py) import from _signal_consumer or _signal_fallback"
  artifacts:
    - path: "src/do_uw/stages/render/context_builders/governance_evaluative.py"
      provides: "Signal-backed board quality, comp flags, structural governance evaluations"
      min_lines: 50
    - path: "src/do_uw/stages/render/context_builders/litigation_evaluative.py"
      provides: "Signal-backed defense strength, SEC enforcement, SoL, reserve evaluations"
      min_lines: 50
    - path: "src/do_uw/stages/render/context_builders/governance.py"
      provides: "Rewritten governance builder, <300 lines"
      min_lines: 50
    - path: "src/do_uw/stages/render/context_builders/litigation.py"
      provides: "Rewritten litigation builder, <300 lines"
      min_lines: 50
  key_links:
    - from: "src/do_uw/stages/render/context_builders/governance.py"
      to: "GOV.BOARD.*, GOV.COMP.*, GOV.STRUCTURE.*"
      via: "safe_get_result and safe_get_signals_by_prefix"
      pattern: "safe_get_result.*GOV\\."
    - from: "src/do_uw/stages/render/context_builders/litigation.py"
      to: "LIT.DEFENSE.*, LIT.SEC.*, LIT.SOL.*, LIT.RESERVE.*"
      via: "safe_get_result and safe_get_signals_by_prefix"
      pattern: "safe_get_result.*LIT\\."
---

<objective>
Rewrite governance.py (460 lines) and litigation.py (495 lines) to consume signal results for all evaluative content while preserving display data as direct state reads. Extract evaluative logic into separate modules to keep each file under 300 lines.

Purpose: These two builders cover the governance quality and litigation risk dimensions -- critical D&O underwriting factors. Signal-backing their evaluative content ensures every governance judgment and litigation assessment is traceable.
Output: 4 files (2 rewritten, 2 new evaluative modules), all under 300 lines
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-context-builder-rewrites/113-RESEARCH.md
@.planning/phases/113-context-builder-rewrites/113-01-SUMMARY.md

@src/do_uw/stages/render/context_builders/governance.py
@src/do_uw/stages/render/context_builders/litigation.py
@src/do_uw/stages/render/context_builders/_signal_consumer.py
@src/do_uw/stages/render/context_builders/_signal_fallback.py
@src/do_uw/stages/render/context_builders/_governance_helpers.py

<interfaces>
<!-- Signal consumer API -->

From _signal_fallback.py:
```python
def safe_get_result(results: dict | None, signal_id: str) -> SignalResultView | SignalUnavailable
def safe_get_signals_by_prefix(results: dict | None, prefix: str) -> list[SignalResultView]
```

From _signal_consumer.py:
```python
def signal_to_display_level(status: str, threshold_level: str) -> str
```

<!-- Evaluative vs Display classification from research -->

governance.py evaluative content (must come from signals):
- Board quality score/interpretation (GOV.BOARD.*)
- Compensation flags (GOV.COMP.*)
- Structural governance issues (GOV.STRUCTURE.*)
- Narrative coherence (GOV.NARRATIVE.*)
- Board independence assessment, tenure concerns

governance.py display data (stays direct):
- Directors list (names, titles, tenures, committees)
- Major holders table
- Board departures
- Compensation amounts/tables

litigation.py evaluative content (must come from signals):
- Defense strength rating (LIT.DEFENSE.*)
- SEC enforcement stage assessment (LIT.SEC.*)
- Statute of limitations urgency (LIT.SOL.*)
- Reserve adequacy judgment (LIT.RESERVE.*)
- Litigation risk classification

litigation.py display data (stays direct):
- Case names, filing dates, courts
- Settlement amounts, provisions
- Timeline of events
- Case details and parties
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite governance.py -- extract evaluative to governance_evaluative.py</name>
  <files>
    src/do_uw/stages/render/context_builders/governance.py
    src/do_uw/stages/render/context_builders/governance_evaluative.py
  </files>
  <action>
Extract all evaluative judgment logic from governance.py into governance_evaluative.py. Keep display data (director lists, holders, departures, compensation tables) in governance.py.

**governance_evaluative.py** (<300 lines): Create functions:

1. `_extract_board_quality_signals(signal_results)` -- board quality score, independence ratio assessment, tenure concerns from `GOV.BOARD.*` signals. Returns list of triggered board flags with signal_id, display_level, evidence, threshold_context.

2. `_extract_compensation_signals(signal_results)` -- compensation red flags from `GOV.COMP.*` signals (excessive pay, pay-performance disconnect, golden parachutes). Returns triggered comp flags.

3. `_extract_structural_governance_signals(signal_results)` -- governance structure issues from `GOV.STRUCTURE.*` signals (dual-class stock, poison pills, staggered board). Returns triggered structural flags.

4. `_extract_governance_narrative_signals(signal_results)` -- narrative coherence assessment from `GOV.NARRATIVE.*` signals if any. Returns assessment dict.

Each function uses safe_get_result/safe_get_signals_by_prefix with fallback to empty list when signals unavailable.

**governance.py** (<300 lines): Rewrite `extract_governance(state, signal_results=None)` to:
- Keep display data extraction (directors, holders, departures) as direct state reads
- Call evaluative functions from governance_evaluative.py
- Merge results into SAME context dict structure
- Preserve all existing template keys

**Critical:** governance.py uses `_governance_helpers.py` (145 lines, kept). Ensure imports are preserved. Grep templates for governance context keys before modifying. Use safe_get_result() exclusively. Grep `brain/signals/` for exact GOV.* signal IDs.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && wc -l src/do_uw/stages/render/context_builders/governance.py src/do_uw/stages/render/context_builders/governance_evaluative.py && uv run pytest tests/stages/render/ -x -q -k "governance" --timeout=30 2>&1 | tail -5 && echo "--- signal consumption in governance.py ---" && grep -c "safe_get_result\|safe_get_signals_by_prefix\|_signal_fallback\|_signal_consumer" src/do_uw/stages/render/context_builders/governance.py src/do_uw/stages/render/context_builders/governance_evaluative.py</automated>
  </verify>
  <done>governance.py under 300 lines; governance_evaluative.py under 300 lines; evaluative content from GOV.* signals; governance.py imports from _signal_consumer or _signal_fallback; all governance render tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite litigation.py -- extract evaluative to litigation_evaluative.py</name>
  <files>
    src/do_uw/stages/render/context_builders/litigation.py
    src/do_uw/stages/render/context_builders/litigation_evaluative.py
  </files>
  <action>
Extract all evaluative judgment logic from litigation.py into litigation_evaluative.py. Keep display data (case lists, settlements, provisions, timelines) in litigation.py.

**litigation_evaluative.py** (<300 lines): Create functions:

1. `_extract_defense_signals(signal_results)` -- defense strength rating, defense quality assessment from `LIT.DEFENSE.*` signals. Returns defense strength dict with level, evidence, basis.

2. `_extract_sec_enforcement_signals(signal_results)` -- SEC enforcement stage, investigation status from `LIT.SEC.*` signals. Returns enforcement assessment dict.

3. `_extract_sol_signals(signal_results)` -- statute of limitations urgency, expiry proximity from `LIT.SOL.*` signals. Returns SoL urgency dict.

4. `_extract_reserve_signals(signal_results)` -- reserve adequacy judgment, provision sufficiency from `LIT.RESERVE.*` signals. Returns reserve assessment dict.

5. `_extract_litigation_risk_signals(signal_results)` -- overall litigation risk classification from aggregated `LIT.*` signals. Returns risk classification dict.

Each function uses safe_get_result/safe_get_signals_by_prefix with fallback.

**litigation.py** (<300 lines): Rewrite `extract_litigation(state, signal_results=None)` to:
- Keep display data extraction (cases, settlements, provisions, parties) as direct state reads
- Call evaluative functions from litigation_evaluative.py
- Merge results into SAME context dict structure

**Critical:** Same template-key preservation. Grep templates and `brain/signals/` for exact LIT.* signal IDs. Use safe_get_result() exclusively.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && wc -l src/do_uw/stages/render/context_builders/litigation.py src/do_uw/stages/render/context_builders/litigation_evaluative.py && uv run pytest tests/stages/render/ -x -q -k "litigation" --timeout=30 2>&1 | tail -5 && echo "--- signal consumption in litigation.py ---" && grep -c "safe_get_result\|safe_get_signals_by_prefix\|_signal_fallback\|_signal_consumer" src/do_uw/stages/render/context_builders/litigation.py src/do_uw/stages/render/context_builders/litigation_evaluative.py</automated>
  </verify>
  <done>litigation.py under 300 lines; litigation_evaluative.py under 300 lines; evaluative content from LIT.* signals; litigation.py imports from _signal_consumer or _signal_fallback; all litigation render tests pass</done>
</task>

</tasks>

<verification>
- `wc -l` confirms all 4 files under 300 lines
- `uv run pytest tests/stages/render/ -x -q` passes (all 790+ render tests)
- `grep -rn "safe_get_result\|safe_get_signals_by_prefix" src/do_uw/stages/render/context_builders/governance_evaluative.py` shows signal consumption
- `grep -rn "safe_get_result\|safe_get_signals_by_prefix" src/do_uw/stages/render/context_builders/litigation_evaluative.py` shows signal consumption
- `grep -rn "_signal_fallback\|_signal_consumer" src/do_uw/stages/render/context_builders/governance.py src/do_uw/stages/render/context_builders/litigation.py` confirms primary builders import signal infrastructure
- `uv run pytest tests/stages/render/test_signal_consumption.py -x -q` passes with governance and litigation added to SIGNAL_CONSUMING_BUILDERS
</verification>

<success_criteria>
- governance.py and litigation.py each under 300 lines
- governance_evaluative.py and litigation_evaluative.py each under 300 lines
- All evaluative content sourced from GOV.* and LIT.* signals respectively
- Primary builder files (governance.py, litigation.py) import from _signal_consumer/_signal_fallback
- Display data still direct state reads
- All render tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/113-context-builder-rewrites/113-03-SUMMARY.md`
</output>
