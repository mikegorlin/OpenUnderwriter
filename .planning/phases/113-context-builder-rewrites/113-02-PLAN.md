---
phase: 113-context-builder-rewrites
plan: 02
type: execute
wave: 2
depends_on: [113-01]
files_modified:
  - src/do_uw/stages/render/context_builders/financials.py
  - src/do_uw/stages/render/context_builders/financials_evaluative.py
  - src/do_uw/stages/render/context_builders/market.py
  - src/do_uw/stages/render/context_builders/market_evaluative.py
autonomous: true
requirements: [BUILD-02, BUILD-03]

must_haves:
  truths:
    - "Financial evaluative content (distress zones, earnings quality, leverage, tax risk, liquidity) sourced from FIN.* signal results"
    - "Market evaluative content (volatility regime, short interest assessment, insider verdict, guidance track record) sourced from STOCK.* signal results"
    - "Tabular data (line items, sparklines, statements, stock prices, transactions) remains as direct state reads"
    - "All existing render tests pass (no template key changes)"
    - "Each module under 300 lines"
    - "Both primary builder files (financials.py, market.py) import from _signal_consumer or _signal_fallback"
  artifacts:
    - path: "src/do_uw/stages/render/context_builders/financials_evaluative.py"
      provides: "Signal-backed distress, earnings quality, leverage, tax, liquidity evaluations"
      min_lines: 50
    - path: "src/do_uw/stages/render/context_builders/market_evaluative.py"
      provides: "Signal-backed volatility, short interest, insider, guidance evaluations"
      min_lines: 50
    - path: "src/do_uw/stages/render/context_builders/financials.py"
      provides: "Rewritten to consume signals for evaluative content, <300 lines"
      min_lines: 50
    - path: "src/do_uw/stages/render/context_builders/market.py"
      provides: "Rewritten to consume signals for evaluative content, <300 lines"
      min_lines: 50
  key_links:
    - from: "src/do_uw/stages/render/context_builders/financials.py"
      to: "FIN.DISTRESS.*, FIN.EARNINGS.*, FIN.LEVERAGE.*, FIN.TAX.*, FIN.LIQUIDITY.*"
      via: "safe_get_result and safe_get_signals_by_prefix"
      pattern: "safe_get_result.*FIN\\."
    - from: "src/do_uw/stages/render/context_builders/market.py"
      to: "STOCK.VOLATILITY.*, STOCK.SHORT.*, STOCK.INSIDER.*, FWRD.GUIDANCE.*"
      via: "safe_get_result and safe_get_signals_by_prefix"
      pattern: "safe_get_result.*STOCK\\."
---

<objective>
Rewrite financials.py (626 lines) and market.py (496 lines) to consume signal results for all evaluative content while preserving tabular/display data as direct state reads. Extract evaluative logic into separate modules to keep each file under 300 lines.

Purpose: These two builders cover the financial health and market risk dimensions of the worksheet. Converting their evaluative content to signal-backed removes the largest category of signal bypasses.
Output: 4 files (2 rewritten, 2 new evaluative modules), all under 300 lines
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-context-builder-rewrites/113-RESEARCH.md
@.planning/phases/113-context-builder-rewrites/113-01-SUMMARY.md

@src/do_uw/stages/render/context_builders/financials.py
@src/do_uw/stages/render/context_builders/market.py
@src/do_uw/stages/render/context_builders/_signal_consumer.py
@src/do_uw/stages/render/context_builders/_signal_fallback.py

<interfaces>
<!-- Signal consumer API (from Phase 104) -->

From _signal_fallback.py:
```python
def safe_get_result(results: dict | None, signal_id: str) -> SignalResultView | SignalUnavailable
def safe_get_value(results: dict | None, signal_id: str) -> str | float | None
def safe_get_level(results: dict | None, signal_id: str) -> str
def safe_get_signals_by_prefix(results: dict | None, prefix: str) -> list[SignalResultView]
```

From _signal_consumer.py:
```python
def signal_to_display_level(status: str, threshold_level: str) -> str
```

<!-- Evaluative vs Display classification from research -->

financials.py evaluative content (must come from signals):
- Distress zones: Altman Z (FIN.DISTRESS.altman_z_score), Beneish M (FIN.EARNINGS.beneish_m_score), Ohlson O (FIN.DISTRESS.ohlson_o_score)
- Earnings quality: revenue quality, accrual ratio, cash conversion
- Leverage assessment: debt ratio interpretation, interest coverage adequacy
- Tax risk: effective tax rate flags
- Liquidity: current ratio zone, quick ratio assessment

financials.py display data (stays direct):
- Line item values, sparklines, statement tables, quarterly trends, peer matrix

market.py evaluative content (must come from signals):
- Volatility regime (STOCK.VOLATILITY.*)
- Short interest assessment (STOCK.SHORT.*)
- Insider trading verdict (STOCK.INSIDER.*)
- Guidance track record (FWRD.GUIDANCE.*)
- Beta interpretation, return assessment

market.py display data (stays direct):
- Stock prices, volume, institutional holders, insider transactions, valuation ratios
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite financials.py -- extract evaluative to financials_evaluative.py</name>
  <files>
    src/do_uw/stages/render/context_builders/financials.py
    src/do_uw/stages/render/context_builders/financials_evaluative.py
  </files>
  <action>
Extract all evaluative judgment logic from financials.py into a new financials_evaluative.py module. The main financials.py keeps tabular/display data extraction and calls into financials_evaluative.py for signal-backed evaluations.

**financials_evaluative.py** (<300 lines): Create functions that extract evaluative content from signals:

1. `_extract_distress_signals(signal_results, fin)` -- Altman Z zone, Beneish M zone, Ohlson O zone from `FIN.DISTRESS.*` and `FIN.EARNINGS.beneish_*` signals. Fallback to direct state `fin.distress.*` fields.

2. `_extract_earnings_quality_signals(signal_results, fin)` -- earnings quality assessments from `FIN.EARNINGS.*` signals (revenue quality, accrual ratio, cash conversion). Fallback to direct state.

3. `_extract_leverage_signals(signal_results, fin)` -- leverage/debt assessments from `FIN.LEVERAGE.*` signals. Fallback to direct state.

4. `_extract_tax_signals(signal_results, fin)` -- tax risk flags from `FIN.TAX.*` signals. Fallback to direct state.

5. `_extract_liquidity_signals(signal_results, fin)` -- liquidity zone assessments from `FIN.LIQUIDITY.*` signals. Fallback to direct state.

Each function returns a dict that gets merged into the main financials context dict.

**financials.py** (<300 lines): Rewrite `extract_financials(state, signal_results=None)` to:
- Keep all tabular data extraction (line items, sparklines, quarterly context) as direct state reads
- Call the evaluative functions from financials_evaluative.py with signal_results
- Merge evaluative results into the context dict
- Return the SAME dict keys as before (verify against templates)

**Critical:** Before modifying, grep templates for every key that financials context provides. Ensure no keys are lost or renamed. Use `safe_get_result()` from `_signal_fallback.py` for all signal access (never raw dict indexing). Pattern:
```python
z_result = safe_get_result(signal_results, "FIN.DISTRESS.altman_z_score")
if z_result:
    result["z_zone"] = z_result.evidence
    result["z_level"] = signal_to_display_level(z_result.status, z_result.threshold_level)
else:
    result["z_zone"] = fin.distress.altman_z_score.zone if fin.distress and fin.distress.altman_z_score else "N/A"
```

Grep `brain/signals/` for exact signal IDs before wiring -- do NOT guess signal IDs.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && wc -l src/do_uw/stages/render/context_builders/financials.py src/do_uw/stages/render/context_builders/financials_evaluative.py && uv run pytest tests/stages/render/ -x -q -k "financial" --timeout=30 2>&1 | tail -5 && echo "--- signal consumption in financials.py ---" && grep -c "safe_get_result\|safe_get_signals_by_prefix\|_signal_fallback\|_signal_consumer" src/do_uw/stages/render/context_builders/financials.py src/do_uw/stages/render/context_builders/financials_evaluative.py</automated>
  </verify>
  <done>financials.py under 300 lines; financials_evaluative.py under 300 lines; all evaluative content sourced from FIN.* signals with state fallback; financials.py imports from _signal_consumer or _signal_fallback; all financial render tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite market.py -- extract evaluative to market_evaluative.py</name>
  <files>
    src/do_uw/stages/render/context_builders/market.py
    src/do_uw/stages/render/context_builders/market_evaluative.py
  </files>
  <action>
Extract all evaluative judgment logic from market.py into a new market_evaluative.py module. The main market.py keeps display data and calls into market_evaluative.py.

**market_evaluative.py** (<300 lines): Create functions that extract evaluative content from signals:

1. `_extract_volatility_signals(signal_results, stock_data)` -- volatility regime classification, vol_regime_duration from `STOCK.VOLATILITY.*` signals. Fallback to direct state.

2. `_extract_short_interest_signals(signal_results, stock_data)` -- short interest assessment, short ratio interpretation from `STOCK.SHORT.*` signals. Fallback to direct state.

3. `_extract_insider_signals(signal_results, stock_data)` -- insider trading net buying/selling verdict, insider confidence from `STOCK.INSIDER.*` signals. Fallback to direct state.

4. `_extract_guidance_signals(signal_results, fwd)` -- guidance track record, estimate revision signals from `FWRD.GUIDANCE.*` signals. Fallback to direct state.

5. `_extract_return_signals(signal_results, stock_data)` -- beta interpretation, return assessment from `STOCK.RETURN.*` signals. Fallback to direct state.

**market.py** (<300 lines): Rewrite `extract_market(state, signal_results=None)` to:
- Keep all display data (prices, volumes, institutional holders, insider transactions, valuation ratios) as direct state reads
- Call evaluative functions from market_evaluative.py with signal_results
- Merge into the SAME context dict structure

**Critical:** Same template-key preservation rules as financials. Grep templates before modifying. Use safe_get_result() exclusively. Grep `brain/signals/` for exact signal IDs.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && wc -l src/do_uw/stages/render/context_builders/market.py src/do_uw/stages/render/context_builders/market_evaluative.py && uv run pytest tests/stages/render/ -x -q -k "market" --timeout=30 2>&1 | tail -5 && echo "--- signal consumption in market.py ---" && grep -c "safe_get_result\|safe_get_signals_by_prefix\|_signal_fallback\|_signal_consumer" src/do_uw/stages/render/context_builders/market.py src/do_uw/stages/render/context_builders/market_evaluative.py</automated>
  </verify>
  <done>market.py under 300 lines; market_evaluative.py under 300 lines; all evaluative content sourced from STOCK.* signals with state fallback; market.py imports from _signal_consumer or _signal_fallback; all market render tests pass</done>
</task>

</tasks>

<verification>
- `wc -l` confirms all 4 files under 300 lines
- `uv run pytest tests/stages/render/ -x -q` passes (all 790+ render tests)
- `grep -rn "safe_get_result\|safe_get_signals_by_prefix" src/do_uw/stages/render/context_builders/financials_evaluative.py` shows signal consumption
- `grep -rn "safe_get_result\|safe_get_signals_by_prefix" src/do_uw/stages/render/context_builders/market_evaluative.py` shows signal consumption
- `grep -rn "_signal_fallback\|_signal_consumer" src/do_uw/stages/render/context_builders/financials.py src/do_uw/stages/render/context_builders/market.py` confirms primary builders import signal infrastructure
- `uv run pytest tests/stages/render/test_signal_consumption.py -x -q` passes with financials and market added to SIGNAL_CONSUMING_BUILDERS
</verification>

<success_criteria>
- financials.py and market.py each under 300 lines
- financials_evaluative.py and market_evaluative.py each under 300 lines
- All evaluative content (distress zones, earnings quality, volatility regime, insider verdict) sourced from signals
- Primary builder files (financials.py, market.py) import from _signal_consumer/_signal_fallback
- Tabular data still direct state reads
- All render tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/113-context-builder-rewrites/113-02-SUMMARY.md`
</output>
