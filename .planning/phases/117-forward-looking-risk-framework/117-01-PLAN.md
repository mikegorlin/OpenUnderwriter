---
phase: 117-forward-looking-risk-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/models/forward_looking.py
  - src/do_uw/brain/config/underwriting_posture.yaml
  - src/do_uw/brain/config/monitoring_triggers.yaml
  - src/do_uw/brain/config/nuclear_triggers.yaml
  - src/do_uw/stages/extract/llm/schemas/forward_looking.py
  - tests/models/test_forward_looking.py
  - tests/stages/extract/test_forward_looking_schema.py
autonomous: true
requirements:
  - FORWARD-01
  - FORWARD-02
  - FORWARD-04
  - SCORE-03
  - TRIGGER-03

must_haves:
  truths:
    - "All forward-looking data structures are defined as Pydantic v2 models with source/confidence fields"
    - "Posture decision matrix lives in brain YAML, not Python"
    - "Nuclear trigger definitions live in brain YAML with data source mappings"
    - "LLM extraction schema captures both quantitative and qualitative forward statements"
  artifacts:
    - path: "src/do_uw/models/forward_looking.py"
      provides: "ForwardStatement, CredibilityScore, MonitoringTrigger, PostureElement, PostureRecommendation, NuclearTriggerCheck, QuickScreenResult, WatchItem, CatalystEvent, GrowthEstimate, ForwardLookingData"
      exports: ["ForwardStatement", "CredibilityScore", "MonitoringTrigger", "PostureRecommendation", "NuclearTriggerCheck", "QuickScreenResult", "ForwardLookingData"]
    - path: "src/do_uw/brain/config/underwriting_posture.yaml"
      provides: "Tier-to-posture mapping and factor override rules"
      contains: "posture_matrix"
    - path: "src/do_uw/brain/config/nuclear_triggers.yaml"
      provides: "5 nuclear trigger definitions"
      contains: "NUC-01"
    - path: "src/do_uw/stages/extract/llm/schemas/forward_looking.py"
      provides: "LLM extraction schema for forward statements from 10-K/8-K"
      exports: ["ForwardLookingExtraction", "ExtractedForwardStatement"]
  key_links:
    - from: "src/do_uw/models/forward_looking.py"
      to: "src/do_uw/models/common.py"
      via: "SourcedValue import"
      pattern: "from do_uw.models.common import SourcedValue"
    - from: "src/do_uw/models/forward_looking.py"
      to: "src/do_uw/models/state.py"
      via: "ForwardLookingData stored on ExtractedData or AnalysisResults"
      pattern: "ForwardLookingData"
---

<objective>
Define all Pydantic data models, brain YAML configuration files, and LLM extraction schemas that the rest of Phase 117 depends on.

Purpose: Establish the contracts (types, configs, schemas) before any implementation begins. Every subsequent plan imports from these files.
Output: models/forward_looking.py, 3 brain YAML configs, LLM extraction schema, tests for all models.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-forward-looking-risk-framework/117-CONTEXT.md
@.planning/phases/117-forward-looking-risk-framework/117-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs from existing codebase -->

From src/do_uw/models/common.py:
```python
class SourcedValue(BaseModel, Generic[T]):
    value: T
    source: str = ""
    confidence: str = "MEDIUM"  # HIGH, MEDIUM, LOW
```

From src/do_uw/models/state.py:
```python
class ExtractedData(BaseModel):
    financials: ExtractedFinancials | None = None
    market: MarketSignals | None = None
    governance: GovernanceData | None = None
    litigation: LitigationLandscape | None = None
    ai_risk: AIRiskAssessment | None = None
    # ... forward_looking will be added here

class AnalysisResults(BaseModel):
    checks_executed: int = 0
    checks_passed: int = 0
    checks_failed: int = 0
    # ... forward analysis results stored here or in benchmark

class AnalysisState(BaseModel):
    # stages read/write to this single model
    extracted: ExtractedData
    analysis: AnalysisResults
    scoring: ScoringResult | None
    benchmark: BenchmarkResults | None
```

From src/do_uw/models/scoring.py:
```python
class TierClassification(BaseModel):
    tier: Tier  # WIN, WANT, WRITE, WATCH, WALK, NO_TOUCH
    score_range_low: int
    score_range_high: int

class ScoringResult(BaseModel):
    quality_score: float
    tier: TierClassification | None
    factor_scores: list[FactorScore]
```

From src/do_uw/models/market_events.py:
```python
class EarningsGuidanceAnalysis(BaseModel):
    provides_forward_guidance: bool = False
    quarters: list[EarningsQuarterRecord] = []
    beat_rate: SourcedValue[float] | None = None
    consecutive_miss_count: int = 0
    philosophy: GuidancePhilosophy = GuidancePhilosophy.UNKNOWN

class EarningsQuarterRecord(BaseModel):
    quarter: str = ""
    consensus_eps_low: SourcedValue[float] | None = None
    consensus_eps_high: SourcedValue[float] | None = None
    guidance_revenue_low: SourcedValue[float] | None = None
    guidance_revenue_high: SourcedValue[float] | None = None
    actual_eps: SourcedValue[float] | None = None
    actual_revenue: SourcedValue[float] | None = None
    result: EarningsResult = EarningsResult.UNKNOWN
```

From src/do_uw/stages/extract/llm/schemas/common.py:
```python
# Common schema patterns for LLM extraction
class ExtractedRiskFactor(BaseModel): ...
class ExtractedLegalProceeding(BaseModel): ...
```

From brain/config/scoring.json tiers:
```json
{"tier": "WIN", "min_score": 86, "max_score": 100, "action": "Must-have account, compete aggressively", "pricing_multiplier": "0.5-0.7x", "tower_position": "PRIMARY"}
{"tier": "WANT", "min_score": 71, "max_score": 85, ...}
{"tier": "WRITE", "min_score": 51, "max_score": 70, ...}
{"tier": "WATCH", "min_score": 31, "max_score": 50, ...}
{"tier": "WALK", "min_score": 11, "max_score": 30, ...}
{"tier": "NO_TOUCH", "min_score": 0, "max_score": 10, ...}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pydantic models + state integration for forward-looking data</name>
  <files>src/do_uw/models/forward_looking.py, src/do_uw/models/state.py, tests/models/test_forward_looking.py</files>
  <read_first>
    - src/do_uw/models/common.py (SourcedValue pattern)
    - src/do_uw/models/state.py (ExtractedData, AnalysisResults -- where to add ForwardLookingData)
    - src/do_uw/models/market_events.py (EarningsGuidanceAnalysis, EarningsQuarterRecord -- patterns to follow)
    - src/do_uw/models/scoring.py (ScoringResult, FactorScore -- consumed by posture)
  </read_first>
  <action>
Create `src/do_uw/models/forward_looking.py` with the following Pydantic v2 models (all with `model_config = ConfigDict(frozen=False)`):

1. **ForwardStatement** -- Single forward-looking claim extracted from SEC filings:
   - `metric_name: str` (revenue, EPS, margin, etc.)
   - `current_value: str = ""` (latest actual value as string for display)
   - `current_value_numeric: float | None = None` (for miss risk computation)
   - `guidance_claim: str = ""` (the forward-looking claim or target)
   - `guidance_midpoint: float | None = None` (numeric midpoint for miss risk calc)
   - `guidance_type: str = "QUANTITATIVE"` (QUANTITATIVE or QUALITATIVE)
   - `miss_risk: str = "UNKNOWN"` (HIGH, MEDIUM, LOW, UNKNOWN)
   - `miss_risk_rationale: str = ""`
   - `sca_relevance: str = ""` (SCA theory if missed -- deterministic mapping)
   - `source_filing: str = ""` (10-K or 8-K accession number)
   - `source_date: str = ""` (filing date YYYY-MM-DD)
   - `confidence: str = "MEDIUM"` (HIGH/MEDIUM/LOW per data integrity rules)

2. **CredibilityQuarter** -- Single quarter guidance vs actual:
   - `quarter: str = ""` (e.g., "Q3 2025")
   - `metric: str = ""` (EPS, Revenue, etc.)
   - `guided_value: str = ""` (what management guided)
   - `actual_value: str = ""` (what was reported)
   - `beat_or_miss: str = ""` (BEAT, MISS, INLINE, UNKNOWN)
   - `magnitude_pct: float | None = None` (% above/below guidance)
   - `source: str = ""` (yfinance or 8-K accession)

3. **CredibilityScore** -- Aggregate management credibility:
   - `beat_rate_pct: float = 0.0`
   - `quarters_assessed: int = 0`
   - `credibility_level: str = "MEDIUM"` (HIGH >80%, MEDIUM 50-80%, LOW <50%)
   - `quarter_records: list[CredibilityQuarter] = []`
   - `source: str = "yfinance + 8-K LLM"`

4. **CatalystEvent** -- Forward catalyst:
   - `event: str = ""`
   - `timing: str = ""` (when expected)
   - `impact_if_negative: str = ""`
   - `litigation_risk: str = ""` (HIGH/MEDIUM/LOW)
   - `source: str = ""`

5. **GrowthEstimate** -- EPS/revenue estimate:
   - `period: str = ""` (Current Q, Current Y, Next Y)
   - `metric: str = ""` (EPS, Revenue)
   - `estimate: str = ""`
   - `estimate_numeric: float | None = None`
   - `trend: str = ""` (UP, DOWN, FLAT)
   - `source: str = "yfinance"`

6. **MonitoringTrigger** -- Post-bind surveillance trigger:
   - `trigger_name: str`
   - `action: str`
   - `threshold: str` (company-specific)
   - `current_value: str = ""`
   - `source: str = ""`

7. **PostureElement** -- Single element of posture recommendation:
   - `element: str` (decision, retention, limit, pricing, exclusions, monitoring, re_evaluation)
   - `recommendation: str`
   - `rationale: str`

8. **PostureRecommendation** -- Full posture:
   - `tier: str = ""`
   - `elements: list[PostureElement] = []`
   - `overrides_applied: list[str] = []` (e.g., "F.1>0: litigation exclusion added")

9. **NuclearTriggerCheck** -- One of 5 nuclear triggers:
   - `trigger_id: str` (NUC-01 through NUC-05)
   - `name: str`
   - `fired: bool = False`
   - `evidence: str = ""` (positive evidence for status)
   - `source: str = ""`

10. **WatchItem** -- Item requiring ongoing attention:
    - `item: str`
    - `current_state: str = ""`
    - `threshold: str = ""`
    - `re_evaluation: str = ""`
    - `source: str = ""`

11. **TriggerMatrixRow** -- Single row in quick screen:
    - `signal_id: str = ""`
    - `signal_name: str = ""`
    - `flag_level: str = ""` (RED, YELLOW)
    - `section: str = ""`
    - `section_anchor: str = ""`
    - `do_context: str = ""`

12. **ProspectiveCheck** -- Forward-looking check:
    - `check_name: str`
    - `finding: str = ""`
    - `status: str = "UNKNOWN"` (GREEN, YELLOW, RED, UNKNOWN)
    - `source: str = ""`

13. **QuickScreenResult** -- Full quick screen:
    - `trigger_matrix: list[TriggerMatrixRow] = []`
    - `nuclear_triggers: list[NuclearTriggerCheck] = []`
    - `nuclear_fired_count: int = 0`
    - `prospective_checks: list[ProspectiveCheck] = []`
    - `red_count: int = 0`
    - `yellow_count: int = 0`

14. **ForwardLookingData** -- Container for all forward-looking analysis:
    - `forward_statements: list[ForwardStatement] = []`
    - `credibility: CredibilityScore | None = None`
    - `catalysts: list[CatalystEvent] = []`
    - `growth_estimates: list[GrowthEstimate] = []`
    - `monitoring_triggers: list[MonitoringTrigger] = []`
    - `posture: PostureRecommendation | None = None`
    - `quick_screen: QuickScreenResult | None = None`
    - `watch_items: list[WatchItem] = []`
    - `zero_verifications: list[dict[str, str]] = []` (factor_id, evidence, source)

Then modify `src/do_uw/models/state.py`:
- Add import: `from do_uw.models.forward_looking import ForwardLookingData`
- Add field to `AnalysisState`: `forward_looking: ForwardLookingData = Field(default_factory=ForwardLookingData)`
  (Top-level on AnalysisState, not nested in ExtractedData, because forward-looking data spans extraction through benchmark.)

Create `tests/models/test_forward_looking.py`:
- Test model instantiation with defaults for all 14 classes
- Test ForwardStatement with all fields populated (source/confidence present)
- Test CredibilityScore level derivation
- Test PostureRecommendation with overrides
- Test NuclearTriggerCheck fired=True vs False
- Test QuickScreenResult red/yellow counting
- Test ForwardLookingData container with nested models
- Minimum 15 test functions
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run pytest tests/models/test_forward_looking.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/do_uw/models/forward_looking.py exists and contains `class ForwardStatement`, `class CredibilityScore`, `class MonitoringTrigger`, `class PostureElement`, `class PostureRecommendation`, `class NuclearTriggerCheck`, `class QuickScreenResult`, `class ForwardLookingData`, `class WatchItem`, `class CatalystEvent`, `class GrowthEstimate`, `class TriggerMatrixRow`, `class ProspectiveCheck`, `class CredibilityQuarter`
    - src/do_uw/models/state.py contains `forward_looking: ForwardLookingData`
    - src/do_uw/models/state.py contains `from do_uw.models.forward_looking import ForwardLookingData`
    - tests/models/test_forward_looking.py contains at least 15 `def test_` functions
    - `uv run pytest tests/models/test_forward_looking.py -x` exits 0
    - Every model class has `confidence` or `source` field (data integrity rule)
    - File is under 500 lines (anti-context-rot rule)
  </acceptance_criteria>
  <done>All 14 Pydantic models instantiate correctly, state.py has ForwardLookingData field, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Brain YAML configs + LLM extraction schema</name>
  <files>src/do_uw/brain/config/underwriting_posture.yaml, src/do_uw/brain/config/monitoring_triggers.yaml, src/do_uw/brain/config/nuclear_triggers.yaml, src/do_uw/stages/extract/llm/schemas/forward_looking.py, tests/stages/extract/test_forward_looking_schema.py</files>
  <read_first>
    - src/do_uw/brain/config/scoring.json (tier definitions with action, pricing_multiplier, tower_position -- posture derives from these)
    - src/do_uw/stages/extract/llm/schemas/ten_k.py (existing LLM extraction schema pattern)
    - src/do_uw/stages/extract/llm/schemas/common.py (shared extraction types)
    - src/do_uw/stages/extract/llm/schemas/eight_k.py (8-K extraction pattern)
    - .planning/phases/117-forward-looking-risk-framework/117-CONTEXT.md (locked decisions: posture matrix, factor overrides, nuclear triggers)
  </read_first>
  <action>
**1. Create `src/do_uw/brain/config/underwriting_posture.yaml`:**

```yaml
# Underwriting posture decision matrix.
# Maps scoring tier to recommended underwriting position.
# Per Phase 117 CONTEXT.md: lives in brain YAML, not Python.
posture_matrix:
  WIN:
    decision: "Full terms"
    retention: "Standard"
    limit: "Full tower"
    pricing: "At-model"
    exclusions: "Standard"
    monitoring: "Annual review"
    re_evaluation: "At renewal"
  WANT:
    decision: "Full terms with monitoring"
    retention: "Standard"
    limit: "Full tower"
    pricing: "Slight adjustment"
    exclusions: "Standard"
    monitoring: "Semi-annual review"
    re_evaluation: "6 months"
  WRITE:
    decision: "Conditional terms"
    retention: "Elevated retention"
    limit: "Sublimit consideration"
    pricing: "+10-20% over model"
    exclusions: "Targeted exclusions"
    monitoring: "Quarterly review"
    re_evaluation: "3 months"
  WATCH:
    decision: "Restricted terms"
    retention: "High retention"
    limit: "Sublimit required"
    pricing: "+25-50% over model"
    exclusions: "Broad exclusions"
    monitoring: "Continuous monitoring"
    re_evaluation: "Monthly"
  WALK:
    decision: "Decline consideration"
    retention: "Maximum retention"
    limit: "Minimum limit"
    pricing: "+50%+ over model"
    exclusions: "Extensive exclusions"
    monitoring: "Monthly review"
    re_evaluation: "Monthly"
  NO_TOUCH:
    decision: "Decline"
    retention: "N/A"
    limit: "N/A"
    pricing: "N/A"
    exclusions: "N/A"
    monitoring: "N/A"
    re_evaluation: "N/A"

factor_overrides:
  - condition: "F1 > 0"
    factor_id: "F.1"
    description: "Active SCA or recent litigation"
    add_exclusion: "Litigation exclusion (pending/prior matters)"
    add_monitoring: "Quarterly litigation status check"
    rationale: "Active SCA creates direct loss exposure; prior litigation indicates elevated recurrence risk"
  - condition: "F7 > 5"
    factor_id: "F.7"
    description: "Heavy insider selling"
    add_monitoring: "Monthly insider trading report"
    rationale: "Heavy selling indicates potential scienter evidence in future SCA"
  - condition: "F3 > 0"
    factor_id: "F.3"
    description: "Restatement history"
    add_exclusion: "Financial reporting exclusion"
    add_monitoring: "Quarterly financial reporting review"
    rationale: "Restatement history increases recurrence risk and 10b-5 exposure"
  - condition: "F9 > 0"
    factor_id: "F.9"
    description: "SEC enforcement"
    add_exclusion: "Regulatory investigation exclusion"
    add_monitoring: "Monthly SEC filing monitor"
    rationale: "SEC enforcement creates direct regulatory exposure and derivative suit risk"

# Nuclear trigger escalation: any fired nuclear trigger overrides posture
nuclear_escalation:
  message: "NUCLEAR TRIGGER FIRED -- escalate to senior underwriting review"
  override_monitoring: "Immediate daily monitoring"
  override_re_evaluation: "Immediate"
```

**2. Create `src/do_uw/brain/config/monitoring_triggers.yaml`:**

```yaml
# Company-specific monitoring trigger definitions.
# Thresholds are computed at runtime from company data.
triggers:
  - id: MON-01
    name: "SCA Filing"
    description: "Securities class action filed against the company"
    action: "Immediate review of coverage terms and reserves"
    default_threshold: "Any Stanford SCAC match"
    threshold_source: "Stanford SCAC database"
    data_field: "litigation.active_cases"
  - id: MON-02
    name: "Stock Below Support"
    description: "Stock price drops below technical support level"
    action: "Review stock drop analysis and D&O exposure"
    default_threshold: "Price below 52-week low"
    threshold_source: "market_data.year_low"
    data_field: "market.stock_performance.fifty_two_week_low"
  - id: MON-03
    name: "Insider Selling Pace"
    description: "Insider selling acceleration beyond normal pace"
    action: "Review insider trading patterns for scienter indicators"
    default_threshold: ">2x current quarterly selling rate"
    threshold_source: "insider_trading analysis"
    data_field: "market.insider_trading.quarterly_sell_value"
  - id: MON-04
    name: "EPS Miss"
    description: "Earnings per share miss exceeds threshold"
    action: "Review earnings guidance credibility and forward statements"
    default_threshold: ">10% miss vs guidance midpoint"
    threshold_source: "earnings_guidance analysis"
    data_field: "market.earnings_guidance.quarters"
  - id: MON-05
    name: "C-Suite Departure"
    description: "CEO, CFO, or other C-suite executive departure"
    action: "Review circumstances of departure and D&O implications"
    default_threshold: "Any CEO/CFO/COO departure"
    threshold_source: "8-K Item 5.02 filings"
    data_field: "governance.leadership"
  - id: MON-06
    name: "Peer SCA"
    description: "Securities class action filed against industry peer"
    action: "Review for sector contagion risk"
    default_threshold: "Any SCAC match in same SIC code"
    threshold_source: "Stanford SCAC + SIC classification"
    data_field: "company.sic_code"
```

**3. Create `src/do_uw/brain/config/nuclear_triggers.yaml`:**

```yaml
# Nuclear trigger definitions -- 5 binary checks that demand immediate escalation.
# Each trigger has a data source mapping for deterministic verification.
nuclear_triggers:
  - id: NUC-01
    name: "Active Securities Class Action"
    description: "Company is a defendant in an active SCA"
    data_source: "Stanford SCAC database match"
    state_path: "extracted.litigation.active_cases"
    check_type: "list_not_empty"
    evidence_template_fired: "Stanford SCAC: {case_details}"
    evidence_template_clean: "Stanford SCAC clean -- no active SCA found"
  - id: NUC-02
    name: "SEC Investigation / Enforcement"
    description: "Active SEC enforcement action or AAER"
    data_source: "SEC AAER database + 10-K disclosure"
    state_path: "extracted.litigation.sec_enforcement"
    check_type: "list_not_empty"
    evidence_template_fired: "SEC enforcement: {action_details}"
    evidence_template_clean: "No SEC enforcement actions or AAERs found"
  - id: NUC-03
    name: "Financial Restatement"
    description: "Company has restated financial results"
    data_source: "10-K disclosure + 8-K Item 4.02"
    state_path: "analysis.restatement_detected"
    check_type: "boolean_true"
    evidence_template_fired: "Restatement disclosed: {restatement_details}"
    evidence_template_clean: "No financial restatements disclosed in filings"
  - id: NUC-04
    name: "CEO/CFO Departure Under Pressure"
    description: "CEO or CFO departed under circumstances suggesting pressure"
    data_source: "8-K Item 5.02 filing analysis"
    state_path: "extracted.governance.executive_departures"
    check_type: "departure_under_pressure"
    evidence_template_fired: "{executive_name} departed {date}: {circumstances}"
    evidence_template_clean: "No CEO/CFO departures under pressure identified"
  - id: NUC-05
    name: "Going Concern Opinion"
    description: "Auditor issued going concern qualification"
    data_source: "10-K audit opinion"
    state_path: "extracted.financials.going_concern"
    check_type: "boolean_true"
    evidence_template_fired: "Going concern: {auditor} opinion dated {date}"
    evidence_template_clean: "Clean audit opinion -- no going concern qualification"
```

**4. Create `src/do_uw/stages/extract/llm/schemas/forward_looking.py`:**

Pydantic models for LLM extraction of forward-looking statements from 10-K and 8-K filings. Follow the pattern in `ten_k.py` (optional fields with defaults, `BeforeValidator` coercions for LLM output resilience):

```python
class ExtractedForwardStatement(BaseModel):
    metric: str = Field(default="", description="The metric being guided (revenue, EPS, margin, growth rate, etc.)")
    target_value: str = Field(default="", description="Guided value or range (e.g., '$4.50-$4.70 EPS', '15-17% growth')")
    target_numeric_low: float | None = Field(default=None, description="Low end of guidance range (numeric)")
    target_numeric_high: float | None = Field(default=None, description="High end of guidance range (numeric)")
    timeframe: str = Field(default="", description="When (FY2026, Q2 2026, next 12 months)")
    context: str = Field(default="", description="Surrounding context from filing (max 300 chars)")
    is_quantitative: bool = Field(default=False, description="True if specific numbers provided")
    filing_section: str = Field(default="", description="Where found: MD&A, Risk Factors, Outlook, etc.")

class ExtractedGuidanceChange(BaseModel):
    change_type: str = Field(default="", description="RAISE, CUT, WITHDRAW, REAFFIRM")
    metric: str = Field(default="", description="Metric affected")
    prior_value: str = Field(default="", description="Previous guidance")
    new_value: str = Field(default="", description="Updated guidance")
    date: str = Field(default="", description="Date of change")

class ExtractedCatalyst(BaseModel):
    event: str = Field(default="", description="The catalyst event")
    expected_timing: str = Field(default="", description="When expected")
    potential_impact: str = Field(default="", description="Impact if negative")
    mentioned_in: str = Field(default="", description="Filing section where mentioned")

class ForwardLookingExtraction(BaseModel):
    forward_statements: list[ExtractedForwardStatement] = Field(default_factory=list)
    guidance_changes: list[ExtractedGuidanceChange] = Field(default_factory=list)
    catalyst_events: list[ExtractedCatalyst] = Field(default_factory=list)
    provides_numeric_guidance: bool = Field(default=False, description="True if company provides explicit numeric guidance")
    guidance_summary: str = Field(default="", description="1-2 sentence summary of forward outlook")
```

Add `_coerce_currency_float` BeforeValidator on numeric fields (same pattern as ten_k.py).

**5. Create `tests/stages/extract/test_forward_looking_schema.py`:**
- Test ForwardLookingExtraction instantiation with defaults
- Test ExtractedForwardStatement with all fields
- Test currency float coercion on target_numeric_low/high
- Test ForwardLookingExtraction with mixed quantitative/qualitative statements
- Minimum 8 test functions
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run pytest tests/stages/extract/test_forward_looking_schema.py tests/models/test_forward_looking.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/do_uw/brain/config/underwriting_posture.yaml exists and contains `posture_matrix:` with keys WIN, WANT, WRITE, WATCH, WALK, NO_TOUCH
    - src/do_uw/brain/config/underwriting_posture.yaml contains `factor_overrides:` with conditions for F1, F3, F7, F9
    - src/do_uw/brain/config/monitoring_triggers.yaml exists and contains 6 trigger definitions (MON-01 through MON-06)
    - src/do_uw/brain/config/nuclear_triggers.yaml exists and contains 5 nuclear trigger definitions (NUC-01 through NUC-05)
    - src/do_uw/stages/extract/llm/schemas/forward_looking.py exists and contains `class ForwardLookingExtraction`, `class ExtractedForwardStatement`, `class ExtractedCatalyst`, `class ExtractedGuidanceChange`
    - tests/stages/extract/test_forward_looking_schema.py exits 0 with at least 8 test functions
    - `uv run pytest tests/ -x -q --timeout=30` full suite still passes
  </acceptance_criteria>
  <done>Brain YAML configs define posture matrix, monitoring triggers, and nuclear triggers; LLM extraction schema handles both quantitative and qualitative forward statements; all tests pass</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/models/test_forward_looking.py tests/stages/extract/test_forward_looking_schema.py -x -v` -- all pass
- `uv run pytest tests/ -x -q --timeout=30` -- full suite still green
- `python -c "from do_uw.models.forward_looking import ForwardLookingData; print('OK')"` -- import succeeds
- `python -c "from do_uw.stages.extract.llm.schemas.forward_looking import ForwardLookingExtraction; print('OK')"` -- import succeeds
- YAML files parse: `python -c "import yaml; yaml.safe_load(open('src/do_uw/brain/config/underwriting_posture.yaml')); print('OK')"`
</verification>

<success_criteria>
All 14 Pydantic models defined and tested, 3 brain YAML configs with complete decision matrix / trigger definitions, LLM extraction schema handles quantitative + qualitative forward statements, state.py has ForwardLookingData field, full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/117-forward-looking-risk-framework/117-01-SUMMARY.md`
</output>
