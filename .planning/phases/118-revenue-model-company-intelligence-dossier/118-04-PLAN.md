---
phase: 118-revenue-model-company-intelligence-dossier
plan: 04
type: execute
wave: 3
depends_on: ["118-01", "118-03"]
files_modified:
  - src/do_uw/stages/render/context_builders/dossier_what_company_does.py
  - src/do_uw/stages/render/context_builders/dossier_money_flows.py
  - src/do_uw/stages/render/context_builders/dossier_revenue_card.py
  - src/do_uw/stages/render/context_builders/dossier_segments.py
  - src/do_uw/stages/render/context_builders/dossier_unit_economics.py
  - src/do_uw/stages/render/context_builders/dossier_waterfall.py
  - src/do_uw/stages/render/context_builders/dossier_emerging_risks.py
  - src/do_uw/stages/render/context_builders/dossier_asc606.py
  - tests/stages/render/test_dossier_context_builders.py
autonomous: true
requirements:
  - DOSSIER-01
  - DOSSIER-02
  - DOSSIER-03
  - DOSSIER-04
  - DOSSIER-05
  - DOSSIER-06
  - DOSSIER-08
  - DOSSIER-09
must_haves:
  truths:
    - "8 context builders produce template-ready dicts from DossierData"
    - "Context builders contain zero evaluative/D&O logic (pure formatters)"
    - "Each builder returns an availability flag when data is missing"
  artifacts:
    - path: "src/do_uw/stages/render/context_builders/dossier_what_company_does.py"
      provides: "Business description + core D&O exposure context"
      exports: ["extract_what_company_does"]
    - path: "src/do_uw/stages/render/context_builders/dossier_revenue_card.py"
      provides: "Revenue model card table context"
      exports: ["extract_revenue_model_card"]
    - path: "tests/stages/render/test_dossier_context_builders.py"
      provides: "Context builder output shape tests"
      min_lines: 120
  key_links:
    - from: "src/do_uw/stages/render/context_builders/dossier_*.py"
      to: "src/do_uw/models/dossier.py"
      via: "reads state.dossier fields"
      pattern: "state\\.dossier"
---

<objective>
Create 8 context builders that transform DossierData into template-ready dicts for Jinja2 rendering.

Purpose: Context builders are pure data formatters following the established pattern from Phase 117. They read pre-computed data from state.dossier and format it for templates. Zero evaluative logic -- D&O commentary was already generated in BENCHMARK enrichment.
Output: 8 context builder modules + comprehensive tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/118-revenue-model-company-intelligence-dossier/118-RESEARCH.md

<interfaces>
<!-- From src/do_uw/stages/render/context_builders/forward_risk_map.py pattern: -->
```python
def extract_forward_risk_map(
    state: AnalysisState,
    signal_results: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Format forward-looking data for template. No evaluative logic."""
    fl = state.forward_looking
    if not fl or not fl.forward_statements:
        return {"forward_available": False}
    # ... pure formatting, returns dict with template-ready data
```

<!-- From src/do_uw/stages/render/formatters.py: -->
```python
def format_percentage(value: float | None, decimals: int = 1) -> str: ...
def na_if_none(value: Any) -> str: ...
def safe_float(value: Any) -> float | None: ...
```

<!-- DossierData fields (from 118-01): -->
```python
class DossierData(BaseModel):
    business_description_plain: str
    core_do_exposure: str
    revenue_flow_diagram: str
    revenue_flow_narrative: str
    revenue_card: list[RevenueModelCardRow]
    segment_dossiers: list[RevenueSegmentDossier]
    concentration_dimensions: list[ConcentrationDimension]
    unit_economics: list[UnitEconomicMetric]
    unit_economics_narrative: str
    waterfall_rows: list[WaterfallRow]
    waterfall_narrative: str
    emerging_risks: list[EmergingRisk]
    asc_606_elements: list[ASC606Element]
    billings_vs_revenue_narrative: str
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 8 dossier context builders</name>
  <files>
    src/do_uw/stages/render/context_builders/dossier_what_company_does.py
    src/do_uw/stages/render/context_builders/dossier_money_flows.py
    src/do_uw/stages/render/context_builders/dossier_revenue_card.py
    src/do_uw/stages/render/context_builders/dossier_segments.py
    src/do_uw/stages/render/context_builders/dossier_unit_economics.py
    src/do_uw/stages/render/context_builders/dossier_waterfall.py
    src/do_uw/stages/render/context_builders/dossier_emerging_risks.py
    src/do_uw/stages/render/context_builders/dossier_asc606.py
  </files>
  <read_first>
    - src/do_uw/stages/render/context_builders/forward_risk_map.py (reference pattern -- pure formatter)
    - src/do_uw/stages/render/context_builders/credibility_context.py (another Phase 117 builder)
    - src/do_uw/stages/render/formatters.py (format_percentage, na_if_none, safe_float)
    - src/do_uw/models/dossier.py (DossierData fields)
  </read_first>
  <action>
Create 8 context builders, each following the exact forward_risk_map.py pattern:

**1. `dossier_what_company_does.py` -- `extract_what_company_does(state, signal_results=None) -> dict`**
Returns: `{"what_company_does_available": bool, "business_description": str, "core_do_exposure": str}`

**2. `dossier_money_flows.py` -- `extract_money_flows(state, signal_results=None) -> dict`**
Returns: `{"money_flows_available": bool, "flow_diagram": str (pre-formatted), "flow_narrative": str}`

**3. `dossier_revenue_card.py` -- `extract_revenue_model_card(state, signal_results=None) -> dict`**
Returns: `{"revenue_card_available": bool, "rows": list[dict]}` where each row has: attribute, value, do_risk, row_class (CSS class from risk_level: "risk-high"/"risk-medium"/"risk-low"/"")

**4. `dossier_segments.py` -- `extract_revenue_segments(state, signal_results=None) -> dict`**
Returns: `{"segments_available": bool, "segments": list[dict], "concentration_dimensions": list[dict], "concentration_available": bool}`. Segments include segment_name, revenue_pct, growth_rate, rev_rec_method, do_exposure, row_class.

**5. `dossier_unit_economics.py` -- `extract_unit_economics(state, signal_results=None) -> dict`**
Returns: `{"unit_economics_available": bool, "metrics": list[dict], "narrative": str}`. Each metric has: metric, value, benchmark, assessment, do_risk.

**6. `dossier_waterfall.py` -- `extract_revenue_waterfall(state, signal_results=None) -> dict`**
Returns: `{"waterfall_available": bool, "rows": list[dict], "narrative": str}`. Each row has: label, value, delta, narrative.

**7. `dossier_emerging_risks.py` -- `extract_emerging_risks(state, signal_results=None) -> dict`**
Returns: `{"emerging_risks_available": bool, "risks": list[dict]}`. Each risk has: risk, probability, impact, timeframe, do_factor, status, probability_class (CSS).

**8. `dossier_asc606.py` -- `extract_asc_606(state, signal_results=None) -> dict`**
Returns: `{"asc_606_available": bool, "elements": list[dict], "billings_narrative": str}`. Each element has: element, approach, complexity, do_risk, complexity_class.

**Common patterns across ALL 8 builders:**
- Function signature: `def extract_X(state: AnalysisState, *, signal_results: dict[str, Any] | None = None) -> dict[str, Any]:`
- First check: `if not state.dossier or not state.dossier.{relevant_field}: return {"{x}_available": False}`
- CSS class mapping: `_RISK_CSS = {"HIGH": "risk-high", "MEDIUM": "risk-medium", "LOW": "risk-low"}`
- Use `na_if_none()` from formatters for any potentially None values
- Module docstring references Phase 118
- Zero evaluative logic -- no if/elif based on data content for D&O commentary
  </action>
  <verify>
    <automated>uv run python -c "
from do_uw.stages.render.context_builders.dossier_what_company_does import extract_what_company_does
from do_uw.stages.render.context_builders.dossier_money_flows import extract_money_flows
from do_uw.stages.render.context_builders.dossier_revenue_card import extract_revenue_model_card
from do_uw.stages.render.context_builders.dossier_segments import extract_revenue_segments
from do_uw.stages.render.context_builders.dossier_unit_economics import extract_unit_economics
from do_uw.stages.render.context_builders.dossier_waterfall import extract_revenue_waterfall
from do_uw.stages.render.context_builders.dossier_emerging_risks import extract_emerging_risks
from do_uw.stages.render.context_builders.dossier_asc606 import extract_asc_606
print('All 8 builders importable')
"</automated>
  </verify>
  <acceptance_criteria>
    - 8 files exist in src/do_uw/stages/render/context_builders/dossier_*.py
    - Each file contains a function starting with `extract_`
    - Each function returns a dict with an `_available` boolean key
    - No file contains `if.*tier\b|if.*score\b|if.*HIGH` evaluative conditionals (D&O logic belongs in benchmark)
    - All 8 modules importable without errors
  </acceptance_criteria>
  <done>8 context builders created as pure data formatters, each returning template-ready dicts with availability flags and CSS classes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create context builder tests</name>
  <files>tests/stages/render/test_dossier_context_builders.py</files>
  <read_first>
    - src/do_uw/stages/render/context_builders/dossier_what_company_does.py (just created)
    - src/do_uw/stages/render/context_builders/dossier_revenue_card.py (just created)
    - src/do_uw/stages/render/context_builders/dossier_segments.py (just created)
    - src/do_uw/models/dossier.py (model structure for fixture data)
  </read_first>
  <behavior>
    - Test 1: extract_what_company_does with populated data returns available=True + description + exposure
    - Test 2: extract_what_company_does with empty dossier returns available=False
    - Test 3: extract_revenue_model_card with 3 rows returns 3 formatted rows with row_class
    - Test 4: extract_revenue_model_card with HIGH risk_level produces row_class="risk-high"
    - Test 5: extract_revenue_segments with segments + concentration returns both available=True
    - Test 6: extract_money_flows with diagram text returns available=True
    - Test 7: extract_unit_economics with metrics returns list of formatted dicts
    - Test 8: extract_revenue_waterfall with rows returns formatted waterfall
    - Test 9: extract_emerging_risks maps probability to CSS class
    - Test 10: extract_asc_606 with HIGH complexity produces complexity_class="risk-high"
    - Test 11: All 8 builders return available=False when state.dossier is empty default
    - Test 12: No builder raises on None signal_results
  </behavior>
  <action>
Create `tests/stages/render/test_dossier_context_builders.py` with:

1. **Fixture:** `_make_state_with_dossier()` helper that returns an AnalysisState with populated DossierData containing at least 2 revenue card rows, 3 segments, 5 concentration dimensions, 3 unit economics metrics, 2 waterfall rows, 3 emerging risks, 2 ASC 606 elements.

2. **12+ test functions** covering the behaviors above. Use `pytest.mark.parametrize` for the "all 8 return available=False" test.

3. **Key assertions:**
   - Output dicts have correct keys (e.g., "revenue_card_available", "rows")
   - CSS classes are correctly mapped from risk_level
   - Availability flags are True when data exists, False when empty
   - No exceptions when signal_results=None
  </action>
  <verify>
    <automated>uv run pytest tests/stages/render/test_dossier_context_builders.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/stages/render/test_dossier_context_builders.py has at least 12 test functions
    - Tests import from all 8 dossier_*.py context builders
    - `uv run pytest tests/stages/render/test_dossier_context_builders.py -x -q` exits 0
  </acceptance_criteria>
  <done>All 8 context builders tested for correct output shape, CSS class mapping, availability flags, and graceful handling of empty data.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/stages/render/test_dossier_context_builders.py -x -q` -- all context builder tests pass
- All 8 context builders importable
- Zero evaluative logic in any builder (grep for if/elif on tier/score)
</verification>

<success_criteria>
- 8 context builders follow the forward_risk_map.py pattern exactly
- Each returns availability flag + template-ready data
- Pure formatters -- no D&O interpretation logic
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/118-revenue-model-company-intelligence-dossier/118-04-SUMMARY.md`
</output>
