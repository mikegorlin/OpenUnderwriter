---
phase: 118-revenue-model-company-intelligence-dossier
plan: 05
type: execute
wave: 3
depends_on: ["118-01"]
files_modified:
  - src/do_uw/templates/html/sections/dossier.html.j2
  - src/do_uw/templates/html/sections/dossier/what_company_does.html.j2
  - src/do_uw/templates/html/sections/dossier/money_flows.html.j2
  - src/do_uw/templates/html/sections/dossier/revenue_model_card.html.j2
  - src/do_uw/templates/html/sections/dossier/revenue_segments.html.j2
  - src/do_uw/templates/html/sections/dossier/unit_economics.html.j2
  - src/do_uw/templates/html/sections/dossier/revenue_waterfall.html.j2
  - src/do_uw/templates/html/sections/dossier/emerging_risk_radar.html.j2
  - src/do_uw/templates/html/sections/dossier/asc_606.html.j2
  - tests/stages/render/test_dossier_templates.py
autonomous: true
requirements:
  - DOSSIER-01
  - DOSSIER-02
  - DOSSIER-03
  - DOSSIER-04
  - DOSSIER-05
  - DOSSIER-06
  - DOSSIER-08
  - DOSSIER-09
must_haves:
  truths:
    - "9 Jinja2 templates render the dossier section and 8 subsections"
    - "Every data table template includes a D&O Risk column"
    - "Templates render do_risk strings as-is (zero evaluative logic)"
    - "Revenue flow diagram renders in <pre> block with monospace font"
  artifacts:
    - path: "src/do_uw/templates/html/sections/dossier.html.j2"
      provides: "Section wrapper including all 8 subsection templates"
      contains: "{% include"
    - path: "src/do_uw/templates/html/sections/dossier/revenue_model_card.html.j2"
      provides: "Revenue model card table with D&O Risk column"
      contains: "D&O Risk"
    - path: "tests/stages/render/test_dossier_templates.py"
      provides: "Template rendering tests with fixture data"
      min_lines: 100
  key_links:
    - from: "src/do_uw/templates/html/sections/dossier.html.j2"
      to: "src/do_uw/templates/html/sections/dossier/*.html.j2"
      via: "{% include %} directives"
      pattern: "include.*dossier/"
---

<objective>
Create 9 Jinja2 templates for the Company Intelligence Dossier section: 1 section wrapper + 8 subsection templates.

Purpose: Templates are dumb renderers -- they display pre-computed data from context builders. Every data table has a D&O Risk column. Templates use `{{ row.do_risk }}` directly, with zero evaluative logic.
Output: 9 Jinja2 template files + rendering tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/118-revenue-model-company-intelligence-dossier/118-RESEARCH.md

<interfaces>
<!-- Context builder output shapes (from 118-04 plan): -->
```python
# extract_what_company_does returns:
{"what_company_does_available": True, "business_description": "...", "core_do_exposure": "..."}

# extract_revenue_model_card returns:
{"revenue_card_available": True, "rows": [
    {"attribute": "Model Type", "value": "B2B SaaS", "do_risk": "...", "row_class": "risk-high"},
]}

# extract_revenue_segments returns:
{"segments_available": True, "segments": [...], "concentration_dimensions": [...], "concentration_available": True}

# extract_emerging_risks returns:
{"emerging_risks_available": True, "risks": [
    {"risk": "...", "probability": "High", "impact": "Very High", "timeframe": "6-12mo",
     "do_factor": "F.5", "status": "Monitoring", "probability_class": "risk-high"},
]}
```

<!-- Existing template pattern (from forward_looking/risk_map.html.j2): -->
```jinja2
{% if forward_risk_map.forward_available %}
<div class="subsection">
  <h4>Forward-Looking Statement Risk Map</h4>
  <table class="data-table">
    <thead><tr><th>Metric</th><th>Current</th>...</tr></thead>
    <tbody>
    {% for stmt in forward_risk_map.statements %}
      <tr class="{{ stmt.row_class }}">
        <td>{{ stmt.metric_name }}</td>
        ...
      </tr>
    {% endfor %}
    </tbody>
  </table>
</div>
{% endif %}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 9 dossier Jinja2 templates</name>
  <files>
    src/do_uw/templates/html/sections/dossier.html.j2
    src/do_uw/templates/html/sections/dossier/what_company_does.html.j2
    src/do_uw/templates/html/sections/dossier/money_flows.html.j2
    src/do_uw/templates/html/sections/dossier/revenue_model_card.html.j2
    src/do_uw/templates/html/sections/dossier/revenue_segments.html.j2
    src/do_uw/templates/html/sections/dossier/unit_economics.html.j2
    src/do_uw/templates/html/sections/dossier/revenue_waterfall.html.j2
    src/do_uw/templates/html/sections/dossier/emerging_risk_radar.html.j2
    src/do_uw/templates/html/sections/dossier/asc_606.html.j2
  </files>
  <read_first>
    - src/do_uw/templates/html/sections/forward_looking.html.j2 (section wrapper pattern)
    - src/do_uw/templates/html/sections/forward_looking/risk_map.html.j2 (data table with D&O column pattern)
    - src/do_uw/templates/html/sections/forward_looking/credibility.html.j2 (table + narrative pattern)
    - src/do_uw/templates/html/sections/company/business_model.html.j2 (existing business model template)
    - src/do_uw/templates/html/sections/company/revenue_segments.html.j2 (existing revenue segments)
  </read_first>
  <action>
Create the following templates, all following the existing project patterns:

**1. `dossier.html.j2` (section wrapper):**
```jinja2
{# Company Intelligence Dossier -- Phase 118 #}
<div class="section" id="intelligence-dossier">
  <h2>Company &amp; Business -- Intelligence Dossier</h2>
  {% include "sections/dossier/what_company_does.html.j2" %}
  {% include "sections/dossier/money_flows.html.j2" %}
  {% include "sections/dossier/revenue_model_card.html.j2" %}
  {% include "sections/dossier/revenue_segments.html.j2" %}
  {% include "sections/dossier/unit_economics.html.j2" %}
  {% include "sections/dossier/revenue_waterfall.html.j2" %}
  {% include "sections/dossier/emerging_risk_radar.html.j2" %}
  {% include "sections/dossier/asc_606.html.j2" %}
</div>
```

**2. `what_company_does.html.j2`:**
- Guard: `{% if dossier_what.what_company_does_available %}`
- H3: "What the Company Does"
- Paragraph: `{{ dossier_what.business_description }}`
- Callout box: `<div class="do-callout">{{ dossier_what.core_do_exposure }}</div>`

**3. `money_flows.html.j2`:**
- Guard: `{% if dossier_flows.money_flows_available %}`
- H3: "How Money Flows"
- `<pre class="revenue-flow">{{ dossier_flows.flow_diagram }}</pre>`
- Narrative paragraph: `{{ dossier_flows.flow_narrative }}`

**4. `revenue_model_card.html.j2`:**
- Guard: `{% if dossier_card.revenue_card_available %}`
- H3: "Revenue Model Card"
- Table with columns: Attribute | Value | D&O Risk
- Row loop: `{% for row in dossier_card.rows %}` with `class="{{ row.row_class }}"`
- `{{ row.do_risk }}` rendered as-is in D&O Risk column (NO truncate filter)

**5. `revenue_segments.html.j2`:**
- Guard: `{% if dossier_segments.segments_available %}`
- H3: "Revenue Segment Breakdown"
- Table with columns: Segment | Revenue % | Growth | Rev-Rec Method | D&O Litigation Exposure
- Below table: H4 "Concentration Assessment" with concentration_dimensions table (Dimension | Metric | Risk Level | D&O Implication)

**6. `unit_economics.html.j2`:**
- Guard: `{% if dossier_unit.unit_economics_available %}`
- H3: "Unit Economics"
- Table with columns: Metric | Value | Benchmark | Assessment | D&O Risk
- Narrative paragraph: `{{ dossier_unit.narrative }}`

**7. `revenue_waterfall.html.j2`:**
- Guard: `{% if dossier_waterfall.waterfall_available %}`
- H3: "Revenue Waterfall (Growth Decomposition)"
- Table with columns: Component | Value | Delta | Insight
- Narrative paragraph: `{{ dossier_waterfall.narrative }}`

**8. `emerging_risk_radar.html.j2`:**
- Guard: `{% if dossier_risks.emerging_risks_available %}`
- H3: "Emerging Risk Radar"
- Table with columns: Risk | Probability | Impact | Timeframe | D&O Factor | Status
- Each probability cell uses `class="{{ risk.probability_class }}"` for color coding

**9. `asc_606.html.j2`:**
- Guard: `{% if dossier_asc.asc_606_available %}`
- H3: "Revenue Recognition (ASC 606)"
- Table with columns: Element | Approach | Complexity | D&O Risk
- Each complexity cell uses `class="{{ element.complexity_class }}"`
- Below table: narrative about billings vs revenue gap: `{{ dossier_asc.billings_narrative }}`

**CSS class conventions (already in project stylesheets):**
- `risk-high` = red background
- `risk-medium` = yellow background
- `risk-low` = green background
- `do-callout` = emphasized callout box (may need to be added to stylesheet -- check existing styles first)
- `data-table` = standard table styling
- `revenue-flow` = monospace pre-formatted block

**NEVER use `| truncate()` on any D&O Risk, narrative, or analytical text.**
  </action>
  <verify>
    <automated>uv run python -c "
import jinja2, pathlib, sys
tpl_dir = pathlib.Path('src/do_uw/templates/html')
env = jinja2.Environment(loader=jinja2.FileSystemLoader(str(tpl_dir)))
errors = []
expected = [
    'sections/dossier/what_company_does.html.j2',
    'sections/dossier/money_flows.html.j2',
    'sections/dossier/revenue_model_card.html.j2',
    'sections/dossier/revenue_segments.html.j2',
    'sections/dossier/unit_economics.html.j2',
    'sections/dossier/revenue_waterfall.html.j2',
    'sections/dossier/emerging_risk_radar.html.j2',
    'sections/dossier/asc_606.html.j2',
]
for name in expected:
    try:
        env.get_template(name)
    except Exception as e:
        errors.append(f'{name}: {e}')
if errors:
    print('FAILED templates:'); [print(f'  {e}') for e in errors]; sys.exit(1)
print(f'All {len(expected)} subsection templates parse successfully as valid Jinja2')
"</automated>
  </verify>
  <acceptance_criteria>
    - src/do_uw/templates/html/sections/dossier.html.j2 contains `{% include "sections/dossier/` (8 includes)
    - src/do_uw/templates/html/sections/dossier/revenue_model_card.html.j2 contains `D&O Risk`
    - src/do_uw/templates/html/sections/dossier/revenue_model_card.html.j2 contains `{{ row.do_risk }}`
    - src/do_uw/templates/html/sections/dossier/money_flows.html.j2 contains `<pre`
    - src/do_uw/templates/html/sections/dossier/emerging_risk_radar.html.j2 contains `{{ risk.probability_class }}`
    - No template file contains `| truncate` on analytical content
    - 8 subsection template files exist in the dossier/ directory
    - All 8 subsection templates parse as valid Jinja2 without syntax errors
  </acceptance_criteria>
  <done>9 Jinja2 templates created. Section wrapper includes all 8 subsections. Every data table has a D&O Risk column. Templates are dumb renderers. All parse as valid Jinja2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create template rendering tests</name>
  <files>tests/stages/render/test_dossier_templates.py</files>
  <read_first>
    - src/do_uw/templates/html/sections/dossier/revenue_model_card.html.j2 (just created)
    - src/do_uw/templates/html/sections/dossier/what_company_does.html.j2 (just created)
    - src/do_uw/templates/html/sections/dossier/emerging_risk_radar.html.j2 (just created)
    - tests/stages/render/test_dossier_context_builders.py (fixture patterns from 118-04)
  </read_first>
  <behavior>
    - Test 1: what_company_does template renders business description and core D&O exposure
    - Test 2: money_flows template renders pre-formatted diagram
    - Test 3: revenue_model_card renders table with D&O Risk column header
    - Test 4: revenue_model_card renders row with risk-high class when risk_level=HIGH
    - Test 5: revenue_segments renders segment table + concentration assessment
    - Test 6: unit_economics renders metrics table + narrative paragraph
    - Test 7: revenue_waterfall renders waterfall table
    - Test 8: emerging_risk_radar renders risk table with D&O Factor column
    - Test 9: asc_606 renders ASC 606 table with billings narrative
    - Test 10: section wrapper includes all 8 subsections
    - Test 11: all templates handle available=False (render nothing)
  </behavior>
  <action>
Create `tests/stages/render/test_dossier_templates.py`:

1. **Setup:** Create a Jinja2 Environment pointing to the project templates directory. Use `jinja2.FileSystemLoader` with the correct path.

2. **Fixture data:** Build context dicts matching the context builder output shapes -- populated with realistic sample data for a fictitious company.

3. **11+ tests** that render each template with fixture data and assert:
   - Expected HTML elements present (table headers, CSS classes, content)
   - D&O Risk column headers appear in data table templates
   - `do_risk` text appears in rendered output
   - No `{{ ` or `{% ` jinja2 syntax errors in output (unresolved variables)
   - Available=False produces no visible content

4. **Template loading pattern:**
   ```python
   from jinja2 import Environment, FileSystemLoader
   import pathlib

   TEMPLATE_DIR = pathlib.Path(__file__).resolve().parents[3] / "src" / "do_uw" / "templates" / "html"
   env = Environment(loader=FileSystemLoader(str(TEMPLATE_DIR)))
   template = env.get_template("sections/dossier/revenue_model_card.html.j2")
   html = template.render(dossier_card=fixture_data)
   assert "D&amp;O Risk" in html or "D&O Risk" in html
   ```
  </action>
  <verify>
    <automated>uv run pytest tests/stages/render/test_dossier_templates.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/stages/render/test_dossier_templates.py has at least 11 test functions
    - Tests cover all 8 subsection templates + section wrapper
    - `uv run pytest tests/stages/render/test_dossier_templates.py -x -q` exits 0
  </acceptance_criteria>
  <done>All 9 templates render correctly with fixture data. D&O Risk columns present. Available=False handled. 11+ tests pass.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/stages/render/test_dossier_templates.py -x -q` -- all template tests pass
- `ls src/do_uw/templates/html/sections/dossier/*.html.j2 | wc -l` returns 8
- `grep -r "truncate" src/do_uw/templates/html/sections/dossier/` returns no matches on analytical content
</verification>

<success_criteria>
- 9 templates created (1 wrapper + 8 subsections)
- Every data table template has a D&O Risk column
- Revenue flow renders in monospace pre block
- Templates are dumb renderers with zero evaluative logic
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/118-revenue-model-company-intelligence-dossier/118-05-SUMMARY.md`
</output>
