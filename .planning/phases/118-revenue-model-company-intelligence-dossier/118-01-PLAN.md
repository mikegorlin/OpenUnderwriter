---
phase: 118-revenue-model-company-intelligence-dossier
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/models/dossier.py
  - src/do_uw/models/state.py
  - tests/models/test_dossier.py
autonomous: true
requirements:
  - DOSSIER-01
  - DOSSIER-02
  - DOSSIER-03
  - DOSSIER-04
  - DOSSIER-05
  - DOSSIER-06
  - DOSSIER-08
  - DOSSIER-09
must_haves:
  truths:
    - "DossierData model exists with all 8 subsection data structures"
    - "AnalysisState has a top-level `dossier` field of type DossierData"
    - "All sub-models (RevenueModelCardRow, ConcentrationDimension, EmergingRisk, etc.) are importable"
  artifacts:
    - path: "src/do_uw/models/dossier.py"
      provides: "DossierData and all sub-models"
      min_lines: 120
      exports: ["DossierData", "RevenueModelCardRow", "ConcentrationDimension", "EmergingRisk", "ASC606Element", "UnitEconomicMetric", "WaterfallRow", "RevenueSegmentDossier"]
    - path: "tests/models/test_dossier.py"
      provides: "Model instantiation and serialization tests"
      min_lines: 80
  key_links:
    - from: "src/do_uw/models/state.py"
      to: "src/do_uw/models/dossier.py"
      via: "import DossierData + Field on AnalysisState"
      pattern: "dossier.*DossierData"
---

<objective>
Create all Pydantic v2 models for the Company Intelligence Dossier and wire DossierData as a top-level field on AnalysisState.

Purpose: Establish the data contracts that all subsequent plans (extraction, enrichment, context builders, templates) depend on. Models-first ensures type safety across the pipeline.
Output: `src/do_uw/models/dossier.py` with DossierData + sub-models, updated `src/do_uw/models/state.py`, comprehensive model tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-revenue-model-company-intelligence-dossier/118-RESEARCH.md

<interfaces>
<!-- From src/do_uw/models/state.py (lines 24, 361-362): -->
```python
from do_uw.models.forward_looking import ForwardLookingData

class AnalysisState(BaseModel):
    forward_looking: ForwardLookingData = Field(
        default_factory=ForwardLookingData,
        description="Forward-looking risk data: statements, credibility, posture, triggers",
    )
```
<!-- Pattern: top-level field with default_factory, same as forward_looking -->

<!-- From src/do_uw/models/forward_looking.py (lines 1-60): -->
```python
from pydantic import BaseModel, ConfigDict, Field

class ForwardStatement(BaseModel):
    model_config = ConfigDict(frozen=False)
    metric_name: str = Field(default="", description="...")
    # ... all fields with defaults, type hints, descriptions
```
<!-- Pattern: ConfigDict(frozen=False), all fields have defaults, str Fields default to "" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DossierData model with all sub-models</name>
  <files>src/do_uw/models/dossier.py, tests/models/test_dossier.py</files>
  <read_first>
    - src/do_uw/models/forward_looking.py (pattern for cross-stage data models)
    - src/do_uw/models/state.py (see how ForwardLookingData is structured)
    - src/do_uw/models/company.py (existing fields: revenue_segments, customer_concentration, geographic_footprint)
  </read_first>
  <behavior>
    - Test 1: DossierData() instantiates with all defaults (empty lists, empty strings)
    - Test 2: DossierData with populated revenue_card serializes to dict and back
    - Test 3: RevenueModelCardRow accepts attribute="Model Type", value="B2B SaaS", do_risk="...", risk_level="HIGH"
    - Test 4: ConcentrationDimension accepts all 5 dimension types (Customer, Geographic, Product, Channel, Payer)
    - Test 5: EmergingRisk accepts probability/impact/timeframe/do_factor/status
    - Test 6: ASC606Element accepts element/approach/complexity/do_risk
    - Test 7: UnitEconomicMetric accepts metric/value/benchmark/assessment/do_risk
    - Test 8: WaterfallRow accepts label/value/delta/narrative
    - Test 9: RevenueSegmentDossier accepts segment_name, revenue_pct, growth_rate, rev_rec_method, do_exposure, risk_level
    - Test 10: DossierData.model_json_schema() produces valid JSON schema (no errors)
  </behavior>
  <action>
Create `src/do_uw/models/dossier.py` with these Pydantic v2 models following the ForwardLookingData pattern (ConfigDict(frozen=False), all fields with defaults):

1. **RevenueModelCardRow** -- attribute: str, value: str, do_risk: str, risk_level: str (default "LOW")
2. **ConcentrationDimension** -- dimension: str, metric: str, risk_level: str (default "LOW"), do_implication: str
3. **EmergingRisk** -- risk: str, probability: str, impact: str, timeframe: str, do_factor: str, status: str
4. **ASC606Element** -- element: str, approach: str, complexity: str (default "LOW"), do_risk: str
5. **UnitEconomicMetric** -- metric: str, value: str, benchmark: str, assessment: str, do_risk: str
6. **WaterfallRow** -- label: str, value: str, delta: str (default ""), narrative: str (default "")
7. **RevenueSegmentDossier** -- segment_name: str, revenue_pct: str, growth_rate: str, rev_rec_method: str, do_exposure: str, risk_level: str (default "LOW")
8. **DossierData** -- top-level model with fields for all 8 subsections (excluding 5.7 competitive landscape which is Phase 119):
   - 5.1: business_description_plain: str, core_do_exposure: str
   - 5.2: revenue_flow_diagram: str, revenue_flow_narrative: str
   - 5.3: revenue_card: list[RevenueModelCardRow]
   - 5.4: segment_dossiers: list[RevenueSegmentDossier], concentration_dimensions: list[ConcentrationDimension]
   - 5.5: unit_economics: list[UnitEconomicMetric], unit_economics_narrative: str
   - 5.6: waterfall_rows: list[WaterfallRow], waterfall_narrative: str
   - 5.8: emerging_risks: list[EmergingRisk]
   - 5.9: asc_606_elements: list[ASC606Element], billings_vs_revenue_narrative: str
   - Metadata: source_filings: list[str], extraction_confidence: str (default "MEDIUM")

All str fields default to "". All list fields default to default_factory=list. Module docstring references Phase 118.

Then create `tests/models/test_dossier.py` with the 10 test behaviors above. Use `pytest` parametrize for ConcentrationDimension dimension types.
  </action>
  <verify>
    <automated>uv run pytest tests/models/test_dossier.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/do_uw/models/dossier.py contains `class DossierData(BaseModel):`
    - src/do_uw/models/dossier.py contains `class RevenueModelCardRow(BaseModel):`
    - src/do_uw/models/dossier.py contains `class ConcentrationDimension(BaseModel):`
    - src/do_uw/models/dossier.py contains `class EmergingRisk(BaseModel):`
    - src/do_uw/models/dossier.py contains `class ASC606Element(BaseModel):`
    - src/do_uw/models/dossier.py contains `class UnitEconomicMetric(BaseModel):`
    - src/do_uw/models/dossier.py contains `class WaterfallRow(BaseModel):`
    - src/do_uw/models/dossier.py contains `class RevenueSegmentDossier(BaseModel):`
    - src/do_uw/models/dossier.py contains `model_config = ConfigDict(frozen=False)` (at least 8 occurrences)
    - tests/models/test_dossier.py contains at least 10 test functions
    - `uv run pytest tests/models/test_dossier.py -x -q` exits 0
  </acceptance_criteria>
  <done>All 8 Pydantic models defined with proper defaults, types, and descriptions. 10+ tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DossierData into AnalysisState</name>
  <files>src/do_uw/models/state.py</files>
  <read_first>
    - src/do_uw/models/state.py (current state model -- see forward_looking field at line 361)
    - src/do_uw/models/dossier.py (just created in Task 1)
  </read_first>
  <action>
Add DossierData to AnalysisState following the exact pattern of ForwardLookingData:

1. Add import at top of state.py (around line 24, near the ForwardLookingData import):
   `from do_uw.models.dossier import DossierData`

2. Add field to AnalysisState class (after the forward_looking field, around line 363):
   ```python
   dossier: DossierData = Field(
       default_factory=DossierData,
       description="Company intelligence dossier: revenue model, unit economics, emerging risks, ASC 606",
   )
   ```

3. Verify existing tests still pass -- the new field has a default_factory so it's backward compatible with existing serialized state.json files.
  </action>
  <verify>
    <automated>uv run pytest tests/models/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/do_uw/models/state.py contains `from do_uw.models.dossier import DossierData`
    - src/do_uw/models/state.py contains `dossier: DossierData = Field(`
    - src/do_uw/models/state.py contains `default_factory=DossierData`
    - `uv run pytest tests/models/ -x -q` exits 0
  </acceptance_criteria>
  <done>AnalysisState.dossier field exists, defaults to empty DossierData, all existing model tests pass.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/models/ -x -q` -- all model tests pass including new dossier tests
- `from do_uw.models.dossier import DossierData, RevenueModelCardRow, ConcentrationDimension, EmergingRisk, ASC606Element, UnitEconomicMetric, WaterfallRow, RevenueSegmentDossier` succeeds
- `from do_uw.models.state import AnalysisState; s = AnalysisState(); assert s.dossier is not None` succeeds
</verification>

<success_criteria>
- DossierData model with 8 sub-models defined and tested
- AnalysisState.dossier wired as top-level field
- All existing tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/118-revenue-model-company-intelligence-dossier/118-01-SUMMARY.md`
</output>
