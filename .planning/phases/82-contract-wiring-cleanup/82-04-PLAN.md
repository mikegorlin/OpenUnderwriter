---
phase: 82-contract-wiring-cleanup
plan: 04
type: execute
wave: 3
depends_on: ["82-02"]
files_modified:
  - src/do_uw/brain/brain_audit.py
  - src/do_uw/brain/templates/audit_report.html
  - src/do_uw/cli/cli_brain.py
  - tests/brain/test_brain_audit.py
autonomous: false
requirements: [SCHEMA-08]

must_haves:
  truths:
    - "Running brain audit produces an HTML report showing all signals with provenance metadata"
    - "The HTML report is filterable by section, signal_class, and tier"
    - "Every signal in the report shows data_source, formula (if populated), threshold_provenance, and render_target"
    - "The report has institutional-quality visual presentation matching worksheet output style"
    - "Signals with unattributed threshold_provenance are visually flagged for future attribution"
  artifacts:
    - path: "src/do_uw/brain/brain_audit.py"
      provides: "HTML audit report generation"
      contains: "audit_report"
    - path: "src/do_uw/brain/templates/audit_report.html"
      provides: "Jinja2 template for institutional-quality audit HTML"
      contains: "signal_class"
  key_links:
    - from: "src/do_uw/brain/brain_audit.py"
      to: "src/do_uw/brain/templates/audit_report.html"
      via: "Jinja2 rendering"
      pattern: "render.*template"
    - from: "src/do_uw/cli/cli_brain.py"
      to: "src/do_uw/brain/brain_audit.py"
      via: "CLI brain audit command"
      pattern: "audit"
---

<objective>
Build the institutional-quality HTML audit report for brain signal provenance.

Purpose: Make the brain's signal system fully auditable -- every signal's data source, evaluation formula, threshold rationale, and rendering target visible in a beautiful, filterable HTML report. Per user: "very clear and visually beautiful."
Output: `brain audit` CLI command produces an HTML audit report, checkpoint for visual quality.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-contract-wiring-cleanup/82-CONTEXT.md
@.planning/phases/82-contract-wiring-cleanup/82-RESEARCH.md
@.planning/phases/82-contract-wiring-cleanup/82-02-SUMMARY.md

<interfaces>
From src/do_uw/brain/brain_audit.py (492 lines):
- Existing audit functions that check signal wiring, orphans, coverage
- Will be extended with HTML report generation

From src/do_uw/brain/brain_signal_schema.py (post Plan 01):
```python
class BrainSignalProvenance(BaseModel):
    origin: str
    confidence: str | None = None
    formula: str = ""  # V3
    threshold_provenance: ThresholdProvenance | None = None  # V3
    render_target: str = ""  # V3
    data_source: str = ""  # V3
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build HTML audit report generator and Jinja2 template</name>
  <files>src/do_uw/brain/brain_audit.py, src/do_uw/brain/templates/audit_report.html, tests/brain/test_brain_audit.py, src/do_uw/cli/cli_brain.py</files>
  <action>
1. Create `src/do_uw/brain/templates/` directory if it doesn't exist.

2. Create `audit_report.html` Jinja2 template with institutional-quality styling:
   - Use the same CSS foundation as the worksheet HTML (professional fonts, clean tables, proper spacing)
   - Page header: "Brain Signal Audit Report" with generation timestamp and signal counts
   - **Filter bar** at top: CSS-only checkbox toggles for signal_class (foundational/evaluative/inference), tier (1/2/3), and section grouping. Use CSS `:checked ~ .signal-row` selectors for filtering without JavaScript. If pure CSS is too limiting, use minimal vanilla JS (< 50 lines) for filter logic.
   - **Summary stats card**: Total signals, by class, by tier, coverage percentages (% with data_source, % with formula, % with threshold_provenance)
   - **Signal table** grouped by manifest section (derived from group -> manifest group -> section):
     - Columns: ID, Name, Class, Tier, Group, Data Source, Formula, Threshold Provenance (source + rationale), Render Target
     - Row coloring: light yellow for "unattributed" threshold_provenance, white for attributed
     - Expandable detail rows (CSS-only `<details>`) showing full provenance block, depends_on list, field_path
   - **Provenance coverage section**: Table showing how many signals have each provenance field populated vs empty, with percentage bars
   - Responsive: readable on wide screens (table layout) and narrower screens (card layout fallback)

3. Add `generate_audit_html(output_path: Path) -> Path` function to `brain_audit.py`:
   - Load all signals via BrainLoader
   - Group by section (derive from group field -> manifest group -> manifest section)
   - Load manifest to get section ordering
   - Render Jinja2 template with signal data
   - Write to output_path (default: `output/brain_audit_report.html`)
   - Return the output path

4. Create `tests/brain/test_brain_audit.py`:
   - `test_audit_html_generates`: Call `generate_audit_html()` to a temp path, verify file exists and contains expected HTML structure
   - `test_audit_html_has_all_signals`: Parse generated HTML, verify signal count matches BrainLoader count
   - `test_audit_html_has_filter_controls`: Verify filter checkboxes exist in HTML
   - `test_audit_provenance_coverage_section`: Verify coverage stats section exists

5. Update `brain audit` CLI command in `cli_brain.py` to call `generate_audit_html()` and print the output path. If `brain audit` already exists as a CLI command, add `--html` flag to trigger HTML output (keep existing text output as default).
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/brain/test_brain_audit.py -x -q --timeout=30</automated>
  </verify>
  <done>brain audit --html produces an HTML report at output/brain_audit_report.html. Report contains all 476 signals grouped by section, filter controls, provenance coverage stats, and institutional-quality styling. Tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual quality check of audit HTML report</name>
  <files>output/brain_audit_report.html</files>
  <action>
  Human verifies the audit HTML report meets institutional quality standards.
  </action>
  <what-built>HTML audit report showing all 476 brain signals with complete provenance metadata, filterable by signal_class and tier, with institutional-quality visual presentation.</what-built>
  <how-to-verify>
    1. Run: `cd /Users/gorlin/projects/UW/do-uw && uv run python -m do_uw.cli brain audit --html` (or equivalent command)
    2. Open the generated HTML file in a browser
    3. Verify:
       - Professional, clean layout matching worksheet quality
       - Filter controls work (toggle signal classes, tiers)
       - Signal table shows: ID, Name, Class, Group, Data Source, Threshold Provenance
       - "Unattributed" threshold signals are highlighted yellow
       - Coverage stats section shows provenance field completion percentages
       - Expandable details show depends_on and field_path
       - All ~476 signals are present
    4. Open on a narrower window to verify responsive behavior
  </how-to-verify>
  <verify>Human visual inspection of audit report</verify>
  <done>Audit HTML report approved by user as institutional-quality.</done>
  <resume-signal>Type "approved" or describe visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
Audit report generates without error:
```bash
uv run python -m do_uw.cli brain audit --html
```

Tests pass:
```bash
uv run pytest tests/brain/test_brain_audit.py -x -q --timeout=30
```

Full brain test suite still passes:
```bash
uv run pytest tests/brain/ -x -q --timeout=60
```
</verification>

<success_criteria>
- `brain audit --html` produces an institutional-quality HTML report
- Report is filterable by signal_class, tier, and section
- All 476 signals shown with provenance metadata
- Unattributed thresholds are visually flagged
- Coverage statistics visible (% with data_source, formula, etc.)
- Human approves visual quality
</success_criteria>

<output>
After completion, create `.planning/phases/82-contract-wiring-cleanup/82-04-SUMMARY.md`
</output>
