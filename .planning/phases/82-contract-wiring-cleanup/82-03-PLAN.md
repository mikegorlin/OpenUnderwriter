---
phase: 82-contract-wiring-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["82-02"]
files_modified:
  - src/do_uw/stages/analyze/signal_engine.py
  - src/do_uw/stages/analyze/signal_disposition.py
  - src/do_uw/brain/chain_validator.py
  - src/do_uw/brain/brain_health.py
  - src/do_uw/brain/brain_audit.py
  - src/do_uw/cli/cli_brain_trace.py
  - tests/brain/test_brain_contract.py
  - tests/brain/test_foundational_coverage.py
  - tests/brain/test_chain_validator.py
  - tests/test_signal_forensic_wiring.py
  - tests/knowledge/test_enriched_roundtrip.py
autonomous: true
requirements: [SCHEMA-05, SCHEMA-07]

must_haves:
  truths:
    - "signal_engine uses signal_class instead of type for foundational skip logic"
    - "All consumers of facet field now read group field"
    - "brain audit, brain health, brain trace all work with group instead of facet"
    - "Running the full pipeline produces identical output before and after consumer updates"
    - "CI contract test fails the build if any ACTIVE signal is missing required v3 fields"
    - "All brain/ and signal-related tests pass"
  artifacts:
    - path: "src/do_uw/stages/analyze/signal_engine.py"
      provides: "signal_class-based foundational skip and execution ordering"
      contains: "signal_class"
    - path: "src/do_uw/brain/chain_validator.py"
      provides: "chain validation using signal_class"
      contains: "signal_class"
    - path: "tests/brain/test_brain_contract.py"
      provides: "Enforcing v3 contract test (no more dual-mode)"
      contains: "TestSignalGroupAssignment"
  key_links:
    - from: "src/do_uw/stages/analyze/signal_engine.py"
      to: "src/do_uw/brain/signals/**/*.yaml"
      via: "signal_class field read"
      pattern: "signal_class.*foundational"
    - from: "src/do_uw/brain/brain_audit.py"
      to: "src/do_uw/brain/signals/**/*.yaml"
      via: "group field for signal organization"
      pattern: "\\.group"
---

<objective>
Update all consumers from old fields (type, facet) to v3 fields (signal_class, group) and enforce the v3 contract.

Purpose: Complete the migration by ensuring all code reads v3 fields exclusively, removing any dual-mode fallbacks, and enabling CI enforcement.
Output: All consumers updated, tests cleaned to v3-only assertions, CI contract test enforces v3 fields.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-contract-wiring-cleanup/82-CONTEXT.md
@.planning/phases/82-contract-wiring-cleanup/82-RESEARCH.md
@.planning/phases/82-contract-wiring-cleanup/82-01-SUMMARY.md
@.planning/phases/82-contract-wiring-cleanup/82-02-SUMMARY.md

<interfaces>
<!-- Consumer update map from RESEARCH.md -->

Files referencing `type` field (change to `signal_class`):
- brain_signal_schema.py:303 -- type field definition (REMOVE)
- signal_engine.py:96 -- sig.get("type") == "foundational"
- signal_disposition.py:152 -- signal_type == "foundational"
- chain_validator.py:176,334,338 -- signal.type == "foundational"
- test_brain_contract.py (6 refs)
- test_foundational_coverage.py:47
- test_enriched_roundtrip.py:195

Files referencing `facet` field (change to `group`):
- brain_signal_schema.py:287 -- facet field definition (REMOVE)
- cli_brain_trace.py:255,258,323,326,458 -- signal_def.get("facet")
- brain_health.py:112-126 -- section YAML facet coverage
- brain_audit.py:347-379 -- orphaned signal check via sections
- test_brain_contract.py:107 -- sig.get("facet")
- test_signal_forensic_wiring.py:329 -- "facet" in s
- test_chain_validator.py:74 -- "facet": facet
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all production code consumers from type/facet to signal_class/group</name>
  <files>src/do_uw/stages/analyze/signal_engine.py, src/do_uw/stages/analyze/signal_disposition.py, src/do_uw/brain/chain_validator.py, src/do_uw/brain/brain_health.py, src/do_uw/brain/brain_audit.py, src/do_uw/cli/cli_brain_trace.py, src/do_uw/brain/brain_signal_schema.py</files>
  <action>
Update every consumer file listed in the RESEARCH.md consumer update map. For each file:

**signal_engine.py** (~3 changes):
- Replace `sig.get("type") == "foundational"` with `sig.get("signal_class") == "foundational"` everywhere
- If there are other references to `type` field (not Python's `type()` builtin), update them

**signal_disposition.py** (~1 change):
- Replace `signal_type == "foundational"` check to use `signal_class` attribute

**chain_validator.py** (~3 changes at lines 176, 334, 338):
- Replace `signal.type == "foundational"` with `signal.signal_class == "foundational"`
- Replace any `signal.facet` references with `signal.group`

**brain_health.py** (~2 changes at lines 112-126):
- Section YAML facet coverage check now uses `group` field from signals
- Replace facet-based grouping with group-based grouping
- If it reads section YAML to get facet lists, update to derive expected groups from manifest instead

**brain_audit.py** (~2 changes at lines 347-379):
- Orphaned signal check should use `group` field instead of section YAML facet membership
- Update any signal-to-section mapping to use group field

**cli_brain_trace.py** (~5 changes at lines 255, 258, 323, 326, 458):
- Replace all `signal_def.get("facet")` with `signal_def.get("group")`
- Update any display strings from "facet" to "group"

**brain_signal_schema.py**:
- Remove the `type` field definition (line 303): `type: Literal["evaluate", "foundational"]`
- Remove the `facet` field definition (line 287): `facet: str`
- Keep all v3 fields (group, signal_class, depends_on, field_path) as the replacements

IMPORTANT: Search for ALL references to these field names across the codebase using grep before committing. The research map may not be exhaustive. Run:
```bash
grep -rn '\.type\b.*==\|\.facet\b\|get("type")\|get("facet")' src/do_uw/ --include="*.py" | grep -v __pycache__ | grep -v '.pyc'
```
and update any additional hits.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/brain/ -x -q --timeout=60 && uv run python -c "from do_uw.brain.brain_unified_loader import BrainLoader; sigs = BrainLoader().load_signals(); print(f'{len(sigs)} signals loaded OK')"</automated>
  </verify>
  <done>All production code reads signal_class instead of type, group instead of facet. Old field definitions removed from BrainSignalEntry. All brain tests pass. Signals still load correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Update all test files and enforce v3 contract</name>
  <files>tests/brain/test_brain_contract.py, tests/brain/test_foundational_coverage.py, tests/brain/test_chain_validator.py, tests/test_signal_forensic_wiring.py, tests/knowledge/test_enriched_roundtrip.py</files>
  <action>
Update test files to use v3 fields exclusively (no more dual-mode):

**test_brain_contract.py**:
- Remove all dual-mode fallbacks added in Plan 01 (no more "accept either type or signal_class")
- All checks now use `signal_class` and `group` exclusively
- Remove `@pytest.mark.skip` from all v3 tests (migration is done)
- `TestSignalFacetOrGroupAssignment` -> rename back to `TestSignalGroupAssignment` (facet is gone)
- Remove `TestFacetIntegrity` class entirely (section YAML will be deleted in Phase 84; the contract test for group-to-manifest validation replaces it)
- Ensure `TestSignalGroupAssignment.test_group_values_exist_in_manifest` is active and passing

**test_foundational_coverage.py**:
- Replace `signal.get("type") == "foundational"` with `signal.get("signal_class") == "foundational"`

**test_chain_validator.py**:
- Replace `"facet": facet` in test data with `"group": group`
- Update any `type` references to `signal_class`

**test_signal_forensic_wiring.py**:
- Replace `"facet" in s` with `"group" in s`
- Update any `type` field references

**test_enriched_roundtrip.py**:
- Replace `s.get("type") != "foundational"` with `s.get("signal_class") != "foundational"`

After updating, verify NO remaining references to old field names in test files:
```bash
grep -rn '"type"\|"facet"\|\.type\b\|\.facet\b' tests/ --include="*.py" | grep -v __pycache__ | grep -v "threshold.type\|source_type\|work_type\|content_type\|file_type\|chart_type\|mime_type\|display_type\|check_type\|value_type"
```
(Filter out legitimate uses of "type" in other contexts like threshold.type)
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/brain/test_brain_contract.py tests/brain/test_foundational_coverage.py tests/brain/test_chain_validator.py tests/test_signal_forensic_wiring.py tests/knowledge/test_enriched_roundtrip.py -x -q --timeout=60</automated>
  </verify>
  <done>All test files use v3 fields exclusively. No skip markers remain. Contract test enforces v3 fields on all ACTIVE signals. Full test suite passes. No remaining references to old type/facet field names in signal-related contexts.</done>
</task>

</tasks>

<verification>
Full brain test suite:
```bash
uv run pytest tests/brain/ -x -q --timeout=60
```

Signal-related tests:
```bash
uv run pytest tests/test_signal_forensic_wiring.py tests/knowledge/test_enriched_roundtrip.py -x -q --timeout=30
```

No stale references to old fields:
```bash
grep -rn 'get("type")\|get("facet")\|\.type ==\|\.facet ==' src/do_uw/ tests/ --include="*.py" | grep -v __pycache__ | grep -v "threshold\|source_type\|work_type\|content_type\|file_type\|chart_type\|mime_type" | wc -l
# Should be 0
```

Pipeline still works (spot check):
```bash
uv run python -c "from do_uw.brain.brain_unified_loader import BrainLoader; sigs = BrainLoader().load_signals(); print(f'{len(sigs)} signals, all OK')"
```
</verification>

<success_criteria>
- All production consumers use signal_class and group exclusively
- Old type and facet fields removed from BrainSignalEntry schema
- All tests updated to v3 fields, no skip markers
- CI contract test actively enforces v3 fields on ACTIVE signals
- Full test suite passes
- No stale references to old field names
</success_criteria>

<output>
After completion, create `.planning/phases/82-contract-wiring-cleanup/82-03-SUMMARY.md`
</output>
