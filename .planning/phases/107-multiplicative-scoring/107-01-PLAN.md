---
phase: 107-multiplicative-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/models/scoring.py
  - src/do_uw/stages/score/scoring_lens.py
  - src/do_uw/stages/score/hae_scoring.py
  - src/do_uw/stages/score/hae_crf.py
  - tests/stages/score/test_hae_scoring.py
autonomous: true
requirements: [SCORE-01, SCORE-02, SCORE-03, SCORE-04]

must_haves:
  truths:
    - "H/A/E composite scores are computed from signal results using subcategory weights from scoring_model_design.yaml"
    - "Multiplicative product P = H x A x E captures interaction effects with 0.05 floor per composite"
    - "CRF vetoes override tier assignment via ELECTRE discordance regardless of composite scores"
    - "Decision tiers map from both P score ranges AND individual dimension criteria, most restrictive wins"
    - "Liberty calibration adjusts composite weights by attachment tier and product type"
  artifacts:
    - path: "src/do_uw/stages/score/scoring_lens.py"
      provides: "ScoringLens Protocol + ScoringLensResult model + HAETier enum"
      exports: ["ScoringLens", "ScoringLensResult", "HAETier"]
    - path: "src/do_uw/stages/score/hae_scoring.py"
      provides: "H/A/E composite computation, multiplicative model, tier assignment, Liberty calibration"
      exports: ["HAEScoringLens"]
    - path: "src/do_uw/stages/score/hae_crf.py"
      provides: "CRF ELECTRE discordance evaluation with time/claim-status awareness"
      exports: ["evaluate_crf_discordance", "CRFVetoResult"]
    - path: "src/do_uw/models/scoring.py"
      provides: "Extended ScoringResult with hae_result field"
      contains: "hae_result"
    - path: "tests/stages/score/test_hae_scoring.py"
      provides: "Unit tests for H/A/E composites, multiplicative model, CRF vetoes, tier assignment"
      min_lines: 100
  key_links:
    - from: "src/do_uw/stages/score/hae_scoring.py"
      to: "brain/framework/scoring_model_design.yaml"
      via: "YAML loading for subcategory weights and tier thresholds"
      pattern: "scoring_model_design"
    - from: "src/do_uw/stages/score/hae_scoring.py"
      to: "_signal_consumer.py"
      via: "SignalResultView.rap_class and rap_subcategory for signal classification"
      pattern: "rap_class|rap_subcategory"
    - from: "src/do_uw/stages/score/hae_crf.py"
      to: "brain/framework/scoring_model_design.yaml"
      via: "CRF veto catalog from crf_discordance section"
      pattern: "crf_veto_catalog"
    - from: "src/do_uw/stages/score/hae_scoring.py"
      to: "brain/framework/decision_framework.yaml"
      via: "5-tier recommendation outputs for tier assignment"
      pattern: "recommendation_tiers"
---

<objective>
Implement the H/A/E multiplicative scoring model with pluggable lens architecture, CRF ELECTRE discordance, and decision tier assignment.

Purpose: Replace the additive 10-factor scoring model with a multiplicative model that captures interaction effects between Host, Agent, and Environment risk dimensions. CRF vetoes are non-compensatory overrides. This is the core scoring engine for v7.0.

Output: Scoring lens protocol, H/A/E composite computation, multiplicative P = H x A x E, CRF discordance engine, 5-tier recommendation assignment, Liberty calibration -- all with comprehensive unit tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-multiplicative-scoring/107-CONTEXT.md
@.planning/phases/106-model-research/106-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From src/do_uw/stages/render/context_builders/_signal_consumer.py:
```python
@dataclass(frozen=True)
class SignalResultView:
    signal_id: str
    status: str           # "TRIGGERED", "CLEAR", "SKIPPED", "INFO"
    value: str | float | None
    threshold_level: str  # "red", "yellow", ""
    rap_class: str        # "host", "agent", "environment"
    rap_subcategory: str  # e.g. "host.identity", "agent.financial_conduct"
    mechanism: str        # "threshold", "peer_comparison", "trend", etc.
    # ... other fields
```

From src/do_uw/models/scoring.py:
```python
class ScoringResult(BaseModel):
    composite_score: float = 100.0
    quality_score: float = 100.0
    tier: TierClassification | None = None
    red_flags: list[RedFlagResult] = []
    # ... many other fields -- extend with hae_result
```

From src/do_uw/models/state.py (signal_results location):
```python
# state.analysis.signal_results: dict[str, Any]
# Keys are signal IDs like "FIN.ACCT.restatement", values are raw dicts
```

From brain/framework/scoring_model_design.yaml (key sections to consume):
- composites.signal_evaluation_mapping: RED=1.0, YELLOW=0.5, CLEAR=0.0, SKIPPED=null
- composites.host_subcategory_weights: 7 subcategories with weights summing to 1.0
- composites.agent_subcategory_weights: 7 subcategories with weights summing to 1.0
- composites.environment_subcategory_weights: 6 subcategories with weights summing to 1.0
- interaction_model.floor_adjustment.floor_value: 0.05
- crf_discordance.crf_veto_catalog: 6 CRF vetoes with signals and veto_target tiers
- tier_mapping.composite_to_tier: 6 tiers with p_range and individual_criteria
- liberty_calibration: attachment + product weight multipliers

From brain/framework/decision_framework.yaml:
- recommendation_tiers: 5 tiers (PREFERRED, STANDARD, ELEVATED, HIGH_RISK, PROHIBITED)
- recommendation_outputs: 6 dimensions (pricing, layer, terms, monitoring, referral, communication)

IMPORTANT: 107-CONTEXT.md specifies 5-tier model (PREFERRED / STANDARD / ELEVATED / HIGH_RISK / PROHIBITED),
overriding the design doc's 6-tier model. CAUTIOUS and ADVERSE are collapsed.
Use decision_framework.yaml's 5 tiers as the canonical tier set.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scoring lens protocol + Pydantic models</name>
  <files>
    src/do_uw/stages/score/scoring_lens.py,
    src/do_uw/models/scoring.py,
    tests/stages/score/test_hae_scoring.py
  </files>
  <behavior>
    - HAETier enum has exactly 5 values: PREFERRED, STANDARD, ELEVATED, HIGH_RISK, PROHIBITED (per CONTEXT.md 5-tier decision)
    - ScoringLensResult has fields: lens_name, tier (HAETier), composites dict, product score, confidence, recommendations dict, crf_vetoes list
    - ScoringLens Protocol has method: evaluate(signal_results: dict, ...) -> ScoringLensResult
    - ScoringResult.hae_result is Optional[ScoringLensResult], default None
    - HAETier ordering: PREFERRED < STANDARD < ELEVATED < HIGH_RISK < PROHIBITED (for max() operations)
  </behavior>
  <action>
    Create `src/do_uw/stages/score/scoring_lens.py`:
    1. Define `HAETier(StrEnum)` with 5 values matching decision_framework.yaml: PREFERRED, STANDARD, ELEVATED, HIGH_RISK, PROHIBITED. Include `__ge__`/`__gt__`/`__le__`/`__lt__` for tier comparison using ordering dict.
    2. Define `CRFVetoResult(BaseModel)` with: crf_id (str), condition (str), veto_target (HAETier), signals_matched (list[str]), is_active (bool), time_context (str -- "recent", "aging", "expired"), claim_status (str -- "NO_CLAIM", "CLAIM_FILED", "CLAIM_RESOLVED").
    3. Define `ScoringLensResult(BaseModel)` with: lens_name (str), tier (HAETier), composites (dict[str, float] -- keys "host", "agent", "environment"), product_score (float), confidence (str), recommendations (dict[str, str] -- keys matching 6 output dimensions from decision_framework.yaml), crf_vetoes (list[CRFVetoResult]), tier_source (str -- "composite", "individual", "crf_override"), individual_tier (HAETier | None), composite_tier (HAETier | None).
    4. Define `ScoringLens(Protocol)` with method: `evaluate(self, signal_results: dict[str, Any], *, company: Any | None = None, liberty_attachment: float | None = None, liberty_product: str | None = None) -> ScoringLensResult`

    Extend `src/do_uw/models/scoring.py`:
    5. Add `hae_result: ScoringLensResult | None = Field(default=None, description="H/A/E multiplicative scoring lens result")` to ScoringResult. Import ScoringLensResult from scoring_lens.
    6. Add to __all__ list.

    Write tests first in `tests/stages/score/test_hae_scoring.py`:
    - Test HAETier ordering (PREFERRED < PROHIBITED)
    - Test ScoringLensResult construction with all fields
    - Test ScoringResult.hae_result field exists and defaults to None
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run pytest tests/stages/score/test_hae_scoring.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - HAETier enum with 5 tiers and comparison operators
    - ScoringLens Protocol defining the pluggable lens contract
    - ScoringLensResult and CRFVetoResult Pydantic models
    - ScoringResult extended with hae_result field
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: H/A/E composite computation + multiplicative model + tier assignment</name>
  <files>
    src/do_uw/stages/score/hae_scoring.py,
    tests/stages/score/test_hae_scoring.py
  </files>
  <behavior>
    - compute_subcategory_score(): given signal results for signals in a subcategory, returns weighted average of RED=1.0/YELLOW=0.5/CLEAR=0.0, excluding SKIPPED. CRF signals get 3x weight. Returns None if all SKIPPED.
    - compute_category_composite(): given subcategory scores and weights from scoring_model_design.yaml, returns weighted average in [0,1]. Excludes null subcategories.
    - compute_multiplicative_product(): P = max(H, 0.05) x max(A, 0.05) x max(E, 0.05), result in [0,1]
    - classify_tier_from_composites(): dual-path -- tier from P range AND tier from individual criteria, most restrictive wins
    - Liberty calibration: adjusts composites by attachment tier and product type before multiplication
    - All CLEAR signals -> H=0, A=0, E=0 -> P=0.000125 (floor cubed) -> PREFERRED
    - Mixed signals: H=0.8, A=0.8, E=0.2 -> P=0.128 -> ELEVATED (per design doc example)
    - All HIGH signals: H=0.8, A=0.8, E=0.8 -> P=0.512 -> PROHIBITED
    - Individual criteria override: H=0.75, A=0.1, E=0.1 -> P is low but tier elevates due to host_above threshold
  </behavior>
  <action>
    Create `src/do_uw/stages/score/hae_scoring.py`:

    1. `_load_scoring_model() -> dict`: Load brain/framework/scoring_model_design.yaml via BrainLoader pattern (use yaml.safe_load on the file path). Cache as module-level singleton like _signal_consumer.py pattern.

    2. `_load_decision_framework() -> dict`: Load brain/framework/decision_framework.yaml. Cache similarly.

    3. `_load_rap_mapping() -> dict[str, tuple[str, str]]`: Load rap_signal_mapping.yaml, return dict mapping signal_id -> (rap_class, rap_subcategory). Cache.

    4. `_signal_score(status: str, threshold_level: str) -> float | None`: Map signal evaluation to numeric score per scoring_model_design.yaml signal_evaluation_mapping. TRIGGERED+red=1.0, TRIGGERED+yellow=0.5, CLEAR=0.0, SKIPPED=None. Use threshold_level to distinguish red vs yellow for TRIGGERED.

    5. `compute_subcategory_score(signal_results: dict[str, Any], subcategory: str, rap_mapping: dict, brain_signals: dict) -> float | None`: Iterate all signals in the given subcategory (from rap_mapping), compute signal_score for each, weight by claims_correlation if available else 1.0, multiply CRF signals by 3.0, return weighted average excluding None. Return None if all SKIPPED/missing.

    6. `compute_category_composite(subcategory_scores: dict[str, float | None], weights: dict[str, dict]) -> float`: Compute weighted average of non-null subcategory scores using weights from scoring_model_design.yaml. Return 0.0 if all null.

    7. `apply_liberty_calibration(h: float, a: float, e: float, attachment: float | None, product: str | None) -> tuple[float, float, float]`: Apply attachment-tier and product-type weight multipliers from scoring_model_design.yaml liberty_calibration section. Clamp each to [0, 1] after adjustment.

    8. `compute_multiplicative_product(h: float, a: float, e: float, floor: float = 0.05) -> float`: P = max(H, floor) x max(A, floor) x max(E, floor).

    9. `classify_tier_from_p(p: float) -> HAETier`: Map P to tier using 5-tier thresholds (adapted from 6-tier design doc to 5-tier per CONTEXT.md). Thresholds: PREFERRED [0, 0.01), STANDARD [0.01, 0.08), ELEVATED [0.08, 0.25), HIGH_RISK [0.25, 0.50), PROHIBITED [0.50, 1.0]. Note: These merge CAUTIOUS into ELEVATED and ADVERSE into HIGH_RISK from the 6-tier design.

    10. `classify_tier_from_individual(h: float, a: float, e: float) -> HAETier`: Apply individual dimension criteria. PREFERRED: h<=0.25, a<=0.20, e<=0.30. STANDARD: h<=0.45, a<=0.40, e<=0.50. ELEVATED: any>0.50. HIGH_RISK: any>0.70. PROHIBITED: via CRF only (returns HIGH_RISK as max from individual criteria alone).

    11. `_get_recommendations(tier: HAETier, framework: dict) -> dict[str, str]`: Look up the 6 recommendation outputs for the given tier from decision_framework.yaml.

    12. `class HAEScoringLens` implementing `ScoringLens` Protocol:
        - `evaluate(signal_results, *, company=None, liberty_attachment=None, liberty_product=None) -> ScoringLensResult`:
          a. Load configs (scoring model, decision framework, rap mapping, brain signals)
          b. Compute all subcategory scores (20 subcategories)
          c. Compute H, A, E composites from subcategory scores + weights
          d. Apply Liberty calibration if attachment/product provided
          e. Compute P = H x A x E with floor
          f. Classify tier from P (composite path)
          g. Classify tier from individual criteria
          h. NOTE: CRF discordance is applied externally (Task 3) -- do NOT apply CRF here
          i. Take max(composite_tier, individual_tier) as pre-CRF tier
          j. Build recommendations from tier
          k. Return ScoringLensResult with all fields populated

    Add tests to `tests/stages/score/test_hae_scoring.py`:
    - test_signal_score_mapping: RED=1.0, YELLOW=0.5, CLEAR=0.0, SKIPPED=None
    - test_subcategory_all_clear: returns 0.0
    - test_subcategory_all_skipped: returns None
    - test_subcategory_mixed: weighted average correct
    - test_subcategory_crf_3x_weight: CRF signals weighted 3x
    - test_category_composite_excludes_null: null subcategories excluded from average
    - test_multiplicative_all_low: P near 0
    - test_multiplicative_all_high: P=0.512 for H=A=E=0.8
    - test_multiplicative_floor: P=0.000125 for H=A=E=0.0 (floor cubed)
    - test_tier_from_p_preferred: P=0.005 -> PREFERRED
    - test_tier_from_p_prohibited: P=0.6 -> PROHIBITED
    - test_individual_override: high single dimension elevates tier
    - test_liberty_calibration_high_attachment: host weight increases
    - test_liberty_calibration_side_a: agent weight increases
    - test_full_lens_evaluate: end-to-end with mock signal results

    Use mock signal_results dicts and mock YAML loading (monkeypatch the module-level caches) to avoid requiring real brain data in tests.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run pytest tests/stages/score/test_hae_scoring.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <done>
    - H/A/E composite scores computed from classified signal results using weights from scoring_model_design.yaml
    - Multiplicative P = H x A x E with 0.05 floor per composite
    - Dual-path tier assignment: both P range and individual criteria, most restrictive wins
    - Liberty calibration adjusts weights by attachment and product
    - 15+ unit tests covering all computation paths, edge cases, and full lens evaluation
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CRF ELECTRE discordance with time/claim-status awareness</name>
  <files>
    src/do_uw/stages/score/hae_crf.py,
    tests/stages/score/test_hae_scoring.py
  </files>
  <behavior>
    - evaluate_crf_discordance(): checks 6 CRF veto categories from scoring_model_design.yaml against signal results
    - Each CRF is time-aware: recent event -> full veto, aging event -> reduced veto, expired DDL -> veto decays
    - Each CRF is claim-status-aware: NO_CLAIM / CLAIM_FILED / CLAIM_RESOLVED affects veto target
    - CRF-FRAUD (active SEC enforcement) -> PROHIBITED regardless of composites
    - CRF-RESTATEMENT (material) -> HIGH_RISK (elevated from ELEVATED in design doc per CONTEXT.md: "Material restatement should map to HIGH_RISK")
    - CRF-INSOLVENCY (zone of insolvency) -> PROHIBITED
    - CRF-DOJ (criminal investigation) -> PROHIBITED
    - CRF-MULTI (3+ CRFs simultaneously) -> HIGH_RISK
    - CRF-MATERIAL-WEAKNESS -> ELEVATED
    - Final tier = max(pre_crf_tier, max(active_crf_veto_targets))
    - When no CRFs fire, tier unchanged
    - Decay: implement structure with time_context input, exact parameters are placeholders (per CONTEXT.md deferred item)
  </behavior>
  <action>
    Create `src/do_uw/stages/score/hae_crf.py`:

    1. `_load_crf_catalog() -> list[dict]`: Load crf_veto_catalog from scoring_model_design.yaml. Cache.

    2. `_check_crf_signal_active(crf: dict, signal_results: dict[str, Any]) -> tuple[bool, list[str]]`: Check if any of the CRF's required signals are active (status=TRIGGERED, threshold_level=red). Return (is_active, matched_signal_ids).

    3. `_determine_time_context(signal_results: dict[str, Any], signals: list[str]) -> str`: Inspect signal details/evidence for temporal markers. Return "recent" (< 1 year), "aging" (1-3 years), or "expired" (> 3 years or DDL passed). Default to "recent" if temporal data unavailable (conservative).

    4. `_determine_claim_status(signal_results: dict[str, Any]) -> str`: Check litigation signals for claim filing status. Return "NO_CLAIM", "CLAIM_FILED", or "CLAIM_RESOLVED". Look at LIT.* signal details for claim indicators.

    5. `_adjust_veto_for_time_and_claims(base_veto: HAETier, time_context: str, claim_status: str) -> HAETier`: Apply time/claim adjustments:
       - "recent" + "NO_CLAIM": full veto (claim imminent)
       - "recent" + "CLAIM_FILED": full veto (exposure crystallized)
       - "aging" + "NO_CLAIM": reduce by 1 tier (exposure diminishing)
       - "aging" + "CLAIM_FILED": full veto (claim still active)
       - "expired" + "NO_CLAIM": reduce by 2 tiers (DDL likely passed)
       - "expired" + "CLAIM_RESOLVED": no veto (risk resolved)
       - Never reduce below STANDARD (CRF was triggered, always at least some concern)
       - Per CONTEXT.md: "Decay curves need more thought" -- these are initial parameters, calibration_required=true.

    6. `evaluate_crf_discordance(signal_results: dict[str, Any], pre_crf_tier: HAETier) -> tuple[HAETier, list[CRFVetoResult]]`:
       a. Load CRF catalog
       b. For each CRF: check signals, determine time context, determine claim status
       c. For CRF-MULTI: count active CRFs, fire if >= 3
       d. Build CRFVetoResult for each evaluated CRF
       e. Apply time/claim adjustments to each active veto target
       f. Final tier = max(pre_crf_tier, max(adjusted_veto_targets))
       g. Return (final_tier, all_veto_results)

    Add tests to `tests/stages/score/test_hae_scoring.py`:
    - test_no_crf_active: tier unchanged, empty veto list
    - test_fraud_crf_recent: PREFERRED -> PROHIBITED
    - test_restatement_crf_recent: STANDARD -> HIGH_RISK
    - test_insolvency_crf: -> PROHIBITED
    - test_material_weakness_crf: STANDARD -> ELEVATED
    - test_multi_crf_3_active: -> HIGH_RISK
    - test_crf_time_decay_aging: veto reduced by 1 tier
    - test_crf_time_decay_expired_no_claim: veto reduced by 2 tiers
    - test_crf_claim_filed_no_decay: full veto maintained when claim active
    - test_crf_resolved_expired: no veto
    - test_crf_override_max: multiple CRFs, highest wins
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run pytest tests/stages/score/test_hae_scoring.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <done>
    - CRF ELECTRE discordance evaluates 6 veto categories against signal results
    - Time-aware: recent/aging/expired temporal context affects veto strength
    - Claim-status-aware: NO_CLAIM/CLAIM_FILED/CLAIM_RESOLVED modifies veto behavior
    - Non-compensatory: CRF veto always overrides composite score when more restrictive
    - 11+ unit tests covering all CRF categories, time decay, claim status, and override logic
  </done>
</task>

</tasks>

<verification>
1. All tests pass: `uv run pytest tests/stages/score/test_hae_scoring.py -x -v`
2. No existing tests broken: `uv run pytest tests/stages/score/ -x --timeout=60`
3. Type checking: `uv run pyright src/do_uw/stages/score/scoring_lens.py src/do_uw/stages/score/hae_scoring.py src/do_uw/stages/score/hae_crf.py`
4. Lint: `uv run ruff check src/do_uw/stages/score/scoring_lens.py src/do_uw/stages/score/hae_scoring.py src/do_uw/stages/score/hae_crf.py`
5. No file exceeds 500 lines
</verification>

<success_criteria>
- ScoringLens Protocol defines the pluggable lens contract for all future scoring models
- HAEScoringLens computes H/A/E composites from 514 classified signals using empirically-weighted subcategories
- P = H x A x E product captures interaction effects (high+high+low != medium+medium+medium)
- CRF ELECTRE discordance is time-aware and claim-status-aware, overriding favorable profiles
- Decision tiers match the 5-tier model from decision_framework.yaml (PREFERRED through PROHIBITED)
- Liberty calibration adjusts weights for excess-only position and product type
- 25+ unit tests covering composites, multiplication, CRF, tiers, Liberty calibration
</success_criteria>

<output>
After completion, create `.planning/phases/107-multiplicative-scoring/107-01-SUMMARY.md`
</output>
