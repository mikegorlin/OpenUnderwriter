---
phase: 122-manifest-reorder-audit-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/brain/manifest_schema.py
  - src/do_uw/brain/output_manifest.yaml
  - tests/brain/test_manifest_schema.py
autonomous: true
requirements: [STRUCT-01, STRUCT-02, STRUCT-03, STRUCT-05, STRUCT-06]

must_haves:
  truths:
    - "ManifestSection model accepts a 'layer' field with values decision/analysis/audit"
    - "output_manifest.yaml lists sections in narrative story order: decision layer first, analysis middle, audit last"
    - "Litigation section appears at position 6 in the analysis layer (after Financial Health)"
    - "Governance section appears at position 7 in the analysis layer (after Litigation)"
    - "Company Profile and Intelligence Dossier are merged into a single 'company_operations' section"
    - "alternative_data and adversarial_critique sections are removed from manifest"
    - "ai_risk groups are absorbed into scoring section"
    - "Every section in manifest has a layer field"
  artifacts:
    - path: "src/do_uw/brain/manifest_schema.py"
      provides: "ManifestSection with layer field"
      contains: "layer"
    - path: "src/do_uw/brain/output_manifest.yaml"
      provides: "Reordered manifest with layer fields and merged sections"
      contains: "layer: decision"
    - path: "tests/brain/test_manifest_schema.py"
      provides: "Tests for layer field validation"
      contains: "test_layer_field"
  key_links:
    - from: "src/do_uw/brain/output_manifest.yaml"
      to: "src/do_uw/brain/manifest_schema.py"
      via: "ManifestSection.layer field"
      pattern: "layer.*decision|analysis|audit"
---

<objective>
Add the `layer` field to ManifestSection schema, reorder output_manifest.yaml sections into the narrative story flow, merge Company Profile + Intelligence Dossier into "Company & Operations", absorb AI Risk into Scoring, and remove dead sections (alternative_data, adversarial_critique).

Purpose: The manifest is the single source of truth for worksheet structure. Getting the order and layer classification right here means the template rendering (Plan 02) just reads what the manifest says.

Output: Updated manifest schema with `layer` field, reordered manifest YAML, updated tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-manifest-reorder-audit-layer/122-CONTEXT.md

<interfaces>
<!-- Key types the executor needs -->

From src/do_uw/brain/manifest_schema.py:
```python
class ManifestSection(BaseModel):
    model_config = ConfigDict(extra="forbid")
    id: str
    name: str
    template: str
    render_mode: Literal["required", "optional"] = "required"
    groups: list[ManifestGroup] = Field(default_factory=list)
    facets: list[ManifestFacet] = Field(default_factory=list)  # deprecated

class OutputManifest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    manifest_version: str
    sections: list[ManifestSection]
```

From src/do_uw/stages/render/section_renderer.py:
```python
def build_section_context(state: AnalysisState | None = None, **kwargs: Any) -> dict[str, Any]:
    # Returns {"section_context": {...}, "manifest_sections": [...]}
    # manifest_sections items: {"id", "name", "template", "render_mode", "facets"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add layer field to ManifestSection schema + tests</name>
  <files>src/do_uw/brain/manifest_schema.py, tests/brain/test_manifest_schema.py</files>
  <read_first>
    - src/do_uw/brain/manifest_schema.py (full file -- understand ManifestSection model, extra="forbid")
    - tests/brain/test_manifest_schema.py (full file -- understand existing test patterns)
  </read_first>
  <behavior>
    - Test: ManifestSection accepts layer="decision" and stores it
    - Test: ManifestSection accepts layer="analysis" and stores it
    - Test: ManifestSection accepts layer="audit" and stores it
    - Test: ManifestSection rejects layer="invalid" with ValidationError
    - Test: ManifestSection defaults layer to "analysis" when not provided (backward compat)
    - Test: load_manifest() returns sections with layer field populated
    - Test: Every section in the real output_manifest.yaml has a layer field (after Task 2 runs; mark xfail until then)
  </behavior>
  <action>
    1. In `manifest_schema.py`, add to `ManifestSection`:
       ```python
       layer: Literal["decision", "analysis", "audit"] = Field(
           default="analysis",
           description="Document tier: decision (top), analysis (middle), audit (collapsed appendix)",
       )
       ```
       Default "analysis" ensures backward compat -- existing sections without explicit layer work fine.

    2. In `tests/brain/test_manifest_schema.py`, add `TestManifestSectionLayer` class with tests for:
       - Valid layer values (decision, analysis, audit)
       - Invalid layer rejection
       - Default to "analysis" when omitted
       - Integration: load real manifest and verify all sections have layer field

    3. Run tests to confirm schema change works with existing manifest (all sections default to "analysis" until Task 2 adds explicit values).
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/brain/test_manifest_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "layer.*Literal.*decision.*analysis.*audit" src/do_uw/brain/manifest_schema.py
    - grep -q "test_layer_field\|TestManifestSectionLayer" tests/brain/test_manifest_schema.py
    - uv run pytest tests/brain/test_manifest_schema.py -x passes
  </acceptance_criteria>
  <done>ManifestSection schema accepts layer field with 3 valid values and defaults to "analysis". Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Reorder manifest YAML into narrative story flow with layer fields</name>
  <files>src/do_uw/brain/output_manifest.yaml</files>
  <read_first>
    - src/do_uw/brain/output_manifest.yaml (full file -- understand all current sections and groups)
    - src/do_uw/templates/html/sections/dossier.html.j2 (understand dossier template structure)
    - src/do_uw/templates/html/sections/company.html.j2 (understand company template structure)
    - src/do_uw/templates/html/sections/ai_risk.html.j2 (understand AI risk template -- groups will move to scoring)
  </read_first>
  <action>
    Rewrite output_manifest.yaml with these changes:

    **Section order (the risk story):**

    DECISION LAYER (layer: decision):
    1. `identity` -- Cover & Identity (layer: decision)
    2. `executive_summary` -- Executive Summary (layer: decision)
    3. `red_flags` -- Critical Risk Flags (layer: decision)

    ANALYSIS LAYER (layer: analysis):
    4. `company_operations` -- Company & Operations (layer: analysis) -- MERGED from business_profile + intelligence_dossier. Keep ALL groups from both sections combined. Set template to `sections/company.html.j2` (existing company template will be updated in Plan 02 to include dossier groups). Order: business_description, business_model, revenue_segments first (from business_profile), then dossier groups (what_company_does, money_flows, revenue_model_card, etc.), then remaining business_profile groups (exposure_factors, customer_concentration, etc.).
    5. `financial_health` -- Financial Health (layer: analysis, unchanged)
    6. `litigation` -- Litigation & Regulatory (layer: analysis, position 6 per STRUCT-02)
    7. `governance` -- Governance Assessment (layer: analysis, position 7 per STRUCT-03)
    8. `market_activity` -- Market Activity (layer: analysis)
    9. `forward_looking` -- Forward-Looking Indicators (layer: analysis)
    10. `scoring` -- Scoring & Risk Assessment (layer: analysis) -- ABSORB ai_risk groups: add overall_score, dimension_breakdown, competitive_position, forward_assessment, ai_risk_density_alerts as groups WITHIN scoring section (after existing scoring groups)

    AUDIT LAYER (layer: audit):
    11. `meeting_prep` -- Meeting Preparation (layer: audit)
    12. `sources` -- Sources (layer: audit)
    13. `qa_audit` -- QA Audit (layer: audit)
    14. `coverage` -- Coverage Analysis (layer: audit)

    **DELETE sections entirely:**
    - `alternative_data` (51 lines, empty per user -- absorb into relevant analysis sections or delete)
    - `adversarial_critique` (31 lines, stub per user -- delete until real content exists)

    **For the company_operations merge:**
    - New section id: `company_operations`
    - New section name: "Company & Operations"
    - template: `sections/company.html.j2`
    - Combine all groups from business_profile and intelligence_dossier into one groups list
    - business_profile groups come first (they're the structured data), then dossier groups (narrative/intelligence)
    - Keep every single group -- do NOT drop any

    **For scoring absorbing ai_risk:**
    - Add the 5 ai_risk groups (overall_score, dimension_breakdown, competitive_position, forward_assessment, ai_risk_density_alerts) to the scoring section's groups list, placed after the existing scoring groups but before scoring_checks/scoring_density_alerts

    **Bump manifest_version to '2.0'** to signal structural change.

    Verify the YAML is valid by running `uv run python -c "from do_uw.brain.manifest_schema import load_manifest; m = load_manifest(); print(f'{len(m.sections)} sections loaded')"`.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run python -c "
from do_uw.brain.manifest_schema import load_manifest
m = load_manifest()
ids = [s.id for s in m.sections]
print(f'Sections: {len(ids)}')
print(f'Order: {ids}')
assert 'company_operations' in ids, 'Missing company_operations'
assert 'business_profile' not in ids, 'business_profile should be merged'
assert 'intelligence_dossier' not in ids, 'intelligence_dossier should be merged'
assert 'alternative_data' not in ids, 'alternative_data should be deleted'
assert 'adversarial_critique' not in ids, 'adversarial_critique should be deleted'
assert 'ai_risk' not in ids, 'ai_risk should be absorbed into scoring'
lit_idx = ids.index('litigation')
gov_idx = ids.index('governance')
fin_idx = ids.index('financial_health')
assert lit_idx == fin_idx + 1, f'Litigation should follow Financial, got {lit_idx} vs {fin_idx}'
assert gov_idx == lit_idx + 1, f'Governance should follow Litigation, got {gov_idx} vs {lit_idx}'
for s in m.sections:
    assert s.layer in ('decision', 'analysis', 'audit'), f'{s.id} missing layer'
print('All assertions passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "layer: decision" src/do_uw/brain/output_manifest.yaml
    - grep -q "layer: analysis" src/do_uw/brain/output_manifest.yaml
    - grep -q "layer: audit" src/do_uw/brain/output_manifest.yaml
    - grep -q "company_operations" src/do_uw/brain/output_manifest.yaml
    - grep -qv "alternative_data" src/do_uw/brain/output_manifest.yaml (should NOT contain)
    - grep -qv "adversarial_critique" src/do_uw/brain/output_manifest.yaml (should NOT contain)
    - The verification script above passes
  </acceptance_criteria>
  <done>Manifest YAML has 14 sections in narrative story order with layer fields. Litigation at position 6, Governance at position 7. Company Profile + Dossier merged. AI Risk absorbed into Scoring. Dead sections removed. Schema validates.</done>
</task>

<task type="auto">
  <name>Task 3: Fix existing tests that reference deleted/renamed section IDs</name>
  <files>tests/brain/test_manifest_schema.py, tests/brain/test_template_facet_audit.py, tests/brain/test_brain_contract.py, tests/brain/test_chain_validator.py, tests/stages/render/test_section_renderer.py, tests/stages/render/test_manifest_rendering.py</files>
  <read_first>
    - tests/brain/test_template_facet_audit.py (check for hardcoded section IDs)
    - tests/brain/test_brain_contract.py (check for hardcoded section IDs)
    - tests/brain/test_chain_validator.py (check for hardcoded section IDs)
    - tests/stages/render/test_section_renderer.py (check for hardcoded section IDs)
    - tests/stages/render/test_manifest_rendering.py (check for hardcoded section IDs)
  </read_first>
  <action>
    After Tasks 1-2 change the manifest, some tests may reference old section IDs (business_profile, intelligence_dossier, alternative_data, adversarial_critique, ai_risk) or expect a specific section count/order.

    For each test file:
    1. Search for references to deleted/renamed section IDs
    2. Update `business_profile` references to `company_operations`
    3. Update `intelligence_dossier` references to `company_operations`
    4. Remove references to `alternative_data`, `adversarial_critique`, `ai_risk` as standalone sections
    5. Update any section count assertions (was 18 sections, now 14)
    6. Update any section order assertions

    Also update `section_renderer.py` to pass `layer` in the manifest_sections list:
    - In `build_section_context()`, add `"layer": ms.layer` to the manifest_sections dict for each section

    Run full manifest-related test suite to confirm all green.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/brain/test_manifest_schema.py tests/brain/test_template_facet_audit.py tests/brain/test_brain_contract.py tests/brain/test_chain_validator.py tests/stages/render/test_section_renderer.py tests/stages/render/test_manifest_rendering.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q '"layer": ms.layer' src/do_uw/stages/render/section_renderer.py OR grep -q "'layer': ms.layer" src/do_uw/stages/render/section_renderer.py
    - All 6 test files pass without failures
    - No references to "business_profile" or "intelligence_dossier" or "alternative_data" or "adversarial_critique" as section IDs remain in test files (references as group IDs within company_operations are fine)
  </acceptance_criteria>
  <done>All manifest-related tests pass with the new section structure. Section renderer passes layer info to templates.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/brain/ -x -q` -- all brain tests pass
2. `uv run pytest tests/stages/render/test_section_renderer.py tests/stages/render/test_manifest_rendering.py -x -q` -- render tests pass
3. `uv run python -c "from do_uw.brain.manifest_schema import load_manifest; m = load_manifest(); [print(f'{s.layer}: {s.id}') for s in m.sections]"` -- shows correct layer/order
</verification>

<success_criteria>
- ManifestSection has layer field (decision/analysis/audit) defaulting to "analysis"
- Manifest YAML has 14 sections in story order with explicit layer on every section
- Litigation at analysis position 6, Governance at analysis position 7
- company_operations merges both Company Profile and Intelligence Dossier groups
- No alternative_data, adversarial_critique, or standalone ai_risk sections
- All manifest and render tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/122-manifest-reorder-audit-layer/122-01-SUMMARY.md`
</output>
