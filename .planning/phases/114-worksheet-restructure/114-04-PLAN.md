---
phase: 114-worksheet-restructure
plan: 04
type: execute
wave: 3
depends_on: ["114-02", "114-03"]
files_modified:
  - src/do_uw/templates/html/sidebar.css
  - src/do_uw/templates/html/styles.css
  - src/do_uw/templates/html/base.html.j2
  - tests/test_visual_regression.py
  - tests/stages/render/test_print_divergence.py
  - tests/stages/render/test_reading_paths.py
autonomous: false
requirements:
  - WS-04
  - WS-06
  - WS-08

must_haves:
  truths:
    - "Sidebar TOC has audience-oriented groups (Overview, Analysis, Appendix) with working anchor links"
    - "Screen HTML has interactive navigation: collapsible sections, signal drill-down, scroll-spy sidebar"
    - "Print/PDF hides sidebar, hides signal drill-down panels, expands content sections, linearizes CRF banner"
    - "Golden baselines updated to reflect new scorecard, executive brief, CRF banner, epistemological trace, decision record sections"
    - "CRF alert pills link to correct section anchors"
    - "UW reading path: scorecard -> CRF alerts -> section of concern -> epistemological trace"
    - "Secondary reader path: executive brief -> decision record"
  artifacts:
    - path: "tests/stages/render/test_print_divergence.py"
      provides: "Screen vs print UX divergence tests"
      min_lines: 30
    - path: "tests/stages/render/test_reading_paths.py"
      provides: "Reading path and navigation tests"
      min_lines: 30
  key_links:
    - from: "base.html.j2 sidebar"
      to: "scorecard.html.j2"
      via: "href=#scorecard anchor link"
      pattern: 'href="#scorecard"'
    - from: "base.html.j2 sidebar"
      to: "epistemological_trace.html.j2"
      via: "href=#epistemological-trace anchor link"
      pattern: 'href="#epistemological-trace"'
    - from: "crf_banner.html.j2"
      to: "domain section templates"
      via: "crf_alerts[].section_link anchor references"
      pattern: "section_link"
---

<objective>
Polish the print/PDF experience, update golden baselines for visual regression, and validate reading paths for both primary UW and secondary reader audiences.

Purpose: Ensure the restructured worksheet works as a polished deliverable in both screen (interactive) and print/PDF (linear) modes. The worksheet is the DELIVERABLE -- attached to emails, filed as official record. Print quality is non-negotiable.

Output: Updated print CSS, reading path tests, visual regression baselines, print divergence tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-worksheet-restructure/114-CONTEXT.md
@.planning/phases/114-worksheet-restructure/114-RESEARCH.md
@.planning/phases/114-worksheet-restructure/114-01-SUMMARY.md
@.planning/phases/114-worksheet-restructure/114-02-SUMMARY.md
@.planning/phases/114-worksheet-restructure/114-03-SUMMARY.md

@src/do_uw/templates/html/base.html.j2
@src/do_uw/templates/html/sidebar.css
@src/do_uw/templates/html/styles.css
@tests/test_visual_regression.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Print/PDF CSS divergence + reading path tests</name>
  <files>
    src/do_uw/templates/html/sidebar.css,
    src/do_uw/templates/html/styles.css,
    src/do_uw/templates/html/base.html.j2,
    tests/stages/render/test_print_divergence.py,
    tests/stages/render/test_reading_paths.py
  </files>
  <action>
    Update @media print rules in styles.css:
    - Scorecard: `break-after: page` (single page), scale radar chart for print: `transform: scale(0.85)` inside `.scorecard-radar`, set explicit max-height on `.scorecard-grid` to prevent 2-page overflow
    - Executive brief: `break-after: page`
    - CRF banner: not sticky in print, rendered as bordered box with print-safe background
    - Signal drill-down: `details.signal-drilldown { display: none; }` in print (epistemological trace appendix serves this purpose in print)
    - Heatmap: `print-color-adjust: exact` on all cells and badges
    - Epistemological trace: `font-size: 7pt`, `thead { display: table-header-group }` for repeating headers
    - Decision record: `break-before: page`, ensure bordered boxes visible in print
    - Domain sections: keep existing `break-before: page` behavior

    Update sidebar.css:
    - Add sidebar entries for new sections (scorecard, executive-brief, crf-banner, decision-record, epistemological-trace)
    - Sidebar groups: "Overview" (bold header), "Analysis" (bold header), "Appendix" (bold header)
    - Sidebar alert style: `.sidebar-alert { color: #D97706; font-weight: 600; }` for CRF link
    - Sidebar still hidden in @media print (existing behavior)

    Update base.html.j2:
    - Verify pdf_mode details expansion excludes signal-drilldown:
      `document.querySelectorAll('details:not(.signal-drilldown)').forEach(...)`
    - Verify IntersectionObserver tracks new sections (scorecard, executive-brief, etc.) -- since it queries `section[id]`, new sections just need `<section id="...">` wrapping (which they should have from Plan 02/03)
    - Add new section IDs to the IntersectionObserver selector if needed

    Create test_print_divergence.py:
    - Test: @media print rules exist for scorecard (break-after), executive-brief (break-after), decision-record (break-before)
    - Test: signal-drilldown has display:none in print CSS
    - Test: heatmap cells have print-color-adjust: exact
    - Test: sidebar hidden in print (existing behavior preserved)
    - Test: CRF banner not sticky in print
    - Test: trace table thead has table-header-group display
    - Method: Parse styles.css content and verify @media print blocks contain expected rules. Alternative: render HTML with pdf_mode=True and verify details expansion excludes signal-drilldown class.

    Create test_reading_paths.py:
    - Test: sidebar TOC contains links to #scorecard, #executive-brief, #crf-banner (conditional), all domain sections, #decision-record, #epistemological-trace
    - Test: sidebar has 3 group headers (Overview, Analysis, Appendix)
    - Test: CRF alert pills have href attributes matching known section anchors (#financial-health, #litigation, etc.)
    - Test: section anchor IDs exist in rendered HTML for all sidebar links
    - Method: Render full template with mock context and parse HTML for anchor presence and link validity.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/render/test_print_divergence.py tests/stages/render/test_reading_paths.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>Print CSS ensures clean page breaks, signal drill-downs hidden in print, sidebar links match section anchors, reading paths validated for both audiences</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete worksheet restructure</name>
  <files>tests/test_visual_regression.py</files>
  <action>Run pipeline and visually verify the complete restructured worksheet in browser and print preview. Update golden baselines after approval.</action>
  <what-built>
    Complete worksheet restructure: scorecard page, executive brief, CRF alert bar, H/A/E dimension badges, signal heatmap, signal drill-down panels, epistemological trace appendix, decision record, updated sidebar, print/PDF CSS.
  </what-built>
  <how-to-verify>
    1. Run a test pipeline: `underwrite RPM --fresh` (or load existing state.json)
    2. Open the generated HTML worksheet in browser
    3. Verify Page 1 (Risk Scorecard):
       - Tier badge prominent and readable
       - H/A/E radar chart rendered
       - 10-factor scores visible
       - Top concern signals listed
       - Key metrics strip (market cap, revenue, etc.)
       - Signal heatmap with colored cells
    4. Verify Page 2 (Executive Brief):
       - Self-contained summary readable without scrolling further
       - Company profile, key risks, notable findings present
    5. Verify CRF Alert Bar (if any alerts):
       - Dark banner below scorecard
       - Alert pills link to correct sections (click to verify)
    6. Verify Domain Sections:
       - H/A/E dimension badges in section headers (H=blue, A=red, E=green)
       - Click signal references to verify drill-down panels expand with provenance data
    7. Verify Sidebar:
       - Overview/Analysis/Appendix group headers
       - Scroll-spy highlights current section
       - All links navigate to correct sections
    8. Verify Epistemological Trace Appendix:
       - All signals present (triggered + clean + skipped)
       - Grouped by H/A/E
       - All 9 columns populated
    9. Verify Decision Record:
       - Posture options (BIND/DECLINE/REFER/TERMS) visible
       - Tier distribution reference shown
       - "No system recommendation" text present
    10. Print/PDF check: Open print preview (Ctrl+P)
        - Scorecard fits on one page
        - Executive brief on its own page
        - Signal drill-down panels NOT expanded
        - Sidebar hidden
        - Heatmap colors preserved
        - Trace table headers repeat
    11. Update golden baselines if satisfied: `VISUAL_REGRESSION=1 uv run pytest tests/test_visual_regression.py --update-golden`
  </how-to-verify>
  <verify>Visual inspection confirms all components render correctly in browser and print preview</verify>
  <done>Human approved worksheet quality, golden baselines updated</done>
  <resume-signal>Type "approved" to update golden baselines, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `uv run pytest tests/stages/render/ -x -q` passes (all render tests)
- `uv run pytest -x -q` passes (full test suite)
- Visual inspection confirms scorecard, brief, CRF bar, trace, decision record render correctly
- Print preview confirms page breaks, hidden drill-downs, preserved colors
- Golden baselines regenerated after visual approval
</verification>

<success_criteria>
1. Print CSS creates clean page breaks (scorecard, brief each on own page)
2. Signal drill-down panels hidden in print/PDF
3. Sidebar TOC has audience-oriented groups with working links
4. All section anchors reachable from sidebar
5. CRF alert pills navigate to correct sections
6. Visual regression baselines updated
7. Human verification confirms worksheet quality meets "most amazing, fully produced" bar
</success_criteria>

<output>
After completion, create `.planning/phases/114-worksheet-restructure/114-04-SUMMARY.md`
</output>
