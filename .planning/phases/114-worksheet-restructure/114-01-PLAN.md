---
phase: 114-worksheet-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/stages/render/html_context_assembly.py
  - src/do_uw/stages/render/html_renderer.py
  - src/do_uw/stages/render/context_builders/scorecard_context.py
  - src/do_uw/stages/render/context_builders/heatmap_context.py
  - src/do_uw/stages/render/context_builders/crf_bar_context.py
  - src/do_uw/stages/render/context_builders/epistemological_trace.py
  - src/do_uw/stages/render/context_builders/decision_context.py
  - tests/stages/render/test_scorecard_context.py
  - tests/stages/render/test_heatmap_context.py
  - tests/stages/render/test_crf_bar_context.py
  - tests/stages/render/test_epistemological_trace.py
  - tests/stages/render/test_decision_context.py
autonomous: true
requirements:
  - WS-01
  - WS-03
  - WS-07

must_haves:
  truths:
    - "html_renderer.py is under 500 lines after split"
    - "5 new context builders produce typed dicts for scorecard, heatmap, CRF bar, epistemological trace, and decision record"
    - "All existing render tests still pass after html_renderer split"
    - "Scorecard context extracts tier, H/A/E composites, factor scores, top concerns, and metrics strip from state"
    - "Heatmap context groups all signals by H/A/E dimension and subcategory into a grid structure"
    - "CRF bar context extracts CRF vetoes and red flags with section links"
    - "Epistemological trace context produces per-signal provenance rows for ALL signals (triggered, clean, skipped)"
    - "Decision context provides tier distribution reference data and posture fields"
  artifacts:
    - path: "src/do_uw/stages/render/html_context_assembly.py"
      provides: "Extracted build_html_context + helpers from html_renderer.py"
      min_lines: 200
    - path: "src/do_uw/stages/render/context_builders/scorecard_context.py"
      provides: "Scorecard page data assembly"
      exports: ["build_scorecard_context"]
    - path: "src/do_uw/stages/render/context_builders/heatmap_context.py"
      provides: "Signal heatmap grid data"
      exports: ["build_heatmap_context"]
    - path: "src/do_uw/stages/render/context_builders/crf_bar_context.py"
      provides: "CRF alert bar data"
      exports: ["build_crf_bar_context"]
    - path: "src/do_uw/stages/render/context_builders/epistemological_trace.py"
      provides: "Full signal provenance table data"
      exports: ["build_epistemological_trace"]
    - path: "src/do_uw/stages/render/context_builders/decision_context.py"
      provides: "Decision record page data"
      exports: ["build_decision_context"]
  key_links:
    - from: "src/do_uw/stages/render/html_renderer.py"
      to: "src/do_uw/stages/render/html_context_assembly.py"
      via: "import build_html_context"
      pattern: "from.*html_context_assembly.*import.*build_html_context"
    - from: "src/do_uw/stages/render/html_context_assembly.py"
      to: "context_builders/scorecard_context.py"
      via: "build_scorecard_context call"
      pattern: "build_scorecard_context"
    - from: "context_builders/epistemological_trace.py"
      to: "context_builders/_signal_consumer.py"
      via: "get_signal_result for provenance"
      pattern: "get_signal_result"
---

<objective>
Split html_renderer.py (697 lines, exceeds 500-line limit) and create 5 new context builders that provide data for the restructured worksheet components.

Purpose: Establish the data layer for Phase 114's worksheet restructure. All new template components (scorecard, heatmap, CRF bar, epistemological trace, decision record) need typed context dicts before templates can consume them.

Output: html_renderer.py under 500 lines, html_context_assembly.py with extracted context logic, 5 new context builders with unit tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-worksheet-restructure/114-CONTEXT.md
@.planning/phases/114-worksheet-restructure/114-RESEARCH.md

@src/do_uw/stages/render/html_renderer.py
@src/do_uw/stages/render/md_renderer.py
@src/do_uw/stages/render/context_builders/hae_context.py
@src/do_uw/stages/render/context_builders/_signal_consumer.py
@src/do_uw/stages/render/context_builders/_signal_fallback.py
@src/do_uw/stages/render/context_builders/scoring.py
@src/do_uw/stages/render/context_builders/pattern_context.py
@src/do_uw/stages/render/context_builders/severity_context.py
@src/do_uw/models/state.py

<interfaces>
<!-- Key types and contracts from existing codebase -->

From src/do_uw/stages/render/context_builders/_signal_consumer.py:
- get_signal_result(signal_id, signal_results) -> SignalResultView | None
- SignalResultView fields: signal_id, status, value, source, confidence, threshold_level, threshold_context, epistemology_rule_origin, epistemology_threshold_basis, factors, rap_class, rap_subcategory

From src/do_uw/stages/render/context_builders/hae_context.py:
- build_hae_context(state: AnalysisState) -> dict[str, Any]
  Returns: hae_available, host_composite, agent_composite, environment_composite, probability, tier, crf_vetoes, radar_labels, radar_values

From src/do_uw/stages/render/html_renderer.py:
- build_html_context(state: AnalysisState, chart_dir: Path | None = None) -> dict[str, Any]
  -- Currently 250+ lines (lines 210-461), needs extraction to html_context_assembly.py
- _render_html_template(context: dict[str, Any]) -> str
  -- Keeps in html_renderer.py (lines 464-560)
- render_html_pdf(state: AnalysisState, ...) -> Path
  -- Keeps in html_renderer.py (lines 562-697)

From src/do_uw/models/state.py:
- AnalysisState.scoring: ScoringResult | None
  - .tier_label, .quality_score, .composite_score, .factor_scores, .red_flags
  - .hae_result: ScoringLensResult | None (composites, crf_vetoes, tier, product_score)
  - .severity_result: SeverityLensResult | None
  - .pattern_engine_result: PatternEngineResult | None
- AnalysisState.analysis: AnalysisResult | None
  - .signal_results: dict[str, Any]
- AnalysisState.extracted: ExtractedData | None
  - .financials, .market, .governance, .litigation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split html_renderer.py into html_context_assembly.py + slim renderer</name>
  <files>src/do_uw/stages/render/html_context_assembly.py, src/do_uw/stages/render/html_renderer.py</files>
  <action>
    Extract `build_html_context()` and all its helper functions (_extract_return_decomposition, _extract_chart_metrics, _build_filing_date_lookup, _risk_class, _ZONE_STATUS_MAP, and any other private helpers called only by build_html_context) into a new `html_context_assembly.py` module.

    In html_renderer.py, keep only:
    - `_render_html_template(context)` (lines ~464-560)
    - `render_html_pdf(state, ...)` (lines ~562-697)
    - Any helpers used exclusively by those two functions (PDF-specific code)
    - Add `from do_uw.stages.render.html_context_assembly import build_html_context` import
    - Re-export `build_html_context` for backward compatibility: add to __all__ or keep a thin wrapper

    In html_context_assembly.py:
    - Move all imports needed by build_html_context
    - Move build_html_context and all its private helpers
    - Add calls to the 5 new context builders (scorecard, heatmap, crf_bar, epistemological_trace, decision) -- import and call each, merging results into context dict
    - Each new builder call wrapped in try/except with graceful degradation (logger.debug, set defaults)

    Verify both files are under 500 lines. The split should be mechanical -- no behavior changes.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/render/ -x -q --timeout=60 2>&1 | tail -20</automated>
  </verify>
  <done>html_renderer.py under 500 lines, html_context_assembly.py under 500 lines, all existing render tests pass, build_html_context importable from both modules</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create 5 new context builders with unit tests</name>
  <files>
    src/do_uw/stages/render/context_builders/scorecard_context.py,
    src/do_uw/stages/render/context_builders/heatmap_context.py,
    src/do_uw/stages/render/context_builders/crf_bar_context.py,
    src/do_uw/stages/render/context_builders/epistemological_trace.py,
    src/do_uw/stages/render/context_builders/decision_context.py,
    tests/stages/render/test_scorecard_context.py,
    tests/stages/render/test_heatmap_context.py,
    tests/stages/render/test_crf_bar_context.py,
    tests/stages/render/test_epistemological_trace.py,
    tests/stages/render/test_decision_context.py
  </files>
  <behavior>
    scorecard_context:
    - Test: state with scoring -> returns scorecard_available=True, tier, quality_score, factors_summary list, top_concerns sorted by severity (max 8), metrics_strip
    - Test: state without scoring -> returns scorecard_available=False
    - Test: top_concerns only includes TRIGGERED signals, sorted by threshold_level descending
    - Test: metrics_strip includes market_cap, revenue, employees, years_public from company/financials

    heatmap_context:
    - Test: signals grouped into 3 H/A/E buckets, each subdivided by rap_subcategory
    - Test: each cell has id, status, level, value
    - Test: empty signal_results -> returns heatmap_available=False
    - Test: SKIPPED signals included (shown as gray cells)

    crf_bar_context:
    - Test: CRF vetoes from hae_result extracted with CRITICAL severity
    - Test: red_flags from scoring extracted with HIGH severity
    - Test: each alert has section_link (mapped from flag type to section anchor)
    - Test: no scoring -> returns empty alerts list

    epistemological_trace:
    - Test: ALL signals (triggered, clean, skipped) appear in trace rows
    - Test: rows grouped by H/A/E dimension (host, agent, environment)
    - Test: each row has: signal_id, status, raw_data, source, confidence, source_type, threshold, rule_origin, evaluation, score_contribution, rap_class, rap_subcategory
    - Test: source_type derived from confidence (HIGH->audited, MEDIUM->unaudited, LOW->web/derived)
    - Test: trace_total count matches total signal count

    decision_context:
    - Test: returns tier_distribution with industry-standard D&O tier percentages
    - Test: returns current_tier from scoring
    - Test: returns posture_fields (empty dict for UW to fill)
    - Test: no scoring -> returns decision_available=False
  </behavior>
  <action>
    Create each context builder following the established pattern (see hae_context.py):
    - Module docstring explaining purpose
    - Single public function: build_X_context(state: AnalysisState) -> dict[str, Any]
    - Use _signal_consumer.py's get_signal_result for signal data access
    - Use brain signal YAML data via _signal_consumer for epistemology fields
    - Keep each file under 150 lines

    For scorecard_context.py: Extract tier badge, H/A/E composites (reuse hae_context), 10-factor scores table, top 8 triggered signals sorted by severity, key financial metrics strip (market_cap, revenue, employees, years_public, sector, beta).

    For heatmap_context.py: Group ALL signal results by rap_class (host/agent/environment) then by rap_subcategory. Each cell = {id, status, level, value}. Use SignalResultView for typed access.

    For crf_bar_context.py: Extract CRF vetoes from hae_result + red_flags from scoring. Map each to a section anchor: CRF-RESTATEMENT -> #financial-health, CRF-SEC-ENFORCEMENT -> #litigation, CRF-MATERIAL-WEAKNESS -> #financial-health, CRF-MULTI -> #scoring, etc.

    For epistemological_trace.py: Iterate ALL signal_results. For each, build row with full provenance columns from SignalResultView. Group by rap_class. Sort within group by: TRIGGERED first, then by signal_id. Derive source_type from confidence level.

    For decision_context.py: Provide tier_distribution as static industry reference data (from D&O market studies: PREFERRED ~15%, STANDARD ~40%, ELEVATED ~25%, HIGH_RISK ~15%, PROHIBITED ~5%). Provide current_tier from state.scoring. Provide empty posture_fields dict. Flag as "Industry Reference" source.

    Wire all 5 builders into html_context_assembly.py's build_html_context function.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/render/test_scorecard_context.py tests/stages/render/test_heatmap_context.py tests/stages/render/test_crf_bar_context.py tests/stages/render/test_epistemological_trace.py tests/stages/render/test_decision_context.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>5 context builders pass all unit tests, each under 150 lines, all wired into build_html_context, existing render tests still pass</done>
</task>

</tasks>

<verification>
- `wc -l src/do_uw/stages/render/html_renderer.py` shows under 500
- `wc -l src/do_uw/stages/render/html_context_assembly.py` shows under 500
- `uv run pytest tests/stages/render/ -x -q` passes (all existing + new tests)
- Each new context builder file is under 150 lines
</verification>

<success_criteria>
1. html_renderer.py is under 500 lines (from 697)
2. 5 new context builders produce correctly shaped typed dicts
3. All existing render tests pass without modification
4. New context builders have comprehensive unit test coverage
5. build_html_context is importable from both html_renderer (backward compat) and html_context_assembly
</success_criteria>

<output>
After completion, create `.planning/phases/114-worksheet-restructure/114-01-SUMMARY.md`
</output>
