---
phase: 114-worksheet-restructure
plan: 02
type: execute
wave: 2
depends_on: ["114-01"]
files_modified:
  - src/do_uw/templates/html/base.html.j2
  - src/do_uw/templates/html/worksheet.html.j2
  - src/do_uw/templates/html/sections/scorecard.html.j2
  - src/do_uw/templates/html/sections/executive_brief.html.j2
  - src/do_uw/templates/html/sections/crf_banner.html.j2
  - src/do_uw/templates/html/components/heatmap.html.j2
  - src/do_uw/templates/html/components/hae_badge.html.j2
  - src/do_uw/templates/html/components/signal_drilldown.html.j2
  - src/do_uw/templates/html/components/confidence_badge.html.j2
  - src/do_uw/templates/html/styles.css
  - tests/stages/render/test_scorecard.py
  - tests/stages/render/test_executive_brief.py
  - tests/stages/render/test_hae_badges.py
autonomous: true
requirements:
  - WS-01
  - WS-02
  - WS-05
  - WS-08

must_haves:
  truths:
    - "Page 1 Risk Scorecard renders: tier badge, H/A/E radar chart, 10-factor table, top concern signals, key metrics strip, signal heatmap mini"
    - "Page 2 Executive Brief renders: company profile summary, key risks, notable findings as standalone page"
    - "CRF alert bar renders below scorecard with all critical risk factors as linked pills"
    - "H/A/E dimension badges appear in domain section headers"
    - "Signal drill-down details panels expand inline with provenance data"
    - "Confidence badges show HIGH/MEDIUM/LOW next to LLM-generated content"
    - "Scorecard page fits on single printed page (break-after: page)"
    - "Executive brief is self-contained — readable without rest of report"
  artifacts:
    - path: "src/do_uw/templates/html/sections/scorecard.html.j2"
      provides: "Page 1 risk scorecard template"
      min_lines: 50
    - path: "src/do_uw/templates/html/sections/executive_brief.html.j2"
      provides: "Page 2 executive brief template"
      min_lines: 40
    - path: "src/do_uw/templates/html/sections/crf_banner.html.j2"
      provides: "CRF persistent alert bar template"
      min_lines: 15
    - path: "src/do_uw/templates/html/components/heatmap.html.j2"
      provides: "Signal heatmap grid component"
      min_lines: 20
    - path: "src/do_uw/templates/html/components/hae_badge.html.j2"
      provides: "H/A/E dimension badge macro"
      min_lines: 8
  key_links:
    - from: "src/do_uw/templates/html/worksheet.html.j2"
      to: "sections/scorecard.html.j2"
      via: "include before manifest_sections loop"
      pattern: "include.*scorecard"
    - from: "src/do_uw/templates/html/worksheet.html.j2"
      to: "sections/executive_brief.html.j2"
      via: "include after scorecard"
      pattern: "include.*executive_brief"
    - from: "src/do_uw/templates/html/worksheet.html.j2"
      to: "sections/crf_banner.html.j2"
      via: "include after executive brief"
      pattern: "include.*crf_banner"
    - from: "sections/scorecard.html.j2"
      to: "components/heatmap.html.j2"
      via: "include for heatmap mini"
      pattern: "include.*heatmap"
---

<objective>
Create the scorecard page, executive brief, CRF alert bar, and reusable component templates (heatmap, H/A/E badges, signal drill-down, confidence badges). Restructure worksheet.html.j2 to include new structural zones before existing domain sections.

Purpose: Transform the worksheet from a sequential section-by-section layout to a scorecard-first report with an executive brief that stands alone for secondary readers. This is the most visible change in Phase 114 -- the user's "most amazing, fully produced worksheet document."

Output: 3 new section templates, 4 new component templates, updated worksheet.html.j2 with structural zones, CSS for scorecard grid and components.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-worksheet-restructure/114-CONTEXT.md
@.planning/phases/114-worksheet-restructure/114-RESEARCH.md
@.planning/phases/114-worksheet-restructure/114-01-SUMMARY.md

@src/do_uw/templates/html/base.html.j2
@src/do_uw/templates/html/worksheet.html.j2
@src/do_uw/templates/html/styles.css
@src/do_uw/templates/html/components/badges.html.j2
@src/do_uw/templates/html/components/tables.html.j2
@src/do_uw/templates/html/sections/cover.html.j2
@src/do_uw/templates/html/sections/executive.html.j2
@src/do_uw/stages/render/design_system.py
@src/do_uw/stages/render/charts/radar_chart.py
@src/do_uw/stages/render/charts/sparklines.py

<interfaces>
<!-- Context builder outputs consumed by templates (from Plan 01) -->

From scorecard_context.py:
- scorecard_available: bool
- tier: str (e.g., "STANDARD", "ELEVATED")
- quality_score: float
- composite_score: float
- factors_summary: list[dict] with {name: str, pct: int}
- top_concerns: list[dict] with {id: str, evidence: str, level: str} (max 8)
- metrics_strip: dict with {market_cap: str, revenue: str, employees: str, years_public: str, sector: str, beta: str}

From heatmap_context.py:
- heatmap_available: bool
- heatmap_grid: dict[str, dict[str, list[dict]]] -- {rap_class: {subcategory: [{id, status, level, value}]}}

From crf_bar_context.py:
- crf_alerts: list[dict] with {id: str, description: str, severity: str, section_link: str}
- crf_count: int

From hae_context.py (existing):
- hae_available: bool
- radar_labels: list[str], radar_values: list[float]
- host_composite, agent_composite, environment_composite: float
- tier: str, crf_vetoes: list[str]

From pattern_context.py (existing):
- pattern_context.engines: list with engine firing data

From executive_summary context (existing, in build_template_context):
- executive_summary: dict with snapshot, key_findings, risk_assessment
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scorecard, executive brief, CRF banner templates + component macros</name>
  <files>
    src/do_uw/templates/html/sections/scorecard.html.j2,
    src/do_uw/templates/html/sections/executive_brief.html.j2,
    src/do_uw/templates/html/sections/crf_banner.html.j2,
    src/do_uw/templates/html/components/heatmap.html.j2,
    src/do_uw/templates/html/components/hae_badge.html.j2,
    src/do_uw/templates/html/components/signal_drilldown.html.j2,
    src/do_uw/templates/html/components/confidence_badge.html.j2
  </files>
  <action>
    Create scorecard.html.j2 (Page 1 -- Risk Scorecard):
    - CSS Grid layout: 2-column grid (60/40 split)
    - Left column: company identity bar (name, ticker, sector, market cap) at top, then 10-factor scores as compact horizontal bar chart using existing table macros
    - Right column: tier badge (large, centered) with Moody's-style tier coloring, H/A/E radar chart SVG (from chart_svgs.radar if available, else embed_chart), key metrics strip below
    - Below 2-column grid: full-width signal heatmap (include heatmap component) and top concerns list (max 8 items)
    - Use `page-break-after: always` for print isolation
    - Guard all blocks with `{% if scorecard_available %}` for graceful degradation
    - Typography: Georgia 14pt company name, tabular-nums for all numbers
    - Color palette: Navy (#0B1D3A) headers, gold (#D4A843) tier badge border accents

    Create executive_brief.html.j2 (Page 2 -- Executive Brief):
    - Standalone structured narrative: Company profile summary, key risk findings (from executive_summary.key_findings), notable findings
    - Use existing enriched negatives/positives from es_ctx.negatives_enriched / positives_enriched
    - Include confidence badges next to LLM-sourced content
    - Must be readable without any other section (self-contained for secondary readers)
    - `page-break-after: always` for print

    Create crf_banner.html.j2 (CRF Alert Bar):
    - Full-width banner, dark navy background (#0B1D3A), 4px gold left border (#D4A843)
    - Each CRF alert as a pill with severity icon + short description
    - Each pill links to relevant section anchor (using crf_alerts[].section_link)
    - Hidden if crf_count == 0
    - Print: renders as boxed section, no sticky behavior

    Create heatmap.html.j2 component:
    - Pure HTML/CSS grid (not SVG) -- each cell is a small colored square (~24px)
    - 3 row groups: Host, Agent, Environment (each labeled)
    - Within each group, subcategories as columns
    - Cell colors: TRIGGERED=#B91C1C, ELEVATED=#D97706, CLEAR=#64748B, SKIPPED=#E5E7EB, DEFERRED=#D97706 border
    - Add `-webkit-print-color-adjust: exact; print-color-adjust: exact` for PDF colors
    - Each cell wraps a signal_drilldown details element (click to expand)

    Create hae_badge.html.j2 macro:
    - `{% macro hae_badge(dimensions) %}` accepting list of dimension strings
    - Small pill badges: H=#1D4ED8 blue, A=#B91C1C red, E=#059669 green
    - Single letter display (H, A, E) with white text on colored background
    - Used in section headers throughout domain sections

    Create signal_drilldown.html.j2 macro:
    - `{% macro signal_drill(signal) %}` accepting signal dict
    - Uses `<details class="signal-drilldown">` pattern (existing collapsible)
    - Summary shows signal_id (monospace) + status badge
    - Expanded panel: 2-column key-value table with raw_data, source, threshold, rule_origin, confidence, score_contribution
    - CSS class `signal-drilldown` for PDF exclusion (hidden in print, epistemological trace serves this purpose)

    Create confidence_badge.html.j2 macro:
    - `{% macro confidence(level) %}` accepting "HIGH", "MEDIUM", "LOW"
    - HIGH: solid navy background, white text
    - MEDIUM: navy outline, navy text
    - LOW: dashed amber outline, amber text
    - Inline micro-badge (font-size: 0.6rem)

    H/A/E section mapping (hardcode in hae_badge usage):
    - Identity/Company: [Host]
    - Financial: [Host]
    - Market: [Host, Environment]
    - Governance: [Host, Agent]
    - Litigation: [Agent, Environment]
    - Scoring: [Host, Agent, Environment]
    - AI Risk: [Environment]
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run python -c "
from jinja2 import Environment, FileSystemLoader
import os
tpl_dir = 'src/do_uw/templates/html'
env = Environment(loader=FileSystemLoader(tpl_dir))
for name in ['sections/scorecard.html.j2', 'sections/executive_brief.html.j2', 'sections/crf_banner.html.j2', 'components/heatmap.html.j2', 'components/hae_badge.html.j2', 'components/signal_drilldown.html.j2', 'components/confidence_badge.html.j2']:
    t = env.get_template(name)
    print(f'OK: {name}')
print('All templates parse successfully')
" 2>&1</automated>
  </verify>
  <done>7 new template files created, all parse without Jinja2 syntax errors, scorecard uses CSS Grid layout, heatmap uses print-safe colors, all components follow existing template patterns</done>
</task>

<task type="auto">
  <name>Task 2: Restructure worksheet.html.j2 + update base.html.j2 sidebar + add scorecard CSS + integration tests</name>
  <files>
    src/do_uw/templates/html/worksheet.html.j2,
    src/do_uw/templates/html/base.html.j2,
    src/do_uw/templates/html/styles.css,
    tests/stages/render/test_scorecard.py,
    tests/stages/render/test_executive_brief.py,
    tests/stages/render/test_hae_badges.py
  </files>
  <action>
    Update worksheet.html.j2:
    - Add structural zones BEFORE the manifest_sections loop:
      1. `{% include "sections/scorecard.html.j2" %}` (Page 1)
      2. `{% include "sections/executive_brief.html.j2" %}` (Page 2)
      3. `{% include "sections/crf_banner.html.j2" %}` (CRF alerts)
    - Keep existing `{% for section in manifest_sections %}` loop for domain sections (Company, Financial, Market, Governance, Litigation, Scoring)
    - After manifest sections, before existing appendices: add `{% include "appendices/epistemological_trace.html.j2" %}` (placeholder include -- template created in Plan 03)
    - Add `{% include "sections/decision_record.html.j2" %}` before appendices (placeholder -- Plan 03)
    - Keep existing signal_audit and render_audit appendix includes

    Update base.html.j2 sidebar TOC:
    - Restructure into audience-oriented groups:
      - "Overview" group: Risk Scorecard (#scorecard), Executive Brief (#executive-brief), CRF Alerts (#crf-banner) conditional on crf_count > 0
      - "Analysis" group: Company, Financial, Market, Governance, Litigation, Scoring (existing links, keep current anchors)
      - "Appendix" group: Decision Record (#decision-record), Signal Trace (#epistemological-trace), Meeting Prep, Sources, QA Audit, Coverage
    - Import hae_badge macro in base.html.j2 for section headers

    Add CSS to styles.css for new components (append, do not restructure existing):
    - .scorecard-grid: CSS Grid 2-column layout (60/40), gap 0.5rem, break-after: page
    - .scorecard-tier: tier badge styling with gold border, large font
    - .scorecard-metrics: compact key-value strip
    - .scorecard-concerns: numbered list with severity indicators
    - .crf-banner: dark navy bg, gold left border, flex layout for pills
    - .crf-pill: pill badge with severity color
    - .hae-badge: inline pill for H/A/E dimension badges (3 color variants)
    - .signal-drilldown: collapsed by default, gray bg when expanded
    - .confidence-badge: 3 variants (HIGH/MEDIUM/LOW)
    - .heatmap-grid: CSS Grid for signal cells
    - .heatmap-cell: small colored square with print-color-adjust
    - .executive-brief: page-break-after: always
    - @media print rules: scorecard break-after, crf-banner not sticky, signal-drilldown hidden, heatmap print-color-adjust

    Update the pdf_mode JavaScript in base.html.j2:
    - Modify details expansion to EXCLUDE signal-drilldown class:
      `document.querySelectorAll('details:not(.signal-drilldown)').forEach(...)`

    Create test_scorecard.py:
    - Test that scorecard template renders with mock scorecard context (tier, factors, concerns, metrics)
    - Test that scorecard gracefully degrades when scorecard_available=False
    - Test that heatmap renders correct number of cells for given signal grid

    Create test_executive_brief.py:
    - Test that brief renders with mock executive_summary context
    - Test that brief includes enriched negative/positive findings
    - Test standalone readability: all context variables used by brief are self-contained

    Create test_hae_badges.py:
    - Test that hae_badge macro produces correct HTML for each dimension
    - Test that badges render with correct CSS classes (hae-host, hae-agent, hae-environment)
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/render/test_scorecard.py tests/stages/render/test_executive_brief.py tests/stages/render/test_hae_badges.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>Worksheet renders with scorecard before domain sections, sidebar has audience-oriented groups, scorecard fits single printed page, executive brief is self-contained, all new and existing tests pass</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/stages/render/ -x -q` passes (all tests including new)
- Scorecard template includes tier badge, radar chart reference, factor table, heatmap, top concerns
- Executive brief template is self-contained (no references to variables only available in later sections)
- CRF banner conditionally hidden when no alerts
- styles.css print rules hide signal-drilldown, ensure print-color-adjust on heatmap
- Sidebar TOC has 3 groups: Overview, Analysis, Appendix
</verification>

<success_criteria>
1. worksheet.html.j2 includes scorecard, executive brief, CRF banner before domain sections
2. Scorecard CSS Grid renders 2-column layout with break-after: page for print
3. Executive brief is readable in isolation (self-contained context)
4. H/A/E badges appear in section headers with correct dimension colors
5. Signal drill-down panels hidden in print CSS (epistemological trace serves print purpose)
6. All template files parse without Jinja2 errors
7. New integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/114-worksheet-restructure/114-02-SUMMARY.md`
</output>
