---
phase: 114-worksheet-restructure
plan: 03
type: execute
wave: 2
depends_on: ["114-01"]
files_modified:
  - src/do_uw/templates/html/appendices/epistemological_trace.html.j2
  - src/do_uw/templates/html/sections/decision_record.html.j2
  - src/do_uw/templates/html/styles.css
  - tests/stages/render/test_epistemological_trace.py
  - tests/stages/render/test_decision_record.py
autonomous: true
requirements:
  - WS-03
  - WS-07

must_haves:
  truths:
    - "Epistemological trace appendix renders ALL signals -- triggered, clean, AND skipped"
    - "Trace table grouped by H/A/E dimension with correct columns: Signal ID, Status, Raw Data, Source, Threshold, Confidence, Source Type, Evaluation, Score Contribution"
    - "Decision record page provides posture fields (bind/decline/refer/terms) with free-text rationale area"
    - "Decision record shows tier distribution as soft guidance (Industry Reference, not recommendation)"
    - "Trace table thead repeats on each printed page via CSS display: table-header-group"
    - "Decision record has break-before: page for clean print layout"
  artifacts:
    - path: "src/do_uw/templates/html/appendices/epistemological_trace.html.j2"
      provides: "Full signal provenance table template"
      min_lines: 60
    - path: "src/do_uw/templates/html/sections/decision_record.html.j2"
      provides: "UW decision documentation page template"
      min_lines: 40
  key_links:
    - from: "appendices/epistemological_trace.html.j2"
      to: "context_builders/epistemological_trace.py"
      via: "trace_rows grouped dict consumed by template"
      pattern: "trace_rows"
    - from: "sections/decision_record.html.j2"
      to: "context_builders/decision_context.py"
      via: "tier_distribution and posture_fields consumed"
      pattern: "tier_distribution"
---

<objective>
Create the epistemological trace appendix template and decision record page template -- the two remaining structural components of the restructured worksheet.

Purpose: Part C (Epistemological Trace) proves provenance for every signal evaluation -- the compliance audit trail. The Decision Record transforms the worksheet from a passive report into the official underwriting record. Together they complete the three-part report structure.

Output: 2 new templates with full CSS styling and integration tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-worksheet-restructure/114-CONTEXT.md
@.planning/phases/114-worksheet-restructure/114-RESEARCH.md
@.planning/phases/114-worksheet-restructure/114-01-SUMMARY.md

@src/do_uw/templates/html/base.html.j2
@src/do_uw/templates/html/styles.css
@src/do_uw/templates/html/appendices/signal_audit.html.j2
@src/do_uw/stages/render/context_builders/_signal_consumer.py

<interfaces>
<!-- Context builder outputs consumed by these templates (from Plan 01) -->

From epistemological_trace.py:
- trace_rows: dict[str, list[dict]] -- {"host": [...], "agent": [...], "environment": [...]}
  Each row: {signal_id, status, raw_data, source, confidence, source_type, threshold, rule_origin, evaluation, score_contribution, rap_class, rap_subcategory}
- trace_total: int -- total signal count

From decision_context.py:
- decision_available: bool
- current_tier: str (e.g., "STANDARD")
- tier_distribution: list[dict] with {tier: str, pct: int, label: str}
  Industry reference: PREFERRED ~15%, STANDARD ~40%, ELEVATED ~25%, HIGH_RISK ~15%, PROHIBITED ~5%
- posture_fields: dict (empty for UW to fill, keys: posture, rationale, conditions, overrides)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create epistemological trace appendix template</name>
  <files>
    src/do_uw/templates/html/appendices/epistemological_trace.html.j2,
    src/do_uw/templates/html/styles.css,
    tests/stages/render/test_epistemological_trace.py
  </files>
  <action>
    Create epistemological_trace.html.j2:
    - Section with id="epistemological-trace" and break-before: page
    - Title: "Epistemological Trace — Signal Provenance Audit Trail"
    - Summary stats bar: total signals, triggered count, clean count, skipped count, deferred count
    - 3 grouped tables (one per H/A/E dimension):
      - Group header: "Host (Structural Risk)" / "Agent (Behavioral Risk)" / "Environment (External Risk)" with signal count
      - Table columns: Signal ID | Status | Raw Data | Source (filing+date) | Threshold Applied | Confidence | Source Type | Evaluation Result | Score Contribution
      - Status column: colored badge (TRIGGERED=red, CLEAR=blue-gray, SKIPPED=gray, DEFERRED=amber, ELEVATED=amber)
      - Source Type column: audited/unaudited/web/derived (from source_type field)
      - Confidence column: confidence_badge macro (HIGH/MEDIUM/LOW)
      - Signal ID column: monospace font
      - Table uses `display: table-header-group` on thead for print header repetition
    - Use print-color-adjust: exact on status badges
    - Guard with `{% if trace_rows %}` for graceful degradation
    - Table should be compact (font-size: 0.7rem for print, 0.75rem for screen) -- this will be a large table (500+ rows)
    - Alternate row striping for readability

    Add CSS to styles.css (append):
    - .trace-table: compact table with border-collapse, tabular-nums
    - .trace-table thead: display table-header-group for print, sticky for screen
    - .trace-status-badge: small inline badge with status colors
    - .trace-group-header: H/A/E group separator with dimension color left border
    - Responsive: horizontal scroll on narrow screens
    - Print: font-size 7pt for trace table to fit on pages

    Create test_epistemological_trace.py:
    - Test: renders with mock trace_rows containing triggered, clean, skipped signals
    - Test: all 3 H/A/E groups appear with correct headers
    - Test: all required columns present in table header
    - Test: trace_total matches sum of all groups
    - Test: graceful degradation when trace_rows is empty
    - Test: status badges have correct CSS classes
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/render/test_epistemological_trace.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>Epistemological trace template renders all signals grouped by H/A/E, all 9 columns present, status badges colored correctly, print-safe with repeating headers, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create decision record page template</name>
  <files>
    src/do_uw/templates/html/sections/decision_record.html.j2,
    src/do_uw/templates/html/styles.css,
    tests/stages/render/test_decision_record.py
  </files>
  <action>
    Create decision_record.html.j2:
    - Section with id="decision-record" and break-before: page
    - Title: "Underwriting Decision Record"
    - Subtitle: "Official underwriting posture and rationale for {{ company_name }} ({{ ticker }})"

    Tier Distribution Reference Panel:
    - Horizontal bar chart (pure HTML/CSS, no JS) showing industry-standard tier distribution
    - Each tier as a colored bar segment with percentage label
    - Highlight current_tier with bold border/accent
    - Label: "Industry Reference — D&O Tier Distribution (based on SCAC/Cornerstone data)"
    - NOT a recommendation -- explicitly labeled as reference data

    Posture Fields (static form-like layout for print):
    - Underwriting Posture: radio-button-style options rendered as a grid: BIND / DECLINE / REFER / TERMS & CONDITIONS
    - Current tier shown as context (not recommendation)
    - Rationale: multi-line text area placeholder (bordered box with "Record rationale here" ghost text)
    - Conditions: bulleted list placeholder for terms/conditions
    - Overrides: section for documenting any system-recommendation overrides with rationale
    - All fields are display-only (not interactive forms -- user deferred interactive form fields)
    - Print-friendly: bordered boxes with clear labels, sufficient space for handwritten notes

    No System Recommendation:
    - Explicitly do NOT show a suggested posture
    - Only show tier distribution as context -- the underwriter forms their own view from evidence
    - Small note: "No system recommendation provided — this section documents the underwriter's independent assessment"

    Guard with `{% if decision_available %}` for graceful degradation

    Add CSS to styles.css (append):
    - .decision-record: page layout with clean typography
    - .tier-distribution-bar: horizontal stacked bar chart
    - .tier-segment: individual segment with percentage
    - .tier-highlight: bold border for current tier
    - .posture-grid: 2x2 grid of posture options
    - .posture-option: styled as selectable card (print: checkbox-style)
    - .rationale-box: bordered text area placeholder with min-height
    - .conditions-box: bordered list area
    - Print: ensure all boxes have visible borders, adequate spacing for handwriting

    Create test_decision_record.py:
    - Test: renders with mock decision context (current_tier="ELEVATED", tier_distribution with 5 tiers)
    - Test: tier distribution shows all 5 tiers with percentages
    - Test: current tier is highlighted
    - Test: posture options (BIND/DECLINE/REFER/TERMS) all present
    - Test: no system recommendation text present
    - Test: graceful degradation when decision_available=False
    - Test: "Industry Reference" label present (not "Recommendation")
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/render/test_decision_record.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>Decision record template renders posture fields and tier distribution reference, explicitly avoids system recommendation, print-friendly layout, tests pass</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/stages/render/test_epistemological_trace.py tests/stages/render/test_decision_record.py -x -q` passes
- Epistemological trace has all 9 required columns
- All signal statuses (triggered, clean, skipped, deferred) render with correct colors
- Decision record explicitly shows "No system recommendation" language
- Tier distribution labeled as "Industry Reference"
- Both templates use break-before: page for print
- styles.css remains under 800 lines (currently 573, adding ~100 lines of CSS)
</verification>

<success_criteria>
1. Epistemological trace renders ALL signals (triggered + clean + skipped) with full provenance
2. Trace table grouped by H/A/E with repeating headers in print
3. Decision record provides posture documentation without anchoring bias (no recommendation)
4. Tier distribution shows industry reference percentages with current tier highlighted
5. Both templates print cleanly with page breaks
6. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/114-worksheet-restructure/114-03-SUMMARY.md`
</output>
