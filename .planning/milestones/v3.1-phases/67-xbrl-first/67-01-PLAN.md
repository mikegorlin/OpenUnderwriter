---
phase: 67-xbrl-first
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/brain/config/xbrl_concepts.json
  - src/do_uw/stages/extract/xbrl_mapping.py
  - src/do_uw/stages/analyze/financial_models.py
  - tests/test_xbrl_concepts.py
autonomous: true
requirements: [XBRL-01, XBRL-06]

must_haves:
  truths:
    - "xbrl_concepts.json contains 120+ concepts with priority-ordered tag lists"
    - "Every concept has an expected_sign field"
    - "Total liabilities derivation handles minority interest, preferred stock, and LiabilitiesAndStockholdersEquity edge cases"
    - "All existing tests pass without modification"
  artifacts:
    - path: "src/do_uw/brain/config/xbrl_concepts.json"
      provides: "120+ XBRL concept mappings with expected_sign"
      contains: "expected_sign"
    - path: "src/do_uw/stages/extract/xbrl_mapping.py"
      provides: "XBRLConcept TypedDict with expected_sign field"
      contains: "expected_sign"
    - path: "src/do_uw/stages/analyze/financial_models.py"
      provides: "Hardened total liabilities derivation cascade"
      contains: "minority_interest"
    - path: "tests/test_xbrl_concepts.py"
      provides: "Validation tests for concept config integrity"
  key_links:
    - from: "src/do_uw/brain/config/xbrl_concepts.json"
      to: "src/do_uw/stages/extract/xbrl_mapping.py"
      via: "load_xbrl_mapping() loads and parses config"
      pattern: "load_xbrl_mapping"
    - from: "src/do_uw/stages/extract/xbrl_mapping.py"
      to: "src/do_uw/stages/extract/financial_statements.py"
      via: "financial_statements imports XBRLConcept, load_xbrl_mapping, resolve_concept"
      pattern: "from do_uw.stages.extract.xbrl_mapping import"
---

<objective>
Expand XBRL concept coverage from 40 to 120+ concepts and harden the total liabilities derivation. This is the foundation everything else in Phase 67 builds on.

Purpose: The system currently extracts only 40 XBRL concepts. ~40 financial metrics are LLM-extracted with MEDIUM confidence. Expanding to 120+ concepts with proper tag lists replaces LLM extraction with exact GAAP numbers at HIGH confidence.

Output: Expanded xbrl_concepts.json with expected_sign on all entries, updated XBRLConcept TypedDict, hardened total liabilities derivation, validation tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-xbrl-first/67-RESEARCH.md

@src/do_uw/brain/config/xbrl_concepts.json
@src/do_uw/stages/extract/xbrl_mapping.py
@src/do_uw/stages/extract/financial_statements.py
@src/do_uw/stages/analyze/financial_models.py
@tests/test_financial_statements.py

<interfaces>
From src/do_uw/stages/extract/xbrl_mapping.py:
```python
class XBRLConcept(TypedDict):
    canonical_name: str
    xbrl_tags: list[str]
    unit: str
    period_type: str  # "instant" or "duration"
    statement: str
    description: str

def load_xbrl_mapping(path: Path | None = None) -> dict[str, XBRLConcept]: ...
def resolve_concept(facts, mapping, concept_name, form_type="10-K") -> list[dict] | None: ...
def extract_concept_value(facts, concept, form_type="10-K", unit="USD") -> list[dict]: ...
def get_latest_value(entries) -> dict | None: ...
def get_period_values(entries, periods=3) -> list[dict]: ...
```

From src/do_uw/stages/extract/validation.py:
```python
@dataclass
class ExtractionReport:
    extractor_name: str
    expected_fields: list[str]
    found_fields: list[str]
    missing_fields: list[str]
    unexpected_fields: list[str]
    coverage_pct: float
    confidence: Confidence
    source_filing: str
    fallbacks_used: list[str]
    warnings: list[str]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand xbrl_concepts.json + update XBRLConcept TypedDict</name>
  <files>src/do_uw/brain/config/xbrl_concepts.json, src/do_uw/stages/extract/xbrl_mapping.py, tests/test_xbrl_concepts.py</files>
  <behavior>
    - Test: xbrl_concepts.json loads successfully and has >= 110 entries
    - Test: every concept has all required fields (canonical_name, xbrl_tags, unit, period_type, statement, description, expected_sign)
    - Test: expected_sign values are one of "positive", "negative", "any"
    - Test: all existing 40 concepts still present with unchanged xbrl_tags
    - Test: load_xbrl_mapping() returns dict with expected_sign accessible on each entry
    - Test: concepts cover all 3 statement types (income, balance_sheet, cash_flow) plus "derived" for computed concepts
    - Test: no duplicate canonical_name values
    - Test: xbrl_tags are non-empty for non-derived concepts (statement != "derived")
  </behavior>
  <action>
1. Update `XBRLConcept` TypedDict in `xbrl_mapping.py` to add `expected_sign: str` field.
2. Update `load_xbrl_mapping()` to populate `expected_sign` with default `"any"` for backward compatibility.
3. Add `expected_sign` field to ALL 40 existing concepts in `xbrl_concepts.json` using the sign convention reference from 67-RESEARCH.md.
4. Add ~50 new income statement concepts from research: stock_based_compensation, income_before_tax, discontinued_operations, gain_loss_investments, gain_loss_sale_assets, other_comprehensive_income, operating_expenses_total, restructuring_charges, impairment_charges.
5. Add ~20 new balance sheet concepts from research: short_term_investments, prepaid_expenses, other_current_assets, long_term_investments, accrued_liabilities, deferred_tax_asset, deferred_tax_liability, pension_liability, accumulated_other_comprehensive_income, treasury_stock, common_stock_value, additional_paid_in_capital, liabilities_and_stockholders_equity, preferred_stock_value, right_of_use_asset, minority_interest.
6. Add ~15 new cash flow concepts from research: acquisitions_net, debt_issuance, debt_repayment, stock_issuance, interest_paid, taxes_paid, stock_based_comp_cf, change_in_working_capital, net_change_in_cash.
7. Add ~20 derived concept stubs (statement="derived", xbrl_tags=[]) for computed metrics: gross_margin_pct, operating_margin_pct, net_margin_pct, ebitda_margin_pct, effective_tax_rate, interest_coverage_ratio, current_ratio, quick_ratio, debt_to_equity, debt_to_ebitda, tangible_book_value, net_debt, book_value_per_share, free_cash_flow, fcf_to_revenue, capex_to_revenue, capex_to_depreciation, dividend_payout_ratio, fcf_per_share, revenue_growth_yoy. Use expected_sign="any" for ratios/margins, "positive" for absolute values where applicable.
8. Each new concept MUST have 3+ XBRL tags in priority order (except derived concepts). Use tag lists from 67-RESEARCH.md "New Concepts to Add" section.
9. Create `tests/test_xbrl_concepts.py` with schema validation tests.

IMPORTANT: Do NOT change the structure of existing concepts beyond adding `expected_sign`. Tag lists, descriptions, units, period_types for existing 40 concepts must remain unchanged.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/test_xbrl_concepts.py -x -v && uv run pytest tests/test_financial_statements.py -x -v</automated>
  </verify>
  <done>xbrl_concepts.json has 110+ concepts all with expected_sign, XBRLConcept TypedDict updated, load_xbrl_mapping handles expected_sign, all existing tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden total liabilities derivation cascade</name>
  <files>src/do_uw/stages/analyze/financial_models.py, tests/test_financial_models_tl.py</files>
  <behavior>
    - Test: direct Liabilities tag returns its value (priority 1)
    - Test: TA - SE derivation works when Liabilities tag missing (priority 2)
    - Test: minority_interest subtracted from SE before derivation when present (priority 3)
    - Test: LiabilitiesAndStockholdersEquity - SE fallback works (priority 4)
    - Test: returns None when no derivation possible
    - Test: handles None inputs gracefully (no exceptions)
    - Test: total_liabilities > total_assets produces warning log
    - Test: prior period derivation also uses the cascade
  </behavior>
  <action>
1. In `financial_models.py`, extract the total liabilities derivation from `_collect_all_inputs()` into a standalone `derive_total_liabilities(inputs: dict[str, float | None]) -> float | None` function.
2. Implement the 4-step derivation cascade from 67-RESEARCH.md:
   - Priority 1: Direct `total_liabilities` value (already extracted)
   - Priority 2: `total_assets - stockholders_equity` (existing logic)
   - Priority 3: `total_assets - stockholders_equity + minority_interest` when minority interest exists (SE tag includes NCI)
   - Priority 4: `liabilities_and_stockholders_equity - stockholders_equity` (fallback for companies using only L&SE tag)
3. Add sanity check: if derived TL > TA, log a warning (may indicate negative equity, which is valid but worth flagging).
4. Update `_collect_all_inputs()` to:
   - Add `minority_interest` and `liabilities_and_stockholders_equity` to the concepts list
   - Call `derive_total_liabilities()` instead of inline derivation
   - Apply cascade to both latest and prior periods
5. Create `tests/test_financial_models_tl.py` with edge case tests.

IMPORTANT: The `minority_interest` and `liabilities_and_stockholders_equity` concepts must exist in xbrl_concepts.json (Task 1 adds them). These new concepts in the config allow `_extract_input()` to find them in the statement line items.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/test_financial_models_tl.py -x -v && uv run pytest tests/test_financial_statements.py -x -v</automated>
  </verify>
  <done>Total liabilities derivation uses 4-step cascade, handles minority interest and preferred stock, prior period also uses cascade, all tests pass</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_xbrl_concepts.py tests/test_financial_models_tl.py tests/test_financial_statements.py -x -v` -- all pass
2. `uv run python -c "from do_uw.stages.extract.xbrl_mapping import load_xbrl_mapping; m = load_xbrl_mapping(); print(f'{len(m)} concepts loaded'); assert len(m) >= 110; assert all('expected_sign' in v for v in m.values())"` -- confirms 110+ concepts with expected_sign
3. `uv run pytest tests/ -x --timeout=120` -- full test suite passes (no regressions)
</verification>

<success_criteria>
- xbrl_concepts.json has 110+ concepts (was 40)
- Every concept has expected_sign field
- XBRLConcept TypedDict includes expected_sign
- Total liabilities derivation handles 4 edge case patterns
- All existing tests pass without modification
- New validation tests cover config integrity
</success_criteria>

<output>
After completion, create `.planning/phases/67-xbrl-first/67-01-SUMMARY.md`
</output>
