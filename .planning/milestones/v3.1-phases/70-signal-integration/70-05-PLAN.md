---
phase: 70-signal-integration
plan: 05
type: execute
wave: 1
depends_on: [70-04]
files_modified:
  - tests/fixtures/signal_baselines/WWD_baseline.json
  - tests/fixtures/signal_baselines/WWD_detail_baseline.json
autonomous: false
requirements: [SIG-02, SIG-05, SIG-06, SIG-07]
gap_closure: true

must_haves:
  truths:
    - "WWD golden baseline exists in tests/fixtures/signal_baselines/"
    - "Cross-ticker validation passes for all 5 tickers (AAPL, RPM, SNA, V, WWD)"
  artifacts:
    - path: "tests/fixtures/signal_baselines/WWD_baseline.json"
      provides: "WWD signal evaluation golden baseline"
    - path: "tests/fixtures/signal_baselines/WWD_detail_baseline.json"
      provides: "WWD per-signal detail baseline"
  key_links:
    - from: "tests/test_signal_cross_ticker.py"
      to: "tests/fixtures/signal_baselines/WWD_baseline.json"
      via: "golden master comparison"
      pattern: "WWD.*baseline"
---

<objective>
Generate WWD baseline by re-running the pipeline on WWD ticker (which predates signal_results format), then regenerating the golden baseline. This closes the last SIG-07 gap.

Purpose: WWD is one of the 5 validation tickers. Its state.json lacks signal_results because it was generated before signal_results existed. Re-running produces a current-format state.json that the cross-ticker test can baseline.
Output: WWD golden baseline files, all 5 tickers passing cross-ticker validation.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/70-signal-integration/70-VERIFICATION.md
@.planning/phases/70-signal-integration/70-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run WWD pipeline to generate fresh state.json with signal_results</name>
  <files>output/WWD-*/state.json</files>
  <action>
  The user must run the WWD pipeline to generate a current-format state.json that includes signal_results. This requires MCP servers and network access which Claude cannot initiate autonomously.

  Command: `cd /Users/gorlin/projects/UW/do-uw && uv run python -m do_uw.cli analyze WWD`
  </action>
  <how-to-verify>
    1. Run the WWD pipeline: `cd /Users/gorlin/projects/UW/do-uw && uv run python -m do_uw.cli analyze WWD`
    2. Verify output directory has state.json: `ls output/WWD-*/state.json`
    3. Verify state.json has signal_results: `python -c "import json, pathlib; d=json.load(open(sorted(pathlib.Path('output').glob('WWD-*/state.json'))[-1])); print(f'signal_results: {len(d.get(\"analysis\",{}).get(\"signal_results\",{}))}')"`
  </how-to-verify>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && python -c "import json, pathlib; d=json.load(open(sorted(pathlib.Path('output').glob('WWD-*/state.json'))[-1])); assert len(d.get('analysis',{}).get('signal_results',{})) > 0, 'No signal_results in WWD state.json'"</automated>
  </verify>
  <done>WWD state.json exists with signal_results populated.</done>
  <resume-signal>Type "pipeline complete" when WWD pipeline has finished and state.json has signal_results, or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Generate WWD golden baseline + verify all 5 tickers</name>
  <files>
    tests/fixtures/signal_baselines/WWD_baseline.json
    tests/fixtures/signal_baselines/WWD_detail_baseline.json
  </files>
  <action>
  1. Delete any stale WWD baseline files if they exist:
     ```bash
     rm -f tests/fixtures/signal_baselines/WWD_baseline.json tests/fixtures/signal_baselines/WWD_detail_baseline.json
     ```

  2. Run the cross-ticker test for WWD only -- it will create the golden baseline on first run:
     ```bash
     uv run pytest tests/test_signal_cross_ticker.py -k "WWD" -x -v
     ```

  3. Verify WWD baseline was created:
     ```bash
     ls -la tests/fixtures/signal_baselines/WWD*
     ```

  4. Run the full cross-ticker test suite to verify all 5 tickers pass:
     ```bash
     uv run pytest tests/test_signal_cross_ticker.py -x -v
     ```

  5. Run the full test suite to confirm no regressions:
     ```bash
     uv run pytest tests/ -x -q --tb=short
     ```
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && test -f tests/fixtures/signal_baselines/WWD_baseline.json && uv run pytest tests/test_signal_cross_ticker.py -x -q --tb=short -v 2>&1 | tail -20</automated>
  </verify>
  <done>WWD golden baseline exists. Cross-ticker validation passes for all 5 tickers (AAPL, RPM, SNA, V, WWD). SIG-07 fully satisfied.</done>
</task>

</tasks>

<verification>
1. `test -f tests/fixtures/signal_baselines/WWD_baseline.json` -- WWD baseline exists
2. `uv run pytest tests/test_signal_cross_ticker.py -x -q -v` -- all 5 tickers pass
3. `uv run pytest tests/ -x -q --tb=short` -- all tests pass
</verification>

<success_criteria>
- WWD golden baseline files exist in tests/fixtures/signal_baselines/
- Cross-ticker validation passes for all 5 tickers (AAPL, RPM, SNA, V, WWD)
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/70-signal-integration/70-05-SUMMARY.md`
</output>
