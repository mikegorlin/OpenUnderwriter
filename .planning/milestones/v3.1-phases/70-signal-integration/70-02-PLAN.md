---
phase: 70-signal-integration
plan: 02
type: execute
wave: 2
depends_on: [70-01]
files_modified:
  - src/do_uw/brain/signals/fin/balance.yaml
  - src/do_uw/brain/signals/fin/income.yaml
  - src/do_uw/brain/signals/fin/temporal.yaml
  - src/do_uw/brain/signals/fin/forensic.yaml
  - src/do_uw/brain/signals/fin/accounting.yaml
  - src/do_uw/brain/signals/biz/core.yaml
  - src/do_uw/stages/analyze/signal_mappers_analytical.py
  - src/do_uw/stages/analyze/signal_field_routing.py
  - tests/test_signal_xbrl_upgrade.py
autonomous: true
requirements: [SIG-02, SIG-03, SIG-04]

must_haves:
  truths:
    - "45 XBRL-replaceable signals now point to XBRL-sourced field_keys instead of LLM narratives"
    - "28 XBRL-enhanceable signals use XBRL for numeric thresholds while retaining LLM narrative"
    - "Shadow evaluation logs both old and new values for every changed signal"
    - "Zero test regressions from field_key changes"
  artifacts:
    - path: "src/do_uw/brain/signals/fin/balance.yaml"
      provides: "Updated XBRL field_keys for balance sheet signals"
      contains: "data_strategy"
    - path: "src/do_uw/stages/analyze/signal_mappers_analytical.py"
      provides: "Shadow evaluation comparison logic"
      contains: "_shadow_xbrl"
    - path: "tests/test_signal_xbrl_upgrade.py"
      provides: "Tests for XBRL upgrade wiring"
  key_links:
    - from: "brain/signals/fin/*.yaml"
      to: "signal_mappers_analytical.py"
      via: "data_strategy.field_key routing"
      pattern: "field_key.*xbrl"
    - from: "signal_mappers_analytical.py"
      to: "brain_xbrl_shadow DuckDB table"
      via: "_log_xbrl_shadow()"
      pattern: "brain_xbrl_shadow"
---

<objective>
Upgrade 45 XBRL-replaceable signals to XBRL-sourced field_keys and enhance 28 signals with dual XBRL+LLM data sources. Implement shadow evaluation logging to track every value change for regression detection.

Purpose: Replaces LLM-extracted financial data (MEDIUM confidence) with XBRL-sourced data (HIGH confidence) across 73 existing signals, with full shadow evaluation to catch regressions.
Output: Updated YAML field_keys, extended mappers, shadow evaluation table, regression tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-signal-integration/70-RESEARCH.md
@.planning/research/signal-xbrl-audit.md
@.planning/phases/70-signal-integration/70-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: mapper now accepts analysis param -->
From src/do_uw/stages/analyze/signal_mappers_analytical.py (after Plan 01):
```python
def map_phase26_check(
    signal_id: str,
    check_config: dict[str, Any],
    extracted: ExtractedData,
    company: CompanyProfile | None = None,
    analysis: AnalysisResults | None = None,  # Added in Plan 01
) -> dict[str, Any] | None:

def _map_forensic_check(
    signal_id: str,
    extracted: ExtractedData,
    analysis: AnalysisResults | None = None,  # Added in Plan 01
) -> dict[str, Any]:
```

Key XBRL data paths (from Phase 67-68):
- `state.extracted.financials.statements.income_statement` - dict of concept -> SourcedValue
- `state.extracted.financials.statements.balance_sheet` - dict of concept -> SourcedValue
- `state.extracted.financials.statements.cash_flow` - dict of concept -> SourcedValue
- `state.extracted.financials.quarterly_xbrl` - list of QuarterlyPeriod with .values dict

Existing mapper routing (signal_mappers_analytical.py):
- FIN.LIQ.* -> _map_fin_liquidity()
- FIN.DEBT.* -> _map_fin_debt()
- FIN.PROFIT.* -> _map_fin_profitability()
- FIN.TEMPORAL.* -> _map_temporal_check()
- FIN.FORENSIC.* -> _map_forensic_check()
- FIN.QUALITY.* -> _map_quality_check()
- BIZ.* -> signal_mappers.py
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade 45 XBRL-replaceable signal field_keys + shadow evaluation infrastructure</name>
  <files>
    src/do_uw/brain/signals/fin/balance.yaml
    src/do_uw/brain/signals/fin/income.yaml
    src/do_uw/brain/signals/fin/temporal.yaml
    src/do_uw/brain/signals/fin/forensic.yaml
    src/do_uw/brain/signals/fin/accounting.yaml
    src/do_uw/brain/signals/biz/core.yaml
    src/do_uw/stages/analyze/signal_mappers_analytical.py
    src/do_uw/stages/analyze/signal_field_routing.py
    tests/test_signal_xbrl_upgrade.py
  </files>
  <action>
  **CRITICAL ANTI-PATTERN WARNING:** Do NOT change signal thresholds. Only change `data_strategy.field_key` paths. Threshold calibration happens AFTER shadow evaluation in Plan 03.

  1. **Shadow evaluation infrastructure** (signal_mappers_analytical.py): Add `_log_xbrl_shadow()` function per research doc Pattern 4. Creates `brain_xbrl_shadow` DuckDB table if not exists. Logs: run_id, signal_id, ticker, old_source, old_value, new_source, new_value, value_delta, status_match, evaluated_at. Fire-and-forget error handling (try/except with logger.warning).

  2. **Update 45 XBRL-replaceable signals** across 6 YAML files. For each signal, change `data_strategy.field_key` to the XBRL-sourced path and update `data_strategy.primary_source` to SEC_10K. Key categories from audit:

     **Liquidity (fin/balance.yaml):** FIN.LIQ.position -> `xbrl_current_ratio`, FIN.LIQ.working_capital -> `xbrl_working_capital`

     **Debt (fin/balance.yaml):** FIN.DEBT.structure -> `xbrl_debt_to_ebitda`, FIN.DEBT.coverage -> `xbrl_interest_coverage`, FIN.DEBT.maturity -> `xbrl_debt_maturity`

     **Profitability (fin/income.yaml):** FIN.PROFIT.revenue -> `xbrl_revenue_growth`, FIN.PROFIT.margins -> `xbrl_operating_margin`

     **Temporal (fin/temporal.yaml):** FIN.TEMPORAL.revenue_deceleration -> `xbrl_revenue_deceleration`, FIN.TEMPORAL.margin_compression -> `xbrl_margin_compression`, FIN.TEMPORAL.operating_margin_compression -> `xbrl_op_margin_compression`, FIN.TEMPORAL.cfo_ni_divergence -> `xbrl_cfo_ni_divergence`, FIN.TEMPORAL.profitability_trend -> `xbrl_profitability_trend`, FIN.TEMPORAL.cash_flow_deterioration -> `xbrl_cfo_deterioration` + remaining temporal signals

     **Forensic (fin/forensic.yaml):** FIN.FORENSIC.fis_composite -> `forensic_m_score_composite`, FIN.FORENSIC.enhanced_sloan -> `forensic_sloan_accruals`, FIN.FORENSIC.accrual_intensity -> `forensic_cash_flow_manipulation` + remaining forensic signals

     **Quality (fin/accounting.yaml):** FIN.QUALITY.dso_ar_divergence -> `xbrl_dso_ar_divergence`, FIN.QUALITY.q4_revenue_concentration -> `xbrl_q4_concentration`, FIN.QUALITY.quality_of_earnings -> `xbrl_earnings_quality`

     **Business (biz/core.yaml):** BIZ.SIZE.market_cap -> `xbrl_market_cap`, BIZ.MODEL.revenue_segment -> `xbrl_revenue_segments`, BIZ.MODEL.revenue_geo -> `xbrl_revenue_geo`

  3. **Extend mappers** for XBRL data access: Update `_map_fin_liquidity()`, `_map_fin_debt()`, `_map_fin_profitability()`, `_map_temporal_check()`, `_map_quality_check()` to:
     - Read XBRL data from `extracted.financials.statements.*` (already available)
     - Read quarterly data from `extracted.financials.quarterly_xbrl` for temporal signals
     - Return both `xbrl_{field}` (new primary) AND old key (for shadow comparison)
     - Call `_log_xbrl_shadow()` when both values are available

  4. **Field routing** (signal_field_routing.py): Add all new `xbrl_*` field_keys to FIELD_FOR_CHECK dict.

  5. **Tests** (tests/test_signal_xbrl_upgrade.py):
     - Verify at least 40 signals in fin/*.yaml have `xbrl_` or `forensic_` prefixed field_keys
     - Verify mappers return XBRL-keyed values when statements data is present
     - Verify mappers return legacy values as fallback when XBRL data is None
     - Verify _log_xbrl_shadow creates table without error
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run python -c "
import yaml
from pathlib import Path
xbrl_count = 0
for f in Path('src/do_uw/brain/signals/fin').glob('*.yaml'):
    with open(f) as fh:
        sigs = yaml.safe_load(fh)
    if sigs:
        for s in sigs:
            fk = s.get('data_strategy', {}).get('field_key', '')
            if fk.startswith('xbrl_') or fk.startswith('forensic_'):
                xbrl_count += 1
for f in Path('src/do_uw/brain/signals/biz').glob('*.yaml'):
    with open(f) as fh:
        sigs = yaml.safe_load(fh)
    if sigs:
        for s in sigs:
            fk = s.get('data_strategy', {}).get('field_key', '')
            if fk.startswith('xbrl_'):
                xbrl_count += 1
print(f'XBRL-sourced signals: {xbrl_count}')
assert xbrl_count >= 40, f'Only {xbrl_count} XBRL-sourced (need 40+)'
" && uv run pytest tests/test_signal_xbrl_upgrade.py -x -q --tb=short && uv run pytest tests/ -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>45 signals upgraded to XBRL field_keys. Shadow evaluation infrastructure in place logging old vs new values. Mappers extended to read XBRL statements and quarterly data. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance 28 dual-source signals</name>
  <files>
    src/do_uw/brain/signals/fin/accounting.yaml
    src/do_uw/brain/signals/fin/forensic.yaml
    src/do_uw/brain/signals/gov/effect.yaml
    src/do_uw/stages/analyze/signal_mappers_analytical.py
  </files>
  <action>
  Enhance 28 signals from Bucket B (XBRL-enhanceable) to use XBRL for numeric threshold evaluation while retaining LLM narrative for context.

  **Pattern:** For each enhanced signal:
  - Primary field_key changes to `xbrl_*` variant (for threshold evaluation)
  - Add `_narrative` suffixed field_key for LLM context retention
  - Mapper returns BOTH: `{"xbrl_restatement_flag": 0/1, "restatement_narrative": "...text..."}`
  - Threshold evaluates against the `xbrl_*` key; narrative preserved for display

  **Key signals to enhance (from audit Bucket B):**

  Accounting (fin/accounting.yaml): FIN.ACCT.restatement, FIN.ACCT.auditor_opinion, FIN.ACCT.material_weakness, FIN.ACCT.going_concern, FIN.ACCT.ic_weakness, FIN.ACCT.significant_estimates, FIN.ACCT.related_party_disclosure, FIN.ACCT.segment_reporting

  Debt (fin/balance.yaml or forensic.yaml): FIN.DEBT.credit_rating, FIN.DEBT.covenants, FIN.DEBT.convertible_exposure

  Governance (gov/effect.yaml): GOV.BOARD.independence, GOV.EXEC.ceo_profile, GOV.EXEC.cfo_profile, GOV.BOARD.expertise_gaps

  **For each signal:**
  1. Update `data_strategy.field_key` to `xbrl_{metric}` variant
  2. Keep or add `data_strategy.narrative_key` pointing to LLM extraction path
  3. Update mapper to return both numeric XBRL value and LLM narrative context

  **Do NOT change thresholds** -- shadow evaluation first, threshold adjustment later.

  Run `_log_xbrl_shadow()` for each enhanced signal where both old and new values are present.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run python -c "
import yaml
from pathlib import Path
enhanced = 0
for d in ['fin', 'gov', 'biz']:
    p = Path(f'src/do_uw/brain/signals/{d}')
    for f in p.glob('*.yaml'):
        with open(f) as fh:
            sigs = yaml.safe_load(fh)
        if sigs:
            for s in sigs:
                ds = s.get('data_strategy', {})
                fk = ds.get('field_key', '')
                nk = ds.get('narrative_key', '')
                if fk.startswith('xbrl_') and nk:
                    enhanced += 1
print(f'Dual-source enhanced signals: {enhanced}')
assert enhanced >= 20, f'Only {enhanced} enhanced (need 20+, target 28)'
" && uv run pytest tests/ -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>28 signals enhanced with dual XBRL+LLM sources. XBRL used for threshold evaluation, LLM narrative retained for display context. Shadow evaluation active for all changes. All tests pass.</done>
</task>

</tasks>

<verification>
1. `uv run python -c "..."` -- verify 45+ signals have XBRL field_keys
2. `uv run python -c "..."` -- verify 20+ signals have dual XBRL+narrative keys
3. `uv run pytest tests/ -x -q` -- all tests pass
4. No signal thresholds were modified (only field_keys changed)
</verification>

<success_criteria>
- 45 signals upgraded to XBRL-sourced field_keys
- 28 signals enhanced with dual XBRL + LLM sources
- Shadow evaluation logging operational (brain_xbrl_shadow table)
- Zero test regressions
- No threshold changes (shadow eval validates before threshold adjustment)
</success_criteria>

<output>
After completion, create `.planning/phases/70-signal-integration/70-02-SUMMARY.md`
</output>
