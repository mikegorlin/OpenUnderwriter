---
phase: 70-signal-integration
plan: 03
type: execute
wave: 3
depends_on: [70-02]
files_modified:
  - src/do_uw/brain/signals/gov/effect.yaml
  - src/do_uw/brain/signals/gov/insider.yaml
  - src/do_uw/brain/signals/fin/forensic.yaml
  - src/do_uw/brain/signals/fin/accounting.yaml
  - src/do_uw/brain/signals/fwrd/warn_sentiment.yaml
  - src/do_uw/brain/signals/fwrd/warn_ops.yaml
  - src/do_uw/stages/analyze/signal_mappers_analytical.py
  - src/do_uw/stages/analyze/signal_mappers_forward.py
  - src/do_uw/stages/analyze/signal_field_routing.py
  - tests/test_signal_reactivation.py
  - tests/test_signal_cross_ticker.py
autonomous: true
requirements: [SIG-05, SIG-07, SIG-08]

must_haves:
  truths:
    - "15+ previously SKIPPED signals now produce TRIGGERED or CLEAR results"
    - "35 web-search-candidate signals have data_strategy wired to Brave Search / Exa acquisition"
    - "Cross-ticker validation passes for AAPL, RPM, SNA, V, WWD with documented deltas"
    - "Shadow evaluation shows zero UNEXPECTED signal flips (expected flips from SKIPPED->TRIGGERED are documented)"
  artifacts:
    - path: "tests/test_signal_reactivation.py"
      provides: "Verifies reactivated signals wire correctly"
    - path: "tests/test_signal_cross_ticker.py"
      provides: "Cross-ticker signal regression baseline"
  key_links:
    - from: "brain/signals/gov/effect.yaml"
      to: "signal_mappers_analytical.py"
      via: "reactivated signal field_keys"
      pattern: "GOV.EFFECT"
    - from: "brain/signals/fwrd/warn_*.yaml"
      to: "signal_mappers_forward.py"
      via: "web search data_strategy"
      pattern: "FWRD.WARN"
---

<objective>
Reactivate 15+ broken/skipped signals with newly available XBRL and web search data. Wire 35 web-search-candidate signals to Brave Search + Exa. Run cross-ticker validation on 5 tickers and update baselines.

Purpose: Closes the gap between signals defined in brain YAML and signals that actually fire. Completes the Phase 70 signal integration by ensuring maximum signal coverage.
Output: Reactivated signals, web search wiring, cross-ticker validation baselines, delta documentation.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-signal-integration/70-RESEARCH.md
@.planning/research/signal-xbrl-audit.md
@.planning/phases/70-signal-integration/70-01-SUMMARY.md
@.planning/phases/70-signal-integration/70-02-SUMMARY.md

<interfaces>
<!-- From Plan 01+02: mapper with analysis param + shadow evaluation -->
From signal_mappers_analytical.py (after Plan 01+02):
```python
def map_phase26_check(
    signal_id: str,
    check_config: dict[str, Any],
    extracted: ExtractedData,
    company: CompanyProfile | None = None,
    analysis: AnalysisResults | None = None,
) -> dict[str, Any] | None:

def _log_xbrl_shadow(
    run_id: str, signal_id: str, ticker: str,
    old_source: str, old_value: Any,
    new_source: str, new_value: Any,
    status_match: bool,
) -> None:
```

From research -- broken signal reactivation candidates:
- GOV.EFFECT.late_filing -> SEC EDGAR filing dates
- GOV.EFFECT.nt_filing -> SEC EDGAR filing type filter
- FIN.QUALITY.q4_revenue_concentration -> Quarterly XBRL (Phase 68)
- FIN.QUALITY.deferred_revenue_trend -> XBRL multi-period (Phase 67-68)
- LIT.DEFENSE.forum_selection -> 10-K text extraction
- LIT.DEFENSE.contingent_liabilities -> XBRL contingent concepts

From research -- web search signal wiring:
- FWRD.WARN.whistleblower_exposure -> Brave Search: "{company} whistleblower"
- FWRD.WARN.social_sentiment -> Brave/Exa: "{company} employee complaints"
- FWRD.WARN.glassdoor_sentiment -> Brave Search: "{company} glassdoor rating"
- GOV.BOARD.prior_litigation -> Brave Search: "{director_name} litigation"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reactivate 15+ broken signals + wire 35 web search signals</name>
  <files>
    src/do_uw/brain/signals/gov/effect.yaml
    src/do_uw/brain/signals/gov/insider.yaml
    src/do_uw/brain/signals/fin/forensic.yaml
    src/do_uw/brain/signals/fin/accounting.yaml
    src/do_uw/brain/signals/fwrd/warn_sentiment.yaml
    src/do_uw/brain/signals/fwrd/warn_ops.yaml
    src/do_uw/stages/analyze/signal_mappers_analytical.py
    src/do_uw/stages/analyze/signal_mappers_forward.py
    src/do_uw/stages/analyze/signal_field_routing.py
    tests/test_signal_reactivation.py
  </files>
  <action>
  **Part A: Reactivate 15+ broken/skipped signals**

  Update signal YAML definitions and mapper wiring for each reactivation candidate:

  1. **GOV.EFFECT.late_filing** (gov/effect.yaml): Update field_key to `sec_filing_timeliness`. Mapper reads `extracted.filings` metadata for filing dates vs deadline. If filing dates not available in current extraction, wire to `extracted.filing_analysis` attributes.

  2. **GOV.EFFECT.nt_filing** (gov/effect.yaml): Update field_key to `sec_nt_filing_detected`. Mapper checks for NT 10-K / NT 10-Q filing types in acquired filing list.

  3. **FIN.QUALITY.q4_revenue_concentration** (fin/accounting.yaml): Update field_key to `xbrl_q4_concentration`. Mapper reads quarterly XBRL data (`extracted.financials.quarterly_xbrl`) and computes Q4 revenue as % of annual.

  4. **FIN.QUALITY.deferred_revenue_trend** (fin/accounting.yaml): Update field_key to `xbrl_deferred_revenue_trend`. Mapper reads multi-period XBRL deferred revenue concept.

  5. **LIT.DEFENSE.forum_selection**: Wire to existing 10-K text extraction for forum selection clause detection.

  6. **LIT.DEFENSE.contingent_liabilities**: Wire to XBRL contingent liability concepts from Phase 67 expansion.

  7-15+. Wire remaining broken signals from the audit list. For each:
     - Identify the data source now available (XBRL statement, quarterly, forensic result, or web search)
     - Update field_key in YAML
     - Add mapper logic to extract the value
     - Add field_key to FIELD_FOR_CHECK if not already present

  **Part B: Wire 35 web-search-candidate signals**

  For signals in Bucket C (web search candidates), update `data_strategy` to reference web search acquisition:

  1. Update FWRD.WARN.* signals in `warn_sentiment.yaml` and `warn_ops.yaml`:
     - `data_strategy.field_key` points to web search results stored on state (e.g., `web_search_whistleblower`)
     - `data_strategy.primary_source: WEB_SEARCH`
     - `data_strategy.search_query` field added with template: `"{company_name} {risk_term}"`
     - Mapper returns search hit count and relevance score from existing blind_spot sweep results

  2. Update GOV.BOARD.prior_litigation, GOV.BOARD.character_conduct, GOV.EXEC.character_conduct:
     - Wire to existing blind spot sweep results that already search for executive names + litigation terms
     - Field_key references the appropriate state path where web search results are stored

  3. For each web search signal, the mapper in `signal_mappers_forward.py`:
     - Reads from `extracted.sentiment.web_search_results` or `extracted.blind_spot_findings`
     - Filters by relevance to the signal's risk topic
     - Returns a count/boolean indicating whether relevant results were found
     - Signals that find no web search data return CLEAR (absence of risk signal)

  **IMPORTANT:** These signals depend on the existing web search infrastructure in ACQUIRE. They do NOT create new web search calls -- they consume results already collected by the blind spot sweep and news acquisition. Group queries by topic as recommended in research (one search serves multiple signals).

  **Tests** (tests/test_signal_reactivation.py):
  - Verify at least 15 previously-SKIPPED signal IDs now have valid field_keys
  - Verify mapper returns non-None for reactivated signals when data present
  - Verify web search signals return CLEAR (not SKIPPED) when no search results found
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/test_signal_reactivation.py -x -q --tb=short && uv run pytest tests/ -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>15+ broken signals reactivated with valid field_keys. 35 web search signals wired to blind spot / news search results. Tests verify reactivation wiring. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-ticker validation + baseline recalibration</name>
  <files>
    tests/test_signal_cross_ticker.py
  </files>
  <action>
  **CRITICAL: This task runs LAST, after all signal changes are complete.** It validates that signal changes produce expected results across multiple tickers.

  1. **Create cross-ticker signal validation test** (tests/test_signal_cross_ticker.py):

     The test loads saved state.json files from output directories (if available) and runs signal evaluation against them, comparing results to expected baselines.

     Structure:
     ```python
     @pytest.mark.parametrize("ticker", ["WWD", "RPM", "SNA", "V", "AAPL"])
     def test_signal_evaluation_baseline(ticker):
         """Verify signal evaluation produces expected baseline results."""
         state_path = Path(f"output/{ticker}*/state.json")
         # Skip if no output available
         matches = sorted(Path("output").glob(f"{ticker}*/state.json"))
         if not matches:
             pytest.skip(f"No output for {ticker}")
         # Load state, run signal evaluation, compare to baseline
     ```

  2. **Validation checks per ticker:**
     - Total signal count is within 5% of baseline (no mass breakage)
     - TRIGGERED count is reasonable (not all TRIGGERED or all CLEAR)
     - No signal that was previously TRIGGERED flipped to SKIPPED (regression)
     - New forensic signals (FIN.FORENSIC.*) produce at least 50% non-SKIPPED results for tickers with XBRL data
     - Previously SKIPPED signals that were reactivated now show TRIGGERED or CLEAR (not still SKIPPED)

  3. **Document signal deltas:** The test should output a summary showing:
     - Per-ticker: total signals, triggered, clear, skipped
     - Signals that changed status vs previous baseline
     - New signals added (forensic, opportunity)
     - Reactivated signals that moved from SKIPPED to TRIGGERED/CLEAR

  4. **Baseline handling:** Store expected baselines as JSON fixtures in `tests/fixtures/signal_baselines/`. If fixture doesn't exist yet, the test creates it on first run (golden master pattern). Subsequent runs compare against the fixture.

  **NOTE on shadow evaluation validation:** After all signal changes, query the `brain_xbrl_shadow` table to report:
  - Total shadow comparisons logged
  - Signals with value_delta > 10% (flagged for review)
  - Status mismatches (old TRIGGERED -> new CLEAR or vice versa)

  This is informational logging, not a test gate. Shadow results inform future threshold calibration.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/test_signal_cross_ticker.py -x -q --tb=short -v 2>&1 | tail -20</automated>
  </verify>
  <done>Cross-ticker validation tests created for 5 tickers. Golden baselines established (or generated on first run). Signal delta report documents all changes. Shadow evaluation results logged. All tests pass.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_signal_reactivation.py -v` -- reactivated signals verified
2. `uv run pytest tests/test_signal_cross_ticker.py -v` -- cross-ticker validation passes
3. `uv run pytest tests/ -x -q --tb=short` -- all tests pass
4. Shadow evaluation table has entries (informational check)
</verification>

<success_criteria>
- 15+ previously SKIPPED signals now produce TRIGGERED or CLEAR results
- 35 web search signals wired to existing blind spot / news data
- Cross-ticker validation passes for available test tickers
- Golden baselines established for future regression detection
- Shadow evaluation log shows comparisons for upgraded signals
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/70-signal-integration/70-03-SUMMARY.md`
</output>
