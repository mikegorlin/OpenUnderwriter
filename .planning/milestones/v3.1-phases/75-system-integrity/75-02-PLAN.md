---
phase: 75-system-integrity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/brain/test_template_facet_audit.py
  - src/do_uw/brain/sections/scoring.yaml
autonomous: true
requirements: [SYS-04, SYS-05]

must_haves:
  truths:
    - "CI test asserts every facet template field points to an existing .html.j2 file"
    - "CI test asserts every non-wrapper template is referenced by at least one facet"
    - "Orphaned templates are either reclassified as wrappers or consolidated"
  artifacts:
    - path: "tests/brain/test_template_facet_audit.py"
      provides: "CI validation for template-facet integrity"
      exports: ["test_no_dangling_facet_templates", "test_no_orphaned_facet_templates", "test_wrapper_templates_exist"]
  key_links:
    - from: "tests/brain/test_template_facet_audit.py"
      to: "brain/sections/*.yaml"
      via: "load_all_sections() + glob templates"
      pattern: "load_all_sections"
    - from: "tests/brain/test_template_facet_audit.py"
      to: "templates/html/sections/"
      via: "rglob *.html.j2"
      pattern: "rglob.*html\\.j2"
---

<objective>
Create automated template-to-facet validation CI test and resolve orphaned templates.

Purpose: The brain-driven architecture routes rendering through facet YAML definitions pointing to templates. Without CI validation, facet references can dangle (pointing to deleted templates) or templates can become orphaned (never rendered). Research found 0 dangling but 16 orphaned -- 15 are section wrappers (valid) and 1 may be truly orphaned.

Output: CI test for template-facet integrity, cleaned up orphan status.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/75-system-integrity/75-RESEARCH.md

@src/do_uw/brain/brain_section_schema.py

<interfaces>
<!-- From brain_section_schema.py: -->
<!-- load_all_sections(sections_dir: Path) -> dict[str, SectionSpec] -->
<!-- SectionSpec has .facets: list[FacetSpec] -->
<!-- FacetSpec has .template: str (relative path like "sections/company/identity.html.j2") -->
<!-- FacetSpec has .id: str -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create template-facet audit CI test</name>
  <files>tests/brain/test_template_facet_audit.py</files>
  <action>
Create `tests/brain/test_template_facet_audit.py` with these tests:

1. `test_no_dangling_facet_templates()`: Load all sections via `load_all_sections()`. For every facet in every section, assert `facet.template` resolves to an existing file under `src/do_uw/templates/html/`. Use Path resolution. Report which facet ID has the dangling reference on failure.

2. `test_no_orphaned_facet_templates()`: Collect all facet-referenced templates from all sections. Glob all `*.html.j2` files under `src/do_uw/templates/html/sections/`. Define a WRAPPER_TEMPLATES set containing known section-level wrapper templates (the 15 identified in research: company.html.j2, governance.html.j2, litigation.html.j2, market.html.j2, financial.html.j2, scoring.html.j2, executive.html.j2, ai_risk.html.j2, red_flags.html.j2, identity.html.j2, cover.html.j2, financial_statements.html.j2, scoring_hazard.html.j2, scoring_perils.html.j2, scoring_peril_map.html.j2). For every template NOT in WRAPPER_TEMPLATES, assert it appears in the facet-referenced set. Report which template is orphaned on failure.

3. `test_wrapper_templates_exist()`: Assert every template in the WRAPPER_TEMPLATES set actually exists on disk. This catches stale wrapper entries.

4. `test_all_sections_have_facets()`: Assert every section in load_all_sections() has at least one facet defined (no empty facets lists).

5. `test_facet_ids_unique_within_section()`: Within each section, all facet IDs must be unique.

Use pytest parametrize where it improves error reporting (e.g., parametrize over sections for dangling check).
  </action>
  <verify>
    <automated>uv run pytest tests/brain/test_template_facet_audit.py -x -q</automated>
  </verify>
  <done>All 5 template-facet audit tests pass. Zero dangling references, zero orphaned templates (all accounted for as wrappers or facet-referenced).</done>
</task>

<task type="auto">
  <name>Task 2: Investigate and resolve scoring/nlp_analysis.html.j2 orphan</name>
  <files>src/do_uw/brain/sections/scoring.yaml</files>
  <action>
Research identified `scoring/nlp_analysis.html.j2` as potentially truly orphaned (not a wrapper, not facet-referenced).

1. Check if `sections/scoring/nlp_analysis.html.j2` exists on disk.
2. Check if any section YAML references it via a facet.
3. Check the STATE.md decision: "[Phase 61]: Updated existing nlp_analysis facet in scoring.yaml to point to nlp_dashboard template" -- this suggests nlp_analysis.html.j2 was REPLACED by nlp_dashboard.html.j2.
4. If nlp_analysis.html.j2 is truly orphaned (replaced by nlp_dashboard):
   - If the content is identical or subset of nlp_dashboard, delete it.
   - If it has unique content, add it to WRAPPER_TEMPLATES in the test (if it's included by another template) or wire it to a facet.
5. If it turns out to not exist (research was wrong), update the WRAPPER_TEMPLATES set in the test accordingly.
6. Ensure the test from Task 1 passes cleanly after resolution.

Do NOT delete templates that are `{% include %}`-d by other templates -- check for include references first.
  </action>
  <verify>
    <automated>uv run pytest tests/brain/test_template_facet_audit.py -x -q</automated>
  </verify>
  <done>All orphaned templates accounted for. CI test passes with zero orphans, zero dangles.</done>
</task>

</tasks>

<verification>
- Template-facet audit tests pass: `uv run pytest tests/brain/test_template_facet_audit.py -x -q`
- Existing brain tests still pass: `uv run pytest tests/brain/ -x -q`
- No legitimate templates were deleted
</verification>

<success_criteria>
- CI test validates zero dangling facet references and zero orphaned templates
- All section wrappers are documented in the WRAPPER_TEMPLATES set
- The nlp_analysis orphan is resolved (deleted, reclassified, or re-wired)
</success_criteria>

<output>
After completion, create `.planning/phases/75-system-integrity/75-02-SUMMARY.md`
</output>
