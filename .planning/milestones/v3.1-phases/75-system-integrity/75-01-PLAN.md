---
phase: 75-system-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/TIER1_MANIFEST.md
  - src/do_uw/brain/signals/base/peer.yaml
  - docs/SIGNAL_AUTHOR_GUIDE.md
  - tests/brain/test_foundational_coverage.py
autonomous: true
requirements: [SYS-01, SYS-02, SYS-03]

must_haves:
  truths:
    - "Tier 1 manifest document exists listing every always-acquired data source"
    - "Every Tier 1 data source has a corresponding foundational signal in brain/signals/base/"
    - "Signal author guide documents foundational vs evaluative, acquisition blocks, gap_bucket"
  artifacts:
    - path: "docs/TIER1_MANIFEST.md"
      provides: "Explicit manifest of all always-acquired data sources"
      contains: "Frames API"
    - path: "src/do_uw/brain/signals/base/peer.yaml"
      provides: "Foundational signal for Frames API peer benchmarking"
      contains: "BASE.PEER.frames"
    - path: "docs/SIGNAL_AUTHOR_GUIDE.md"
      provides: "Guide for adding/modifying brain signals"
      contains: "foundational"
    - path: "tests/brain/test_foundational_coverage.py"
      provides: "CI test ensuring foundational signals cover Tier 1"
      exports: ["test_all_tier1_sources_have_foundational_signal", "test_no_duplicate_foundational_signals"]
  key_links:
    - from: "docs/TIER1_MANIFEST.md"
      to: "brain/signals/base/"
      via: "traceability table mapping each data source to signal ID"
      pattern: "BASE\\."
    - from: "tests/brain/test_foundational_coverage.py"
      to: "brain/signals/base/"
      via: "loading all foundational signals and checking against manifest"
      pattern: "type.*foundational"
---

<objective>
Create the Tier 1 data manifest document, fill the Frames API foundational signal gap, and write the signal author guide.

Purpose: The brain-driven architecture requires explicit traceability -- every always-acquired data source must map to a foundational signal. Phase 72 built the Frames API client but never declared a foundational signal for it. This plan closes that gap and documents the full manifest for provenance.

Output: Tier 1 manifest document, peer.yaml with BASE.PEER.frames signal, signal author guide, CI coverage test.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-system-integrity/75-RESEARCH.md

@src/do_uw/brain/signals/base/filings.yaml
@src/do_uw/brain/signals/base/market.yaml
@src/do_uw/brain/signals/base/xbrl.yaml
@src/do_uw/brain/signals/base/forensics.yaml
@src/do_uw/brain/signals/base/litigation.yaml
@src/do_uw/brain/signals/base/news.yaml

<interfaces>
<!-- Foundational signals use type: foundational in brain/signals/base/*.yaml -->
<!-- BrainSignalEntry schema: id, name, type (foundational|evaluate), category, acquisition, data_strategy -->
<!-- load_all_sections() from brain_section_schema.py loads section YAML -->
<!-- BrainDBLoader.load_all_signals() loads all signals including foundational -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Tier 1 manifest + Frames foundational signal + coverage test</name>
  <files>docs/TIER1_MANIFEST.md, src/do_uw/brain/signals/base/peer.yaml, tests/brain/test_foundational_coverage.py</files>
  <action>
1. Create `docs/TIER1_MANIFEST.md` with a traceability table listing every always-acquired data source and its corresponding foundational signal. Structure:
   - Table columns: Data Source | Signal ID | File | Description | Fields
   - Group by category: Filings (10-K, 10-Q, DEF 14A, 8-K, Form 4), Market (stock price, institutional ownership, short interest, insider trading), XBRL (balance sheet, income stmt, cash flow, quarterly, derived), Forensics (7-8 modules), Litigation (SCAC, 10-K Item 3, CourtListener), News (blind spot pre/post, company news), Peer (Frames API - NEW)
   - Read all 6 existing base/*.yaml files to enumerate the 31 existing foundational signals
   - Note the Two-Tier acquisition model explanation at the top

2. Create `src/do_uw/brain/signals/base/peer.yaml` with foundational signal:
   - Signal ID: `BASE.PEER.frames`
   - type: foundational
   - category: peer_benchmarking
   - name: "SEC Frames API Peer Benchmarking"
   - description: "Cross-filer percentile ranking via SEC Frames API for 10+ key metrics"
   - acquisition block: fields listing `analyzed.benchmarks.frames_percentiles`, `analyzed.benchmarks.sector_percentiles`
   - data_strategy: field_key pointing to frames benchmark data paths
   - Follow exact YAML format of existing base/*.yaml files (e.g., base/xbrl.yaml)

3. Create `tests/brain/test_foundational_coverage.py`:
   - `test_all_tier1_sources_have_foundational_signal()`: load all base/*.yaml, verify count >= 32 (31 existing + 1 new)
   - `test_foundational_signals_have_type_field()`: every signal in base/ has `type: foundational`
   - `test_no_duplicate_foundational_signal_ids()`: no duplicate signal IDs across base/ files
   - `test_all_foundational_signals_have_acquisition_block()`: every foundational signal has an acquisition section with fields
   - Use PyYAML to load YAML files directly (follow existing test patterns in tests/brain/)
  </action>
  <verify>
    <automated>uv run pytest tests/brain/test_foundational_coverage.py -x -q</automated>
  </verify>
  <done>Tier 1 manifest document lists all 32+ foundational signals with traceability. BASE.PEER.frames signal exists in peer.yaml. CI test validates coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Create signal author guide</name>
  <files>docs/SIGNAL_AUTHOR_GUIDE.md</files>
  <action>
Create `docs/SIGNAL_AUTHOR_GUIDE.md` documenting:

1. **Signal Types**: foundational (type: foundational, always-acquired Tier 1 data) vs evaluative (type: evaluate, threshold-based checks). When to use each.

2. **Adding a Foundational Signal**: place in brain/signals/base/, must have acquisition block with fields, used for Tier 1 data manifest traceability. Example from base/xbrl.yaml.

3. **Adding an Evaluative Signal**: place in brain/signals/{domain}/, must have data_strategy with field_key, threshold, comparison_operator. Example from fin/forensic_xbrl.yaml.

4. **Acquisition Blocks**: when to add `acquisition:` (signal needs data NOT in Tier 1 manifest), vs when to reference existing data (signal uses data already staged by Tier 1). Include examples of both patterns.

5. **gap_bucket and gap_keywords**: what they do (help categorize data gaps for future resolution), when to populate them, examples.

6. **Signal Naming Convention**: domain prefix (BASE, FIN, GOV, LIT, MKT, OPS), sub-category, specific check name. Examples.

7. **data_strategy Fields**: field_key (path to state data), narrative_key (for dual-source), threshold, comparison_operator, zone definitions.

8. **Testing**: how to verify a new signal works (run pipeline, check brain_signal_runs table, verify fire/clear status).

Keep under 300 lines. Reference existing YAML files as examples rather than duplicating content.
  </action>
  <verify>
    <automated>test -f docs/SIGNAL_AUTHOR_GUIDE.md && wc -l docs/SIGNAL_AUTHOR_GUIDE.md | awk '{if ($1 > 50 && $1 < 400) print "PASS: "$1" lines"; else print "FAIL: "$1" lines"}'</automated>
  </verify>
  <done>Signal author guide exists with sections covering foundational vs evaluative, acquisition blocks, gap_bucket, naming conventions, data_strategy, and testing.</done>
</task>

</tasks>

<verification>
- All foundational coverage tests pass
- Tier 1 manifest document lists 32+ signals with traceability
- Signal author guide covers all required topics
- Existing tests still pass: `uv run pytest tests/brain/ -x -q`
</verification>

<success_criteria>
- docs/TIER1_MANIFEST.md exists with complete traceability table
- BASE.PEER.frames foundational signal exists in peer.yaml
- CI test validates 100% Tier 1 coverage
- Signal author guide is comprehensive and under 300 lines
</success_criteria>

<output>
After completion, create `.planning/phases/75-system-integrity/75-01-SUMMARY.md`
</output>
