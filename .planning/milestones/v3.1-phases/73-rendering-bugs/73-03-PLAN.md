---
phase: 73-rendering-bugs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/stages/extract/extract_litigation.py
  - src/do_uw/stages/extract/prompts.py
  - src/do_uw/stages/score/red_flag_gates.py
  - src/do_uw/stages/score/red_flag_gates_enhanced.py
  - src/do_uw/stages/analyze/signal_mappers_ext.py
  - src/do_uw/templates/html/styles.css
  - src/do_uw/templates/html/sections/identity.html.j2
  - src/do_uw/templates/html/base.html.j2
  - tests/test_false_sca_filter.py
autonomous: true
requirements:
  - RENDER-06
  - RENDER-07
  - RENDER-08
  - RENDER-09

must_haves:
  truths:
    - "Boilerplate 10-K litigation language is NOT classified as active SCA"
    - "CRF-1 red flag does not trigger on boilerplate-only litigation disclosures"
    - "PDF header does not overlap with first-page content"
    - "Company logo displays properly in both HTML browser view and PDF"
    - "All new Phase 73 templates render correctly in PDF (details expanded, charts visible)"
  artifacts:
    - path: "src/do_uw/stages/extract/prompts.py"
      provides: "Hardened LLM extraction prompt with explicit boilerplate rejection examples"
    - path: "src/do_uw/stages/score/red_flag_gates.py"
      provides: "Case specificity gate for SCA classification"
    - path: "tests/test_false_sca_filter.py"
      provides: "Tests for false SCA filtering across all 3 layers"
  key_links:
    - from: "prompts.py"
      to: "extract_litigation.py"
      via: "LLM prompt used in litigation extraction"
      pattern: "LITIGATION.*PROMPT|litigation.*prompt"
    - from: "signal_mappers_ext.py"
      to: "red_flag_gates.py"
      via: "Both call _is_boilerplate_litigation()"
      pattern: "_is_boilerplate_litigation"
---

<objective>
Fix the false SCA classification bug (boilerplate 10-K language triggering CRF-1), fix PDF header overlap, complete company logo embedding, and ensure all Phase 73 templates work in PDF.

Purpose: Eliminate false positive red flags that undermine analyst trust, fix visual bugs in PDF output, and ensure HTML/PDF parity for all new templates.

Output: 3-layer false SCA fix (prompt + pattern + specificity gate), CSS fix for PDF header, logo completion, PDF parity verification.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-rendering-bugs/73-CONTEXT.md
@.planning/phases/73-rendering-bugs/73-RESEARCH.md

@src/do_uw/stages/extract/extract_litigation.py
@src/do_uw/stages/extract/prompts.py
@src/do_uw/stages/score/red_flag_gates.py
@src/do_uw/stages/score/red_flag_gates_enhanced.py
@src/do_uw/stages/analyze/signal_mappers_ext.py
@src/do_uw/templates/html/styles.css
@src/do_uw/templates/html/sections/identity.html.j2
@src/do_uw/templates/html/base.html.j2
@src/do_uw/stages/render/html_renderer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix false SCA classification with 3-layer filter</name>
  <files>
    src/do_uw/stages/extract/prompts.py
    src/do_uw/stages/analyze/signal_mappers_ext.py
    src/do_uw/stages/score/red_flag_gates.py
    src/do_uw/stages/score/red_flag_gates_enhanced.py
    tests/test_false_sca_filter.py
  </files>
  <behavior>
    - Test: Boilerplate case "Company is subject to various legal proceedings in the ordinary course of business" -> classified as boilerplate, NOT SCA
    - Test: Boilerplate variant "Company is party to legal matters arising in the ordinary course" -> boilerplate
    - Test: Boilerplate variant "involved in certain legal proceedings and claims" -> boilerplate
    - Test: Real SCA "Smith v. Company Inc., Case No. 1:24-cv-01234, S.D.N.Y." -> NOT boilerplate, IS SCA
    - Test: Case lacking ALL of named plaintiff, court, filing date -> specificity gate triggers, treated as boilerplate
    - Test: Case with named plaintiff but no court -> NOT blocked by specificity gate (partial specificity OK)
    - Test: CRF-1 red flag does NOT trigger when only boilerplate SCAs exist
    - Test: CRF-1 red flag DOES trigger when real SCA exists alongside boilerplate
    - Test: Unverified SCA (no web/SCAC corroboration) gets LOW confidence + "unverified" caveat in CRF description
  </behavior>
  <action>
    **Layer 1: Prompt hardening (prompts.py)**
    - Find the litigation extraction prompt (around line 45-51 per research)
    - Add explicit rejection examples:
      - "Do NOT extract generic disclosures like 'Company is subject to various legal proceedings arising in the ordinary course of business'"
      - "Do NOT extract 'Company may be involved in certain litigation and claims' or similar boilerplate"
      - "Only extract cases with SPECIFIC details: named parties, court/jurisdiction, case numbers, or specific allegations"
    - Keep existing prompt structure, just add rejection examples

    **Layer 2: Pattern expansion (signal_mappers_ext.py)**
    - Find `_is_boilerplate_litigation()` function and its `_BOILERPLATE_PATTERNS` list
    - Add these patterns (case-insensitive matching):
      - "PARTY TO LEGAL MATTERS"
      - "LEGAL MATTERS ARISING"
      - "INVOLVED IN LITIGATION"
      - "SUBJECT TO CLAIMS"
      - "LEGAL PROCEEDINGS AND CLAIMS"
      - "INVOLVED IN CERTAIN LEGAL"
      - "SUBJECT TO VARIOUS LEGAL"
      - "PARTY TO CERTAIN LEGAL"
      - "NORMAL COURSE OF BUSINESS" (if not already present)
    - Ensure signal_mappers_ext.py stays under 500 lines

    **Layer 3: Case specificity gate (red_flag_gates.py)**
    - In the SCA evaluation section (around line 220-257), add a specificity check BEFORE CRF-1 triggers:
    - `_has_case_specificity(case: dict) -> bool`: Returns True if case has AT LEAST ONE of:
      - Named plaintiff (case name contains " v. " or " vs. ")
      - Court/jurisdiction (non-empty court field or case number pattern like "X:XX-cv-XXXXX")
      - Specific filing date (date field is not None and not generic year-only)
    - Cases failing ALL three specificity checks -> treated as boilerplate, excluded from CRF-1 count
    - Cases passing boilerplate name check BUT failing specificity -> log warning, still exclude

    **Unverified SCA handling (red_flag_gates.py or red_flag_gates_enhanced.py)**
    - When SCA passes filters but lacks web search / SCAC / CourtListener corroboration:
      - Downgrade confidence to LOW
      - Append "(unverified)" to the red flag description text
      - CRF-1 still triggers (conservative approach) but description notes unverified status
    - This requires checking whether the case has corroboration data -- look at `case.get("sources")` or similar field

    **red_flag_gates.py** is at 529 lines -- be careful not to exceed 500. If the specificity gate function is more than ~20 lines, consider extracting to a helper or trimming existing code.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/test_false_sca_filter.py -x -v</automated>
  </verify>
  <done>
    - Boilerplate 10-K language passes through all 3 filter layers and is NOT classified as SCA
    - Real SCAs with named plaintiffs and case numbers are NOT filtered out
    - CRF-1 red flag only triggers on genuine SCA findings
    - Unverified SCAs get LOW confidence + "(unverified)" caveat
    - All existing scoring tests pass (no regression)
  </done>
</task>

<task type="auto">
  <name>Task 2: PDF header fix, company logo polish, and HTML/PDF parity</name>
  <files>
    src/do_uw/templates/html/styles.css
    src/do_uw/templates/html/sections/identity.html.j2
    src/do_uw/templates/html/base.html.j2
  </files>
  <action>
    **PDF header overlap fix (styles.css)**
    - Find the `@media print` section (should be near end of file)
    - Add padding-top to the first content element to clear the fixed header:
      ```css
      @media print {
        .worksheet-main > *:first-child {
          padding-top: 0.4in; /* Clear the fixed PDF running header */
        }
      }
      ```
    - Also check: if `.cover-header` has negative top margin in print mode, remove it or offset it
    - Verify that `styles.css` stays under 550 lines (it's at 535)
    - Test by checking the PDF output -- header should not overlap identity section

    **Company logo polish (identity.html.j2 + base.html.j2)**
    - Check current logo implementation in `identity.html.j2` (lines 11-12 per research) and `base.html.j2` (lines 69-70)
    - Ensure logo renders in PDF mode: the `company_logo_b64` context var should be available in PDF HTML too
    - Handle SVG favicons: some companies return SVG from Google Favicons API -- check content-type or file signature, wrap in `<img>` with appropriate MIME type
    - Add fallback: if logo fails to load, show company initial letter in a styled circle (CSS-only fallback)
    - Logo in PDF cover page: if `pdf_mode` is True, show logo larger (48x48px) in the cover header area
    - Logo should have `alt="{{ company_name }} logo"` for accessibility

    **HTML/PDF parity for Phase 73 templates (RENDER-09)**
    - Quarterly trend tabs: in PDF mode, ALL three tabs should be visible (no tab switching needed). Add `@media print` rule to show all `.fin-tab-panel` elements and hide radio inputs/labels for `trend-tabs` group.
    - Forensic dashboard: `<details>` elements already auto-expanded by Playwright evaluate(). Verify hazard cards render in 2-column grid in PDF (check `@media print` doesn't collapse grid).
    - Peer percentile bars: verify `.percentile-fill` renders with correct width in PDF. Playwright renders CSS width correctly. No special handling needed.
    - Add `page-break-inside: avoid` to `.forensic-band`, `.beneish-table`, and the quarterly trend summary strip to prevent awkward page splits.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/ -k "render or visual" --timeout=60 -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - PDF first page content does not overlap with running header
    - Company logo displays in both browser HTML and PDF
    - Quarterly trend tabs all visible in PDF (no hidden tab panels)
    - Forensic dashboard details expanded in PDF
    - No table/card splits across page breaks
    - styles.css under 550 lines
    - All render tests pass
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/ -k "render or score or sca or filter" -x -q` -- all tests pass
2. Run pipeline on a ticker with known boilerplate litigation -- verify CRF-1 does NOT trigger on boilerplate
3. Generate PDF and verify: no header overlap, logo visible, all Phase 73 templates render correctly
4. Compare HTML and PDF output for visual parity on new templates
</verification>

<success_criteria>
- False SCA bug eliminated: boilerplate litigation text never triggers CRF-1 red flag
- PDF header overlap fixed: first-page content has proper spacing below fixed header
- Company logo displays in HTML topbar, identity block, and PDF cover
- All Phase 73 templates (quarterly trends, forensic dashboard, peer percentiles) render correctly in PDF
- No regression in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/73-rendering-bugs/73-03-SUMMARY.md`
</output>
