---
phase: 73-rendering-bugs
plan: 02
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - src/do_uw/stages/render/context_builders/financials_peers.py
  - src/do_uw/stages/render/context_builders/financials.py
  - src/do_uw/stages/render/context_builders/market.py
  - src/do_uw/brain/sections/financial_health.yaml
  - src/do_uw/brain/sections/market_activity.yaml
  - src/do_uw/templates/html/sections/financial/peer_percentiles.html.j2
  - src/do_uw/templates/html/sections/market/insider_trading.html.j2
  - tests/test_financials_peers_context.py
autonomous: true
requirements:
  - RENDER-03
  - RENDER-04

must_haves:
  truths:
    - "Peer percentile display shows dual horizontal bars (navy=overall, gold=sector) for each benchmarked metric"
    - "Percentile bars are color-coded by risk quartile with direction awareness (high leverage=red, high margin=green)"
    - "Insider trading table shows ownership concentration column and cluster event highlighting"
    - "Both templates handle missing data gracefully"
  artifacts:
    - path: "src/do_uw/stages/render/context_builders/financials_peers.py"
      provides: "Peer percentile context builder"
      exports: ["build_peer_percentile_context"]
    - path: "src/do_uw/templates/html/sections/financial/peer_percentiles.html.j2"
      provides: "Peer percentile horizontal bar template"
    - path: "src/do_uw/templates/html/sections/market/insider_trading.html.j2"
      provides: "Enhanced insider trading table"
  key_links:
    - from: "financials.py"
      to: "financials_peers.py"
      via: "import build_peer_percentile_context"
      pattern: "from.*financials_peers.*import"
    - from: "financial_health.yaml"
      to: "peer_percentiles.html.j2"
      via: "facet template path"
      pattern: "template:.*peer_percentiles"
---

<objective>
Create peer percentile display template with dual horizontal bars and enhance the insider trading table with ownership concentration data from Phase 71.

Purpose: Show underwriters exactly where a company sits relative to all SEC filers and sector peers on key financial metrics, and surface insider ownership concentration patterns.

Output: New peer percentile context builder + template, enhanced insider trading template, updated facet YAML.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-rendering-bugs/73-CONTEXT.md
@.planning/phases/73-rendering-bugs/73-RESEARCH.md
@.planning/phases/73-rendering-bugs/73-01-SUMMARY.md

@src/do_uw/stages/render/context_builders/financials.py
@src/do_uw/stages/render/context_builders/market.py
@src/do_uw/brain/sections/financial_health.yaml
@src/do_uw/brain/sections/market_activity.yaml
@src/do_uw/templates/html/sections/market/insider_trading.html.j2
@src/do_uw/templates/html/styles/components.css

<interfaces>
<!-- From Phase 72: Frames API peer benchmarking -->
<!-- state.benchmarked.frames_percentiles: dict[str, MetricPercentile] -->
<!-- MetricPercentile: overall_percentile (float), sector_percentile (float|None), -->
<!--   filer_count (int), sector_filer_count (int|None), company_value (float) -->
<!-- metric_details on state.benchmarked contains merged percentile data -->

<!-- From Phase 71: insider trading enhancement -->
<!-- state.extracted.insider_transactions with ownership_concentration per insider -->
<!-- InsiderTransaction: owner, title, shares_post_transaction, ownership_pct -->

<!-- Existing CSS in components.css: -->
<!-- .percentile-bar { position: relative; height: 20px; background: #f0f0f0; } -->
<!-- .percentile-fill { position: absolute; top: 0; left: 0; height: 100%; } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Peer percentile context builder and template</name>
  <files>
    src/do_uw/stages/render/context_builders/financials_peers.py
    src/do_uw/stages/render/context_builders/financials.py
    src/do_uw/brain/sections/financial_health.yaml
    src/do_uw/templates/html/sections/financial/peer_percentiles.html.j2
    tests/test_financials_peers_context.py
  </files>
  <action>
    **financials_peers.py** (~120 lines):
    - `build_peer_percentile_context(state: AnalysisState) -> dict[str, Any]`
    - Returns `{"has_data": bool, "metrics": list[dict], "filer_count": int, "sector_name": str|None}`
    - Each metric dict: `{"label": str, "overall": float|None, "sector": float|None, "company_value": str, "risk_color": str, "favorable_direction": "high"|"low"}`
    - Direction-aware coloring: for each metric, define whether high is good or bad:
      - HIGH IS GOOD (green when high): operating_margin, net_margin, roe, current_ratio, interest_coverage, revenue_growth
      - HIGH IS BAD (red when high): debt_to_equity, debt_to_ebitda, total_liabilities
      - NEUTRAL (navy): revenue, net_income, total_assets (size metrics, no risk color)
    - Color logic: If metric is in bottom 25th percentile in risky direction -> `var(--do-risk-red)`. Top 25th in favorable direction -> `var(--do-risk-green)`. Middle -> `var(--do-navy)`.
    - Source data from `state.benchmarked.frames_percentiles` or `state.benchmarked.metric_details`
    - 15 metrics total (10 direct XBRL + 5 derived ratios per CONTEXT decision)
    - Format company_value: currency for dollar amounts, percentage for ratios, multiplier for coverage ratios

    **financials.py** -- add import + call (~5 lines):
    - Import `build_peer_percentile_context`
    - Call in `extract_financials()`, merge under `peer_percentiles` key

    **financial_health.yaml** -- add facet entry:
    - `id: peer_percentiles`, `name: "Peer Benchmarking (SEC Filers)"`, `render_as: chart`, `template: sections/financial/peer_percentiles.html.j2`
    - Place AFTER peer_matrix facet (or at end of facet list)

    **peer_percentiles.html.j2** (~80 lines):
    - Table with columns: Metric | Company Value | Percentile | Position Bar
    - Dual horizontal bar per metric row: navy bar for overall (all SEC filers), gold bar (50% opacity) for sector
    - Use existing `.percentile-bar` + `.percentile-fill` CSS from components.css
    - Legend row at top explaining navy=all filers, gold=sector peers
    - Metric label in left column, company's actual value in second column
    - Percentile text (e.g., "72nd") in third column
    - Bar in fourth column with min-width:200px
    - Apply `risk_color` as background on the overall bar fill
    - Guard with `{% if fin.peer_percentiles.has_data %}`
    - Add `@media print` rule: bars render as solid fills (no transparency issues)

    **Tests** (~60 lines):
    - Test returns has_data=False when benchmark data absent
    - Test direction-aware color mapping (margin high=green, leverage high=red)
    - Test percentile formatting
    - Test metric count matches expected 15
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/test_financials_peers_context.py -x -v</automated>
  </verify>
  <done>
    - Peer percentile template renders dual bars (navy overall, gold sector) for 15 metrics
    - Color coding is direction-aware (red for risky quartile, green for favorable)
    - Template shows gap_notice when peer data is missing
    - financials_peers.py under 150 lines
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhanced insider trading table with ownership concentration</name>
  <files>
    src/do_uw/stages/render/context_builders/market.py
    src/do_uw/brain/sections/market_activity.yaml
    src/do_uw/templates/html/sections/market/insider_trading.html.j2
  </files>
  <action>
    **market.py** -- enhance existing insider trading context (~20 lines added):
    - In the existing insider trading context building, add:
      - `ownership_pct` field per insider (from Phase 71 `shares_post_transaction` data)
      - `is_cluster` flag: True if 3+ insiders transact same direction within 5 trading days
      - `plan_10b5_1` indicator: True if transaction footnotes mention "10b5-1" or "Rule 10b5-1"
    - Check market.py stays under 500 lines (currently 344, plenty of room)

    **insider_trading.html.j2** -- enhance existing template:
    - Add "Ownership %" column after Amount column showing post-transaction ownership percentage
    - Add "10b5-1" badge (small pill) next to transaction type if plan_10b5_1 is True
    - Cluster event highlighting: if `is_cluster` is True, add a left-border accent (3px solid var(--do-risk-amber)) on the row
    - Add cluster event summary callout above the table if any clusters exist: "N cluster events detected (3+ insiders transacting within 5 days)"
    - All new columns gracefully handle None values with `format_na` filter

    **market_activity.yaml** -- no structural changes needed (insider_trading facet already exists). Verify template path is correct.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/ -k "render or market" --timeout=60 -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Insider trading table has ownership % column
    - 10b5-1 badge displays when applicable
    - Cluster events highlighted with amber left border
    - market.py under 500 lines
    - No regression in existing render tests
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/ -k "render or market or financials" -x -q` -- all tests pass
2. Peer percentile bars visible in HTML output (dual navy/gold)
3. Insider trading table shows new columns without layout breakage
</verification>

<success_criteria>
- Peer percentile display renders 15 metrics with dual horizontal bars and direction-aware coloring
- Insider trading table shows ownership concentration, 10b5-1 status, and cluster highlights
- All templates handle missing data gracefully
- No file exceeds 500-line limit
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/73-rendering-bugs/73-02-SUMMARY.md`
</output>
