---
phase: 100-display-integration-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/brain/output_manifest.yaml
  - src/do_uw/stages/render/context_builders/company.py
  - src/do_uw/templates/html/sections/company/business_model.html.j2
  - src/do_uw/templates/html/sections/company/corporate_events.html.j2
  - src/do_uw/templates/html/sections/company/structural_complexity.html.j2
  - src/do_uw/templates/html/sections/company.html.j2
autonomous: true
requirements: [RENDER-01, RENDER-02]

must_haves:
  truths:
    - "Business Profile section groups are ordered: Identity, Business Model, Operations, Events, Environment, Sector, Structure"
    - "Business model template renders all 6 BMOD dimensions with badge indicators and KV tables"
    - "Corporate events template renders M&A, IPO, restatements, capital changes, business changes with score badges"
    - "Structural complexity template renders disclosure score, non-GAAP, related parties, OBS, holding depth with level indicators"
    - "All 6 new dimension groups render data from context builders (not hardcoded)"
  artifacts:
    - path: "src/do_uw/brain/output_manifest.yaml"
      provides: "Reordered business_profile groups matching underwriting-standard layout"
      contains: "business_model"
    - path: "src/do_uw/stages/render/context_builders/company.py"
      provides: "Context builders for corporate_events and structural_complexity"
      exports: ["extract_company"]
    - path: "src/do_uw/templates/html/sections/company/business_model.html.j2"
      provides: "Full business model template with all 6 dimensions"
    - path: "src/do_uw/templates/html/sections/company/corporate_events.html.j2"
      provides: "Corporate events template with M&A/IPO/restatement/capital/business changes"
    - path: "src/do_uw/templates/html/sections/company/structural_complexity.html.j2"
      provides: "Structural complexity template with 5 opacity dimensions"
  key_links:
    - from: "src/do_uw/stages/render/context_builders/company.py"
      to: "templates/html/sections/company/*.html.j2"
      via: "context dict keys matching template variable names"
      pattern: "corporate_events_signals|structural_complexity_signals"
    - from: "src/do_uw/brain/output_manifest.yaml"
      to: "templates/html/sections/company/"
      via: "group template path references"
      pattern: "template:.*sections/company/"
---

<objective>
Restructure Business Profile section and upgrade placeholder templates to full rendering

Purpose: All v6.0 signal dimensions (Business Model, Corporate Events, Structural Complexity) render with professional-quality templates using data from context builders. The manifest group ordering reflects underwriting-standard section flow: Identity -> Business Model -> Operations -> Events -> Environment -> Sector -> Structure.

Output: Reordered manifest, 3 upgraded templates, 2 new context builders (corporate events, structural complexity)
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-business-model-extraction/93-02-SUMMARY.md
@.planning/phases/95-corporate-event-extraction/95-01-SUMMARY.md
@.planning/phases/96-structural-complexity-extraction/96-01-SUMMARY.md
@src/do_uw/brain/output_manifest.yaml
@src/do_uw/stages/render/context_builders/company.py
@src/do_uw/templates/html/sections/company/business_model.html.j2
@src/do_uw/templates/html/sections/company/corporate_events.html.j2
@src/do_uw/templates/html/sections/company/structural_complexity.html.j2
@src/do_uw/templates/html/sections/company.html.j2

<interfaces>
From src/do_uw/stages/render/context_builders/company.py:
- extract_company(state) returns dict with keys: business_model, subsidiary_structure, workforce_distribution, operational_resilience, environment_assessment, has_environment_data, sector_risk, has_sector_risk, operational_complexity_signals, has_ops_complexity
- extract_business_model(state) returns dict with keys: revenue_model_type, concentration_score, concentration_level, concentration_flags, key_person, lifecycle, disruption, segment_margins
- _build_environment_assessment(state) returns (dict, bool) tuple
- _build_sector_risk(state) returns (dict, bool) tuple
- _build_operational_complexity(state) returns (dict, bool) tuple

Corporate events data lives in: state.extracted.text_signals["corporate_events"] (if present), state.extracted.xbrl_forensics.ma_forensics, state.extracted.capital_markets, state.extracted.audit (restatements), state.company.business_changes

Structural complexity data lives in: state.extracted.text_signals (keys: disclosure_complexity, nongaap_measures, related_party_transactions, obs_guarantees, holding_layers)

From Phase 95 signal_mappers_events.py:
- BIZ.EVENT signals map from xbrl_forensics.ma_forensics, capital_markets, audit, business_changes

From Phase 96 signal_mappers.py:
- BIZ.STRUC signals map from text signal counts via _text_signal_count()

Template variables follow the pattern: `corporate_events_signals` and `structural_complexity_signals` (already used in placeholder templates)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder manifest groups + add context builders for events and structural complexity</name>
  <files>src/do_uw/brain/output_manifest.yaml, src/do_uw/stages/render/context_builders/company.py</files>
  <action>
1. Reorder the `business_profile` section groups in output_manifest.yaml to match the underwriting-standard layout:
   - business_description (Identity subsection)
   - business_model (Business Model)
   - revenue_segments (Business Model detail)
   - exposure_factors (Business Model detail)
   - customer_concentration (Business Model detail)
   - supplier_concentration (Business Model detail)
   - operational_complexity (Operations)
   - subsidiary_structure (Operations detail)
   - workforce_distribution (Operations detail)
   - operational_resilience (Operations detail)
   - corporate_events (Events)
   - risk_factors (Environment context)
   - external_environment (Environment)
   - sector_risk (Sector)
   - structural_complexity (Structure)
   - geographic_footprint (Structure detail)
   - company_checks (Summary)
   - company_density_alerts (Alerts)

2. Add `_build_corporate_events(state)` context builder in company.py that returns (dict, bool) tuple:
   - Read M&A data from state.extracted.xbrl_forensics.ma_forensics if available (acquisition_count, total_goodwill_pct, goodwill_impairment)
   - Read IPO/offering data from state.company (years_public) and state.extracted.capital_markets if available
   - Read restatement data from state.extracted.audit if available (has_restatement, material_weakness)
   - Read capital changes from state.extracted.capital_markets if available
   - Read business_changes from state.company.business_changes if available
   - Compute M&A risk score using Phase 95 additive formula: serial_acquirer(2) + goodwill>40%(1) + goodwill_growth>25%(1)
   - Map scores to level/color in context builder (not template): >=3 HIGH/red, >=1 MODERATE/amber, else LOW/green
   - Return dict with keys: ma_activity (dict with score, level, color, details), ipo_exposure (dict), restatements (dict), capital_changes (list), business_changes (list)
   - All analytical logic in context builder, template is pure consumer

3. Add `_build_structural_complexity(state)` context builder in company.py that returns (dict, bool) tuple:
   - Read text_signals from state.extracted.text_signals for keys: disclosure_complexity, nongaap_measures, related_party_transactions, obs_guarantees, holding_layers
   - For each text signal, extract mention_count and present boolean
   - Compute disclosure complexity score: risk_factor_count + critical_accounting_count + fls_density (from text signal counts)
   - Map each dimension to level: >=3 HIGH/red, >=1 MODERATE/amber, else LOW/green
   - Return dict with keys: disclosure_complexity (dict with score, level, color, details), nongaap (dict), related_parties (dict), obs_exposure (dict), holding_structure (dict)
   - All analytical logic in context builder, template is pure consumer

4. Wire both new builders into extract_company() return dict:
   - Add `corporate_events_signals` and `has_corporate_events` keys
   - Add `structural_complexity_signals` and `has_structural_complexity` keys
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run python -c "from do_uw.stages.render.context_builders.company import extract_company; print('OK')" && uv run pytest tests/stages/render/test_section_renderer.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Manifest groups reordered, both context builders return structured data, extract_company includes corporate_events_signals and structural_complexity_signals</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade placeholder templates to full professional rendering</name>
  <files>src/do_uw/templates/html/sections/company/business_model.html.j2, src/do_uw/templates/html/sections/company/corporate_events.html.j2, src/do_uw/templates/html/sections/company/structural_complexity.html.j2, src/do_uw/templates/html/sections/company.html.j2</files>
  <action>
1. Upgrade business_model.html.j2 from placeholder to full template:
   - Revenue Model Type with badge indicator
   - Concentration Risk with score badge (HIGH=red, MODERATE=amber, LOW=green) and flag list
   - Key Person Risk with founder-led indicator, CEO tenure, succession plan status, risk score badge
   - Segment Lifecycle table (name, stage, growth rate) if lifecycle data exists
   - Disruption Risk with level badge and threat list
   - Segment Margins table (name, margin, prior margin, change bps) if margin data exists
   - Use existing badge-pill CSS classes (bg-red-700, bg-amber-500, bg-emerald-600) consistent with environment/sector templates
   - Zero hardcoded thresholds -- all levels come from context builder dict values

2. Upgrade corporate_events.html.j2 from placeholder to full template:
   - M&A Activity section with risk score badge and details (acquisition count, goodwill %, impairment)
   - IPO/Offering Exposure with active window indicator
   - Restatement History with red indicator when present
   - Capital Structure Changes as bullet list
   - Business Changes as bullet list (filter out empty)
   - Guard every section with `{% if ... %}` for graceful degradation
   - Variable name: `corporate_events_signals` (matching context builder key)

3. Upgrade structural_complexity.html.j2 from placeholder to full template:
   - Disclosure Complexity with score badge and detail breakdown
   - Non-GAAP Usage with count and level indicator
   - Related Party Density with materiality level
   - Off-Balance-Sheet Exposure with score badge
   - Holding Structure Depth with layer count
   - Each row uses KV table layout with level badge in value cell
   - Variable name: `structural_complexity_signals` (matching context builder key)

4. Update company.html.j2 legacy fallback section to include new fragment templates in the correct order matching the manifest reordering. The facet-driven dispatch path already handles ordering from manifest, but the legacy fallback `{% include %}` list should also reflect the new order.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/brain/test_template_facet_audit.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All 3 templates upgraded from placeholders to full professional rendering with badge indicators, KV tables, and graceful degradation. Zero hardcoded thresholds in templates.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/brain/test_template_facet_audit.py -x` passes (all manifest groups have templates)
- `uv run pytest tests/stages/render/test_section_renderer.py -x` passes (section rendering intact)
- Manual: `uv run python -c "from do_uw.brain.manifest_schema import load_manifest; m = load_manifest(); bp = [s for s in m.sections if s.id == 'business_profile'][0]; print([g.id for g in bp.groups])"` shows correct group ordering
</verification>

<success_criteria>
- Manifest business_profile groups ordered: Identity -> Business Model -> Operations -> Events -> Environment -> Sector -> Structure
- 3 placeholder templates upgraded to full rendering with badge indicators and KV tables
- 2 new context builders (_build_corporate_events, _build_structural_complexity) provide template-ready data
- Zero hardcoded thresholds in any template (all levels/colors from context builder)
</success_criteria>

<output>
After completion, create `.planning/phases/100-display-integration-output/100-01-SUMMARY.md`
</output>
