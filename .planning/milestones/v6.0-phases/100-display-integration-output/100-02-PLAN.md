---
phase: 100-display-integration-output
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/templates/html/sections/executive/complexity_dashboard.html.j2
  - src/do_uw/brain/output_manifest.yaml
  - src/do_uw/stages/render/context_builders/company.py
  - src/do_uw/stages/render/md_renderer.py
  - src/do_uw/stages/render/sections/sect2_company.py
autonomous: true
requirements: [RENDER-03, RENDER-04]

must_haves:
  truths:
    - "Executive Summary contains a complexity dashboard card showing composite scores for business model, operations, and structural dimensions"
    - "Word/PDF output renders all new v6.0 subsections (business model, operations, events, environment, sector, structure)"
    - "Complexity dashboard reads scores from brain signal evaluation results, not a separate computation"
  artifacts:
    - path: "src/do_uw/templates/html/sections/executive/complexity_dashboard.html.j2"
      provides: "Complexity dashboard card in Executive Summary"
      min_lines: 20
    - path: "src/do_uw/stages/render/sections/sect2_company.py"
      provides: "Word renderer with new v6.0 subsections"
      contains: "business_model"
  key_links:
    - from: "src/do_uw/stages/render/context_builders/company.py"
      to: "templates/html/sections/executive/complexity_dashboard.html.j2"
      via: "complexity_dashboard key in template context"
      pattern: "complexity_dashboard"
    - from: "src/do_uw/stages/render/sections/sect2_company.py"
      to: "context_builders/company.py"
      via: "context dict consumption"
      pattern: "context.*business_model|context.*corporate_events"
---

<objective>
Add complexity dashboard to Executive Summary and update Word/PDF output for all v6.0 dimensions

Purpose: Underwriters see a single complexity overview card in the Executive Summary showing how complex this risk is across business model, operations, and structure. Word/PDF outputs achieve parity with HTML by rendering all new subsections.

Output: New Executive Summary dashboard template + group, updated Word section 2 renderer
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/do_uw/brain/output_manifest.yaml
@src/do_uw/stages/render/context_builders/company.py
@src/do_uw/stages/render/md_renderer.py
@src/do_uw/stages/render/html_renderer.py
@src/do_uw/stages/render/sections/sect2_company.py

<interfaces>
From context_builders/company.py extract_company() return dict:
- business_model: dict with revenue_model_type, concentration_score/level/flags, key_person, lifecycle, disruption, segment_margins
- operational_complexity_signals: dict with composite_score, composite_level, composite_color, jurisdiction_count, indicators, etc.
- has_ops_complexity: bool
- corporate_events_signals: dict with ma_activity, ipo_exposure, restatements, capital_changes, business_changes (added in Plan 01)
- has_corporate_events: bool (added in Plan 01)
- structural_complexity_signals: dict with disclosure_complexity, nongaap, related_parties, obs_exposure, holding_structure (added in Plan 01)
- has_structural_complexity: bool (added in Plan 01)
- environment_assessment: dict with regulatory_intensity, geopolitical_risk, esg_gap, cyber_risk, macro_sensitivity
- has_environment_data: bool
- sector_risk: dict with hazard, claims, regulatory, peer
- has_sector_risk: bool

From sect2_company.py:
- render_section_2(doc, context, ds) delegates to helper functions
- context["company"] holds the extract_company() output
- context["_state"] holds raw AnalysisState (legacy access)
- docx_helpers: add_risk_indicator, add_sourced_paragraph, add_styled_table

From output_manifest.yaml executive_summary section:
- Groups: data_quality_notice, company_profile, risk_classification, key_findings, claim_probability, tower_recommendation, executive_density_alerts
- New complexity_dashboard group needs to be added after risk_classification
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create complexity dashboard in Executive Summary</name>
  <files>src/do_uw/templates/html/sections/executive/complexity_dashboard.html.j2, src/do_uw/brain/output_manifest.yaml, src/do_uw/stages/render/context_builders/company.py, src/do_uw/stages/render/md_renderer.py</files>
  <action>
1. Add `_build_complexity_dashboard(state)` function in company.py (or a new dedicated builder -- keep company.py under 500 lines, split if needed):
   - Read operational complexity composite score from the extract_company/operational_complexity path
   - Read structural complexity composite from text_signals (sum of disclosure + nongaap + related_party + obs + holding counts)
   - Read business model concentration_score + key_person risk_score + disruption threat_count to form a business model complexity composite
   - Map each dimension to level/color: >=3 HIGH/red, >=2 MODERATE/amber, else LOW/green (thresholds appropriate per dimension)
   - Return dict with keys: dimensions (list of {name, score, max_score, level, color}), overall_level, overall_color
   - All score-to-level mapping in builder, template is pure consumer

2. Wire `complexity_dashboard` into build_template_context in md_renderer.py:
   - Import the builder function
   - Add `context["complexity_dashboard"] = _build_complexity_dashboard(state)` after company extraction
   - Guard with `if state.company is not None`

3. Create `complexity_dashboard.html.j2` template:
   - Horizontal card layout with 3 dimension gauges (Business Model, Operations, Structure)
   - Each gauge shows: dimension name, score/max, level badge with color
   - Overall complexity indicator at top
   - Use existing metric_card CSS patterns from executive section
   - Zero hardcoded thresholds: all levels and colors from context dict

4. Add complexity_dashboard group to executive_summary section in output_manifest.yaml:
   - Position after risk_classification group (before key_findings)
   - id: complexity_dashboard
   - name: "Complexity Dashboard"
   - template: sections/executive/complexity_dashboard.html.j2
   - render_as: metric_card
   - requires: [company]
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run python -c "from do_uw.brain.manifest_schema import load_manifest; m = load_manifest(); es = [s for s in m.sections if s.id == 'executive_summary'][0]; print([g.id for g in es.groups])" && uv run pytest tests/brain/test_template_facet_audit.py -x -q 2>&1 | tail -3</automated>
  </verify>
  <done>Complexity dashboard card renders in Executive Summary with 3 dimension scores (business model, operations, structure) from brain signal evaluation data</done>
</task>

<task type="auto">
  <name>Task 2: Update Word renderer for all v6.0 subsections</name>
  <files>src/do_uw/stages/render/sections/sect2_company.py</files>
  <action>
1. Add new helper functions to sect2_company.py for rendering v6.0 subsections in Word format:

   `_render_business_model(doc, context, ds)`:
   - Read context["company"]["business_model"] dict
   - Render KV table: Revenue Model, Concentration Risk (level + flags), Key Person Risk (level + details), Disruption Risk
   - Add segment lifecycle table if data exists
   - Add segment margins table if data exists

   `_render_operational_complexity(doc, context, ds)`:
   - Read context["company"]["operational_complexity_signals"] dict
   - Render composite score with level
   - Render KV table of component dimensions
   - Render structural indicators as bullet list

   `_render_corporate_events(doc, context, ds)`:
   - Read context["company"]["corporate_events_signals"] dict
   - Render M&A activity with risk score
   - Render restatement/IPO/capital change indicators
   - Render business changes list

   `_render_environment_sector(doc, context, ds)`:
   - Read context["company"]["environment_assessment"] dict
   - Render 5 environment signals as KV table rows with level
   - Read context["company"]["sector_risk"] dict
   - Render hazard tier, claim patterns, regulatory overlay, peer comparison

   `_render_structural_complexity(doc, context, ds)`:
   - Read context["company"]["structural_complexity_signals"] dict
   - Render 5 complexity dimensions as KV table rows with level

2. Wire new helpers into render_section_2():
   - Call after _render_subsidiary_summary and before peer comparison
   - Guard each with `if context.get("company", {}).get("key") is not None` checks
   - Order: business_model -> operational_complexity -> corporate_events -> environment -> sector -> structural_complexity

3. Keep sect2_company.py under 500 lines. If approaching limit, split the new v6.0 helpers into `sect2_company_v6.py` and import from there.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run python -c "from do_uw.stages.render.sections.sect2_company import render_section_2; print('Word renderer imports OK')" && uv run pytest tests/stages/render/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Word renderer includes all v6.0 subsections matching HTML output. Section 2 renders business model, operations, events, environment, sector, and structure data when available.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/brain/test_template_facet_audit.py -x` passes (new complexity_dashboard template resolves)
- `uv run pytest tests/stages/render/ -x` passes (all render tests)
- `uv run python -c "from do_uw.stages.render.sections.sect2_company import render_section_2; print('OK')"` imports cleanly
</verification>

<success_criteria>
- Executive Summary contains complexity_dashboard group rendering 3 dimension scores
- Word Section 2 renders all 6 new v6.0 dimension subsections
- All scores/levels in dashboard and Word output come from context builders (no hardcoded thresholds)
- PDF output gets parity automatically (uses build_template_context -> HTML -> PDF path)
</success_criteria>

<output>
After completion, create `.planning/phases/100-display-integration-output/100-02-SUMMARY.md`
</output>
