---
phase: 99-operational-scoring-signals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/brain/signals/biz/operations.yaml
  - src/do_uw/stages/analyze/signal_mappers.py
  - src/do_uw/stages/analyze/signal_field_routing.py
  - src/do_uw/brain/field_registry.yaml
  - src/do_uw/brain/field_registry_functions.py
  - src/do_uw/stages/render/context_builders/company.py
  - src/do_uw/templates/html/sections/company/operational_complexity.html.j2
  - src/do_uw/brain/output_manifest.yaml
  - tests/test_ops_signals.py
  - tests/brain/test_signal_group_resolution.py
  - tests/stages/render/test_section_renderer.py
autonomous: true
requirements: [OPS-01, OPS-05]

must_haves:
  truths:
    - "BIZ.OPS.* signals evaluate through the V2 signal engine (not SKIPPED)"
    - "Operational complexity composite score aggregates subsidiary, workforce, segment, and VIE/dual-class indicators"
    - "Composite signal fires at defined thresholds (>20 jurisdictions + >5 segments = HIGH)"
    - "operational_complexity template renders composite score with indicator breakdown"
  artifacts:
    - path: "src/do_uw/brain/signals/biz/operations.yaml"
      provides: "BIZ.OPS.complexity_score composite signal definition"
      contains: "BIZ.OPS.complexity_score"
    - path: "src/do_uw/stages/analyze/signal_mappers.py"
      provides: "BIZ.OPS.* signal mapper routing"
      contains: "_map_ops_fields"
    - path: "src/do_uw/stages/analyze/signal_field_routing.py"
      provides: "BIZ.OPS field routing entries"
      contains: "BIZ.OPS"
    - path: "src/do_uw/stages/render/context_builders/company.py"
      provides: "_build_operational_complexity context builder"
      contains: "_build_operational_complexity"
    - path: "src/do_uw/templates/html/sections/company/operational_complexity.html.j2"
      provides: "Operational complexity dashboard template"
      min_lines: 20
  key_links:
    - from: "src/do_uw/stages/analyze/signal_mappers.py"
      to: "src/do_uw/stages/extract/operational_extraction.py"
      via: "proxy state pattern for BIZ.OPS"
      pattern: "_map_ops_fields"
    - from: "src/do_uw/stages/render/context_builders/company.py"
      to: "src/do_uw/templates/html/sections/company/operational_complexity.html.j2"
      via: "context dict -> Jinja2 template"
      pattern: "_build_operational_complexity"
---

<objective>
Wire BIZ.OPS.* signals into the evaluation pipeline and create a composite operational complexity score that aggregates subsidiary structure, workforce, segment data, and existing special-structure indicators (VIE, dual-class).

Purpose: Complete OPS-01 (wire existing complexity indicators) and OPS-05 (unified composite score) so underwriters see a single operational complexity assessment backed by all operational signals.

Output: Working signal evaluation pipeline for BIZ.OPS.*, composite BIZ.OPS.complexity_score signal, context builder, and rendering template.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-operational-data-extraction/94-01-SUMMARY.md
@.planning/phases/93-business-model-extraction/93-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/do_uw/brain/signals/biz/operations.yaml:
3 existing signals: BIZ.OPS.subsidiary_structure (field_path: subsidiary_structure),
BIZ.OPS.workforce (field_path: workforce_distribution),
BIZ.OPS.resilience (field_path: operational_resilience).
All have group: operational_complexity, schema_version: 3.

From src/do_uw/stages/extract/operational_extraction.py:
```python
def extract_subsidiary_structure(state) -> tuple[SourcedValue[dict] | None, ExtractionReport]
  # Returns: {total_subsidiaries, jurisdiction_count, jurisdictions, high_reg_count, low_reg_count, tax_haven_count}
def extract_workforce_distribution(state) -> tuple[SourcedValue[dict] | None, ExtractionReport]
  # Returns: {total_employees, domestic_count, international_count, domestic_pct, international_pct, unionized_count, unionized_pct}
def extract_operational_resilience(state) -> tuple[SourcedValue[dict] | None, ExtractionReport]
  # Returns: {geographic_concentration_score, primary_geography, single_facility_risk, supply_chain_depth, supply_chain_description, overall_assessment}
```

From src/do_uw/stages/analyze/signal_mappers.py:
- BIZ.OPS currently falls through to `_map_company_fields` (prefix == "BIZ") which has NO BIZ.OPS field mapping
- BIZ.OPS is NOT in signal_field_routing.py at all
- ENVR/SECT pattern: separate `_map_*_fields` function with _StateProxy, lazy import, narrow_result
- BIZ.EVENT already has dedicated routing: `if prefix2 == "BIZ.EVENT": ...`

Routing dispatch in map_signal_data():
```python
prefix2 = ".".join(signal_id.split(".")[:2])
if prefix2 == "BIZ.EVENT": return map_event_fields(...)
if prefix == "BIZ": return _map_company_fields(...)
if prefix == "ENVR": return _map_environment_fields(...)
if prefix == "SECT": return _map_sector_fields(...)
```

From src/do_uw/stages/analyze/signal_field_routing.py:
Existing BIZ.STRUC entries at lines 121-126 follow pattern:
```python
"BIZ.STRUC.disclosure_complexity": "disclosure_complexity_score",
```

From src/do_uw/brain/output_manifest.yaml:
operational_complexity group exists at position after supplier_concentration.
Template: sections/company/operational_complexity.html.j2 (currently stub).

Existing indicator signals (OPS-01 targets):
- BIZ.STRUCT.vie_spe (field_path: vie_spe_present, group: business_description)
- GOV.RIGHTS.dual_class (field_path: dual_class, group: structural_governance)
- BIZ.STRUC.obs_exposure (field_path: obs_exposure_score, group: structural_complexity)
- BIZ.STRUC.holding_structure (field_path: holding_structure_depth, group: structural_complexity)

Current test assertions:
- test_company_fragment_count: 19 templates
- test_signal_group_count_is_55: 55 unique group IDs

Context builder pattern (from Phase 98 _build_sector_risk):
- Reads text_signals from state.extracted for evaluated signal values
- Maps scores to levels/colors via dict lookup
- Returns template-ready dict consumed by Jinja2 template
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add composite signal YAML + signal mapper routing + field routing</name>
  <files>
    src/do_uw/brain/signals/biz/operations.yaml
    src/do_uw/stages/analyze/signal_mappers.py
    src/do_uw/stages/analyze/signal_field_routing.py
    src/do_uw/brain/field_registry.yaml
    src/do_uw/brain/field_registry_functions.py
  </files>
  <action>
1. **Add BIZ.OPS.complexity_score signal to operations.yaml** (append after BIZ.OPS.resilience):
   - id: BIZ.OPS.complexity_score
   - name: Operational Complexity Score
   - work_type: evaluate, layer: signal, schema_version: 3
   - factors: [F9, F10]
   - group: operational_complexity, signal_class: evaluative
   - field_path: ops_complexity_score
   - evaluation formula: weighted composite of jurisdiction_count, high_reg_count, segment_count, workforce_international_pct, vie_present, dual_class_present
   - thresholds: >15 RED, >8 YELLOW (composite score on 0-20 scale)
   - presentation detail_levels: glance ("Complexity: {level}"), standard ("{value}/20 — {detail}"), deep (all component fields)
   - context_templates: TRIGGERED: "Elevated operational complexity: score {value}" / CLEAR: "Operational complexity within normal range"
   - depends_on: [BIZ.OPS.subsidiary_structure, BIZ.OPS.workforce, BIZ.OPS.resilience]
   - acquisition sources: company.subsidiary_structure, company.workforce_distribution, company.operational_resilience, text_signals.vie_spe, governance.dual_class

2. **Add BIZ.OPS.* routing in signal_mappers.py**:
   - Add `if prefix2 == "BIZ.OPS":` BEFORE the generic `if prefix == "BIZ":` in map_signal_data()
   - Create `_map_ops_fields()` function following the ENVR proxy pattern:
     - Lazy import from operational_extraction
     - Build _StateProxy with extracted, company, acquired_data
     - Call all 3 extraction functions to build result dict
     - Add composite score computation: jurisdiction_count weighted (1pt per 5 jurisdictions, max 5) + high_reg_count (1pt per 2, max 3) + segment_count (1pt per 2 segments, max 3) + international_pct (1pt per 20%, max 3) + vie_present (2pts) + dual_class_present (2pts) + unionization>20% (2pts) = max ~20
     - For VIE: read from text_signals via `_text_signal_count(extracted, "vie_spe")`
     - For dual_class: read from extracted.governance if available
     - For segment_count: use len(company.revenue_segments) if available
     - narrow_result on the combined dict
   - Pass company and analysis args through

3. **Add signal_field_routing entries** for all 4 BIZ.OPS signals:
   ```python
   "BIZ.OPS.subsidiary_structure": "jurisdiction_count",
   "BIZ.OPS.workforce": "international_pct",
   "BIZ.OPS.resilience": "geographic_concentration_score",
   "BIZ.OPS.complexity_score": "ops_complexity_score",
   ```

4. **Add field_registry.yaml entry** for ops_complexity_score:
   ```yaml
   ops_complexity_score:
     type: COMPUTED
     function: compute_ops_complexity_score
     args:
       - company.subsidiary_structure
       - company.workforce_distribution
       - company.operational_resilience
     description: Composite operational complexity score (0-20)
   ```

5. **Add compute_ops_complexity_score to field_registry_functions.py**:
   - Extract jurisdiction_count, high_reg_count from subsidiary_structure SourcedValue
   - Extract international_pct, unionized_pct from workforce_distribution SourcedValue
   - Read segment count from revenue_segments
   - Compute weighted composite per formula above
   - Return integer score 0-20
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run python -c "from do_uw.brain.brain_schema import BrainSignalEntry; import yaml; sigs = yaml.safe_load(open('src/do_uw/brain/signals/biz/operations.yaml')); [BrainSignalEntry.model_validate(s) for s in sigs]; print(f'{len(sigs)} signals validated')"</automated>
  </verify>
  <done>4 BIZ.OPS signals in operations.yaml (3 existing + 1 composite), all validate. Signal mapper routes BIZ.OPS.* to _map_ops_fields. Field routing entries exist for all 4. Field registry has COMPUTED entry for ops_complexity_score.</done>
</task>

<task type="auto">
  <name>Task 2: Context builder + HTML template + manifest + tests</name>
  <files>
    src/do_uw/stages/render/context_builders/company.py
    src/do_uw/templates/html/sections/company/operational_complexity.html.j2
    src/do_uw/brain/output_manifest.yaml
    tests/test_ops_signals.py
    tests/brain/test_signal_group_resolution.py
    tests/stages/render/test_section_renderer.py
  </files>
  <action>
1. **Create `_build_operational_complexity()` in company.py** (following _build_sector_risk pattern):
   - Read text_signals from state.extracted for BIZ.OPS signal results
   - Extract key values: jurisdiction_count, high_reg_count, total_employees, international_pct, unionized_pct, geographic_concentration_score, supply_chain_depth, overall_assessment
   - Read composite ops_complexity_score from text_signals (or compute inline as fallback)
   - Map composite score to level via dict: >=15 "HIGH", >=8 "MODERATE", else "LOW"
   - Map level to color: HIGH -> "red", MODERATE -> "amber", LOW -> "green"
   - Build indicators list for OPS-01 (VIE, dual-class, holding structure) by reading relevant text_signals:
     - vie_present: bool from text_signals.vie_spe > 0
     - dual_class: bool from governance data or text_signals
     - holding_depth: from text_signals.holding_structure_depth
     - obs_exposure: from text_signals.obs_exposure_score
   - Return dict with: composite_score, composite_level, composite_color, subsidiary_data, workforce_data, resilience_data, indicators list
   - All score-to-label mapping in builder (NOT in template)

2. **Wire _build_operational_complexity into extract_company()**:
   - Call it and add result under key "operational_complexity_signals" in the company context dict
   - This feeds into the operational_complexity.html.j2 template

3. **Replace stub operational_complexity.html.j2** with real template:
   - Composite score badge at top (colored by level)
   - KV table showing component scores:
     - Jurisdictions: {jurisdiction_count} ({high_reg_count} high-regulatory)
     - Workforce: {total_employees} ({international_pct}% international)
     - Geographic Concentration: {geographic_concentration_score}/100
     - Supply Chain: {supply_chain_depth}
   - Indicators section (OPS-01): VIE/SPE, Dual-Class, Holding Depth, OBS Exposure — show as badge (present/absent) with level coloring
   - Template uses ONLY variables passed from context builder, ZERO hardcoded thresholds or analytical logic
   - Use existing CSS classes from other company templates (kv-table, badge patterns)

4. **Verify operational_complexity group in output_manifest.yaml** already exists (it does, at position after supplier_concentration). No manifest change needed unless template path changed.

5. **Update test assertions**:
   - test_signal_group_count_is_55: likely stays 55 since BIZ.OPS.complexity_score uses existing "operational_complexity" group
   - test_company_fragment_count: stays 19 since template already exists as stub (no new file created)

6. **Create tests/test_ops_signals.py** with ~12 tests:
   - Test BIZ.OPS.complexity_score YAML validates with BrainSignalEntry
   - Test _map_ops_fields returns expected keys (jurisdiction_count, international_pct, ops_complexity_score, etc.)
   - Test composite score computation: 0 when no data, correct when data present
   - Test composite score thresholds: score >15 = RED, >8 = YELLOW
   - Test _build_operational_complexity returns all expected context keys
   - Test context builder score-to-level mapping (>=15 HIGH, >=8 MODERATE, else LOW)
   - Test indicators include VIE and dual-class flags
   - Test template renders without error with sample context data
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/test_ops_signals.py tests/brain/test_signal_group_resolution.py tests/stages/render/test_section_renderer.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>Context builder produces template-ready dict with composite score, level, color, and indicators. HTML template renders composite dashboard with component breakdown and OPS-01 indicator badges. All tests pass including new signal tests, group count, and fragment count.</done>
</task>

</tasks>

<verification>
1. All 4 BIZ.OPS signals validate in YAML schema
2. Signal mapper routes BIZ.OPS.* prefix to dedicated function
3. Composite score computation produces sensible values (0 for empty company, higher for complex companies)
4. Context builder maps all scores to levels/colors (no logic in template)
5. HTML template renders operational complexity section with composite badge and indicators
6. `uv run pytest tests/test_ops_signals.py -x` passes all tests
7. `uv run pytest tests/brain/ tests/stages/render/ -x -q` passes (no regressions)
</verification>

<success_criteria>
- BIZ.OPS.complexity_score signal defined in operations.yaml with full v3 schema
- BIZ.OPS.* signals route through _map_ops_fields in signal_mappers.py (not falling through to _map_company_fields)
- Composite score aggregates jurisdiction, workforce, segment, VIE, and dual-class indicators on 0-20 scale
- Composite fires RED at >15, YELLOW at >8
- Operational complexity template displays composite score badge + component breakdown + OPS-01 indicators
- All existing tests pass without regression
</success_criteria>

<output>
After completion, create `.planning/phases/99-operational-scoring-signals/99-01-SUMMARY.md`
</output>
