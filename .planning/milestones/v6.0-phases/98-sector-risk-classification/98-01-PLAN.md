---
phase: 98-sector-risk-classification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/brain/config/sector_hazard_tiers.yaml
  - src/do_uw/brain/config/sector_claim_patterns.yaml
  - src/do_uw/brain/config/sector_regulatory_overlay.yaml
  - src/do_uw/brain/config/sector_peer_benchmarks.yaml
  - src/do_uw/brain/signals/biz/sector.yaml
  - src/do_uw/stages/extract/sector_classification.py
  - tests/test_sector_signals.py
autonomous: true
requirements:
  - SECT-01
  - SECT-02
  - SECT-03
  - SECT-04

must_haves:
  truths:
    - "Company's GICS sub-industry maps to a D&O hazard tier (Highest/High/Moderate/Lower)"
    - "Company's sector has top 3 claim theories with provenance"
    - "Company's sector has named regulators and regulatory intensity baseline"
    - "Company's D&O risk dimensions can be compared against sector median benchmarks"
  artifacts:
    - path: "src/do_uw/brain/config/sector_hazard_tiers.yaml"
      provides: "GICS sub-industry to hazard tier mapping with filing rate data"
      contains: "Highest"
    - path: "src/do_uw/brain/config/sector_claim_patterns.yaml"
      provides: "Top 3 claim theories per GICS industry group"
      contains: "claim_theories"
    - path: "src/do_uw/brain/config/sector_regulatory_overlay.yaml"
      provides: "Named regulators per GICS industry group"
      contains: "regulators"
    - path: "src/do_uw/brain/config/sector_peer_benchmarks.yaml"
      provides: "Sector median D&O risk dimension benchmarks"
      contains: "median"
    - path: "src/do_uw/brain/signals/biz/sector.yaml"
      provides: "4 SECT signal definitions with v3 schema"
      contains: "SECT.hazard_tier"
    - path: "src/do_uw/stages/extract/sector_classification.py"
      provides: "Extraction module computing 4 sector signal values"
      exports: ["extract_sector_signals"]
    - path: "tests/test_sector_signals.py"
      provides: "Unit tests for sector extraction and reference data lookup"
      min_lines: 80
  key_links:
    - from: "src/do_uw/stages/extract/sector_classification.py"
      to: "src/do_uw/brain/config/sector_hazard_tiers.yaml"
      via: "YAML lookup by GICS code"
      pattern: "yaml\\.safe_load.*hazard_tier"
    - from: "src/do_uw/stages/extract/sector_classification.py"
      to: "state.company.gics_code"
      via: "GICS sub-industry from company identity"
      pattern: "gics_code|sic_code"
---

<objective>
Create 4 SECT brain signal YAML definitions, 4 static reference data tables with full provenance, and an extraction module that computes sector classification values from GICS/SIC codes.

Purpose: Underwriters need to see where a company sits in the D&O risk landscape for its sector -- hazard tier, typical claim patterns, regulatory baseline, and peer comparison -- all from authoritative reference data with provenance.

Output: 4 reference data YAML files in brain/config/, 4 SECT signal YAML definitions in brain/signals/biz/sector.yaml, extraction module sector_classification.py, and unit tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-sector-risk-classification/98-CONTEXT.md
@.planning/phases/97-external-environment-assessment/97-01-SUMMARY.md

<interfaces>
<!-- Existing code patterns the executor needs -->

From src/do_uw/brain/signals/env/environment.yaml (signal v3 schema pattern):
- Each signal has: id, name, work_type, layer, factors, threshold, data_strategy, provenance, display, group, signal_class, field_path, depends_on, schema_version: 3, acquisition, evaluation, presentation blocks

From src/do_uw/stages/extract/environment_assessment.py (extraction pattern):
- Module exports a single function: extract_environment_signals(state) -> dict
- Returns flat dict with field_key -> value pairs (e.g., regulatory_intensity_score: 3, regulatory_details: {...})
- Each signal has a dedicated _compute_* function

From src/do_uw/brain/config/sic_gics_mapping.json (reference data pattern):
- JSON with $schema, description, version, then data under a key
- GICS codes are 8-digit strings (e.g., "10102010" = Oil & Gas Exploration & Production)

From state.company (available fields):
- state.company.identity.sic_code: str (4-digit SIC)
- state.company.gics_code: str | None (8-digit GICS, may be None)
- sic_to_sector() in stages/resolve/sec_identity.py maps SIC to ~15 sector codes

From src/do_uw/brain/config/industry_theories.json (existing claim theories):
- SIC-range keyed dict with industry name and array of theories (theory, description, legal_basis)
- 288 lines, covers ~10 SIC ranges -- SECT-02 should consolidate and expand this at GICS level
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 reference data tables and 4 SECT brain signal YAML definitions</name>
  <files>
    src/do_uw/brain/config/sector_hazard_tiers.yaml
    src/do_uw/brain/config/sector_claim_patterns.yaml
    src/do_uw/brain/config/sector_regulatory_overlay.yaml
    src/do_uw/brain/config/sector_peer_benchmarks.yaml
    src/do_uw/brain/signals/biz/sector.yaml
  </files>
  <action>
  Create 4 reference data YAML files in brain/config/ with full provenance. Use YAML (not JSON) to match the user decision for config files alongside existing JSON. Each file must have metadata block with: source publications (SCAC, Cornerstone Research, NERA Economic Consulting annual D&O studies), data year, methodology notes.

  **sector_hazard_tiers.yaml**: Map GICS 8-digit sub-industry codes to D&O hazard tiers. Structure:
  ```yaml
  metadata:
    sources: [Cornerstone Research SCA Filings 2023, NERA D&O Trends 2023, Stanford SCAC]
    data_year: 2023
    methodology: Filing rates per 1000 companies by GICS sub-industry, averaged over 3 years
    tiers:
      Highest: ">15 filings per 1000"
      High: "8-15 filings per 1000"
      Moderate: "3-8 filings per 1000"
      Lower: "<3 filings per 1000"
  tiers:
    "45301020":  # Semiconductor Equipment
      tier: High
      filing_rate: 12.3
      context: "Technology sector has 2.3x average SCA filing rate"
    # ... more entries
  fallback_by_gics_sector:
    "10": { tier: Moderate, context: "Energy sector baseline" }
    "15": { tier: High, context: "Materials sector baseline" }
    # ... 11 GICS sectors as fallback when sub-industry not found
  ```
  Include at minimum: all 11 GICS sector-level fallbacks + 20-30 high-risk sub-industries with specific filing rates. Seed from known D&O litigation hotspots: biotech/pharma (Highest), fintech/SPAC (Highest), social media/ad-tech (High), semiconductors (High), commercial banks (High), healthcare services (High), specialty retail (Moderate), utilities (Lower), real estate (Lower).

  **sector_claim_patterns.yaml**: Top 3 claim theories per GICS industry group (4-digit). Structure:
  ```yaml
  metadata:
    sources: [Stanford SCAC Annual Reports, Cornerstone Research Settlement Data]
    data_year: 2023
  patterns:
    "4530":  # Semiconductors & Equipment
      industry_group: "Semiconductors & Semiconductor Equipment"
      claim_theories:
        - theory: "Revenue guidance miss"
          frequency: "Most common"
          legal_basis: "10b-5"
          description: "Failure to disclose declining demand signals before earnings miss"
        - theory: "..."
        - theory: "..."
  ```
  Cover at minimum 15-20 industry groups including all high-filing-rate sectors. Reference existing industry_theories.json for SIC-based theories but convert to GICS-level and expand. Include provenance (which study, which year).

  **sector_regulatory_overlay.yaml**: Named regulators and regulatory intensity per GICS industry group. Structure:
  ```yaml
  metadata:
    sources: [Federal Register, sector-specific regulatory guides]
    data_year: 2024
    note: "Sector-level baseline. Company-specific intensity in ENVR.regulatory_intensity"
  overlays:
    "3520":  # Pharmaceuticals, Biotechnology & Life Sciences
      industry_group: "Pharmaceuticals, Biotechnology & Life Sciences"
      intensity: High
      regulators: ["FDA", "CMS", "OIG", "DEA", "FTC"]
      trend: "Increased FTC scrutiny of pharma M&A and anti-competitive practices since 2023"
  ```
  Cover all 11 GICS sectors with at least sector-level regulators. Add specific entries for 10-15 high-regulation industry groups with named regulators. SECT-03 is the sector BASELINE -- distinct from ENVR-01 which is company-specific.

  **sector_peer_benchmarks.yaml**: Sector median D&O risk dimensions by GICS sector (2-digit). Structure:
  ```yaml
  metadata:
    sources: [Cornerstone Research Settlement Survey, NERA D&O Trends, internal pipeline calibration]
    data_year: 2023
    dimensions_explained:
      overall_score: "Median D&O risk score (0-100 scale)"
      litigation_frequency: "SCA filings per 1000 companies per year"
      governance_quality: "Median governance quality score (0-100)"
      financial_health: "Median Altman Z-Score equivalent"
    outlier_threshold: "1 standard deviation from sector median"
  benchmarks:
    "45":  # Information Technology
      sector_name: "Information Technology"
      overall_score: { median: 62, std_dev: 15 }
      litigation_frequency: { median: 8.2, std_dev: 4.1 }
      governance_quality: { median: 68, std_dev: 12 }
      financial_health: { median: 4.2, std_dev: 2.1 }
  ```
  Cover all 11 GICS sectors. Benchmarks are approximate calibration values seeded from D&O study aggregates -- label provenance clearly as "calibrated from aggregate study data, not company-level analysis."

  **brain/signals/biz/sector.yaml**: Define 4 SECT signals following the v3 schema pattern from environment.yaml:
  - SECT.hazard_tier: field_path=sector_hazard_tier, group=sector_risk, evaluation thresholds: tier == "Highest" -> RED, tier == "High" -> YELLOW
  - SECT.claim_patterns: field_path=sector_claim_patterns, group=sector_risk, display_when=has_data, no evaluation thresholds (reference/display signal)
  - SECT.regulatory_overlay: field_path=sector_regulatory_overlay, group=sector_risk, evaluation: intensity == "High" -> YELLOW (sector baseline, not RED)
  - SECT.peer_comparison: field_path=sector_peer_comparison, group=sector_risk, evaluation: outlier_count >= 2 -> RED, >= 1 -> YELLOW

  Each signal needs: id, name, work_type (evaluate for hazard_tier/peer_comparison, display for claim_patterns/regulatory_overlay), layer, factors [F1], threshold (tiered), data_strategy, provenance (origin: v6_sect, data_source: REFERENCE_DATA), display, group: sector_risk, signal_class (evaluative or reference), field_path, schema_version: 3, acquisition (type: REFERENCE_DATA), evaluation, presentation (detail_levels with glance/standard/deep, context_templates).
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && python -c "
import yaml, pathlib
# Verify all 4 reference data files parse correctly
for f in ['sector_hazard_tiers', 'sector_claim_patterns', 'sector_regulatory_overlay', 'sector_peer_benchmarks']:
    data = yaml.safe_load(pathlib.Path(f'src/do_uw/brain/config/{f}.yaml').read_text())
    assert 'metadata' in data, f'{f}: missing metadata'
    assert 'sources' in data['metadata'], f'{f}: missing provenance sources'
    print(f'{f}: OK ({len(data)} top-level keys)')
# Verify sector signal YAML
sigs = list(yaml.safe_load_all(pathlib.Path('src/do_uw/brain/signals/biz/sector.yaml').read_text()))
# yaml.safe_load_all returns generator; sector.yaml is a list
import itertools
sigs_flat = list(itertools.chain.from_iterable(s if isinstance(s, list) else [s] for s in sigs))
assert len(sigs_flat) >= 4, f'Expected 4+ signals, got {len(sigs_flat)}'
ids = [s['id'] for s in sigs_flat]
assert 'SECT.hazard_tier' in ids
assert 'SECT.claim_patterns' in ids
assert 'SECT.regulatory_overlay' in ids
assert 'SECT.peer_comparison' in ids
for s in sigs_flat:
    assert s.get('schema_version') == 3, f'{s[\"id\"]}: missing schema_version 3'
    assert s.get('group') == 'sector_risk', f'{s[\"id\"]}: wrong group'
print(f'sector.yaml: OK ({len(sigs_flat)} signals)')
print('All reference data and signal definitions valid')
"
    </automated>
  </verify>
  <done>4 YAML reference data tables with provenance exist in brain/config/. 4 SECT signal YAML definitions with v3 schema, sector_risk group, and acquisition/evaluation/presentation blocks exist in brain/signals/biz/sector.yaml.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create sector extraction module with TDD</name>
  <files>
    src/do_uw/stages/extract/sector_classification.py
    tests/test_sector_signals.py
  </files>
  <behavior>
    - extract_sector_signals(state) returns dict with 4 field_key values
    - sector_hazard_tier: returns tier string from GICS sub-industry lookup, falls back to GICS sector, falls back to SIC-derived sector
    - sector_claim_patterns: returns top 3 claim theories dict from GICS industry group lookup
    - sector_regulatory_overlay: returns regulators list + intensity + trend from GICS industry group lookup
    - sector_peer_comparison: returns outlier_count + dimension comparisons given company's score vs sector median
    - When GICS code is None, falls back to SIC -> sic_to_sector() mapping
    - When no match found at any level, returns sensible defaults (tier="Moderate", empty patterns, etc.)
  </behavior>
  <action>
  Write failing tests first (RED), then implement extraction module (GREEN).

  **Tests (RED phase):**
  Create tests/test_sector_signals.py with tests for:
  1. test_hazard_tier_gics_sub_industry_match -- known GICS code maps to correct tier
  2. test_hazard_tier_gics_sector_fallback -- unknown sub-industry falls back to sector-level tier
  3. test_hazard_tier_sic_fallback -- when GICS is None, derives from SIC code
  4. test_hazard_tier_no_match_default -- completely unknown codes return "Moderate"
  5. test_claim_patterns_lookup -- known industry group returns 3 theories
  6. test_claim_patterns_empty -- unknown group returns empty list
  7. test_regulatory_overlay_lookup -- known group returns regulators + intensity
  8. test_peer_comparison_outlier_detection -- company score > median + std_dev flagged as outlier
  9. test_peer_comparison_no_outliers -- company within normal range shows 0 outliers
  10. test_full_extraction_integration -- extract_sector_signals returns complete dict

  Use mock state objects with company.gics_code and company.identity.sic_code set to known values from the reference data.

  **Implementation (GREEN phase):**
  Create src/do_uw/stages/extract/sector_classification.py following the environment_assessment.py pattern:
  - Module-level: load reference YAML files once (cached via module-level variables)
  - `extract_sector_signals(state) -> dict`: main entry point
  - `_compute_hazard_tier(gics_code, sic_code) -> dict`: lookup with 3-level fallback (sub-industry -> sector -> SIC-derived)
  - `_compute_claim_patterns(gics_code) -> dict`: lookup by 4-digit GICS industry group
  - `_compute_regulatory_overlay(gics_code) -> dict`: lookup by 4-digit GICS industry group
  - `_compute_peer_comparison(gics_code, company_score, governance_score, financial_score, litigation_count) -> dict`: compare vs sector median, count outliers (>1 std_dev from median on any dimension)

  GICS code anatomy: 8 digits -> sector (first 2), industry group (first 4), industry (first 6), sub-industry (all 8). Use this for fallback hierarchy.

  For peer comparison, read company's actual score from state.score_result.total_score if available, governance from state.extracted.governance_quality_score, financial from state.extracted.financials. If any dimension unavailable, skip that comparison (don't count as outlier).

  All reference data loaded from brain/config/ YAML files. No hardcoded tiers or thresholds in the extraction module.
  </action>
  <verify>
    <automated>cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/test_sector_signals.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>extract_sector_signals(state) returns dict with sector_hazard_tier (tier string + context), sector_claim_patterns (top 3 theories), sector_regulatory_overlay (regulators + intensity + trend), sector_peer_comparison (outlier_count + dimension comparisons). All 10+ tests pass. Fallback chain (GICS sub-industry -> GICS sector -> SIC) works correctly.</done>
</task>

</tasks>

<verification>
- All 4 reference data YAML files parse correctly with provenance metadata
- All 4 SECT signal YAML definitions have schema_version: 3 and sector_risk group
- extract_sector_signals returns complete dict for known and unknown GICS codes
- Fallback chain works: GICS sub-industry -> GICS sector -> SIC-derived sector -> defaults
- All tests pass
</verification>

<success_criteria>
- 4 reference data tables exist with provenance from D&O studies
- 4 SECT brain signal definitions follow v3 schema pattern
- Extraction module computes all 4 signal values from state data
- Tests cover lookup, fallback, and edge cases
</success_criteria>

<output>
After completion, create `.planning/phases/98-sector-risk-classification/98-01-SUMMARY.md`
</output>
