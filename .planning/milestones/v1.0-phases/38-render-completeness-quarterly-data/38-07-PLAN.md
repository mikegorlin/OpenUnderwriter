---
phase: 38-render-completeness-quarterly-data
plan: 07
type: tdd
wave: 3
depends_on:
  - 38-02
  - 38-04
  - 38-05
  - 38-06
files_modified:
  - tests/test_cross_format_consistency.py
  - tests/test_render_coverage.py
  - src/do_uw/stages/render/coverage.py
autonomous: true
requirements:
  - SC-1
  - SC-7

must_haves:
  truths:
    - "Cross-format consistency test verifies Word, Markdown, and HTML/PDF contain the same section headings"
    - "Cross-format consistency test verifies key data points appear in all formats"
    - "Render coverage test achieves >90% on a sample state with populated domains in all three formats (Markdown, Word, HTML)"
    - "Uncovered fields are documented with clear rationale for exclusion"
  artifacts:
    - path: "tests/test_cross_format_consistency.py"
      provides: "Cross-format section and data consistency verification"
    - path: "tests/test_render_coverage.py"
      provides: "Updated render coverage test with >90% threshold"
  key_links:
    - from: "tests/test_cross_format_consistency.py"
      to: "src/do_uw/stages/render/md_renderer.py"
      via: "renders Markdown and extracts sections"
      pattern: "render_markdown"
    - from: "tests/test_cross_format_consistency.py"
      to: "src/do_uw/stages/render/word_renderer.py"
      via: "renders Word doc and extracts sections"
      pattern: "render_word_document"
    - from: "tests/test_render_coverage.py"
      to: "src/do_uw/stages/render/coverage.py"
      via: "compute_coverage with >90% assertion"
      pattern: "compute_coverage.*assert.*90"
---

<objective>
Build cross-format consistency test and finalize render coverage test to verify >90% coverage after all gap closure work.

Purpose: SC-7 requires a test verifying Word, Markdown, and PDF contain the same section headings and key data points. SC-1 requires >90% automated coverage. This plan runs AFTER all gap closure (Plans 04-06) to validate the complete picture.

Output: Two test suites — cross-format consistency and final render coverage — both passing.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-render-completeness-quarterly-data/38-02-SUMMARY.md
@.planning/phases/38-render-completeness-quarterly-data/38-04-SUMMARY.md
@.planning/phases/38-render-completeness-quarterly-data/38-05-SUMMARY.md
@.planning/phases/38-render-completeness-quarterly-data/38-06-SUMMARY.md
@src/do_uw/stages/render/coverage.py
@tests/test_render_coverage.py
</context>

<feature>
  <name>Cross-Format Consistency + Final Coverage Validation</name>
  <files>tests/test_cross_format_consistency.py, tests/test_render_coverage.py, src/do_uw/stages/render/coverage.py</files>
  <behavior>
    **Cross-format consistency test:**
    - Build a sample AnalysisState with representative data in all domains
    - Render to Markdown (via render_markdown)
    - Render to Word (via render_word_document)
    - Extract section headings from Markdown: regex `^#{1,3} (.+)$`
    - Extract section headings from Word: paragraph styles with heading levels
    - Assert same logical sections present in both (mapped through a SECTION_REGISTRY of canonical section names, since exact heading text may differ slightly between formats: "Section 3: Financial Health" in MD vs "FINANCIAL HEALTH" in Word)
    - Extract key data values (ticker, company name, total score, market cap tier) from both
    - Assert each key value appears in both formats
    - Note: PDF test can be skipped if WeasyPrint not installed (already returns None in test env). The HTML context comes from the same build_template_context() as MD, so HTML/PDF consistency is guaranteed by architecture.

    **Final render coverage test (all three formats per user decision):**
    - Update the coverage test fixture from Plan 02 to include data for all newly-rendered domains (classification, hazard_profile, risk_factors, forensic_composites, executive_summary, quarterly_updates, derivative_suits, contingent_liabilities, board forensics with interlocks)
    - Extend `compute_coverage()` to accept multiple rendered texts (or call it once per format). Per user decision: "Full field coverage test — walk the state model, verify every non-null field appears in all three formats. Strict."
    - Run compute_coverage() against Markdown rendered output and assert coverage_pct > 90
    - Run compute_coverage() against Word rendered output (extract text from docx paragraphs + table cells) and assert coverage_pct > 90
    - Run compute_coverage() against HTML rendered output and assert coverage_pct > 90
    - For any fields below 90% in ANY format, investigate whether they are legitimately excluded or need additional rendering
    - Update EXCLUSION_PATHS if new legitimate exclusions are discovered (intermediate computation fields, internal routing metadata)
    - Print the uncovered fields list per format as test output for debugging

    Test cases:
    - test_md_and_word_share_section_headings: Both have all 8 major sections
    - test_key_values_in_both_formats: ticker, score, company name appear in both
    - test_md_coverage_exceeds_90_percent: compute_coverage on Markdown returns >90%
    - test_word_coverage_exceeds_90_percent: compute_coverage on Word extracted text returns >90%
    - test_html_coverage_exceeds_90_percent: compute_coverage on HTML returns >90%
    - test_uncovered_fields_documented: any field below threshold has a rationale in EXCLUSION_PATHS or documented
  </behavior>
  <implementation>
    1. Create `tests/test_cross_format_consistency.py`:
       - Fixture: rich AnalysisState with data in company, financials, governance, litigation, scoring, classification, hazard_profile, executive_summary. IMPORTANT: the fixture must include ELEVATED density indicators in at least the financials and litigation sections (e.g., multiple triggered checks, multiple litigation entries, multiple risk factors) so that density-gated render paths are exercised. A clean-company fixture with minimal data will not trigger these paths and will miss coverage gaps.
       - Section heading extraction helpers for MD (regex) and Word (python-docx paragraph iteration)
       - SECTION_REGISTRY mapping canonical names to format-specific heading patterns
       - Test that verifies both formats have all canonical sections
       - Test that verifies key data points appear in both format outputs

    2. Update `tests/test_render_coverage.py`:
       - Enrich the sample state fixture with ALL data domains populated
       - Add helper to extract plain text from Word docx (iterate paragraphs + table cells)
       - Assert coverage_pct > 90 for Markdown rendered text
       - Assert coverage_pct > 90 for Word extracted text (per user decision: all three formats)
       - Assert coverage_pct > 90 for HTML rendered text
       - List uncovered fields per format in test output for visibility
       - Any field not rendered must have a documented exclusion rationale

    3. Update `src/do_uw/stages/render/coverage.py` if needed:
       - Ensure compute_coverage() works with any rendered text string (already format-agnostic by design)
       - Refine EXCLUSION_PATHS based on discoveries during gap closure
       - Improve format-aware matchers if new format patterns discovered
       - Add any missing value type handlers
  </implementation>
</feature>

<verification>
1. `uv run pytest tests/test_cross_format_consistency.py -v` — all tests pass
2. `uv run pytest tests/test_render_coverage.py -v` — all tests pass, coverage >90%
3. `uv run pytest tests/ -k "render" -v` — all render-related tests pass together
</verification>

<success_criteria>
- Cross-format test verifies Word and Markdown have same section headings
- Cross-format test verifies key data points appear in both formats
- Render coverage exceeds 90% on a populated sample state in all three formats (Markdown, Word, HTML)
- All uncovered fields have documented exclusion rationale
- Full render test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/38-render-completeness-quarterly-data/38-07-SUMMARY.md`
</output>
