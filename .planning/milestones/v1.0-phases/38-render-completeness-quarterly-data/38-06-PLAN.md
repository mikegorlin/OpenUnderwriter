---
phase: 38-render-completeness-quarterly-data
plan: 06
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - src/do_uw/stages/render/md_renderer_helpers_analysis.py
  - src/do_uw/stages/render/md_renderer.py
  - src/do_uw/templates/markdown/sections/executive.md.j2
  - src/do_uw/templates/markdown/sections/company.md.j2
  - src/do_uw/templates/markdown/sections/scoring.md.j2
  - src/do_uw/templates/html/sections/executive.html.j2
  - src/do_uw/templates/html/sections/company.html.j2
  - src/do_uw/templates/html/sections/scoring.html.j2
  - src/do_uw/stages/render/sections/sect2_company_hazard.py
  - src/do_uw/stages/render/sections/sect2_company.py
  - src/do_uw/stages/render/sections/sect7_scoring_analysis.py
  - src/do_uw/stages/render/sections/sect7_scoring.py
autonomous: true
requirements:
  - SC-1

must_haves:
  truths:
    - "Executive summary section renders snapshot, inherent risk baseline, key findings, and underwriting thesis"
    - "Classification data renders (market cap tier, sector, base filing rate, severity bands)"
    - "Hazard profile renders all 55 dimensions with name, exposure level, and evidence — sorted by score descending — with top risk dimensions highlighted above the full table"
    - "Risk factors from Item 1A render (D&O-relevant, new, unique — filtered per user decision)"
    - "Forensic composite scores render (FIS, RQS, CFQS)"
    - "All empty/absent domains show 'None found' or 'Not available'"
  artifacts:
    - path: "src/do_uw/stages/render/md_renderer_helpers_analysis.py"
      provides: "Extraction helpers for classification, hazard, risk factors, forensic composites, temporal signals"
    - path: "src/do_uw/templates/markdown/sections/executive.md.j2"
      provides: "Full executive summary with snapshot, risk baseline, findings, thesis"
    - path: "src/do_uw/templates/markdown/sections/company.md.j2"
      provides: "Risk factors subsection in company profile"
  key_links:
    - from: "src/do_uw/stages/render/md_renderer_helpers_analysis.py"
      to: "src/do_uw/models/classification.py"
      via: "reads ClassificationResult"
      pattern: "classification"
    - from: "src/do_uw/stages/render/md_renderer_helpers_analysis.py"
      to: "src/do_uw/models/hazard_profile.py"
      via: "reads HazardProfile"
      pattern: "hazard_profile"
    - from: "src/do_uw/stages/render/md_renderer.py"
      to: "src/do_uw/stages/render/md_renderer_helpers_analysis.py"
      via: "imports extract_classification, extract_hazard_profile, etc."
      pattern: "extract_classification|extract_hazard|extract_risk_factors"
---

<objective>
Render all missing data domains: executive summary, classification, hazard profile, risk factors, forensic composites, temporal signals, NLP signals, executive risk, and peril map. These are state fields with populated data but zero rendering path in any format.

Purpose: SC-1 requires >90% render coverage. Research shows approximately 15+ data domains have no rendering at all. This plan closes the largest gaps — classification, hazard profile, risk factors, and analysis composites — which together represent roughly 30-40% of the uncovered fields.

Output: New extraction helpers and updated templates for all missing data domains.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-render-completeness-quarterly-data/38-01-SUMMARY.md
@src/do_uw/models/state.py
@src/do_uw/models/classification.py
@src/do_uw/models/hazard_profile.py
@src/do_uw/models/executive_summary.py
@src/do_uw/stages/render/md_renderer.py
@src/do_uw/stages/render/md_renderer_helpers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create analysis extraction helpers + classification/hazard/risk factor context</name>
  <files>
    src/do_uw/stages/render/md_renderer_helpers_analysis.py
    src/do_uw/stages/render/md_renderer.py
  </files>
  <action>
    **Create `md_renderer_helpers_analysis.py`** with extraction helpers for all missing data domains:

    1. `extract_classification(state) -> dict`:
       - market_cap_tier, sector_code, sector_name, years_public
       - base_filing_rate_pct, severity_band_low_m, severity_band_high_m
       - ddl_exposure_base_m, ipo_multiplier, cap_filing_multiplier
       - Format percentages, currency amounts

    2. `extract_hazard_profile(state) -> dict`:
       - ies_score (0-100), ies_label (LOW/MODERATE/ELEVATED/HIGH/CRITICAL based on score)
       - categories: list of {category_code, category_name, score, max_score, exposure_level}
       - all_dimensions: list of ALL scored dimensions (all 55) sorted by score descending, each with {dimension_id, name, score, category, exposure_level, evidence}. Per user decision: "Full hazard profile table — all 55 dimensions rendered with dimension name, exposure level, and evidence"
       - top_risk_dimensions: subset of all_dimensions where exposure_level is ELEVATED/HIGH/CRITICAL — used for a highlight callout above the full table
       - interaction_effects: list of triggered interaction patterns

    3. `extract_risk_factors(state) -> list[dict]`:
       - Filter to D&O-relevant, new this year, or unique (per user decision: "D&O-relevant, new, or unique only")
       - Each: title, category, severity, is_new, do_relevance, source_passage (truncated to 200 chars)
       - Group by category

    4. `extract_forensic_composites(state) -> dict`:
       - FIS (Financial Integrity Score), RQS (Reporting Quality Score), CFQS (Cash Flow Quality Score)
       - Each with score, interpretation, component details if available

    5. `extract_executive_risk(state) -> dict`:
       - weighted_score, findings list, risk level

    6. `extract_temporal_signals(state) -> list[dict]`:
       - Each signal with type, direction, magnitude, description

    7. `extract_nlp_signals(state) -> dict`:
       - readability score, tone assessment, risk language density

    8. `extract_peril_map(state) -> dict`:
       - overall_assessment, assessments list with peril_type, likelihood, severity, evidence

    **Update `md_renderer.py` `build_template_context()`**:
    - Import new extraction functions
    - Add context keys: `classification`, `hazard_profile`, `risk_factors`, `forensic_composites`, `executive_risk`, `temporal_signals`, `nlp_signals`, `peril_map`
    - Guard each with appropriate None checks on state fields

    Keep the new file under 500 lines. These are extraction/formatting functions, not complex logic.
  </action>
  <verify>
    `uv run python -c "from do_uw.stages.render.md_renderer_helpers_analysis import extract_classification; print('OK')"`
    `wc -l src/do_uw/stages/render/md_renderer_helpers_analysis.py` — under 500 lines
    `uv run pytest tests/test_render_outputs.py -v` — existing tests pass (no context key breakage)
  </verify>
  <done>
    - 8 extraction helpers created for all missing data domains
    - build_template_context() passes all new context keys to templates
    - No existing tests broken
    - File under 500 lines
  </done>
</task>

<task type="auto">
  <name>Task 2: Render missing domains in MD/HTML templates</name>
  <files>
    src/do_uw/templates/markdown/sections/executive.md.j2
    src/do_uw/templates/markdown/sections/company.md.j2
    src/do_uw/templates/markdown/sections/scoring.md.j2
    src/do_uw/templates/html/sections/executive.html.j2
    src/do_uw/templates/html/sections/company.html.j2
    src/do_uw/templates/html/sections/scoring.html.j2
  </files>
  <action>
    **Update `executive.md.j2`** to render full executive summary:
    - Company Snapshot (ticker, name, market cap, revenue, employees, industry, exchange)
    - Inherent Risk Baseline (sector base rate, cap multiplier, adjusted rate, severity ranges)
    - Key Findings — Top Negatives (table: evidence, section, impact, theory)
    - Key Findings — Top Positives (same format)
    - Underwriting Thesis (narrative, risk type, top factors)
    - Deal Context (if not placeholder)
    - Market Intelligence (if available)
    - If executive_summary is None, show "Executive summary data not available"

    **Update `company.md.j2`** to add new subsections:
    - **Classification**: table with market cap tier, sector, years public, base filing rate, severity band
    - **Hazard Profile**: IES score with label, highlight callout for top risk dimensions (ELEVATED/HIGH/CRITICAL), then full table of ALL 55 dimensions sorted by score descending showing dimension name, exposure level, and evidence. Per user decision: "Full hazard profile table — all 55 dimensions rendered." Category summary table below.
    - **Risk Factors (D&O-Relevant)**: filtered Item 1A risk factors grouped by category, each with title, severity badge, new-this-year flag, and truncated source passage
    - If no risk factors: "No D&O-relevant risk factors identified."

    **Update `scoring.md.j2`** to add new subsections:
    - **Forensic Composite Scores**: FIS, RQS, CFQS with scores and interpretations
    - **Executive Risk Profile**: weighted score and findings
    - **Temporal Signals**: trend signals table
    - **NLP Filing Analysis**: readability, tone, risk language density
    - **Peril Map**: overall assessment + individual peril assessments table

    **Update corresponding HTML templates** (executive.html.j2, company.html.j2, scoring.html.j2) with the same data using HTML formatting.

    **Important:** Each template section should render data when available AND show "Not available" or "None found" when the domain has no data. Per user decision: "Show all fields including empty."

    Check all template files stay under 500 lines. If any approaches the limit, split into sub-includes.
  </action>
  <verify>
    `uv run pytest tests/test_render_outputs.py -v` — tests pass
    `wc -l src/do_uw/templates/markdown/sections/*.md.j2` — all under 500 lines
    `uv run python -c "from do_uw.stages.render.md_renderer import build_template_context; from do_uw.models.state import AnalysisState; ctx = build_template_context(AnalysisState(ticker='TEST')); print(list(ctx.keys()))"` — new keys present
  </verify>
  <done>
    - Executive summary renders all subcomponents (snapshot, risk baseline, findings, thesis)
    - Classification, hazard profile, risk factors render in company section
    - Forensic composites, executive risk, temporal signals, NLP signals, peril map render in scoring section
    - Empty domains show "Not available" or "None found"
    - All template files under 500 lines
  </done>
</task>

<task type="auto">
  <name>Task 3: Render missing domains in Word renderer</name>
  <files>
    src/do_uw/stages/render/sections/sect2_company_hazard.py
    src/do_uw/stages/render/sections/sect2_company.py
    src/do_uw/stages/render/sections/sect7_scoring_analysis.py
    src/do_uw/stages/render/sections/sect7_scoring.py
  </files>
  <action>
    Per phase goal: "Word, Markdown, and PDF outputs are consistent in data coverage." The new data domains in Tasks 1-2 target MD/HTML only. This task adds matching Word renderer sections so all three formats have the same data.

    **Create `sect2_company_hazard.py`** (~200 lines):
    - Function `render_hazard_profile(doc, state, ds)`:
      - IES score with label
      - Top risk dimensions highlight callout (ELEVATED/HIGH/CRITICAL)
      - Full table of ALL 55 hazard dimensions: dimension name, exposure level, evidence — sorted by score descending. Per user decision: "Full hazard profile table — all 55 dimensions"
      - Category summary table
    - Function `render_classification(doc, state, ds)`:
      - Market cap tier, sector, years public, base filing rate, severity band table
    - Function `render_risk_factors(doc, state, ds)`:
      - D&O-relevant, new, or unique risk factors grouped by category
      - Or "No D&O-relevant risk factors identified." if none

    **Update `sect2_company.py`** to call `render_hazard_profile`, `render_classification`, and `render_risk_factors` at appropriate positions in the company section.

    **Create `sect7_scoring_analysis.py`** (~200 lines):
    - Function `render_forensic_composites(doc, state, ds)`:
      - FIS, RQS, CFQS with scores and interpretations
    - Function `render_temporal_signals(doc, state, ds)`:
      - Temporal trend signals table
    - Function `render_nlp_signals(doc, state, ds)`:
      - Readability score, tone assessment, risk language density

    **Update `sect7_scoring.py`** to call the new forensic composites, temporal signals, and NLP signals renderers after existing scoring detail.

    Note: Peril map already has `sect7_peril_map.py`. Executive risk is covered by `sect1_executive.py`. Verify both are adequate — if gaps exist, extend them.

    Keep all new files under 500 lines. Empty/absent domains show "Not available" or "None found."
  </action>
  <verify>
    `uv run python -c "from do_uw.stages.render.sections.sect2_company_hazard import render_hazard_profile; print('OK')"`
    `uv run python -c "from do_uw.stages.render.sections.sect7_scoring_analysis import render_forensic_composites; print('OK')"`
    `wc -l src/do_uw/stages/render/sections/sect2_company_hazard.py` — under 500 lines
    `wc -l src/do_uw/stages/render/sections/sect7_scoring_analysis.py` — under 500 lines
    `uv run pytest tests/test_render_outputs.py -v` — existing tests pass
  </verify>
  <done>
    - Hazard profile, classification, risk factors render in Word via sect2_company_hazard.py
    - Forensic composites, temporal signals, NLP signals render in Word via sect7_scoring_analysis.py
    - All three formats (MD, Word, HTML) now have the same data domains
    - Empty domains show "Not available" / "None found"
    - All files under 500 lines
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_render_outputs.py -v` — all tests pass
2. All files under 500 lines
3. Render empty state — all new sections show "Not available" gracefully in MD, HTML, AND Word
4. Render populated state — new sections show real data in all three formats
</verification>

<success_criteria>
- All 15+ previously-unrendered data domains now have rendering paths in all three formats (MD, Word, HTML)
- Executive summary is fully rendered (snapshot, risk baseline, findings, thesis)
- Classification, hazard profile (all 55 dimensions), risk factors appear in all formats
- Analysis composites (FIS, RQS, CFQS), temporal signals, NLP signals, peril map appear in all formats
- Empty fields show "None found" / "Not available" per user decision
- Word renderer has matching sections for every domain added to MD/HTML
</success_criteria>

<output>
After completion, create `.planning/phases/38-render-completeness-quarterly-data/38-06-SUMMARY.md`
</output>
