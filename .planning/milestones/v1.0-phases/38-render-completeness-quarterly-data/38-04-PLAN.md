---
phase: 38-render-completeness-quarterly-data
plan: 04
type: execute
wave: 2
depends_on:
  - 38-01
  - 38-03
files_modified:
  - src/do_uw/templates/markdown/sections/financial.md.j2
  - src/do_uw/stages/render/md_renderer_helpers_financial.py
  - src/do_uw/stages/render/sections/sect3_financial.py
  - src/do_uw/stages/render/sections/sect3_quarterly.py
  - src/do_uw/templates/html/sections/financial.html.j2
  - src/do_uw/stages/render/html_renderer.py
autonomous: true
requirements:
  - SC-2
  - SC-3
  - SC-6

must_haves:
  truths:
    - "Full income statement, balance sheet, and cash flow tables render in Markdown with all available line items"
    - "Recent Quarterly Update subsection renders in all three formats when 10-Q data is present"
    - "YTD comparison shows current vs prior year matching period"
    - "Quarterly section omitted when no 10-Q filed after most recent 10-K"
    - "Confidence badges appear on financial data points"
  artifacts:
    - path: "src/do_uw/templates/markdown/sections/financial.md.j2"
      provides: "Full financial statements + quarterly update in MD"
    - path: "src/do_uw/stages/render/sections/sect3_quarterly.py"
      provides: "Quarterly update Word renderer"
  key_links:
    - from: "src/do_uw/stages/render/md_renderer_helpers_financial.py"
      to: "src/do_uw/models/financials.py"
      via: "reads QuarterlyUpdate from ExtractedFinancials"
      pattern: "quarterly_updates"
    - from: "src/do_uw/templates/markdown/sections/financial.md.j2"
      to: "src/do_uw/stages/render/md_renderer_helpers_financial.py"
      via: "template context from extract_financials()"
      pattern: "financials\\."
---

<objective>
Render full financial statement tables (income, balance sheet, cash flow with ALL line items) and the "Recent Quarterly Update" subsection in all three output formats.

Purpose: SC-6 requires all three financial statements with all available line items (not just revenue and net income). SC-2/SC-3 require quarterly data rendering with YTD comparison. Currently the MD template only shows a summary. The Word renderer already has full tables via sect3_tables.py — this plan brings MD and HTML to parity and adds quarterly rendering to all formats.

Output: Full financial statement tables in Markdown/HTML, quarterly update section in all formats.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-render-completeness-quarterly-data/38-01-SUMMARY.md
@.planning/phases/38-render-completeness-quarterly-data/38-03-SUMMARY.md
@src/do_uw/stages/render/md_renderer_helpers_financial.py
@src/do_uw/stages/render/sections/sect3_tables.py
@src/do_uw/stages/render/sections/sect3_financial.py
@src/do_uw/models/financials.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full financial statement tables in MD + quarterly update context extraction</name>
  <files>
    src/do_uw/stages/render/md_renderer_helpers_financial.py
    src/do_uw/templates/markdown/sections/financial.md.j2
  </files>
  <action>
    **Extend extract_financials()** in `md_renderer_helpers_financial.py` to produce full statement data:

    1. Add `income_statement_rows`, `balance_sheet_rows`, `cash_flow_rows` — each a list of dicts: `{"label": str, "values": dict[period_label, formatted_value], "yoy_change": str|None}`. Iterate ALL line_items from each FinancialStatement, format values with format_currency(compact=True), include YoY change as format_change_indicator.

    2. Add `statement_periods` — the period column headers (e.g., ["FY2025", "FY2024"])

    3. Add `quarterly_updates` — list of dicts extracted from state.extracted.financials.quarterly_updates:
       ```python
       {
           "quarter": "Q1 FY2026",
           "period_end": "2025-12-28",
           "filing_date": "2026-01-30",
           "revenue": format_currency(qu.revenue.value, compact=True) if qu.revenue else "N/A",
           "net_income": format_currency(qu.net_income.value, compact=True) if qu.net_income else "N/A",
           "eps": f"${qu.eps.value:.2f}" if qu.eps else "N/A",
           "prior_revenue": format_currency(qu.prior_year_revenue, compact=True) if qu.prior_year_revenue else "N/A",
           "prior_net_income": ...,
           "prior_eps": ...,
           "revenue_change": format_change_indicator(...) if both available else "",
           "new_legal_proceedings": qu.new_legal_proceedings,
           "legal_updates": qu.legal_proceedings_updates,
           "going_concern": qu.going_concern,
           "going_concern_detail": qu.going_concern_detail,
           "material_weaknesses": qu.material_weaknesses,
           "md_a_highlights": qu.md_a_highlights,
           "subsequent_events": qu.subsequent_events,
       }
       ```

    **Update `financial.md.j2`** to render:
    1. **Full Income Statement table** — iterate income_statement_rows with period columns + YoY change column. Use proper Markdown table formatting.
    2. **Full Balance Sheet table** — same pattern
    3. **Full Cash Flow table** — same pattern
    4. **Recent Quarterly Update subsection** (only if quarterly_updates is non-empty):
       - Header: `### Recent Quarterly Update ({quarter}, filed {filing_date})`
       - Metrics table: Revenue, Net Income, EPS with current vs prior year + change %
       - Material Changes section (if any md_a_highlights)
       - New Legal Proceedings (if any)
       - Going Concern flag (if True)
       - Material Weaknesses (if any)
       - Subsequent Events (if any)
    5. Keep existing distress indicators, leverage, liquidity, earnings quality, audit sections

    **Important:** The existing summary fields (revenue, net_income, etc.) should REMAIN for backward compatibility — the full statement tables are ADDITIONS. Don't remove the summary snapshot at the top of the section.

    Verify `md_renderer_helpers_financial.py` stays under 500 lines. If approaching limit, extract quarterly context building into a separate helper function at the bottom of the file or split into `md_renderer_helpers_quarterly.py`.
  </action>
  <verify>
    `uv run pytest tests/test_render_outputs.py -v` — existing tests still pass
    `wc -l src/do_uw/stages/render/md_renderer_helpers_financial.py` — under 500 lines
    `wc -l src/do_uw/templates/markdown/sections/financial.md.j2` — under 500 lines
  </verify>
  <done>
    - Full financial statement tables render in Markdown with all line items
    - Quarterly update subsection renders when data present, omitted when absent
    - YTD comparison table with current vs prior year
    - All existing financial rendering preserved
  </done>
</task>

<task type="auto">
  <name>Task 2: Quarterly update in Word renderer + HTML template alignment</name>
  <files>
    src/do_uw/stages/render/sections/sect3_quarterly.py
    src/do_uw/stages/render/sections/sect3_financial.py
    src/do_uw/templates/html/sections/financial.html.j2
    src/do_uw/stages/render/html_renderer.py
  </files>
  <action>
    **Word renderer** — create `sect3_quarterly.py`:
    - Function `render_quarterly_update(doc, state, ds)` that renders the quarterly update subsection
    - Metric comparison table (Revenue, NI, EPS: current, prior year, change)
    - Material changes, new legal proceedings, going concern, material weaknesses, subsequent events
    - Called from `sect3_financial.py` after existing financial tables if `state.extracted.financials.quarterly_updates` is non-empty

    **Update sect3_financial.py** to import and call `render_quarterly_update` at the end of the financial section.

    **HTML template** — update `financial.html.j2`:
    - Add full financial statement tables (income, BS, CF) mirroring the MD template structure
    - Add quarterly update subsection block
    - HTML context already comes from `build_template_context()` which calls extract_financials(), so the same context is available
    - Use HTML table formatting with appropriate CSS classes for number alignment

    **Update html_renderer.py** if needed to pass quarterly data through to the HTML context. The `build_html_context()` function may need to include the new fields from extract_financials().

    Keep all new files under 500 lines.
  </action>
  <verify>
    `uv run pytest tests/test_render_outputs.py -v` — all tests pass
    `wc -l src/do_uw/stages/render/sections/sect3_quarterly.py` — under 500 lines
    `uv run python -c "from do_uw.stages.render.sections.sect3_quarterly import render_quarterly_update; print('OK')"`
  </verify>
  <done>
    - Quarterly update renders in Word format via sect3_quarterly.py
    - Full financial statement tables and quarterly update render in HTML template
    - All three formats show the same quarterly data when present
    - No file over 500 lines
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_render_outputs.py -v` — all existing tests pass
2. All new/modified files under 500 lines
3. Render a sample state with quarterly data — quarterly section appears in MD output
4. Render a sample state without quarterly data — quarterly section is absent
</verification>

<success_criteria>
- Full income statement, balance sheet, cash flow tables render in Markdown with ALL available line items
- Quarterly update section renders in MD, Word, and HTML when 10-Q data present
- Quarterly section omitted when no 10-Q data
- YTD comparison shows current vs prior year matching period
- Confidence badges on financial data points
</success_criteria>

<output>
After completion, create `.planning/phases/38-render-completeness-quarterly-data/38-04-SUMMARY.md`
</output>
