---
phase: 38-render-completeness-quarterly-data
plan: 05
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - src/do_uw/templates/markdown/sections/governance.md.j2
  - src/do_uw/templates/markdown/sections/litigation.md.j2
  - src/do_uw/stages/render/md_renderer_helpers_ext.py
  - src/do_uw/stages/render/sections/sect5_governance_board.py
  - src/do_uw/stages/render/sections/sect6_litigation.py
  - src/do_uw/templates/html/sections/governance.html.j2
  - src/do_uw/templates/html/sections/litigation.html.j2
autonomous: true
requirements:
  - SC-4
  - SC-5

must_haves:
  truths:
    - "Board forensic profiles render interlocks, relationship_flags, true_independence_concerns, committees in MD template"
    - "All derivative suits render with individual case detail in all formats (not just a count)"
    - "Contingent liabilities (ASC 450 disclosures) render in litigation section"
    - "Workforce, product, environmental matters render in litigation section"
    - "Empty fields show 'None found' to confirm the system checked"
  artifacts:
    - path: "src/do_uw/templates/markdown/sections/governance.md.j2"
      provides: "Full board forensic profiles in MD"
    - path: "src/do_uw/templates/markdown/sections/litigation.md.j2"
      provides: "All litigation matter types in MD"
  key_links:
    - from: "src/do_uw/stages/render/md_renderer_helpers_ext.py"
      to: "src/do_uw/models/governance_forensics.py"
      via: "reads BoardForensicProfile.interlocks, .relationship_flags, .committees"
      pattern: "interlocks|relationship_flags|committees"
    - from: "src/do_uw/stages/render/md_renderer_helpers_ext.py"
      to: "src/do_uw/models/litigation.py"
      via: "reads derivative_suits, contingent_liabilities, workforce_product_environmental"
      pattern: "derivative_suits|contingent_liabilities|workforce_product"
---

<objective>
Complete board forensic rendering (interlocks, relationship flags, committees, independence concerns) and full litigation rendering (derivative suits with case details, contingent liabilities, workforce/product/environmental matters) in all three output formats.

Purpose: SC-5 requires board member profiles with interlock analysis, tenure flags, and committee assignments. SC-4 requires ALL litigation matters including derivative suits with full detail. Research shows 4 of 10 board forensic fields render in MD and derivative suits show only as a count. This plan closes those gaps.

Output: Complete governance forensics + complete litigation in MD, Word, and HTML.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-render-completeness-quarterly-data/38-01-SUMMARY.md
@src/do_uw/stages/render/md_renderer_helpers_ext.py
@src/do_uw/models/governance_forensics.py
@src/do_uw/models/litigation.py
@src/do_uw/stages/render/sections/sect5_governance_board.py
@src/do_uw/stages/render/sections/sect6_litigation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete board forensics in MD + extend governance context extraction</name>
  <files>
    src/do_uw/stages/render/md_renderer_helpers_ext.py
    src/do_uw/templates/markdown/sections/governance.md.j2
    src/do_uw/templates/html/sections/governance.html.j2
  </files>
  <action>
    **Extend extract_governance()** in `md_renderer_helpers_ext.py` to include full forensic data for each board member:

    Currently each board member dict has: name, tenure, is_independent, other_boards. Add:
    - `committees`: list[str] (committee names like "Audit", "Compensation", "Nominating")
    - `interlocks`: list[str] (description of interlock relationships)
    - `relationship_flags`: list[str] (relationship concerns)
    - `true_independence_concerns`: list[str] (concerns about actual vs stated independence)
    - `prior_litigation`: int (count of prior litigation involvement)
    - `is_overboarded`: bool (serves on too many boards)

    Read these from `BoardForensicProfile` in `state.extracted.governance.board_forensics`.

    **Update governance.md.j2** to render full board member profiles:
    - For each board member, render a sub-section (or detailed table row) with:
      - Name, tenure, independence status
      - Committees: comma-separated list or "None"
      - Other boards: comma-separated list or "None"
      - Interlocks: bulleted list or "None found"
      - Relationship flags: bulleted list or "None found"
      - Independence concerns: bulleted list or "None found"
      - Overboarded: "Yes" / "No"
      - Prior litigation: count or "None"

    Per user decision: "Full forensic profile per board member" and "Show all fields including empty" — every field appears even if no findings. "None found" confirms the system checked.

    **Update governance.html.j2** to include the same data with appropriate HTML formatting.

    **Important:** Check line counts. `md_renderer_helpers_ext.py` is 481 lines. If adding governance extraction pushes it over 500, split extract_governance() into a separate file `md_renderer_helpers_governance.py` and import it.
  </action>
  <verify>
    `wc -l src/do_uw/stages/render/md_renderer_helpers_ext.py` — under 500 lines (or split file is under 500)
    `wc -l src/do_uw/templates/markdown/sections/governance.md.j2` — under 500 lines
    `uv run pytest tests/test_render_outputs.py -v` — existing tests pass
  </verify>
  <done>
    - Board forensic profiles include committees, interlocks, relationship_flags, independence_concerns, overboarded, prior_litigation
    - All fields render in MD and HTML even when empty (show "None found")
    - No file over 500 lines
  </done>
</task>

<task type="auto">
  <name>Task 2: Complete litigation rendering — derivative suits, contingent liabilities, other matters</name>
  <files>
    src/do_uw/stages/render/md_renderer_helpers_ext.py
    src/do_uw/templates/markdown/sections/litigation.md.j2
    src/do_uw/stages/render/sections/sect6_litigation.py
    src/do_uw/templates/html/sections/litigation.html.j2
  </files>
  <action>
    **Extend extract_litigation()** in `md_renderer_helpers_ext.py` to include:

    1. `derivative_suits`: list of dicts with full case detail (case_name, filing_date, court, status, allegations, relief_sought — or whatever fields CaseDetail/DerivativeSuit model has). Currently only the count is rendered.

    2. `contingent_liabilities`: list of dicts from state.extracted.litigation.contingent_liabilities. Each should have: description, amount_range, probability_assessment, source.

    3. `workforce_product_environmental`: dict/list from state.extracted.litigation.workforce_product_environmental. Render employment matters, product liability, environmental matters.

    4. `whistleblower_indicators`: list from state.extracted.litigation.whistleblower_indicators. Render any findings or "None found".

    **Update litigation.md.j2** to render all matter types:

    1. Existing SCA cases (already rendered) — verify ALL cases are shown (no truncation)
    2. **Derivative Suits** subsection: individual case details for each derivative suit with case name, date, court, allegations, status. If none: "No derivative suits found."
    3. **Contingent Liabilities** subsection: ASC 450 disclosures with description, amount range, probability. If none: "No material contingent liabilities disclosed."
    4. **Workforce, Product & Environmental** subsection: categorized by type (employment, product, environmental). If none: "None found."
    5. **Whistleblower Indicators** subsection: findings or "None found."

    Per user decision: "Show everything" and "Show all fields including empty."

    **Update Word renderer** (sect6_litigation.py): Add rendering for derivative suit details, contingent liabilities, workforce matters. The Word renderer may already show some of these — check first and only add what's missing.

    **Update HTML template** (litigation.html.j2): Same data as MD.

    **Important:** Check sect6_litigation.py (480 lines) — if adding new sections pushes over 500, split into sect6_litigation_detail.py for derivative/contingent/other matter rendering.
  </action>
  <verify>
    `wc -l src/do_uw/stages/render/md_renderer_helpers_ext.py` — under 500 lines (or split)
    `wc -l src/do_uw/templates/markdown/sections/litigation.md.j2` — under 500 lines
    `wc -l src/do_uw/stages/render/sections/sect6_litigation.py` — under 500 lines (or split)
    `uv run pytest tests/test_render_outputs.py -v` — existing tests pass
  </verify>
  <done>
    - Derivative suits render with individual case detail in MD, Word, and HTML
    - Contingent liabilities render with amount ranges and probability
    - Workforce, product, environmental matters render
    - Whistleblower indicators render or show "None found"
    - Empty subsections show "None found" (not omitted)
    - No file over 500 lines
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_render_outputs.py -v` — all tests pass
2. All files under 500 lines
3. Render a state with board forensics — committees, interlocks, relationship_flags visible
4. Render a state with derivative suits — individual case details visible
5. Render an empty state — all subsections show "None found" or equivalent
</verification>

<success_criteria>
- Board member profiles show all 10 forensic fields including interlocks, committees, relationship flags
- All derivative suits render with full case detail, not just a count
- Contingent liabilities, workforce/product/environmental matters, whistleblower indicators all render
- Empty fields explicitly show "None found" for underwriter trust
- All three formats (MD, Word, HTML) include the same data
</success_criteria>

<output>
After completion, create `.planning/phases/38-render-completeness-quarterly-data/38-05-SUMMARY.md`
</output>
