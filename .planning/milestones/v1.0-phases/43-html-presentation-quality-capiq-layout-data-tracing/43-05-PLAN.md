---
phase: 43-html-presentation-quality-capiq-layout-data-tracing
plan: 05
type: execute
wave: 3
depends_on:
  - "43-01"
  - "43-02"
  - "43-03"
  - "43-04"
files_modified:
  - tests/stages/render/test_html_layout.py
autonomous: false
requirements:
  - VIS-05
  - OUT-04
  - OUT-03

must_haves:
  truths:
    - "Rendered HTML passes automated layout structure tests: two-column layout class present, sidebar TOC links match required sections, topbar has no score fields"
    - "Section order in rendered HTML is: identity → executive-summary → red-flags → scoring → financial-health → market → governance → litigation → sources"
    - "Red Flags section shows triggered/elevated flags sorted by severity"
    - "Financial and market key metric blocks show 3-column grid with label, value, context"
    - "Sources appendix appears with at least 1 numbered source in AAPL render"
    - "All existing tests pass (no regressions from Plans 01-04)"
    - "Human reviewer approves the rendered AAPL HTML as institutionally credible"
  artifacts:
    - path: "tests/stages/render/test_html_layout.py"
      provides: "Automated layout, section order, topbar, footnote registry, sidebar tests"
    - path: "output/TEST/worksheet.html"
      provides: "Reference HTML output for human review"
  key_links:
    - from: "tests/stages/render/test_html_layout.py"
      to: "src/do_uw/stages/render/html_renderer.py build_html_context()"
      via: "test fixtures calling build_html_context with minimal AnalysisState"
      pattern: "build_html_context"
    - from: "tests/stages/render/test_html_layout.py"
      to: "FootnoteRegistry"
      via: "test_footnote_registry_deduplication"
      pattern: "FootnoteRegistry"
---

<objective>
Create the automated test suite that validates all Phase 43 layout requirements, run a full AAPL render to confirm the complete worksheet, and complete a human visual review checkpoint. Also perform any gap-closure fixes identified during the end-to-end render (missing section IDs, CSS tweaks, sidebar link mismatches).

Purpose: Phase 43 is a presentation overhaul — the quality gate is both automated (correct HTML structure) and human (does it look institutionally credible?). This plan provides both gates. The test file serves as a regression shield for future phases.

Output: test_html_layout.py with 8+ tests covering all layout requirements. Rendered AAPL worksheet for human approval. Any gap fixes from the integrated render.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-html-presentation-quality-capiq-layout-data-tracing/43-CONTEXT.md
@.planning/phases/43-html-presentation-quality-capiq-layout-data-tracing/43-RESEARCH.md
@src/do_uw/stages/render/html_renderer.py
@tests/stages/render/test_html_renderer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_html_layout.py with automated structure verification tests</name>
  <files>tests/stages/render/test_html_layout.py</files>
  <action>
    Create `tests/stages/render/test_html_layout.py` with the following tests. Each test uses a minimal AnalysisState fixture (reuse existing fixtures from test_html_renderer.py where possible) and renders the HTML template, then parses with Python's html.parser or string search to verify structure.

    **Tests to implement:**

    1. `test_two_column_layout()` — Render HTML, assert "worksheet-layout" class present in output HTML string. Assert "sidebar-toc" class present.

    2. `test_sidebar_toc_links()` — Render HTML, assert all 9 required anchor hrefs are present: `#identity`, `#executive-summary`, `#red-flags`, `#scoring`, `#financial-health`, `#market`, `#governance`, `#litigation`, `#sources`.

    3. `test_topbar_identity_only()` — Render HTML. Assert "sticky-topbar" div is present. Assert "sticky-topbar-tier" class is NOT present (scores/tier removed). Assert "sticky-topbar-metric" class is NOT present. Assert `sticky-topbar-company` class is present.

    4. `test_section_order()` — Render HTML. Find positions of section ids in the HTML string: `id="identity"`, `id="executive-summary"`, `id="red-flags"`, `id="scoring"`, `id="financial-health"`, `id="market"` (NOT `id="market-trading"` — fixed in Plan 03), `id="governance"`, `id="litigation"`. Assert positions are in ascending order (correct document flow). Also assert `id="market-trading"` does NOT appear (ensures the anchor rename was applied).

    5. `test_red_flags_section_standalone()` — Render HTML with state fixture that includes at least 1 triggered check result (mock or real). Assert `<section id="red-flags"` present in output. Assert it appears BEFORE `<section id="scoring"`.

    6. `test_red_flags_empty_state()` — Render HTML with state fixture that has no red flags. Assert `<section id="red-flags"` present but contains "No triggered" text.

    7. `test_footnote_registry_deduplication()` — Unit test (no HTML rendering). Instantiate FootnoteRegistry directly. Call register("10-K 2024") twice. Assert both calls return the same number. Call register("DEF 14A 2024"). Assert returns 2. Assert len(registry) == 2. Assert all_sources == [(1, "10-K 2024"), (2, "DEF 14A 2024")].

    8. `test_sources_appendix_renders()` — Render HTML with a state fixture that has at least 1 filing document. Assert `<section id="sources"` present. Assert at least one `<li id="fn-` element in output.

    9. `test_sidebar_print_css()` — Read `styles.css` file content. Assert "@media print" block present. Assert "sidebar-toc" appears inside the @media print block with "display: none".

    10. `test_data_row_macro_in_render()` — Render HTML with real AAPL state or a state fixture with financial data. Assert "dr-label" class and "dr-value" class and "dr-context" class appear in output (confirms data_row macro is used).

    **Implementation pattern:**
    ```python
    import pytest
    from pathlib import Path
    from do_uw.stages.render.html_renderer import build_html_context, render_html_pdf
    from do_uw.models.state import AnalysisState
    # Import FootnoteRegistry from wherever it was placed (html_footnotes or html_renderer)

    @pytest.fixture
    def minimal_state() -> AnalysisState:
        """Minimal state for layout structure tests."""
        # Reuse or adapt from test_html_renderer.py fixtures
        ...

    def _render_html_string(state: AnalysisState) -> str:
        """Render HTML template to string without writing to disk."""
        from do_uw.stages.render.html_renderer import _render_html_template
        context = build_html_context(state, chart_dir=None)
        return _render_html_template(context)
    ```

    Check if `_render_html_template` is the right internal function name in html_renderer.py. If there's a different render-to-string function, use that instead. If none exists, render to a temp file and read it back.

    All tests must be independent (no shared state between tests). Use type hints on all functions. Run ruff on the test file before considering complete.
  </action>
  <verify>
    <automated>uv run pytest tests/stages/render/test_html_layout.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <done>test_html_layout.py has 10 tests. All 10 pass. Tests cover: two-column layout, sidebar links, topbar identity-only, section order, red flags standalone section, footnote registry unit tests, sources appendix, print CSS, data_row macro usage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human visual review of AAPL HTML worksheet</name>
  <action>Generate AAPL HTML output and present for human review: run `uv run do-uw analyze AAPL --output-dir output/AAPL --formats html` then open the result for inspection.</action>
  <what-built>
    Complete Phase 43 HTML presentation overhaul:
    - Two-column layout: sticky sidebar TOC (180px left) + main content (right)
    - Sidebar TOC with 9 section links, active section highlighted as you scroll
    - Sticky topbar: company name, ticker, sector, market cap — no scores/tier
    - Section order: Identity → Exec Summary → Red Flags → Scoring → Financial → Market → Governance → Litigation → Sources
    - Red Flags as dedicated section (priority-sorted triggered/elevated table)
    - 3-column data grid (Label | Value | Context) in financial and market key metrics
    - Footnote/Sources infrastructure (numbered sources appendix)
    - 10 automated layout tests passing
    - No file exceeds 500 lines
    - All pre-existing tests pass
  </what-built>
  <how-to-verify>
    Generate a fresh AAPL worksheet and open the HTML in your browser:

    ```bash
    uv run do-uw analyze AAPL --output-dir output/AAPL --formats html
    open output/AAPL/worksheet.html
    ```

    Check the following in the browser:

    1. **Sidebar**: Two-column layout visible — sidebar on left with 9 section links. Scroll down through the document and confirm the active section highlights in the sidebar.

    2. **Top bar**: Shows "Apple Inc. (AAPL) | Technology | $3.8T" (or similar) — NO score numbers, NO tier badge, NO "Standard Terms" or action text.

    3. **Section order**: Scroll down. Confirm order is: Identity block → Executive Summary → Red Flags → Scoring → Financial → Market → Governance → Litigation → Sources.

    4. **Red Flags section**: Confirm Red Flags appears immediately after Executive Summary. If AAPL has triggered flags, they should show in a table with Severity | Flag | Finding | Source columns.

    5. **Financial metrics**: Confirm the key financial metrics show as a 3-column grid (Label | Value | Context/benchmark). Labels in bold navy, values in center column, context/peer data in right column.

    6. **Market metrics**: Confirm stock performance metrics show the 3-column grid.

    7. **Sources appendix**: Scroll to the bottom. Confirm a numbered Sources list appears (e.g., "1. 10-K (SEC EDGAR), filed 2024-02-23").

    8. **Print view**: Open browser print preview (Cmd+P on Mac). Confirm sidebar is hidden, layout is single-column, content is readable.

    9. **Overall credibility**: Does this look like a professional institutional document (S&P CapIQ quality), or does it still look like a web page? The goal is dense, data-rich, institutionally credible.
  </how-to-verify>
  <resume-signal>Type "approved" if the worksheet meets institutional quality standards, or describe specific issues to fix (e.g., "sidebar not visible", "Red Flags section missing", "financial grid missing context column")</resume-signal>
</task>

</tasks>

<verification>
Before checkpoint:
```bash
uv run pytest tests/stages/render/ -x -q
uv run do-uw analyze AAPL --output-dir output/AAPL --formats html
```
All tests pass. AAPL HTML generated for visual review.
</verification>

<success_criteria>
- test_html_layout.py: 10 tests, all passing
- All pre-existing render tests still pass
- Full test suite green: `uv run pytest tests/stages/render/ -x -q`
- AAPL HTML rendered without errors
- Human approves the rendered worksheet as institutionally credible
- Known gaps documented in SUMMARY.md for any items not fully addressed
</success_criteria>

<output>
After completion, create `.planning/phases/43-html-presentation-quality-capiq-layout-data-tracing/43-05-SUMMARY.md`
</output>
