---
phase: 41-peril-organized-scoring-golden-html-output
plan: 03
type: execute
wave: 2
depends_on: ["41-01", "41-02"]
files_modified:
  - src/do_uw/templates/html/sections/scoring.html.j2
  - src/do_uw/stages/render/html_renderer.py
autonomous: false
requirements: []

must_haves:
  truths:
    - "HTML output for a real ticker contains peril assessment table, F/S badges, and all quarterly updates"
    - "HTML is the golden (primary) output format — all scoring content renders correctly"
    - "No regressions in Word or PDF output formats"
    - "Peril assessment complements (not replaces) existing plaintiff lens peril map"
  artifacts:
    - path: "src/do_uw/templates/html/sections/scoring.html.j2"
      provides: "Complete scoring section with peril assessment + F/S badges + plaintiff peril map"
    - path: "output/"
      provides: "Rendered HTML/Word output for verification"
  key_links:
    - from: "src/do_uw/stages/render/html_renderer.py"
      to: "src/do_uw/stages/render/md_renderer.py"
      via: "build_html_context inherits build_template_context"
      pattern: "build_template_context"
---

<objective>
Verify end-to-end HTML golden output quality for a real ticker, ensuring peril assessment, F/S annotations, and all quarterly updates render correctly.

Purpose: Plans 01 and 02 built the plumbing and template changes. This plan validates everything works together on real data, identifies any remaining rendering gaps, and polishes the HTML output to golden standard. This is the quality gate — no claiming "done" without seeing real rendered output.

Output: Verified HTML output with all new features visible, any fix-ups applied, human review checkpoint.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/41-peril-organized-scoring-golden-html-output/41-RESEARCH.md
@.planning/phases/41-peril-organized-scoring-golden-html-output/41-01-SUMMARY.md
@.planning/phases/41-peril-organized-scoring-golden-html-output/41-02-SUMMARY.md

Key files:
@src/do_uw/templates/html/sections/scoring.html.j2
@src/do_uw/templates/html/sections/financial.html.j2
@src/do_uw/stages/render/html_renderer.py
@src/do_uw/stages/render/md_renderer_helpers_scoring.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end rendering validation and fix-ups</name>
  <files>
    src/do_uw/templates/html/sections/scoring.html.j2
    src/do_uw/stages/render/html_renderer.py
  </files>
  <action>
**Step 1: Run pipeline on existing state data.**
Look for existing `state.json` files in `output/` directories from prior pipeline runs. If one exists (e.g., `output/WWD-*/state.json` or `output/AAPL-*/state.json`), use it to re-render output without needing a full pipeline run:

```bash
# Check for existing state files
ls output/*/state.json 2>/dev/null

# If state exists, reset render status and re-render
# Or directly use the render stage:
uv run python -c "
from do_uw.models.state import AnalysisState
from pathlib import Path
import json

# Load most recent state
state_files = sorted(Path('output').glob('*/state.json'))
if state_files:
    state = AnalysisState.model_validate_json(state_files[-1].read_text())
    print(f'Loaded state for {state.ticker}')
    print(f'Check results: {len(state.analysis.check_results) if state.analysis else 0}')
    print(f'Scoring: {state.scoring is not None}')
"
```

If no state file exists, generate sample output using the render test fixture pattern or run `uv run python -m do_uw analyze WWD --skip-acquire` (if prior cached data exists).

**Step 2: Render HTML and inspect.**
Re-render the HTML output from the loaded state. Check the generated HTML file for:
- [ ] D&O Claim Peril Assessment section appears (h3 heading, table with 8 perils)
- [ ] Active perils have deep dive boxes with trigger/amplifier/mitigator details
- [ ] 10-Factor Scoring table shows F/S role badges next to each factor name
- [ ] Role badges use correct colors (blue F, orange S, purple F+S)
- [ ] Quarterly updates section shows all valid quarters (not just first)
- [ ] Trend summary table appears if 2+ quarters rendered
- [ ] Existing plaintiff lens peril map still renders (lines 642-684 of scoring.html.j2)
- [ ] No JavaScript errors, broken HTML, or visual artifacts

**Step 3: Fix any rendering issues found.**
Common issues to watch for:
- `peril_scoring` empty because brain.duckdb not rebuilt → run `brain build` first
- Factor `role` key missing → verify yaml loading in extract_scoring
- Template syntax errors from Jinja2 namespace pattern for quarterly filtering
- CSS class mismatches between Tailwind v4 compiled.css and template classes

Apply targeted fixes to template or context code. Do NOT change scoring logic.

**Step 4: Verify full test suite still passes.**
```bash
uv run python -m pytest tests/ -x --no-header -q
```
Fix any regressions introduced by Plans 01 or 02.

**Step 5: Cross-format check.**
Verify Word output still renders correctly — the Word renderer (`sect7_scoring.py` → `sect7_scoring_perils.py`) already has its own peril rendering. Confirm no conflicts.
  </action>
  <verify>
    <automated>uv run python -m pytest tests/ -x --no-header -q</automated>
    <manual>Open rendered HTML file in browser, verify peril assessment, F/S badges, and quarterly sections are visible and properly styled</manual>
  </verify>
  <done>HTML output contains peril assessment table, F/S role badges, all quarterly updates with trend summary, no visual artifacts, all tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of HTML golden output</name>
  <files>output/</files>
  <action>
Human reviews rendered HTML output in browser. Automated steps already completed in Task 1.

What was built:
- Peril-organized scoring section in HTML output with F/S role badges on 10-factor table
- Multi-quarter rendering with trend analysis
- Complementary to existing plaintiff lens peril map

How to verify:
1. Open the rendered HTML file from output/ directory in a web browser
2. Scroll to Section 7: Scoring and Risk Assessment
3. Verify D&O Claim Peril Assessment section appears with a table of 8 perils showing risk levels and active chains
4. Verify active perils have colored deep-dive boxes showing trigger checks, amplifiers, and mitigators
5. Verify 10-Factor Scoring table has small colored badges (F/S/F+S) next to each factor name
6. Scroll to Financial section and verify multiple quarterly updates render (if data available)
7. Verify trend summary table appears at bottom of quarterly section
8. Verify existing Peril Map (plaintiff lens) section still renders lower in the scoring section
9. Overall: Does the scoring section now tell a coherent story organized by risk type?
  </action>
  <verify>Human opens HTML in browser and confirms all features render correctly</verify>
  <done>Human approves output quality — type "approved" or describe issues that need fixing</done>
</task>

</tasks>

<verification>
1. HTML file renders in browser without errors
2. All 3 new features visible: peril assessment, F/S badges, multi-quarter
3. No regressions in existing sections
4. `uv run python -m pytest tests/ -x --no-header -q` passes
5. Human visual review confirms professional quality
</verification>

<success_criteria>
- HTML golden output contains all Phase 41 features on real ticker data
- Peril assessment and plaintiff lens peril map BOTH appear (complementary views)
- F/S role badges render with correct colors on all 10 factors
- Multiple quarterly updates render with trend summary
- Human reviewer approves output quality
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/41-peril-organized-scoring-golden-html-output/41-03-SUMMARY.md`
</output>
