---
phase: 45-codebase-cleanup-architecture-hardening
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/stages/extract/regulatory_extract.py
  - src/do_uw/stages/extract/regulatory_extract_sec.py
  - src/do_uw/stages/extract/regulatory_extract_agencies.py
  - src/do_uw/stages/score/factor_data.py
  - src/do_uw/stages/score/factor_data_market.py
  - src/do_uw/stages/acquire/clients/sec_client.py
  - src/do_uw/stages/acquire/clients/sec_client_filing.py
  - src/do_uw/stages/analyze/financial_formulas.py
  - src/do_uw/stages/analyze/financial_formulas_distress.py
  - src/do_uw/cli_knowledge.py
  - src/do_uw/cli_knowledge_checks.py
  - src/do_uw/validation/qa_report.py
  - src/do_uw/validation/qa_report_generator.py
autonomous: true
requirements:
  - ARCH-05

must_haves:
  truths:
    - "No file in the score, acquire, analyze, cli, or validation subsystems exceeds 500 lines"
    - "All callers of moved functions import from the new module path directly — no re-export shims"
    - "The original oversized files that became pure split targets are deleted"
    - "pytest passes with 0 regressions after splits"
    - "AAPL pipeline completes without errors after all splits"
  artifacts:
    - path: "src/do_uw/stages/extract/regulatory_extract_sec.py"
      provides: "SEC-specific regulatory extraction (split from regulatory_extract.py)"
    - path: "src/do_uw/stages/extract/regulatory_extract_agencies.py"
      provides: "Non-SEC agency extraction (split from regulatory_extract.py)"
    - path: "src/do_uw/stages/score/factor_data_market.py"
      provides: "Market/trading factor data helpers (split from factor_data.py)"
    - path: "src/do_uw/stages/acquire/clients/sec_client_filing.py"
      provides: "Filing fetch/parse helpers (split from sec_client.py)"
    - path: "src/do_uw/stages/analyze/financial_formulas_distress.py"
      provides: "Distress model formulas (split from financial_formulas.py)"
    - path: "src/do_uw/cli_knowledge_checks.py"
      provides: "Knowledge check commands (split from cli_knowledge.py)"
    - path: "src/do_uw/validation/qa_report_generator.py"
      provides: "Report generation logic (split from qa_report.py)"
  key_links:
    - from: "all callers of regulatory_extract"
      to: "regulatory_extract_sec.py or regulatory_extract_agencies.py"
      via: "direct import updated by grep-find-replace across caller files"
      pattern: "from do_uw.stages.extract.regulatory_extract_sec import|from do_uw.stages.extract.regulatory_extract_agencies import"
    - from: "all callers of financial_formulas distress functions"
      to: "financial_formulas_distress.py"
      via: "direct import; financial_formulas.py trimmed to import distress module"
      pattern: "from do_uw.stages.analyze.financial_formulas_distress import"
    - from: "all callers of qa_report generator functions"
      to: "qa_report_generator.py"
      via: "direct import; qa_report.py trimmed to import generator module"
      pattern: "from do_uw.validation.qa_report_generator import"
---

<objective>
Split 6 more files that violate the 500-line rule across the extract, score, acquire, analyze, CLI, and validation subsystems. Delete originals or trim in-place; no re-export shims — all callers updated directly.

Files to split:
- `regulatory_extract.py` (521 lines) → SEC vs non-SEC agency split; original deleted
- `factor_data.py` (514 lines) → market/trading factors split out; original trimmed in-place
- `sec_client.py` (511 lines) → filing fetch helpers split out; original trimmed in-place
- `financial_formulas.py` (509 lines) → distress models split out; original trimmed in-place
- `cli_knowledge.py` (505 lines) → check commands split out; original trimmed in-place
- `qa_report.py` (503 lines) → report generation split out; original trimmed in-place

Purpose: Same as Plan 05 — CLAUDE.md's 500-line rule. CONTEXT.md decision: no re-export shims. After splitting, originals that become pure dispatch hubs are deleted and callers are updated directly. For orchestration files (where the original still has distinct content), trim in-place.

Output: 7 new focused files; 6 original files reduced below 500 lines; zero re-export shims.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split regulatory_extract.py, factor_data.py, sec_client.py</name>
  <files>
    src/do_uw/stages/extract/regulatory_extract.py
    src/do_uw/stages/extract/regulatory_extract_sec.py
    src/do_uw/stages/extract/regulatory_extract_agencies.py
    src/do_uw/stages/score/factor_data.py
    src/do_uw/stages/score/factor_data_market.py
    src/do_uw/stages/acquire/clients/sec_client.py
    src/do_uw/stages/acquire/clients/sec_client_filing.py
  </files>
  <action>
    Read all three original files fully before splitting.

    **Split regulatory_extract.py (521 lines):**
    Strategy: Split by agency type (SEC vs other).
    - Create `regulatory_extract_sec.py` with SEC-specific extraction functions (SEC enforcement actions, AAER database, SEC comment letters, SEC investigation detection)
    - Create `regulatory_extract_agencies.py` with non-SEC agency functions (CFTC, DOJ, FTC, state regulators, OSHA, EPA patterns)
    - Each new file gets a module docstring explaining its domain
    - grep for all callers of `regulatory_extract`:
      ```
      grep -r "from do_uw.stages.extract.regulatory_extract import\|from do_uw.stages.extract import regulatory_extract" src/ tests/ --include="*.py" -l
      ```
    - Update each caller to import from `regulatory_extract_sec` or `regulatory_extract_agencies` depending on which functions it uses
    - If regulatory_extract.py still has meaningful orchestration content after the split (calls both sec and agencies sub-modules), keep it trimmed to < 500 lines with explicit imports from the split files (NOT `import *`). If it becomes a pure dispatch stub with no unique logic, delete it and ensure all callers import from the split files directly.

    **Split factor_data.py (514 lines):**
    Strategy: Split by factor group.
    - Identify market/trading data functions: functions that fetch or format short interest, stock performance, insider trading factor data
    - Move them to `factor_data_market.py` with module docstring: "Market and trading factor data helpers. Split from factor_data.py (Phase 45, 500-line rule)."
    - In `factor_data.py`, add explicit imports (NOT `import *`):
      ```python
      from do_uw.stages.score.factor_data_market import (
          # list each moved function name
      )
      ```
    - grep for direct callers of moved functions and update those that import from factor_data directly to import from factor_data_market instead
    - Target: factor_data.py < 420 lines; factor_data_market.py < 150 lines

    **Split sec_client.py (511 lines):**
    Strategy: Split filing fetching from identity resolution.
    - Filing fetch functions: functions that retrieve actual filing documents (10-K, 10-Q, DEF14A, 8-K fetchers)
    - Move filing fetch functions to `sec_client_filing.py` with module docstring: "Filing fetch helpers for SEC EDGAR. Split from sec_client.py (Phase 45, 500-line rule)."
    - In `sec_client.py`, add explicit imports (NOT `import *`):
      ```python
      from do_uw.stages.acquire.clients.sec_client_filing import (
          # list each moved function name
      )
      ```
    - grep for direct callers of moved functions and update those that import from sec_client directly to import from sec_client_filing instead
    - The `SECClient` class (if any) stays in sec_client.py; filing-specific functions move

    After all three splits, verify line counts:
    ```
    wc -l src/do_uw/stages/extract/regulatory_extract*.py src/do_uw/stages/score/factor_data*.py src/do_uw/stages/acquire/clients/sec_client*.py
    ```
    All must be <= 500 lines (regulatory_extract.py may not exist if deleted).

    Verify no `from ... import *` patterns introduced:
    ```
    grep -r "from.*import \*" src/do_uw/stages/extract/regulatory_extract*.py src/do_uw/stages/score/factor_data*.py src/do_uw/stages/acquire/clients/sec_client*.py 2>/dev/null
    ```
    Must be empty.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && python -c "import do_uw.stages.score.factor_data; import do_uw.stages.acquire.clients.sec_client; print('split imports OK')" && wc -l src/do_uw/stages/score/factor_data.py src/do_uw/stages/acquire/clients/sec_client.py src/do_uw/stages/score/factor_data_market.py src/do_uw/stages/acquire/clients/sec_client_filing.py && grep -r "from.*import \*" src/do_uw/stages/score/factor_data*.py src/do_uw/stages/acquire/clients/sec_client*.py 2>/dev/null | wc -l</automated>
    <manual>All files <= 500 lines; no `import *` patterns; original files either deleted or trimmed</manual>
  </verify>
  <done>regulatory_extract_sec.py, regulatory_extract_agencies.py, factor_data_market.py, sec_client_filing.py created; all originals <= 500 lines; no re-export shims; callers updated with direct imports</done>
</task>

<task type="auto">
  <name>Task 2: Split financial_formulas.py, cli_knowledge.py, qa_report.py</name>
  <files>
    src/do_uw/stages/analyze/financial_formulas.py
    src/do_uw/stages/analyze/financial_formulas_distress.py
    src/do_uw/cli_knowledge.py
    src/do_uw/cli_knowledge_checks.py
    src/do_uw/validation/qa_report.py
    src/do_uw/validation/qa_report_generator.py
  </files>
  <action>
    Read all three files fully before splitting.

    **Split financial_formulas.py (509 lines):**
    Strategy: Split distress models from other formulas.
    - Distress model functions: Altman Z-Score, Beneish M-Score, Ohlson O-Score, Piotroski F-Score calculation functions
    - Move distress model functions to `financial_formulas_distress.py` with module docstring: "Financial distress scoring formulas (Altman Z-Score, Beneish M-Score, etc.). Split from financial_formulas.py (Phase 45, 500-line rule)."
    - In `financial_formulas.py`, replace the moved function bodies with explicit named imports (NOT `import *`):
      ```python
      from do_uw.stages.analyze.financial_formulas_distress import (
          altman_z_score,  # list each moved function name explicitly
          beneish_m_score,
          # etc.
      )
      ```
    - grep for direct callers of the moved distress functions:
      ```
      grep -r "from do_uw.stages.analyze.financial_formulas import.*altman\|from do_uw.stages.analyze.financial_formulas import.*beneish\|from do_uw.stages.analyze.financial_formulas import.*ohlson\|from do_uw.stages.analyze.financial_formulas import.*piotroski" src/ tests/ --include="*.py" -l
      ```
      Update those callers to import from `financial_formulas_distress` directly.
    - Target: financial_formulas.py < 400 lines; financial_formulas_distress.py < 200 lines

    **Split cli_knowledge.py (505 lines):**
    Strategy: Split by command group.
    - Identify check-related commands: commands for listing checks, inspecting checks, running checks, check statistics
    - Move check command functions to `cli_knowledge_checks.py` with its own `checks_app = typer.Typer(name="checks", help="...")`
    - In `cli_knowledge.py`, register `checks_app` as a sub-command group (if Typer supports this) or keep the @app.command decorators in cli_knowledge.py and move only the helper/utility functions to cli_knowledge_checks.py
    - Do NOT use `import *` from cli_knowledge_checks. Use explicit named imports.
    - grep for callers of any functions moved to cli_knowledge_checks and update them
    - Target: cli_knowledge.py < 400 lines

    **Split qa_report.py (503 lines):**
    Strategy: Split report generation from validation logic.
    - Validation logic: functions that check individual data points, run assertion checks, compute completeness metrics
    - Report generation: functions that format and print the final QA report table/summary
    - Move report generation to `qa_report_generator.py` with module docstring: "QA report formatting and output generation. Split from qa_report.py (Phase 45, 500-line rule)."
    - In `qa_report.py`, add explicit named imports (NOT `import *`):
      ```python
      from do_uw.validation.qa_report_generator import (
          generate_qa_report,  # list each moved function name
          # etc.
      )
      ```
    - grep for direct callers of moved report-generation functions and update those callers to import from `qa_report_generator` directly
    - Target: qa_report.py < 400 lines; qa_report_generator.py < 150 lines

    After splits, verify:
    ```
    wc -l src/do_uw/stages/analyze/financial_formulas*.py src/do_uw/cli_knowledge*.py src/do_uw/validation/qa_report*.py
    ```
    All must be <= 500 lines.

    Verify no re-export shims introduced:
    ```
    grep -r "from.*import \*" src/do_uw/stages/analyze/financial_formulas*.py src/do_uw/cli_knowledge*.py src/do_uw/validation/qa_report*.py 2>/dev/null
    ```
    Must be empty.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && python -c "import do_uw.stages.analyze.financial_formulas; import do_uw.cli_knowledge; import do_uw.validation.qa_report; print('all imports OK')" && wc -l src/do_uw/stages/analyze/financial_formulas.py src/do_uw/cli_knowledge.py src/do_uw/validation/qa_report.py && grep -r "from.*import \*" src/do_uw/stages/analyze/financial_formulas*.py src/do_uw/cli_knowledge*.py src/do_uw/validation/qa_report*.py 2>/dev/null | wc -l</automated>
    <manual>All three original files must be <= 500 lines; no `import *` patterns anywhere in split files</manual>
  </verify>
  <done>financial_formulas_distress.py, cli_knowledge_checks.py, qa_report_generator.py exist; all originals <= 500 lines; no re-export shims; callers updated with direct imports</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite, verify 500-line compliance, run AAPL pipeline</name>
  <files></files>
  <action>
    Run the full test suite:
    ```
    cd /Users/gorlin/Projects/do-uw/do-uw
    uv run pytest tests/ -x -q 2>&1 | tail -15
    ```

    Then run a comprehensive 500-line compliance check across all source files:
    ```
    find src/do_uw -name "*.py" -not -path "*/__pycache__/*" | while read f; do
        lines=$(wc -l < "$f")
        if [ "$lines" -gt 500 ]; then
            echo "VIOLATION: $f ($lines lines)"
        fi
    done
    ```
    This should return no violations. If any violations remain, fix them before moving on.

    Also verify no `from ... import *` re-export shims were accidentally introduced in any of the split files from Plans 05 or 06:
    ```
    grep -r "from.*import \*" src/do_uw/stages/render/md_renderer_helpers*.py src/do_uw/stages/render/md_renderer_helpers_financial*.py src/do_uw/stages/extract/regulatory_extract*.py src/do_uw/stages/score/factor_data*.py src/do_uw/stages/acquire/clients/sec_client*.py src/do_uw/stages/analyze/financial_formulas*.py src/do_uw/cli_knowledge*.py src/do_uw/validation/qa_report*.py 2>/dev/null
    ```
    Must return empty.

    Run the full AAPL pipeline to confirm end-to-end operation:
    ```
    uv run do-uw run AAPL
    ```
    Pipeline must complete without errors and produce an output file in output/AAPL/.
    ```
    ls output/AAPL/ | tail -3
    ```

    Note: The three files at exactly 500 lines (peer_group.py, check_mappers.py, brain_loader.py) are borderline — check if any edits in Plans 05-06 pushed them over.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run pytest tests/ -x -q 2>&1 | tail -5 && find src/do_uw -name "*.py" -not -path "*/__pycache__/*" -exec wc -l {} + | awk '$1 > 500 {print "VIOLATION:", $2, "("$1" lines)"}' | grep -v "total" && uv run do-uw run AAPL 2>&1 | tail -5 && ls output/AAPL/ | tail -3</automated>
    <manual>No VIOLATION lines should appear; all tests should pass; AAPL pipeline completes; output file generated</manual>
  </verify>
  <done>All tests pass; zero files in src/do_uw exceed 500 lines; no re-export shims in any split file; AAPL pipeline completes and output file is generated</done>
</task>

</tasks>

<verification>
```
cd /Users/gorlin/Projects/do-uw/do-uw
# Verify no 500-line violations
find src/do_uw -name "*.py" -not -path "*/__pycache__/*" -exec wc -l {} + | awk '$1 > 500 {print "VIOLATION:", $2, "("$1" lines)"}' | grep -v "total"
# Verify no re-export shims in split files
grep -r "from.*import \*" src/do_uw/stages/render/md_renderer_helpers*.py src/do_uw/stages/analyze/financial_formulas*.py src/do_uw/validation/qa_report*.py 2>/dev/null && echo "SHIM DETECTED" || echo "No shims OK"
# Run tests
uv run pytest tests/ -q 2>&1 | tail -5
# Run AAPL pipeline
uv run do-uw run AAPL 2>&1 | tail -5
ls output/AAPL/ | tail -3
```
</verification>

<success_criteria>
- All 6 original files are <= 500 lines
- 7 new split files exist and are importable
- Zero `from ... import *` re-export stubs anywhere in the split files
- Zero files in src/do_uw exceed 500 lines (full compliance)
- All callers updated with direct named imports to new module paths
- All tests pass
- AAPL pipeline completes without errors and output file is generated
</success_criteria>

<output>
After completion, create `.planning/phases/45-codebase-cleanup-architecture-hardening/45-06-SUMMARY.md`
</output>
