---
phase: 45-codebase-cleanup-architecture-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/stages/analyze/check_mappers_phase26.py
  - src/do_uw/stages/analyze/check_mappers_analytical.py
  - src/do_uw/stages/analyze/check_mappers_fwrd.py
  - src/do_uw/stages/analyze/check_mappers_forward.py
  - src/do_uw/stages/score/red_flag_gates_phase26.py
  - src/do_uw/stages/score/red_flag_gates_enhanced.py
  - src/do_uw/stages/analyze/check_mappers.py
  - src/do_uw/stages/score/red_flag_gates.py
  - src/do_uw/stages/analyze/check_field_routing.py
autonomous: true
requirements:
  - ARCH-04

must_haves:
  truths:
    - "No file in the codebase is named with a phase number (check_mappers_phase26, red_flag_gates_phase26, check_mappers_fwrd)"
    - "All imports of renamed files resolve without ImportError"
    - "All 359 checks execute without error after the rename"
    - "pytest passes with 0 regressions"
  artifacts:
    - path: "src/do_uw/stages/analyze/check_mappers_analytical.py"
      provides: "Analytical engine check mapper (renamed from check_mappers_phase26.py)"
    - path: "src/do_uw/stages/analyze/check_mappers_forward.py"
      provides: "Forward-looking check mapper (renamed from check_mappers_fwrd.py)"
    - path: "src/do_uw/stages/score/red_flag_gates_enhanced.py"
      provides: "Enhanced red flag gate logic (renamed from red_flag_gates_phase26.py)"
  key_links:
    - from: "src/do_uw/stages/analyze/check_mappers.py"
      to: "src/do_uw/stages/analyze/check_mappers_analytical.py"
      via: "lazy import inside map_phase26_check function"
      pattern: "from do_uw.stages.analyze.check_mappers_analytical import"
    - from: "src/do_uw/stages/analyze/check_mappers_analytical.py"
      to: "src/do_uw/stages/analyze/check_mappers_forward.py"
      via: "lazy import inside map_analytical_check function"
      pattern: "from do_uw.stages.analyze.check_mappers_forward import"
    - from: "src/do_uw/stages/score/red_flag_gates.py"
      to: "src/do_uw/stages/score/red_flag_gates_enhanced.py"
      via: "lazy import at line 52"
      pattern: "from do_uw.stages.score.red_flag_gates_enhanced import"
---

<objective>
Rename 3 phase-numbered production files to descriptive permanent names, and update all import sites.

Purpose: Phase-numbered names (`check_mappers_phase26.py`, `red_flag_gates_phase26.py`, `check_mappers_fwrd.py`) signal "temporary cleanup pending" to every developer. These are permanent production logic files. Renaming enforces CLAUDE.md architectural clarity and eliminates ongoing confusion.

Output: 3 new files with descriptive names; 3 old files removed; all import sites updated; tests pass.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename check mapper files</name>
  <files>
    src/do_uw/stages/analyze/check_mappers_analytical.py
    src/do_uw/stages/analyze/check_mappers_forward.py
    src/do_uw/stages/analyze/check_mappers.py
    src/do_uw/stages/analyze/check_field_routing.py
  </files>
  <action>
    Perform three renames and update all import sites:

    **Step 1: Copy and delete check_mappers_phase26.py**
    - Copy `src/do_uw/stages/analyze/check_mappers_phase26.py` to `src/do_uw/stages/analyze/check_mappers_analytical.py`
    - In the new file, update the module docstring: replace any mention of "phase26" or "Phase 26" with "analytical engine checks"
    - Update the internal lazy import at line 55: change `from do_uw.stages.analyze.check_mappers_fwrd import map_fwrd_check` to `from do_uw.stages.analyze.check_mappers_forward import map_fwrd_check` (keep the imported name `map_fwrd_check` for now since check_mappers_forward.py will export it)
    - Delete `src/do_uw/stages/analyze/check_mappers_phase26.py`

    **Step 2: Copy and delete check_mappers_fwrd.py**
    - Copy `src/do_uw/stages/analyze/check_mappers_fwrd.py` to `src/do_uw/stages/analyze/check_mappers_forward.py`
    - In the new file, update the module docstring: replace "Split from check_mappers_phase26.py" with "Split from check_mappers_analytical.py"
    - Delete `src/do_uw/stages/analyze/check_mappers_fwrd.py`

    **Step 3: Update check_mappers.py (line 115)**
    - In `src/do_uw/stages/analyze/check_mappers.py`, find the lazy import: `from do_uw.stages.analyze.check_mappers_phase26 import map_phase26_check`
    - Change it to: `from do_uw.stages.analyze.check_mappers_analytical import map_phase26_check`
    - The imported function name `map_phase26_check` stays the same for now (renaming the function itself is out of scope for this plan)

    **Step 4: Update check_field_routing.py**
    - In `src/do_uw/stages/analyze/check_field_routing.py`, find the comment `# handled by check_mappers_phase26.py and don't need entries.`
    - Update to: `# handled by check_mappers_analytical.py and don't need entries.`

    **Do NOT rename the function `map_phase26_check` itself** — that would require tracing callers across the entire check mapper chain and is out of scope. Only the file names change.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && python -c "from do_uw.stages.analyze.check_mappers import map_check; print('import OK')"</automated>
    <manual>Confirm old files are gone: ls src/do_uw/stages/analyze/check_mappers_phase26.py should give "No such file"</manual>
  </verify>
  <done>check_mappers_analytical.py and check_mappers_forward.py exist; check_mappers_phase26.py and check_mappers_fwrd.py are deleted; check_mappers.py imports from check_mappers_analytical</done>
</task>

<task type="auto">
  <name>Task 2: Rename red_flag_gates_phase26.py and update callers</name>
  <files>
    src/do_uw/stages/score/red_flag_gates_enhanced.py
    src/do_uw/stages/score/red_flag_gates.py
  </files>
  <action>
    **Step 1: Copy and delete red_flag_gates_phase26.py**
    - Copy `src/do_uw/stages/score/red_flag_gates_phase26.py` to `src/do_uw/stages/score/red_flag_gates_enhanced.py`
    - In the new file, update the module docstring: replace any mention of "phase26" or "Phase 26" with "enhanced red flag gate logic"
    - Delete `src/do_uw/stages/score/red_flag_gates_phase26.py`

    **Step 2: Update red_flag_gates.py (line 52)**
    - In `src/do_uw/stages/score/red_flag_gates.py`, find the lazy import block at line 52:
      ```python
      from do_uw.stages.score.red_flag_gates_phase26 import (
      ```
    - Change the module path to:
      ```python
      from do_uw.stages.score.red_flag_gates_enhanced import (
      ```
    - Keep all imported names exactly as they are. Do NOT rename the imported functions.

    Then run `uv run pytest tests/stages/score/ -x -q` to verify no regressions.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && python -c "from do_uw.stages.score.red_flag_gates import evaluate_critical_risk_factors; print('import OK')" && uv run pytest tests/stages/score/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>red_flag_gates_enhanced.py exists; red_flag_gates_phase26.py is deleted; red_flag_gates.py imports from red_flag_gates_enhanced; score stage tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and verify no regressions</name>
  <files></files>
  <action>
    Run the full test suite to confirm all renames are clean:
    ```
    uv run pytest tests/ -x -q 2>&1 | tail -20
    ```

    If any test fails due to the renames, trace the import path and fix the missing reference. Common failure patterns:
    - A test file directly importing from the old module path → update that test's import
    - A `__pycache__` artifact confusing Python → run `find . -type d -name __pycache__ | xargs rm -rf` then rerun

    Also verify no stale references remain:
    ```
    grep -r "check_mappers_phase26\|check_mappers_fwrd\|red_flag_gates_phase26" src/ tests/ --include="*.py"
    ```
    This grep must return empty output (no matches).
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run pytest tests/ -x -q 2>&1 | tail -10 && grep -r "check_mappers_phase26\|check_mappers_fwrd\|red_flag_gates_phase26" src/ tests/ --include="*.py" | grep -v "CONCERNS.md" | wc -l</automated>
    <manual>The grep count must be 0</manual>
  </verify>
  <done>All tests pass; zero occurrences of phase-numbered module names in src/ and tests/; AAPL pipeline completes and output file is generated</done>
</task>

<task type="auto">
  <name>Task 4: Run AAPL pipeline to confirm end-to-end operation</name>
  <files></files>
  <action>
    Run the full AAPL pipeline to confirm the renames did not break any runtime path:
    ```
    cd /Users/gorlin/Projects/do-uw/do-uw
    uv run do-uw run AAPL
    ```
    Pipeline must complete without errors.
    ```
    ls output/AAPL/ | tail -3
    ```
    Confirm an output file was generated.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run do-uw run AAPL 2>&1 | tail -5 && ls output/AAPL/ | tail -3</automated>
    <manual>Pipeline completes without errors; output directory contains new run</manual>
  </verify>
  <done>AAPL pipeline completes without errors; output file is generated in output/AAPL/</done>
</task>

</tasks>

<verification>
Run after all tasks complete:
```
cd /Users/gorlin/Projects/do-uw/do-uw
uv run pytest tests/ -q 2>&1 | tail -5
grep -r "check_mappers_phase26\|check_mappers_fwrd\|red_flag_gates_phase26" src/ tests/ --include="*.py" | wc -l
ls src/do_uw/stages/analyze/check_mappers_analytical.py src/do_uw/stages/analyze/check_mappers_forward.py src/do_uw/stages/score/red_flag_gates_enhanced.py
uv run do-uw run AAPL 2>&1 | tail -5
ls output/AAPL/ | tail -3
```
Expected: tests pass, grep returns 0, all three new files exist, pipeline completes.
</verification>

<success_criteria>
- check_mappers_analytical.py, check_mappers_forward.py, red_flag_gates_enhanced.py all exist
- check_mappers_phase26.py, check_mappers_fwrd.py, red_flag_gates_phase26.py are deleted
- Zero occurrences of old names in src/ or tests/ Python files
- All existing tests pass (0 regressions)
- AAPL pipeline completes without errors and output file is generated
</success_criteria>

<output>
After completion, create `.planning/phases/45-codebase-cleanup-architecture-hardening/45-01-SUMMARY.md`
</output>
