---
phase: 45-codebase-cleanup-architecture-hardening
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/knowledge/compat_loader.py
  - src/do_uw/knowledge/__init__.py
  - src/do_uw/stages/analyze/__init__.py
  - src/do_uw/stages/score/__init__.py
  - src/do_uw/stages/benchmark/__init__.py
  - src/do_uw/stages/render/sections/sect7_coverage_gaps.py
  - src/do_uw/cli_knowledge_traceability.py
autonomous: true
requirements:
  - ARCH-04

must_haves:
  truths:
    - "No Python file contains the string 'BackwardCompatLoader' as a class name or import"
    - "All four pipeline stages (ANALYZE, SCORE, BENCHMARK, RENDER) still load brain data correctly"
    - "BrainKnowledgeLoader is the name used everywhere the loader is instantiated"
    - "pytest passes with 0 regressions"
  artifacts:
    - path: "src/do_uw/knowledge/compat_loader.py"
      provides: "BrainKnowledgeLoader class (renamed from BackwardCompatLoader)"
      contains: "class BrainKnowledgeLoader"
  key_links:
    - from: "src/do_uw/stages/analyze/__init__.py"
      to: "src/do_uw/knowledge/compat_loader.py"
      via: "from do_uw.knowledge.compat_loader import BrainKnowledgeLoader"
      pattern: "BrainKnowledgeLoader"
    - from: "src/do_uw/knowledge/__init__.py"
      to: "src/do_uw/knowledge/compat_loader.py"
      via: "public re-export in __all__"
      pattern: "BrainKnowledgeLoader"
---

<objective>
Rename `BackwardCompatLoader` to `BrainKnowledgeLoader` everywhere — class definition, all import sites, docstrings, and comments.

Purpose: The name "BackwardCompatLoader" signals "will be removed soon" to every reader. It is actually the primary production loader for ANALYZE, SCORE, BENCHMARK, and RENDER. The misleading name causes Claude instances and human developers to assume it is temporary and bypass it when adding new functionality, creating fragmentation. ARCH-04 requires each component name to reflect its actual role.

Output: `compat_loader.py` defines `BrainKnowledgeLoader`; all 6 callers updated; `knowledge/__init__.py` exports the new name; backward-compat alias kept for 1 cycle if needed.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename class in compat_loader.py and add backward-compat alias</name>
  <files>
    src/do_uw/knowledge/compat_loader.py
  </files>
  <action>
    Read the full file `src/do_uw/knowledge/compat_loader.py`.

    Make these changes:
    1. Rename class `BackwardCompatLoader` to `BrainKnowledgeLoader` (the class definition line: `class BackwardCompatLoader:` → `class BrainKnowledgeLoader:`)
    2. Update the module docstring to say "BrainKnowledgeLoader" instead of "BackwardCompatLoader"
    3. Update all inline comments that say "BackwardCompatLoader" → "BrainKnowledgeLoader"
    4. Update the usage example in the docstring: `from do_uw.knowledge.compat_loader import BrainKnowledgeLoader`
    5. At the very END of the file, after the class definition, add a backward-compat alias:
       ```python
       # Backward-compat alias — remove after Phase 45 callers are updated
       BackwardCompatLoader = BrainKnowledgeLoader
       ```
       This ensures any cached `.pyc` files or missed import sites don't immediately break.
    6. Any log messages inside the class that say "BackwardCompatLoader" → update to "BrainKnowledgeLoader"

    Do NOT change the file path — keep it as `compat_loader.py` (renaming the file is unnecessary and would require more import updates).
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && python -c "from do_uw.knowledge.compat_loader import BrainKnowledgeLoader; print('BrainKnowledgeLoader import OK'); from do_uw.knowledge.compat_loader import BackwardCompatLoader; print('alias OK')"</automated>
  </verify>
  <done>compat_loader.py defines BrainKnowledgeLoader; BackwardCompatLoader alias exists; both import without error</done>
</task>

<task type="auto">
  <name>Task 2: Update all 6 caller sites to use BrainKnowledgeLoader</name>
  <files>
    src/do_uw/knowledge/__init__.py
    src/do_uw/stages/analyze/__init__.py
    src/do_uw/stages/score/__init__.py
    src/do_uw/stages/benchmark/__init__.py
    src/do_uw/stages/render/sections/sect7_coverage_gaps.py
    src/do_uw/cli_knowledge_traceability.py
  </files>
  <action>
    Update each file to use `BrainKnowledgeLoader` instead of `BackwardCompatLoader`:

    **src/do_uw/knowledge/__init__.py**
    - Change `from do_uw.knowledge.compat_loader import (BackwardCompatLoader, ...)` to include `BrainKnowledgeLoader`
    - Update `__all__` list: add `"BrainKnowledgeLoader"` and keep `"BackwardCompatLoader"` for the transition cycle
    - Update the module-level comment that says "Compat: BackwardCompatLoader" → "Loader: BrainKnowledgeLoader"

    **src/do_uw/stages/analyze/__init__.py** (line 19, line 353)
    - Line 19: `from do_uw.knowledge.compat_loader import BackwardCompatLoader` → `from do_uw.knowledge.compat_loader import BrainKnowledgeLoader`
    - Line 353: `loader = BackwardCompatLoader(playbook_id=playbook_id)` → `loader = BrainKnowledgeLoader(playbook_id=playbook_id)`
    - Update the comments at lines 338-339 that reference BackwardCompatLoader → BrainKnowledgeLoader

    **src/do_uw/stages/score/__init__.py** (line 20, line 228)
    - Line 20: update import
    - Line 228: `loader = BackwardCompatLoader()` → `loader = BrainKnowledgeLoader()`

    **src/do_uw/stages/benchmark/__init__.py** (line 15, line 135)
    - Line 15: update import
    - Line 135: `loader = BackwardCompatLoader()` → `loader = BrainKnowledgeLoader()`

    **src/do_uw/stages/render/sections/sect7_coverage_gaps.py** (lines 54, 56)
    - Line 54: `from do_uw.knowledge.compat_loader import BackwardCompatLoader` → `... import BrainKnowledgeLoader`
    - Line 56: `loader = BackwardCompatLoader()` → `loader = BrainKnowledgeLoader()`

    **src/do_uw/cli_knowledge_traceability.py** (lines 174, 178)
    - Line 174: update import
    - Line 178: `loader = BackwardCompatLoader()` → `loader = BrainKnowledgeLoader()`

    After all edits, verify with:
    ```
    grep -r "BackwardCompatLoader" src/ --include="*.py" | grep -v "compat_loader.py" | grep -v "alias"
    ```
    This should return empty (the alias line in compat_loader.py is expected; no other file should use the old name directly).
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && python -c "from do_uw.stages.analyze import AnalyzeStage; from do_uw.stages.score import ScoreStage; from do_uw.stages.benchmark import BenchmarkStage; print('all stage imports OK')" && grep -r "BackwardCompatLoader" src/ --include="*.py" | grep -v "compat_loader.py" | wc -l</automated>
    <manual>grep count must be 0 (only the alias in compat_loader.py uses the old name)</manual>
  </verify>
  <done>All 6 caller sites updated; zero non-alias occurrences of BackwardCompatLoader in src/; all stage imports resolve</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite to verify rename is clean</name>
  <files></files>
  <action>
    Run:
    ```
    cd /Users/gorlin/Projects/do-uw/do-uw
    uv run pytest tests/ -x -q 2>&1 | tail -15
    ```

    If tests fail due to BackwardCompatLoader references in test files, update those test imports too. The backward-compat alias in compat_loader.py means existing test code using the old name will still work, but update them anyway for consistency.

    Also check tests/ for any direct BackwardCompatLoader usage:
    ```
    grep -r "BackwardCompatLoader" tests/ --include="*.py"
    ```
    Update any found to BrainKnowledgeLoader.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass; BrainKnowledgeLoader is the canonical name in all non-alias locations</done>
</task>

<task type="auto">
  <name>Task 4: Run AAPL pipeline to confirm end-to-end operation</name>
  <files></files>
  <action>
    Run the full AAPL pipeline to confirm the rename did not break any runtime loader path:
    ```
    cd /Users/gorlin/Projects/do-uw/do-uw
    uv run do-uw run AAPL
    ```
    Pipeline must complete without errors.
    ```
    ls output/AAPL/ | tail -3
    ```
    Confirm an output file was generated.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run do-uw run AAPL 2>&1 | tail -5 && ls output/AAPL/ | tail -3</automated>
    <manual>Pipeline completes without errors; output directory contains new run</manual>
  </verify>
  <done>AAPL pipeline completes without errors; output file is generated in output/AAPL/</done>
</task>

</tasks>

<verification>
```
cd /Users/gorlin/Projects/do-uw/do-uw
uv run pytest tests/ -q 2>&1 | tail -5
python -c "from do_uw.knowledge.compat_loader import BrainKnowledgeLoader; loader = BrainKnowledgeLoader(); print('loader instantiation OK')"
grep -rn "class BrainKnowledgeLoader" src/do_uw/knowledge/compat_loader.py
uv run do-uw run AAPL 2>&1 | tail -5
ls output/AAPL/ | tail -3
```
</verification>

<success_criteria>
- `class BrainKnowledgeLoader` is defined in compat_loader.py
- BackwardCompatLoader exists only as an alias in compat_loader.py
- All stage imports use BrainKnowledgeLoader
- All tests pass
- AAPL pipeline completes without errors and output file is generated
</success_criteria>

<output>
After completion, create `.planning/phases/45-codebase-cleanup-architecture-hardening/45-02-SUMMARY.md`
</output>
