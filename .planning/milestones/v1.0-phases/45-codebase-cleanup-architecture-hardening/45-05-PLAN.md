---
phase: 45-codebase-cleanup-architecture-hardening
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/stages/render/md_renderer_helpers.py
  - src/do_uw/stages/render/md_renderer_helpers_narrative.py
  - src/do_uw/stages/render/md_renderer_helpers_tables.py
  - src/do_uw/stages/render/md_renderer_helpers_financial.py
  - src/do_uw/stages/render/md_renderer_helpers_financial_income.py
  - src/do_uw/stages/render/md_renderer_helpers_financial_balance.py
  - src/do_uw/stages/extract/company_profile.py
  - src/do_uw/stages/extract/company_profile_items.py
  - src/do_uw/stages/extract/earnings_guidance.py
  - src/do_uw/stages/extract/earnings_guidance_classify.py
autonomous: true
requirements:
  - ARCH-05

must_haves:
  truths:
    - "No render helper file exceeds 500 lines"
    - "No extract file in this plan exceeds 500 lines"
    - "All existing callers of moved functions import from the new module path directly"
    - "The original oversized files are deleted — no re-export shims left behind"
    - "pytest passes with 0 regressions after splits"
  artifacts:
    - path: "src/do_uw/stages/render/md_renderer_helpers_narrative.py"
      provides: "Narrative/prose helper functions extracted from md_renderer_helpers.py"
    - path: "src/do_uw/stages/render/md_renderer_helpers_tables.py"
      provides: "Table/list rendering helpers extracted from md_renderer_helpers.py"
    - path: "src/do_uw/stages/render/md_renderer_helpers_financial_income.py"
      provides: "Income statement helpers extracted from md_renderer_helpers_financial.py"
    - path: "src/do_uw/stages/render/md_renderer_helpers_financial_balance.py"
      provides: "Balance sheet helpers extracted from md_renderer_helpers_financial.py"
    - path: "src/do_uw/stages/extract/company_profile_items.py"
      provides: "Item-level parsing extracted from company_profile.py"
    - path: "src/do_uw/stages/extract/earnings_guidance_classify.py"
      provides: "Classification logic extracted from earnings_guidance.py"
  key_links:
    - from: "all callers of md_renderer_helpers"
      to: "src/do_uw/stages/render/md_renderer_helpers_narrative.py or md_renderer_helpers_tables.py"
      via: "direct import updated by grep-find-replace across all caller files"
      pattern: "from do_uw.stages.render.md_renderer_helpers_narrative import|from do_uw.stages.render.md_renderer_helpers_tables import"
    - from: "all callers of md_renderer_helpers_financial"
      to: "src/do_uw/stages/render/md_renderer_helpers_financial_income.py or md_renderer_helpers_financial_balance.py"
      via: "direct import updated by grep-find-replace across all caller files"
      pattern: "from do_uw.stages.render.md_renderer_helpers_financial_income import|from do_uw.stages.render.md_renderer_helpers_financial_balance import"
    - from: "src/do_uw/stages/extract/company_profile.py"
      to: "src/do_uw/stages/extract/company_profile_items.py"
      via: "direct import at top of company_profile.py for functions it still calls"
      pattern: "from do_uw.stages.extract.company_profile_items import"
---

<objective>
Split 4 files that violate the 500-line rule in the render and extract subsystems. Delete the originals after splitting — no re-export shims.

Files to split:
- `md_renderer_helpers.py` (590 lines) → narrative helpers + table helpers; original DELETED
- `md_renderer_helpers_financial.py` (528 lines) → income statement + balance sheet helpers; original DELETED
- `company_profile.py` (583 lines) → item parsing split out; trimmed in-place (not deleted)
- `earnings_guidance.py` (533 lines) → classification split out; trimmed in-place (not deleted)

Purpose: CLAUDE.md's 500-line rule exists to prevent context rot. Files over 500 lines force Claude to read more context per task, degrading quality. Each split produces a focused, purpose-named file. CONTEXT.md locks: no re-export shims — originals are deleted and all callers are updated directly.

Output: 6 new focused files; 2 original render files deleted; 2 extract files reduced below 500 lines in-place; all callers updated with direct imports.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split md_renderer_helpers.py (590 lines) — delete original, update all callers</name>
  <files>
    src/do_uw/stages/render/md_renderer_helpers_narrative.py
    src/do_uw/stages/render/md_renderer_helpers_tables.py
  </files>
  <action>
    Read `md_renderer_helpers.py` fully to understand its structure before splitting.

    **Step 1: Identify function groups**
    - Narrative functions: any function that builds prose descriptions, bullet summaries, or analytical narratives
    - Table functions: any function that builds markdown tables, lists, headers, or structural elements

    **Step 2: Create `md_renderer_helpers_narrative.py`**
    - Module docstring: "Narrative and prose helpers for the Markdown renderer. Split from md_renderer_helpers.py (Phase 45, 500-line rule)."
    - Copy only the imports needed by narrative functions
    - Copy the narrative functions verbatim
    - Target: < 350 lines

    **Step 3: Create `md_renderer_helpers_tables.py`**
    - Module docstring: "Table and structural helpers for the Markdown renderer. Split from md_renderer_helpers.py (Phase 45, 500-line rule)."
    - Copy only the imports needed by table functions
    - Copy the table functions verbatim
    - Target: < 350 lines

    **Step 4: Find all callers of the original file**
    ```
    grep -r "from do_uw.stages.render.md_renderer_helpers import\|from do_uw.stages.render import md_renderer_helpers\|import do_uw.stages.render.md_renderer_helpers" src/ tests/ --include="*.py" -l
    ```
    For each caller file found, read it and update each import statement to point to the correct new module (`md_renderer_helpers_narrative` or `md_renderer_helpers_tables`) based on which functions it uses.

    **Step 5: Delete the original file**
    ```
    rm src/do_uw/stages/render/md_renderer_helpers.py
    ```
    There must be NO re-export shim left at this path. Callers must import from the split files directly.

    Verify line counts after splitting:
    ```
    wc -l src/do_uw/stages/render/md_renderer_helpers_narrative.py src/do_uw/stages/render/md_renderer_helpers_tables.py
    ```
    Both must be <= 500 lines. The original file must not exist.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && python -c "import do_uw.stages.render.md_renderer_helpers_narrative; import do_uw.stages.render.md_renderer_helpers_tables; print('split render helper imports OK')" && test ! -f src/do_uw/stages/render/md_renderer_helpers.py && echo "original deleted OK" && wc -l src/do_uw/stages/render/md_renderer_helpers_narrative.py src/do_uw/stages/render/md_renderer_helpers_tables.py</automated>
    <manual>Original md_renderer_helpers.py must not exist; both split files must be <= 500 lines</manual>
  </verify>
  <done>md_renderer_helpers_narrative.py and md_renderer_helpers_tables.py exist; md_renderer_helpers.py is deleted; all callers updated with direct imports to split files</done>
</task>

<task type="auto">
  <name>Task 2: Split md_renderer_helpers_financial.py (528 lines) — delete original, update all callers</name>
  <files>
    src/do_uw/stages/render/md_renderer_helpers_financial_income.py
    src/do_uw/stages/render/md_renderer_helpers_financial_balance.py
  </files>
  <action>
    Read `md_renderer_helpers_financial.py` fully before splitting.

    **Step 1: Identify function groups**
    - Income statement functions: revenue, gross profit, operating income, EBIT, net income formatters
    - Balance sheet functions: assets, liabilities, equity formatters

    **Step 2: Create `md_renderer_helpers_financial_income.py`**
    - Module docstring: "Income statement helpers for the Markdown renderer. Split from md_renderer_helpers_financial.py (Phase 45, 500-line rule)."
    - Copy income statement functions with their required imports
    - Target: < 300 lines

    **Step 3: Create `md_renderer_helpers_financial_balance.py`**
    - Module docstring: "Balance sheet helpers for the Markdown renderer. Split from md_renderer_helpers_financial.py (Phase 45, 500-line rule)."
    - Copy balance sheet functions with their required imports
    - Target: < 300 lines

    **Step 4: Find all callers of the original file**
    ```
    grep -r "from do_uw.stages.render.md_renderer_helpers_financial import\|from do_uw.stages.render import md_renderer_helpers_financial\|import do_uw.stages.render.md_renderer_helpers_financial" src/ tests/ --include="*.py" -l
    ```
    For each caller file found, read it and update each import to point to the correct new module based on which functions it uses.

    **Step 5: Delete the original file**
    ```
    rm src/do_uw/stages/render/md_renderer_helpers_financial.py
    ```
    No re-export shim. Callers must import from split files directly.

    Verify:
    ```
    wc -l src/do_uw/stages/render/md_renderer_helpers_financial_income.py src/do_uw/stages/render/md_renderer_helpers_financial_balance.py
    ```
    Both must be <= 500 lines.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && python -c "import do_uw.stages.render.md_renderer_helpers_financial_income; import do_uw.stages.render.md_renderer_helpers_financial_balance; print('financial split imports OK')" && test ! -f src/do_uw/stages/render/md_renderer_helpers_financial.py && echo "original deleted OK" && wc -l src/do_uw/stages/render/md_renderer_helpers_financial_income.py src/do_uw/stages/render/md_renderer_helpers_financial_balance.py</automated>
    <manual>Original md_renderer_helpers_financial.py must not exist; both split files must be <= 500 lines</manual>
  </verify>
  <done>md_renderer_helpers_financial_income.py and md_renderer_helpers_financial_balance.py exist; md_renderer_helpers_financial.py is deleted; all callers updated with direct imports</done>
</task>

<task type="auto">
  <name>Task 3: Split company_profile.py and earnings_guidance.py in-place, verify line counts</name>
  <files>
    src/do_uw/stages/extract/company_profile.py
    src/do_uw/stages/extract/company_profile_items.py
    src/do_uw/stages/extract/earnings_guidance.py
    src/do_uw/stages/extract/earnings_guidance_classify.py
  </files>
  <action>
    Read both files fully before splitting.

    **Splitting company_profile.py (583 lines):**

    Strategy: Extract item-level parsing functions (functions like `_parse_item_1_*`, `_extract_business_*`, `_parse_sic_*`, individual paragraph parsers) to `company_profile_items.py`. Keep profile assembly logic (the function that calls item parsers and builds the CompanyProfile object) in `company_profile.py`.

    Step 1: Create `company_profile_items.py` with:
    - Module docstring: "Item-level parsing functions for company profile extraction. Split from company_profile.py (Phase 45, 500-line rule)."
    - Move the item-parsing functions and their required imports

    Step 2: In `company_profile.py`, replace the moved function bodies with an import at the top:
    ```python
    from do_uw.stages.extract.company_profile_items import (
        # list the moved function names explicitly
    )
    ```
    Note: company_profile.py is NOT deleted — it stays as the orchestration file, just trimmed.

    Also grep for any external callers of the moved functions:
    ```
    grep -r "from do_uw.stages.extract.company_profile import" src/ tests/ --include="*.py" -l
    ```
    If any caller imports a function that was moved to company_profile_items.py, update that caller to import from company_profile_items directly.

    Target: company_profile.py < 400 lines; company_profile_items.py < 300 lines.

    **Splitting earnings_guidance.py (533 lines):**

    Strategy: Extract classification logic (beat/miss/inline classification, magnitude calculation, quarter categorization) to `earnings_guidance_classify.py`.

    Step 1: Create `earnings_guidance_classify.py` with:
    - Module docstring: "Earnings guidance beat/miss classification logic. Split from earnings_guidance.py (Phase 45, 500-line rule)."
    - Move classification functions and their required imports

    Step 2: In `earnings_guidance.py`, replace the moved function bodies with an import at the top:
    ```python
    from do_uw.stages.extract.earnings_guidance_classify import (
        # list the moved function names explicitly
    )
    ```
    earnings_guidance.py is NOT deleted — it stays as the orchestration file.

    Also grep for external callers of moved functions and update any that import from earnings_guidance directly.

    After all splits, verify line counts:
    ```
    wc -l src/do_uw/stages/extract/company_profile.py src/do_uw/stages/extract/company_profile_items.py src/do_uw/stages/extract/earnings_guidance.py src/do_uw/stages/extract/earnings_guidance_classify.py
    ```
    All must be <= 500 lines.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && python -c "import do_uw.stages.extract.company_profile; import do_uw.stages.extract.earnings_guidance; print('extract imports OK')" && wc -l src/do_uw/stages/extract/company_profile.py src/do_uw/stages/extract/company_profile_items.py src/do_uw/stages/extract/earnings_guidance.py src/do_uw/stages/extract/earnings_guidance_classify.py</automated>
    <manual>All four files must be <= 500 lines; both new split files must exist</manual>
  </verify>
  <done>company_profile_items.py and earnings_guidance_classify.py exist; both original extract files are below 500 lines; no re-export shims anywhere; all callers updated with direct imports</done>
</task>

<task type="auto">
  <name>Task 4: Run 500-line compliance scan, full test suite, and AAPL pipeline</name>
  <files></files>
  <action>
    Run a 500-line compliance check across the render and extract directories to catch any sibling files that also need splitting:
    ```
    find src/do_uw/stages/render src/do_uw/stages/extract -name "*.py" | xargs wc -l | awk '$1 > 500 {print "VIOLATION:", $2, "("$1" lines)"}'
    ```
    No violations should appear. If any are found, split them before proceeding.

    Run the full test suite to confirm no regressions from any of the splits in Tasks 1-3:
    ```
    cd /Users/gorlin/Projects/do-uw/do-uw
    uv run pytest tests/ -x -q 2>&1 | tail -15
    ```

    Then run the full AAPL pipeline to confirm end-to-end output:
    ```
    uv run do-uw run AAPL
    ```
    Pipeline must complete without errors and produce an output file in output/AAPL/.
    ```
    ls output/AAPL/ | tail -3
    ```
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && find src/do_uw/stages/render src/do_uw/stages/extract -name "*.py" | xargs wc -l | awk '$1 > 500 {print "VIOLATION:", $2}' && uv run pytest tests/ -x -q 2>&1 | tail -5 && uv run do-uw run AAPL 2>&1 | tail -5 && ls output/AAPL/ | tail -3</automated>
    <manual>No 500-line violations; tests pass; pipeline completes; output directory contains new run</manual>
  </verify>
  <done>No 500-line violations in render or extract directories; all tests pass; AAPL pipeline completes and output file is generated</done>
</task>

</tasks>

<verification>
```
cd /Users/gorlin/Projects/do-uw/do-uw
# Confirm original oversized files are deleted (render pair) or trimmed (extract pair)
test ! -f src/do_uw/stages/render/md_renderer_helpers.py && echo "md_renderer_helpers.py deleted OK"
test ! -f src/do_uw/stages/render/md_renderer_helpers_financial.py && echo "md_renderer_helpers_financial.py deleted OK"
# Check all split files are under 500 lines
find src/do_uw/stages/render src/do_uw/stages/extract -name "*.py" | xargs wc -l | awk '$1 > 500 {print "VIOLATION:", $2}'
# Confirm no re-export shims exist (no "from ... import *" in new split files)
grep -r "from.*import \*" src/do_uw/stages/render/md_renderer_helpers_*.py src/do_uw/stages/render/md_renderer_helpers_financial_*.py 2>/dev/null && echo "SHIM DETECTED" || echo "No re-export shims OK"
# Run tests
uv run pytest tests/ -q 2>&1 | tail -5
# Run full AAPL pipeline
uv run do-uw run AAPL 2>&1 | tail -5
ls output/AAPL/ | tail -3
```
</verification>

<success_criteria>
- 6 new split files created in render and extract subsystems
- md_renderer_helpers.py and md_renderer_helpers_financial.py are DELETED (not shimmed)
- company_profile.py and earnings_guidance.py are trimmed below 500 lines
- No `from ... import *` re-export stubs anywhere in the split files
- All callers updated with direct imports to the new module paths
- All tests pass
- AAPL pipeline completes without errors and output file is generated
</success_criteria>

<output>
After completion, create `.planning/phases/45-codebase-cleanup-architecture-hardening/45-05-SUMMARY.md`
</output>
