---
phase: 01-foundation-domain-knowledge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/do_uw/__init__.py
  - src/do_uw/py.typed
  - src/do_uw/models/__init__.py
  - src/do_uw/stages/__init__.py
  - src/do_uw/stages/resolve/__init__.py
  - src/do_uw/stages/acquire/__init__.py
  - src/do_uw/stages/extract/__init__.py
  - src/do_uw/stages/analyze/__init__.py
  - src/do_uw/stages/score/__init__.py
  - src/do_uw/stages/benchmark/__init__.py
  - src/do_uw/stages/render/__init__.py
  - src/do_uw/config/__init__.py
  - src/do_uw/cache/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/models/__init__.py
  - tests/config/__init__.py
  - scripts/check_file_lengths.py
  - .gitignore
  - ruff.toml
autonomous: true

must_haves:
  truths:
    - "Running `uv run do-uw --help` prints CLI help without import errors"
    - "Running `uv run ruff check src/` exits cleanly with no errors"
    - "Running `uv run pyright src/` exits cleanly with no errors"
    - "Running `uv run pytest` discovers the test directory and exits (0 tests collected is OK)"
    - "Running `uv run python scripts/check_file_lengths.py` exits 0 (no files over 500 lines)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package definition with all dependencies, CLI entry point, tool config"
      contains: "do-uw"
    - path: "src/do_uw/__init__.py"
      provides: "Package root with version"
      contains: "__version__"
    - path: "scripts/check_file_lengths.py"
      provides: "ARCH-05 enforcement script"
      contains: "MAX_LINES = 500"
    - path: "tests/conftest.py"
      provides: "Shared test fixtures"
  key_links:
    - from: "pyproject.toml"
      to: "src/do_uw/cli.py"
      via: "[project.scripts] entry point"
      pattern: 'do-uw.*=.*"do_uw.cli:app"'
---

<objective>
Create the Python package skeleton with uv, all dependencies, directory structure, dev tooling (ruff, pyright, pytest), and the ARCH-05 file-length enforcement script -- so that every subsequent plan has a working, linted, type-checked package to build on.

Purpose: Establishes the project foundation that all other Phase 1 plans depend on. Without a working package structure and toolchain, nothing else can proceed.
Output: A `do-uw` Python package installable via `uv`, with all directories from the CLAUDE.md file structure, dev tooling configured, and a file-length CI check script.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-domain-knowledge/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize uv project with all dependencies and directory structure</name>
  <files>
    pyproject.toml
    uv.lock
    src/do_uw/__init__.py
    src/do_uw/py.typed
    src/do_uw/models/__init__.py
    src/do_uw/stages/__init__.py
    src/do_uw/stages/resolve/__init__.py
    src/do_uw/stages/acquire/__init__.py
    src/do_uw/stages/extract/__init__.py
    src/do_uw/stages/analyze/__init__.py
    src/do_uw/stages/score/__init__.py
    src/do_uw/stages/benchmark/__init__.py
    src/do_uw/stages/render/__init__.py
    src/do_uw/config/__init__.py
    src/do_uw/cache/__init__.py
    src/do_uw/cli.py
    tests/__init__.py
    tests/conftest.py
    tests/models/__init__.py
    tests/config/__init__.py
    .gitignore
  </files>
  <action>
    1. Run `uv init --name do-uw --python 3.12 --package --lib` in the project root to create the project. If this creates a flat layout, restructure to use `src/` layout.

    2. Edit `pyproject.toml` to match this specification exactly:

    ```toml
    [project]
    name = "do-uw"
    version = "0.1.0"
    description = "D&O Liability Underwriting Worksheet System"
    requires-python = ">=3.12"
    dependencies = [
        "pydantic>=2.10",
        "typer>=0.15",
        "rich>=13.0",
        "httpx>=0.28",
    ]

    [project.scripts]
    do-uw = "do_uw.cli:app"

    [build-system]
    requires = ["hatchling"]
    build-backend = "hatchling.build"

    [tool.hatch.build.targets.wheel]
    packages = ["src/do_uw"]

    [tool.pytest.ini_options]
    testpaths = ["tests"]
    pythonpath = ["src"]
    ```

    3. Add dev dependencies: `uv add --dev ruff pyright pytest pytest-asyncio`

    4. Create the full directory tree. Every `__init__.py` should be minimal:
       - `src/do_uw/__init__.py`: Contains `__version__ = "0.1.0"` and a module docstring.
       - `src/do_uw/py.typed`: Empty marker file for PEP 561 type checking support.
       - All stage `__init__.py` files: Empty (just a docstring with the stage name).
       - `src/do_uw/models/__init__.py`: Empty (docstring only).
       - `src/do_uw/config/__init__.py`: Empty (docstring only).
       - `src/do_uw/cache/__init__.py`: Empty (docstring only).

    5. Create a minimal `src/do_uw/cli.py` with just a Typer app so the entry point resolves:
       ```python
       """CLI entry point for do-uw."""
       import typer

       app = typer.Typer(name="do-uw", help="D&O Liability Underwriting Worksheet")

       @app.command()
       def analyze(
           ticker: str = typer.Argument(help="Stock ticker symbol (e.g., AAPL)"),
           verbose: bool = typer.Option(False, "--verbose", "-v", help="Show detailed output"),
       ) -> None:
           """Analyze a company for D&O underwriting."""
           # Placeholder -- pipeline integration in plan 01-03
           typer.echo(f"Analyzing {ticker.upper()}...")
       ```

    6. Create `tests/conftest.py` with a `tmp_path` fixture alias and a comment noting that shared fixtures go here. Create empty `tests/__init__.py`, `tests/models/__init__.py`, `tests/config/__init__.py`.

    7. Create `.gitignore` with standard Python entries: `__pycache__/`, `*.pyc`, `.venv/`, `dist/`, `*.egg-info/`, `.ruff_cache/`, `.pytest_cache/`, `.pyright/`, `*.db`, `.cache/`, `.DS_Store`. Also add `src/do_uw/brain/` with a comment explaining that large JSON knowledge files are tracked separately or copied at build time.

    8. Run `uv sync` to generate `uv.lock` and install all dependencies.

    IMPORTANT: Do NOT use `uv init` if it would overwrite existing project files. If the project root already has a `pyproject.toml`, edit it directly instead.
    IMPORTANT: Use `src/` layout (not flat layout) per CLAUDE.md file structure.
    IMPORTANT: Do NOT add the `brain/` directory to `.gitignore` -- the JSON knowledge files WILL be committed. The `.gitignore` entry should only cover the cache database files.
  </action>
  <verify>
    Run all of these and confirm exit code 0:
    ```bash
    uv sync
    uv run do-uw --help
    uv run python -c "from do_uw import __version__; print(__version__)"
    ```
  </verify>
  <done>
    - `uv run do-uw --help` prints help text with the `analyze` command
    - `uv run python -c "from do_uw import __version__"` imports without error
    - `uv.lock` exists and is populated
    - All directories from the CLAUDE.md file structure exist with `__init__.py` files
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure dev tooling (ruff, pyright, pytest) and ARCH-05 file-length check</name>
  <files>
    ruff.toml
    pyproject.toml
    scripts/check_file_lengths.py
  </files>
  <action>
    1. Create `ruff.toml` (separate from pyproject.toml for clarity):
       ```toml
       target-version = "py312"
       line-length = 99
       src = ["src"]

       [lint]
       select = [
           "E",    # pycodestyle errors
           "F",    # pyflakes
           "B",    # flake8-bugbear
           "I",    # isort
           "UP",   # pyupgrade
           "S",    # flake8-bandit (security)
           "C4",   # flake8-comprehensions
           "RUF",  # ruff-specific rules
       ]

       [lint.isort]
       known-first-party = ["do_uw"]

       [lint.per-file-ignores]
       "tests/**" = ["S101"]  # Allow assert in tests
       ```

    2. Add pyright config to `pyproject.toml` (append to existing file):
       ```toml
       [tool.pyright]
       pythonVersion = "3.12"
       typeCheckingMode = "strict"
       venvPath = "."
       venv = ".venv"
       ```

    3. Create `scripts/check_file_lengths.py`:
       ```python
       #!/usr/bin/env python3
       """Enforce ARCH-05: No source file over 500 lines.

       Run: uv run python scripts/check_file_lengths.py
       Exit code 0 = pass, 1 = violations found.
       """
       import sys
       from pathlib import Path

       MAX_LINES = 500
       WARN_LINES = 400
       SRC_DIR = Path("src")
       TESTS_DIR = Path("tests")

       def check_file_lengths() -> int:
           failures: list[tuple[Path, int]] = []
           warnings: list[tuple[Path, int]] = []

           for search_dir in [SRC_DIR, TESTS_DIR]:
               if not search_dir.exists():
                   continue
               for py_file in search_dir.rglob("*.py"):
                   line_count = sum(1 for _ in py_file.open())
                   if line_count > MAX_LINES:
                       failures.append((py_file, line_count))
                   elif line_count > WARN_LINES:
                       warnings.append((py_file, line_count))

           for path, count in sorted(warnings):
               print(f"  WARN: {path} has {count} lines (approaching {MAX_LINES} limit)")

           for path, count in sorted(failures):
               print(f"  FAIL: {path} has {count} lines (max {MAX_LINES})")

           if failures:
               print(f"\n{len(failures)} file(s) exceed {MAX_LINES} line limit.")
               return 1

           if warnings:
               print(f"\n{len(warnings)} file(s) approaching limit. All files within {MAX_LINES} lines.")
           else:
               print(f"All files within {MAX_LINES} line limit.")
           return 0

       if __name__ == "__main__":
           sys.exit(check_file_lengths())
       ```

    4. Run ruff, pyright, and the file-length check to verify the skeleton passes all quality gates.

    WHY separate ruff.toml: Keeps pyproject.toml cleaner and ruff config is easy to find/edit. Both approaches work; this is a style choice.
    WHY bandit (S) rules: Security linting catches hardcoded passwords, SQL injection, etc. from day one.
    WHY per-file-ignores for tests: Tests use `assert` statements which bandit flags (S101). This is expected in test code.
  </action>
  <verify>
    ```bash
    uv run ruff check src/ tests/
    uv run ruff format --check src/ tests/
    uv run pyright src/
    uv run python scripts/check_file_lengths.py
    uv run pytest --co  # collect only, confirm discovery works
    ```
    All commands must exit code 0.
  </verify>
  <done>
    - `ruff check` passes with zero errors on all source and test files
    - `ruff format --check` passes (all files formatted)
    - `pyright` passes in strict mode on all source files
    - `scripts/check_file_lengths.py` exits 0 with "All files within 500 line limit"
    - `pytest --co` discovers the tests directory (0 tests collected is OK for now)
  </done>
</task>

</tasks>

<verification>
Run this complete verification sequence:
```bash
# Package installs and imports
uv sync
uv run do-uw --help
uv run python -c "from do_uw import __version__; print(__version__)"

# Quality gates
uv run ruff check src/ tests/
uv run ruff format --check src/ tests/
uv run pyright src/
uv run python scripts/check_file_lengths.py

# Test infrastructure
uv run pytest --co

# Directory structure exists
ls src/do_uw/models/ src/do_uw/stages/ src/do_uw/config/ src/do_uw/cache/
ls src/do_uw/stages/resolve/ src/do_uw/stages/acquire/ src/do_uw/stages/extract/
ls src/do_uw/stages/analyze/ src/do_uw/stages/score/ src/do_uw/stages/benchmark/ src/do_uw/stages/render/
```
</verification>

<success_criteria>
1. `uv run do-uw --help` shows help with the `analyze` command (CORE-03 entry point)
2. All quality gates pass: ruff, pyright strict, file-length check (ARCH-01, ARCH-05)
3. Complete directory structure exists matching CLAUDE.md specification
4. `uv.lock` is generated with pinned dependencies (ARCH-01)
5. Pytest discovers the test directory
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-domain-knowledge/01-01-SUMMARY.md`
</output>
