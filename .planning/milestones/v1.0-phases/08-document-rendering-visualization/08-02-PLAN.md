---
phase: 08-document-rendering-visualization
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/do_uw/stages/render/sections/__init__.py
  - src/do_uw/stages/render/sections/sect1_executive.py
  - src/do_uw/stages/render/sections/sect2_company.py
  - src/do_uw/stages/render/sections/sect3_financial.py
  - src/do_uw/stages/render/sections/sect3_tables.py
  - src/do_uw/stages/render/sections/sect4_market.py
  - src/do_uw/stages/render/charts/stock_charts.py
  - src/do_uw/stages/render/charts/__init__.py
  - tests/test_render_sections_1_4.py
autonomous: true

must_haves:
  truths:
    - "Section 1 (Executive Summary) renders thesis narrative, company snapshot, tier with composite score (prominent but contextual), key findings (top 5 negatives and positives), inherent risk baseline, and claim probability -- narrative-first approach"
    - "Section 2 (Company Profile) renders business description, revenue segments, geographic footprint, subsidiary count, D&O exposure factors, with source citations (OUT-04)"
    - "Section 3 (Financial Health) renders financial statements with conditional formatting (VIS-04): red for deteriorating, blue for improving, amber for caution (NO green). Includes distress scores, debt analysis, audit profile"
    - "Section 4 (Market/Trading) renders stock drop analysis, insider trading, short interest, earnings guidance. Includes 1-year and 5-year stock performance charts (VIS-01) indexed to 100 with red triangle single-day drop markers and orange shaded multi-day bands"
    - "Every section begins with a summary paragraph synthesizing findings (OUT-03)"
    - "Every flagged item includes D&O underwriting context (OUT-05)"
  artifacts:
    - path: "src/do_uw/stages/render/sections/sect1_executive.py"
      provides: "Executive summary section renderer"
      contains: "render_section_1"
    - path: "src/do_uw/stages/render/sections/sect2_company.py"
      provides: "Company profile section renderer"
      contains: "render_section_2"
    - path: "src/do_uw/stages/render/sections/sect3_financial.py"
      provides: "Financial health narrative and distress/debt/audit rendering"
      contains: "render_section_3"
    - path: "src/do_uw/stages/render/sections/sect3_tables.py"
      provides: "Financial statement tables with conditional formatting"
      contains: "render_financial_tables"
    - path: "src/do_uw/stages/render/sections/sect4_market.py"
      provides: "Market and trading section renderer"
      contains: "render_section_4"
    - path: "src/do_uw/stages/render/charts/stock_charts.py"
      provides: "Stock price charts with event markers (VIS-01)"
      contains: "create_stock_performance_chart"
  key_links:
    - from: "src/do_uw/stages/render/word_renderer.py"
      to: "src/do_uw/stages/render/sections/sect1_executive.py"
      via: "render_section_1 import and call"
      pattern: "render_section_1"
    - from: "src/do_uw/stages/render/sections/sect4_market.py"
      to: "src/do_uw/stages/render/charts/stock_charts.py"
      via: "create_stock_performance_chart call"
      pattern: "create_stock_performance_chart"
    - from: "src/do_uw/stages/render/sections/sect3_financial.py"
      to: "src/do_uw/stages/render/docx_helpers.py"
      via: "set_cell_shading for conditional formatting"
      pattern: "set_cell_shading"
---

<objective>
Implement section renderers for Sections 1-4 (Executive Summary, Company Profile, Financial Health, Market/Trading) including the stock performance chart generator (VIS-01) and financial table conditional formatting (VIS-04).

Purpose: These are the first 4 of 7 worksheet sections. Section 1 (Executive Summary) is the most critical page -- it's what the underwriter reads first. Section 3 needs conditional formatting tables. Section 4 needs the stock performance charts per the JACK vs XLY reference design.

Output: Four fully rendered sections in the Word document with charts, tables, conditional formatting, source citations, and D&O context on flagged items.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-document-rendering-visualization/08-CONTEXT.md
@.planning/phases/08-document-rendering-visualization/08-RESEARCH.md
@.planning/phases/08-document-rendering-visualization/08-01-SUMMARY.md
@src/do_uw/models/state.py
@src/do_uw/models/executive_summary.py
@src/do_uw/models/company.py
@src/do_uw/models/financials.py
@src/do_uw/models/market.py
@src/do_uw/models/market_events.py
@src/do_uw/models/scoring.py
@src/do_uw/models/scoring_output.py
@src/do_uw/stages/render/design_system.py
@src/do_uw/stages/render/docx_helpers.py
@src/do_uw/stages/render/chart_helpers.py
@src/do_uw/stages/render/formatters.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section 1 (Executive Summary) and Section 2 (Company Profile) renderers</name>
  <files>
    src/do_uw/stages/render/sections/__init__.py
    src/do_uw/stages/render/sections/sect1_executive.py
    src/do_uw/stages/render/sections/sect2_company.py
  </files>
  <action>
1. Create `sections/__init__.py` with imports re-exporting all render_section_N functions.

2. Create `sect1_executive.py` (~450 lines) -- THE most important page:
   - `render_section_1(doc: Document, state: AnalysisState, ds: DesignSystem) -> None`
   - **Narrative-first approach** (per CONTEXT.md): Lead with the risk story, then data
   - Layout order (all on the first conceptual page):
     a. Section heading "Executive Summary"
     b. **Underwriting Thesis** -- 2-3 sentence narrative from state.executive_summary.thesis.narrative. This is the LEAD. Bold, prominent paragraph style. If thesis is None, use "Analysis incomplete -- thesis not available."
     c. **Company Snapshot** -- small table or key-value layout: ticker, company name, market cap, revenue, employees, industry, exchange. Source from state.executive_summary.snapshot. Use format_currency for financials.
     d. **Tier Classification** -- prominent but contextual (per CONTEXT.md). Show tier name (e.g., "WATCH"), composite score (e.g., "62/100"), and quality score side by side. Use color from risk heat spectrum based on tier. Include action guidance from tier.action. Source: state.scoring.tier
     e. **Inherent Risk Baseline** -- table showing sector base rate, market cap multiplier, adjusted rate, severity ranges (25th/50th/75th/95th). Source: state.executive_summary.inherent_risk. Include methodology_note.
     f. **Key Findings -- Top Negatives** -- numbered list of up to 5 KeyFinding items from state.executive_summary.key_findings.negatives. Each shows: evidence_narrative, section_origin, scoring_impact, theory_mapping. Use red/orange risk indicator colors.
     g. **Key Findings -- Top Positives** -- same format from key_findings.positives. Use blue (moderate) risk colors.
     h. **Claim Probability** (SECT1-05 from state.scoring.claim_probability) -- show band, company-specific rate, industry base rate comparison
     i. **Tower Recommendation** (SECT1-06 from state.scoring.tower_recommendation) -- show tier, position summary, layer assessments
   - Handle None at every level: state.executive_summary, .snapshot, .thesis, .key_findings, state.scoring, etc. Use "Not Available" or "Analysis pending" placeholders.
   - Add source citations on every data point using format_citation from formatters.
   - Add D&O context on flagged items using add_risk_indicator from docx_helpers.

3. Create `sect2_company.py` (~350 lines):
   - `render_section_2(doc: Document, state: AnalysisState, ds: DesignSystem) -> None`
   - Layout:
     a. Section heading "Section 2: Company Profile"
     b. **Summary paragraph** (OUT-03): Synthesize key profile attributes into a narrative paragraph
     c. **Company Identity** table: legal name, ticker, CIK, SIC/NAICS, exchange, incorporation state, FPI status. Source: state.company.identity (SourcedValue fields -- use format_citation)
     d. **Business Description** -- paragraph from state.company.business_description
     e. **Revenue Segments** -- table if state.extracted.financials has segment data
     f. **Geographic Footprint** -- table with state.company.geographic_footprint, subsidiary count, headquarter jurisdiction
     g. **D&O Exposure Factors** -- table from state.company.do_exposure_factors: factor name, level, rationale. Color-code by exposure level using risk spectrum.
   - All data points with source citations from SourcedValue fields.
   - Handle None for state.company, state.extracted, and nested fields.

4. Wire into word_renderer.py: Update the word_renderer to import and call render_section_1 and render_section_2 instead of placeholder paragraphs. Use try/except ImportError as fallback to placeholder text.
  </action>
  <verify>
    cd /Users/gorlin/projects/research && python -c "from do_uw.stages.render.sections.sect1_executive import render_section_1; from do_uw.stages.render.sections.sect2_company import render_section_2; print('OK')" && python -m pyright src/do_uw/stages/render/sections/sect1_executive.py src/do_uw/stages/render/sections/sect2_company.py
  </verify>
  <done>
    Section 1 renders executive summary with thesis narrative first, company snapshot, tier (prominent but contextual), inherent risk, key findings, claim probability, and tower recommendation. Section 2 renders company profile with identity, business description, revenue segments, geography, D&O exposure factors. Both have summary paragraphs (OUT-03), source citations (OUT-04), D&O context (OUT-05). Narrative-first approach honored.
  </done>
</task>

<task type="auto">
  <name>Task 2: Section 3 (Financial Health) with conditional formatting and Section 4 (Market/Trading) with stock charts</name>
  <files>
    src/do_uw/stages/render/sections/sect3_financial.py
    src/do_uw/stages/render/sections/sect3_tables.py
    src/do_uw/stages/render/sections/sect4_market.py
    src/do_uw/stages/render/charts/__init__.py
    src/do_uw/stages/render/charts/stock_charts.py
    tests/test_render_sections_1_4.py
  </files>
  <action>
1. Create `sect3_financial.py` (~400 lines):
   - `render_section_3(doc: Document, state: AnalysisState, ds: DesignSystem) -> None`
   - Layout:
     a. Section heading "Section 3: Financial Health"
     b. **Summary paragraph** (OUT-03): narrative from state.extracted.financials.financial_narrative (SourcedValue[str])
     c. **Financial Statements** -- delegate to render_financial_tables() from sect3_tables.py
     d. **Distress Indicators** -- table with Altman Z-Score, Beneish M-Score, Ohlson O-Score, Piotroski F-Score. Show current value, zone classification (SAFE/GRAY/DISTRESS), 4-quarter trajectory. Color-code by zone using risk spectrum. Source: state.extracted.financials.distress
     e. **Debt Analysis** -- table with total debt, debt-to-equity, interest coverage, maturity profile, refinancing risk. Source: state.extracted.financials.debt_analysis
     f. **Audit Profile** -- auditor name, tenure, opinion type, material weaknesses, restatements, late filings, CAMs. Red flag items (restatements, material weaknesses) highlighted with D&O context explaining why it matters for D&O insurance. Source: state.extracted.financials.audit_profile
     g. **Peer Group Comparison** -- table listing peers with key comparative metrics. Source: state.extracted.financials.peer_group

2. Create `sect3_tables.py` (~350 lines) -- split for 500-line compliance:
   - `render_financial_tables(doc: Document, state: AnalysisState, ds: DesignSystem) -> None`
   - **VIS-04 conditional formatting implementation:**
     - Build income statement, balance sheet, and cash flow tables from state.extracted.financials.statements
     - Each table: metric name, FY Prior, FY Latest, YTD (if available), YoY Change
     - YoY Change column: apply conditional cell shading:
       - RED (#FCE8E6 background) for deteriorating metrics (negative revenue change, increased debt, etc.)
       - BLUE (#DCEEF8 background) for improving metrics (increased margins, reduced debt, etc.) -- use BLUE, NOT green, per user decision: VIS-04's original "green for improving" is superseded by Phase 8 context (nothing is "safe" in underwriting)
       - AMBER (#FFF3CD background) for mixed/slight changes
       - **NO GREEN ANYWHERE** in conditional formatting or risk indicators. Verify no green hex codes (#00xx00, #28a745, etc.) appear in any render file.
       - The determination of "good" vs "bad" direction depends on the metric: revenue UP = good, debt UP = bad, margins UP = good. Include a metric-direction mapping dict.
     - Format numbers as currency or percentage as appropriate
     - Source citations on each line item from SourcedValue fields

3. Create `charts/__init__.py` (empty or minimal).

4. Create `stock_charts.py` (~400 lines) -- VIS-01 implementation per CONTEXT.md reference:
   - `create_stock_performance_chart(state: AnalysisState, period: str = "1Y", ds: DesignSystem) -> io.BytesIO | None`:
     - Extract stock price history from state.acquired_data.market_data or state.extracted.market.stock
     - Extract sector ETF price history (if available)
     - **Index both to 100** at the start of the period
     - Plot company line: solid navy (#1A1446), linewidth 1.5
     - Plot sector ETF line: dashed gray, linewidth 1.0
     - **Single-day drops >=8%**: Red downward-triangle markers (marker='v', color='#CC0000'). Label each with magnitude + date using offset labels and leader lines (per JACK vs XLY reference). E.g., "-12.3% 2025-03-15"
     - **Multi-day drops (5d >=15%)**: Orange/salmon shaded vertical bands (axvspan with alpha=0.3, color='#E67300'). Label with magnitude + date range using leader lines above the band.
     - **Legend** in top-right: show company name (solid navy), sector ETF (dashed gray), and threshold counts: "1-day >=8% drop (shown: N; total >=5%: M)"
     - Y-axis label: "Indexed to 100"
     - Title: "{TICKER} vs {ETF} -- {period} Performance"
     - Return BytesIO via save_chart_to_bytes, or None if no price data available
     - **Note on VIS-01 optional enhancements:** Volume bars on secondary axis and a primary peer average line are listed in VIS-01 but are optional enhancements. The primary focus is matching the JACK vs XLY reference design (company vs sector ETF with drop markers). Volume bars and peer average may be added in a future iteration if the chart is not already too dense.
   - `create_stock_performance_chart_5y(...)`: Same function but for 5-year period. May use wider thresholds or the same.
   - Handle missing data: if no stock prices available, return None. The section renderer skips the chart.

5. Create `sect4_market.py` (~400 lines):
   - `render_section_4(doc: Document, state: AnalysisState, ds: DesignSystem) -> None`
   - Layout:
     a. Section heading "Section 4: Market & Trading Analysis"
     b. **Summary paragraph** (OUT-03)
     c. **Stock Performance Charts** -- embed 1-year and 5-year charts from create_stock_performance_chart. If charts return None, add "Stock price data not available" paragraph.
     d. **Stock Drop Analysis** -- table of significant drops from state.extracted.market.stock_drops (if available): date, magnitude, type (single-day/multi-day), triggering event, sector comparison
     e. **Insider Trading** -- summary table from state.extracted.market.insider_trading: trade clusters, 10b5-1 vs discretionary, net insider sentiment. Flag cluster selling with D&O context.
     f. **Short Interest** -- current short interest, days to cover, trend from state.extracted.market.short_interest. Flag elevated short interest.
     g. **Earnings Guidance** -- trailing quarter track record from state.extracted.market.earnings_guidance: beats/misses, miss magnitude, stock price reaction. Flag persistent misses with D&O context.

6. Wire sections 3-4 into word_renderer.py.

7. Create `tests/test_render_sections_1_4.py` (~350 lines):
   - Create a minimal AnalysisState fixture with representative data for all 4 sections
   - Test render_section_1 with full state: verify document has "Executive Summary" heading, thesis paragraph, key findings
   - Test render_section_1 with None executive_summary: verify "Not Available" text, no crash
   - Test render_section_2 with company profile data: verify identity table, exposure factors
   - Test render_section_3 with financial data: verify statement tables exist, distress table, conditional formatting applied
   - Test render_section_3 with None financials: graceful degradation
   - Test render_section_4 with market data: verify stock chart embedding (or placeholder if no data)
   - Test stock_charts.create_stock_performance_chart returns BytesIO with PNG data (or None for empty data)
   - Test conditional formatting: verify cells with deteriorating metrics have red-ish shading
  </action>
  <verify>
    cd /Users/gorlin/projects/research && python -m pytest tests/test_render_sections_1_4.py -v && python -m pyright src/do_uw/stages/render/sections/ src/do_uw/stages/render/charts/ && python -m ruff check src/do_uw/stages/render/ && wc -l src/do_uw/stages/render/sections/*.py src/do_uw/stages/render/charts/*.py
  </verify>
  <done>
    Sections 1-4 render with narrative-first approach, source citations, D&O context on flagged items. Financial tables have conditional formatting (VIS-04) with red/blue/amber (no green). Stock charts (VIS-01) show indexed-to-100 performance with red triangle markers for single-day drops and orange shaded bands for multi-day drops. All files under 500 lines. Tests pass.
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_render_sections_1_4.py -v` -- all tests pass
2. `python -m pyright src/do_uw/stages/render/` -- 0 errors
3. `python -m ruff check src/do_uw/stages/render/` -- 0 errors
4. `wc -l src/do_uw/stages/render/sections/*.py src/do_uw/stages/render/charts/*.py` -- all under 500 lines
5. `python -m pytest tests/ -x --timeout=120` -- all existing tests still pass
6. Verify stock chart uses indexed-to-100, navy company line, dashed gray sector line, red triangles, orange bands
7. Verify conditional formatting uses red/blue/amber (NO green anywhere)
8. Verify every section starts with a summary paragraph (OUT-03)
9. Verify source citations appear on data points (OUT-04)
</verification>

<success_criteria>
- Section 1 renders complete executive summary with thesis-first narrative approach
- Section 2 renders company profile with identity, business, geography, D&O exposure
- Section 3 renders financial health with conditional formatted tables (red/blue/amber, no green)
- Section 4 renders market analysis with embedded stock performance charts
- Stock charts: indexed to 100, company vs sector ETF, red triangle markers, orange shaded bands
- Every section has summary paragraph (OUT-03), source citations (OUT-04), D&O context (OUT-05)
- All files under 500 lines, all tests pass, pyright strict clean
</success_criteria>

<output>
After completion, create `.planning/phases/08-document-rendering-visualization/08-02-SUMMARY.md`
</output>
