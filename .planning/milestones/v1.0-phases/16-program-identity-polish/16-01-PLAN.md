---
phase: 16-program-identity-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/do_uw/__init__.py
  - src/do_uw/cli.py
  - src/do_uw/stages/render/word_renderer.py
  - src/do_uw/stages/render/design_system.py
  - src/do_uw/dashboard/app.py
  - src/do_uw/dashboard/design.py
  - src/do_uw/dashboard/charts.py
  - src/do_uw/dashboard/charts_financial.py
  - src/do_uw/stages/render/docx_helpers.py
  - src/do_uw/stages/render/pdf_renderer.py
  - src/do_uw/static/css/dashboard.css
  - src/do_uw/templates/dashboard/base.html
  - src/do_uw/templates/dashboard/index.html
  - src/do_uw/templates/dashboard/section.html
  - src/do_uw/templates/dashboard/partials/_peer_comparison.html
  - src/do_uw/templates/dashboard/partials/_meeting_prep.html
  - src/do_uw/templates/dashboard/partials/_chart_container.html
  - src/do_uw/templates/dashboard/partials/_summary_card.html
  - src/do_uw/templates/dashboard/partials/_finding_detail.html
  - src/do_uw/templates/dashboard/partials/_factor_detail.html
  - src/do_uw/templates/markdown/worksheet.md.j2
  - src/do_uw/templates/pdf/styles.css
  - tests/test_dashboard.py
  - tests/conftest.py
  - tests/__init__.py
  - tests/test_cli.py
autonomous: true

must_haves:
  truths:
    - "Running `angry-dolphin version` displays 'Angry Dolphin Underwriting' (no Liberty Mutual reference)"
    - "The Word document footer shows 'Angry Dolphin Underwriting | Confidential'"
    - "The dashboard page title shows 'Angry Dolphin' and navbar shows 'Angry Dolphin Underwriting'"
    - "All CSS custom properties use '--ad-' prefix instead of '--lm-' prefix"
    - "All source code comments reference 'Angry Dolphin' brand colors, not 'Liberty Mutual'"
    - "The DaisyUI theme attribute is 'ad' not 'lm'"
    - "All 1892+ existing tests pass after branding changes"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package name and CLI entry points"
      contains: "angry-dolphin"
    - path: "src/do_uw/cli.py"
      provides: "CLI help text with Angry Dolphin branding"
      contains: "Angry Dolphin"
    - path: "src/do_uw/templates/dashboard/base.html"
      provides: "Dashboard template with --ad- CSS variables"
      contains: "--ad-navy"
  key_links:
    - from: "src/do_uw/dashboard/design.py"
      to: "src/do_uw/templates/dashboard/base.html"
      via: "CSS_VARIABLES dict keys must match CSS var() references in templates"
      pattern: "--ad-navy.*--ad-gold"
    - from: "src/do_uw/static/css/dashboard.css"
      to: "src/do_uw/templates/dashboard/base.html"
      via: "CSS var() references must match :root variable declarations"
      pattern: "var\\(--ad-"
---

<objective>
Rebrand all user-facing strings from "Liberty Mutual" to "Angry Dolphin Underwriting" and rename CSS variable prefixes from `--lm-` to `--ad-` (Angry Dolphin) for consistency.

Purpose: The program is branded as "Angry Dolphin Underwriting" but residual Liberty Mutual references remain in design system comments, CSS variable names, and some code comments. This plan ensures consistent branding everywhere a user or developer would see it.

Output: All user-facing strings, CSS variables, code comments, and template references consistently use "Angry Dolphin" branding with no residual "Liberty Mutual" references.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Key files to modify:
@src/do_uw/stages/render/design_system.py  -- Comments reference "Liberty Mutual brand colors", "Liberty Blue", "Liberty Yellow", "Liberty Gold"
@src/do_uw/dashboard/design.py  -- Comments reference "Liberty Mutual brand", CSS_VARIABLES dict has "--lm-" prefixed keys
@src/do_uw/dashboard/charts.py  -- Comment: "Liberty Mutual brand colors"
@src/do_uw/dashboard/charts_financial.py  -- Comment: "Liberty Mutual brand colors"
@src/do_uw/stages/render/docx_helpers.py  -- Comment: "Liberty Gold"
@src/do_uw/stages/render/pdf_renderer.py  -- Comment: "Liberty Mutual branding"
@src/do_uw/static/css/dashboard.css  -- Comment: "Liberty Mutual branding", theme `data-theme="lm"`, `--lm-` CSS var references
@src/do_uw/templates/dashboard/base.html  -- `data-theme="lm"`, `:root` `--lm-` vars, inline style `var(--lm-...)` references
@src/do_uw/templates/dashboard/index.html  -- Inline `var(--lm-navy)` references
@src/do_uw/templates/dashboard/section.html  -- `var(--lm-navy)` references
@src/do_uw/templates/dashboard/partials/_peer_comparison.html  -- `var(--lm-navy)` references
@src/do_uw/templates/dashboard/partials/_meeting_prep.html  -- `var(--lm-navy)` references
@src/do_uw/templates/dashboard/partials/_chart_container.html  -- `var(--lm-navy)` references
@src/do_uw/templates/dashboard/partials/_summary_card.html  -- `var(--lm-navy)` references
@src/do_uw/templates/dashboard/partials/_finding_detail.html  -- `var(--lm-navy)` references
@src/do_uw/templates/dashboard/partials/_factor_detail.html  -- `var(--lm-navy)` references
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Liberty Mutual references with Angry Dolphin in all source code comments and CSS variables</name>
  <files>
    src/do_uw/stages/render/design_system.py
    src/do_uw/dashboard/design.py
    src/do_uw/dashboard/charts.py
    src/do_uw/dashboard/charts_financial.py
    src/do_uw/stages/render/docx_helpers.py
    src/do_uw/stages/render/pdf_renderer.py
    src/do_uw/static/css/dashboard.css
    src/do_uw/templates/dashboard/base.html
    src/do_uw/templates/dashboard/index.html
    src/do_uw/templates/dashboard/section.html
    src/do_uw/templates/dashboard/partials/_peer_comparison.html
    src/do_uw/templates/dashboard/partials/_meeting_prep.html
    src/do_uw/templates/dashboard/partials/_chart_container.html
    src/do_uw/templates/dashboard/partials/_summary_card.html
    src/do_uw/templates/dashboard/partials/_finding_detail.html
    src/do_uw/templates/dashboard/partials/_factor_detail.html
  </files>
  <action>
    This task has two sub-parts: (A) update code comments and (B) rename CSS variable prefixes.

    **Part A: Code comments** -- Replace all "Liberty Mutual" references in code comments and docstrings:
    - `design_system.py` line 3: "Liberty Mutual brand colors" -> "Angry Dolphin brand colors"
    - `design_system.py` line 25: "Liberty Mutual brand colors" -> "Angry Dolphin brand colors"
    - `design_system.py` line 26: "Liberty Blue" -> "AD Navy"
    - `design_system.py` line 27: "Liberty Yellow" -> "AD Gold"
    - `design_system.py` line 93: "Liberty Mutual branding" -> "Angry Dolphin branding"
    - `design.py` line 3: "Liberty Mutual brand colors" -> "Angry Dolphin brand colors"
    - `design.py` line 12: "Liberty Mutual brand" -> "Angry Dolphin brand"
    - `charts.py` line 22: "Liberty Mutual brand colors" -> "Angry Dolphin brand colors"
    - `charts_financial.py` line 20: "Liberty Mutual brand colors" -> "Angry Dolphin brand colors"
    - `docx_helpers.py` line 315: "Liberty Gold" -> "AD Gold"
    - `pdf_renderer.py` line 5: "Liberty Mutual branding" -> "Angry Dolphin branding"
    - `dashboard.css` line 2: "Liberty Mutual branding" -> "Angry Dolphin branding"

    **Part B: CSS variable prefix rename** -- Rename all `--lm-` prefixed CSS variables to `--ad-` (Angry Dolphin) across ALL files that define or reference them. This is a global search-and-replace within these files:

    1. `design.py`: Change CSS_VARIABLES dict keys from `"--lm-navy"`, `"--lm-gold"`, `"--lm-text"`, `"--lm-text-light"`, `"--lm-white"` to `"--ad-navy"`, `"--ad-gold"`, `"--ad-text"`, `"--ad-text-light"`, `"--ad-white"`.

    2. `base.html`:
       - Change `data-theme="lm"` to `data-theme="ad"`
       - In `:root` block: rename `--lm-navy` -> `--ad-navy`, `--lm-gold` -> `--ad-gold`, `--lm-text` -> `--ad-text`, `--lm-text-light` -> `--ad-text-light`, `--lm-white` -> `--ad-white`
       - All inline `var(--lm-...)` references -> `var(--ad-...)`

    3. `dashboard.css`:
       - Change `[data-theme="lm"]` to `[data-theme="ad"]`
       - Comment references: `--lm-navy` -> `--ad-navy`, `--lm-gold` -> `--ad-gold`
       - All `var(--lm-...)` references -> `var(--ad-...)`

    4. ALL template HTML files (index.html, section.html, and all 8 partials): Replace every `var(--lm-navy)` with `var(--ad-navy)`, `var(--lm-gold)` with `var(--ad-gold)`, `var(--lm-text)` with `var(--ad-text)`, `var(--lm-text-light)` with `var(--ad-text-light)`, `var(--lm-white)` with `var(--ad-white)`.

    IMPORTANT: Do NOT change the actual color VALUES (#1A1446, #FFD000, etc.) -- only the variable names and comments. The visual appearance should be identical.
    IMPORTANT: Do NOT change Python import paths (do_uw stays as do_uw). Only user-facing strings and CSS variable names change.
  </action>
  <verify>
    Run: `grep -rn "Liberty\|--lm-\|lm-navy\|lm-gold\|lm-text\|lm-white\|data-theme=\"lm\"" src/ --include="*.py" --include="*.html" --include="*.css" --include="*.j2"`
    Should return zero results.

    Run: `grep -rn "\-\-ad-navy\|--ad-gold" src/do_uw/dashboard/design.py src/do_uw/templates/dashboard/base.html src/do_uw/static/css/dashboard.css`
    Should return results confirming the new prefix is in place.
  </verify>
  <done>
    All "Liberty Mutual" references replaced with "Angry Dolphin" in comments and docstrings. All "--lm-" CSS variable prefixes renamed to "--ad-". data-theme attribute changed from "lm" to "ad". No visual appearance changes (color values preserved). Zero grep matches for any Liberty/LM references in src/.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update test assertions and verify full test suite passes</name>
  <files>
    tests/test_dashboard.py
    tests/conftest.py
    tests/__init__.py
    tests/test_cli.py
  </files>
  <action>
    Update test files that assert on branding strings:

    1. `tests/test_dashboard.py` line 146: The assertion `assert "Angry Dolphin" in response.text` should still pass (no change needed since we kept "Angry Dolphin" in base.html). However, verify the test still works since we changed `data-theme="lm"` to `data-theme="ad"` -- if any test asserts on that, update it.

    2. Review all test files for any assertions on `--lm-` CSS variables or `data-theme="lm"` and update them to `--ad-` / `data-theme="ad"`.

    3. Run the full test suite: `cd /Users/gorlin/projects/research && python -m pytest tests/ -x -q`
    4. Run ruff: `cd /Users/gorlin/projects/research && python -m ruff check src/ tests/`
    5. Run pyright: `cd /Users/gorlin/projects/research && python -m pyright src/`

    If any test fails due to branding string assertions, update the assertion to match the new branding.
  </action>
  <verify>
    `python -m pytest tests/ -x -q` -- all 1892+ tests pass
    `python -m ruff check src/ tests/` -- zero lint errors
    `python -m pyright src/` -- zero type errors
  </verify>
  <done>
    All tests pass with updated branding. Zero lint errors. Zero type errors. The branding rename is fully validated across the entire codebase.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "Liberty" src/` returns zero results
2. `grep -rn "\-\-lm-" src/` returns zero results
3. `grep -rn "data-theme=\"lm\"" src/` returns zero results
4. `grep -rn "Angry Dolphin" src/ | wc -l` returns 15+ results (confirming branding is present)
5. `grep -rn "\-\-ad-navy" src/ | wc -l` returns 20+ results (confirming CSS variables renamed)
6. `python -m pytest tests/ -x -q` passes all 1892+ tests
7. `python -m ruff check src/ tests/` returns zero errors
8. `python -m pyright src/` returns zero errors
</verification>

<success_criteria>
- Zero "Liberty Mutual" or "Liberty Blue/Yellow/Gold" references anywhere in src/
- Zero "--lm-" CSS variable references anywhere in src/ or templates/
- All CSS variables consistently use "--ad-" prefix
- data-theme attribute is "ad" everywhere
- All user-facing strings say "Angry Dolphin Underwriting" (already done, just verify)
- All 1892+ existing tests pass
- Zero lint/type errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-program-identity-polish/16-01-SUMMARY.md`
</output>
