---
phase: 39-system-integration-quality-validation
plan: 04
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - src/do_uw/stages/analyze/check_field_routing.py
  - src/do_uw/stages/analyze/check_mappers.py
  - src/do_uw/stages/analyze/check_mappers_phase26.py
  - src/do_uw/stages/analyze/check_helpers.py
  - src/do_uw/stages/analyze/check_evaluators.py
  - src/do_uw/brain/checks.json
  - scripts/backtest_audit.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every TRIGGERED check on AAPL and TSLA is genuinely relevant to the data it evaluates"
    - "No SKIPPED check exists that should have fired based on available data (no routing failures)"
    - "Employee count does not trigger as labor dependency"
    - "Stock price does not trigger as PE ratio threshold"
    - "TSLA EXEC.PRIOR_LIT checks reflect real data, not hallucinated val=75.0"
    - "FIN.LIQ.position threshold is realistic (not red at 6.0 for current ratio)"
    - "A reusable backtest audit script exists for future validation"
  artifacts:
    - path: "scripts/backtest_audit.py"
      provides: "Reusable backtest audit tool for checking TRIGGERED and SKIPPED results"
      contains: "audit_backtest"
  key_links:
    - from: "src/do_uw/stages/analyze/check_field_routing.py"
      to: "src/do_uw/brain/checks.json"
      via: "check_id prefix routing"
      pattern: "FIELD_FOR_CHECK|field_key"
    - from: "scripts/backtest_audit.py"
      to: "src/do_uw/knowledge/backtest.py"
      via: "import"
      pattern: "from do_uw.knowledge.backtest"
---

<objective>
Audit every TRIGGERED and SKIPPED check result on both AAPL and TSLA. Fix all false triggers and routing failures. Per user decision: zero tolerance — every triggered check must be genuinely relevant, and every skipped check that should have fired is equally bad. Fix the check logic itself (field routing, threshold parsing, mapper logic), no exclusion rules or workarounds.

Purpose: False triggers and missed signals both destroy underwriter trust. An underwriter seeing "labor dependency" flagged because of employee count would question every other finding. A missed real risk signal is worse — it defeats the system's purpose.

Output: Backtest audit script, all false triggers fixed, AAPL and TSLA check results accurate.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/do_uw/stages/analyze/check_field_routing.py
@src/do_uw/stages/analyze/check_mappers.py
@src/do_uw/stages/analyze/check_helpers.py
@src/do_uw/stages/analyze/check_evaluators.py
@src/do_uw/brain/checks.json
@src/do_uw/knowledge/backtest.py
@output/AAPL/state.json
@output/TSLA/state.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backtest audit script and run full audit on AAPL + TSLA</name>
  <files>scripts/backtest_audit.py</files>
  <action>
1. Create `scripts/backtest_audit.py` — a reusable tool that:
   - Loads a state.json file and runs all brain checks against it
   - Categorizes each TRIGGERED result with context: check_id, value found, threshold, data source field, and whether the trigger appears genuine
   - Categorizes each SKIPPED result: check_id, expected data source, why it was skipped (DATA_UNAVAILABLE vs ROUTING_FAILURE vs TYPE_MISMATCH)
   - Produces a human-readable report with sections: TRIGGERED (suspected false), TRIGGERED (confirmed genuine), SKIPPED (should have fired), SKIPPED (legitimately unavailable)
   - Can be run as: `uv run python scripts/backtest_audit.py output/AAPL/state.json`

2. Run the audit on AAPL state.json. Research found these suspicious results:
   - `FIN.LIQ.position`: val=0.8933, red threshold 6.0 — a current ratio of 0.89 being compared against 6.0 is suspicious. Check if the threshold is meant for a different metric or if the check definition has the wrong threshold.
   - `FIN.LIQ.working_capital`: val=0.8933, red threshold 1.0 — same value as position, may be routing to current_ratio instead of working capital
   - `FIN.LIQ.efficiency`: val=0.217, yellow threshold 0.5 — verify this is the right metric
   - 97 SKIPPED checks: categorize into genuine unavailable vs. routing failures

3. Run the audit on TSLA state.json. Research found these suspicious results:
   - `EXEC.PRIOR_LIT.any_officer`: val=75.0 — likely from hallucinated "Kimbal Musk with 75 prior litigations". Trace the data path to find where 75.0 comes from.
   - `EXEC.PRIOR_LIT.ceo_cfo`: val=75.0 — same issue
   - `FIN.ACCT.material_weakness`: val=2.0 — verify this is genuine
   - 43 SKIPPED checks: categorize

4. The audit report should be saved to `output/{TICKER}/backtest_audit.txt` for review.

5. Document every finding with: check_id, current behavior, expected behavior, root cause, proposed fix location.
  </action>
  <verify>
```bash
cd /Users/gorlin/projects/UW/do-uw && uv run python scripts/backtest_audit.py output/AAPL/state.json 2>&1 | head -50
```
  </verify>
  <done>Backtest audit script exists and runs on both AAPL and TSLA. All TRIGGERED and SKIPPED results are categorized. False triggers and routing failures are identified with specific root causes and fix locations.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all identified false triggers and routing failures</name>
  <files>
    src/do_uw/stages/analyze/check_field_routing.py
    src/do_uw/stages/analyze/check_mappers.py
    src/do_uw/stages/analyze/check_mappers_phase26.py
    src/do_uw/stages/analyze/check_helpers.py
    src/do_uw/stages/analyze/check_evaluators.py
    src/do_uw/brain/checks.json
  </files>
  <action>
Fix every false trigger and routing failure identified in Task 1. Based on research, expected fixes include:

1. **Employee count as labor dependency** (BIZ.DEPEND.labor): Fix field routing so this check routes to a labor dependency flag/score, NOT to `employee_count`. If the field doesn't exist in state, the check should correctly SKIP (not trigger on wrong data).

2. **Stock price as PE ratio** (if found): Fix field routing so PE ratio checks route to the actual PE ratio field, not stock price.

3. **FIN.LIQ.position threshold 6.0**: Investigate — a red threshold of 6.0 for current ratio makes no sense (that would mean only ratios above 6.0 are acceptable). Either the threshold is wrong in checks.json, or the check is meant for a different metric. Fix the threshold or routing.

4. **TSLA EXEC.PRIOR_LIT val=75.0**: Trace through `check_mappers_phase26.py` to find where this value originates. If it's reading from a hallucinated executive data field, fix the data path. If the executive list is empty but old cached data persists, clear the stale data.

5. **SKIPPED routing failures**: For each SKIPPED check that should have fired (data exists in state but routing can't find it), fix the field routing in `check_field_routing.py` or the mapper in `check_mappers.py`.

For EACH fix:
- Modify the CHECK LOGIC (field routing, threshold, mapper), NOT the data
- Do NOT add exclusion rules or "skip this check for ticker X" workarounds
- Run the backtest audit script after each fix to verify the change
- Ensure the fix doesn't introduce new false triggers on the other ticker

After all fixes:
- Re-run full backtest audit on both AAPL and TSLA
- Verify: zero suspected false triggers, all routing failures fixed
- Run `uv run pytest --tb=short -q` to ensure no test regressions
  </action>
  <verify>
```bash
cd /Users/gorlin/projects/UW/do-uw && \
  echo "=== AAPL Audit ===" && \
  uv run python scripts/backtest_audit.py output/AAPL/state.json 2>&1 | grep -E "FALSE|ROUTING_FAILURE" && \
  echo "(should be empty)" && \
  echo "=== TSLA Audit ===" && \
  uv run python scripts/backtest_audit.py output/TSLA/state.json 2>&1 | grep -E "FALSE|ROUTING_FAILURE" && \
  echo "(should be empty)" && \
  echo "=== Tests ===" && \
  uv run pytest --tb=short -q 2>&1 | tail -5
```
  </verify>
  <done>Every false trigger fixed by correcting check logic (field routing, thresholds, mappers). Zero false triggers on both AAPL and TSLA. Zero routing failures causing incorrect SKIPPED status. All tests pass. No exclusion rules or workarounds — the check logic itself is correct.</done>
</task>

</tasks>

<verification>
1. `uv run python scripts/backtest_audit.py output/AAPL/state.json` — zero FALSE triggers, zero ROUTING_FAILURE skips
2. `uv run python scripts/backtest_audit.py output/TSLA/state.json` — zero FALSE triggers, zero ROUTING_FAILURE skips
3. Employee count does not appear in any TRIGGERED check
4. TSLA EXEC.PRIOR_LIT does not show val=75.0 from hallucinated data
5. `uv run pytest --tb=short -q` — all tests pass
</verification>

<success_criteria>
- Reusable backtest audit script at `scripts/backtest_audit.py`
- Zero false triggers on both AAPL and TSLA
- All routing failures fixed
- Check logic itself corrected, no workarounds
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/39-system-integration-quality-validation/39-04-SUMMARY.md`
</output>
