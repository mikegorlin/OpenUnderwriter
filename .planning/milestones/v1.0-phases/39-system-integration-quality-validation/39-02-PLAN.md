---
phase: 39-system-integration-quality-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/dead_code_scan.py
  - src/do_uw/brain/enrichment_data.py
  - src/do_uw/brain/enrichment_data_ext.py
  - src/do_uw/knowledge/calibrate.py
  - src/do_uw/knowledge/calibrate_impact.py
  - src/do_uw/brain/brain_writer.py
  - src/do_uw/brain/brain_writer_export.py
  - src/do_uw/cli_brain.py
  - src/do_uw/cli_brain_ext.py
  - pyproject.toml
autonomous: true
requirements: []
must_haves:
  truths:
    - "A reusable dead code scan command exists and can be re-run after any phase"
    - "No source file in src/do_uw/ exceeds 500 lines (pragmatic enforcement per user decision)"
    - "All split files maintain the same public API — no import changes needed by consumers"
    - "vulture dead code scan runs without errors and produces actionable output"
  artifacts:
    - path: "scripts/dead_code_scan.py"
      provides: "Reusable dead code detection wrapper using vulture"
      contains: "vulture"
    - path: "src/do_uw/brain/enrichment_data_ext.py"
      provides: "Split extension for enrichment_data.py"
    - path: "src/do_uw/knowledge/calibrate_impact.py"
      provides: "Split extension for calibrate.py"
    - path: "src/do_uw/brain/brain_writer_export.py"
      provides: "Split extension for brain_writer.py"
  key_links:
    - from: "src/do_uw/brain/enrichment_data.py"
      to: "src/do_uw/brain/enrichment_data_ext.py"
      via: "import delegation"
      pattern: "from.*enrichment_data_ext import"
---

<objective>
Build a reusable dead code detection tool and split all source files exceeding 500 lines. Per user decision: pragmatic enforcement (well-organized cohesive files slightly over are acceptable), but the 11 files identified in research genuinely need splitting. The dead code scanner must be reusable after any phase.

Purpose: Anti-context-rot rule enforcement. Files over 500 lines are harder to navigate. Dead code accumulates across 39 phases. Both tools become permanent quality infrastructure.

Output: `scripts/dead_code_scan.py` wrapper, vulture dev dependency, 4-5 files split to under 500 lines each.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/do_uw/brain/enrichment_data.py
@src/do_uw/knowledge/calibrate.py
@src/do_uw/cli_brain.py
@src/do_uw/brain/brain_writer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build reusable dead code detection tool with vulture</name>
  <files>scripts/dead_code_scan.py, pyproject.toml</files>
  <action>
1. Add vulture as a dev dependency:
   ```bash
   uv add --dev vulture
   ```

2. Create `scripts/dead_code_scan.py` — a reusable wrapper that:
   - Runs vulture on `src/do_uw/` with a project-specific whitelist
   - Creates a whitelist file at `scripts/vulture_whitelist.py` listing known false positives (Pydantic model fields used by serialization, CLI callback functions, pytest fixtures, __all__ exports)
   - Outputs a categorized report: unused functions, unused imports, unused variables, unused classes
   - Returns non-zero exit code if genuine dead code found (for CI integration)
   - Can be invoked as `uv run python scripts/dead_code_scan.py` or `uv run python scripts/dead_code_scan.py --fix-imports` (the latter runs ruff to auto-fix unused imports)

3. Create `scripts/vulture_whitelist.py` with known false positives:
   - Pydantic `model_config`, `model_validator`, `field_validator` decorated methods
   - Typer CLI callback functions (invoked by framework)
   - pytest fixtures (invoked by framework)
   - `__all__` exports
   - Stage protocol methods (`run`, `name`, etc.)

4. Run the scan once and review output. Fix any genuinely dead imports using ruff (`ruff check --fix --select F401`). Do NOT delete functions/classes — just report them. The user will decide what to remove.

5. Verify the tool is reusable: running it again produces consistent output.
  </action>
  <verify>
```bash
cd /Users/gorlin/projects/UW/do-uw && uv run python scripts/dead_code_scan.py 2>&1 | head -30
```
  </verify>
  <done>vulture is a dev dependency. `scripts/dead_code_scan.py` runs without errors, produces categorized dead code report, and can be re-run after any phase. Whitelist handles known false positives (Pydantic, Typer, pytest).</done>
</task>

<task type="auto">
  <name>Task 2: Split source files exceeding 500 lines</name>
  <files>
    src/do_uw/brain/enrichment_data.py
    src/do_uw/brain/enrichment_data_ext.py
    src/do_uw/knowledge/calibrate.py
    src/do_uw/knowledge/calibrate_impact.py
    src/do_uw/brain/brain_writer.py
    src/do_uw/brain/brain_writer_export.py
    src/do_uw/cli_brain.py
    src/do_uw/cli_brain_ext.py
  </files>
  <action>
Apply pragmatic 500-line enforcement per user decision. Focus on the 4 largest files (>550 lines) where splitting genuinely improves navigation. Files 505-535 lines are borderline — split only if there's a natural semantic boundary.

**Must split (>550 lines):**

1. `enrichment_data.py` (767 lines) → Split into:
   - `enrichment_data.py` (~400 lines): Core enrichment data functions
   - `enrichment_data_ext.py` (~370 lines): Extended/specialized enrichment functions
   - Keep public API in `enrichment_data.py` by re-importing from ext (so consumers don't need to change imports)

2. `calibrate.py` (727 lines) → Split into:
   - `calibrate.py` (~400 lines): Core calibration logic (preview, apply, rollback)
   - `calibrate_impact.py` (~330 lines): Impact analysis, score simulation, comparison functions
   - Keep public API in `calibrate.py`

3. `cli_brain.py` (633 lines) → Split into:
   - `cli_brain.py` (~350 lines): Primary brain CLI commands (list, show, search, status)
   - `cli_brain_ext.py` (~280 lines): Extended commands (export, import, audit, compare)
   - Keep the Typer app in `cli_brain.py`, register sub-commands from ext

4. `brain_writer.py` (561 lines) → Split into:
   - `brain_writer.py` (~300 lines): Core writer (create, update, delete checks)
   - `brain_writer_export.py` (~260 lines): Export/import functions (export_to_json, import_from_json, sync)
   - Keep public API in `brain_writer.py`

**Pragmatic pass (505-535 lines):**
Review `earnings_guidance.py` (533), `regulatory_extract.py` (521), `company_profile.py` (518), `sec_client.py` (511), `financial_formulas.py` (509), `factor_data.py` (506), `cli_knowledge.py` (505). Split only if there's a natural boundary. Don't force splits on well-organized files.

After splitting `brain_writer.py` into `brain_writer.py` + `brain_writer_export.py`:
- Verify export_to_json() still works by running it and checking checks.json has 384 entries:
  ```bash
  uv run python -c "
  from do_uw.brain.brain_writer import export_to_json
  export_to_json()
  import json
  with open('src/do_uw/brain/checks.json') as f:
      checks = json.load(f)
  assert len(checks) == 384, f'Expected 384 checks, got {len(checks)}'
  print(f'checks.json synced: {len(checks)} checks')
  "
  ```
  This covers SC3 ("checks.json synced from DuckDB, 384 checks visible").

After each split:
- Run `uv run python -c "from do_uw.brain.enrichment_data import *"` to verify imports still work
- Run `uv run pytest --tb=short -q` to ensure no regressions
- Run `uv run python scripts/check_file_lengths.py` to verify all files under 500 lines
  </action>
  <verify>
```bash
cd /Users/gorlin/projects/UW/do-uw && uv run python scripts/check_file_lengths.py 2>&1 | grep -E "FAIL|PASS|src/do_uw" | head -20 && echo "---" && uv run pytest --tb=short -q 2>&1 | tail -5
```
  </verify>
  <done>All 4 major files (enrichment_data, calibrate, cli_brain, brain_writer) are split to under 500 lines each. Public APIs preserved — no consumer import changes needed. All tests pass. `check_file_lengths.py` shows no source file failures (or only borderline files the user accepted).</done>
</task>

</tasks>

<verification>
1. `uv run python scripts/dead_code_scan.py` — runs without errors, produces report
2. `uv run python scripts/check_file_lengths.py` — no FAIL on source files (or minimal borderline)
3. `uv run pytest --tb=short -q` — all tests pass
4. `uv run python -c "from do_uw.brain.enrichment_data import *; from do_uw.knowledge.calibrate import *; from do_uw.brain.brain_writer import *"` — imports work
</verification>

<success_criteria>
- Reusable dead code detection tool at `scripts/dead_code_scan.py` with vulture
- 4 major source files split below 500 lines
- No consumer import breakage from splits
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/39-system-integration-quality-validation/39-02-SUMMARY.md`
</output>
