---
phase: 39-system-integration-quality-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/models/state.py
  - tests/test_state_deserialization.py
  - src/do_uw/stages/render/md_renderer_helpers.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "AAPL state.json loads into AnalysisState with all top-level fields non-None"
    - "TSLA state.json loads into AnalysisState with all top-level fields non-None"
    - "Rendering AAPL produces 'Apple Inc.' not 'Unknown Company'"
    - "No section in rendered output shows 'data not available' when state.json contains data for that section"
    - "Score is not 100/100 when scoring data exists — deductions apply"
  artifacts:
    - path: "tests/test_state_deserialization.py"
      provides: "Regression test loading real state files and asserting key fields non-None"
      contains: "test_aapl_state_loads"
  key_links:
    - from: "src/do_uw/models/state.py"
      to: "output/AAPL/state.json"
      via: "Pydantic deserialization"
      pattern: "AnalysisState.model_validate"
---

<objective>
Fix the critical AAPL rendering bug where the worksheet shows "Unknown Company" and "data not available" despite complete data in state.json. This is a deserialization regression — a model change between Feb 15 and Feb 21 broke state loading.

Purpose: This bug undermines trust in the entire system. An underwriter receiving this output would immediately reject it. Every subsequent plan depends on working state loading and rendering.

Output: Working AAPL render, state deserialization regression test, root cause identified and fixed.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/do_uw/models/state.py
@output/AAPL/state.json
@src/do_uw/stages/render/md_renderer_helpers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix AAPL state deserialization regression</name>
  <files>src/do_uw/models/state.py</files>
  <action>
1. Load `output/AAPL/state.json` with `AnalysisState.model_validate_json()` and check which top-level fields are None or empty that should have data. Compare against the raw JSON to find the mismatch.

2. Run `git log --since=2026-02-15 -- src/do_uw/models/` to identify what model changes occurred after Feb 15 (when the AAPL_rerender worked correctly).

3. Root-cause the deserialization failure. Common causes:
   - Field rename without alias (e.g., `Field(alias="old_name")`)
   - Type change that rejects old format (e.g., `str` to `Enum`)
   - New required field without default (older state files missing it)
   - Nested model structural change (e.g., flattening or nesting a sub-model)

4. Fix the model so that existing state.json files deserialize correctly. If a field was renamed, add `alias` or `validation_alias`. If a type changed, add a validator. If a required field was added, provide a sensible default.

5. Verify the fix: load AAPL state.json, assert `state.identity.legal_name.value == "Apple Inc."`, assert `state.company.market_cap.value` is not None, assert `state.company.employee_count.value` is not None.

6. Also check TSLA state.json loads correctly with the same fix.

7. Check that scoring data loads correctly — a score of 100/100 with NO_TOUCH tier is contradictory and suggests score deductions aren't applying because data isn't loading.
  </action>
  <verify>
```bash
cd /Users/gorlin/projects/UW/do-uw && uv run python -c "
from do_uw.models.state import AnalysisState
import json
with open('output/AAPL/state.json') as f:
    data = json.load(f)
state = AnalysisState.model_validate(data)
assert state.identity.legal_name.value == 'Apple Inc.', f'Got: {state.identity.legal_name}'
assert state.company.market_cap.value is not None, 'market_cap is None'
print('AAPL state loads correctly')
"
```
  </verify>
  <done>AAPL and TSLA state.json files deserialize into AnalysisState with all key fields populated (identity, company, financials, market, governance, litigation, scoring). The deserialization regression is identified and fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Create state deserialization regression test and verify render output</name>
  <files>tests/test_state_deserialization.py, src/do_uw/stages/render/md_renderer_helpers.py</files>
  <action>
1. Create `tests/test_state_deserialization.py` with tests that load the AAPL and TSLA state.json files and assert key fields are non-None:
   - `test_aapl_state_loads()`: Load output/AAPL/state.json, assert identity.legal_name.value == "Apple Inc.", company.market_cap.value is not None, company.employee_count.value is not None, financials is not None
   - `test_tsla_state_loads()`: Load output/TSLA/state.json, assert identity.legal_name.value == "Tesla, Inc.", company is not None
   - `test_state_score_not_perfect_when_data_exists()`: If scoring data exists, score should not be exactly 100.0 (indicates deductions aren't applying)
   - Skip tests gracefully if state files don't exist (they're gitignored)

2. Verify that the Markdown renderer now produces correct output:
   - Run the re-render on AAPL state.json
   - Verify output contains "Apple Inc." not "Unknown Company"
   - Verify no section says "data not available" when corresponding state data is populated

3. If the rendering issue is in `md_renderer_helpers.py` (e.g., `extract_company` function not finding the right fields), fix the field access path.

4. Run the full test suite to ensure no regressions.
  </action>
  <verify>
```bash
cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/test_state_deserialization.py -v && uv run pytest --tb=short -q 2>&1 | tail -5
```
  </verify>
  <done>Regression test exists that will catch future deserialization breakage. AAPL renders with correct company name, populated sections, and reasonable score (not 100/100 with NO_TOUCH).</done>
</task>

</tasks>

<verification>
1. `uv run python -c "from do_uw.models.state import AnalysisState; import json; state = AnalysisState.model_validate(json.load(open('output/AAPL/state.json'))); assert state.identity.legal_name.value == 'Apple Inc.'"` — passes
2. `uv run pytest tests/test_state_deserialization.py -v` — all tests pass
3. Grep rendered output for "Unknown Company" — not found
4. Full test suite passes
</verification>

<success_criteria>
- AAPL and TSLA state files load with all key fields populated
- Rendering produces correct company name and populated sections
- Regression test prevents future deserialization breakage
- Full test suite passes (no regressions from model fix)
</success_criteria>

<output>
After completion, create `.planning/phases/39-system-integration-quality-validation/39-01-SUMMARY.md`
</output>
