---
phase: 06-scoring-patterns-risk-synthesis
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/do_uw/models/scoring.py
  - src/do_uw/stages/score/__init__.py
  - src/do_uw/stages/score/factor_scoring.py
  - src/do_uw/stages/score/red_flag_gates.py
  - src/do_uw/stages/score/tier_classification.py
  - tests/test_score_stage.py
autonomous: true

must_haves:
  truths:
    - "10 factor scores (F1-F10) are computed from scoring.json rules against ExtractedData with transparent sub-component breakdowns"
    - "11 CRF gates are evaluated BEFORE factor scoring, with triggered gates imposing hard quality score ceilings"
    - "Tier classification assigns WIN/WANT/WRITE/WATCH/WALK/NO_TOUCH based on quality_score using scoring.json boundaries"
    - "CRF ID normalization handles mismatch between scoring.json (CRF-001) and red_flags.json (CRF-01)"
    - "Factor scores are capped at their max_points values; total risk points cannot exceed 100"
    - "Scoring model is expanded with fields for risk_type, allegation_mapping, claim_probability, severity_scenarios, tower_recommendation, red_flag_summary"
  artifacts:
    - path: "src/do_uw/models/scoring.py"
      provides: "Expanded scoring models (new sub-models for SECT7-04 through SECT7-10)"
      contains: "class ClaimProbability"
    - path: "src/do_uw/stages/score/factor_scoring.py"
      provides: "10-factor scoring engine reading scoring.json rules"
      contains: "def score_all_factors"
    - path: "src/do_uw/stages/score/red_flag_gates.py"
      provides: "11 CRF gate evaluation and ceiling application"
      contains: "def evaluate_red_flag_gates"
    - path: "src/do_uw/stages/score/tier_classification.py"
      provides: "Tier classification from quality score"
      contains: "def classify_tier"
    - path: "src/do_uw/stages/score/__init__.py"
      provides: "ScoreStage orchestrator (replaces stub) -- partial, completed in 06-04"
      contains: "class ScoreStage"
    - path: "tests/test_score_stage.py"
      provides: "Tests for factor scoring, CRF gates, tier, model expansion"
      min_lines: 100
  key_links:
    - from: "stages/score/__init__.py"
      to: "factor_scoring.py"
      via: "score_all_factors()"
    - from: "stages/score/__init__.py"
      to: "red_flag_gates.py"
      via: "evaluate_red_flag_gates()"
    - from: "stages/score/__init__.py"
      to: "tier_classification.py"
      via: "classify_tier()"
    - from: "factor_scoring.py"
      to: "brain/scoring.json"
      via: "ConfigLoader rules"
---

<objective>
Build the SCORE stage core: expand the scoring Pydantic models for SECT7-04 through SECT7-10, implement the 10-factor scoring engine, 11 CRF red flag gates, and tier classification. The ScoreStage orchestrator is created in partial form (factor scoring + CRF + tier only); pattern detection, allegation mapping, severity, and tower are added in Plans 06-03 and 06-04.

Purpose: The SCORE stage transforms extracted facts into a quantified risk assessment -- factor scores, quality score, and tier classification. This is the core of the D&O underwriting decision.
Output: Expanded models/scoring.py, 3 score engine files, partial ScoreStage, tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/do_uw/models/scoring.py
@src/do_uw/models/state.py
@src/do_uw/brain/scoring.json
@src/do_uw/brain/red_flags.json
@src/do_uw/brain/sectors.json
@src/do_uw/config/loader.py
@src/do_uw/stages/score/__init__.py
@src/do_uw/models/financials.py
@src/do_uw/models/market.py
@src/do_uw/models/governance.py
@src/do_uw/models/litigation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand scoring models and build factor scoring engine</name>
  <files>
    src/do_uw/models/scoring.py
    src/do_uw/stages/score/factor_scoring.py
    tests/test_score_stage.py
  </files>
  <action>
  Expand models/scoring.py (target ~400 lines, current 220):
  Add the following Pydantic models AFTER existing models:

  - `class RiskType(StrEnum)`: BINARY_EVENT, GROWTH_DARLING, GUIDANCE_DEPENDENT, REGULATORY_SENSITIVE, TRANSFORMATION, STABLE_MATURE, DISTRESSED (SECT7-04)
  - `class RiskTypeClassification(BaseModel)`: primary (RiskType), secondary (RiskType | None), evidence (list[str]), needs_calibration (bool = True)
  - `class AllegationTheory(StrEnum)`: A_DISCLOSURE, B_GUIDANCE, C_PRODUCT_OPS, D_GOVERNANCE, E_MA (SECT7-05)
  - `class TheoryExposure(BaseModel)`: theory (AllegationTheory), exposure_level (str: HIGH/MODERATE/LOW), findings (list[str]), factor_sources (list[str])
  - `class AllegationMapping(BaseModel)`: theories (list[TheoryExposure]), primary_exposure (AllegationTheory), concentration_analysis (str), needs_calibration (bool = True)
  - `class ProbabilityBand(StrEnum)`: LOW, MODERATE, ELEVATED, HIGH, VERY_HIGH (SECT7-07)
  - `class ClaimProbability(BaseModel)`: band (ProbabilityBand), range_low_pct (float), range_high_pct (float), industry_base_rate_pct (float), adjustment_narrative (str), needs_calibration (bool = True)
  - `class SeverityScenario(BaseModel)`: percentile (int: 25/50/75/95), label (str: favorable/median/adverse/catastrophic), ddl_amount (float), settlement_estimate (float), defense_cost_estimate (float), total_exposure (float)
  - `class SeverityScenarios(BaseModel)`: market_cap (float), decline_scenarios (dict[str, float] for 10%/20%/30%), scenarios (list[SeverityScenario]), needs_calibration (bool = True)
  - `class TowerPosition(StrEnum)`: PRIMARY, LOW_EXCESS, MID_EXCESS, HIGH_EXCESS, DECLINE
  - `class LayerAssessment(BaseModel)`: position (TowerPosition), risk_assessment (str), premium_guidance (str), attachment_range (str)
  - `class TowerRecommendation(BaseModel)`: recommended_position (TowerPosition), minimum_attachment (str), layers (list[LayerAssessment]), side_a_assessment (str), needs_calibration (bool = True)
  - `class FlagSeverity(StrEnum)`: CRITICAL, HIGH, MODERATE, LOW (SECT7-10)
  - `class FlaggedItem(BaseModel)`: description (str), source (str), severity (FlagSeverity), scoring_impact (str), allegation_theory (str), trajectory (str: NEW/WORSENING/STABLE/IMPROVING)
  - `class RedFlagSummary(BaseModel)`: items (list[FlaggedItem]), critical_count (int), high_count (int), moderate_count (int), low_count (int)

  Add fields to ScoringResult:
  - `risk_type: RiskTypeClassification | None = None`
  - `allegation_mapping: AllegationMapping | None = None`
  - `claim_probability: ClaimProbability | None = None`
  - `severity_scenarios: SeverityScenarios | None = None`
  - `tower_recommendation: TowerRecommendation | None = None`
  - `red_flag_summary: RedFlagSummary | None = None`
  - `calibration_notes: list[str] = Field(default_factory=lambda: [], description="NEEDS CALIBRATION items per SECT7-11")`
  - `binding_ceiling_id: str | None = Field(default=None, description="CRF ID of the binding ceiling")`

  If scoring.py approaches 500 lines, split new models into models/scoring_output.py and import them into scoring.py for re-export.

  Create stages/score/factor_scoring.py (~450 lines):
  - `def score_all_factors(scoring_config: dict[str, Any], extracted: ExtractedData, company: CompanyProfile | None, sectors_config: dict[str, Any]) -> list[FactorScore]`: Iterate over all 10 factors in scoring_config["factors"], call `_score_factor()` for each, return list of FactorScore.
  - `def _score_factor(factor_key: str, factor_config: dict[str, Any], extracted: ExtractedData, company: CompanyProfile | None, sectors: dict[str, Any]) -> FactorScore`: Score a single factor. Steps:
    1. Get factor data from ExtractedData (use helper functions per factor)
    2. Evaluate rules from factor_config["rules"] -- find the highest-triggered rule
    3. Apply bonuses from factor_config.get("bonuses", [])
    4. Apply multipliers (insider_amplifier for F2, market_cap_multipliers for F2)
    5. Cap total at factor_config["max_points"]
    6. Return FactorScore with sub_components breakdown
    NOTE: Pattern modifier application is deferred to 06-04 after patterns are detected in 06-03.
  - Per-factor data extraction helpers (private functions):
    - `_get_f1_data(extracted, company)` -> dict with active_sca, settled_scas (with years_since), sec_enforcement, derivative_suits
    - `_get_f2_data(extracted)` -> dict with decline_from_high, insider_amplifier_data, peer_performance
    - `_get_f3_data(extracted)` -> dict with restatements (months_since), auditor_changes, material_weaknesses
    - `_get_f4_data(extracted)` -> dict with ipo_date, spac_merger_date, major_ma
    - `_get_f5_data(extracted)` -> dict with earnings_misses_8q, max_miss_magnitude, guidance_withdrawn
    - `_get_f6_data(extracted, sectors)` -> dict with short_interest_pct, sector_baseline, si_trend
    - `_get_f7_data(extracted, sectors)` -> dict with volatility_90d, sector_volatility, extreme_days
    - `_get_f8_data(extracted, sectors)` -> dict with debt_to_ebitda, sector_leverage, cash_runway_months, going_concern, covenant_breach
    - `_get_f9_data(extracted)` -> dict with board_independence, ceo_chair_duality, ceo_tenure, cfo_tenure, dual_class
    - `_get_f10_data(extracted)` -> dict with ceo_tenure_months, cfo_tenure_months, interim_ceo, interim_cfo
  - Helper: `_find_matching_rule(rules: list[dict], data: dict) -> tuple[dict | None, float]` -- evaluate rules and return highest-triggered rule + points
  - Helper: `_get_sector_code(company: CompanyProfile | None) -> str` -- extract sector code, default to "DEFAULT"
  - Safely access ExtractedData fields: check None at each level, use .value for SourcedValues. When data is unavailable, log and return 0 points for that factor (not skip the factor -- factors always produce a score, even if 0).

  Write tests in test_score_stage.py:
  - Test FactorScore creation
  - Test score_all_factors with minimal ExtractedData (all None -> all 0 points)
  - Test _score_factor for F1 with active SCA -> 20 points
  - Test _score_factor for F2 with 45% decline -> 9 points (40-50% range)
  - Test F2 insider amplifier (cluster selling -> 2.0x multiplier, capped at 15)
  - Test F5 with 3 misses -> 6 points
  - Test F6 with short interest 3x sector -> 4 points
  - Test factor max_points capping (multiplied score exceeds max)
  - Test _get_sector_code with None company returns "DEFAULT"
  - Test all new Pydantic models serialize/deserialize (RiskType, ProbabilityBand, etc.)
  </action>
  <verify>
  cd /Users/gorlin/projects/research && uv run pytest tests/test_score_stage.py -v && uv run pyright src/do_uw/models/scoring.py src/do_uw/stages/score/factor_scoring.py --pythonversion 3.12 && wc -l src/do_uw/models/scoring.py
  </verify>
  <done>Scoring models expanded with all SECT7-04 through SECT7-10 output models. 10-factor scoring engine computes factor scores from scoring.json rules. All tests pass. scoring.py under 500 lines.</done>
</task>

<task type="auto">
  <name>Task 2: CRF red flag gates, tier classification, and ScoreStage partial orchestrator</name>
  <files>
    src/do_uw/stages/score/red_flag_gates.py
    src/do_uw/stages/score/tier_classification.py
    src/do_uw/stages/score/__init__.py
    tests/test_score_stage.py
  </files>
  <action>
  Create stages/score/red_flag_gates.py (~250 lines):
  - `def evaluate_red_flag_gates(red_flags_config: dict[str, Any], scoring_config: dict[str, Any], extracted: ExtractedData, company: CompanyProfile | None) -> list[RedFlagResult]`: Evaluate all 11 CRF gates. Load escalation_triggers from red_flags_config and ceilings from scoring_config["critical_red_flag_ceilings"]["ceilings"]. For each trigger:
    1. Normalize CRF ID: map between CRF-01 (red_flags.json) and CRF-001 (scoring.json) formats. Helper: `_normalize_crf_id(raw_id: str) -> str` strips leading zeros from numeric portion for comparison.
    2. Evaluate the trigger condition against extracted data:
       - CRF-01 (Active SCA): Check extracted.litigation for active SCAs
       - CRF-02 (Wells Notice): Check extracted.litigation.sec_enforcement_pipeline for wells_notice
       - CRF-03 (DOJ Investigation): Check extracted.litigation for doj_investigation
       - CRF-04 (Going Concern): Check extracted.financials.audit.going_concern
       - CRF-05 (Restatement): Check extracted.financials.audit for restatements < 12 months
       - CRF-06 (SPAC <$5): Check extracted.market.capital_markets for SPAC + stock price
       - CRF-07 (Short Report): Check extracted.market.short_interest for activist reports < 6 months
       - CRF-08 (>60% Decline): Check extracted.market.stock for decline_from_high > 60%
       - CRF-09 (7-day drop >10%): Check extracted.market.stock for recent drops
       - CRF-10 (30-day drop >15%): Check extracted.market.stock for recent drops
       - CRF-11 (90-day drop >25%): Check extracted.market.stock for recent drops
    3. If condition met: triggered=True, ceiling_applied=max_quality_score, max_tier=max_tier
    4. Collect evidence strings for each trigger
  - `def apply_crf_ceilings(composite_score: float, red_flag_results: list[RedFlagResult]) -> tuple[float, str | None]`: Find lowest triggered ceiling. Return (quality_score = min(composite, lowest_ceiling), binding_ceiling_id). Returns (composite_score, None) if no triggers.
  - Safely handle missing data: if extracted.litigation is None, all litigation CRFs evaluate as not-triggered (data unavailable, not "no issue").

  Create stages/score/tier_classification.py (~300 lines):
  - `def classify_tier(quality_score: float, tier_config: list[dict[str, Any]]) -> TierClassification`: Iterate tier_config (from scoring.json["tiers"]), find the tier whose min_score <= quality_score <= max_score. Return TierClassification with all fields populated from config (tier, score_range_low, score_range_high, probability_range, pricing_multiplier, action). Fallback to NO_TOUCH if no range matches.
  - `def compute_claim_probability(tier: TierClassification, company: CompanyProfile | None, sectors_config: dict[str, Any]) -> ClaimProbability`: Compute claim probability band from tier.probability_range. Parse the percentage range string into low/high floats. Look up industry_base_rate from sectors_config using company sector. Return ClaimProbability with band, range values, and narrative.
  - `def _probability_range_to_band(range_str: str) -> ProbabilityBand`: Parse "<2%" -> LOW, "2-5%" -> MODERATE, "5-10%" -> ELEVATED, "10-15%" -> HIGH, ">20%" -> VERY_HIGH.
  - `def _parse_probability_range(range_str: str) -> tuple[float, float]`: Parse "2-5%" -> (2.0, 5.0), "<2%" -> (0.0, 2.0), ">20%" -> (20.0, 25.0).

  Replace the stub ScoreStage in __init__.py (~250 lines):
  - Keep `name` property returning "score".
  - Keep `validate_input` checking analyze stage COMPLETED.
  - Rewrite `run(self, state)` -- PARTIAL implementation (patterns, allegation, severity, tower added in 06-03/06-04):
    1. `state.mark_stage_running(self.name)`
    2. Load brain config: `loader = ConfigLoader(); brain = loader.load_all()`
    3. Validate state.extracted exists
    4. Step A: Evaluate CRF gates FIRST (per processing_rules): `red_flag_results = evaluate_red_flag_gates(brain.red_flags, brain.scoring, state.extracted, state.company)`
    5. Step B: Score all 10 factors: `factor_scores = score_all_factors(brain.scoring, state.extracted, state.company, brain.sectors)`
    6. Step C: Compute composite score: `total_risk = sum(f.points_deducted for f in factor_scores); composite = 100.0 - total_risk`
    7. Step D: Apply CRF ceilings: `quality_score, binding_id = apply_crf_ceilings(composite, red_flag_results)`
    8. Step E: Classify tier: `tier = classify_tier(quality_score, brain.scoring["tiers"])`
    9. Step F: Compute claim probability: `claim_prob = compute_claim_probability(tier, state.company, brain.sectors)`
    10. Populate state.scoring with ScoringResult(composite_score, quality_score, total_risk_points, factor_scores, red_flags, tier, claim_probability, binding_ceiling_id, calibration_notes=["SECT7-11: All scoring parameters need calibration"])
    11. `state.mark_stage_completed(self.name)`
  - NOTE: patterns_detected, allegation_mapping, risk_type, severity_scenarios, tower_recommendation, red_flag_summary left as None -- populated by 06-03 and 06-04.
  - Log: "Score: quality_score={qs:.1f}, composite={cs:.1f}, tier={tier}, CRF binding={binding_id}"

  Add tests to test_score_stage.py:
  - Test evaluate_red_flag_gates with active SCA -> CRF-01 triggered, ceiling 30
  - Test evaluate_red_flag_gates with going concern -> CRF-04 triggered, ceiling 50
  - Test evaluate_red_flag_gates with no triggers -> all not triggered
  - Test apply_crf_ceilings with two triggers -> lowest ceiling wins
  - Test apply_crf_ceilings with no triggers -> score unchanged
  - Test _normalize_crf_id("CRF-001") and _normalize_crf_id("CRF-01") both normalize
  - Test classify_tier with score 90 -> WIN
  - Test classify_tier with score 45 -> WATCH
  - Test classify_tier with score 5 -> NO_TOUCH
  - Test compute_claim_probability produces correct band
  - Test ScoreStage.run with mocked config and ExtractedData -> state.scoring populated with factor scores, CRF results, tier
  - Verify all files under 500 lines
  </action>
  <verify>
  cd /Users/gorlin/projects/research && uv run pytest tests/test_score_stage.py -v && uv run pyright src/do_uw/stages/score/ --pythonversion 3.12 && uv run ruff check src/do_uw/stages/score/ src/do_uw/models/scoring.py && wc -l src/do_uw/stages/score/*.py src/do_uw/models/scoring.py
  </verify>
  <done>CRF gates evaluate 11 triggers with ceiling application. Tier classification maps quality scores to tiers. ScoreStage orchestrator runs factor scoring + CRF + tier end-to-end. All files under 500 lines, pyright strict clean.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/gorlin/projects/research
# All score tests pass
uv run pytest tests/test_score_stage.py -v
# Type checking clean
uv run pyright src/do_uw/stages/score/ src/do_uw/models/scoring.py --pythonversion 3.12
# Linting clean
uv run ruff check src/do_uw/stages/score/ src/do_uw/models/scoring.py
# All files under 500 lines
wc -l src/do_uw/stages/score/*.py src/do_uw/models/scoring.py
# Full test suite (existing tests not broken)
uv run pytest --tb=short -q
```
</verification>

<success_criteria>
1. ScoringResult model has all SECT7-04 through SECT7-10 output fields
2. 10 factor scores are computed with sub-component breakdowns from scoring.json rules
3. F2 insider amplifier correctly multiplies and caps at max_points
4. 11 CRF gates evaluate with correct ceiling values; lowest ceiling is binding
5. CRF ID normalization handles both CRF-01 and CRF-001 formats
6. Tier classification maps score ranges to 6 tiers correctly
7. Claim probability band computed from tier probability_range
8. ScoreStage orchestrator runs the pipeline: CRF -> factors -> composite -> ceiling -> tier -> probability
9. SECT7-06 (Claims Correlation Scoring) is integrated via historical_lift weights in scoring.json factor rules; flagged for calibration per SECT7-11
10. All files under 500 lines, pyright strict clean, ruff clean
11. Existing 750+ tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-scoring-patterns-risk-synthesis/06-02-SUMMARY.md`
</output>
