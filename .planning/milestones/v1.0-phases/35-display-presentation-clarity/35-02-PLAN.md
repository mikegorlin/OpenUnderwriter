---
phase: 35-display-presentation-clarity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/stages/benchmark/narrative_helpers.py
  - src/do_uw/stages/benchmark/risk_levels.py
  - src/do_uw/stages/benchmark/__init__.py
  - src/do_uw/stages/analyze/check_engine.py
  - src/do_uw/stages/render/sections/sect7_scoring.py
  - src/do_uw/stages/render/sections/sect8_ai_risk.py
  - src/do_uw/stages/render/sections/sect1_helpers.py
  - tests/stages/benchmark/test_narrative_helpers.py
autonomous: true
requirements:
  - CORE-04
  - DATA-14

must_haves:
  truths:
    - "_score_to_risk_level is importable from stages/benchmark/risk_levels.py"
    - "build_thesis_narrative, build_risk_narrative, build_claim_narrative are importable from stages/benchmark/narrative_helpers.py"
    - "check_engine.py propagates content_type into check result dicts"
    - "sect7_scoring.py imports _score_to_risk_level from benchmark/ (not local definition)"
    - "sect8_ai_risk.py imports threat label functions from benchmark/ (not local definition)"
  artifacts:
    - path: "src/do_uw/stages/benchmark/narrative_helpers.py"
      provides: "Narrative builder functions relocated from render/"
      contains: "build_thesis_narrative"
    - path: "src/do_uw/stages/benchmark/risk_levels.py"
      provides: "Score-to-risk-level and threat label functions"
      contains: "_score_to_risk_level"
    - path: "tests/stages/benchmark/test_narrative_helpers.py"
      provides: "Tests for relocated functions"
      contains: "test_score_to_risk_level"
  key_links:
    - from: "src/do_uw/stages/benchmark/__init__.py"
      to: "src/do_uw/stages/benchmark/narrative_helpers.py"
      via: "import in _precompute_narratives"
      pattern: "from do_uw.stages.benchmark.narrative_helpers import"
    - from: "src/do_uw/stages/render/sections/sect7_scoring.py"
      to: "src/do_uw/stages/benchmark/risk_levels.py"
      via: "import risk level function"
      pattern: "from do_uw.stages.benchmark.risk_levels import"
---

<objective>
Move analytical logic out of render/ into benchmark/ and propagate check content_type through to results.

Purpose: Zero analytical logic in render/ is a phase success criterion. Functions like `_score_to_risk_level()`, `_score_to_threat_label()`, and the narrative builders currently live in render/ but are analytical. Moving them to benchmark/ establishes proper stage boundaries. Additionally, propagating content_type through check_engine results enables knowledge-driven display dispatch in later plans.

Output: narrative_helpers.py and risk_levels.py in benchmark/, updated imports in render/ files, content_type in check results, tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/do_uw/stages/benchmark/__init__.py
@src/do_uw/stages/render/sections/sect7_scoring.py
@src/do_uw/stages/render/sections/sect8_ai_risk.py
@src/do_uw/stages/render/sections/sect1_helpers.py
@src/do_uw/stages/analyze/check_engine.py
@.planning/phases/35-display-presentation-clarity/35-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate analytical functions from render/ to benchmark/</name>
  <files>src/do_uw/stages/benchmark/narrative_helpers.py, src/do_uw/stages/benchmark/risk_levels.py, src/do_uw/stages/render/sections/sect7_scoring.py, src/do_uw/stages/render/sections/sect8_ai_risk.py, src/do_uw/stages/render/sections/sect1_helpers.py, src/do_uw/stages/benchmark/__init__.py, tests/stages/benchmark/test_narrative_helpers.py</files>
  <action>
**Create `src/do_uw/stages/benchmark/risk_levels.py`:**
Move these functions from their current locations:
- `_score_to_risk_level(score: float) -> str` from `sect7_scoring.py` — maps quality score to risk level via thresholds (86/71/51/26)
- `_score_to_threat_label(score: float) -> str` from `sect8_ai_risk.py` — maps AI risk score to threat label (70/40 thresholds)
- `_dim_score_threat(score: float) -> str` from `sect8_ai_risk.py` — maps dimension score to threat level

Make all three functions public (drop underscore prefix since they're now a proper API): `score_to_risk_level`, `score_to_threat_label`, `dim_score_threat`.

**Create `src/do_uw/stages/benchmark/narrative_helpers.py`:**
Move from `sect1_helpers.py`:
- `build_thesis_narrative(state: AnalysisState) -> str`
- `build_risk_narrative(inherent_risk: InherentRiskBaseline, state: AnalysisState) -> str`
- `build_claim_narrative(state: AnalysisState) -> str`

These functions are already called from BenchmarkStage._precompute_narratives() via lazy import. Moving them makes the import direct.

**Update render/ files to import from benchmark/:**
- `sect7_scoring.py`: Replace local `_score_to_risk_level` with import from `do_uw.stages.benchmark.risk_levels`. Keep a backward-compat alias `_score_to_risk_level = score_to_risk_level` for any other callers.
- `sect8_ai_risk.py`: Replace local `_score_to_threat_label` and `_dim_score_threat` with imports from `do_uw.stages.benchmark.risk_levels`.
- `sect1_helpers.py`: Replace the moved functions with re-imports from `do_uw.stages.benchmark.narrative_helpers` — keep same public names for backward compat.

**Update `BenchmarkStage._precompute_narratives()`:**
- Replace lazy imports `from do_uw.stages.render.sections.sect1_helpers import ...` with direct imports `from do_uw.stages.benchmark.narrative_helpers import ...`
- Replace lazy import `from do_uw.stages.render.sections.sect7_scoring import _score_to_risk_level` with `from do_uw.stages.benchmark.risk_levels import score_to_risk_level`

**Tests:**
Write `tests/stages/benchmark/test_narrative_helpers.py`:
- test_score_to_risk_level: 90->EXCELLENT, 75->GOOD, 55->MODERATE, 35->CONCERNING, 15->SEVERE
- test_score_to_threat_label: 80->HIGH, 50->MEDIUM, 20->LOW
- test_dim_score_threat: Same threshold checks
- test_build_thesis_narrative: Returns non-empty string for a minimal state
- test_build_risk_narrative: Returns non-empty string
  </action>
  <verify>
Run: `cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/benchmark/test_narrative_helpers.py -v` — all tests pass.
Run: `cd /Users/gorlin/projects/UW/do-uw && uv run python -c "from do_uw.stages.benchmark.risk_levels import score_to_risk_level; print(score_to_risk_level(75))"` — prints risk level.
Run: `cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/ -k 'sect7 or sect8 or sect1' --no-header -q 2>&1 | tail -5` — existing render tests still pass (imports still work via re-export).
  </verify>
  <done>All analytical functions relocated from render/ to benchmark/. Render files import from benchmark/. Backward compat re-exports maintained. BenchmarkStage uses direct imports.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate content_type through check engine results</name>
  <files>src/do_uw/stages/analyze/check_engine.py</files>
  <action>
In `check_engine.py`, after the content_type dispatch (around line 108-114), ensure the result dict includes `content_type` as a field. Currently the check_engine dispatches by content_type but does NOT store it on the result dict.

Find the section where `result` is produced (after evaluate_check/evaluate_management_display/evaluate_inference_pattern calls) and add:

```python
result["content_type"] = content_type
```

This must be added to ALL code paths that produce a check result — after the if/elif/else block for content_type dispatch and before the result is appended to check_results.

Also ensure the `check_id` field is on every result (it likely already is, but verify).

This is a small, surgical change. No new tests needed — existing check engine tests validate the result dict structure and should still pass. But verify by running the existing test suite.
  </action>
  <verify>
Run: `cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/analyze/test_check_engine.py -v` — all pass.
Run: `cd /Users/gorlin/projects/UW/do-uw && uv run python -c "
from do_uw.stages.analyze.check_engine import execute_checks
# Quick smoke test: content_type should be in result keys
print('Import OK')
"` — imports cleanly.
  </verify>
  <done>Every check result dict includes a `content_type` field (EVALUATIVE_CHECK, MANAGEMENT_DISPLAY, or INFERENCE_PATTERN). RENDER can now dispatch display formatting by content_type.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/stages/benchmark/ -v` — all pass
- `uv run pytest tests/stages/analyze/test_check_engine.py -v` — all pass
- `uv run pytest tests/ -k 'sect7 or sect8 or sect1' -v` — existing render tests pass (backward compat)
- `grep -r "_score_to_risk_level" src/do_uw/stages/render/` — should show only import/alias, no local definition
- `grep -r "content_type" src/do_uw/stages/analyze/check_engine.py` — should show result["content_type"] = content_type
</verification>

<success_criteria>
Analytical functions live in benchmark/, not render/. Check results carry content_type for display dispatch. All existing tests pass via backward compat re-exports.
</success_criteria>

<output>
After completion, create `.planning/phases/35-display-presentation-clarity/35-02-SUMMARY.md`
</output>
