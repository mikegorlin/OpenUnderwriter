---
phase: 35-display-presentation-clarity
plan: 07
type: execute
wave: 4
depends_on: ["35-01", "35-05", "35-06"]
files_modified:
  - src/do_uw/stages/analyze/section_assessments.py
  - src/do_uw/stages/render/sections/sect3_financial.py
  - src/do_uw/stages/render/sections/sect2_company_details.py
  - tests/stages/render/test_zero_analytical_logic.py
  - tests/stages/render/test_render_integration.py
autonomous: true
requirements:
  - CORE-04
  - OUT-02

must_haves:
  truths:
    - "grep for threshold comparisons in render/ returns zero results (success criterion 4)"
    - "No scoring logic outside stages/score/"
    - "No data acquisition outside stages/acquire/"
    - "Pipeline still has exactly 7 stages: RESOLVE, ACQUIRE, EXTRACT, ANALYZE, SCORE, BENCHMARK, RENDER"
    - "PDF output generates without error from a test fixture state"
    - "Word output generates without error from a test fixture state"
    - "All pre-existing tests pass"
  artifacts:
    - path: "tests/stages/render/test_zero_analytical_logic.py"
      provides: "Automated audit that render/ has no analytical logic"
      contains: "test_no_threshold_comparisons_in_render"
    - path: "tests/stages/render/test_render_integration.py"
      provides: "End-to-end render test with fixture state"
      contains: "test_all_formats_render"
  key_links:
    - from: "src/do_uw/stages/analyze/section_assessments.py"
      to: "src/do_uw/stages/render/sections/sect2_company_details.py"
      via: "high_risk_jurisdiction: concerns in SectionDensity read by render"
      pattern: "high_risk_jurisdiction:"
    - from: "tests/stages/render/test_zero_analytical_logic.py"
      to: "src/do_uw/stages/render/"
      via: "AST analysis of render files"
      pattern: "ast.parse"
---

<objective>
Final cleanup: remove remaining analytical logic from render/, verify zero-analytical-logic constraint, and run end-to-end render integration tests.

Purpose: Phase 35 success criterion 4 requires "grep for threshold comparisons in render/ returns zero results." This plan eliminates the remaining analytical logic identified in research (sect3_financial.py local fallback, sect2_company_details.py jurisdiction classification), then creates automated verification tests. Also ensures the full pipeline still produces all three output formats correctly.

Output: Cleaned render/ files, automated zero-analytical-logic test, end-to-end render integration test.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/do_uw/stages/analyze/section_assessments.py
@src/do_uw/stages/render/sections/sect3_financial.py
@src/do_uw/stages/render/sections/sect2_company_details.py
@.planning/phases/35-display-presentation-clarity/35-05-SUMMARY.md
@.planning/phases/35-display-presentation-clarity/35-06-SUMMARY.md
@.planning/phases/35-display-presentation-clarity/35-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove remaining analytical logic from render/</name>
  <files>src/do_uw/stages/analyze/section_assessments.py, src/do_uw/stages/render/sections/sect3_financial.py, src/do_uw/stages/render/sections/sect2_company_details.py</files>
  <action>
Per the research findings, these render/ files still contain analytical logic:

1. **`sect3_financial.py` lines ~218-221: `_is_financial_clean()` local fallback**
   - Currently: `if state.analysis.financial_clean is None: ... local computation`
   - Fix: Remove the local computation. Use `state.analysis.section_densities.get("financial")` as primary, fall back to `state.analysis.financial_clean` boolean, and if both are None default to CLEAN (don't recompute in render).
   - The three-tier density from Plan 01 is always computed in ANALYZE, so the None case should no longer occur. But the fallback to CLEAN is safer than local recomputation.

2. **`sect2_company_details.py`: `_HIGH_RISK_JURISDICTIONS` set and `_risk_flag()` function**
   - Currently: `_HIGH_RISK_JURISDICTIONS` hardcoded set in render/ classifies jurisdictions as high-risk; `_risk_flag()` checks each jurisdiction against this set.
   - **Destination file:** `src/do_uw/stages/analyze/section_assessments.py`
   - **Where the set moves:** Add `_HIGH_RISK_JURISDICTIONS` set to `section_assessments.py` (alongside existing `_is_governance_clean` etc. helpers). This file already classifies section risk; jurisdiction risk is a natural fit.
   - **Where the classification result is stored:** In the `SectionDensity.concerns` list for the "company" section density. Add a new helper `_classify_jurisdiction_risk(state) -> list[str]` in `section_assessments.py` that:
     1. Reads `state.company.geographic_footprint` (list of SourcedValue[dict])
     2. For each geography, checks if name/region matches any `_HIGH_RISK_JURISDICTIONS` entry (case-insensitive substring match, same logic currently in `_risk_flag()`)
     3. Returns a list of concern strings, e.g. `["high_risk_jurisdiction:China", "high_risk_jurisdiction:Russia"]`
   - **Integration:** In `_compute_company_density()` (added by Plan 01 Task 2), call `_classify_jurisdiction_risk(state)` and append results to `SectionDensity.concerns`. If any high-risk jurisdictions found, escalate density to at least ELEVATED.
   - **How render reads it:** In `sect2_company_details.py`, replace `_HIGH_RISK_JURISDICTIONS` set and `_risk_flag()` function with a read from pre-computed concerns:
     ```python
     company_density = state.analysis.section_densities.get("company")
     high_risk_names = set()
     if company_density:
         for c in company_density.concerns:
             if c.startswith("high_risk_jurisdiction:"):
                 high_risk_names.add(c.split(":", 1)[1].lower())
     ```
     Then in the geography rendering loop, check `if name.lower() in high_risk_names` to apply red styling. No analytical logic — just reading a pre-computed set from state.
   - **Remove from render/:** Delete `_HIGH_RISK_JURISDICTIONS` set, delete `_risk_flag()` function, delete any direct jurisdiction classification logic.

3. **Scan for any remaining threshold comparisons:**
   - `grep -rn "score > \|score < \|score >= \|score <= \|> [0-9]\|< [0-9]" src/do_uw/stages/render/` — fix any remaining instances
   - Exclude: pure display formatting (e.g., `colspan > 0` or CSS-related comparisons)
   - Any comparison against a numerical threshold that determines content/styling based on a score should use a pre-computed classification

4. **Verify imports:**
   - After Plan 02, sect7_scoring.py and sect8_ai_risk.py should already import from benchmark/
   - After Plan 03, narrative generation should be from pre_computed_narratives
   - Verify these imports are correct and no local analytical functions remain
  </action>
  <verify>
Run: `cd /Users/gorlin/projects/UW/do-uw && grep -rn "def _is_financial_clean\|def _is_high_risk_jurisdiction\|def _score_to_risk_level\|def _score_to_threat_label\|def _dim_score_threat\|_HIGH_RISK_JURISDICTIONS\|def _risk_flag" src/do_uw/stages/render/` — should return ZERO results (no local definitions or hardcoded sets).
Run: `cd /Users/gorlin/projects/UW/do-uw && grep -rn "_HIGH_RISK_JURISDICTIONS\|_classify_jurisdiction_risk" src/do_uw/stages/analyze/section_assessments.py` — should show the set and classifier function exist in ANALYZE.
Run: `cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/render/ tests/stages/analyze/test_section_assessments.py -v --no-header -q 2>&1 | tail -5` — all pass.
  </verify>
  <done>All analytical logic removed from render/. No threshold comparisons, no score evaluations, no classification logic. RENDER is purely formatting.</done>
</task>

<task type="auto">
  <name>Task 2: Create zero-analytical-logic audit test and render integration test</name>
  <files>tests/stages/render/test_zero_analytical_logic.py, tests/stages/render/test_render_integration.py</files>
  <action>
**`tests/stages/render/test_zero_analytical_logic.py`** (~150 lines):

Create an automated test that verifies Phase 35 success criterion 4: "grep for threshold comparisons in render/ returns zero results."

1. **`test_no_threshold_comparisons_in_render()`:**
   - Use Python's `ast` module to parse all .py files in `src/do_uw/stages/render/`
   - Walk AST looking for `ast.Compare` nodes where:
     - One operand references a "score", "threshold", "risk", "level" variable
     - The comparator is a numeric constant (not a string, not a len() check)
   - Allow: `len(x) > 0`, `x is not None`, string comparisons, enum checks
   - Disallow: `score > 86`, `risk_level >= 3`, `threshold < 5.0`
   - Report any violations with file + line number

2. **`test_no_scoring_functions_defined_in_render()`:**
   - Scan all .py files in render/ for function definitions
   - Check that no function name contains "score_to", "threat_label", "is_financial_clean", "is_high_risk"
   - Allow re-imports/aliases (which just import from benchmark/)

3. **`test_no_anthropic_imports_in_render()`:**
   - Verify no render/ file imports anthropic or instructor (LLM calls belong in BENCHMARK)
   - Exception: if a render file imports for type-checking only (TYPE_CHECKING block)

4. **`test_pipeline_stages_count()`:**
   - `from do_uw.models.state import PIPELINE_STAGES`
   - Assert exactly 7 stages (CORE-04)

**`tests/stages/render/test_render_integration.py`** (~200 lines):

1. **Create a comprehensive test fixture:**
   - Build a minimal but complete AnalysisState with:
     - company profile, extracted data, analysis results, scoring, benchmark
     - section_densities populated (mix of CLEAN, ELEVATED, CRITICAL)
     - pre_computed_narratives populated
     - check_results with content_type field
   - Use the SAMPLE ticker pattern from existing tests

2. **`test_all_formats_render(tmp_path)`:**
   - Call RenderStage.run() with the fixture state and tmp_path
   - Assert Word file exists and is valid (non-empty)
   - Assert Markdown file exists and contains section headings
   - Assert PDF generation attempted (may skip if Playwright not installed — xfail)

3. **`test_word_has_density_indicators(tmp_path)`:**
   - Render Word doc from fixture with CRITICAL financial section
   - Read generated .docx
   - Assert "CRITICAL RISK" text appears in document

4. **`test_markdown_has_narratives(tmp_path)`:**
   - Render Markdown from fixture with pre_computed_narratives
   - Read generated .md
   - Assert narrative text appears

5. **`test_html_context_has_density_and_narratives()`:**
   - Call build_html_context with fixture state
   - Assert context has `densities` and `narratives` keys
   - Assert `densities` has correct section levels
  </action>
  <verify>
Run: `cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/render/test_zero_analytical_logic.py tests/stages/render/test_render_integration.py -v` — all pass.
Run: `cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/ -v --no-header -q 2>&1 | tail -10` — full test suite passes.
  </verify>
  <done>Automated zero-analytical-logic audit passes. End-to-end render integration produces all three formats. Phase 35 success criteria verified.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/stages/render/test_zero_analytical_logic.py -v` — all pass (zero analytical logic in render/)
- `uv run pytest tests/stages/render/test_render_integration.py -v` — all pass (all formats render)
- `uv run pytest tests/ --no-header -q` — full suite passes
- `grep -rn "def _is_financial_clean\|def _score_to_risk_level" src/do_uw/stages/render/` — zero results
- Pipeline has exactly 7 stages
</verification>

<success_criteria>
Phase 35 success criteria verified:
1. Section density from analysis: Three-tier CLEAN/ELEVATED/CRITICAL computed in ANALYZE
2. Narrative generation in BENCHMARK: All narratives pre-computed and stored in state
3. Knowledge-driven display: Check content_type drives display format
4. Zero analytical logic in render: Automated test proves this
5. Pre-computed values: All density, narratives, risk levels in state; RENDER is purely formatting
All requirement IDs covered across plans 01-07.
</success_criteria>

<output>
After completion, create `.planning/phases/35-display-presentation-clarity/35-07-SUMMARY.md`
</output>
