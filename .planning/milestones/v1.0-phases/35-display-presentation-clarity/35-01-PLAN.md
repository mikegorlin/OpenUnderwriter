---
phase: 35-display-presentation-clarity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/models/state.py
  - src/do_uw/models/density.py
  - src/do_uw/stages/analyze/section_assessments.py
  - tests/stages/analyze/test_section_assessments.py
autonomous: true
requirements:
  - CORE-04

must_haves:
  truths:
    - "DensityLevel enum has exactly three values: CLEAN, ELEVATED, CRITICAL"
    - "SectionDensity model has level and subsection_overrides fields"
    - "AnalysisResults has section_densities dict and pre_computed_narratives dict"
    - "section_assessments.py computes three-tier density (not boolean) per section"
    - "Existing boolean _clean fields are preserved for backward compat but deprecated"
  artifacts:
    - path: "src/do_uw/models/density.py"
      provides: "DensityLevel enum, SectionDensity model, PreComputedNarratives model"
      contains: "class DensityLevel"
    - path: "src/do_uw/stages/analyze/section_assessments.py"
      provides: "Three-tier density computation"
      contains: "DensityLevel.ELEVATED"
    - path: "tests/stages/analyze/test_section_assessments.py"
      provides: "Tests for three-tier density"
      contains: "DensityLevel.CRITICAL"
  key_links:
    - from: "src/do_uw/stages/analyze/section_assessments.py"
      to: "src/do_uw/models/density.py"
      via: "imports DensityLevel, SectionDensity"
      pattern: "from do_uw.models.density import"
    - from: "src/do_uw/models/state.py"
      to: "src/do_uw/models/density.py"
      via: "AnalysisResults.section_densities field"
      pattern: "section_densities"
---

<objective>
Add three-tier density assessment model and upgrade section assessments from boolean to CLEAN/ELEVATED/CRITICAL.

Purpose: Foundation for density-driven rendering. Every downstream plan depends on DensityLevel being available on state. The existing boolean `*_clean` fields become deprecated wrappers, while `section_densities` provides granular per-section and per-subsection density levels.

Output: DensityLevel enum, SectionDensity model, PreComputedNarratives model, upgraded section_assessments.py with three-tier logic, tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/do_uw/models/state.py
@src/do_uw/stages/analyze/section_assessments.py
@.planning/phases/35-display-presentation-clarity/35-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create density models and extend AnalysisResults</name>
  <files>src/do_uw/models/density.py, src/do_uw/models/state.py</files>
  <action>
Create `src/do_uw/models/density.py` with:

1. `DensityLevel(StrEnum)` with values CLEAN, ELEVATED, CRITICAL — matches the user's locked decision on three tiers.

2. `SectionDensity(BaseModel)` with:
   - `level: DensityLevel = DensityLevel.CLEAN` — section-level baseline
   - `subsection_overrides: dict[str, DensityLevel] = Field(default_factory=dict)` — per-subsection overrides keyed by subsection ID (e.g., "4.1_people_risk": CRITICAL)
   - `concerns: list[str] = Field(default_factory=list)` — specific concerns driving density (for ELEVATED amber indicators)
   - `critical_evidence: list[str] = Field(default_factory=list)` — evidence chains for CRITICAL deep dives

3. `PreComputedNarratives(BaseModel)` with:
   - `executive_summary: str | None = None`
   - `company: str | None = None`
   - `financial: str | None = None`
   - `market: str | None = None`
   - `governance: str | None = None`
   - `litigation: str | None = None`
   - `scoring: str | None = None`
   - `ai_risk: str | None = None`
   - `meeting_prep_questions: list[str] = Field(default_factory=list)` — typed closure default factory per project pattern

Then modify `src/do_uw/models/state.py` AnalysisResults:
- Add `section_densities: dict[str, SectionDensity] = Field(default_factory=dict, description="Three-tier density assessment per section, computed in ANALYZE")` — import SectionDensity from density.py
- Add `pre_computed_narratives: PreComputedNarratives | None = Field(default=None, description="LLM-generated narratives pre-computed in BENCHMARK")` — import PreComputedNarratives from density.py
- Keep existing boolean `*_clean` fields as-is (backward compat) but add `json_schema_extra={"deprecated": True}` to each — per project pattern from [17-04]
  </action>
  <verify>
Run: `cd /Users/gorlin/projects/UW/do-uw && uv run python -c "from do_uw.models.density import DensityLevel, SectionDensity, PreComputedNarratives; print('OK:', list(DensityLevel))"` — should print CLEAN, ELEVATED, CRITICAL.
Run: `cd /Users/gorlin/projects/UW/do-uw && uv run python -c "from do_uw.models.state import AnalysisResults; r = AnalysisResults(); print('densities:', r.section_densities, 'narratives:', r.pre_computed_narratives)"` — should print empty dict and None.
  </verify>
  <done>DensityLevel, SectionDensity, PreComputedNarratives models exist. AnalysisResults has section_densities and pre_computed_narratives fields. Existing *_clean fields preserved with deprecated marker.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade section_assessments.py to three-tier density</name>
  <files>src/do_uw/stages/analyze/section_assessments.py, tests/stages/analyze/test_section_assessments.py</files>
  <action>
Rewrite `compute_section_assessments()` in `section_assessments.py` to:

1. Still compute boolean `*_clean` fields (backward compat) using existing logic.
2. Additionally compute `section_densities` dict with three-tier assessment per section.

Logic for three-tier (per the user's locked decision):
- **CLEAN**: No concerns. Same as existing `*_clean = True`.
- **ELEVATED**: Some concerns but not critical. E.g., governance has CEO/Chair duality but independence is OK. Financial has grey zone scores but no distress. Market has moderate short interest (5-10%) but no severe drops. Litigation has resolved cases but no active ones.
- **CRITICAL**: Severe issues. Active litigation, distress zone models, SEC enforcement beyond comment letters, activist investors with active campaigns, multiple red flags converging.

For each section (governance, litigation, financial, market, company, scoring):
- Create a helper `_compute_*_density(state) -> SectionDensity` that returns level + concerns list + critical_evidence list.
- The helper checks multiple conditions and escalates from CLEAN -> ELEVATED -> CRITICAL.

Per-subsection overrides: For governance, check each subsection independently:
- "4.1_people_risk": Check executive forensics, departures, overboarding
- "4.2_structural_governance": Check board independence, duality, committees
- "4.3_transparency": Check audit quality, restatements, comment letters
- "4.4_activist": Check activist presence

Keep the file under 500 lines. If it grows too large, split into section_assessments.py + section_density_helpers.py.

Write tests in `tests/stages/analyze/test_section_assessments.py`:
- Test CLEAN scenario (all sections clean)
- Test ELEVATED governance (CEO/Chair duality but otherwise fine)
- Test CRITICAL litigation (active SCA + enforcement action)
- Test CRITICAL financial (distress zone + going concern)
- Test per-subsection overrides work
- Test backward compat: boolean `*_clean` fields still set correctly
  </action>
  <verify>
Run: `cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/analyze/test_section_assessments.py -v` — all tests pass.
Run: `cd /Users/gorlin/projects/UW/do-uw && uv run ruff check src/do_uw/models/density.py src/do_uw/stages/analyze/section_assessments.py` — no lint errors.
  </verify>
  <done>section_assessments.py produces DensityLevel.CLEAN/ELEVATED/CRITICAL per section with per-subsection overrides. Boolean backward compat preserved. All tests pass.</done>
</task>

</tasks>

<verification>
- `uv run python -c "from do_uw.models.density import DensityLevel; assert len(DensityLevel) == 3"`
- `uv run pytest tests/stages/analyze/test_section_assessments.py -v` — all pass
- `uv run ruff check src/do_uw/models/density.py src/do_uw/models/state.py src/do_uw/stages/analyze/section_assessments.py`
- `wc -l src/do_uw/stages/analyze/section_assessments.py` — under 500 lines
</verification>

<success_criteria>
Three-tier DensityLevel (CLEAN/ELEVATED/CRITICAL) replaces boolean clean fields as the primary density signal. SectionDensity model supports per-subsection overrides. PreComputedNarratives model ready for Plan 03. All existing tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/35-display-presentation-clarity/35-01-SUMMARY.md`
</output>
