---
phase: 35-display-presentation-clarity
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/stages/analyze/section_assessments.py
  - src/do_uw/stages/analyze/section_density_helpers.py
autonomous: true
requirements: [CORE-04]
gap_closure: true

must_haves:
  truths:
    - "section_assessments.py is under 500 lines"
    - "section_density_helpers.py contains jurisdiction risk and company density logic"
    - "All 32 existing section_assessments tests still pass without modification"
  artifacts:
    - path: "src/do_uw/stages/analyze/section_assessments.py"
      provides: "Three-tier density computation (CLEAN/ELEVATED/CRITICAL)"
      contains: "compute_section_assessments"
    - path: "src/do_uw/stages/analyze/section_density_helpers.py"
      provides: "_HIGH_RISK_JURISDICTIONS, _classify_jurisdiction_risk, _compute_company_density"
      contains: "_HIGH_RISK_JURISDICTIONS"
  key_links:
    - from: "src/do_uw/stages/analyze/section_assessments.py"
      to: "src/do_uw/stages/analyze/section_density_helpers.py"
      via: "from do_uw.stages.analyze.section_density_helpers import _compute_company_density"
      pattern: "section_density_helpers"
---

<objective>
Split section_assessments.py (561 lines) to comply with the project's 500-line limit.

Purpose: Close the anti-pattern gap -- CLAUDE.md requires "No source file over 500 lines." Plan 01 explicitly anticipated this split and Plan 07 pushed the file 61 lines over by adding jurisdiction classification logic.

Output: Two files under 500 lines each, all tests passing, no import changes needed for consumers.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/do_uw/stages/analyze/section_assessments.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract jurisdiction risk and company density into section_density_helpers.py</name>
  <files>
    src/do_uw/stages/analyze/section_density_helpers.py
    src/do_uw/stages/analyze/section_assessments.py
  </files>
  <action>
1. Create `src/do_uw/stages/analyze/section_density_helpers.py` containing:
   - Module docstring explaining this is the split-out density helpers
   - Imports: `from do_uw.models.density import DensityLevel, SectionDensity` and `from do_uw.models.state import AnalysisState`
   - `_HIGH_RISK_JURISDICTIONS` set (lines 473-476 of current file)
   - `_classify_jurisdiction_risk(state: AnalysisState) -> list[str]` function (lines 479-508)
   - `_compute_company_density(state: AnalysisState) -> SectionDensity` function (lines 511-537)

2. In section_assessments.py:
   - Remove the `_HIGH_RISK_JURISDICTIONS` set, `_classify_jurisdiction_risk()`, and `_compute_company_density()` functions (lines 466-537, approximately 72 lines)
   - Add import: `from do_uw.stages.analyze.section_density_helpers import _compute_company_density`
   - The existing call to `_compute_company_density(state)` in `compute_section_assessments()` continues to work via the import
   - Verify the remaining file is under 500 lines (561 - 72 = ~489 lines)

3. Do NOT change any public APIs. The only consumer of section_assessments.py is `stages/analyze/__init__.py` which imports `compute_section_assessments` -- that function signature and behavior remain identical.

4. Do NOT modify any test files -- if the split is correct, all 32 tests pass unchanged because they import `compute_section_assessments` which internally calls the now-imported helper.
  </action>
  <verify>
`wc -l src/do_uw/stages/analyze/section_assessments.py` -- must be under 500
`wc -l src/do_uw/stages/analyze/section_density_helpers.py` -- must be under 500
`uv run pytest tests/stages/analyze/test_section_assessments.py -x -q` -- all 32 tests pass
`uv run pyright src/do_uw/stages/analyze/section_density_helpers.py src/do_uw/stages/analyze/section_assessments.py` -- no type errors
  </verify>
  <done>
section_assessments.py is under 500 lines. section_density_helpers.py contains _HIGH_RISK_JURISDICTIONS, _classify_jurisdiction_risk(), and _compute_company_density(). All 32 existing tests pass without modification. No public API changes.
  </done>
</task>

</tasks>

<verification>
1. `wc -l src/do_uw/stages/analyze/section_assessments.py` < 500
2. `uv run pytest tests/stages/analyze/test_section_assessments.py -x -q` -- all 32 pass
3. `uv run pytest tests/stages/render/test_zero_analytical_logic.py -x -q` -- AST audit still passes
4. `uv run pyright src/do_uw/stages/analyze/section_density_helpers.py` -- clean
</verification>

<success_criteria>
- section_assessments.py under 500 lines (project anti-pattern rule satisfied)
- No test modifications needed
- No consumer import changes needed
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/35-display-presentation-clarity/35-09-SUMMARY.md`
</output>
