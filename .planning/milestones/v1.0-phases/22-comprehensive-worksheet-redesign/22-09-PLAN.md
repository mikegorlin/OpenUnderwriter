---
phase: 22-comprehensive-worksheet-redesign
plan: 09
type: execute
wave: 3
depends_on: ["22-01"]
files_modified:
  - src/do_uw/dashboard/state_api.py
  - src/do_uw/dashboard/state_api_ext.py
  - src/do_uw/templates/dashboard/index.html
  - src/do_uw/templates/dashboard/section.html
  - src/do_uw/templates/dashboard/partials/_summary_card.html
  - src/do_uw/templates/dashboard/partials/_finding_detail.html
  - src/do_uw/templates/dashboard/partials/_peer_comparison.html
  - src/do_uw/templates/dashboard/partials/_meeting_prep.html
autonomous: true

must_haves:
  truths:
    - "Dashboard shows rich data matching Word/Markdown content quality"
    - "No sections showing empty or N/A content when state has data"
    - "Drill-down navigation works with complete data"
    - "Summary cards reflect actual risk signals from LLM extraction"
  artifacts:
    - path: "src/do_uw/dashboard/state_api.py"
      provides: "Dashboard data extraction from AnalysisState"
      contains: "get_section_data"
    - path: "src/do_uw/dashboard/state_api_ext.py"
      provides: "Extended dashboard data extraction (litigation, scoring, AI risk)"
      contains: "get_litigation_data"
    - path: "src/do_uw/templates/dashboard/index.html"
      provides: "Dashboard index page with summary cards and charts"
      contains: "summary-card"
  key_links:
    - from: "state_api.py"
      to: "state.extracted + state.scoring"
      via: "Same AnalysisState used by Word/Markdown renderers"
    - from: "index.html"
      to: "state_api.py"
      via: "FastAPI route -> template rendering"
---

<objective>
Update dashboard state API and templates to show rich data from LLM extraction, matching the Word/Markdown redesign.

Purpose: The dashboard reads from the same AnalysisState but has its own data extraction layer (state_api.py) and HTML templates. These need updating to show the same rich data as the redesigned Word/Markdown output. The dashboard's interactive features (charts, drill-down) should benefit from the richer data.

Output: Updated state_api.py + new split + updated dashboard templates
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/do_uw/dashboard/state_api.py
@src/do_uw/dashboard/app.py
@src/do_uw/templates/dashboard/index.html
@src/do_uw/templates/dashboard/section.html
@.planning/phases/22-comprehensive-worksheet-redesign/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dashboard state API for rich data</name>
  <files>
    src/do_uw/dashboard/state_api.py
    src/do_uw/dashboard/state_api_ext.py
  </files>
  <action>
Rewrite state_api.py (currently 417 lines) to extract rich data from AnalysisState, and create state_api_ext.py for 500-line compliance.

The dashboard state API is a parallel data extraction path from AnalysisState (similar to md_renderer_helpers.py). It needs the same enrichment to show complete data.

**state_api.py** (summary + sections 1-4):
1. **get_summary(state)**: Return dict with:
   - Company name, ticker, tier, composite score
   - Key negatives (top 5) with severity and source
   - Key positives (top 5)
   - Risk signals count by severity
   - Thesis narrative text

2. **get_section_data(state, section_id)**: Dispatch to per-section extractors:
   - Section 1: Same data as extract_exec_summary
   - Section 2: Company identity, business description, revenue segments, geo footprint
   - Section 3: Financial tables, distress indicators, audit risk, debt structure
   - Section 4: Stock drops, insider trades, short interest, earnings guidance

3. **get_company_data(state)**: Rich company profile for header/sidebar

4. **get_financial_data(state)**: Complete financial data for charts and drill-down

**state_api_ext.py** (NEW -- sections 5-8 + meeting prep):
5. **get_governance_data(state)**: Board composition, compensation, ownership, governance flags
6. **get_litigation_data(state)**: Cases, enforcement, defense, contingencies
7. **get_scoring_data(state)**: Factor scores, red flags, patterns, allegation mapping
8. **get_ai_risk_data(state)**: AI risk assessment with sub-dimensions
9. **get_meeting_prep_data(state)**: Data-driven questions with categories

Wire: state_api.py imports and delegates to state_api_ext.py for sections 5+.

Key differences from md_renderer_helpers:
- Dashboard data often needs slightly different format (chart-ready dicts, htmx-compatible)
- Include `css_class` fields for risk-level styling in templates
- Include `detail_url` for drill-down links

Both files under 500 lines.
  </action>
  <verify>
Run `cd /Users/gorlin/projects/research && python -m pytest tests/test_dashboard_state_api.py tests/test_dashboard.py -x -q` -- tests pass.
Run `cd /Users/gorlin/projects/research && python -m pyright src/do_uw/dashboard/state_api.py src/do_uw/dashboard/state_api_ext.py` -- no type errors.
Verify both under 500 lines: `wc -l src/do_uw/dashboard/state_api.py src/do_uw/dashboard/state_api_ext.py`
  </verify>
  <done>
Dashboard state API extracts rich data from AnalysisState matching Word/Markdown content quality. Split into two files for 500-line compliance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update dashboard templates for rich data</name>
  <files>
    src/do_uw/templates/dashboard/index.html
    src/do_uw/templates/dashboard/section.html
    src/do_uw/templates/dashboard/partials/_summary_card.html
    src/do_uw/templates/dashboard/partials/_finding_detail.html
    src/do_uw/templates/dashboard/partials/_peer_comparison.html
    src/do_uw/templates/dashboard/partials/_meeting_prep.html
  </files>
  <action>
Update dashboard HTML templates to present the richer data from the updated state API.

**index.html** (currently 207 lines):
1. Summary cards: Show actual key findings (not generic placeholders)
2. Risk signals section: Count of critical/elevated/moderate signals with drill-down links
3. Thesis narrative displayed as lead paragraph
4. Score breakdown visible on landing page (10-factor mini-bars)
5. Active red flags listed with severity indicators
6. Quick-access links to each section with data status (has_data badge)

**section.html** (currently 70 lines):
1. Section detail view: Rich tables matching Word/Markdown content
2. Source citations in tooltip or expandable detail
3. Risk level styling using DaisyUI badge/alert components
4. Data tables with responsive columns (collapse on mobile)

**Partials updates:**

**_summary_card.html** (28 lines):
- Show actual finding text, not just count
- Include severity indicator and source reference
- Click to expand shows full evidence

**_finding_detail.html** (61 lines):
- Show contributing checks with their values and sources
- Include allegation theory mapping
- Scoring impact visible

**_peer_comparison.html** (64 lines):
- Show more metrics with actual peer data
- Visual bars for relative position
- Rank within peer group

**_meeting_prep.html** (102 lines):
- Organize questions by category (tabs or accordion)
- Show source finding that prompted each question
- Priority indicators
- Printable layout option

Use DaisyUI 5 + Tailwind v4 classes (already available via CDN). No new dependencies.
  </action>
  <verify>
Run `cd /Users/gorlin/projects/research && python -m pytest tests/test_dashboard.py tests/test_dashboard_state_api.py tests/test_dashboard_charts.py -x -q` -- tests pass.
  </verify>
  <done>
Dashboard templates render rich data matching Word/Markdown quality. Summary cards, section drill-downs, findings detail, peer comparison, and meeting prep all show complete LLM-extracted data.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/gorlin/projects/research && python -m pytest tests/test_dashboard.py tests/test_dashboard_state_api.py tests/test_dashboard_charts.py -x -q` -- all tests pass
2. `cd /Users/gorlin/projects/research && python -m pyright src/do_uw/dashboard/state_api.py src/do_uw/dashboard/state_api_ext.py` -- no type errors
3. All Python files under 500 lines
4. Dashboard app still starts and serves pages
</verification>

<success_criteria>
- Dashboard shows rich data matching Word/Markdown content
- No sections showing empty/N/A when state has data
- Drill-down navigation reveals detailed finding data
- Summary cards reflect actual risk signals
- Meeting prep shows categorized questions with sources
</success_criteria>

<output>
After completion, create `.planning/phases/22-comprehensive-worksheet-redesign/22-09-SUMMARY.md`
</output>
