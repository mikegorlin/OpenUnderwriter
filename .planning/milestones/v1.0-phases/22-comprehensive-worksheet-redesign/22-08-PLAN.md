---
phase: 22-comprehensive-worksheet-redesign
plan: 08
type: execute
wave: 3
depends_on: ["22-07"]
files_modified:
  - src/do_uw/templates/pdf/worksheet.html.j2
  - src/do_uw/templates/pdf/styles.css
  - src/do_uw/stages/render/pdf_renderer.py
autonomous: true

must_haves:
  truths:
    - "PDF output matches Markdown content (same template context)"
    - "Professional CSS styling with print-ready layout"
    - "Tables properly formatted for PDF page widths"
    - "Risk coloring and visual indicators render correctly in PDF"
  artifacts:
    - path: "src/do_uw/templates/pdf/worksheet.html.j2"
      provides: "HTML template for PDF generation with rich tables"
      contains: "Section 1"
    - path: "src/do_uw/templates/pdf/styles.css"
      provides: "Professional print-ready CSS"
      contains: "font-family"
    - path: "src/do_uw/stages/render/pdf_renderer.py"
      provides: "PDF rendering orchestrator using WeasyPrint"
      contains: "render_pdf"
  key_links:
    - from: "pdf_renderer.py"
      to: "md_renderer.py"
      via: "build_template_context() reuse"
    - from: "worksheet.html.j2"
      to: "md_renderer_helpers*.py"
      via: "Same template context dict"
---

<objective>
Redesign PDF template and stylesheet for production-quality output matching the Word/Markdown redesign.

Purpose: The PDF output reuses build_template_context() from the Markdown renderer, so the data is already available from Plan 07. This plan focuses on the HTML template (worksheet.html.j2) and CSS styling (styles.css) to produce a professional print-ready PDF with proper table formatting, risk coloring, page breaks, and visual hierarchy.

Output: Redesigned worksheet.html.j2 + styles.css + updated pdf_renderer.py
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/do_uw/templates/pdf/worksheet.html.j2
@src/do_uw/templates/pdf/styles.css
@src/do_uw/stages/render/pdf_renderer.py
@src/do_uw/stages/render/md_renderer.py
@.planning/phases/22-comprehensive-worksheet-redesign/22-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign PDF HTML template</name>
  <files>src/do_uw/templates/pdf/worksheet.html.j2</files>
  <action>
Rewrite worksheet.html.j2 (currently 175 lines) with the same section structure as the Markdown template but formatted as proper HTML for WeasyPrint PDF generation.

The HTML template uses the SAME template context from build_template_context() (Plan 07), so all the rich data is already available. The template just presents it differently.

**Key differences from Markdown template:**
1. HTML tables with proper `<thead>/<tbody>` for WeasyPrint styling
2. CSS class annotations for conditional formatting (risk-critical, risk-elevated, risk-moderate)
3. Page break hints between major sections (`page-break-before: always`)
4. Header/footer with company name + page number
5. Chart image embedding via `<img>` tags (base64 or file path)

**Structure (mirrors Markdown template):**
- All 8 sections + meeting prep appendix
- Same data, same narratives, same tables
- Rich HTML tables with proper column alignment
- Risk level CSS classes for conditional coloring
- Source citations in small text
- D&O context in styled callout boxes

**Template patterns:**
- `<table class="data-table">` for all data tables
- `<tr class="risk-{{ item.risk_level|lower }}">` for conditional row styling
- `<div class="callout callout-{{ type }}">` for D&O context boxes
- `<span class="citation">{{ source }}</span>` for source citations
- `{% if section.has_data %}` consistent with Markdown template

Keep under 500 lines.
  </action>
  <verify>
Verify under 500 lines: `wc -l src/do_uw/templates/pdf/worksheet.html.j2`
  </verify>
  <done>
PDF HTML template renders all 8 sections with rich tables, narratives, and source citations using proper HTML/CSS for WeasyPrint.
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign CSS stylesheet + update pdf_renderer</name>
  <files>
    src/do_uw/templates/pdf/styles.css
    src/do_uw/stages/render/pdf_renderer.py
  </files>
  <action>
Rewrite styles.css (currently 197 lines) for professional print-ready PDF output.

**CSS requirements:**
1. **Typography**: Professional serif body text, sans-serif headings (no comic sans). Match Angry Dolphin brand identity.
2. **Page layout**: A4/Letter size, proper margins, header/footer regions
3. **Tables**:
   - Alternating row backgrounds (light grey/white)
   - Proper cell padding and alignment
   - Compact but readable font sizes (9-10pt for tables)
   - Column width hints for common table patterns
4. **Risk coloring** (NO green):
   - `.risk-critical { background: #FCE8E6; }` (light red)
   - `.risk-elevated { background: #FFF3CD; }` (amber)
   - `.risk-moderate { background: #DCEEF8; }` (light blue)
   - `.risk-low { background: transparent; }`
5. **Callout boxes**: Styled borders for D&O context, warnings, notes
6. **Citations**: Small, grey, monospace text
7. **Tier badges**: Colored background labels
8. **Page breaks**: Between major sections, avoid breaking inside tables
9. **Print media queries**: `@page` rules for margins, headers, footers

**pdf_renderer.py updates** (currently 143 lines):
- Ensure it passes the same enhanced template context from build_template_context()
- Add any new Jinja2 filters needed for HTML formatting (e.g., `|risk_class` -> CSS class name)
- Verify chart embedding works with the new template structure

Keep both files under 500 lines.
  </action>
  <verify>
Run `cd /Users/gorlin/projects/research && python -m pytest tests/test_render_outputs.py -x -q` -- tests pass (PDF rendering works).
Run `cd /Users/gorlin/projects/research && python -m pyright src/do_uw/stages/render/pdf_renderer.py` -- no type errors.
Verify under 500 lines: `wc -l src/do_uw/templates/pdf/styles.css src/do_uw/stages/render/pdf_renderer.py`
  </verify>
  <done>
PDF output is professionally styled with print-ready layout, proper table formatting, risk coloring, and visual hierarchy. Content matches Markdown/Word output.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/gorlin/projects/research && python -m pytest tests/test_render_outputs.py -x -q` -- all tests pass
2. `cd /Users/gorlin/projects/research && python -m pyright src/do_uw/stages/render/pdf_renderer.py` -- no type errors
3. All files under 500 lines
4. render_pdf() signature unchanged
</verification>

<success_criteria>
- PDF output matches Word/Markdown content richness
- Professional print-ready styling
- Tables properly formatted for PDF page widths
- Risk coloring renders correctly
- Page breaks between major sections
</success_criteria>

<output>
After completion, create `.planning/phases/22-comprehensive-worksheet-redesign/22-08-SUMMARY.md`
</output>
