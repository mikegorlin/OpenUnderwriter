---
phase: 10.1-pricing-model-enhancement
plan: 04
subsystem: pricing
tags: [typer-cli, rich-tables, yoy-tracking, carrier-rotation, csv-import, partial-data, file-split]

# Dependency graph
requires:
  - phase: 10.1-02
    provides: ProgramStore CRUD for programs, policy years, brokers, carriers
  - phase: 10.1-03
    provides: pricing_ingestion.ingest_document for CLI ingest command
provides:
  - "pricing_analytics_trends.py with compute_yoy_changes, detect_carrier_rotations"
  - "cli_pricing_programs.py with 6 program management CLI commands"
  - "Partial-data CSV import (ticker-only rows create programs)"
  - "Backward-compat re-exports from pricing_analytics.py"
affects: [pricing-model-enhancement, interactive-dashboard]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "File splitting with re-export for backward compat"
    - "Typer sub-app chain: cli.py -> cli_pricing.py -> cli_pricing_programs.py"
    - "Dual-path CSV import: full data -> Quote, partial -> Program"
    - "Patch at source module for lazy imports in tests"

key-files:
  created:
    - src/do_uw/knowledge/pricing_analytics_trends.py
    - src/do_uw/cli_pricing_programs.py
    - tests/knowledge/test_pricing_analytics_trends.py
    - tests/test_cli_pricing_programs.py
  modified:
    - src/do_uw/knowledge/pricing_analytics.py
    - src/do_uw/cli_pricing.py
    - tests/test_cli_pricing.py
    - ruff.toml

key-decisions:
  - "Patch at do_uw.knowledge.pricing_store_programs.ProgramStore for lazy-import CLI tests"
  - "Patch at do_uw.knowledge.pricing_ingestion.ingest_document for ingest command tests"
  - "_CSV_REQUIRED reduced to just {ticker} for partial-data import support"
  - "TBD carriers excluded from rotation tracking (not real carriers)"
  - "Zero-value denominators return None pct_change (div-by-zero guard)"

patterns-established:
  - "Typer 3-level sub-app chain: main app -> pricing app -> programs sub-app"
  - "Dual-path CSV import with has_full check for Quote vs Program routing"
  - "YoY change computation with graceful None handling for partial data"

# Metrics
duration: ~15min
completed: 2026-02-09
---

# Phase 10.1 Plan 04: Analytics Split and Program Management CLI Summary

**Split pricing_analytics.py and cli_pricing.py for 500-line compliance, added 6 program management CLI commands with YoY tracking, carrier rotation detection, document ingestion, and partial-data CSV import**

## Performance

- **Duration:** ~15 min
- **Tasks:** 2
- **Files created:** 4
- **Files modified:** 4

## Accomplishments
- Split pricing_analytics.py (435L) into pricing_analytics.py (386L) + pricing_analytics_trends.py (248L) with backward-compat re-exports
- Split cli_pricing.py (471L) into cli_pricing.py (398L) + cli_pricing_programs.py (443L) with Typer sub-app chain
- Added compute_yoy_changes() and detect_carrier_rotations() for program evolution tracking
- Created 6 CLI commands: add-program, list-programs, program-history, add-policy-year, brokers, ingest
- Updated CSV import for dual-path: full data -> Quote, partial data -> Program + optional PolicyYear
- 30 new tests (14 in test_pricing_analytics_trends.py + 16 in test_cli_pricing_programs.py)

## Task Commits

Each task was committed atomically:

1. **Task 1: Split analytics and add partial-data support** - `7db4d6b` (feat)
2. **Task 2: Split CLI and add program management commands** - `faffb96` (feat)

## Files Created/Modified
- `src/do_uw/knowledge/pricing_analytics_trends.py` - TrendPoint, MarketTrends, compute_trends (moved), compute_yoy_changes, detect_carrier_rotations (new)
- `src/do_uw/knowledge/pricing_analytics.py` - Trimmed after split, re-exports for backward compat
- `src/do_uw/cli_pricing_programs.py` - 6 program management CLI commands with Rich table output
- `src/do_uw/cli_pricing.py` - Registered programs sub-app, dual-path CSV import, helper functions
- `tests/knowledge/test_pricing_analytics_trends.py` - 14 tests for new trend functions
- `tests/test_cli_pricing_programs.py` - 16 tests for program CLI commands
- `tests/test_cli_pricing.py` - Updated test for new partial-data import behavior
- `ruff.toml` - Added B008 ignore for cli_pricing_programs.py

## Decisions Made
- Patch at source module path (`do_uw.knowledge.pricing_store_programs.ProgramStore`) for lazy-import CLI tests rather than patching at CLI module level (create=True doesn't intercept lazy imports)
- `_CSV_REQUIRED` reduced from 5 fields to just `{"ticker"}` to support partial-data import
- TBD carrier names excluded from rotation tracking (not real carriers)
- Zero-value denominators in YoY computation return None (not infinity or error)
- Existing `test_import_csv_missing_columns` updated to `test_import_csv_missing_ticker_column` to match new partial-data semantics

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed existing test for partial-data import behavior**
- **Found during:** Task 2 (full test suite verification)
- **Issue:** `test_import_csv_missing_columns` expected exit_code=1 for CSV with `ticker,premium` headers, but new partial-data import treats this as valid (has ticker)
- **Fix:** Renamed test to `test_import_csv_missing_ticker_column` and changed CSV to have only `premium,limit` headers (no ticker) which correctly triggers the missing-column error
- **Files modified:** tests/test_cli_pricing.py
- **Verification:** All 1573 tests pass
- **Committed in:** faffb96 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug fix)
**Impact on plan:** Minor test update required by the intentional _CSV_REQUIRED change. No scope creep.

## Issues Encountered
- Ingest test mocking: Initially patched `do_uw.cli_pricing_programs.ingest_document` with `create=True`, but the real `pricing_ingestion.py` module (created by parallel plan 10.1-03) was being imported instead. Fixed by patching at source: `do_uw.knowledge.pricing_ingestion.ingest_document`.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Analytics and CLI infrastructure complete for pricing model
- All 4 plan 10.1 deliverables implemented: ORM models, ProgramStore, rate inference + ingestion, CLI + analytics
- Ready for phase verification and any remaining integration work

---
*Phase: 10.1-pricing-model-enhancement*
*Completed: 2026-02-09*
