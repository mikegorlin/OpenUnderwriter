---
phase: 10.1-pricing-model-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/knowledge/pricing_models.py
  - src/do_uw/knowledge/pricing_models_program.py
  - src/do_uw/models/pricing.py
  - tests/knowledge/test_pricing_models_program.py
  - tests/models/test_pricing_models.py
autonomous: true

must_haves:
  truths:
    - "Program, PolicyYear, Broker, and Carrier ORM models exist with correct relationships"
    - "TowerLayer ORM gains layer_type, layer_label, commission_pct, data_source, carrier_id, policy_year_id fields"
    - "LayerType, DataCompleteness, DataSource enums exist and are importable"
    - "ProgramInput, PolicyYearInput, BrokerInput Pydantic models accept partial data (most fields optional)"
    - "All existing tests pass unchanged (zero regression)"
  artifacts:
    - path: "src/do_uw/knowledge/pricing_models_program.py"
      provides: "Program, PolicyYear, Broker, Carrier ORM models"
      contains: "class Program"
    - path: "src/do_uw/knowledge/pricing_models.py"
      provides: "Enhanced TowerLayer with layer_type, carrier_id, policy_year_id"
      contains: "layer_type"
    - path: "src/do_uw/models/pricing.py"
      provides: "LayerType, DataCompleteness, DataSource enums; ProgramInput, PolicyYearInput, BrokerInput models"
      contains: "class LayerType"
  key_links:
    - from: "src/do_uw/knowledge/pricing_models_program.py"
      to: "src/do_uw/knowledge/models.py"
      via: "Base import"
      pattern: "from do_uw.knowledge.models import Base"
    - from: "src/do_uw/knowledge/pricing_models_program.py"
      to: "src/do_uw/knowledge/pricing_models.py"
      via: "TowerLayer relationship"
      pattern: "relationship.*TowerLayer"
---

<objective>
Create the enhanced ORM and Pydantic models for the Phase 10.1 pricing model rework.

Purpose: The new Program -> PolicyYear -> TowerLayer hierarchy (with Broker and Carrier as separate entities) is the foundation for all subsequent plans. This plan creates the data model layer without modifying the database or store -- pure model definitions and tests.

Output: New ORM models (Program, PolicyYear, Broker, Carrier), enhanced TowerLayer, new enums (LayerType, DataCompleteness, DataSource), and new Pydantic input/output models for programs and policy years.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-pricing-model-enhancement/10.1-CONTEXT.md
@.planning/phases/10.1-pricing-model-enhancement/10.1-RESEARCH.md
@src/do_uw/knowledge/pricing_models.py
@src/do_uw/knowledge/models.py
@src/do_uw/models/pricing.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Program, PolicyYear, Broker, Carrier ORM models and enhance TowerLayer</name>
  <files>
    src/do_uw/knowledge/pricing_models_program.py
    src/do_uw/knowledge/pricing_models.py
  </files>
  <action>
    **New file: `pricing_models_program.py`** — Create ORM models following SQLAlchemy 2.0 Mapped[] pattern (match existing pricing_models.py style):

    1. **Broker** (`brokers` table):
       - `id`: Mapped[int], primary key, autoincrement
       - `brokerage_name`: Mapped[str], nullable=False (minimum required field for a broker)
       - `producer_name`: Mapped[str | None], nullable=True
       - `email`: Mapped[str | None], nullable=True
       - `phone`: Mapped[str | None], nullable=True
       - `notes_text`: Mapped[str | None], nullable=True
       - `created_at`: Mapped[datetime], nullable=False
       - `__repr__` showing id, brokerage_name, producer_name

    2. **Carrier** (`carriers` table):
       - `id`: Mapped[int], primary key, autoincrement
       - `carrier_name`: Mapped[str], nullable=False, index=True
       - `am_best_rating`: Mapped[str | None], nullable=True
       - `appetite_notes`: Mapped[str | None], nullable=True
       - `notes_text`: Mapped[str | None], nullable=True
       - `created_at`: Mapped[datetime], nullable=False
       - `__repr__` showing id, carrier_name

    3. **Program** (`programs` table):
       - `id`: Mapped[int], primary key, autoincrement
       - `ticker`: Mapped[str], nullable=False, index=True
       - `company_name`: Mapped[str | None], nullable=True (NOT required -- D2 minimum viable record)
       - `anniversary_month`: Mapped[int | None], nullable=True
       - `anniversary_day`: Mapped[int | None], nullable=True
       - `broker_id`: Mapped[int | None], ForeignKey("brokers.id"), nullable=True
       - `notes_text`: Mapped[str | None], nullable=True
       - `created_at`: Mapped[datetime], nullable=False
       - `updated_at`: Mapped[datetime], nullable=False
       - Relationships: `policy_years` (list[PolicyYear], back_populates, cascade all/delete-orphan), `broker` (Broker | None)
       - `__repr__` showing id, ticker, anniversary_month/anniversary_day

    4. **PolicyYear** (`policy_years` table):
       - `id`: Mapped[int], primary key, autoincrement
       - `program_id`: Mapped[int], ForeignKey("programs.id"), nullable=False
       - `policy_year`: Mapped[int], nullable=False (e.g. 2025), index=True
       - `effective_date`: Mapped[datetime | None], nullable=True
       - `expiration_date`: Mapped[datetime | None], nullable=True
       - `total_limit`: Mapped[float | None], Float(), nullable=True
       - `total_premium`: Mapped[float | None], Float(), nullable=True
       - `retention`: Mapped[float | None], Float(), nullable=True
       - `status`: Mapped[str], nullable=False, default="QUOTED" (uses QuoteStatus values)
       - `data_completeness`: Mapped[str], nullable=False, default="FRAGMENT"
       - `source`: Mapped[str], nullable=False, default="manual"
       - `source_document`: Mapped[str | None], nullable=True
       - `program_rate_on_line`: Mapped[float | None], Float(), nullable=True (nullable unlike Quote!)
       - `notes_text`: Mapped[str | None], nullable=True
       - `created_at`: Mapped[datetime], nullable=False
       - Relationships: `program` (Program, back_populates), `layers` (list[TowerLayer], back_populates="policy_year", viewonly=True — foreign_keys=[TowerLayer.policy_year_id])
       - `__repr__` showing id, program_id, policy_year

    Import Base from `do_uw.knowledge.models`. Use `from __future__ import annotations`. All `Float()` instantiated (not class reference) for pyright strict. DateTime columns use `DateTime` import from sqlalchemy.

    **Modify: `pricing_models.py`** — Add new OPTIONAL columns to TowerLayer (all nullable, preserving backward compat with existing data):
    - `layer_type`: Mapped[str | None] = mapped_column(String, nullable=True) — values: PRIMARY, EXCESS, SIDE_A
    - `layer_label`: Mapped[str | None] = mapped_column(String, nullable=True) — e.g. "1st Excess", "Lead Side A"
    - `commission_pct`: Mapped[float | None] = mapped_column(Float(), nullable=True)
    - `data_source`: Mapped[str | None] = mapped_column(String, nullable=True) — VERIFIED, INFERRED, ESTIMATED, AI_EXTRACTED
    - `carrier_id`: Mapped[int | None] = mapped_column(Integer, ForeignKey("carriers.id"), nullable=True)
    - `policy_year_id`: Mapped[int | None] = mapped_column(Integer, ForeignKey("policy_years.id"), nullable=True)

    Add `policy_year` relationship: `Mapped[Any] = relationship("PolicyYear", viewonly=True, foreign_keys=[policy_year_id])` (use Any to avoid circular import with pricing_models_program.py; TYPE_CHECKING for type hints is fine too).

    Do NOT remove or rename any existing columns. Existing `quote_id`, `layer_position`, `carrier_name`, `carrier_rating` all remain as-is.

    Add ForeignKey import if not already present. Keep file well under 500 lines.
  </action>
  <verify>
    Run: `cd /Users/gorlin/projects/research && uv run python -c "from do_uw.knowledge.pricing_models_program import Program, PolicyYear, Broker, Carrier; print('OK')"` succeeds.
    Run: `cd /Users/gorlin/projects/research && uv run python -c "from do_uw.knowledge.pricing_models import TowerLayer; print(hasattr(TowerLayer, 'layer_type'), hasattr(TowerLayer, 'carrier_id'), hasattr(TowerLayer, 'policy_year_id'))"` prints `True True True`.
    Run: `cd /Users/gorlin/projects/research && uv run pytest tests/ -x -q` — all existing tests pass.
    Run: `cd /Users/gorlin/projects/research && uv run ruff check src/do_uw/knowledge/pricing_models.py src/do_uw/knowledge/pricing_models_program.py` — no errors.
    Run: `cd /Users/gorlin/projects/research && uv run pyright src/do_uw/knowledge/pricing_models.py src/do_uw/knowledge/pricing_models_program.py` — no errors.
    Verify both files are under 500 lines: `wc -l src/do_uw/knowledge/pricing_models.py src/do_uw/knowledge/pricing_models_program.py`
  </verify>
  <done>
    Program, PolicyYear, Broker, Carrier ORM models exist in pricing_models_program.py. TowerLayer has layer_type, layer_label, commission_pct, data_source, carrier_id, policy_year_id columns. All existing tests pass. No lint/type errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create enhanced Pydantic models and enums, plus tests</name>
  <files>
    src/do_uw/models/pricing.py
    tests/knowledge/test_pricing_models_program.py
    tests/models/test_pricing_models.py
  </files>
  <action>
    **Modify: `src/do_uw/models/pricing.py`** — Add new enums and Pydantic models (keep existing QuoteInput/QuoteOutput/TowerLayerInput/TowerLayerOutput unchanged):

    1. **New enums** (StrEnum, after existing enums):
       - `LayerType`: PRIMARY, EXCESS, SIDE_A (per D1)
       - `DataCompleteness`: FRAGMENT, PARTIAL, COMPLETE (per research Pattern 2)
       - `DataSource`: VERIFIED, INFERRED, ESTIMATED, AI_EXTRACTED (per D7)

    2. **BrokerInput** (BaseModel):
       - `brokerage_name`: str (required)
       - `producer_name`: str | None = None
       - `email`: str | None = None
       - `phone`: str | None = None
       - `notes_text`: str | None = None

    3. **BrokerOutput** (BaseModel):
       - Same as BrokerInput plus `id: int` and `created_at: datetime`

    4. **CarrierInput** (BaseModel):
       - `carrier_name`: str (required)
       - `am_best_rating`: str | None = None
       - `appetite_notes`: str | None = None
       - `notes_text`: str | None = None

    5. **CarrierOutput** (BaseModel):
       - Same as CarrierInput plus `id: int` and `created_at: datetime`

    6. **EnhancedLayerInput** (BaseModel):
       - `layer_type`: LayerType (required — PRIMARY, EXCESS, or SIDE_A)
       - `layer_label`: str | None = None (e.g. "Primary", "1st Excess")
       - `layer_number`: int (required — ordinal within type)
       - `attachment_point`: float | None = None (0 for primary, computed for excess if not provided)
       - `limit_amount`: float | None = None (can be unknown)
       - `premium`: float | None = None (can be unknown)
       - `carrier_name`: str | None = None
       - `carrier_id`: int | None = None (FK to carriers table)
       - `carrier_rating`: str | None = None
       - `is_lead`: bool = False
       - `share_pct`: float | None = None
       - `commission_pct`: float | None = None
       - `data_source`: DataSource = DataSource.VERIFIED
       - `notes_text`: str | None = None

    7. **PolicyYearInput** (BaseModel):
       - `policy_year`: int (required — e.g. 2025)
       - `effective_date`: datetime | None = None
       - `expiration_date`: datetime | None = None
       - `total_limit`: float | None = None
       - `total_premium`: float | None = None
       - `retention`: float | None = None
       - `status`: QuoteStatus = QuoteStatus.QUOTED
       - `data_completeness`: DataCompleteness = DataCompleteness.FRAGMENT
       - `source`: str = "manual"
       - `source_document`: str | None = None
       - `notes_text`: str | None = None
       - `layers`: list[EnhancedLayerInput] = Field(default_factory=lambda: [])

    8. **PolicyYearOutput** (BaseModel):
       - All PolicyYearInput fields plus `id: int`, `program_id: int`, `program_rate_on_line: float | None`, `created_at: datetime`
       - `layers`: list[TowerLayerOutput] = Field(default_factory=lambda: [])

    9. **ProgramInput** (BaseModel) — minimum viable = ticker only (anniversary is optional!):
       - `ticker`: str (required)
       - `company_name`: str | None = None
       - `anniversary_month`: int | None = None
       - `anniversary_day`: int | None = None
       - `broker`: BrokerInput | None = None (inline broker creation)
       - `broker_id`: int | None = None (existing broker FK)
       - `notes_text`: str | None = None

    10. **ProgramOutput** (BaseModel):
        - All ProgramInput scalar fields plus `id: int`, `created_at: datetime`, `updated_at: datetime`
        - `broker`: BrokerOutput | None = None
        - `policy_years`: list[PolicyYearOutput] = Field(default_factory=lambda: [])

    Keep file under 500 lines. All models use `model_config = ConfigDict(frozen=False)`.

    **New test file: `tests/knowledge/test_pricing_models_program.py`** — Test ORM model instantiation:
    - Test Program with minimum viable fields (ticker only)
    - Test Program with all fields including broker relationship
    - Test PolicyYear with minimal fields
    - Test PolicyYear with layers relationship
    - Test Broker and Carrier creation
    - Use in-memory SQLite engine with Base.metadata.create_all for setup

    **New test file: `tests/models/test_pricing_models.py`** — Test Pydantic model validation:
    - Test ProgramInput with just ticker (minimum viable)
    - Test ProgramInput with all fields
    - Test PolicyYearInput with just policy_year
    - Test PolicyYearInput with layers
    - Test EnhancedLayerInput with just type + number
    - Test BrokerInput/CarrierInput
    - Test LayerType, DataCompleteness, DataSource enum values
    - Verify existing QuoteInput still works unchanged
  </action>
  <verify>
    Run: `cd /Users/gorlin/projects/research && uv run pytest tests/knowledge/test_pricing_models_program.py tests/models/test_pricing_models.py -v` — all new tests pass.
    Run: `cd /Users/gorlin/projects/research && uv run pytest tests/ -x -q` — all 1459+ tests pass.
    Run: `cd /Users/gorlin/projects/research && uv run ruff check src/do_uw/models/pricing.py` — no errors.
    Run: `cd /Users/gorlin/projects/research && uv run pyright src/do_uw/models/pricing.py` — no errors.
    Verify pricing.py under 500 lines: `wc -l src/do_uw/models/pricing.py`
  </verify>
  <done>
    LayerType, DataCompleteness, DataSource enums created. ProgramInput accepts just a ticker. PolicyYearInput accepts just policy_year. EnhancedLayerInput accepts partial layer data. All existing QuoteInput/QuoteOutput models unchanged. Tests confirm both ORM and Pydantic models work correctly. All 1459+ existing tests pass.
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/ -x -q` — all existing + new tests pass
2. `uv run ruff check src/do_uw/knowledge/ src/do_uw/models/` — no lint errors
3. `uv run pyright src/do_uw/knowledge/ src/do_uw/models/` — no type errors
4. `wc -l src/do_uw/knowledge/pricing_models.py src/do_uw/knowledge/pricing_models_program.py src/do_uw/models/pricing.py` — all under 500 lines
5. Python import check: `from do_uw.knowledge.pricing_models_program import Program, PolicyYear, Broker, Carrier`
6. Python import check: `from do_uw.models.pricing import LayerType, DataCompleteness, DataSource, ProgramInput, PolicyYearInput`
</verification>

<success_criteria>
- Program ORM model exists with ticker as only required field beyond id/timestamps
- PolicyYear ORM model supports nullable pricing fields (total_limit, total_premium, retention, program_rate_on_line)
- Broker and Carrier are separate tables with FK relationships
- TowerLayer gains layer_type, carrier_id, policy_year_id columns (all nullable for backward compat)
- ProgramInput Pydantic model validates with just {"ticker": "AAPL"}
- All 1459+ existing tests pass with zero regression
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-pricing-model-enhancement/10.1-01-SUMMARY.md`
</output>
