---
phase: 11-interactive-dashboard-decision-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - ruff.toml
  - src/do_uw/dashboard/__init__.py
  - src/do_uw/dashboard/app.py
  - src/do_uw/dashboard/state_api.py
  - src/do_uw/dashboard/design.py
  - src/do_uw/cli_dashboard.py
  - src/do_uw/cli.py
  - src/do_uw/templates/dashboard/base.html
  - src/do_uw/templates/dashboard/index.html
  - src/do_uw/static/css/dashboard.css
  - src/do_uw/static/js/dashboard.js
  - tests/test_dashboard.py
autonomous: true

must_haves:
  truths:
    - "Running `do-uw serve AAPL` starts a local web server and opens a dashboard showing the executive summary"
    - "The dashboard index page displays summary cards for each analytical section (company, financials, market, governance, litigation, scoring)"
    - "The dashboard loads AnalysisState from state.json and presents the same data as the Word document"
    - "The dashboard uses Liberty Mutual branding (navy #1A1446, gold #FFD000, no green in risk spectrum)"
  artifacts:
    - path: "src/do_uw/dashboard/__init__.py"
      provides: "Dashboard package init"
    - path: "src/do_uw/dashboard/app.py"
      provides: "FastAPI app factory with state loading, template setup, static files, and page routes"
      min_lines: 80
    - path: "src/do_uw/dashboard/state_api.py"
      provides: "AnalysisState to dashboard context extraction (reuses build_template_context from md_renderer)"
      min_lines: 50
    - path: "src/do_uw/dashboard/design.py"
      provides: "CSS variable definitions and tier-to-CSS-class mapping for dashboard templates"
      min_lines: 30
    - path: "src/do_uw/cli_dashboard.py"
      provides: "do-uw serve TICKER command that starts the dashboard server"
      min_lines: 30
    - path: "src/do_uw/templates/dashboard/base.html"
      provides: "Base HTML template with CDN links (htmx, Tailwind, DaisyUI, Plotly.js), nav bar, layout"
    - path: "src/do_uw/templates/dashboard/index.html"
      provides: "Dashboard overview page with summary cards for each section"
    - path: "tests/test_dashboard.py"
      provides: "FastAPI TestClient tests for dashboard routes"
      min_lines: 40
  key_links:
    - from: "src/do_uw/cli_dashboard.py"
      to: "src/do_uw/dashboard/app.py"
      via: "create_app(state_path) import"
      pattern: "from do_uw\\.dashboard\\.app import create_app"
    - from: "src/do_uw/cli.py"
      to: "src/do_uw/cli_dashboard.py"
      via: "app.add_typer registration"
      pattern: "add_typer.*dashboard"
    - from: "src/do_uw/dashboard/app.py"
      to: "src/do_uw/dashboard/state_api.py"
      via: "build_dashboard_context import"
      pattern: "build_dashboard_context"
    - from: "src/do_uw/dashboard/state_api.py"
      to: "src/do_uw/stages/render/md_renderer.py"
      via: "build_template_context reuse"
      pattern: "from do_uw\\.stages\\.render\\.md_renderer import build_template_context"
---

<objective>
Create the FastAPI dashboard foundation: app factory, state API layer, CLI serve command,
base HTML template with CDN dependencies, and an index page showing analytical summary cards.

Purpose: Establishes the entire web framework scaffold so that Plans 02 and 03 can add
interactive visualizations and drill-down features on top of a working, testable server.

Output: A working `do-uw serve TICKER` command that starts a web dashboard showing a
summary overview of the analysis.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-interactive-dashboard-decision-ux/11-RESEARCH.md

Key existing files to reference:
@src/do_uw/cli.py — CLI entry point; register dashboard_app via add_typer
@src/do_uw/stages/render/md_renderer.py — build_template_context() to reuse
@src/do_uw/stages/render/design_system.py — Liberty Mutual brand colors
@src/do_uw/stages/render/formatters.py — format_currency, format_percentage, na_if_none
@src/do_uw/pipeline.py — Pipeline.load_state(path)
@src/do_uw/models/state.py — AnalysisState model
@pyproject.toml — add fastapi, uvicorn, plotly dependencies
@ruff.toml — add B008 ignore for cli_dashboard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard Python module, app factory, state API, CLI command</name>
  <files>
    pyproject.toml
    ruff.toml
    src/do_uw/dashboard/__init__.py
    src/do_uw/dashboard/app.py
    src/do_uw/dashboard/state_api.py
    src/do_uw/dashboard/design.py
    src/do_uw/cli_dashboard.py
    src/do_uw/cli.py
  </files>
  <action>
    1. Add dependencies: `uv add fastapi uvicorn plotly`

    2. Create `src/do_uw/dashboard/__init__.py`:
       - Empty init, just docstring and `__all__ = []`

    3. Create `src/do_uw/dashboard/app.py` (~150-200L):
       - `create_app(state_path: Path) -> FastAPI` app factory function
       - Loads AnalysisState via `Pipeline.load_state(state_path)`
       - Stores state on `app.state.analysis_state` with `# type: ignore[attr-defined]`
       - Stores `state_path` on `app.state.state_path` with `# type: ignore[attr-defined]`
       - Mounts static files from `Path(__file__).resolve().parent.parent / "static"`
       - Sets up Jinja2Templates from `Path(__file__).resolve().parent.parent / "templates" / "dashboard"`
       - Registers template filters: format_currency, format_percentage, na_if_none from formatters.py
       - GET "/" route: renders index.html with dashboard context from state_api.build_dashboard_context()
       - GET "/section/{section_id}" route: renders section.html (placeholder for Plan 03)
       - Includes file mtime check: on each request, compare state_path.stat().st_mtime to stored mtime; reload state if changed
       - All routes use `def` (not `async def`) per research recommendation
       - Import plotly as `# type: ignore[import-untyped]` (same pattern as matplotlib)

    4. Create `src/do_uw/dashboard/state_api.py` (~100-150L):
       - `build_dashboard_context(state: AnalysisState) -> dict[str, Any]`
       - Calls `build_template_context(state)` from md_renderer.py as the base
       - Adds dashboard-specific keys:
         - `sections`: list of dicts with {id, title, status_class, summary} for each analytical section (company, financials, market, governance, litigation, scoring)
         - `risk_level_class`: CSS class derived from scoring tier (WIN/WANT=info, WRITE/WATCH=warning, WALK/NO_TOUCH=error)
         - `ticker`: state.ticker
         - `company_name`: extracted from state.company.identity.legal_name
       - Imports from md_renderer: `from do_uw.stages.render.md_renderer import build_template_context`
       - Imports formatters as needed

    5. Create `src/do_uw/dashboard/design.py` (~60-80L):
       - `CSS_VARIABLES: dict[str, str]` mapping CSS custom property names to values, mirroring design_system.py:
         - `--lm-navy: #1A1446`, `--lm-gold: #FFD000`, `--lm-text: #333333`, `--lm-text-light: #666666`
         - Risk spectrum: `--risk-critical: #CC0000`, `--risk-high: #E67300`, `--risk-elevated: #FFB800`, `--risk-moderate: #4A90D9`, `--risk-neutral: #999999`
         - Conditional formatting: `--fmt-deteriorating: #FCE8E6`, `--fmt-caution: #FFF3CD`, `--fmt-improving: #DCEEF8`
       - `tier_to_css_class(tier: str | None) -> str` helper: maps WIN/WANT->badge-info, WRITE->badge-warning, WATCH->badge-warning, WALK/NO_TOUCH->badge-error, None->badge-ghost
       - `risk_level_to_css_class(level: str | None) -> str` helper for CRITICAL/HIGH/ELEVATED/MODERATE/NEUTRAL

    6. Create `src/do_uw/cli_dashboard.py` (~50-70L):
       - `dashboard_app = typer.Typer(name="dashboard", help="Interactive analysis dashboard")`
       - `@dashboard_app.command("serve")` command with:
         - `ticker: str` argument
         - `port: int = typer.Option(8000, "--port", "-p")`
         - `output: Path = typer.Option(Path("output"), "--output", "-o")`
       - Logic: Construct state_path = output / ticker.upper() / "state.json"
       - Check state_path.exists(), print error and raise typer.Exit(1) if not found
       - Print startup message with URL
       - `import uvicorn  # type: ignore[import-untyped]`
       - `from do_uw.dashboard.app import create_app`
       - Call `uvicorn.run(create_app(state_path), host="127.0.0.1", port=port, log_level="warning")`

    7. Update `src/do_uw/cli.py`:
       - Add import: `from do_uw.cli_dashboard import dashboard_app`
       - Add registration: `app.add_typer(dashboard_app, name="dashboard")`

    8. Update `ruff.toml`:
       - Add `"src/do_uw/cli_dashboard.py" = ["B008"]` to per-file-ignores (Typer function calls in defaults)
  </action>
  <verify>
    - `uv run python -c "from do_uw.dashboard.app import create_app; print('OK')"`
    - `uv run python -c "from do_uw.cli_dashboard import dashboard_app; print('OK')"`
    - `uv run pyright src/do_uw/dashboard/ src/do_uw/cli_dashboard.py`
    - `uv run ruff check src/do_uw/dashboard/ src/do_uw/cli_dashboard.py`
    - Verify all new files are under 500 lines: `wc -l src/do_uw/dashboard/*.py src/do_uw/cli_dashboard.py`
  </verify>
  <done>
    create_app() returns a FastAPI app. cli_dashboard.py has the serve command registered. All files pass pyright strict and ruff. No file exceeds 500 lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: HTML templates, static assets, and TestClient tests</name>
  <files>
    src/do_uw/templates/dashboard/base.html
    src/do_uw/templates/dashboard/index.html
    src/do_uw/static/css/dashboard.css
    src/do_uw/static/js/dashboard.js
    tests/test_dashboard.py
  </files>
  <action>
    1. Create `src/do_uw/templates/dashboard/base.html`:
       - Standard HTML5 boilerplate with `<!DOCTYPE html>` and `<html lang="en">`
       - `<head>`:
         - Meta charset, viewport
         - Title: "D&O Dashboard - {{ ticker }}"
         - CDN links: htmx 2.0 (`unpkg.com/htmx.org@2.0.8`), Tailwind CSS v4 (`@tailwindcss/browser` CDN), DaisyUI 5 CDN, Plotly.js basic bundle (`cdn.plot.ly/plotly-basic-latest.min.js`)
         - Link to `/static/css/dashboard.css`
       - `<body>`:
         - DaisyUI navbar component with Liberty Mutual navy background (#1A1446):
           - "D&O Underwriting Dashboard" title in gold (#FFD000)
           - Company name and ticker display
           - Risk tier badge (using DaisyUI badge with appropriate color class from design.py mapping)
         - `<main>` with `{% block content %}{% endblock %}`
         - htmx loading indicator (DaisyUI `loading` component, hidden by default, shown via hx-indicator)
         - Script tag for `/static/js/dashboard.js`
       - CSS custom properties in `<style>` block mirroring design.py CSS_VARIABLES

    2. Create `src/do_uw/templates/dashboard/index.html`:
       - Extends base.html
       - Overview grid layout (DaisyUI grid, responsive: 1 col mobile, 2 cols md, 3 cols lg):
         - Top row: Executive summary card with quality score, tier badge, thesis narrative
         - Section summary cards (6 cards for each analytical section):
           - Company Profile: legal name, market cap, SIC, employee count
           - Financial Health: Z-score zone, revenue, net income, debt summary
           - Market & Trading: current price, 52w range, short interest
           - Governance: board size, independence ratio, CEO comp
           - Litigation: active/historical matter counts
           - Risk Scoring: quality score, tier, factor count, pattern count
         - Each card uses `hx-get="/section/{{ section_id }}"` + `hx-target="#detail-panel"` for drill-down (wired in Plan 03)
         - Each card uses `cursor-pointer hover:shadow-lg` for interactivity hint
       - Detail panel area: `<div id="detail-panel" class="mt-6">` (empty, populated by htmx in Plan 03)
       - Key findings section: top negatives and positives from executive summary (if available)
       - Use Jinja2 template variables from build_dashboard_context()

    3. Create `src/do_uw/static/css/dashboard.css` (~80-120L):
       - CSS custom properties for Liberty Mutual branding (duplicate from base.html style block for separation of concerns -- either location works)
       - Custom card hover transitions
       - Risk badge color overrides mapping risk levels to brand colors
       - DaisyUI theme customization: `[data-theme="lm"]` with `--p` (primary) set to navy, `--a` (accent) set to gold
       - Conditional formatting classes: `.fmt-deteriorating`, `.fmt-caution`, `.fmt-improving`
       - Score gauge styling
       - Responsive adjustments if needed beyond DaisyUI defaults

    4. Create `src/do_uw/static/js/dashboard.js` (~30-50L):
       - `loadChart(elementId, apiUrl)` function: fetch Plotly JSON spec from apiUrl, call Plotly.newPlot with responsive config
       - `loadAllCharts()` function: find all `[data-chart-url]` elements and call loadChart for each
       - DOMContentLoaded listener calling loadAllCharts()
       - htmx afterSwap listener to re-initialize charts in newly loaded fragments

    5. Create `tests/test_dashboard.py` (~100-150L):
       - Helper `_write_test_state(tmp_path: Path) -> Path` that creates a minimal AnalysisState, serializes to state.json, returns path
       - Test: `test_dashboard_index_returns_200` -- create_app with test state, TestClient GET "/", assert 200, assert "D&O" in response.text
       - Test: `test_dashboard_index_shows_ticker` -- assert ticker appears in response
       - Test: `test_dashboard_missing_state_path` -- create_app with nonexistent path raises or returns error
       - Test: `test_static_css_served` -- GET "/static/css/dashboard.css" returns 200
       - Test: `test_static_js_served` -- GET "/static/js/dashboard.js" returns 200
       - Test: `test_section_endpoint_exists` -- GET "/section/financials" returns 200 (even if placeholder)
       - Test: `test_state_api_build_context` -- call build_dashboard_context with test state, verify keys exist
       - Test: `test_cli_serve_missing_ticker` -- test cli_dashboard serve command with missing state file exits with code 1
       - Import TestClient from `fastapi.testclient`
       - Use `from do_uw.dashboard.app import create_app` and `from do_uw.dashboard.state_api import build_dashboard_context`
  </action>
  <verify>
    - `uv run pytest tests/test_dashboard.py -v` -- all tests pass
    - `uv run pyright src/do_uw/dashboard/`
    - `uv run ruff check src/do_uw/dashboard/`
    - `uv run pytest` -- full test suite passes (no regressions)
    - Verify templates exist: `ls src/do_uw/templates/dashboard/`
    - Verify statics exist: `ls src/do_uw/static/css/ src/do_uw/static/js/`
  </verify>
  <done>
    Dashboard serves an index page at localhost:8000 with summary cards for all 6 analytical sections. Static CSS and JS are served. All TestClient tests pass. Full test suite has no regressions. `do-uw dashboard serve TICKER` command is registered and functional.
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_dashboard.py -v` -- all dashboard tests pass
2. `uv run pytest` -- full test suite passes (1585+ tests, no regressions)
3. `uv run pyright src/do_uw/dashboard/ src/do_uw/cli_dashboard.py` -- zero errors
4. `uv run ruff check src/do_uw/dashboard/ src/do_uw/cli_dashboard.py` -- clean
5. `wc -l src/do_uw/dashboard/*.py src/do_uw/cli_dashboard.py` -- all under 500 lines
6. `uv run python -c "from do_uw.dashboard.app import create_app; print('OK')"` -- imports work
7. Static files served at /static/css/dashboard.css and /static/js/dashboard.js
</verification>

<success_criteria>
- `do-uw dashboard serve` command is registered and starts a FastAPI server
- Dashboard index page renders at localhost:8000 with section summary cards
- State is loaded from output/{TICKER}/state.json
- Liberty Mutual branding is applied (navy navbar, gold accent, no green)
- All tests pass including existing 1585+ test suite
- All Python files pass pyright strict and ruff
- No file exceeds 500 lines
</success_criteria>

<output>
After completion, create `.planning/phases/11-interactive-dashboard-decision-ux/11-01-SUMMARY.md`
</output>
