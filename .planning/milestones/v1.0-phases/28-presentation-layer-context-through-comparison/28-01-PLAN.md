---
phase: 28-presentation-layer-context-through-comparison
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/stages/render/sections/sect5_governance.py
  - src/do_uw/stages/render/sections/sect5_governance_board.py
  - src/do_uw/stages/render/sections/sect1_executive.py
  - src/do_uw/stages/render/sections/sect1_executive_tables.py
  - src/do_uw/stages/render/sections/sect2_company_details.py
  - src/do_uw/stages/render/sections/sect2_company_exposure.py
  - src/do_uw/stages/render/sections/__init__.py
autonomous: true

must_haves:
  truths:
    - "No source file in render/sections/ exceeds 500 lines"
    - "All existing render tests pass without modification"
    - "Word document output is byte-identical to pre-split output (no rendering changes)"
  artifacts:
    - path: "src/do_uw/stages/render/sections/sect5_governance_board.py"
      provides: "Board composition, quality metrics, ownership, sentiment, anti-takeover renderers extracted from sect5_governance.py"
      min_lines: 150
    - path: "src/do_uw/stages/render/sections/sect1_executive_tables.py"
      provides: "Snapshot table, tier indicator, inherent risk, tower recommendation renderers extracted from sect1_executive.py"
      min_lines: 100
    - path: "src/do_uw/stages/render/sections/sect2_company_exposure.py"
      provides: "D&O exposure mapping, extracted exposure, standard exposure renderers extracted from sect2_company_details.py"
      min_lines: 80
  key_links:
    - from: "src/do_uw/stages/render/sections/sect5_governance.py"
      to: "src/do_uw/stages/render/sections/sect5_governance_board.py"
      via: "import and function calls"
      pattern: "from.*sect5_governance_board import"
    - from: "src/do_uw/stages/render/sections/sect1_executive.py"
      to: "src/do_uw/stages/render/sections/sect1_executive_tables.py"
      via: "import and function calls"
      pattern: "from.*sect1_executive_tables import"
    - from: "src/do_uw/stages/render/sections/sect2_company_details.py"
      to: "src/do_uw/stages/render/sections/sect2_company_exposure.py"
      via: "import and function calls"
      pattern: "from.*sect2_company_exposure import"
---

<objective>
Fix the three 500-line violations in the render/sections/ directory before any presentation enrichment begins.

Purpose: The anti-context-rot rule (CLAUDE.md) requires no source file over 500 lines. Three section renderers violate this: sect5_governance.py (668), sect1_executive.py (538), sect2_company_details.py (525). These MUST be split before Phase 28 adds peer context and density gating, which would push them further over the limit.

Output: Six files where three existed, all under 500 lines, zero rendering changes.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/do_uw/stages/render/sections/sect5_governance.py
@src/do_uw/stages/render/sections/sect5_governance_comp.py
@src/do_uw/stages/render/sections/sect1_executive.py
@src/do_uw/stages/render/sections/sect1_helpers.py
@src/do_uw/stages/render/sections/sect1_findings.py
@src/do_uw/stages/render/sections/sect2_company_details.py
@src/do_uw/stages/render/sections/sect2_company.py
@src/do_uw/stages/render/sections/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split sect5_governance.py (668 -> ~350 + ~320)</name>
  <files>
    src/do_uw/stages/render/sections/sect5_governance.py
    src/do_uw/stages/render/sections/sect5_governance_board.py
  </files>
  <action>
  Extract the board-focused rendering functions from sect5_governance.py into a new sect5_governance_board.py module.

  Move these functions to sect5_governance_board.py:
  - _render_board_composition() (line ~197)
  - _render_board_quality_metrics() (line ~251)
  - _render_ownership() (line ~312)
  - _render_ownership_table() (line ~353)
  - _render_sentiment() (line ~408)
  - _render_anti_takeover() (line ~463)
  - Helper functions used only by the moved functions: _clean_board_name(), _sv_str(), _sv_bool()

  Keep in sect5_governance.py:
  - render_section_5() (the orchestrator)
  - _get_governance()
  - _render_narrative()
  - _render_leadership() (executive profiles)
  - _add_leadership_do_context()
  - _is_valid_person_name(), _filter_valid_executives(), _extract_primary_role(), _count_shade_factors()

  In sect5_governance.py, add imports from sect5_governance_board and call the moved functions through the import.

  Ensure all type imports (GovernanceData, OwnershipAnalysis, etc.) are present in both files as needed. Keep the same function signatures -- callers should not notice the split.

  The pattern follows the existing sect5_governance.py -> sect5_governance_comp.py delegation model.
  </action>
  <verify>
  Run: `wc -l src/do_uw/stages/render/sections/sect5_governance.py` -- must be under 500
  Run: `wc -l src/do_uw/stages/render/sections/sect5_governance_board.py` -- must be under 500
  Run: `python -c "from do_uw.stages.render.sections.sect5_governance import render_section_5"` -- must succeed
  Run: `uv run pytest tests/ -x -q` -- all tests pass
  </verify>
  <done>sect5_governance.py is under 500 lines. sect5_governance_board.py contains the extracted board/ownership/sentiment renderers. All existing tests pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Split sect1_executive.py (538 -> ~310 + ~230) and sect2_company_details.py (525 -> ~310 + ~220)</name>
  <files>
    src/do_uw/stages/render/sections/sect1_executive.py
    src/do_uw/stages/render/sections/sect1_executive_tables.py
    src/do_uw/stages/render/sections/sect2_company_details.py
    src/do_uw/stages/render/sections/sect2_company_exposure.py
  </files>
  <action>
  **sect1_executive.py split:**
  Extract table/visual rendering functions into sect1_executive_tables.py:
  - _render_snapshot() and _build_snapshot_rows() (snapshot table construction)
  - _render_inherent_risk() (risk spectrum visualization)
  - _render_claim_probability() (claim probability table)
  - _render_tower_recommendation() (tower table)
  - Helper functions: _fmt_sourced_currency(), _cite(), _canonical_sector(), _identity_field(), _build_factor_breakdown(), _build_ceiling_line(), _add_tier_indicator(), _add_band_indicator()

  Keep in sect1_executive.py:
  - render_section_1() (the orchestrator)
  - _render_heading()
  - _render_data_quality_notice()
  - _render_thesis()
  - _render_tier()
  - _render_key_negatives()
  - _render_key_positives()

  Import the moved functions in sect1_executive.py and call through.

  **sect2_company_details.py split:**
  Extract D&O exposure rendering into sect2_company_exposure.py:
  - _render_do_exposure_mapping()
  - _render_extracted_exposure()
  - _render_standard_exposure()
  - _add_exposure_indicator()
  - The _DO_EXPOSURE_TYPES constant
  - Helper: _safe_currency(), _safe_pct()

  Keep in sect2_company_details.py:
  - render_company_details() (the orchestrator)
  - _render_revenue_segments()
  - _render_geographic_footprint()
  - _render_geo_subsidiary()
  - _render_geo_revenue()
  - _risk_flag()
  - _render_concentration()
  - _add_concentration_context()
  - _HIGH_RISK_JURISDICTIONS constant

  Both splits follow the same pattern: orchestrator stays, heavy sub-renderers move to a dedicated module.
  </action>
  <verify>
  Run: `wc -l src/do_uw/stages/render/sections/sect1_executive.py` -- must be under 500
  Run: `wc -l src/do_uw/stages/render/sections/sect2_company_details.py` -- must be under 500
  Run: `python -c "from do_uw.stages.render.sections.sect1_executive import render_section_1"` -- must succeed
  Run: `python -c "from do_uw.stages.render.sections.sect2_company_details import render_company_details"` -- must succeed
  Run: `uv run pytest tests/ -x -q` -- all tests pass
  Run: Verify no file in sections/ exceeds 500 lines: `find src/do_uw/stages/render/sections/ -name "*.py" -exec wc -l {} + | awk '$1 > 500 {print}'` -- must be empty
  </verify>
  <done>All three previously over-limit files are under 500 lines. Three new split-out modules exist. All existing tests pass unchanged. No rendering behavior has changed.</done>
</task>

</tasks>

<verification>
- `find src/do_uw/stages/render/sections/ -name "*.py" -exec wc -l {} + | sort -rn | head -5` shows no file over 500
- `uv run pytest tests/ -x -q` passes (zero test modifications needed)
- The word document output for any ticker is identical before and after (purely mechanical refactoring)
</verification>

<success_criteria>
1. Zero files in render/sections/ exceed 500 lines
2. All existing tests pass without modification
3. No rendering behavior changes (pure refactoring)
4. New split modules follow existing naming convention (sect5_governance_comp.py pattern)
</success_criteria>

<output>
After completion, create `.planning/phases/28-presentation-layer-context-through-comparison/28-01-SUMMARY.md`
</output>
