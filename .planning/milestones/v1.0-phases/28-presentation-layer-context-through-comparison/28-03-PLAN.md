---
phase: 28-presentation-layer-context-through-comparison
plan: 03
type: execute
wave: 3
depends_on: ["28-02"]
files_modified:
  - src/do_uw/stages/render/sections/sect1_executive.py
  - src/do_uw/stages/render/sections/sect1_executive_tables.py
  - src/do_uw/stages/render/sections/sect3_financial.py
  - src/do_uw/stages/render/sections/sect3_audit.py
  - src/do_uw/stages/render/sections/sect4_market.py
  - src/do_uw/stages/render/sections/sect4_market_events.py
autonomous: true

must_haves:
  truths:
    - "Financial metrics (leverage, revenue, distress scores) display with peer percentile context when benchmark data is available"
    - "Market metrics (volatility, short interest) display with peer percentile context when benchmark data is available"
    - "Clean financial sections (all distress safe, earnings quality normal) render as 1-2 sentence summary instead of full detail tables"
    - "Clean market sections (no insider cluster selling, normal short interest, no major drops) render concisely"
    - "Problematic sections still render full detail with all tables and forensic breakdown"
    - "Executive summary snapshot table includes peer percentile for market cap"
  artifacts:
    - path: "src/do_uw/stages/render/sections/sect3_financial.py"
      provides: "Peer-context-enriched financial rendering with issue-driven density gating"
    - path: "src/do_uw/stages/render/sections/sect4_market.py"
      provides: "Peer-context-enriched market rendering with issue-driven density gating"
  key_links:
    - from: "src/do_uw/stages/render/sections/sect3_financial.py"
      to: "src/do_uw/stages/render/peer_context.py"
      via: "imports format_metric_with_context, get_benchmark_for_metric"
      pattern: "from do_uw.stages.render.peer_context import"
    - from: "src/do_uw/stages/render/sections/sect4_market.py"
      to: "src/do_uw/stages/render/peer_context.py"
      via: "imports format_metric_with_context, get_benchmark_for_metric"
      pattern: "from do_uw.stages.render.peer_context import"
---

<objective>
Enrich Sections 1, 3, and 4 with peer context and issue-driven density gating.

Purpose: SC1 (context-through-comparison) requires every metric to include peer comparison. SC2 (issue-driven density) requires clean companies to get concise sections. This plan wires peer_context.py helpers into the financial and market section renderers and adds conditional density rendering.

Output: Sections 1/3/4 enriched with "compared to what?" context and clean/problematic branching.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-presentation-layer-context-through-comparison/28-RESEARCH.md
@.planning/phases/28-presentation-layer-context-through-comparison/28-02-SUMMARY.md
@src/do_uw/stages/render/peer_context.py
@src/do_uw/stages/render/sections/sect1_executive.py
@src/do_uw/stages/render/sections/sect1_executive_tables.py
@src/do_uw/stages/render/sections/sect3_financial.py
@src/do_uw/stages/render/sections/sect3_audit.py
@src/do_uw/stages/render/sections/sect4_market.py
@src/do_uw/stages/render/sections/sect4_market_events.py
@src/do_uw/models/scoring.py (BenchmarkResult, MetricBenchmark)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add peer context to Section 1 and Section 3 + financial density gating</name>
  <files>
    src/do_uw/stages/render/sections/sect1_executive.py
    src/do_uw/stages/render/sections/sect1_executive_tables.py
    src/do_uw/stages/render/sections/sect3_financial.py
    src/do_uw/stages/render/sections/sect3_audit.py
  </files>
  <action>
  **Section 1 (Executive Summary) -- peer context enrichment:**
  In sect1_executive_tables.py (or sect1_executive.py depending on where _render_snapshot lives after Plan 01 split):
  - In the snapshot table, add peer percentile for market_cap if benchmark data available
  - Format: "Market Cap: $80.2B (72nd percentile vs. 8 peers)" using format_metric_with_context from peer_context.py
  - Import get_benchmark_for_metric from peer_context.py
  - Add the peer percentile to the snapshot row, not as a separate line

  **Section 3 (Financial Health) -- peer context enrichment:**
  In sect3_financial.py:
  - Import format_metric_with_context and get_benchmark_for_metric from peer_context.py
  - In _render_distress_panel(): when displaying Altman Z-Score, Beneish M-Score, etc., append peer context if the metric has a benchmark entry. Check state.benchmarked for leverage_debt_ebitda metric.
  - In _render_peer_group() (existing peer group table): enhance with percentile rank display from BenchmarkResult for the revenue and leverage metrics

  **Section 3 -- issue-driven density gating:**
  Add a `_is_financial_health_clean(state: AnalysisState) -> bool` function that returns True when:
  - All distress models are in SAFE zone (Altman Z > 2.99, Beneish M > -1.78, Ohlson O < 0.5, Piotroski F >= 5)
  - No material weaknesses
  - No going concern opinions
  - No restatements
  - Leverage ratio below sector median (if benchmark available)

  In _render_distress_panel(), if _is_financial_health_clean() returns True:
  - Render a single summary paragraph: "Financial Integrity: No concerns identified. All distress models in safe zone. Earnings quality metrics within normal ranges."
  - Skip the detailed distress model tables, earnings quality breakdown, and individual model result tables
  - Still render the summary narrative at the top of the section (it provides context regardless)

  If NOT clean: render full detail as before (no changes to the problematic path).

  In sect3_audit.py: If no material weaknesses, no going concern, no restatements, and no late filings, render a concise audit summary: "Audit Risk: No concerns. [Auditor name], [tenure] year tenure, unqualified opinion." instead of the full audit risk table.

  Keep all files under 500 lines.
  </action>
  <verify>
  Run: `uv run pytest tests/ -x -q` -- all tests pass
  Run: `wc -l src/do_uw/stages/render/sections/sect3_financial.py` -- under 500
  Run: `wc -l src/do_uw/stages/render/sections/sect3_audit.py` -- under 500
  Run: `wc -l src/do_uw/stages/render/sections/sect1_executive.py` -- under 500
  </verify>
  <done>Section 1 snapshot includes market cap percentile. Section 3 shows peer context for financial metrics. Clean companies get concise financial health summary; problematic companies get full forensic detail.</done>
</task>

<task type="auto">
  <name>Task 2: Add peer context to Section 4 + market density gating</name>
  <files>
    src/do_uw/stages/render/sections/sect4_market.py
    src/do_uw/stages/render/sections/sect4_market_events.py
  </files>
  <action>
  **Section 4 (Market & Trading) -- peer context enrichment:**
  In sect4_market.py:
  - Import format_metric_with_context and get_benchmark_for_metric from peer_context.py
  - When rendering volatility metrics (90-day volatility), add peer context: "90-Day Volatility: 42.3% (85th percentile vs. peers; median 28.1%)"
  - When rendering short interest, add peer context: "Short Interest: 8.2% (91st percentile vs. peers; median 3.1%)"
  - Look up volatility_90d and short_interest_pct in BenchmarkResult.metric_benchmarks or peer_rankings

  **Section 4 -- issue-driven density gating:**
  Add `_is_market_clean(state: AnalysisState) -> bool` that returns True when:
  - No stock drops >10% in trailing 18 months
  - No insider cluster selling (3+ insiders within 30 days)
  - Short interest below 5% (or below sector median if benchmark available)
  - No earnings misses >10% in last 4 quarters
  - No adverse events flagged

  In sect4_market.py, if _is_market_clean() returns True:
  - For the insider trading subsection: render "Insider Trading: No unusual activity. [N] transactions in trailing 12 months, no cluster selling identified." instead of the full transaction table
  - For the short interest subsection: render a one-liner with peer context instead of the full analysis

  In sect4_market_events.py, if no stock drops qualify for display:
  - Render "Stock Performance: No significant single-day (>5%) or multi-day (>10%) declines in the analysis period." instead of the empty drop table

  Ensure full detail still renders for problematic companies (no changes to the elevated path).

  Keep all files under 500 lines.
  </action>
  <verify>
  Run: `uv run pytest tests/ -x -q` -- all tests pass
  Run: `wc -l src/do_uw/stages/render/sections/sect4_market.py` -- under 500
  Run: `wc -l src/do_uw/stages/render/sections/sect4_market_events.py` -- under 500
  </verify>
  <done>Section 4 shows peer context for volatility and short interest. Clean market sections render concisely; problematic sections render full detail with event tables and forensic analysis.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x -q` passes
- Peer context appears in Sections 1, 3, and 4 when benchmark data is available
- Clean companies get concise financial/market summaries
- Problematic companies get unchanged full-detail rendering
- No file exceeds 500 lines
</verification>

<success_criteria>
1. Financial metrics show "compared to what?" context (SC1 for Sections 1/3)
2. Market metrics show "compared to what?" context (SC1 for Section 4)
3. Clean financial/market sections are concise (SC2 for Sections 3/4)
4. Problematic sections retain full detail (SC2 preservation)
5. All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/28-presentation-layer-context-through-comparison/28-03-SUMMARY.md`
</output>
