---
phase: 33-brain-driven-worksheet-architecture
plan: 04
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - src/do_uw/brain/checks.json
  - src/do_uw/brain/enrichment_data.py
  - src/do_uw/stages/extract/market_signals.py
  - src/do_uw/stages/extract/market_helpers.py
  - src/do_uw/models/market.py
  - src/do_uw/stages/extract/company_profile.py
  - config/sic_gics_mapping.json
  - tests/brain/test_subsection_reorg.py
  - tests/stages/extract/test_market_easy_wins.py
autonomous: true
requirements:
  - SC1-question-specs
  - SC2-section-artifacts
  - SC3-acquisition-audit

must_haves:
  truths:
    - "checks.json v6_subsection_ids reflect the 36-subsection structure (no references to absorbed 1.5, 1.7, 4.9, or old 4.1-4.9 numbering)"
    - "enrichment_data.py SUBDOMAIN_TO_RISK_QUESTIONS and CHECK_TO_RISK_QUESTIONS updated for reorganized Section 4 (4.1=People Risk, 4.2=Structural Governance, 4.3=Transparency, 4.4=Activist) and merged subsections"
    - "MarketSignals model has new fields: avg_daily_volume, analyst_count, pe_ratio, ev_ebitda, peg_ratio (all SourcedValue) — populated from yfinance info dict during extraction"
    - "CompanyProfile has gics_code field populated from yfinance or SIC mapping"
    - "config/sic_gics_mapping.json exists with standard SIC-to-GICS crosswalk"
    - "All existing tests pass after reorganization"
  artifacts:
    - path: "src/do_uw/brain/checks.json"
      provides: "36-subsection v6_subsection_ids mapping"
      contains: "v6_subsection_ids"
    - path: "src/do_uw/brain/enrichment_data.py"
      provides: "Updated SUBDOMAIN_TO_RISK_QUESTIONS for reorganized sections"
      contains: "SUBDOMAIN_TO_RISK_QUESTIONS"
    - path: "config/sic_gics_mapping.json"
      provides: "SIC to GICS code mapping table"
      contains: "3571"
    - path: "tests/brain/test_subsection_reorg.py"
      provides: "Tests validating 36-subsection structure"
  key_links:
    - from: "src/do_uw/brain/enrichment_data.py"
      to: "src/do_uw/brain/checks.json"
      via: "v6_subsection_ids derived from enrichment mappings"
      pattern: "SUBDOMAIN_TO_RISK_QUESTIONS"
    - from: "src/do_uw/stages/extract/market_signals.py"
      to: "src/do_uw/models/market.py"
      via: "yfinance info dict field extraction"
      pattern: "beta|analyst_count|pe_ratio"
---

<objective>
Codify the question review's subsection reorganization (45->36) into checks.json and enrichment_data.py, and surface easy-win data fields that are already acquired by yfinance but not stored in the state model.

Purpose: The comprehensive question review identified 9 subsections to absorb/merge and 11 data needs (DN-005, DN-009, DN-010, DN-011, DN-012, DN-013, DN-015, DN-028, DN-032, DN-033, DN-034) that are "easy wins" — data already acquired but not surfaced. This plan codifies both the structural reorganization and the easy-win field extraction.

Output: Updated checks.json with 36-subsection mapping, enrichment_data.py with reorganized Section 4, new fields on market model populated from yfinance, GICS code on company profile, SIC-GICS mapping config.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-brain-driven-worksheet-architecture/33-01-SUMMARY.md
@.planning/phases/33-brain-driven-worksheet-architecture/REVIEW-DECISIONS.md
@.planning/phases/33-brain-driven-worksheet-architecture/DATA-NEEDS.md

Key reference files:
@src/do_uw/brain/enrichment_data.py
@src/do_uw/brain/checks.json
@src/do_uw/models/market.py
@src/do_uw/models/company.py
@src/do_uw/stages/extract/company_profile.py
@src/do_uw/stages/extract/market_signals.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorganize subsection mappings (45->36) in enrichment_data.py and checks.json</name>
  <files>
    src/do_uw/brain/enrichment_data.py
    src/do_uw/brain/checks.json
    config/sic_gics_mapping.json
    tests/brain/test_subsection_reorg.py
  </files>
  <action>
    **Part A: Update enrichment_data.py SUBDOMAIN_TO_RISK_QUESTIONS**

    Apply these subsection changes from REVIEW-DECISIONS.md:

    1. **Absorbed subsections (remove as standalone, add cross-refs):**
       - 1.5 (Geographic Footprint) -> absorbed into 1.2 + 1.3. Remove "1.5" from all mappings. Any check that had "1.5" should have "1.2" or "1.3" instead.
       - 1.7 (Competitive Position) -> absorbed into 1.2 + 1.8. Change `"BIZ.COMP": ["1.7"]` to `"BIZ.COMP": ["1.2", "1.8"]`.
       - 4.9 (Media & External) -> absorbed into Early Warning Signals (1.9). Remove "4.9" references, map to "1.9" or "1.10".

    2. **Section 4 reorganization (old 4.1-4.9 -> new 4.1-4.4):**
       - NEW 4.1 People Risk = old 4.1 (Board) + old 4.2 (Executive). Map GOV.BOARD -> ["4.1"], GOV.EFFECT -> ["4.1"], GOV.EXEC -> ["4.1"], EXEC.PROFILE -> ["4.1"], EXEC.TENURE -> ["4.1"], EXEC.DEPARTURE -> ["4.1"], EXEC.PRIOR_LIT -> ["4.1"].
       - NEW 4.2 Structural Governance = old 4.3 (Comp) + old 4.4 (Rights) + old 1.4.3 (RPT). Map GOV.PAY -> ["4.2"], GOV.RIGHTS -> ["4.2"]. Add BIZ.STRUCT.related_party -> ["1.4", "4.2"].
       - NEW 4.3 Transparency & Disclosure = old 4.6 + old 4.7 + old 4.8. Map NLP.RISK -> ["4.3"], NLP.MDA -> ["4.3"], NLP.DISCLOSURE -> ["4.3"], NLP.FILING -> ["4.3"], NLP.WHISTLE -> ["4.3"], NLP.CAM stays at ["3.5"], FWRD.DISC -> ["4.3"], FWRD.NARRATIVE -> ["4.3"].
       - NEW 4.4 Activist Pressure = old 4.5. Map GOV.ACTIVIST -> ["4.4"].

    3. **Merged subsections:**
       - 1.9+1.10 -> "1.9" (Early Warning Signals). Remove "1.10" references, consolidate to "1.9".
       - 5.7+5.8+5.9 -> "5.7" (Litigation Risk Analysis). Change LIT.DEFENSE -> ["5.7"], LIT.PATTERN -> ["5.7"], LIT.SECTOR -> ["5.7"]. Remove "5.8" and "5.9" references.

    4. Update CHECK_TO_RISK_QUESTIONS similarly — for every explicit check mapping that references absorbed/old subsection IDs, update to the new IDs. Key ones:
       - Any "1.5" -> "1.2" or "1.3" as appropriate
       - Any "1.7" -> "1.2" or "1.8" as appropriate
       - Any "1.10" -> "1.9"
       - Any "4.1" (old board) -> "4.1" (new people risk — same ID, broader scope)
       - Any "4.2" (old executive) -> "4.1" (new people risk)
       - Any "4.3" (old compensation) -> "4.2" (new structural governance)
       - Any "4.4" (old shareholder rights) -> "4.2" (new structural governance)
       - Any "4.5" (old activist) -> "4.4" (new activist pressure)
       - Any "4.6" (old disclosure quality) -> "4.3" (new transparency)
       - Any "4.7" (old narrative) -> "4.3" (new transparency)
       - Any "4.8" (old whistleblower) -> "4.3" (new transparency)
       - Any "4.9" (old media) -> "1.9" (early warning signals)
       - Any "5.8" -> "5.7" (merged litigation risk)
       - Any "5.9" -> "5.7" (merged litigation risk)

    **Part B: Regenerate v6_subsection_ids in checks.json**

    After updating enrichment_data.py, regenerate v6_subsection_ids for all 384 checks. This is the same process used in Plan 01 — read CHECK_TO_RISK_QUESTIONS for explicit mappings, fall back to SUBDOMAIN_TO_RISK_QUESTIONS for prefix-based defaults. Every check should end up with only valid new subsection IDs (no 1.5, 1.7, 1.10, 4.2-old, 4.3-old, etc.).

    Validate: No check has any of these removed IDs: "1.5", "1.7", "1.10", "4.5", "4.6", "4.7", "4.8", "4.9", "5.8", "5.9". The valid subsection IDs are: 1.1, 1.2, 1.3, 1.4, 1.6, 1.8, 1.9, 1.11, 2.1-2.8, 3.1-3.8, 4.1-4.4, 5.1-5.7.

    **Part C: Create config/sic_gics_mapping.json**

    Create a JSON mapping file with common SIC codes mapped to 8-digit GICS codes. At minimum include:
    - 3571 (Electronic Computers) -> 45202030 (Technology Hardware, Storage and Peripherals)
    - 7372 (Prepackaged Software) -> 45103010 (Application Software)
    - 3674 (Semiconductors) -> 45301020 (Semiconductors)
    - 6021 (National Commercial Banks) -> 40101010 (Diversified Banks)
    - 2834 (Pharmaceutical Preparations) -> 35201010 (Pharmaceuticals)
    - 5912 (Drug Stores) -> 30101010 (Drug Retail)
    - 7371 (Computer Services) -> 45102010 (IT Consulting)
    - Plus at least 30 additional common SIC-GICS mappings covering major industries (energy, retail, telecom, auto, insurance, aerospace, biotech, etc.)

    **Part D: Write tests**

    Create `tests/brain/test_subsection_reorg.py`:
    - Test that no check in checks.json has a removed subsection ID
    - Test that every check has at least one valid subsection ID from the 36-subsection set
    - Test that the Section 4 reorganization is correct (GOV.BOARD checks map to 4.1, GOV.PAY to 4.2, NLP.RISK to 4.3, GOV.ACTIVIST to 4.4)
    - Test that merged subsections are consolidated (no 5.8/5.9, only 5.7)
    - Test that config/sic_gics_mapping.json loads and has at least 30 entries
    - Test that mapping contains expected entries (3571 -> 45202030, etc.)
  </action>
  <verify>
    Run: `cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/brain/test_subsection_reorg.py -v`
    Run: `cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/brain/ -v` (ensure existing brain tests still pass)
    Run: `cd /Users/gorlin/projects/UW/do-uw && uv run pyright src/do_uw/brain/enrichment_data.py`
  </verify>
  <done>
    enrichment_data.py reflects 36-subsection structure. checks.json has no removed IDs. config/sic_gics_mapping.json has 30+ entries. All brain tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Surface easy-win yfinance fields into MarketSignals and CompanyProfile models</name>
  <files>
    src/do_uw/models/market.py
    src/do_uw/stages/extract/market_signals.py
    src/do_uw/stages/extract/market_helpers.py
    src/do_uw/stages/extract/company_profile.py
    tests/stages/extract/test_market_easy_wins.py
  </files>
  <action>
    **Part A: Add missing fields to models**

    In `src/do_uw/models/market.py`, add to `StockPerformance`:
    - `avg_daily_volume: SourcedValue[int] | None = Field(default=None, description="Average daily trading volume (10-day)")` (DN-032)

    Note: `beta` already exists on StockPerformance. Verify it is populated during extraction.

    Find the AnalystSentimentProfile model (in market_events.py or wherever it lives). It already has `consensus`, `recommendation_mean`, `target_price_mean`, etc. Verify these are populated. If not, the issue is in the extraction code, not the model. Also check if `analyst_count` field exists — if not, add it:
    - `analyst_count: SourcedValue[int] | None = Field(default=None, description="Number of analysts covering")`

    For valuation ratios: The FIELD_FOR_CHECK already routes to `pe_ratio`, `ev_ebitda`, `peg_ratio`. These fields need to exist on either StockPerformance or a new ValuationMetrics sub-model on MarketSignals. Check if they exist already. If not, add them to StockPerformance or create a small ValuationMetrics class:
    - `pe_ratio: SourcedValue[float] | None` (trailing P/E)
    - `forward_pe: SourcedValue[float] | None` (forward P/E)
    - `ev_ebitda: SourcedValue[float] | None` (Enterprise Value / EBITDA)
    - `peg_ratio: SourcedValue[float] | None` (Price/Earnings to Growth)

    In `src/do_uw/models/company.py`, add to CompanyProfile:
    - `gics_code: SourcedValue[str] | None = Field(default=None, description="8-digit GICS industry code")`

    **Part B: Populate fields during extraction**

    In the market extraction code (find where yfinance `info` dict is consumed — likely `market_signals.py` or `market_helpers.py`), add extraction of:

    1. `info['averageDailyVolume10Day']` or `info['averageVolume']` -> `stock.avg_daily_volume`
    2. `info['beta']` -> `stock.beta` (verify already done, fix if not)
    3. `info['numberOfAnalystOpinions']` -> `analyst.analyst_count`
    4. `info['recommendationMean']` -> `analyst.recommendation_mean` (verify already done)
    5. `info['targetMeanPrice']` -> `analyst.target_price_mean` (verify already done)
    6. `info['targetHighPrice']` -> `analyst.target_price_high` (verify)
    7. `info['targetLowPrice']` -> `analyst.target_price_low` (verify)
    8. `info['trailingPE']` -> valuation pe_ratio
    9. `info['forwardPE']` -> valuation forward_pe
    10. `info['enterpriseToEbitda']` -> valuation ev_ebitda
    11. `info['pegRatio']` -> valuation peg_ratio

    For GICS code in company_profile.py:
    12. Try `info.get('sectorKey')` + `info.get('industryKey')` to compose GICS, OR use the SIC code from the company identity and look up in config/sic_gics_mapping.json as fallback. Store in `profile.gics_code`.

    For DN-028 (Goodwill-to-assets ratio): Check if goodwill and total_assets are already in ExtractedFinancials. If yes, compute the ratio and store it. If goodwill is available from XBRL (likely under `us-gaap:Goodwill`), add a `goodwill_to_assets: SourcedValue[float] | None` field to the financials model and compute it.

    Use `sourced_float()` / `sourced_int()` / `sourced_str()` helpers from `sourced.py` to create properly sourced values with source="yfinance" and confidence=MEDIUM.

    **Important:** Keep file modifications under the 500-line limit. If market_signals.py is approaching the limit, put new extraction helpers in market_helpers.py or create a new small helper file.

    **Part C: Write tests**

    Create `tests/stages/extract/test_market_easy_wins.py`:
    - Test that StockPerformance model accepts avg_daily_volume
    - Test that valuation fields exist on model (pe_ratio, ev_ebitda, peg_ratio, forward_pe)
    - Test that CompanyProfile accepts gics_code
    - Test that AnalystSentimentProfile has analyst_count field
    - Mock yfinance info dict and verify extraction populates all new fields
    - Test SIC-to-GICS fallback: given SIC=3571 and no yfinance GICS, verify gics_code is populated from mapping
  </action>
  <verify>
    Run: `cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/extract/test_market_easy_wins.py -v`
    Run: `cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/ -x --timeout=60` (full test suite, fail fast)
    Run: `cd /Users/gorlin/projects/UW/do-uw && uv run pyright src/do_uw/models/market.py src/do_uw/models/company.py`
  </verify>
  <done>
    MarketSignals has avg_daily_volume, valuation ratios, analyst_count populated from yfinance. CompanyProfile has gics_code populated from yfinance or SIC mapping. All tests pass. Pyright clean.
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/brain/ -v` — all brain tests pass including new reorganization tests
2. `uv run pytest tests/stages/extract/ -v` — all extraction tests pass including easy-win fields
3. `uv run pyright src/do_uw/brain/ src/do_uw/models/ src/do_uw/stages/extract/` — no type errors
4. No check in checks.json references removed subsection IDs (1.5, 1.7, 1.10, 4.5-4.9, 5.8, 5.9)
5. config/sic_gics_mapping.json loads and has 30+ entries
</verification>

<success_criteria>
- 36-subsection structure codified in enrichment_data.py and checks.json
- Section 4 reorganized: 4.1=People Risk, 4.2=Structural Governance, 4.3=Transparency, 4.4=Activist
- 11 easy-win data needs addressed: DN-005 (GICS), DN-015 (SIC mapping), DN-032 (beta+volume), DN-033 (analyst), DN-034 (valuation ratios), DN-028 (goodwill ratio)
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/33-brain-driven-worksheet-architecture/33-04-SUMMARY.md`
</output>
