---
phase: 33-brain-driven-worksheet-architecture
plan: 06
type: execute
wave: 3
depends_on: [33-04, 33-05]
files_modified:
  - .planning/phases/33-brain-driven-worksheet-architecture/33-VALIDATION.md
autonomous: true
requirements:
  - SC7-end-to-end-validation
  - SC4-false-trigger-elimination
  - SC3-acquisition-audit
  - SC5-zero-coverage

must_haves:
  truths:
    - "Running AAPL through the pipeline produces check results with no known false triggers"
    - "Every 36 subsections shows either real data or explicit 'data not available' — no silent gaps"
    - "All TRIGGERED checks verified against their question context — triggers answer the right question"
    - "A validation report documents per-subsection status with question counts, check counts, data availability, and remaining gaps"
    - "The validation report explicitly lists what changed from Plans 01-05 and what gaps remain for future phases"
    - "Valuation ratio fields (pe_ratio, ev_ebitda, peg_ratio) populated for AAPL (not SKIPPED)"
    - "Analyst coverage fields (analyst_count, recommendation_mean, target_price_mean) populated for AAPL"
    - "GICS code populated for AAPL"
  artifacts:
    - path: ".planning/phases/33-brain-driven-worksheet-architecture/33-VALIDATION.md"
      provides: "End-to-end validation report for AAPL"
      contains: "Per-Subsection Status"
  key_links:
    - from: "33-VALIDATION.md"
      to: "REVIEW-DECISIONS.md"
      via: "Validates that review decisions are codified"
      pattern: "36 subsections"
---

<objective>
Run the full AAPL pipeline end-to-end with all Plan 01-05 changes and produce a comprehensive validation report documenting per-subsection status, false trigger audit, data gap inventory, and comparison to the pre-Phase-33 baseline.

Purpose: This is the quality gate for Phase 33. It proves that the subsection reorganization, easy-win field surfacing, and wiring/calibration fixes all work together in a real pipeline run. It also documents exactly what remains for future phases (DN items still open, extraction gaps, deferred work).

Output: 33-VALIDATION.md with comprehensive per-subsection assessment.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-brain-driven-worksheet-architecture/33-04-SUMMARY.md
@.planning/phases/33-brain-driven-worksheet-architecture/33-05-SUMMARY.md
@.planning/phases/33-brain-driven-worksheet-architecture/REVIEW-DECISIONS.md
@.planning/phases/33-brain-driven-worksheet-architecture/DATA-NEEDS.md
@.planning/phases/33-brain-driven-worksheet-architecture/QUESTION-TRACKER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run AAPL pipeline and audit all check results</name>
  <files>
    .planning/phases/33-brain-driven-worksheet-architecture/33-VALIDATION.md
  </files>
  <action>
    **Step 1: Run full AAPL pipeline**

    Run the pipeline for AAPL using cached data (no need for fresh acquisition — data is already cached from prior runs):
    ```
    cd /Users/gorlin/projects/UW/do-uw && uv run do-uw analyze AAPL
    ```

    If the pipeline fails, diagnose and document the failure. If it's a code error from Plans 04-05, this must be fixed before proceeding (either document what to fix or create a hot-fix patch).

    **Step 2: Extract check results**

    After pipeline completes, extract the AAPL analysis results. Look for the state JSON output or use the pipeline's result object. Capture:
    - Total checks executed vs skipped
    - All TRIGGERED checks (count + list)
    - All SKIPPED checks (count + by reason)
    - All INFO checks (count + by subsection)
    - All CLEAR checks (count)
    - Check execution by subsection (using v6_subsection_ids from checks.json)

    **Step 3: Audit TRIGGERED checks**

    For every TRIGGERED check, verify:
    1. The check routes to the correct field (not a false trigger from wrong field)
    2. The threshold makes sense for the field value
    3. The trigger answers the question it's supposed to answer

    Known TRIGGERED checks from prior run (verify still correct after fixes):
    - BIZ.CLASS.litigation_history: total_sca_count=1 -> TRIGGERED (correct)
    - EXEC.INSIDER.ceo_net_selling: 100% seller -> TRIGGERED (correct)
    - EXEC.INSIDER.cfo_net_selling: 100% seller -> TRIGGERED (correct)
    - EXEC.INSIDER.cluster_selling: True -> TRIGGERED (correct)
    - GOV.PAY.ceo_total: pay_ratio=533 -> TRIGGERED (correct)
    - FIN.LIQ.position: current_ratio=0.89 -> Should be RED after threshold fix
    - LIT.SCA.historical: total_sca_count=1 -> TRIGGERED (correct)
    - LIT.SCA.derivative: derivative_suit_count=3 -> TRIGGERED (correct)
    - Plus others

    Verify NO false triggers remain after Plan 05 fixes. Specifically check:
    - BIZ.DEPEND.labor does NOT trigger on employee_count=150000
    - STOCK.TRADE.liquidity does NOT evaluate current_price
    - STOCK.ANALYST.coverage does NOT evaluate beat_rate
    - STOCK.VALUATION checks do NOT skip (should evaluate pe_ratio etc.)
    - FIN.GUIDE checks do NOT all return INFO from narrative field

    **Step 4: Audit new data fields**

    Verify that Plan 04 easy-win fields are populated for AAPL:
    - gics_code on CompanyProfile (should be populated via SIC mapping or yfinance)
    - avg_daily_volume on StockPerformance (should be populated from yfinance)
    - beta on StockPerformance (should already be populated, verify)
    - analyst_count on AnalystSentimentProfile (new field)
    - recommendation_mean, target_price_mean (verify populated)
    - pe_ratio, ev_ebitda, peg_ratio, forward_pe (new valuation fields)

    **Step 5: Verify Plan 05 calibration fixes**

    Check that:
    - FIN.LIQ.position evaluates 0.89 against <1.0 threshold (RED, not just INFO)
    - LIT.REG.sec_investigation with sec_enforcement_stage="NONE" -> CLEAR
    - LIT.REG.wells_notice with wells_notice=False -> CLEAR
    - BIZ.DEPEND.customer_conc with "Not mentioned" -> CLEAR positive

    **Step 6: Per-subsection coverage assessment**

    For each of the 36 subsections, compute:
    - Questions in subsection (from QUESTION-TRACKER.md)
    - Checks mapped to subsection (from checks.json v6_subsection_ids)
    - Check results: how many TRIGGERED, CLEAR, INFO, SKIPPED, NOT_RUN
    - Data availability: what % of questions have data flowing
    - Overall assessment: GREEN (>75% answered), YELLOW (25-75%), RED (<25%)
    - Remaining gaps: which DN items still block this subsection

    **Step 7: Write 33-VALIDATION.md**

    Structure the validation report as:

    ```markdown
    # Phase 33 End-to-End Validation Report

    ## Pipeline Run Summary
    - Date, ticker, pipeline duration
    - Total checks: X executed, Y skipped, Z triggered, W clear

    ## Changes from Plans 01-05
    - Subsection reorganization: 45 -> 36 subsections
    - Easy-win fields surfaced: list new fields + values
    - Wiring fixes: list routing changes + impact
    - Calibration fixes: list threshold/evaluation changes

    ## False Trigger Audit
    - Prior false triggers: 5 (list from 33-02-SUMMARY.md)
    - New false triggers found: N (list any)
    - All TRIGGERED checks verified: table with check_id, field, value, threshold, correct?

    ## Per-Subsection Status (36 subsections)
    Table: subsection | questions | checks | triggered | clear | info | skipped | assessment | blockers

    ## New Data Fields Verification
    Table: field | model | value_for_aapl | source | populated?

    ## Remaining Gaps (Deferred to Future Phases)
    - DN items still open: list with target phase
    - Extraction gaps: what data still not acquired
    - Deferred work: DN-036 (DEF 14A), DN-031 (web intel), DN-037 (lit risk), etc.

    ## Comparison to Pre-Phase-33 Baseline
    - Checks that changed status (e.g., SKIPPED -> CLEAR, INFO -> TRIGGERED)
    - New checks added (from Plan 03)
    - Subsections that improved

    ## Conclusion
    Summary assessment of Phase 33 impact
    ```
  </action>
  <verify>
    Verify the pipeline runs to completion: `cd /Users/gorlin/projects/UW/do-uw && uv run do-uw analyze AAPL` exits 0
    Verify 33-VALIDATION.md exists and contains "Per-Subsection Status" section
    Verify the report covers all 36 subsections
    Verify the false trigger audit section lists all TRIGGERED checks with verification
  </verify>
  <done>
    AAPL pipeline runs to completion with no errors. 33-VALIDATION.md documents complete per-subsection assessment for all 36 subsections. All TRIGGERED checks verified as correct (no false triggers). New data fields confirmed populated. Remaining gaps documented with target phase for future work.
  </done>
</task>

</tasks>

<verification>
1. AAPL pipeline runs successfully end-to-end
2. 33-VALIDATION.md exists with comprehensive per-subsection assessment
3. No known false triggers remain (all TRIGGERED checks answer their intended question)
4. New data fields (GICS, valuation ratios, analyst count, avg volume) are populated for AAPL
5. Wiring fixes verified (analyst -> analyst_count, trade -> avg_daily_volume, guidance -> guidance fields)
6. Calibration fixes verified (FIN.LIQ thresholds, SEC enforcement CLEAR, customer concentration positive)
7. Remaining gaps documented with deferred DN items and target phases
</verification>

<success_criteria>
- Pipeline produces a complete AAPL worksheet with no false triggers
- Validation report covers all 36 subsections with assessment ratings
- Every TRIGGERED check verified against question context
- Easy-win fields confirmed populated
- Deferred work clearly documented for Phase 34+
</success_criteria>

<output>
After completion, create `.planning/phases/33-brain-driven-worksheet-architecture/33-06-SUMMARY.md`
</output>
