---
phase: 33-brain-driven-worksheet-architecture
plan: 03
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - src/do_uw/brain/checks.json
  - src/do_uw/brain/enrichment_data.py
  - src/do_uw/stages/analyze/check_mappers.py
  - src/do_uw/stages/analyze/check_mappers_ext.py
  - src/do_uw/stages/analyze/check_field_routing.py
  - tests/brain/test_zero_coverage_checks.py
autonomous: true
requirements:
  - SC5-zero-coverage-closure

must_haves:
  truths:
    - "Subsection 1.4 (Corporate Structure) has at least 3 checks mapping subsidiary count, VIE presence, and related-party complexity"
    - "Subsection 4.9 (Media & External Narrative) has at least 2 checks mapping social sentiment and journalism activity"
    - "Subsection 5.7 (Defense Posture & Reserves) has at least 3 checks mapping defense provisions, contingent liabilities, and defense success"
    - "Subsection 5.8 (Litigation Risk Patterns) has at least 4 checks mapping SOL windows, industry theories, contagion risk, and temporal correlation"
    - "Subsection 5.9 (Sector-Specific Litigation) has at least 2 checks mapping sector patterns and regulatory databases"
    - "All new checks have v6_subsection_ids assigned and are properly mapped in enrichment_data.py"
    - "New checks have field routing in check_field_routing.py that maps to existing extracted data where available"
  artifacts:
    - path: "src/do_uw/brain/checks.json"
      provides: "15-20 new checks for zero-coverage subsections"
      contains: "corporate_structure"
    - path: "tests/brain/test_zero_coverage_checks.py"
      provides: "Test validating new checks exist and are properly structured"
      contains: "test_subsection_1_4"
  key_links:
    - from: "src/do_uw/brain/checks.json"
      to: "src/do_uw/stages/analyze/check_field_routing.py"
      via: "New checks must have field routing entries"
      pattern: "BIZ.STRUCT|LIT.DEFENSE|LIT.PATTERN"
    - from: "src/do_uw/stages/analyze/check_mappers.py"
      to: "src/do_uw/stages/analyze/check_mappers_ext.py"
      via: "New prefix mappers imported and registered from ext module"
      pattern: "check_mappers_ext"
    - from: "src/do_uw/brain/enrichment_data.py"
      to: "src/do_uw/brain/checks.json"
      via: "New check IDs added to CHECK_TO_RISK_QUESTIONS"
      pattern: "5\\.7|5\\.8|5\\.9|1\\.4|4\\.9"
---

<objective>
Create new checks for the 5 zero-coverage subsections (1.4, 4.9, 5.7, 5.8, 5.9) so every subsection has at least some check coverage.

Purpose: The QUESTION-AUDIT identified 5 subsections with 14 total questions and ZERO checks mapped. These include high-impact D&O underwriting concerns like defense posture, litigation patterns, and corporate structure complexity. This plan creates new checks that map to existing extracted data where available, and creates SKIPPED/placeholder checks where data acquisition is needed (deferred to future phases).

Output: ~15-20 new checks in checks.json with full metadata, enrichment mappings, field routing, and data mappers.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/33-brain-driven-worksheet-architecture/33-01-SUMMARY.md
@src/do_uw/brain/checks.json
@src/do_uw/brain/enrichment_data.py
@src/do_uw/stages/analyze/check_mappers.py
@src/do_uw/stages/analyze/check_field_routing.py
@.planning/phases/32-knowledge-driven-acquisition-analysis-pipeline/QUESTION-AUDIT.md (zero-coverage subsections)
@.planning/phases/33-brain-driven-worksheet-architecture/33-RESEARCH.md (Zero-Coverage Subsections section)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create new checks for subsections 1.4, 4.9, 5.7, 5.8, 5.9</name>
  <files>
    src/do_uw/brain/checks.json
    src/do_uw/brain/enrichment_data.py
  </files>
  <action>
Add new checks to checks.json for each zero-coverage subsection. Each check needs the full metadata structure matching existing checks (id, name, section, pillar, factors, required_data, data_locations, threshold, execution_mode, claims_correlation, tier, category, signal_type, hazard_or_signal, plaintiff_lenses, content_type, depth, data_strategy, v6_subsection_ids).

**1.4 Corporate Structure & Complexity (3 new checks):**
- `BIZ.STRUCT.subsidiary_count` — Subsidiary count from 10-K Exhibit 21 data. EVALUATIVE_CHECK with threshold: red > 100, yellow > 50, clear <= 50 subsidiaries. Data source: SEC_10K (exhibit_21 or subsidiaries section). Many companies list this in Item 1. If not available from structured data, route to None (SKIPPED). v6: ["1.4"]
- `BIZ.STRUCT.vie_spe` — VIE/SPE/off-balance-sheet structures presence. EVALUATIVE_CHECK (boolean). Data from 10-K footnotes (consolidation note). Threshold: red if VIE/SPE present (higher complexity). v6: ["1.4"]
- `BIZ.STRUCT.related_party` — Related-party transaction complexity. EVALUATIVE_CHECK. Data from DEF 14A related-party section or 10-K footnotes. Threshold based on count/magnitude. v6: ["1.4"]

**4.9 Media & External Narrative (2 new checks):**
- `FWRD.WARN.social_sentiment` — Social media sentiment analysis. If this check already exists but is SKIPPED, verify its v6_subsection_ids include ["4.9"]. If not, create it. Content type: EVALUATIVE_CHECK. Data source: WEB_SEARCH. v6: ["4.9"]
- `FWRD.WARN.journalism_activity` — Investigative journalism/short seller report activity. If exists but SKIPPED, add v6 mapping. Else create. Content type: EVALUATIVE_CHECK. Data source: WEB_SEARCH. v6: ["4.9"]

**5.7 Defense Posture & Reserves (3 new checks):**
- `LIT.DEFENSE.forum_selection` — Federal forum selection clause presence. EVALUATIVE_CHECK. Data from DEF 14A (GOV.RIGHTS.forum_select extraction exists). Clear if present (good defense provision), yellow if absent. v6: ["5.7"]
- `LIT.DEFENSE.contingent_liabilities` — Contingent liabilities disclosure (ASC 450). EVALUATIVE_CHECK. Data from 10-K footnotes (contingent_liab.py extraction exists). Threshold on magnitude relative to equity. v6: ["5.7"]
- `LIT.DEFENSE.pslra_safe_harbor` — PSLRA safe harbor usage in forward-looking statements. MANAGEMENT_DISPLAY. Data from 10-K Item 7 MD&A. v6: ["5.7"]

**5.8 Litigation Risk Patterns (4 new checks):**
- `LIT.PATTERN.sol_windows` — Open statute of limitations windows per claim type. EVALUATIVE_CHECK. Data from sol_mapper.py (already exists). Threshold: red > 3 open windows, yellow > 1. v6: ["5.8"]
- `LIT.PATTERN.industry_theories` — Industry-specific allegation theories applicable. EVALUATIVE_CHECK. Data from industry_theories.json (already exists). Threshold: red > 5 applicable theories. v6: ["5.8"]
- `LIT.PATTERN.peer_contagion` — Contagion risk from peer lawsuits. INFERENCE_PATTERN. Data from SCAC + peer group. v6: ["5.8", "1.7"]
- `LIT.PATTERN.temporal_correlation` — Financial events correlating with stock drops creating class period windows. INFERENCE_PATTERN. Data from stock_drops.py (already exists). v6: ["5.8", "2.2"]

**5.9 Sector-Specific Litigation (2 new checks):**
- `LIT.SECTOR.industry_patterns` — Sector-specific litigation patterns from industry playbooks. EVALUATIVE_CHECK. Data from Phase 9 industry playbooks. v6: ["5.9"]
- `LIT.SECTOR.regulatory_databases` — Sector-specific regulatory database checks (CFPB, NHTSA, etc.). EVALUATIVE_CHECK. Data source: sector-conditional. v6: ["5.9"]

For all new checks:
- Set execution_mode: "AUTO"
- Set appropriate tier (2 or 3 for new checks)
- Set claims_correlation based on D&O relevance (1.0 for litigation checks, 0.5-0.8 for company/governance)
- Add explicit entries to CHECK_TO_RISK_QUESTIONS in enrichment_data.py

IMPORTANT: Check whether any of the FWRD.WARN checks referenced (social_sentiment, journalism_activity) already exist in checks.json. If they do, just ensure their v6_subsection_ids are set correctly. Do not create duplicates.
  </action>
  <verify>
Run: `python3 -c "
import json
d = json.load(open('src/do_uw/brain/checks.json'))
checks = d['checks']
new_ids = [c['id'] for c in checks if any(s in c.get('v6_subsection_ids', []) for s in ['1.4', '4.9', '5.7', '5.8', '5.9'])]
print(f'Checks covering zero-coverage subsections: {len(new_ids)}')
for cid in sorted(new_ids):
    c = next(x for x in checks if x['id'] == cid)
    print(f'  {cid}: {c[\"v6_subsection_ids\"]}')
"`
At least 14 checks should appear covering all 5 previously-zero subsections.
  </verify>
  <done>All 5 zero-coverage subsections now have checks. Each new check has full metadata, v6_subsection_ids, and enrichment_data.py mappings.</done>
</task>

<task type="auto">
  <name>Task 2: Add field routing and data mappers for new checks</name>
  <files>
    src/do_uw/stages/analyze/check_field_routing.py
    src/do_uw/stages/analyze/check_mappers.py
    src/do_uw/stages/analyze/check_mappers_ext.py
    tests/brain/test_zero_coverage_checks.py
  </files>
  <action>
1. **Field routing (check_field_routing.py):** Add routing entries for all new check IDs. For checks where extracted data exists (forum_selection, contingent_liabilities, sol_windows, industry_theories, temporal_correlation), route to the correct field in ExtractedData. For checks where data doesn't exist yet (subsidiary_count, vie_spe, related_party, social_sentiment, journalism_activity, regulatory_databases), route to None-returning paths so they produce SKIPPED status cleanly.

2. **Data mappers — SPLIT INTO check_mappers_ext.py:** check_mappers.py is already 510 lines (10 over the 500-line hard limit). Before adding ANY new registration lines, the following existing helpers MUST be moved out of check_mappers.py into check_mappers_ext.py to bring it below 490 lines:
   - `_text_signal_value` (defined at ~line 153)
   - `_text_signal_count` (defined at ~line 172)
   - `_BIZ_TEXT_SIG_FIELDS` dict (defined at ~line 191)

   These three are only used by existing BIZ mappers that also call them — move the callers' bodies into check_mappers_ext.py as well, OR add a re-import in check_mappers.py (`from .check_mappers_ext import _text_signal_value, _text_signal_count, _BIZ_TEXT_SIG_FIELDS`) so existing call sites don't break. After moving these helpers, check_mappers.py should be around 440-460 lines, leaving headroom for the ~5-10 new import/registration lines.

   Then add new mapper functions for the 4 new prefixes in check_mappers_ext.py:
   - `BIZ.STRUCT.*` → map to company structure data (if available in extracted)
   - `LIT.DEFENSE.*` → map to litigation defense data (forum_selection, contingent_liabilities are in extracted.litigation or extracted.governance)
   - `LIT.PATTERN.*` → map to litigation pattern data (sol_mapper output, industry_theories, stock_drops)
   - `LIT.SECTOR.*` → map to sector-specific data (industry playbooks)

   In check_mappers.py, add imports from check_mappers_ext and register the new prefix handlers in the mapper dispatch dict. The new mapper functions themselves live in check_mappers_ext.py. Follow existing mapper patterns. For checks that can't get data yet, the mapper should return {} or a dict with None values, causing the evaluator to produce SKIPPED.

   CRITICAL: check_mappers.py must stay under 500 lines after all changes. Run `wc -l src/do_uw/stages/analyze/check_mappers.py` to verify before committing.

3. **Tests (test_zero_coverage_checks.py):** Create test file with:
   - `test_new_checks_exist_in_checks_json`: Verify all new check IDs exist in checks.json
   - `test_new_checks_have_valid_metadata`: Each new check has required fields (id, name, section, threshold, execution_mode, v6_subsection_ids)
   - `test_new_checks_have_field_routing`: Each new check ID is handled by check_field_routing.py (doesn't raise KeyError)
   - `test_zero_coverage_subsections_now_covered`: Verify subsections 1.4, 4.9, 5.7, 5.8, 5.9 each have at least 2 checks with those v6_subsection_ids
  </action>
  <verify>
Run: `wc -l src/do_uw/stages/analyze/check_mappers.py` — result MUST be less than 500.
Run: `cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/brain/test_zero_coverage_checks.py -v`
All tests pass.
Run: `cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/stages/analyze/ -v --tb=short -k "not slow"`
No regressions.
  </verify>
  <done>All new checks have field routing and data mapper entries. Checks that can get data from existing extractors will produce real results. Checks without data sources produce clean SKIPPED. All tests pass.</done>
</task>

</tasks>

<verification>
1. `python3` script counting checks per zero-coverage subsection → each has 2+ checks
2. `uv run pytest tests/brain/test_zero_coverage_checks.py -v` → all pass
3. `uv run pytest tests/stages/analyze/ -v --tb=short` → no regressions
4. `uv run pytest tests/brain/ -v --tb=short` → no regressions
</verification>

<success_criteria>
All 5 previously zero-coverage subsections now have checks with field routing and data mappers. Checks using existing data produce real results; checks needing future data acquisition produce clean SKIPPED. Total new checks: ~15-17.
</success_criteria>

<output>
After completion, create `.planning/phases/33-brain-driven-worksheet-architecture/33-03-SUMMARY.md`
</output>
