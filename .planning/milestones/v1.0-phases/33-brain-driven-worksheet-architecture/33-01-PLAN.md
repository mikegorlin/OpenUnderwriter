---
phase: 33-brain-driven-worksheet-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/brain/checks.json
  - src/do_uw/brain/enrichment_data.py
  - src/do_uw/brain/brain_enrich.py
  - tests/brain/test_enrichment_coverage.py
autonomous: true
requirements:
  - SC1-question-specs
  - SC3-acquisition-audit

must_haves:
  truths:
    - "Every one of the 384 checks in checks.json has a v6_subsection_ids field containing at least one subsection ID"
    - "The v6_subsection_ids assignment derives from enrichment_data.py mappings (CHECK_TO_RISK_QUESTIONS explicit, then SUBDOMAIN_TO_RISK_QUESTIONS fallback)"
    - "All 45 v6 subsections have at least one check mapped to them (excluding the 5 zero-coverage subsections addressed in Plan 03)"
    - "A test validates that all 384 checks have non-empty v6_subsection_ids after enrichment"
  artifacts:
    - path: "src/do_uw/brain/checks.json"
      provides: "All 384 checks with v6_subsection_ids field"
      contains: "v6_subsection_ids"
    - path: "tests/brain/test_enrichment_coverage.py"
      provides: "Coverage validation test"
      contains: "test_all_checks_have_v6_subsection"
  key_links:
    - from: "src/do_uw/brain/enrichment_data.py"
      to: "src/do_uw/brain/checks.json"
      via: "brain_enrich.py populates v6_subsection_ids from enrichment mappings"
      pattern: "v6_subsection_ids"
---

<objective>
Add v6 subsection IDs to every check in checks.json, creating the foundational question-to-check linkage that all subsequent plans depend on.

Purpose: Without v6_subsection_ids on checks, there's no way to query "what checks answer subsection 1.4?" -- the entire top-down architecture depends on this mapping. The enrichment_data.py already has 53 subdomain + 84 explicit mappings that cover most checks. This plan makes those mappings concrete in checks.json itself.

Output: checks.json with v6_subsection_ids on all 384 checks, enrichment pipeline updated to populate the field, coverage test.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/do_uw/brain/enrichment_data.py
@src/do_uw/brain/brain_enrich.py
@src/do_uw/brain/checks.json
@.planning/phases/32-knowledge-driven-acquisition-analysis-pipeline/QUESTION-AUDIT.md (summary section at end)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v6_subsection_ids to all 384 checks in checks.json</name>
  <files>
    src/do_uw/brain/checks.json
    src/do_uw/brain/enrichment_data.py
  </files>
  <action>
Write a script (or do it inline) that reads each check in checks.json, resolves its v6 subsection IDs using the same logic as brain_enrich.py's `_resolve_risk_questions()`:
1. If check_id is in CHECK_TO_RISK_QUESTIONS, use those subsection IDs
2. Else if prefix.subdomain is in SUBDOMAIN_TO_RISK_QUESTIONS, use those
3. Else assign based on PREFIX_TO_REPORT_SECTION with a reasonable default subsection

Add the field `v6_subsection_ids` (list of strings like `["1.1", "5.2"]`) to each check object in the `checks` array.

Review enrichment_data.py coverage first. For any checks that would get empty mappings (no explicit or subdomain match), add appropriate entries to SUBDOMAIN_TO_RISK_QUESTIONS or CHECK_TO_RISK_QUESTIONS. The goal is ZERO checks without at least one v6 subsection ID.

Cross-reference QUESTION-AUDIT.md per-subsection breakdown to verify the mapping makes sense: checks that the audit assigned to a subsection should have that subsection in their v6_subsection_ids.

Do NOT renumber the existing `section` field in checks.json -- it stays as-is. v6_subsection_ids is a NEW field.
  </action>
  <verify>
Run: `python3 -c "import json; d=json.load(open('src/do_uw/brain/checks.json')); checks=d['checks']; empty=[c['id'] for c in checks if not c.get('v6_subsection_ids')]; print(f'Total: {len(checks)}, Missing v6: {len(empty)}'); [print(f'  {e}') for e in empty[:10]]"`
All 384 checks should have v6_subsection_ids. Zero missing.
  </verify>
  <done>Every check in checks.json has a non-empty v6_subsection_ids list. The mapping is consistent with QUESTION-AUDIT.md findings and enrichment_data.py.</done>
</task>

<task type="auto">
  <name>Task 2: Update brain_enrich.py to populate v6_subsection_ids in DuckDB and add coverage test</name>
  <files>
    src/do_uw/brain/brain_enrich.py
    tests/brain/test_enrichment_coverage.py
  </files>
  <action>
1. Update `enrich_brain_checks()` in brain_enrich.py: when creating enriched version rows, ensure `risk_questions` column is populated from the same logic (it already is -- verify this path works with the v6 IDs in checks.json).

2. Also ensure the brain_enrich.py code handles the new `v6_subsection_ids` field when loading checks from checks.json, so the DuckDB `risk_questions` column matches what's in checks.json.

3. Create `tests/brain/test_enrichment_coverage.py` with:
   - `test_all_checks_have_v6_subsection_ids`: Load checks.json, assert every check has non-empty v6_subsection_ids
   - `test_v6_subsection_ids_are_valid`: Assert all IDs match pattern X.Y where X is 1-5 and Y is 1-11
   - `test_enrichment_data_covers_all_subdomains`: Get all unique prefix.subdomain patterns from checks, verify each is in SUBDOMAIN_TO_RISK_QUESTIONS or each check under it is in CHECK_TO_RISK_QUESTIONS
   - `test_subsection_coverage_by_section`: For each of the 5 v6 sections, verify at least some subsections have checks mapped

4. Run existing brain tests to ensure nothing breaks.
  </action>
  <verify>
Run: `cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/brain/test_enrichment_coverage.py -v`
All tests pass.
Run: `cd /Users/gorlin/projects/UW/do-uw && uv run pytest tests/brain/ -v --tb=short`
No regressions in existing brain tests.
  </verify>
  <done>brain_enrich.py populates v6 subsection IDs into DuckDB. Coverage test verifies 100% check mapping. No existing test regressions.</done>
</task>

</tasks>

<verification>
1. `python3 -c "import json; d=json.load(open('src/do_uw/brain/checks.json')); print(sum(1 for c in d['checks'] if c.get('v6_subsection_ids')))"` → 384
2. `uv run pytest tests/brain/test_enrichment_coverage.py -v` → all pass
3. `uv run pytest tests/brain/ -v --tb=short` → no regressions
</verification>

<success_criteria>
All 384 checks in checks.json have v6_subsection_ids populated. The enrichment pipeline maintains this mapping in DuckDB. Coverage test validates 100% mapping with valid subsection IDs.
</success_criteria>

<output>
After completion, create `.planning/phases/33-brain-driven-worksheet-architecture/33-01-SUMMARY.md`
</output>
