---
phase: 21-multi-ticker-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/ground_truth/__init__.py
  - tests/ground_truth/nvda.py
  - tests/ground_truth/mrna.py
  - tests/ground_truth/xom.py
  - tests/ground_truth/pg.py
  - tests/ground_truth/dis.py
  - tests/ground_truth/smci.py
  - tests/ground_truth/coin.py
  - tests/test_ground_truth_validation.py
  - tests/test_ground_truth_coverage.py
autonomous: true

must_haves:
  truths:
    - "Ground truth fixtures exist for 10 companies total (TSLA, AAPL, JPM + 7 new)"
    - "Each new company has the full 13-category ground truth structure"
    - "Known-outcome companies (SMCI, COIN) have ground truth for D&O-relevant signals"
    - "Existing ground truth tests are parametrized across all 10 companies"
  artifacts:
    - path: "tests/ground_truth/nvda.py"
      provides: "NVDA ground truth -- tech/SaaS, 13 categories"
    - path: "tests/ground_truth/mrna.py"
      provides: "MRNA ground truth -- biotech/pharma, 13 categories"
    - path: "tests/ground_truth/xom.py"
      provides: "XOM ground truth -- energy, 13 categories"
    - path: "tests/ground_truth/pg.py"
      provides: "PG ground truth -- CPG/consumer, 13 categories"
    - path: "tests/ground_truth/dis.py"
      provides: "DIS ground truth -- media/entertainment, 13 categories"
    - path: "tests/ground_truth/smci.py"
      provides: "SMCI ground truth -- known-outcome (accounting issues, MW), 13 categories"
    - path: "tests/ground_truth/coin.py"
      provides: "COIN ground truth -- known-outcome (SEC enforcement), 13 categories"
    - path: "tests/ground_truth/__init__.py"
      provides: "ALL_GROUND_TRUTH registry with 10 companies"
  key_links:
    - from: "tests/ground_truth/__init__.py"
      to: "tests/test_ground_truth_validation.py"
      via: "TICKERS list and ALL_GROUND_TRUTH dict used by parametrized tests"
    - from: "tests/ground_truth/smci.py"
      to: "tests/test_ground_truth_validation.py"
      via: "Known-outcome validation: has_material_weakness=True, has_active_sca=True"
---

<objective>
Expand ground truth fixtures from 3 companies (TSLA, AAPL, JPM) to 10 companies covering 5 industry verticals plus 2 known-outcome companies. Each new file follows the existing 13-category structure with hand-verified values from SEC EDGAR filings.

Purpose: 10-company ground truth enables automated accuracy measurement across diverse industries. Known-outcome companies (SMCI, COIN) validate that the system detects real D&O risk signals.

Output: 7 new ground truth files, updated registry, parametrized test coverage across all 10 companies.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-multi-ticker-validation/21-CONTEXT.md
@.planning/phases/21-multi-ticker-validation/21-RESEARCH.md
@tests/ground_truth/tsla.py
@tests/ground_truth/aapl.py
@tests/ground_truth/__init__.py
@tests/ground_truth/helpers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 7 new ground truth files</name>
  <files>
    tests/ground_truth/nvda.py
    tests/ground_truth/mrna.py
    tests/ground_truth/xom.py
    tests/ground_truth/pg.py
    tests/ground_truth/dis.py
    tests/ground_truth/smci.py
    tests/ground_truth/coin.py
  </files>
  <action>
Create 7 new ground truth files following the exact pattern established in `tsla.py`. Each file exports a `GROUND_TRUTH: dict[str, dict[str, Any]]` dict with 13 categories.

**To populate the data:** For each company, use SEC EDGAR APIs and web search to gather and verify values. The ground truth values must be hand-verified from actual SEC filings, NOT generated or estimated.

For each company, look up:
1. **Identity**: CIK from SEC EDGAR Submissions API (`data.sec.gov/submissions/CIK{padded}.json`), SIC code, exchange. Map SIC to sector using the project's sic_to_sector() mapping.
2. **Financials**: Revenue, net income, total assets, total debt, cash from the LATEST available 10-K XBRL data (Company Facts API). Use 10% tolerance pattern for comparisons.
3. **Market**: Market cap tier (MEGA > $200B, LARGE > $10B, MID > $2B, SMALL > $300M, MICRO).
4. **Governance**: Board size, CEO name, CFO name from latest DEF 14A.
5. **Litigation**: has_active_sca from Stanford SCAC, approximate SCA count.
6. **Distress**: Altman Z-Score zone (SAFE > 2.99, GREY 1.81-2.99, DISTRESS < 1.81).
7. **item1_business**: has_business_description (always True), employee count (from yfinance or 10-K), dual class, customer concentration.
8. **item7_mda**: has_critical_accounting_estimates, has_guidance_language.
9. **item8_footnotes**: has_going_concern, has_restatements.
10. **item9a_controls**: has_material_weakness, auditor_name, auditor_opinion.
11. **eight_k_events**: has_event_timeline, event_count_min.
12. **ownership**: insider_ownership_pct_min, top_institutional_holder_contains.
13. **risk_factors**: total_risk_factors_min, has_ai_risk_factor, has_cyber_risk_factor.

**Company-specific notes:**

- **NVDA** (NVIDIA): SIC 3674 (Semiconductors), CIK 1045810, Fiscal year ends January. Revenue ~$130B FY2025. MEGA cap. Jensen Huang CEO. Colette Kress CFO. SAFE distress zone. No active SCA. PricewaterhouseCoopers auditor.

- **MRNA** (Moderna): SIC 2836 (Biological Products), CIK 1682852. Revenue declined from COVID peak. LARGE cap. Stephane Bancel CEO. Jamey Mock CFO. Check for going concern (post-COVID revenue decline).

- **XOM** (Exxon Mobil): SIC 1311 (Crude Petroleum & Natural Gas), CIK 34088. MEGA cap. Darren Woods CEO. Kathryn Mikells CFO. PricewaterhouseCoopers auditor.

- **PG** (Procter & Gamble): SIC 2840 (Soap & Detergent), CIK 80424. MEGA cap. Jon Moeller CEO. Andre Schulten CFO. Deloitte auditor. Fiscal year ends June.

- **DIS** (Walt Disney): SIC 4841 (Cable & Other Pay TV), CIK 1744489. LARGE cap. Bob Iger CEO. Hugh Johnston CFO. PricewaterhouseCoopers auditor. Fiscal year ends September.

- **SMCI** (Super Micro Computer): SIC 3571 (Electronic Computers), CIK 1375365. Known-outcome: accounting issues, auditor resignation (Ernst & Young resigned Oct 2024), delayed 10-K filing, DOJ investigation. has_material_weakness should be True. has_active_sca should be True. New auditor BDO. Check whether 10-K is available (was delayed).

- **COIN** (Coinbase): SIC 6199 (Finance Services), CIK 1679788. Known-outcome: SEC enforcement action (securities lawsuit). has_active_sca should be True. Deloitte auditor.

**CRITICAL:** If exact financial values cannot be confirmed from SEC EDGAR XBRL, use approximate values with a note about the source and use 10% tolerance (`_tolerance` field). For known-outcome companies, the D&O signals (material weakness, active SCA, restatements) are the critical fields -- financial precision matters less.

Each file should be 80-130 lines following the tsla.py format exactly. Include source comments for each section.
  </action>
  <verify>
    `cd /Users/gorlin/projects/research && uv run python -c "from tests.ground_truth.nvda import GROUND_TRUTH; assert 'identity' in GROUND_TRUTH and 'risk_factors' in GROUND_TRUTH"`
    `cd /Users/gorlin/projects/research && uv run python -c "from tests.ground_truth.smci import GROUND_TRUTH; assert GROUND_TRUTH['litigation']['has_active_sca'] is True"`
    `cd /Users/gorlin/projects/research && uv run pyright tests/ground_truth/ --pythonversion 3.12`
  </verify>
  <done>
    7 new ground truth files created with 13 categories each. SMCI and COIN have correct known-outcome D&O signals.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update registry and parametrize tests</name>
  <files>
    tests/ground_truth/__init__.py
    tests/test_ground_truth_validation.py
    tests/test_ground_truth_coverage.py
  </files>
  <action>
**__init__.py** -- Expand registry:
- Import GROUND_TRUTH from all 7 new modules
- Add to ALL_GROUND_TRUTH dict (10 entries total: TSLA, AAPL, JPM, NVDA, MRNA, XOM, PG, DIS, SMCI, COIN)
- Update __all__ list

**test_ground_truth_validation.py** -- Update TICKERS list:
- Change `TICKERS = ["TSLA", "AAPL", "JPM"]` to `TICKERS = list(ALL_GROUND_TRUTH.keys())`
- This automatically parametrizes all existing test functions across 10 companies
- All tests already handle missing state.json via `pytest.skip()`
- No other changes needed -- the parametrize decorator uses TICKERS

**test_ground_truth_coverage.py** -- Same update:
- Change `TICKERS = ["TSLA", "AAPL"]` (or whatever the current list is) to `TICKERS = [t for t in ALL_GROUND_TRUTH if t != "JPM"]`
- JPM is excluded because it's a financial institution with only 6 categories
- All existing tests already handle missing state.json via skip

Both test files should stay under 500 lines.
  </action>
  <verify>
    `cd /Users/gorlin/projects/research && uv run python -c "from tests.ground_truth import ALL_GROUND_TRUTH; assert len(ALL_GROUND_TRUTH) == 10, f'Expected 10, got {len(ALL_GROUND_TRUTH)}'"`
    `cd /Users/gorlin/projects/research && uv run pytest tests/test_ground_truth_validation.py --collect-only 2>&1 | tail -5`
    `cd /Users/gorlin/projects/research && uv run pytest tests/test_ground_truth_coverage.py --collect-only 2>&1 | tail -5`
    `cd /Users/gorlin/projects/research && uv run ruff check tests/ground_truth/`
  </verify>
  <done>
    10 companies in ground truth registry. Validation and coverage tests parametrized across all companies. Tests skip cleanly for companies without state.json.
  </done>
</task>

</tasks>

<verification>
- `uv run python -c "from tests.ground_truth import ALL_GROUND_TRUTH; print(f'{len(ALL_GROUND_TRUTH)} companies'); assert len(ALL_GROUND_TRUTH) == 10"` -- 10 companies
- `uv run pytest tests/test_ground_truth_validation.py --collect-only -q | wc -l` -- more test nodes than before
- `uv run pyright tests/ground_truth/ --pythonversion 3.12` -- no type errors
- `uv run ruff check tests/ground_truth/` -- no lint errors
- Each ground truth file has 13 categories (identity, financials, market, governance, litigation, distress, item1_business, item7_mda, item8_footnotes, item9a_controls, eight_k_events, ownership, risk_factors)
- Known-outcome companies SMCI and COIN have has_active_sca=True
</verification>

<success_criteria>
- 10 companies in ground truth (3 existing + 7 new)
- Coverage across 5+ industry verticals
- 2 known-outcome companies with verified D&O signals
- All tests parametrized and collecting correctly
- No lint/type errors
</success_criteria>

<output>
After completion, create `.planning/phases/21-multi-ticker-validation/21-03-SUMMARY.md`
</output>
