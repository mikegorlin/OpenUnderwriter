---
phase: 13-ai-transformation-risk-factor
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - src/do_uw/stages/render/sections/sect8_ai_risk.py
  - src/do_uw/stages/render/word_renderer.py
  - src/do_uw/stages/render/md_renderer.py
  - src/do_uw/templates/markdown/worksheet.md.j2
  - src/do_uw/stages/score/__init__.py
  - src/do_uw/dashboard/state_api.py
  - src/do_uw/templates/dashboard/partials/_ai_risk_detail.html
  - tests/test_ai_risk_render.py
  - tests/test_ai_risk_pipeline.py
autonomous: true

must_haves:
  truths:
    - "Section 8: AI Transformation Risk renders in Word document with sub-dimensions table, peer comparison, narrative"
    - "Word renderer dispatches to sect8_ai_risk via importlib pattern"
    - "Markdown template includes AI risk section with all sub-dimensions"
    - "ScoreStage calls score_ai_risk() to populate final scoring on state.extracted.ai_risk"
    - "Dashboard serves AI risk section with drill-down detail view"
    - "Pipeline runs end-to-end: EXTRACT populates raw data, SCORE populates scores, RENDER outputs section"
    - "Graceful rendering when AI risk data is None or empty"
  artifacts:
    - path: "src/do_uw/stages/render/sections/sect8_ai_risk.py"
      provides: "render_section_8() Word document renderer for AI risk"
      exports: ["render_section_8"]
    - path: "src/do_uw/stages/render/word_renderer.py"
      provides: "Section 8 registration in _get_section_renderers()"
      contains: "sect8_ai_risk"
    - path: "src/do_uw/templates/markdown/worksheet.md.j2"
      provides: "AI risk section in Markdown template"
      contains: "AI Transformation Risk"
    - path: "src/do_uw/stages/score/__init__.py"
      provides: "ScoreStage calls score_ai_risk()"
      contains: "score_ai_risk"
    - path: "src/do_uw/dashboard/state_api.py"
      provides: "ai_risk section in dashboard"
      contains: "ai_risk"
    - path: "src/do_uw/templates/dashboard/partials/_ai_risk_detail.html"
      provides: "Dashboard AI risk drill-down partial"
  key_links:
    - from: "src/do_uw/stages/render/word_renderer.py"
      to: "src/do_uw/stages/render/sections/sect8_ai_risk.py"
      via: "importlib dispatch"
      pattern: "sect8_ai_risk"
    - from: "src/do_uw/stages/score/__init__.py"
      to: "src/do_uw/stages/score/ai_risk_scoring.py"
      via: "score_ai_risk() call"
      pattern: "score_ai_risk"
    - from: "src/do_uw/dashboard/state_api.py"
      to: "src/do_uw/models/ai_risk.py"
      via: "AIRiskAssessment data extraction"
      pattern: "ai_risk"
---

<objective>
Complete the AI Transformation Risk Factor pipeline: Section 8 rendering (Word/Markdown/Dashboard), ScoreStage integration, and end-to-end pipeline wiring.

Purpose: Connect the AI risk models (Plan 01) and extraction (Plan 02) to the output layer so underwriters see AI risk assessment in their worksheet, markdown output, and interactive dashboard.

Output: Working Section 8 in all output formats, ScoreStage integration, dashboard section, and full pipeline tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ai-transformation-risk-factor/13-RESEARCH.md
@.planning/phases/13-ai-transformation-risk-factor/13-01-SUMMARY.md
@.planning/phases/13-ai-transformation-risk-factor/13-02-SUMMARY.md

Key reference files for patterns:
@src/do_uw/stages/render/sections/sect7_scoring.py -- Section renderer pattern
@src/do_uw/stages/render/word_renderer.py -- Section dispatch registration
@src/do_uw/stages/render/md_renderer.py -- build_template_context pattern
@src/do_uw/templates/markdown/worksheet.md.j2 -- Markdown template structure
@src/do_uw/stages/score/__init__.py -- ScoreStage pipeline
@src/do_uw/dashboard/state_api.py -- Dashboard section extraction pattern
@src/do_uw/templates/dashboard/partials/_summary_card.html -- Dashboard partial pattern
@src/do_uw/models/ai_risk.py -- AIRiskAssessment model (from Plan 01)
@src/do_uw/stages/score/ai_risk_scoring.py -- score_ai_risk() (from Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section 8 renderer, Word/Markdown template updates, ScoreStage wiring</name>
  <files>
    src/do_uw/stages/render/sections/sect8_ai_risk.py
    src/do_uw/stages/render/word_renderer.py
    src/do_uw/stages/render/md_renderer.py
    src/do_uw/templates/markdown/worksheet.md.j2
    src/do_uw/stages/score/__init__.py
    tests/test_ai_risk_render.py
  </files>
  <action>
**A. Create `src/do_uw/stages/render/sections/sect8_ai_risk.py`:**

Following the pattern of sect5_governance.py and sect7_scoring.py:

```python
def render_section_8(doc: Any, state: AnalysisState, ds: Any) -> None:
```

1. Check `state.extracted and state.extracted.ai_risk` -- if None, render "AI risk assessment unavailable" paragraph and return early.

2. **Overall Score Header:**
   - Heading "Section 8: AI Transformation Risk"
   - Overall AI Risk Score as a prominent paragraph: "{score:.0f}/100 ({industry_model_id})"
   - Disclosure trend: "AI Disclosure Trend: {disclosure_trend}"

3. **Sub-Dimensions Table:**
   - Heading "Risk Sub-Dimensions"
   - Table with columns: Dimension, Score (/10), Weight, Threat Level, Key Evidence
   - One row per sub_dimension
   - Use conditional formatting: score >= 7 = red, 4-6 = amber, <= 3 = blue (Liberty Mutual palette, NO green)
   - If sub_dimensions is empty, show "Sub-dimension scoring not available"

4. **Peer Comparison:**
   - Heading "Peer AI Comparison"
   - If peer_comparison_available:
     - Company AI mentions vs peer average
     - Adoption stance (LEADING/INLINE/LAGGING)
     - Peer mention breakdown table if peer_mention_counts available
   - If not: "Peer comparison unavailable. Run with --analyze-peers for peer-relative assessment."

5. **Industry Assessment:**
   - Heading "Industry-Specific Assessment"
   - Narrative paragraph (from narrative field)
   - Source attribution (narrative_source, narrative_confidence)

6. **Forward Indicators:**
   - Heading "Forward-Looking Indicators"
   - Bullet list of forward_indicators
   - If empty: "No forward indicators identified"

7. **Data Sources:**
   - Small-print list of data_sources contributing to this assessment

Use the same helper imports as other section renderers: `add_heading`, `add_paragraph`, `add_table` from `docx_helpers`. Use `_sv_str` pattern for SourcedValue extraction if needed. All doc/style types as `Any` for pyright strict.

**B. Register in `src/do_uw/stages/render/word_renderer.py`:**

In `_get_section_renderers()`, add Section 8 BEFORE the Meeting Prep Appendix entry (between Section 7 and Meeting Prep):

```python
# Section 8: AI Transformation Risk
renderer = _try_import_renderer(
    "do_uw.stages.render.sections.sect8_ai_risk", "render_section_8"
)
renderers.append(("Section 8: AI Transformation Risk", renderer))
```

**C. Update `src/do_uw/stages/render/md_renderer.py`:**

In `build_template_context()`, add an `ai_risk` key to the context dict:
- Extract from state.extracted.ai_risk (if available):
  - overall_score, sub_dimensions (as list of dicts), disclosure_data, patent_activity, competitive_position, narrative, forward_indicators, peer_comparison_available, industry_model_id
- If ai_risk is None, set `ai_risk = None` in context (template will check)

**D. Update `src/do_uw/templates/markdown/worksheet.md.j2`:**

Add Section 8 after Section 7, before the Meeting Prep appendix:

```markdown
## Section 8: AI Transformation Risk

{% if ai_risk %}
**Overall AI Risk Score: {{ ai_risk.overall_score | round(0) }}/100** ({{ ai_risk.industry_model_id }})

**AI Disclosure Trend:** {{ ai_risk.disclosure_trend }}

### Risk Sub-Dimensions

| Dimension | Score (/10) | Weight | Threat Level |
|-----------|------------|--------|--------------|
{% for dim in ai_risk.sub_dimensions %}
| {{ dim.dimension | replace("_", " ") | title }} | {{ dim.score | round(1) }} | {{ (dim.weight * 100) | round(0) }}% | {{ dim.threat_level }} |
{% endfor %}

### Peer AI Comparison

{% if ai_risk.peer_comparison_available %}
- **Company AI Mentions:** {{ ai_risk.competitive_position.company_ai_mentions }}
- **Peer Average:** {{ ai_risk.competitive_position.peer_avg_mentions | round(1) }}
- **Adoption Stance:** {{ ai_risk.competitive_position.adoption_stance }}
{% else %}
Peer comparison unavailable.
{% endif %}

### Industry-Specific Assessment

{{ ai_risk.narrative }}

### Forward-Looking Indicators

{% for indicator in ai_risk.forward_indicators %}
- {{ indicator }}
{% endfor %}
{% if not ai_risk.forward_indicators %}
No forward indicators identified.
{% endif %}
{% else %}
AI risk assessment unavailable.
{% endif %}
```

**E. Wire ScoreStage in `src/do_uw/stages/score/__init__.py`:**

In `ScoreStage.run()`, after the main scoring pipeline completes (after `state.scoring = result`) but before `state.mark_stage_completed(self.name)`, add:

```python
# Phase 13: AI risk scoring (runs on extracted AI data, independent of 10-factor model)
try:
    from do_uw.stages.score.ai_risk_scoring import score_ai_risk
    if state.extracted and state.extracted.ai_risk:
        state.extracted.ai_risk = score_ai_risk(state)
except Exception:
    logger.warning("AI risk scoring failed; continuing without AI risk scores")
```

This updates the existing AIRiskAssessment (populated by EXTRACT with raw data) with scored sub-dimensions and overall score. Use lazy import + try/except for isolation.

**F. Create `tests/test_ai_risk_render.py`:**
- Test `render_section_8` with populated AIRiskAssessment -> renders without error
- Test `render_section_8` with None ai_risk -> renders "unavailable" message
- Test `render_section_8` with empty sub_dimensions list -> handles gracefully
- Test `build_template_context` includes ai_risk key when state has AI risk data
- Test `build_template_context` ai_risk is None when state has no AI risk data
- Test Markdown template renders correctly (load and render Jinja2 with mock context)
  </action>
  <verify>
Run: `cd /Users/gorlin/projects/research && python -m pytest tests/test_ai_risk_render.py -v`
Run: `cd /Users/gorlin/projects/research && python -m pyright src/do_uw/stages/render/sections/sect8_ai_risk.py src/do_uw/stages/score/__init__.py`
Run: `cd /Users/gorlin/projects/research && python -m ruff check src/do_uw/stages/render/sections/sect8_ai_risk.py src/do_uw/stages/render/word_renderer.py src/do_uw/stages/render/md_renderer.py`
  </verify>
  <done>
Section 8 renders in Word with sub-dimensions table, peer comparison, narrative, and forward indicators. Markdown template includes AI risk section. ScoreStage calls score_ai_risk(). All render tests pass, pyright clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard AI risk section and full pipeline integration tests</name>
  <files>
    src/do_uw/dashboard/state_api.py
    src/do_uw/templates/dashboard/partials/_ai_risk_detail.html
    tests/test_ai_risk_pipeline.py
    tests/test_ai_risk_render.py
  </files>
  <action>
**A. Update `src/do_uw/dashboard/state_api.py`:**

1. Add "ai_risk" to `SECTION_TITLES`:
   ```python
   "ai_risk": "AI Transformation Risk",
   ```

2. Add chart config to `_SECTION_CHARTS` (no Plotly charts initially, but reserve the key):
   ```python
   "ai_risk": [],
   ```

3. In the section detail extraction function (look for how other sections build their detail dicts), add an `ai_risk` case that extracts:
   - overall_score, industry_model_id, disclosure_trend
   - sub_dimensions as list of dicts (dimension, score, weight, threat_level, evidence)
   - disclosure_data summary (mention_count, sentiment, yoy_trend)
   - patent_activity summary (ai_patent_count, filing_trend)
   - competitive_position (adoption_stance, percentile_rank, peer_avg_mentions)
   - narrative, forward_indicators
   - peer_comparison_available

Follow the existing pattern for how other sections (e.g., "financials", "governance") extract their data from state.

**B. Create `src/do_uw/templates/dashboard/partials/_ai_risk_detail.html`:**

Following the pattern of `_finding_detail.html` and other partials:

```html
<div class="space-y-6">
  <!-- Overall Score -->
  <div class="stats shadow">
    <div class="stat">
      <div class="stat-title">AI Risk Score</div>
      <div class="stat-value">{{ section.overall_score | round(0) }}/100</div>
      <div class="stat-desc">{{ section.industry_model_id }} | Trend: {{ section.disclosure_trend }}</div>
    </div>
  </div>

  <!-- Sub-Dimensions Table -->
  <div class="overflow-x-auto">
    <table class="table table-zebra">
      <thead>
        <tr>
          <th>Dimension</th>
          <th>Score</th>
          <th>Weight</th>
          <th>Threat Level</th>
        </tr>
      </thead>
      <tbody>
        {% for dim in section.sub_dimensions %}
        <tr>
          <td>{{ dim.dimension | replace("_", " ") | title }}</td>
          <td>
            <span class="{% if dim.score >= 7 %}text-error{% elif dim.score >= 4 %}text-warning{% else %}text-info{% endif %}">
              {{ dim.score | round(1) }}/10
            </span>
          </td>
          <td>{{ (dim.weight * 100) | round(0) }}%</td>
          <td>
            <span class="badge {% if dim.threat_level == 'HIGH' %}badge-error{% elif dim.threat_level == 'MEDIUM' %}badge-warning{% else %}badge-info{% endif %}">
              {{ dim.threat_level }}
            </span>
          </td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>

  <!-- Peer Comparison -->
  {% if section.peer_comparison_available %}
  <div class="card bg-base-200">
    <div class="card-body">
      <h3 class="card-title text-sm">Peer AI Comparison</h3>
      <p>Adoption Stance: <strong>{{ section.competitive_position.adoption_stance }}</strong></p>
      <p>Company Mentions: {{ section.competitive_position.company_ai_mentions }} | Peer Avg: {{ section.competitive_position.peer_avg_mentions | round(1) }}</p>
    </div>
  </div>
  {% endif %}

  <!-- Narrative -->
  {% if section.narrative %}
  <div class="prose max-w-none">
    <h3>Industry Assessment</h3>
    <p>{{ section.narrative }}</p>
  </div>
  {% endif %}

  <!-- Forward Indicators -->
  {% if section.forward_indicators %}
  <div>
    <h3 class="font-bold text-sm mb-2">Forward Indicators</h3>
    <ul class="list-disc list-inside">
      {% for indicator in section.forward_indicators %}
      <li>{{ indicator }}</li>
      {% endfor %}
    </ul>
  </div>
  {% endif %}
</div>
```

Register this partial in the section detail route: when section_id == "ai_risk", use `_ai_risk_detail.html` partial template. Look at how other section partials are loaded in the app routes (likely in `app.py` or the section route handler).

**C. Create `tests/test_ai_risk_pipeline.py`:**

Integration tests for the full AI risk pipeline flow:

1. **Score stage AI risk integration test:**
   - Create a mock AnalysisState with state.extracted.ai_risk populated (raw extraction data from EXTRACT)
   - Set state.extracted.ai_risk with AIDisclosureData having some mention counts
   - Mock BackwardCompatLoader for ScoreStage dependency
   - Run ScoreStage.run(state)
   - Assert state.extracted.ai_risk.overall_score > 0
   - Assert state.extracted.ai_risk.sub_dimensions is non-empty
   - Assert state.extracted.ai_risk.narrative is non-empty

2. **Extract stage AI risk wiring test:**
   - Mock `run_ai_risk_extractors` at `do_uw.stages.extract.run_ai_risk_extractors` (module namespace)
   - Run ExtractStage.run(state) (with all other extractors also mocked per existing test pattern)
   - Assert mock was called
   - Assert state.extracted.ai_risk is the mock return value

3. **Word renderer Section 8 registration test:**
   - Call `_get_section_renderers()` and verify "Section 8: AI Transformation Risk" is in the returned list
   - Verify the renderer function is not None

4. **Dashboard state_api test:**
   - Verify "ai_risk" is in SECTION_TITLES
   - Test section detail extraction with mock state containing AI risk data
   - Test section detail extraction with no AI risk data -> graceful handling

5. **Existing test suite regression check:**
   - Run full test suite to verify no regressions from ExtractStage, ScoreStage, word_renderer changes

**D. Update existing test files if needed:**
   - If any existing pipeline/CLI tests mock ExtractStage or ScoreStage, they may need the new `run_ai_risk_extractors` mock added. Check `tests/test_pipeline.py` and `tests/test_cli.py` -- add the AI risk mock alongside existing sub-orchestrator mocks (market, governance, litigation).
   - Pattern: add `@patch("do_uw.stages.extract.run_ai_risk_extractors")` to test decorators where `run_market_extractors`, `run_governance_extractors`, `run_litigation_extractors` are already patched.
  </action>
  <verify>
Run: `cd /Users/gorlin/projects/research && python -m pytest tests/test_ai_risk_pipeline.py tests/test_ai_risk_render.py -v`
Run: `cd /Users/gorlin/projects/research && python -m pyright src/do_uw/dashboard/state_api.py`
Run: `cd /Users/gorlin/projects/research && python -m ruff check src/do_uw/dashboard/ src/do_uw/stages/render/ src/do_uw/stages/score/`
Run: `cd /Users/gorlin/projects/research && python -m pytest` -- FULL suite must pass
  </verify>
  <done>
Dashboard serves AI risk section with drill-down. Pipeline integration tests confirm EXTRACT -> SCORE -> RENDER chain works. All existing tests pass with no regressions from new wiring. Section 8 visible in all output formats.
  </done>
</task>

</tasks>

<verification>
All checks:
- `cd /Users/gorlin/projects/research && python -m pytest tests/test_ai_risk_render.py tests/test_ai_risk_pipeline.py -v` -- all pass
- `cd /Users/gorlin/projects/research && python -m pyright src/do_uw/stages/render/sections/sect8_ai_risk.py src/do_uw/stages/score/__init__.py src/do_uw/dashboard/state_api.py` -- 0 errors
- `cd /Users/gorlin/projects/research && python -m ruff check src/do_uw/` -- 0 errors
- `cd /Users/gorlin/projects/research && python -m pytest` -- FULL suite passes (critical: no regressions)
- No file exceeds 500 lines: `find src/do_uw -name '*.py' | xargs wc -l | awk '$1 > 500'` returns nothing
- Section 8 appears in Word renderer section list
- Markdown template includes AI risk section
- Dashboard SECTION_TITLES includes "ai_risk"
</verification>

<success_criteria>
1. Section 8 renders in Word doc with sub-dimensions, peer comparison, narrative
2. Markdown template includes AI risk section with all sub-fields
3. ScoreStage calls score_ai_risk() to populate scoring on extracted AI data
4. Dashboard serves AI risk section with drill-down detail view
5. Full pipeline EXTRACT -> SCORE -> RENDER chain works for AI risk
6. Graceful rendering when AI risk data is None
7. All new tests pass, FULL test suite has no regressions
8. No file exceeds 500 lines
</success_criteria>

<output>
After completion, create `.planning/phases/13-ai-transformation-risk-factor/13-03-SUMMARY.md`
</output>
