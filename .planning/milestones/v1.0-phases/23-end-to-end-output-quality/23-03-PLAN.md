---
phase: 23-end-to-end-output-quality
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/stages/render/sections/sect3_tables.py
  - src/do_uw/stages/render/sections/sect3_financial.py
  - src/do_uw/stages/render/sections/sect2_company.py
  - src/do_uw/stages/render/formatters.py
  - tests/test_render_sections_1_4.py
autonomous: true

must_haves:
  truths:
    - "Shares Outstanding displays as '4.1B' not '$4.1B' in financial tables"
    - "Piotroski trajectory shows real period labels and scores, not '?:1.0 -> ?:0.0'"
    - "EPS, per-share, and share-count line items use appropriate non-currency formatting"
    - "Raw enum values are humanized in all section renderers"
  artifacts:
    - path: "src/do_uw/stages/render/sections/sect3_tables.py"
      provides: "Smart formatting that detects non-currency line items"
      contains: "shares"
    - path: "src/do_uw/stages/render/sections/sect3_financial.py"
      provides: "Fixed Piotroski trajectory display"
    - path: "src/do_uw/stages/render/formatters.py"
      provides: "format_count helper for non-currency large numbers"
  key_links:
    - from: "sect3_tables.py _format_value()"
      to: "formatters.py"
      via: "format_compact for share counts"
      pattern: "format_compact"
---

<objective>
Fix rendering bugs: shares outstanding shown with $ prefix, Piotroski trajectory missing period labels, and raw enum values in output.

Purpose: Multiple cross-cutting render-stage defects make the worksheet look unprofessional and inaccurate. The $ prefix on shares outstanding (e.g., "$4.1B shares") is the most visible. These are genuine render-stage bugs (not data bugs) where the formatter is called with the wrong function for the data type.

Output: Fixed formatters that detect share counts / non-currency line items, fixed Piotroski trajectory display, humanized enum values.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-end-to-end-output-quality/23-RESEARCH.md
@src/do_uw/stages/render/sections/sect3_tables.py
@src/do_uw/stages/render/sections/sect3_financial.py
@src/do_uw/stages/render/formatters.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix share count and non-currency formatting in financial tables</name>
  <files>src/do_uw/stages/render/sections/sect3_tables.py, src/do_uw/stages/render/formatters.py</files>
  <action>
The root cause is in `sect3_tables.py` function `_format_value()` at line 437-444. The default path calls `format_currency(value, compact=True)` which prepends `$`. Line items like "Shares Outstanding", "Weighted Average Shares", "Diluted Shares" are NOT currency.

1. In `_format_value()`, add detection for non-currency line items BEFORE the default currency formatting:

```python
def _format_value(value: float, label: str) -> str:
    """Format a financial value based on the metric label."""
    label_lower = label.lower()
    if "margin" in label_lower or "%" in label_lower:
        return format_percentage(value)
    if "eps" in label_lower or "per share" in label_lower:
        return f"${value:.2f}"
    # Share counts and non-monetary counts -- no $ prefix
    if any(kw in label_lower for kw in (
        "shares", "share count", "weighted average",
        "diluted shares", "basic shares",
    )):
        return format_compact(value)
    return format_currency(value, compact=True)
```

2. Verify `format_compact()` in formatters.py produces "4.1B" (no $ prefix) -- it already does, just confirm by reading the function. It calls `_compact_number(value)` which returns "4.1B" without "$".

3. Also check other section renderers (sect2_company.py, sect3_financial.py) for similar issues where `format_currency` might be called on non-currency values. Common candidates:
   - Employee count should use `format_number()` or `format_compact()`, not `format_currency()`
   - Any "count" or "number of" fields

4. If any section renderer uses `format_currency` for employee count, fix it to use `format_number()`.

5. Write a small focused test for `_format_value` with share-count labels.
  </action>
  <verify>Run: `cd /Users/gorlin/projects/research && python -c "
from do_uw.stages.render.sections.sect3_tables import _format_value
# Shares Outstanding should not have $ prefix
result = _format_value(4100000000, 'Shares Outstanding')
assert not result.startswith('$'), f'Still has $ prefix: {result}'
print(f'Shares Outstanding: {result}')
# Revenue should still have $ prefix
result2 = _format_value(339000000000, 'Total Revenue')
assert result2.startswith('$'), f'Missing $ prefix: {result2}'
print(f'Total Revenue: {result2}')
"`</verify>
  <done>"Shares Outstanding" formatted as "4.1B" (no $), "Total Revenue" formatted as "$339.0B" (with $). Employee count uses format_number where applicable.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Piotroski trajectory display and enum humanization</name>
  <files>src/do_uw/stages/render/sections/sect3_financial.py, src/do_uw/stages/render/sections/sect2_company.py</files>
  <action>
1. **Piotroski trajectory**: Read `sect3_financial.py` and find where Piotroski F-Score trajectory is rendered. The research says it shows "?:1.0 -> ?:0.0" which means period labels are missing. The trajectory data is likely a dict or list of (period, score) pairs where the period label is None or "?".

   Fix: When period labels are missing/None/"?", generate synthetic labels from the data (e.g., "FY2023", "FY2024") or use "Period 1", "Period 2" etc. If the distress_trajectory field on the model has period information, use it. If not, derive from the financial statement periods.

   Also handle the case where trajectory score is a string "N/A" not just float (see MEMORY.md bug note: "Distress trajectory score can be string 'N/A' not just float").

2. **Raw enum values**: Search the section renderers for places where StrEnum .value is used directly in output text. Common patterns:
   - `str(enum_field)` or `enum_field.value` producing "SAFE", "DISTRESS", "GREY_ZONE" instead of "Safe", "Distress", "Grey Zone"

   Add a small helper function if one doesn't exist:
   ```python
   def humanize_enum(value: str) -> str:
       """Convert SCREAMING_SNAKE or UPPER enum values to Title Case."""
       return value.replace("_", " ").title()
   ```

   Apply this to:
   - Altman Z-Score zone (SAFE -> Safe, DISTRESS -> Distress, GREY_ZONE -> Grey Zone)
   - Any other raw enum values found in sect2_company.py or sect3_financial.py output

3. Run tests to verify no regressions.
  </action>
  <verify>Run: `cd /Users/gorlin/projects/research && uv run pytest tests/test_render_sections_1_4.py -v --tb=short`</verify>
  <done>Piotroski trajectory shows meaningful period labels and numeric scores. Enum values humanized (SAFE -> Safe, GREY_ZONE -> Grey Zone). No test regressions.</done>
</task>

</tasks>

<verification>
1. `_format_value` correctly formats share counts without $ prefix
2. `uv run pytest tests/test_render_sections_1_4.py -v` -- all pass
3. `uv run pytest tests/ -k "render" -v --tb=short` -- no regressions
</verification>

<success_criteria>
Shares Outstanding shows "4.1B" not "$4.1B". Piotroski trajectory shows "FY2023: 7 -> FY2024: 8" not "?:1.0 -> ?:0.0". Enum values humanized in all section output. No test regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/23-end-to-end-output-quality/23-03-SUMMARY.md`
</output>
