---
phase: 23-end-to-end-output-quality
plan: 06
type: execute
wave: 2
depends_on: ["23-01", "23-03"]
files_modified:
  - src/do_uw/stages/render/sections/sect1_executive.py
  - src/do_uw/stages/render/sections/sect1_findings.py
  - src/do_uw/stages/render/sections/sect2_company.py
  - src/do_uw/stages/render/sections/sect2_company_details.py
autonomous: true

must_haves:
  truths:
    - "Sector label is consistent across all sections (executive summary, company profile, scoring)"
    - "Executive summary key negatives include governance red flags from Section 5 data"
    - "Tier recommendation narrative is consistent with findings"
    - "Company profile shows correct sector from resolve stage, not conflicting sources"
  artifacts:
    - path: "src/do_uw/stages/render/sections/sect1_executive.py"
      provides: "Cross-section coherence in executive summary"
    - path: "src/do_uw/stages/render/sections/sect2_company.py"
      provides: "Consistent sector display"
  key_links:
    - from: "sect1_executive.py"
      to: "state.company.identity.sector"
      via: "sector label"
    - from: "sect2_company.py"
      to: "state.company.identity.sector"
      via: "sector label"
---

<objective>
Fix section coherence issues: ensure consistent sector labels across all sections, executive summary reflects downstream findings, and no contradictions between sections.

Purpose: Research found that NFLX Section 1 says "mega-cap industrials company" while Section 2 says "Entertainment industry" -- two data sources (SIC mapping vs yfinance) disagree and both are displayed without reconciliation. After Plan 01 fixes the SIC mapping, both sources should agree for most companies. This plan ensures the rendered output uses a single authoritative sector label and that section cross-references are consistent.

Output: Consistent sector display across all sections, executive summary that references downstream findings, no contradictions.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-end-to-end-output-quality/23-RESEARCH.md
@src/do_uw/stages/render/sections/sect1_executive.py
@src/do_uw/stages/render/sections/sect2_company.py
@src/do_uw/stages/render/sections/sect2_company_details.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure consistent sector display across sections</name>
  <files>src/do_uw/stages/render/sections/sect1_executive.py, src/do_uw/stages/render/sections/sect2_company.py, src/do_uw/stages/render/sections/sect2_company_details.py</files>
  <action>
1. **Read all section renderers** that display sector information. Key files:
   - `sect1_executive.py` -- executive summary mentions sector in context
   - `sect2_company.py` -- company profile shows sector/industry
   - `sect2_company_details.py` -- may show SIC description
   - `sect7_scoring.py` -- scoring may reference sector

2. **Establish authoritative sector source**: The sector should come from `state.company.identity.sector.value` (which is the SIC-derived code, now fixed by Plan 01). For display, use `sectors.json` `sector_codes.mappings` to get the human-readable name (e.g., "COMM" -> "Communication Services").

3. **Create a helper function** (or verify one exists) that converts sector code to display name:

```python
def sector_display_name(sector_code: str) -> str:
    """Convert sector code to human-readable display name.

    Uses sectors.json mappings. Falls back to titlecase of code.
    """
    _SECTOR_NAMES = {
        "TECH": "Technology",
        "HLTH": "Healthcare",
        "FINS": "Financials",
        "INDU": "Industrials",
        "ENGY": "Energy",
        "CONS": "Consumer Discretionary",
        "COMM": "Communication Services",
        "UTIL": "Utilities",
        "REIT": "Real Estate",
        "BIOT": "Biotech/Pharma",
        "DEFAULT": "Diversified",
    }
    return _SECTOR_NAMES.get(sector_code, sector_code.title())
```

4. **Use this consistently** across all section renderers. Where yfinance "industry" is shown alongside sector, frame it as the specific industry within the sector (e.g., "Sector: Communication Services | Industry: Entertainment").

5. **Fix the executive summary** narrative: Where it says "mega-cap industrials company", ensure it uses the corrected sector label.

6. Search for hardcoded sector strings or places that use `sic_description` directly as the sector label and fix them to use the canonical sector_display_name.
  </action>
  <verify>Run: `cd /Users/gorlin/projects/research && uv run pytest tests/test_render_sections_1_4.py -v --tb=short`</verify>
  <done>Sector label consistent across all sections. Executive summary uses corrected sector. yfinance industry shown as sub-classification, not alternative sector.</done>
</task>

<task type="auto">
  <name>Task 2: Improve executive summary cross-section references</name>
  <files>src/do_uw/stages/render/sections/sect1_executive.py, src/do_uw/stages/render/sections/sect1_findings.py</files>
  <action>
1. **Read sect1_executive.py and sect1_findings.py** to understand how key negatives and key positives are assembled.

2. **Verify that scoring-stage findings flow to executive summary**:
   - Key negatives should include: governance red flags (from ScoreStage), litigation findings (from ScoreStage), financial distress signals
   - Key positives should include: strong financial metrics, good governance scores, clean litigation history
   - The findings come from `state.scoring.key_findings` -- verify this is populated and rendered

3. **Verify tier recommendation consistency**:
   - The tier in Section 1 should match the tier in Section 7
   - If `state.scoring.tier` is WALK but financial indicators are strong, that's a scoring issue (covered separately), but the RENDER should at least display the tier consistently

4. **Add section references to findings** where they don't exist:
   - If a key negative says "Active litigation risk" it should note "(see Section 6)"
   - If a key positive says "Strong financial health" it should note "(see Section 3)"

   Look at the existing findings structure. If findings already have a `section_origin` or `section` field, use it to add cross-references. If not, add simple text references based on finding categories:
   - litigation/regulatory findings -> "(see Section 6: Litigation)"
   - financial findings -> "(see Section 3: Financial Health)"
   - governance findings -> "(see Section 5: Governance)"
   - market/trading findings -> "(see Section 4: Market & Trading)"

5. Run render tests to verify no regressions.
  </action>
  <verify>Run: `cd /Users/gorlin/projects/research && uv run pytest tests/test_render_sections_1_4.py -v --tb=short`</verify>
  <done>Executive summary cross-references downstream sections. Tier recommendation consistent. Key findings include section references for reader navigation.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_render_sections_1_4.py -v --tb=short` -- all pass
2. `uv run pytest tests/ -k "render" -v --tb=short` -- no regressions
3. No conflicting sector labels in the same document
</verification>

<success_criteria>
Sector label consistent across all sections using canonical display name. Executive summary references downstream section findings with "(see Section N)" links. Tier recommendation consistent between Section 1 and Section 7. No contradictions between sections.
</success_criteria>

<output>
After completion, create `.planning/phases/23-end-to-end-output-quality/23-06-SUMMARY.md`
</output>
