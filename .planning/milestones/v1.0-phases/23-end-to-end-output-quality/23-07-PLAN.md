---
phase: 23-end-to-end-output-quality
plan: 07
type: execute
wave: 2
depends_on: ["23-02"]
files_modified:
  - src/do_uw/stages/render/sections/sect5_governance.py
  - src/do_uw/stages/render/sections/sect5_governance_comp.py
  - src/do_uw/stages/render/sections/sect3_audit.py
  - src/do_uw/stages/render/sections/sect6_litigation.py
  - src/do_uw/stages/render/sections/sect6_defense.py
  - tests/test_render_sections_5_8.py
autonomous: true

must_haves:
  truths:
    - "Board member names are clean individual names, not concatenated with company names"
    - "Auditor identity is always shown when available in state (not N/A when data exists)"
    - "Compensation tables show complete NEO data when available"
    - "Empty sections show helpful 'data not available' rather than blank space"
    - "No debug text or raw internal identifiers in rendered output"
  artifacts:
    - path: "src/do_uw/stages/render/sections/sect5_governance.py"
      provides: "Clean board name display"
    - path: "src/do_uw/stages/render/sections/sect3_audit.py"
      provides: "Reliable auditor identity display"
  key_links:
    - from: "sect5_governance.py"
      to: "state.extracted.governance.board"
      via: "board member iteration"
    - from: "sect3_audit.py"
      to: "state.extracted.financials.audit_profile"
      via: "auditor fields"
---

<objective>
Fix completeness issues in governance, audit, and litigation section renderers -- board names display correctly, auditor identity always shown, no empty sections where data exists, no debug text in output.

Purpose: Research found board names concatenated with company names, auditor showing N/A when data exists in state, and empty sections for data that is available. These are render-stage display issues where the renderers don't correctly access the data that extraction put into state. After Plan 02 improves extraction, this plan ensures the renderers correctly display all available data.

Output: Clean board member display, reliable auditor identity, complete compensation tables, helpful fallback messages for missing data.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-end-to-end-output-quality/23-RESEARCH.md
@src/do_uw/stages/render/sections/sect5_governance.py
@src/do_uw/stages/render/sections/sect3_audit.py
@src/do_uw/stages/render/sections/sect6_litigation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix board name display and governance completeness</name>
  <files>src/do_uw/stages/render/sections/sect5_governance.py, src/do_uw/stages/render/sections/sect5_governance_comp.py</files>
  <action>
1. **Read sect5_governance.py** to understand how board member names are rendered. Find the code that iterates over board members and extracts names.

2. **Fix board name concatenation**: The research says board names are "concatenated with company names." This likely happens when the LLM extraction returns "John Smith, Apple Inc." as a single name field, or when the name field includes the company name. Add a name cleaning function:

```python
def _clean_board_name(name: str) -> str:
    """Remove company name artifacts from board member names.

    LLM extraction sometimes includes company name or title suffixes.
    Examples: 'John Smith, Apple Inc.' -> 'John Smith'
              'Jane Doe (Independent)' -> 'Jane Doe'
    """
    # Remove parenthetical suffixes
    name = re.sub(r'\s*\(.*?\)\s*$', '', name)
    # Remove trailing company indicators
    for suffix in [', Inc.', ', Corp.', ', LLC', ', Ltd.', ' Inc.', ' Corp.']:
        idx = name.find(suffix)
        if idx > 0:
            name = name[:idx]
    return name.strip()
```

3. **Ensure complete board table**: Check that all board members from `state.extracted.governance.board` are displayed with:
   - Name (cleaned)
   - Independence status (humanized: "Independent" / "Non-Independent")
   - Committee memberships
   - Tenure
   - Other board seats (overboarding flag if >=4)

4. **Read sect5_governance_comp.py** for compensation table. Verify:
   - All NEOs are listed with complete compensation breakdown
   - CEO pay ratio is displayed when available
   - Golden parachute values shown when available
   - No empty table rows or N/A-only rows

5. **Clean up any debug text**: Search for leftover debug strings like "DEBUG:", "TODO:", "FIXME:", or raw dict/list representations in the rendered output. Also search for raw internal identifiers (e.g., "SourcedValue(", "Confidence.HIGH").
  </action>
  <verify>Run: `cd /Users/gorlin/projects/research && uv run pytest tests/ -k "governance or sect5" -v --tb=short`</verify>
  <done>Board names clean and properly formatted. Compensation tables complete. No debug text or raw identifiers in rendered output.</done>
</task>

<task type="auto">
  <name>Task 2: Fix auditor display and litigation section completeness</name>
  <files>src/do_uw/stages/render/sections/sect3_audit.py, src/do_uw/stages/render/sections/sect6_litigation.py, src/do_uw/stages/render/sections/sect6_defense.py</files>
  <action>
1. **Read sect3_audit.py** and trace how auditor identity is rendered. The research says "SMCI auditor shows N/A" even though BDO is in the state. Likely causes:
   - Wrong field path (looking at old model field vs. new extraction field)
   - Auditor stored in `state.extracted.financials.audit_profile` or `state.extracted.governance.audit_data`
   - The renderer may be looking at a Phase 3 skeleton field that's empty while the Phase 19/20 LLM-extracted data is in a different location

   Fix: Check ALL possible locations for auditor data in the state model and use the first populated one. Common fields:
   - `state.extracted.financials.audit_profile.auditor_name`
   - `state.extracted.governance.board.auditor_identity` (if exists)
   - LLM extraction: look for auditor in the 10-K extraction results

2. **Ensure material weakness display**: SMCI had a known material weakness. Check that the renderer shows it when the field is populated. The research notes it may not be populated by extraction (tracked separately), but the renderer should correctly display it when it IS available.

3. **Read sect6_litigation.py** and verify:
   - SCA table shows all available cases with complete fields
   - SEC enforcement pipeline shows current stage
   - Empty sections say "No active securities class actions identified in filed documents" NOT blank space
   - If blind spot detection was not performed, add a note: "Note: This section relies on SEC filing disclosures only. Web-based discovery was not performed."

4. **Read sect6_defense.py** and verify:
   - Forum selection provisions displayed when available
   - Defense assessment shows meaningful content, not N/A-heavy tables
   - Section renders gracefully when litigation data is minimal

5. Run render tests to verify no regressions.
  </action>
  <verify>Run: `cd /Users/gorlin/projects/research && uv run pytest tests/ -k "render" -v --tb=short`</verify>
  <done>Auditor identity displayed from correct state path. Material weakness shown when populated. Litigation sections have meaningful fallback text. Defense section renders gracefully with minimal data.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/ -k "render" -v --tb=short` -- all pass
2. `uv run pytest tests/ -k "sect5 or sect6 or sect3" -v --tb=short` -- all pass
3. No debug text or raw identifiers in any section renderer output
</verification>

<success_criteria>
Board names clean. Auditor identity shown when available. Compensation complete. Empty sections have helpful fallback text. Litigation section notes when web search was not performed. No debug text or raw identifiers in output.
</success_criteria>

<output>
After completion, create `.planning/phases/23-end-to-end-output-quality/23-07-SUMMARY.md`
</output>
