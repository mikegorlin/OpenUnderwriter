---
phase: 23-end-to-end-output-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/stages/resolve/sec_identity.py
  - src/do_uw/brain/sectors.json
  - tests/test_resolve.py
autonomous: true

must_haves:
  truths:
    - "NFLX (SIC 7841) classified as COMM, not INDU"
    - "DIS (SIC 7990) classified as COMM, not INDU"
    - "COMM sector has base rates and thresholds in sectors.json"
    - "Existing sector mappings for TECH, ENGY, HLTH etc. unchanged"
  artifacts:
    - path: "src/do_uw/stages/resolve/sec_identity.py"
      provides: "Finer-grained SIC-to-sector mapping"
      contains: "COMM"
    - path: "src/do_uw/brain/sectors.json"
      provides: "COMM sector baselines"
      contains: "COMM"
  key_links:
    - from: "sec_identity.py sic_to_sector()"
      to: "sectors.json"
      via: "sector code string"
      pattern: "COMM"
---

<objective>
Fix sector classification so entertainment/media SIC codes (78xx, 79xx) map to COMM (Communication Services) instead of INDU (Industrials). Add COMM sector baselines to sectors.json.

Purpose: NFLX is classified as "Industrials" when it should be "Communication Services / Entertainment". This cascades through scoring, benchmarking, and display. The SIC mapping table in sec_identity.py uses too-coarse ranges: (74, 79) -> INDU conflates management consulting with motion pictures. The research traced this defect to the RESOLVE stage.

Output: Updated SIC mapping with finer granularity for SIC 70-89, COMM sector added to all sector-specific baseline tables in sectors.json, tests validating known SIC codes.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-end-to-end-output-quality/23-RESEARCH.md
@src/do_uw/stages/resolve/sec_identity.py
@src/do_uw/brain/sectors.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine SIC-to-sector mapping and add COMM sector</name>
  <files>src/do_uw/stages/resolve/sec_identity.py, src/do_uw/brain/sectors.json</files>
  <action>
In `sec_identity.py`, replace the coarse (74, 79) -> "INDU" range in `_SIC_SECTOR_MAP` with finer-grained mappings:

```python
(74, 76): "INDU",   # Management, engineering, R&D services
(78, 79): "COMM",   # Motion picture, amusement/recreation -> Communication Services
```

Also split (81, 86) more precisely if any entertainment SIC codes fall in that range. The key SIC codes that must map to COMM:
- 78xx: Motion Picture Distribution/Production/Services (Netflix SIC 7841)
- 79xx: Amusement & Recreation Services (Disney SIC 7990)

In `sectors.json`, add COMM entries to every sector-specific table that doesn't already have one:
- `short_interest`: COMM with values similar to TECH (media stocks have moderate short interest). E.g. `{"normal": 4, "elevated": 7, "high": 10, "extreme": 10, "sector_etf": "XLC"}`
- `volatility_90d`: COMM with typical 2.5, elevated 4, high 6 (similar to CONS)
- `leverage_debt_ebitda`: COMM with normal 3.0, elevated 4.5, critical 6.0, distress 8.0 (media companies carry moderate debt)
- `guidance_miss_sector_adjustments`: COMM with beat_rate 78, multiplier 1.1 (entertainment earnings volatile)
- `insider_trading_sector_context`: COMM similar to TECH defaults
- `dismissal_rates`: COMM with rate 52.5 (media sector moderate dismissal rate)
- `claim_base_rates`: COMM with 3.5 (moderate SCA filing rate)

NOTE: `sector_codes` mapping already has `"COMM": "Communications"` and `sector_etfs` already has COMM -> XLC. Only add to tables that lack COMM.
  </action>
  <verify>
Run: `cd /Users/gorlin/projects/research && python -c "from do_uw.stages.resolve.sec_identity import sic_to_sector; assert sic_to_sector('7841') == 'COMM', f'NFLX: {sic_to_sector(\"7841\")}'; assert sic_to_sector('7990') == 'COMM', f'DIS: {sic_to_sector(\"7990\")}'; assert sic_to_sector('7372') == 'TECH'; assert sic_to_sector('2911') == 'ENGY'; print('All sector mappings correct')"`
  </verify>
  <done>NFLX SIC 7841 and DIS SIC 7990 both resolve to COMM. All existing sector mappings unchanged. COMM sector has baselines in all sectors.json tables.</done>
</task>

<task type="auto">
  <name>Task 2: Add sector classification regression tests</name>
  <files>tests/test_resolve.py</files>
  <action>
Add regression tests to the existing resolve test file for the SIC-to-sector mapping refinements:

1. Test that SIC codes 7841 (Netflix), 7990 (Disney), 7812, 7819, 7922 all map to "COMM"
2. Test that SIC codes 7371, 7372, 7374 still map to "TECH" (computer services)
3. Test that SIC codes 7011, 7211 still map to "CONS" (hotels, personal services)
4. Test that SIC codes 7411, 7500 still map to "INDU" (management services)
5. Test that SIC codes 8011, 8049 still map to "HLTH" (health services)
6. Test that SIC codes 8711, 8742 still map to "TECH" (engineering/management consulting)

Use parametrize for clean test structure. Name the test `test_sic_to_sector_refined_services`.

Also run the full existing test suite to verify no regressions:
`uv run pytest tests/test_resolve.py -v`
  </action>
  <verify>Run: `cd /Users/gorlin/projects/research && uv run pytest tests/test_resolve.py -v -k "sic_to_sector"` -- all tests pass</verify>
  <done>Regression tests for SIC-to-sector mapping covering all service SIC ranges (70-89). No regressions in existing tests.</done>
</task>

</tasks>

<verification>
1. `python -c "from do_uw.stages.resolve.sec_identity import sic_to_sector; print(sic_to_sector('7841'))"` prints "COMM"
2. `uv run pytest tests/test_resolve.py -v` -- all tests pass
3. `python -c "import json; data=json.load(open('src/do_uw/brain/sectors.json')); assert 'COMM' in data['claim_base_rates']; print('COMM in sectors')"` -- prints "COMM in sectors"
</verification>

<success_criteria>
NFLX (SIC 7841) and DIS (SIC 7990) classified as COMM. All existing sector mappings preserved. COMM sector has baselines in all sectors.json tables. Regression tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/23-end-to-end-output-quality/23-01-SUMMARY.md`
</output>
