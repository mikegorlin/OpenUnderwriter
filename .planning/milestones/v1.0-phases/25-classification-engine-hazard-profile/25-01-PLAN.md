---
phase: 25-classification-engine-hazard-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/models/classification.py
  - src/do_uw/models/hazard_profile.py
  - src/do_uw/models/__init__.py
  - src/do_uw/models/state.py
  - src/do_uw/config/classification.json
  - src/do_uw/config/hazard_weights.json
  - src/do_uw/config/hazard_interactions.json
  - src/do_uw/stages/classify/__init__.py
  - src/do_uw/stages/classify/classification_engine.py
  - src/do_uw/stages/classify/severity_bands.py
  - tests/test_classification.py
autonomous: true

must_haves:
  truths:
    - "Classification engine takes market cap, sector code, and years public and returns a deterministic base filing rate + severity band"
    - "All tier boundaries, sector rates, IPO multipliers, and severity bands come from classification.json (zero hardcoded domain values in Python)"
    - "IPO age uses the 3-year cliff model: 2.8x for 0-3 years, 1.5x for 3-5 years, 1.0x after 5 years"
    - "5 market cap tiers (Mega/Large/Mid/Small/Micro) with correct boundary values match user specification"
    - "ClassificationResult and HazardProfile Pydantic models are stored on AnalysisState"
  artifacts:
    - path: "src/do_uw/models/classification.py"
      provides: "ClassificationResult, MarketCapTier Pydantic models"
      contains: "class ClassificationResult"
    - path: "src/do_uw/models/hazard_profile.py"
      provides: "HazardProfile, HazardDimensionScore, CategoryScore, InteractionEffect, HazardCategory models"
      contains: "class HazardProfile"
    - path: "src/do_uw/config/classification.json"
      provides: "Market cap tiers, sector rates, IPO age decay, severity bands"
      contains: "market_cap_tiers"
    - path: "src/do_uw/config/hazard_weights.json"
      provides: "7 category weights, 47 dimension definitions with scoring scales"
      contains: "categories"
    - path: "src/do_uw/config/hazard_interactions.json"
      provides: "Named interaction patterns, dynamic detection config"
      contains: "named_interactions"
    - path: "src/do_uw/stages/classify/classification_engine.py"
      provides: "classify_company() pure function"
      contains: "def classify_company"
    - path: "tests/test_classification.py"
      provides: "Classification engine unit tests"
      contains: "def test_classify"
  key_links:
    - from: "src/do_uw/stages/classify/classification_engine.py"
      to: "src/do_uw/config/classification.json"
      via: "Config loading for tiers, rates, multipliers"
      pattern: "classification\\.json"
    - from: "src/do_uw/models/state.py"
      to: "src/do_uw/models/classification.py"
      via: "AnalysisState.classification field"
      pattern: "classification.*ClassificationResult"
---

<objective>
Build the Layer 1 Classification Engine, all Pydantic models (ClassificationResult + HazardProfile), all three JSON config files, and the AnalysisState extension. The classification engine takes exactly 3 objective variables (market cap tier, industry sector, IPO age) and produces a deterministic base filing rate + severity band. This plan also creates the hazard profile models and config files so Plan 02 (dimension scoring) can work in parallel.

Purpose: Establish the data models, config infrastructure, and classification logic that both the hazard engine (Plan 02) and pipeline integration (Plan 03) depend on.
Output: Working classification engine with tests, complete model definitions, and all config files populated.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-classification-engine-hazard-profile/25-CONTEXT.md
@.planning/phases/25-classification-engine-hazard-profile/25-RESEARCH.md
@.planning/phases/24-check-calibration-knowledge-enrichment/research/HAZARD_DIMENSIONS_RESEARCH.md
@src/do_uw/models/state.py
@src/do_uw/models/__init__.py
@src/do_uw/models/executive_summary.py
@src/do_uw/stages/benchmark/inherent_risk.py
@src/do_uw/brain/sectors.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pydantic models, config files, and AnalysisState extension</name>
  <files>
    src/do_uw/models/classification.py
    src/do_uw/models/hazard_profile.py
    src/do_uw/models/__init__.py
    src/do_uw/models/state.py
    src/do_uw/config/classification.json
    src/do_uw/config/hazard_weights.json
    src/do_uw/config/hazard_interactions.json
  </files>
  <action>
    Create `src/do_uw/models/classification.py`:
    - `MarketCapTier(StrEnum)`: MEGA, LARGE, MID, SMALL, MICRO
    - `ClassificationResult(BaseModel)`: market_cap_tier, sector_code, sector_name, years_public (int|None), base_filing_rate_pct (float), severity_band_low_m (float), severity_band_high_m (float), ddl_exposure_base_m (float, default 0.0), ipo_multiplier (float, default 1.0), cap_filing_multiplier (float), methodology (str, default "classification_v1")
    - Follow existing model patterns (ConfigDict, Field descriptions, StrEnum)

    Create `src/do_uw/models/hazard_profile.py`:
    - `HazardCategory(StrEnum)`: H1 through H7 (BUSINESS, PEOPLE, FINANCIAL, GOVERNANCE, MATURITY, ENVIRONMENT, EMERGING)
    - `HazardDimensionScore(BaseModel)`: dimension_id (str), dimension_name (str), category (HazardCategory), raw_score (float), max_score (float), normalized_score (float 0-100), data_available (bool, default True), data_sources (list[str]), evidence (list[str])
    - `CategoryScore(BaseModel)`: category (HazardCategory), category_name (str), weight_pct (float), raw_score (float), weighted_score (float), dimensions_scored (int), dimensions_total (int), data_coverage_pct (float)
    - `InteractionEffect(BaseModel)`: interaction_id (str), name (str), description (str), triggered_dimensions (list[str]), multiplier (float), is_named (bool, default True)
    - `HazardProfile(BaseModel)`: ies_score (float 0-100), raw_ies_score (float), ies_multiplier (float), dimension_scores (list[HazardDimensionScore]), category_scores (dict[str, CategoryScore]), named_interactions (list[InteractionEffect]), dynamic_interactions (list[InteractionEffect]), interaction_multiplier (float, default 1.0), data_coverage_pct (float), confidence_note (str), underwriter_flags (list[str])

    Update `src/do_uw/models/__init__.py`:
    - Import and re-export ClassificationResult, MarketCapTier from classification
    - Import and re-export HazardProfile, HazardDimensionScore, CategoryScore, InteractionEffect, HazardCategory from hazard_profile
    - Add all to __all__

    Update `src/do_uw/models/state.py`:
    - Add imports for ClassificationResult and HazardProfile
    - Add to AnalysisState: `classification: ClassificationResult | None = Field(default=None, description="Layer 1: Objective classification")` and `hazard_profile: HazardProfile | None = Field(default=None, description="Layer 2: Hazard profile (IES)")`
    - Do NOT modify PIPELINE_STAGES -- classification and hazard run as pre-ANALYZE sub-steps

    Create `src/do_uw/config/classification.json`:
    - `market_cap_tiers`: Array of 5 tiers with tier name, min_cap, filing_rate_range, filing_multiplier, severity_band_m. Use exact values from 25-CONTEXT.md: MEGA (>$200B, 6-8%, multiplier 1.8), LARGE ($10-200B, 4-6%, 1.3), MID ($2-10B, 3-4%, 1.0), SMALL ($300M-2B, 2-3%, 0.7), MICRO (<$300M, 1-2%, 0.5)
    - `sector_rates`: Dict of sector_code -> base filing rate. BIOT: 7.0, TECH: 5.0, HLTH: 4.0, FINS: 4.0, COMM: 3.5, CONS: 3.5, ENGY: 3.0, INDU: 2.5, STPL: 2.0, UTIL: 1.5, REIT: 3.0, DEFAULT: 3.5
    - `ipo_age_decay`: cliff_years: 3, cliff_multiplier: 2.8, transition_years: 5, transition_multiplier: 1.5, seasoned_multiplier: 1.0
    - `ies_multiplier_breakpoints`: Array of [ies, multiplier] pairs for piecewise linear interpolation. Use research recommended values: [0, 0.5], [20, 0.7], [35, 0.85], [50, 1.0], [65, 1.3], [80, 2.0], [90, 2.5], [100, 3.5]
    - `ddl_base_drop_pct`: 15 (default assumed stock drop for DDL computation)

    Create `src/do_uw/config/hazard_weights.json`:
    - `version`: "1.0"
    - `categories`: Dict keyed by category ID (H1-H7), each with: name, weight_pct (use rebalanced: H1=32.5, H2=15, H3=15, H4=7.5, H5=10, H6=10, H7=10), description
    - `dimensions`: Dict keyed by dimension ID (H1-01 through H7-06, all 47), each with: name, category, max_score (from research: varies 2-10 per dimension), description, scoring_method ("proxy"|"quantitative"|"config_lookup"|"derived"), data_sources (list of data field paths), default_score_pct (50 = neutral for missing data)
    - Populate ALL 47 dimensions from HAZARD_DIMENSIONS_RESEARCH.md with their scoring scales and max scores
    - `missing_data_handling`: { "default_score_pct": 50, "low_coverage_threshold_pct": 60, "low_coverage_note": "IES based on limited data coverage" }

    Create `src/do_uw/config/hazard_interactions.json`:
    - `named_interactions`: Array of 5 named patterns from research:
      1. ROOKIE_ROCKET: required_dimensions (H1-09 speed_of_growth >=60%, H2-01 management_experience >=60%, H5-01 ipo_recency >=50%), multiplier_range [1.3, 1.5]
      2. BLACK_BOX: required_dimensions (H1-02 business_complexity >=60%, H3-04 earnings_quality >=50%, H1-11 non_gaap_reliance >=50%), multiplier_range [1.2, 1.4]
      3. IMPERIAL_FOUNDER: required_dimensions (H2-05 founder_led >=70%, H1-10 dual_class >=50%, H4-02 board_independence >=50%), multiplier_range [1.2, 1.5]
      4. ACQUISITION_MACHINE: required_dimensions (H1-08 ma_activity >=60%, H3-03 goodwill_heavy >=50%), multiplier_range [1.15, 1.35]
      5. CASH_BURN_CLIFF: required_dimensions (H3-06 pre_revenue_cash_burn >=70%, H5-01 ipo_recency >=50%, H1-09 speed_of_growth >=40%), multiplier_range [1.25, 1.45]
    - `dynamic_detection`: min_elevated_dimensions: 5, elevated_threshold_pct: 60, co_occurrence_multiplier: [1.05, 1.15], category_concentration_min: 3, category_concentration_multiplier: 1.05
    - Each named interaction includes id, name, description, required_dimensions (dict of dim_id -> {min_score_pct}), multiplier_range, category label
  </action>
  <verify>
    Run: `cd /Users/gorlin/projects/research && python -c "from do_uw.models.classification import ClassificationResult, MarketCapTier; from do_uw.models.hazard_profile import HazardProfile, HazardDimensionScore, CategoryScore, InteractionEffect, HazardCategory; from do_uw.models.state import AnalysisState; s = AnalysisState(ticker='TEST'); assert s.classification is None; assert s.hazard_profile is None; print('Models OK')"` and `python -c "import json; from pathlib import Path; c = Path('src/do_uw/config'); [json.loads((c/f).read_text()) for f in ['classification.json','hazard_weights.json','hazard_interactions.json']]; print('Config OK')"`
  </verify>
  <done>
    All 5 Pydantic models created and importable. All 3 JSON config files loadable with correct structure. AnalysisState has classification and hazard_profile optional fields. PIPELINE_STAGES unchanged at 7.
  </done>
</task>

<task type="auto">
  <name>Task 2: Classification engine and unit tests</name>
  <files>
    src/do_uw/stages/classify/__init__.py
    src/do_uw/stages/classify/classification_engine.py
    src/do_uw/stages/classify/severity_bands.py
    tests/test_classification.py
  </files>
  <action>
    Create `src/do_uw/stages/classify/__init__.py`:
    - Re-export classify_company from classification_engine
    - Brief module docstring: "Classification engine: Layer 1 of the five-layer analysis architecture."

    Create `src/do_uw/stages/classify/classification_engine.py`:
    - `classify_company(market_cap: float | None, sector_code: str, years_public: int | None, config: dict[str, Any]) -> ClassificationResult`
    - Pure function, fully deterministic, no side effects
    - `_determine_cap_tier(market_cap, tiers_config)`: Iterate tiers from largest to smallest. Return MarketCapTier + filing_multiplier. Default: MID, 1.0
    - `_get_sector_rate(sector_code, sector_rates)`: Look up base rate by sector code. Fallback to DEFAULT
    - `_ipo_age_multiplier(years_public, ipo_config)`: Implement 3-year cliff model per user spec. None years_public = 1.0 (assume seasoned). years 0-cliff_years: cliff_multiplier (2.8x). years cliff+1 to transition_years: transition_multiplier (1.5x). years > transition_years: seasoned_multiplier (1.0x)
    - `_compute_ddl_base(market_cap, drop_pct)`: DDL = market_cap * drop_pct / 100. Used for DDL exposure field.
    - Formula: `filing_rate = sector_base_rate * cap_multiplier * ipo_multiplier`. Cap the result at 25% (sanity ceiling).
    - `load_classification_config()`: Load classification.json from config/ using `Path(__file__).parent.parent.parent / "config" / "classification.json"` pattern (consistent with existing config loading)

    Create `src/do_uw/stages/classify/severity_bands.py`:
    - `compute_severity_band(market_cap: float | None, tiers_config: list[dict[str, Any]]) -> tuple[float, float]`: Look up severity band [low_m, high_m] from the matching market cap tier. Default: (10.0, 40.0) for MID tier.

    Create `tests/test_classification.py`:
    - Test `_determine_cap_tier`: MEGA for $500B, LARGE for $50B, MID for $5B, SMALL for $1B, MICRO for $100M, None -> MID
    - Test `_ipo_age_multiplier`: year 0 -> 2.8x, year 2 -> 2.8x, year 3 -> 2.8x, year 4 -> 1.5x, year 5 -> 1.5x, year 6 -> 1.0x, year 20 -> 1.0x, None -> 1.0x
    - Test `classify_company` end-to-end: AAPL-like (mega cap, TECH, 40 years public) -> base rate in 5-10% range. SMCI-like (mid cap, TECH, 15 years) -> different rate. Pre-IPO biotech (small cap, BIOT, 1 year) -> highest rate
    - Test severity bands match expected ranges for each tier
    - Test edge cases: None market_cap, unknown sector code -> DEFAULT, 0 years public
    - All tests use the real classification.json config (load it in fixtures)
  </action>
  <verify>
    Run: `cd /Users/gorlin/projects/research && python -m pytest tests/test_classification.py -v`
  </verify>
  <done>
    classify_company() returns sensible ClassificationResult for all market cap tiers. IPO age cliff model works correctly. All config-driven, zero hardcoded domain values in Python. Tests pass.
  </done>
</task>

</tasks>

<verification>
1. `python -c "from do_uw.models import ClassificationResult, HazardProfile, MarketCapTier, HazardCategory"` succeeds
2. `python -c "from do_uw.models.state import AnalysisState; s = AnalysisState(ticker='T'); assert hasattr(s, 'classification')"` succeeds
3. `python -m pytest tests/test_classification.py -v` all pass
4. `python -m pytest tests/ -x --timeout=30` no regressions in existing tests
5. JSON configs load without error and contain expected structures
6. PIPELINE_STAGES remains exactly 7 stages
</verification>

<success_criteria>
- ClassificationResult model captures all Layer 1 outputs (filing rate, severity band, DDL, IPO multiplier)
- HazardProfile model captures all Layer 2 outputs (IES, 47 dimension scores, category scores, interactions)
- classification.json contains 5 market cap tiers, 12 sector rates, IPO decay config, IES breakpoints
- hazard_weights.json contains all 7 categories with rebalanced weights and all 47 dimension definitions
- hazard_interactions.json contains 5 named interaction patterns and dynamic detection config
- classify_company() is a pure function that reads only from config
- AnalysisState extended with classification + hazard_profile fields without breaking existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/25-classification-engine-hazard-profile/25-01-SUMMARY.md`
</output>
