---
phase: 30-knowledge-system-feedback-loop
plan: 04
type: execute
wave: 3
depends_on: ["30-02", "30-03"]
files_modified:
  - src/do_uw/stages/render/sections/sect1_market_context.py
  - src/do_uw/stages/render/sections/sect1_executive.py
  - tests/stages/render/test_sect1_market_context.py
autonomous: true

must_haves:
  truths:
    - "Worksheet shows a 'Market Context' section with market price ranges for similar-profile companies"
    - "Market Context section displays mispricing signals when available"
    - "Section gracefully handles missing pricing data (shows 'No market data available')"
  artifacts:
    - path: "src/do_uw/stages/render/sections/sect1_market_context.py"
      provides: "Market Context render section"
      contains: "render_market_context"
    - path: "src/do_uw/stages/render/sections/sect1_executive.py"
      provides: "Integration point calling market context renderer"
      contains: "render_market_context"
    - path: "tests/stages/render/test_sect1_market_context.py"
      provides: "Tests for market context rendering with and without data"
  key_links:
    - from: "src/do_uw/stages/render/sections/sect1_executive.py"
      to: "src/do_uw/stages/render/sections/sect1_market_context.py"
      via: "Calls render_market_context() after deal context section"
      pattern: "render_market_context"
    - from: "src/do_uw/stages/render/sections/sect1_market_context.py"
      to: "src/do_uw/models/executive_summary.py"
      via: "Reads MarketIntelligence from state.executive_summary.deal_context.market_intelligence"
      pattern: "market_intelligence"
---

<objective>
Render the Market Context section in the worksheet. MarketIntelligence is already computed in the BENCHMARK stage and stored on state.executive_summary.deal_context.market_intelligence. This plan creates the render section that displays market price ranges, peer segment positioning, and mispricing signals.

Purpose: Connects pricing data to the visible output, giving underwriters market context alongside risk analysis. This is the "last mile" -- data exists, just needs rendering.

Output: sect1_market_context.py render section, integration into sect1_executive.py, tests.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/do_uw/models/executive_summary.py
@src/do_uw/stages/render/sections/sect1_executive.py
@src/do_uw/stages/benchmark/market_position.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Market Context render section</name>
  <files>
    src/do_uw/stages/render/sections/sect1_market_context.py
    src/do_uw/stages/render/sections/sect1_executive.py
    tests/stages/render/test_sect1_market_context.py
  </files>
  <action>
    **sect1_market_context.py** -- Create new render section module. Look at the MarketIntelligence model in `models/executive_summary.py` to understand available fields. Based on the BENCHMARK stage's `get_market_intelligence()` function, the model likely has:
    - `has_data: bool` -- whether pricing data is available
    - Market price range fields (median premium, range, peer segment)
    - Positioning indicators (mispricing signals)

    Create a `render_market_context(doc, state)` function that:
    1. Reads `state.executive_summary.deal_context.market_intelligence`
    2. If `None` or `has_data is False`, renders a brief "Market Context: No market pricing data available" paragraph and returns
    3. If data available, renders:
       - Heading: "Market Context" (use the existing heading style from sect1_executive.py -- look at how other sections add headings)
       - A summary paragraph describing the market position
       - A table with: Metric | Value | Peer Median | Position columns
       - Include: premium rate, rate per million, market segment median
       - Mispricing signals (if any): highlight where the company's implied pricing deviates significantly from peers

    Follow the rendering patterns established in existing sect1_*.py files:
    - Use the same document/style helpers
    - Same table formatting conventions
    - Same paragraph styling

    IMPORTANT: Keep this file under 150 lines. It's a focused render section for one data element.

    **sect1_executive.py** -- Find where the executive summary sections are rendered (after deal context). Add a call to `render_market_context(doc, state)` at the appropriate location. Look for where SECT1-07 (Deal Context) is rendered -- Market Context goes right after as SECT1-08.

    Read sect1_executive.py to understand the call structure before making changes. The integration should be 2-5 lines: import and function call.

    **tests/stages/render/test_sect1_market_context.py** -- Test:
    1. render_market_context with has_data=False -> only "no data" paragraph rendered
    2. render_market_context with has_data=True and populated fields -> table and summary rendered
    3. render_market_context with market_intelligence=None -> graceful handling

    Use the same test patterns as existing render tests. If other render tests mock the document object or use python-docx directly, follow that pattern.
  </action>
  <verify>
    `uv run pytest tests/stages/render/test_sect1_market_context.py -v --tb=short`
    `uv run pytest tests/ -x -q --tb=short 2>&1 | tail -10`
  </verify>
  <done>
    Market Context section renders in the worksheet when pricing data is available. Shows "No market data available" when pricing data is absent. Integrated into sect1_executive.py after Deal Context. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/ -x -q --tb=short` -- all tests pass
2. `uv run pytest tests/stages/render/test_sect1_market_context.py -v` -- market context tests pass
3. Verify sect1_market_context.py exists and exports render_market_context
4. Verify sect1_executive.py calls render_market_context
</verification>

<success_criteria>
- Market Context section renders in the executive summary after Deal Context
- Shows market price ranges, peer segment positioning, and mispricing signals when data available
- Gracefully shows "no data" message when pricing data unavailable
- Follows existing rendering patterns and style conventions
- All existing tests pass with zero regression
</success_criteria>

<output>
After completion, create `.planning/phases/30-knowledge-system-feedback-loop/30-04-SUMMARY.md`
</output>
