---
phase: 40-professional-pdf-visual-polish
plan: 05
type: execute
wave: 3
depends_on: ["40-02", "40-03", "40-04"]
files_modified:
  - src/do_uw/stages/render/html_renderer.py
  - src/do_uw/templates/html/styles.css
  - src/do_uw/templates/html/base.html.j2
  - src/do_uw/templates/html/sections/executive.html.j2
  - src/do_uw/templates/html/sections/company.html.j2
  - src/do_uw/templates/html/sections/financial.html.j2
  - src/do_uw/templates/html/sections/market.html.j2
  - src/do_uw/templates/html/sections/governance.html.j2
  - src/do_uw/templates/html/sections/litigation.html.j2
  - src/do_uw/templates/html/sections/scoring.html.j2
autonomous: false
requirements: []

must_haves:
  truths:
    - "PDF is 15-20 pages dense with data, not 12 pages with blank half-pages"
    - "Body text is 9-10pt, compact spacing, maximizing data per page"
    - "Company name appears correctly (not 'Unknown Company')"
    - "Sector shows actual sector name (not 'DEFAULT')"
    - "No raw SourcedValue objects visible in any output field"
    - "PDF looks like an S&P credit report or Moody's rating report to the user"
  artifacts:
    - path: "src/do_uw/templates/html/styles.css"
      provides: "Density-optimized CSS with compact spacing, 9-10pt body, tight margins"
  key_links:
    - from: "src/do_uw/stages/render/html_renderer.py"
      to: "build_html_context"
      via: "company_name properly extracted from state"
      pattern: "company_name"
    - from: "src/do_uw/templates/html/styles.css"
      to: "All section templates"
      via: "CSS classes for compact layout"
      pattern: "font-size.*9pt|font-size.*10pt"
---

<objective>
Tune information density to match the "dense and compact" S&P report goal, fix data flow issues (Unknown Company, DEFAULT sector), and perform integration testing to verify the complete PDF looks professional.

Purpose: All the pieces (CSS, cover page, tables, charts, badges) need to come together as a cohesive document. The AAPL PDF review found specific data flow issues (company_name, sector) that must be fixed. The target is 15-20 pages of dense, useful data.

Output: Density-optimized CSS, data flow fixes, a polished PDF ready for user review.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-professional-pdf-visual-polish/40-CONTEXT.md
@.planning/phases/40-professional-pdf-visual-polish/40-01-SUMMARY.md
@.planning/phases/40-professional-pdf-visual-polish/40-02-SUMMARY.md
@.planning/phases/40-professional-pdf-visual-polish/40-03-SUMMARY.md
@.planning/phases/40-professional-pdf-visual-polish/40-04-SUMMARY.md
@src/do_uw/stages/render/html_renderer.py
@src/do_uw/templates/html/styles.css
@src/do_uw/templates/html/base.html.j2
@src/do_uw/stages/render/md_renderer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix data flow issues and optimize density</name>
  <files>
    src/do_uw/stages/render/html_renderer.py
    src/do_uw/templates/html/styles.css
    src/do_uw/templates/html/sections/executive.html.j2
    src/do_uw/templates/html/sections/company.html.j2
    src/do_uw/templates/html/sections/financial.html.j2
    src/do_uw/templates/html/sections/market.html.j2
    src/do_uw/templates/html/sections/governance.html.j2
    src/do_uw/templates/html/sections/litigation.html.j2
    src/do_uw/templates/html/sections/scoring.html.j2
  </files>
  <action>
  1. Fix "Unknown Company" issue:
     The company_name in build_html_context comes from build_template_context in md_renderer.py.
     Trace the code path:
     - Check `build_template_context` in md_renderer.py for how company_name is extracted
     - It likely accesses state.company.identity.legal_name which is a SourcedValue
     - If it's returning "Unknown Company", the .value extraction may be failing
     - Fix: ensure company_name = state.company.identity.legal_name.value (not the SourcedValue object)
     - Also check: state.company.identity.company_name vs .legal_name -- which field has the data?
     - Add defensive fallback chain: legal_name.value -> company_name -> ticker -> "Unknown Company"

  2. Fix "DEFAULT" sector issue:
     The sector display in build_html_context:
     ```python
     sector = ""
     if state.company and state.company.identity and state.company.identity.sector:
         sector = str(state.company.identity.sector)
     ```
     The sector is likely a SourcedValue, so `str(state.company.identity.sector)` might be showing the SourcedValue repr.
     Fix: extract `.value` from the SourcedValue. Then run through `sector_display_name()` from formatters.py to convert "TECH" -> "Technology".
     ```python
     raw_sector = state.company.identity.sector
     if isinstance(raw_sector, SourcedValue):
         sector = sector_display_name(str(raw_sector.value))
     elif raw_sector:
         sector = sector_display_name(str(raw_sector))
     ```

  3. Density optimization in `styles.css`:
     Per LOCKED decision: "Dense & compact, 9-10pt body, tight spacing, aim for 15-20 pages"

     Update the base body style and print styles:
     ```css
     body {
       font-size: 10pt;      /* was implicit browser default or 1rem */
       line-height: 1.45;    /* was 1.6 -- tighter for density */
     }

     @media print {
       body {
         font-size: 9pt;     /* even tighter in print */
         line-height: 1.4;
       }
     }
     ```

     Reduce heading sizes for print:
     ```css
     @media print {
       h2 { font-size: 12pt; margin-top: 0.75rem; margin-bottom: 0.5rem; }
       h3 { font-size: 10pt; margin-top: 0.5rem; margin-bottom: 0.25rem; }
     }
     ```

     Reduce table cell padding:
     ```css
     @media print {
       td, th { padding: 3px 6px; }  /* was 8px 12px via Tailwind px-3 py-2 */
     }
     ```

     Reduce spacing between sections:
     ```css
     @media print {
       .my-3 { margin-top: 0.5rem; margin-bottom: 0.5rem; }
       .my-4 { margin-top: 0.75rem; margin-bottom: 0.75rem; }
       .gap-4 { gap: 0.5rem; }
     }
     ```

  4. Template density pass -- scan each section template and reduce verbose whitespace:
     - Remove excessive `my-4` and `my-6` spacers between subsections
     - Replace `gap-4` with `gap-2` on grid layouts where appropriate
     - Reduce padding on callout boxes (discovery-box, warning-box) in print
     - Remove redundant "Elevated Concern" density indicators when the parent section already shows one
       (the research noted "Repetitive ELEVATED CONCERN indicators in governance" -- subsection indicators 5.1-5.4 can be removed if the section-level indicator is present)

  5. Run tests: `uv run pytest tests/stages/render/ -x -q`
  </action>
  <verify>
  - company_name extraction in html_renderer.py properly unwraps SourcedValue
  - sector extraction uses sector_display_name for human-readable name
  - Print body font-size is 9pt in styles.css
  - `uv run pytest tests/stages/render/ -x -q` passes
  </verify>
  <done>Company name and sector display correctly, document is dense and compact at 9-10pt, reduced whitespace and redundant indicators, heading sizes optimized for density</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Visual review of generated PDF</name>
  <what-built>
  Complete PDF visual polish pipeline:
  1. Static CSS replacing Tailwind CDN (Plan 01)
  2. Branded cover page with company info and tier badge (Plan 02)
  3. Professional header/footer on every page (Plan 02)
  4. Accounting-style number formatting with red parentheses (Plan 03)
  5. Polished tables with navy headers, zebra striping, right-aligned numbers (Plan 03)
  6. All 4 charts with light theme and figure captions (Plan 04)
  7. Colored risk indicator pills (Plan 04)
  8. Humanized enum values and stripped markdown (Plan 04)
  9. Dense compact layout at 9-10pt body text (Plan 05)
  10. Fixed company name and sector display (Plan 05)
  </what-built>
  <how-to-verify>
  1. Generate a PDF for a company with cached data (e.g., AAPL if state.json exists):
     `uv run do-uw analyze AAPL --skip-acquire --pdf-only` (or whatever the command is to render from cached state)
     If no cached state, generate from the test fixture: check if there's a render command.

  2. Open the PDF and check:
     - [ ] Cover page: company name, ticker, date, tier badge, "Angry Dolphin" branding
     - [ ] Header on every page: "CONFIDENTIAL -- Apple Inc. D&O Worksheet"
     - [ ] Footer on every page: "Angry Dolphin Underwriting System | Page X of Y | 2026-02-XX"
     - [ ] No "Unknown Company" or "DEFAULT" sector
     - [ ] Financial tables: navy headers, zebra rows, right-aligned numbers
     - [ ] Negative numbers: red $(1,234) format
     - [ ] Charts: stock, radar, ownership, timeline with figure captions
     - [ ] Risk badges: colored pills (red/amber/green)
     - [ ] No raw enum values or markdown formatting
     - [ ] Dense layout: aim for 15-20 pages with data
     - [ ] No blank half-pages
     - [ ] Professional overall impression: "looks like an S&P credit report"

  3. If PDF generation fails, check the console output for errors.
  </how-to-verify>
  <resume-signal>Type "approved" if the PDF looks professional, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Company name shows correctly (not "Unknown Company")
2. Sector shows human name (not "DEFAULT" or code)
3. PDF is dense: ~15-20 pages, 9-10pt body text
4. No blank half-pages
5. No raw SourcedValue objects, enum values, or markdown artifacts
6. Overall professional appearance matching S&P/Moody's report style
7. All render tests pass
8. User approves visual quality
</verification>

<success_criteria>
- Data flow issues fixed (company name, sector resolve correctly)
- Document density matches S&P report target (15-20 pages, compact)
- User confirms PDF "looks like a Bloomberg/S&P credit report"
- All formatting from Plans 01-04 is visually integrated and cohesive
</success_criteria>

<output>
After completion, create `.planning/phases/40-professional-pdf-visual-polish/40-05-SUMMARY.md`
</output>
