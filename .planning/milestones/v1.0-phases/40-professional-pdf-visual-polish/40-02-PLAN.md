---
phase: 40-professional-pdf-visual-polish
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - src/do_uw/templates/html/sections/cover.html.j2
  - src/do_uw/templates/html/worksheet.html.j2
  - src/do_uw/templates/html/base.html.j2
  - src/do_uw/templates/html/styles.css
  - src/do_uw/stages/render/html_renderer.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "PDF opens with a branded cover page showing company name, ticker, date, risk tier badge, and Angry Dolphin branding"
    - "Header shows 'CONFIDENTIAL -- [Company] D&O Worksheet' on every page"
    - "Footer shows 'Angry Dolphin Underwriting System | Page X of Y | [Date]' on every page"
    - "Each major section starts on a new page but sparse sections do NOT create blank pages"
    - "PDF is US Letter portrait with 0.75in margins"
  artifacts:
    - path: "src/do_uw/templates/html/sections/cover.html.j2"
      provides: "Branded cover page template with company info, risk tier badge, branding"
  key_links:
    - from: "src/do_uw/templates/html/worksheet.html.j2"
      to: "sections/cover.html.j2"
      via: "Jinja2 include as first section"
      pattern: "include.*cover"
    - from: "src/do_uw/stages/render/html_renderer.py"
      to: "Playwright page.pdf()"
      via: "header_template and footer_template with company name"
      pattern: "header_template.*CONFIDENTIAL"
---

<objective>
Create a branded cover page, proper page headers/footers, and conditional page break logic so the PDF has the professional structure of an S&P credit report.

Purpose: A cover page immediately signals the document's quality and purpose. Headers/footers provide context on every page. Conditional page breaks prevent the blank-page problem observed in the AAPL PDF.

Output: Cover page template, updated header/footer in Playwright, conditional page break CSS.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-professional-pdf-visual-polish/40-CONTEXT.md
@.planning/phases/40-professional-pdf-visual-polish/40-01-SUMMARY.md
@src/do_uw/templates/html/base.html.j2
@src/do_uw/templates/html/worksheet.html.j2
@src/do_uw/stages/render/html_renderer.py
@src/do_uw/templates/html/components/badges.html.j2
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create branded cover page template</name>
  <files>
    src/do_uw/templates/html/sections/cover.html.j2
    src/do_uw/templates/html/worksheet.html.j2
    src/do_uw/templates/html/styles.css
  </files>
  <action>
  1. Create `src/do_uw/templates/html/sections/cover.html.j2`:
     A full-page cover in S&P/Moody's credit report style:

     Layout (centered vertically and horizontally on the page):
     - Top: "CONFIDENTIAL" in small navy caps
     - Middle block:
       - Company name in large navy heading (2rem+)
       - Ticker in gold accent (e.g., "(AAPL)")
       - Horizontal gold divider line
       - Risk tier badge (reuse existing tier_badge macro) -- prominently displayed
       - Quality score and composite score if available
       - Date of analysis
     - Bottom block:
       - "Angry Dolphin Underwriting System" in navy
       - "D&O Liability Underwriting Worksheet" subtitle
       - A small disclaimer: "For Internal Underwriting Use Only"

     CSS approach:
     - Use flexbox with `min-height: 100vh` to fill the page
     - `flex-direction: column; justify-content: center; align-items: center;`
     - The cover page gets `break-after: page` to force a page break after it

     Template should use context variables: company_name, ticker, generation_date,
     executive_summary.tier_label, executive_summary.quality_score, executive_summary.composite_score

  2. Update `worksheet.html.j2`:
     - Add `{% include "sections/cover.html.j2" %}` as the FIRST line inside `{% block content %}`, BEFORE executive.html.j2
     - This ensures the cover page appears first in the PDF

  3. Add cover page CSS to `styles.css`:
     ```css
     /* Cover page */
     .cover-page {
       min-height: 100vh;
       display: flex;
       flex-direction: column;
       justify-content: center;
       align-items: center;
       text-align: center;
       break-after: page;
     }
     .cover-divider {
       width: 120px;
       height: 3px;
       background-color: var(--do-gold);
       margin: 1.5rem auto;
     }
     ```
  </action>
  <verify>
  - `ls src/do_uw/templates/html/sections/cover.html.j2` exists
  - `grep "cover.html.j2" src/do_uw/templates/html/worksheet.html.j2` returns match
  - `grep "Angry Dolphin" src/do_uw/templates/html/sections/cover.html.j2` returns match
  - `grep "cover-page" src/do_uw/templates/html/styles.css` returns match
  </verify>
  <done>Cover page template renders as first page of PDF with company name, tier badge, date, and branding</done>
</task>

<task type="auto">
  <name>Task 2: Update headers, footers, and conditional page breaks</name>
  <files>
    src/do_uw/stages/render/html_renderer.py
    src/do_uw/templates/html/base.html.j2
    src/do_uw/templates/html/styles.css
    src/do_uw/templates/html/sections/executive.html.j2
    src/do_uw/templates/html/sections/company.html.j2
    src/do_uw/templates/html/sections/financial.html.j2
    src/do_uw/templates/html/sections/market.html.j2
    src/do_uw/templates/html/sections/governance.html.j2
    src/do_uw/templates/html/sections/litigation.html.j2
    src/do_uw/templates/html/sections/scoring.html.j2
    src/do_uw/templates/html/sections/ai_risk.html.j2
  </files>
  <action>
  1. Update Playwright header/footer in `html_renderer.py`:
     Per the LOCKED decision, the header should be:
     `"CONFIDENTIAL -- [Company] D&O Worksheet"`
     And the footer should be:
     `"Angry Dolphin Underwriting System | Page X of Y | [Date]"`

     Update the render_html_pdf function:
     - Pass `company_name` into the header_template using a Jinja2 pre-render or string format:
       ```python
       header_template=(
           '<div style="font-size:7pt; color:#666; width:100%; '
           'text-align:center; padding:0 0.65in;">'
           f'CONFIDENTIAL &mdash; {company_name} D&amp;O Worksheet'
           '</div>'
       ),
       footer_template=(
           '<div style="font-size:7pt; color:#666; width:100%; '
           'text-align:center; padding:0 0.65in;">'
           'Angry Dolphin Underwriting System &bull; '
           'Page <span class="pageNumber"></span> of '
           '<span class="totalPages"></span>'
           f' &bull; {generation_date}'
           '</div>'
       ),
       ```
     - Extract company_name from the context dict before Playwright call
     - Use generation_date from context

  2. Conditional page breaks in section templates:
     Per CONTEXT: "Every major section on new page" but the AAPL PDF had blank pages 6, 8, 10.

     Update each section template to use conditional page breaks.
     The 7 major sections (Executive Summary, Company Profile, Financial, Market, Governance, Litigation, Scoring) ALWAYS get page breaks.
     Minor sections (AI Risk, Meeting Prep, Coverage) only get page breaks if they have substantial content.

     For AI Risk template (`ai_risk.html.j2`):
     - Wrap the page-break class in a conditional: only add if ai_risk data exists and has content
     - `{% if ai_risk and (ai_risk.score is defined or ai_risk.dimensions) %}class="page-break"{% endif %}`

     For all major sections, ensure the `<section>` tag has `class="page-break"`.

  3. Update print CSS in `styles.css`:
     Add orphan/widow and heading protection:
     ```css
     h2, h3 { break-after: avoid; }
     table { break-inside: avoid; }
     .page-break-inside-avoid { break-inside: avoid; }
     ```

  4. Remove the in-document header from `base.html.j2`:
     Since Playwright now renders a proper page header via header_template, the in-document `<header>` with navy background should be kept for the HTML dashboard view but hidden in print:
     - Add `no-print` class to the existing `<header>` element in base.html.j2 so it doesn't appear in PDF (the Playwright header_template handles PDF header)
     - Similarly, add `no-print` to the in-document `<footer>` since Playwright footer_template handles it

  5. Run tests: `uv run pytest tests/stages/render/ -x -q`
  </action>
  <verify>
  - `grep "CONFIDENTIAL" src/do_uw/stages/render/html_renderer.py` returns match with company name
  - `grep "Angry Dolphin" src/do_uw/stages/render/html_renderer.py` returns match in footer
  - Each major section template has `class="page-break"` on its section tag
  - `grep "no-print" src/do_uw/templates/html/base.html.j2` returns matches for header and footer
  - `uv run pytest tests/stages/render/ -x -q` passes
  </verify>
  <done>PDF has branded header/footer on every page, cover page as first page, conditional page breaks prevent blank pages, headings protected from orphaning</done>
</task>

</tasks>

<verification>
1. Cover page renders as first page with company name, tier badge, date, and branding
2. Header on every page shows "CONFIDENTIAL -- [Company] D&O Worksheet"
3. Footer on every page shows "Angry Dolphin Underwriting System | Page X of Y | [Date]"
4. No blank half-pages in the PDF for sections with sparse data
5. Major sections start on new pages; minor sections flow naturally when sparse
6. All render tests pass
</verification>

<success_criteria>
- PDF opens with a full branded cover page
- Every page has correct header and footer per LOCKED decisions
- No more blank pages for sparse sections (AI risk, empty litigation)
- Print CSS properly handles orphans, widows, and heading/table breaks
</success_criteria>

<output>
After completion, create `.planning/phases/40-professional-pdf-visual-polish/40-02-SUMMARY.md`
</output>
