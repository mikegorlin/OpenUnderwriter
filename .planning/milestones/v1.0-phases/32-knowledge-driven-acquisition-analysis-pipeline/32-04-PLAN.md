---
phase: 32-knowledge-driven-acquisition-analysis-pipeline
plan: 04
type: execute
wave: 3
depends_on: [32-01, 32-02, 32-03]
files_modified:
  - src/do_uw/brain/enrichment_data.py
  - src/do_uw/brain/brain_enrich.py
  - src/do_uw/brain/brain_migrate.py
  - src/do_uw/brain/brain_loader.py
  - tests/brain/test_brain_enrich.py
  - tests/brain/test_brain_remap.py
autonomous: true
requirements: [SC-1, SC-3]

must_haves:
  truths:
    - "All 388 checks have risk_questions mapped to v6 subsection IDs (1.1 through 5.9), not old Q1-Q25"
    - "brain_taxonomy contains all 45 subsections from QUESTIONS-FINAL.md as risk_question entities"
    - "SUBDOMAIN_TO_RISK_QUESTIONS in enrichment_data.py uses v6 subsection IDs (X.Y format)"
    - "CHECK_TO_RISK_QUESTIONS overrides in enrichment_data.py use v6 subsection IDs (X.Y format)"
    - "brain_checks version 3 rows contain v6 risk_questions, created by Phase 32 taxonomy remapping"
    - "brain_changelog has entries for every remapped check documenting Q-old to v6 transition"
    - "PREFIX_TO_REPORT_SECTION maps to the 5 v6 section names (company, market, financial, governance, litigation)"
    - "brain_loader.py section_map updated to match v6 section names (financial not financials, no disclosure/forward)"
  artifacts:
    - path: "src/do_uw/brain/enrichment_data.py"
      provides: "Updated mapping tables with v6 subsection IDs (1.1-5.9) replacing Q1-Q25"
    - path: "src/do_uw/brain/brain_enrich.py"
      provides: "Remap function that creates version 3 rows with v6 risk_questions"
    - path: "src/do_uw/brain/brain_loader.py"
      provides: "Updated section_map matching v6 section names"
    - path: "tests/brain/test_brain_remap.py"
      provides: "Validation: all checks mapped to v6, no Q-old references remain, taxonomy entity counts = 45"
  key_links:
    - from: "src/do_uw/brain/enrichment_data.py"
      to: "QUESTIONS-FINAL.md"
      via: "SUBDOMAIN_TO_RISK_QUESTIONS uses v6 X.Y subsection identifiers"
      pattern: "\\d+\\.\\d+"
    - from: "src/do_uw/brain/brain_enrich.py"
      to: "brain.duckdb brain_checks"
      via: "remap_to_v6() creates version 3 rows with updated risk_questions"
      pattern: "remap_to_v6"
---

<objective>
Remap all 388 checks from old Q1-Q25 taxonomy to v6 5-section taxonomy (45 subsections) per QUESTIONS-FINAL.md.

Purpose: The user review (Plan 03) produced QUESTIONS-FINAL.md with a new question numbering scheme. The granularity for brain mapping is SUBSECTION-LEVEL (X.Y format, e.g., 1.1 through 5.9 — 45 entities), NOT individual question-level (X.Y.Z — 208 entities). All check metadata currently uses the old Q1-Q25 identifiers from BRAIN-DESIGN.md. This plan updates every mapping table and creates version 3 DuckDB rows so the entire brain uses the canonical v6 taxonomy at subsection granularity.

Output: Updated enrichment_data.py with v6 subsection IDs, version 3 brain_checks rows, updated brain_taxonomy with 45 subsections, brain_loader.py section_map aligned to v6 section names.

**Scoping note (SC-1/SC-2):** Plans 04-07 deliver the knowledge layer, tooling, and gap detection — the FOUNDATION for the acquisition/extraction flip. The actual wiring of ACQUIRE (brain-reads-to-drive-acquisition, SC-1) and EXTRACT (brain-hints-guide-extraction, SC-2) into the pipeline stages is Phase 33/34 scope. Plans 04-07 do NOT modify the ACQUIRE or EXTRACT stages themselves.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-knowledge-driven-acquisition-analysis-pipeline/QUESTIONS-FINAL.md
@.planning/phases/32-knowledge-driven-acquisition-analysis-pipeline/32-01-SUMMARY.md
@.planning/phases/32-knowledge-driven-acquisition-analysis-pipeline/32-02-SUMMARY.md
@src/do_uw/brain/enrichment_data.py
@src/do_uw/brain/brain_enrich.py
@src/do_uw/brain/brain_migrate.py
@tests/brain/test_brain_enrich.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remap enrichment_data.py to v6 taxonomy and update brain_loader section_map</name>
  <files>
src/do_uw/brain/enrichment_data.py
src/do_uw/brain/brain_loader.py
  </files>
  <action>
Update all mapping tables in enrichment_data.py to use v6 question identifiers from QUESTIONS-FINAL.md instead of old Q1-Q25.

1. **Build Q-old to v6 mapping**: Create a translation table mapping each old Q1-Q25 to one or more v6 subsection IDs. Derive from QUESTIONS-FINAL.md content — Q1 (company identity/classification) maps to 1.1, Q2 (business model) maps to 1.2-1.3, Q6 (financial health) maps to 3.1-3.6, etc. The mapping is many-to-many: old Q6 covers all of Section 3, and v6 has finer granularity.

2. **Update SUBDOMAIN_TO_RISK_QUESTIONS**: Replace Q-IDs with v6 subsection IDs (X.Y format — 45 subsections, NOT X.Y.Z individual questions). Each subdomain should map to the MOST SPECIFIC v6 subsection(s) that match its check content. Examples:
   - `"BIZ.CLASS": ["Q1"]` becomes `"BIZ.CLASS": ["1.1"]` (Identity)
   - `"FIN.LIQ": ["Q6"]` becomes `"FIN.LIQ": ["3.1"]` (Liquidity & Solvency)
   - `"FIN.DEBT": ["Q6"]` becomes `"FIN.DEBT": ["3.2"]` (Leverage & Debt)
   - `"FIN.FORENSIC": ["Q7"]` becomes `"FIN.FORENSIC": ["3.4"]` (Earnings Quality)
   - `"STOCK.PERF": ["Q12"]` becomes `"STOCK.PERF": ["2.1"]` (Stock Price Performance)
   - `"GOV.BOARD": ["Q9"]` becomes `"GOV.BOARD": ["4.1"]` (Board Composition)
   - `"LIT.SCA": ["Q15"]` becomes `"LIT.SCA": ["5.1"]` (SCAs Active)
   The v6 taxonomy is MORE GRANULAR than Q1-Q25, so most subdomains that mapped to one Q now map to one specific v6 subsection. Use only X.Y format IDs (e.g., "1.1", "3.4"), never X.Y.Z.

3. **Update CHECK_TO_RISK_QUESTIONS**: Same approach — replace Q-IDs with v6 subsection IDs for all 84 explicit overrides. Checks that span multiple risk questions should map to multiple v6 subsections.

4. **Update PREFIX_TO_REPORT_SECTION**: The 5 v6 sections are: company, market, financial, governance, litigation. Change:
   - `"FIN": "financials"` to `"FIN": "financial"` (singular to match v6)
   - `"NLP": "disclosure"` to `"NLP": "governance"` (disclosure merged into Section 4 in v6)
   - `"FWRD": "forward"` to `"FWRD": "company"` (forward-looking maps to company risk calendar 1.11)
   Verify the 5 output values match exactly: company, market, financial, governance, litigation.

5. **Do NOT strip detail**: Keep all 84 explicit overrides and all 53 subdomain defaults. Map each one carefully. If unsure which v6 subsection a check belongs to, read the check's name and question text from checks.json to determine the best v6 match.

6. **Update brain_loader.py section_map**: The `section_map` dict in `BrainDBLoader._overlay_enrichment()` currently uses old section names (`"financials"`, `"disclosure"`, `"forward"`). Update to match v6 section names:
   - `"financials"` -> `"financial"` (singular to match v6)
   - Remove `"disclosure"` and `"forward"` entries (these prefixes are remapped in PREFIX_TO_REPORT_SECTION to `"governance"` and `"company"` respectively, so they'll resolve correctly)
   - Final section_map keys: `company`, `market`, `financial`, `governance`, `litigation`

Run existing enrichment tests to verify the module still loads without errors.
  </action>
  <verify>
- `python -c "from do_uw.brain.enrichment_data import SUBDOMAIN_TO_RISK_QUESTIONS; print(len(SUBDOMAIN_TO_RISK_QUESTIONS))"` returns 53+
- No Q-old references remain: `grep -c '"Q[0-9]' src/do_uw/brain/enrichment_data.py` returns 0
- All v6 references match X.Y subsection format (not X.Y.Z): `python -c "from do_uw.brain.enrichment_data import SUBDOMAIN_TO_RISK_QUESTIONS; import re; assert all(all(re.match(r'^\d+\.\d+$', q) for q in qs) for qs in SUBDOMAIN_TO_RISK_QUESTIONS.values())"` passes
- `uv run pytest tests/brain/test_brain_enrich.py -x` passes (may need test updates — handle in Task 2)
  </verify>
  <done>enrichment_data.py uses only v6 subsection IDs (X.Y format, 45 entities), PREFIX_TO_REPORT_SECTION has exactly 5 v6 section values (company, market, financial, governance, litigation), no Q1-Q25 references remain</done>
</task>

<task type="auto">
  <name>Task 2: Apply v6 remap to brain.duckdb and update taxonomy</name>
  <files>
src/do_uw/brain/brain_enrich.py
src/do_uw/brain/brain_migrate.py
tests/brain/test_brain_remap.py
tests/brain/test_brain_enrich.py
  </files>
  <action>
1. **Add remap_to_v6() function in brain_enrich.py**: Create a function that:
   - Reads all brain_checks_current rows
   - For each check, resolves the new v6 risk_questions using the updated enrichment_data.py mappings
   - Creates a new version row (version = current + 1) with the v6 risk_questions
   - Inserts brain_changelog entries documenting the Q-old to v6 transition
   - Returns counts dict (remapped, unchanged, errors)

2. **Update brain_migrate.py taxonomy population**: Replace the 25 Q1-Q25 risk_question entities in brain_taxonomy with the 45 v6 subsections from QUESTIONS-FINAL.md. Granularity is SUBSECTION-LEVEL (X.Y format, 45 entities). Each subsection becomes a risk_question entity with:
   - entity_id: the subsection number in X.Y format (e.g., "1.1", "2.3", "5.9")
   - name: the subsection title (e.g., "Identity", "Volatility & Trading Patterns")
   - description: the subsection question (e.g., "What is this company?")
   - parent_id: the section number (e.g., "1" for all 1.X subsections)
   Do NOT use X.Y.Z individual question IDs (208 entities) — subsection-level is the correct granularity.
   Keep the 5 section-level entities (entity_type="report_section") updated to match v6 section names.
   Keep the 10 factor entities (F1-F10) and 15 hazard entities (HAZ-*) unchanged.

3. **Update test_brain_enrich.py**: Adjust test assertions to expect v6 question IDs instead of Q-IDs. Key checks:
   - Spot-check tests that verify specific check -> risk_question mappings need v6 IDs
   - Distribution assertions: v6 has 45 subsections, not 25 questions
   - Completeness assertions: every AUTO check should still have at least one risk_question

4. **Create test_brain_remap.py**: New test file with:
   - Test that remap_to_v6() creates new version rows for all 388 checks
   - Test that no Q-old references remain in brain_checks_current after remap
   - Test that brain_taxonomy has exactly 45 risk_question entities (X.Y subsection IDs, not X.Y.Z)
   - Test that all risk_question entity_ids match pattern `^\d+\.\d+$` (X.Y format only)
   - Test that brain_changelog has remap entries for all 388 checks
   - Test that remap is idempotent (running twice doesn't create extra versions)

5. **Integration**: Update brain_migrate.py's `migrate_checks_to_brain()` function to call `enrich_brain_checks()` then `remap_to_v6()` in sequence, so a fresh migration produces fully v6-enriched checks.

Run: `uv run pytest tests/brain/ -x -v`
  </action>
  <verify>
- `uv run pytest tests/brain/ -x -v` all passing
- `uv run python -c "from do_uw.brain.brain_schema import connect_brain_db; conn = connect_brain_db(':memory:'); from do_uw.brain.brain_migrate import migrate_checks_to_brain; migrate_checks_to_brain(conn)"` succeeds
- `uv run pytest tests/ -x --timeout 120` confirms no regressions
  </verify>
  <done>brain.duckdb contains version 3 rows with v6 risk_questions for all 388 checks, brain_taxonomy has 45 v6 subsections, all brain tests pass, migration is end-to-end idempotent</done>
</task>

</tasks>

<verification>
- No Q1-Q25 references in enrichment_data.py or brain_checks_current
- brain_taxonomy risk_question count = 45 (from QUESTIONS-FINAL.md)
- brain_changelog documents every remap operation
- All existing brain tests pass with v6 IDs
- Full test suite passes
</verification>

<success_criteria>
The entire brain knowledge base uses v6 taxonomy subsection identifiers (X.Y format: 1.1 through 5.9, 45 entities) from QUESTIONS-FINAL.md. No references to old Q1-Q25 remain in enrichment data or DuckDB current views. brain_loader.py section_map uses v6 section names. Fresh migrations produce v6-ready checks. This is the foundation for all downstream plans.
</success_criteria>

<output>
After completion, create `.planning/phases/32-knowledge-driven-acquisition-analysis-pipeline/32-04-SUMMARY.md`
</output>
