---
phase: 32-knowledge-driven-acquisition-analysis-pipeline
plan: 05
type: execute
wave: 3
depends_on: [32-01, 32-02, 32-03]
files_modified:
  - src/do_uw/knowledge/compat_loader.py
  - src/do_uw/stages/analyze/__init__.py
  - src/do_uw/stages/analyze/check_engine.py
  - src/do_uw/brain/brain_loader.py
  - tests/brain/test_brain_loader.py
  - tests/brain/test_brain_writer.py
  - tests/stages/analyze/test_check_engine_content_type.py
autonomous: true
requirements: [SC-1, SC-4]

must_haves:
  truths:
    - "Pipeline ANALYZE stage loads checks via BrainDBLoader when brain.duckdb exists"
    - "BackwardCompatLoader.load_checks() delegates to BrainDBLoader and returns active checks with enrichment overlay"
    - "MANAGEMENT_DISPLAY checks produce INFO status via evaluate_management_display() — no threshold evaluation"
    - "EVALUATIVE_CHECK evaluation is IDENTICAL to current behavior — zero functional regression"
    - "INFERENCE_PATTERN checks evaluate their individual signal (same as EVALUATIVE_CHECK for now) — pattern composition remains in SCORE"
    - "Check results include _brain_report_section and _brain_risk_questions from DuckDB enrichment"
  artifacts:
    - path: "src/do_uw/stages/analyze/check_engine.py"
      provides: "Content-type-aware dispatch: MANAGEMENT_DISPLAY -> evaluate_management_display(), EVALUATIVE_CHECK -> evaluate_check(), INFERENCE_PATTERN -> evaluate_check() (deferred)"
    - path: "tests/stages/analyze/test_check_engine_content_type.py"
      provides: "Tests: MD check -> INFO status, EC check -> existing behavior, IP check -> existing behavior"
    - path: "tests/brain/test_brain_loader.py"
      provides: "Tests: BrainDBLoader loads active checks, filters retired, overlays enrichment"
  key_links:
    - from: "src/do_uw/stages/analyze/__init__.py"
      to: "src/do_uw/knowledge/compat_loader.py"
      via: "BackwardCompatLoader delegates to BrainDBLoader"
      pattern: "BackwardCompatLoader"
    - from: "src/do_uw/stages/analyze/check_engine.py"
      to: "evaluate_management_display"
      via: "content_type dispatch before threshold evaluation"
      pattern: "content_type.*MANAGEMENT_DISPLAY"
---

<objective>
Wire BrainDBLoader into the pipeline and add content-type-aware evaluation dispatch.

Purpose: BrainDBLoader and BrainWriter already exist (created between Plans 01-02) but aren't fully wired into the pipeline execution path. The pipeline ANALYZE stage still loads checks primarily through BackwardCompatLoader's KnowledgeStore path. This plan ensures the pipeline reads active checks from brain.duckdb with lifecycle filtering and enrichment overlay, and adds content-type dispatch so MANAGEMENT_DISPLAY checks get proper INFO-only evaluation.

Output: Pipeline reads from brain.duckdb, check engine dispatches by content_type, BackwardCompatLoader fully delegates to BrainDBLoader.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/do_uw/brain/brain_loader.py
@src/do_uw/brain/brain_writer.py
@src/do_uw/knowledge/compat_loader.py
@src/do_uw/stages/analyze/__init__.py
@src/do_uw/stages/analyze/check_engine.py
@src/do_uw/stages/analyze/check_evaluators.py
@src/do_uw/stages/analyze/check_helpers.py
@.planning/phases/32-knowledge-driven-acquisition-analysis-pipeline/32-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure BrainDBLoader is the primary check source in the pipeline</name>
  <files>
src/do_uw/knowledge/compat_loader.py
src/do_uw/brain/brain_loader.py
tests/brain/test_brain_loader.py
tests/brain/test_brain_writer.py
  </files>
  <action>
1. **Verify BackwardCompatLoader delegation**: Read `compat_loader.py` to confirm it already delegates to BrainDBLoader when brain.duckdb exists. The `_try_brain_loader()` method should connect and return a BrainDBLoader. Verify that `load_checks()` uses BrainDBLoader's output when available. If the delegation path is already working, no changes needed — just verify with tests.

2. **Add BrainDBLoader tests** (test_brain_loader.py):
   - Test that `load_checks()` returns a dict with "checks" list and "total_checks" count
   - Test that retired checks (lifecycle_state=RETIRED) are excluded from results
   - Test that enrichment fields are overlaid: each check dict should have `_brain_version`, `_brain_lifecycle`, `_brain_report_section`, `_brain_risk_questions`, `_brain_risk_framework_layer`, `_brain_hazards`
   - Test that `load_all()` returns a BrainConfig with checks, scoring, patterns, sectors, red_flags
   - Test that `load_taxonomy()` returns organized taxonomy entries
   - Test that `load_backlog()` returns open backlog items
   - Use in-memory DuckDB for all tests (pass `:memory:` to connect_brain_db)

3. **Add BrainWriter tests** (test_brain_writer.py):
   - Test `insert_check()`: creates version 1 row, changelog entry
   - Test `update_check()`: creates version N+1, preserves unchanged fields, changelog entry
   - Test `retire_check()`: sets lifecycle_state=RETIRED, excluded from brain_checks_active
   - Test `promote_check()`: changes lifecycle_state, creates new version
   - Test `get_changelog()`: returns entries newest-first, filterable by check_id
   - Test `export_json()`: produces valid checks.json format
   - Test duplicate insert raises ValueError

4. **Verify pipeline integration path**: Confirm that `AnalyzeStage.run()` in `stages/analyze/__init__.py` creates a `BackwardCompatLoader(playbook_id=...)` and calls `loader.load_all()`. Trace that this eventually calls BrainDBLoader. If brain.duckdb doesn't exist at runtime, BrainDBLoader auto-migrates (already implemented). Document the full chain in a comment.

Run: `uv run pytest tests/brain/test_brain_loader.py tests/brain/test_brain_writer.py -x -v`
  </action>
  <verify>
- `uv run pytest tests/brain/test_brain_loader.py -x -v` all passing
- `uv run pytest tests/brain/test_brain_writer.py -x -v` all passing
- `uv run pytest tests/ -x --timeout 120` confirms no regressions
  </verify>
  <done>BrainDBLoader is verified as the primary check source with lifecycle filtering and enrichment overlay. BrainWriter operations tested for insert/update/retire/promote/export. Pipeline integration chain confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Add content-type-aware evaluation dispatch in check engine</name>
  <files>
src/do_uw/stages/analyze/check_engine.py
tests/stages/analyze/test_check_engine_content_type.py
  </files>
  <action>
1. **Add evaluate_management_display() to check_engine.py**: After `map_check_data()` returns data for a check, dispatch based on content_type BEFORE calling `evaluate_check()`:

```python
content_type = check.get("content_type", "EVALUATIVE_CHECK")

if content_type == "MANAGEMENT_DISPLAY":
    result = evaluate_management_display(check, data)
elif content_type == "INFERENCE_PATTERN":
    # Phase 32 scope: evaluate individual signal same as evaluative
    # Pattern composition remains in SCORE stage
    result = evaluate_check(check, data)
else:  # EVALUATIVE_CHECK (default)
    result = evaluate_check(check, data)
```

2. **Implement evaluate_management_display()**: Add to check_engine.py (or check_evaluators.py if better fit):
   - Extract first non-None data value from the mapped data dict
   - If data is present: return CheckResult with status=INFO, value=the data, evidence="Management display: {value}"
   - If data is absent/None: return make_skipped(check, data) — same as current skip behavior
   - This function does NOT evaluate thresholds. It verifies data presence only.
   - Use `from do_uw.stages.analyze.check_helpers import extract_factors, make_skipped`
   - Use `from do_uw.stages.analyze.check_results import CheckResult, CheckStatus`

3. **CRITICAL: Do NOT change EVALUATIVE_CHECK evaluation path**: The default path must remain exactly as-is. Content-type dispatch is ADDITIVE. The only functional change is: 64 MANAGEMENT_DISPLAY checks now get INFO instead of going through threshold evaluation (many of which currently SKIP because they have no meaningful thresholds).

4. **Add tests** (test_check_engine_content_type.py):
   - Test MANAGEMENT_DISPLAY check with data present -> INFO status
   - Test MANAGEMENT_DISPLAY check with data absent -> SKIPPED status
   - Test EVALUATIVE_CHECK check -> same behavior as before (threshold evaluation)
   - Test INFERENCE_PATTERN check -> same behavior as EVALUATIVE_CHECK (for now)
   - Test check with no content_type field -> defaults to EVALUATIVE_CHECK behavior
   - Use mock ExtractedData and CompanyProfile fixtures

5. **Where to add the dispatch**: In `execute_checks()` in check_engine.py, within the check iteration loop, AFTER `map_check_data()` and BEFORE the current `evaluate_check()` call. The dispatch wraps the evaluate call, it does not change the mapping step.

Run: `uv run pytest tests/stages/analyze/test_check_engine_content_type.py -x -v`
Run: `uv run pytest tests/stages/analyze/ -x -v` to confirm no regression in existing check evaluation tests
  </action>
  <verify>
- `uv run pytest tests/stages/analyze/test_check_engine_content_type.py -x -v` all passing
- `uv run pytest tests/stages/analyze/ -x --timeout 120` all passing (no regression)
- `uv run pytest tests/ -x --timeout 120` full suite passes
  </verify>
  <done>Check engine dispatches by content_type. MANAGEMENT_DISPLAY checks return INFO when data present, SKIPPED when absent. EVALUATIVE_CHECK path unchanged. INFERENCE_PATTERN delegates to existing evaluate_check(). Full test suite passes.</done>
</task>

</tasks>

<verification>
- BrainDBLoader provides active checks with lifecycle filtering to the pipeline
- BrainWriter tested for all CRUD operations with versioning
- Content-type dispatch works: MD->INFO, EC->threshold, IP->threshold (for now)
- No regression in existing check evaluation
- Full test suite passes
</verification>

<success_criteria>
The pipeline ANALYZE stage reads checks from brain.duckdb via BrainDBLoader with lifecycle filtering (retired checks excluded) and enrichment overlay (brain metadata on every check dict). MANAGEMENT_DISPLAY checks get proper INFO-only evaluation instead of being forced through threshold evaluation. All 381 AUTO checks produce the same or better results than before.
</success_criteria>

<output>
After completion, create `.planning/phases/32-knowledge-driven-acquisition-analysis-pipeline/32-05-SUMMARY.md`
</output>
