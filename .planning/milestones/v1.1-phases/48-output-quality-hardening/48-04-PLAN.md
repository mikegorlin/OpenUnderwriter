---
phase: 48-output-quality-hardening
plan: "04"
type: execute
wave: 3
depends_on: ["48-02", "48-03"]
files_modified:
  - tests/stages/analyze/test_regression_baseline.py
  - output/AAPL/
  - output/RPM/
autonomous: false
requirements: [QA-05]

must_haves:
  truths:
    - "Fresh AAPL pipeline run completes successfully and produces HTML output"
    - "AAPL SKIPPED check count is lower than the v1.0 baseline of 68"
    - "AAPL TRIGGERED check count does not increase versus the v1.0 baseline of 24"
    - "RPM pipeline run completes and produces HTML output without errors"
    - "Human reviewer approves the HTML worksheet quality on AAPL"
    - "test_regression_baseline.py thresholds updated to match post-Phase-47/48 reality"
  artifacts:
    - path: "tests/stages/analyze/test_regression_baseline.py"
      provides: "Updated SKIPPED floor and TRIGGERED ceiling thresholds based on fresh run results"
      contains: "SKIPPED_FLOOR"
    - path: "output/AAPL/"
      provides: "Fresh HTML worksheet reflecting all Phase 48 fixes"
      min_lines: 0
  key_links:
    - from: "do-uw analyze AAPL"
      to: "output/AAPL/{date}/AAPL_worksheet.html"
      via: "full pipeline execution"
      pattern: "AAPL_worksheet.html"
---

<objective>
Run full regression on AAPL and RPM, measure SKIPPED/TRIGGERED counts, update baseline thresholds, and get human approval of the HTML worksheet quality.

Purpose: All three QA fixes are now implemented. This plan validates that they work end-to-end on real data, that the SKIPPED count has actually decreased from the Phase 47 routing fixes, and that the TRIGGERED count has not regressed. Human review confirms the visual output is correct before the phase is marked complete.

Output:
- Fresh AAPL pipeline run with post-Phase-47+48 routing, extraction schema, coerce_value fix, source date fix, and threshold_context display
- Updated test_regression_baseline.py thresholds reflecting the new SKIPPED floor
- Human-approved HTML worksheet quality (threshold_context visible in red flags, source dates visible in QA audit, boolean values showing "True"/"False")
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-output-quality-hardening/48-CONTEXT.md
@.planning/phases/48-output-quality-hardening/48-RESEARCH.md
@.planning/phases/48-output-quality-hardening/48-02-SUMMARY.md
@.planning/phases/48-output-quality-hardening/48-03-SUMMARY.md

# Regression test file
@tests/stages/analyze/test_regression_baseline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run fresh AAPL + RPM pipelines and update regression baseline thresholds</name>
  <files>
    tests/stages/analyze/test_regression_baseline.py
  </files>
  <action>
    **Step 1: Run fresh AAPL pipeline**

    Run the full pipeline for AAPL to get fresh post-Phase-47+48 state. Use the `--force` flag to bypass any cached state from the pre-Phase-47 run:
    ```bash
    uv run do-uw analyze AAPL --force 2>&1 | tail -30
    ```

    If `--force` is not a valid flag, check the CLI help: `uv run do-uw --help`. Use whatever flag bypasses cache.

    After the run, count SKIPPED and TRIGGERED from state.json:
    ```bash
    uv run python -c "
    import json, glob, pathlib
    state_files = sorted(glob.glob('output/AAPL/*/state.json'))
    if state_files:
        state = json.loads(pathlib.Path(state_files[-1]).read_text())
        results = state.get('analysis', {}).get('check_results', {})
        statuses = [v.get('status', '?') for v in results.values()]
        from collections import Counter
        print(Counter(statuses))
    "
    ```

    **Step 2: Run fresh RPM pipeline**

    ```bash
    uv run do-uw analyze RPM --force 2>&1 | tail -20
    ```

    RPM is a secondary regression check — confirm it completes without errors and produces HTML output.

    **Step 3: Update test_regression_baseline.py thresholds**

    Read `tests/stages/analyze/test_regression_baseline.py` to understand the current threshold assertions. Update the `SKIPPED_FLOOR` constant (or equivalent) to the actual post-Phase-47+48 SKIPPED count from the fresh AAPL run.

    The expectation: SKIPPED should now be significantly lower than 68 (the v1.0 baseline):
    - Population B (34 DEF14A checks): should evaluate if LLM extraction worked
    - Population C+D (14 routing gap checks): should evaluate after Phase 47 routing fixes
    - Population A (20 intentionally unmapped): should remain SKIPPED

    If SKIPPED is still close to 68 (less than 10 improvement), investigate why before updating the threshold. Common causes per research:
    - Population B checks still SKIP if LLM extraction did not extract the new DEF14A fields (brain YAML extraction hints may need updating)
    - Population C checks still SKIP if the routing was already fixed in Phase 47 but the evaluator finds no data

    If SKIPPED remains high (>50), add a diagnostic step: inspect state.json to find which specific checks are still SKIPPING and trace the root cause. Document findings in the SUMMARY.

    Do NOT force-pass any check that has no data. If checks remain SKIPPED, the SKIPPED_FLOOR is simply higher than hoped — update the threshold to match reality and document the reason.

    Update thresholds in `test_regression_baseline.py`:
    ```python
    # Post-Phase-47+48: routing fixes + DEF14A expansion should reduce SKIPPED below 68
    SKIPPED_FLOOR = {actual SKIPPED count from fresh run}  # was 68
    TRIGGERED_CEILING = 24  # AAPL is a known-clean company; must not increase
    ```

    After updating thresholds, run the regression test:
    ```bash
    uv run pytest tests/stages/analyze/test_regression_baseline.py -v
    ```

    **Step 4: Run full test suite to confirm clean baseline**
    ```bash
    uv run pytest tests/ -q 2>&1 | tail -5
    ```

    Expected: the Wave 0 tests from Plan 01 should now be GREEN (they test behavior implemented in Plans 02 and 03). Total pass count will exceed 3967. Pre-existing failures remain at ≤2.

    **Step 5: Open AAPL HTML output for human review**

    Open the fresh HTML worksheet in the default browser:
    ```bash
    open output/AAPL/$(ls -t output/AAPL/ | head -1)/AAPL_worksheet.html
    ```
  </action>
  <verify>
    <automated>uv run pytest tests/stages/analyze/test_regression_baseline.py tests/ -q 2>&1 | tail -10</automated>
    <manual>Confirm: (1) AAPL HTML worksheet is open in browser, (2) SKIPPED count is lower than 68, (3) TRIGGERED count is ≤24 (AAPL baseline).</manual>
  </verify>
  <done>Fresh AAPL pipeline run completed. SKIPPED count is lower than 68. TRIGGERED count is ≤24. RPM run completed without errors. test_regression_baseline.py thresholds updated. Full test suite passes. AAPL HTML worksheet is open in browser for review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of HTML worksheet quality</name>
  <files>output/AAPL/</files>
  <action>
    The AAPL HTML worksheet has been opened in the browser by Task 1. All Phase 48 QA fixes are visible:
    - QA-01: QA audit table source column shows "10-K 2024-09-28" format (filing type + date)
    - QA-02: Boolean check values show "True"/"False" instead of "1.00"/"0.00"
    - QA-04: TRIGGERED red flags show threshold criterion text as a muted gray italic line
    - Population A checks have deprecation_note in brain YAML
    - SKIPPED count reduced from 68 baseline
    - TRIGGERED count unchanged (AAPL remains clean)

    Human reviewer examines the QA Audit appendix and Red Flags section in the browser and confirms quality.
  </action>
  <verify>
    <automated>ls output/AAPL/$(ls -t output/AAPL/ | head -1)/AAPL_worksheet.html 2>/dev/null && echo "HTML worksheet exists"</automated>
    <manual>
      1. Navigate to the QA Audit appendix (bottom of document). Verify:
         - Source column shows "10-K YYYY-MM-DD" format for SEC-sourced checks
         - Value column shows "True"/"False" for boolean checks, not "1.00"/"0.00"
         - SKIPPED rows still show "—" in source (not disguised)
      2. Navigate to the Red Flags section. Verify:
         - Each triggered flag has a muted gray italic line below its description showing the threshold criterion
      3. Confirm no obviously incorrect triggered checks appear on AAPL
    </manual>
  </verify>
  <done>Human reviewer has confirmed: source dates visible in QA audit, boolean values correct, threshold_context visible in red flags, no unexpected TRIGGERED checks on AAPL.</done>
  <what-built>
    All Phase 48 QA fixes implemented and verified on fresh AAPL pipeline run. The HTML worksheet shows:
    - Filing type + date in source column ("10-K 2024-09-28" format)
    - "True"/"False" for boolean checks in the value column
    - Muted gray italic threshold criterion below each red flag description
    - Reduced SKIPPED count versus v1.0 baseline of 68
  </what-built>
  <how-to-verify>
    The AAPL HTML worksheet should already be open in your browser from Task 1.

    1. Scroll to the QA Audit appendix (bottom of document). Verify:
       - Source column shows "10-K YYYY-MM-DD" format (not plain "10-K Balance Sheet" or "—")
       - Value column shows "True"/"False" for boolean checks (e.g., GOV.BOARD.ceo_chair_separation)
       - SKIPPED rows still show "—" in source column

    2. Scroll to the Red Flags section. Verify:
       - Each triggered flag has a muted gray italic line below its description
       - The text shows the CRF condition (e.g., "Company has pending securities class action lawsuit")

    3. Confirm SKIPPED count is lower than 68 (visible in QA audit table SKIPPED rows or pipeline log)

    4. Confirm no obviously incorrect triggered checks appear (AAPL should not have new unexpected red flags)
  </how-to-verify>
  <resume-signal>Type "approved" if the HTML worksheet looks correct, or describe any issues found (e.g., "source column still shows dashes for 10-K checks", "threshold context not visible in red flags")</resume-signal>
</task>

</tasks>

<verification>
1. `uv run pytest tests/stages/analyze/test_regression_baseline.py -v` — PASS
2. `uv run pytest tests/ -q 2>&1 | tail -5` — ≥3967 pass, ≤2 pre-existing failures
3. AAPL HTML output exists at `output/AAPL/{date}/AAPL_worksheet.html`
4. Human reviewer approves visual quality of HTML worksheet
</verification>

<success_criteria>
- AAPL SKIPPED count < 68 (the v1.0 baseline)
- AAPL TRIGGERED count ≤ 24 (no false triggers introduced)
- RPM pipeline run completes without Python errors
- test_regression_baseline.py updated with post-Phase-47+48 thresholds and passing
- Human reviewer types "approved" after viewing the HTML worksheet
- All 4 QA requirements (QA-01, QA-02, QA-04, QA-05) are verifiably satisfied in the HTML output
</success_criteria>

<output>
After completion, create `.planning/phases/48-output-quality-hardening/48-04-SUMMARY.md`
</output>
