---
phase: 46-brain-driven-gap-search
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/do_uw/models/state.py
  - src/do_uw/stages/analyze/check_results.py
autonomous: true
requirements:
  - GAP-06

must_haves:
  truths:
    - "AcquiredData has a brain_targeted_search field (dict[str, Any], default empty dict) that serializes cleanly via model_dump()"
    - "CheckResult has a confidence field (str, default 'MEDIUM') visible in model_dump() output so the QA audit template can read check.get('confidence')"
    - "All existing tests pass — no serialization regressions in AcquiredData or CheckResult"
  artifacts:
    - path: "src/do_uw/models/state.py"
      provides: "AcquiredData.brain_targeted_search field"
      contains: "brain_targeted_search"
    - path: "src/do_uw/stages/analyze/check_results.py"
      provides: "CheckResult.confidence field"
      contains: "confidence"
  key_links:
    - from: "src/do_uw/stages/acquire/gap_searcher.py (Plan 03)"
      to: "src/do_uw/models/state.py:AcquiredData.brain_targeted_search"
      via: "acquired.brain_targeted_search[check_id] = gap_result"
      pattern: "brain_targeted_search"
    - from: "src/do_uw/stages/analyze/__init__.py (Plan 04)"
      to: "src/do_uw/stages/analyze/check_results.py:CheckResult.confidence"
      via: "check_result['confidence'] = 'LOW'"
      pattern: "confidence.*LOW"
---

<objective>
Add two data model fields needed by the gap search feature:
1. `AcquiredData.brain_targeted_search` — a `dict[str, Any]` field on the state model where gap search results are persisted per check_id (satisfies GAP-06 requirement that results survive pipeline stages without re-searching)
2. `CheckResult.confidence` — a `str` field with default "MEDIUM" that Plan 04 will set to "LOW" for web-sourced re-evaluations, and which the QA audit template already reads via `check.get('confidence')`

Purpose: These two fields are the data plumbing prerequisites for all other Phase 46 plans. They must exist before the gap searcher (Plan 03) can write to `brain_targeted_search` and before the re-evaluator (Plan 04) can set `confidence=LOW` on re-evaluated checks.

Output: Updated state.py and check_results.py with new fields, all existing tests passing.
</objective>

<execution_context>
@/Users/gorlin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gorlin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-brain-driven-gap-search/46-RESEARCH.md
@src/do_uw/models/state.py
@src/do_uw/stages/analyze/check_results.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add brain_targeted_search to AcquiredData</name>
  <files>
    src/do_uw/models/state.py
  </files>
  <action>
In `src/do_uw/models/state.py`, add a new field to `AcquiredData` after the `company_logo_b64` field (the last existing field, around line 117):

```python
brain_targeted_search: dict[str, Any] = Field(
    default_factory=dict,
    description=(
        "Gap search results keyed by check_id. "
        "Populated by Phase E of ACQUIRE for checks with "
        "data_status_reason='Data mapping not configured'. "
        "Format: {check_id: {query, results_count, keywords_matched, "
        "suggested_status, domain, confidence}}"
    ),
)
```

Follow the exact pattern of the existing `blind_spot_results` and `acquisition_metadata` fields (both `dict[str, Any]`) — do NOT create a typed Pydantic model for gap results, as that would break serialization compatibility per the codebase convention established by those fields.

The `Any` import is already present in state.py (check the existing imports: `from typing import Any`).
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run python -c "
import sys; sys.path.insert(0, 'src')
from do_uw.models.state import AcquiredData
a = AcquiredData()
assert hasattr(a, 'brain_targeted_search'), 'field missing'
assert a.brain_targeted_search == {}, 'default not empty dict'
d = a.model_dump()
assert 'brain_targeted_search' in d, 'not in model_dump'
a.brain_targeted_search['TEST.CHECK'] = {'suggested_status': 'TRIGGERED', 'confidence': 'LOW'}
assert a.brain_targeted_search['TEST.CHECK']['suggested_status'] == 'TRIGGERED'
print('AcquiredData.brain_targeted_search OK')
"
</automated>
  </verify>
  <done>AcquiredData.brain_targeted_search exists as dict[str, Any] with default empty dict, serializes correctly via model_dump(), and can store gap search result dicts keyed by check_id.</done>
</task>

<task type="auto">
  <name>Task 2: Add confidence field to CheckResult, run tests</name>
  <files>
    src/do_uw/stages/analyze/check_results.py
  </files>
  <action>
In `src/do_uw/stages/analyze/check_results.py`, add a `confidence` field to the `CheckResult` Pydantic model. Place it after the `source` field (around line 148):

```python
confidence: str = Field(
    default="MEDIUM",
    description=(
        "Confidence level for this check result: "
        "HIGH (audited/official), MEDIUM (unaudited/estimated), "
        "LOW (web-derived/gap search). "
        "Set to LOW by gap search re-evaluator for WEB (gap) sourced results."
    ),
)
```

The QA audit template `qa_audit.html.j2` already reads `check.get('confidence') or 'MEDIUM'` — adding this field with default "MEDIUM" is backward-compatible: existing check results will serialize with `confidence: "MEDIUM"` which matches the template's current behavior.

After adding the field, run the full test suite to confirm no regressions:
```bash
cd /Users/gorlin/Projects/do-uw/do-uw
uv run pytest tests/ -x -q 2>&1 | tail -20
```

If tests fail, diagnose and fix before marking done. Common failure mode: tests that do `CheckResult(...)` without `confidence` kwarg are fine (it has a default). Tests that assert exact `model_dump()` output may need updating to include `confidence: 'MEDIUM'`.
  </action>
  <verify>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run python -c "
import sys; sys.path.insert(0, 'src')
from do_uw.stages.analyze.check_results import CheckResult, CheckStatus
r = CheckResult(check_id='TEST', check_name='Test', status=CheckStatus.SKIPPED)
assert r.confidence == 'MEDIUM', f'default wrong: {r.confidence}'
d = r.model_dump()
assert 'confidence' in d, 'not in model_dump'
r.confidence = 'LOW'
assert r.confidence == 'LOW'
print('CheckResult.confidence OK')
"
</automated>
    <automated>cd /Users/gorlin/Projects/do-uw/do-uw && uv run pytest tests/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>CheckResult.confidence field exists with default "MEDIUM". All existing tests pass. The QA audit template can now display confidence level for gap-sourced checks (LOW) once Plan 04 populates it.</done>
</task>

</tasks>

<verification>
- AcquiredData.brain_targeted_search is a dict[str, Any] field with default empty dict
- CheckResult.confidence is a str field with default "MEDIUM"
- Both fields appear in model_dump() output
- uv run pytest tests/ -x -q passes with no failures
</verification>

<success_criteria>
After this plan, the two prerequisite data model fields exist. Plan 03 can write gap search results to `acquired.brain_targeted_search[check_id]`. Plan 04 can set `check_result['confidence'] = 'LOW'` on re-evaluated checks.
</success_criteria>

<output>
After completion, create `.planning/phases/46-brain-driven-gap-search/46-02-SUMMARY.md`
</output>
